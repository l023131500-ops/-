# שילוב 2 מערכות לפלטפורמת more30 — kesef + kiosk

> מסמך מחייב לסשן שלא ראה שום דבר מהשיחה. מטרה: להעלות את שתי המערכות לאוויר, עובדות מקצה-לקצה, באיכות הגבוהה ביותר, תחת הדומיין הראשי — **בלי לפגוע בקוד קיים**. שלב א' = לסרוק ולהציע בלבד; אל תבצע כלום עד אישור מפורש שלי.

---

## 0. הקשר (חובה לקרוא)
- more30 = מונו-רפו של ~33 מערכות תחת **דומיין אחד**, ניתוב מבוסס-נתיב: `more30.com/<שם>`. **NetFree חוסם *.vercel.app** — לכן אין תת-דומיינים נפרדים; הכל תחת הדומיין הראשי.
- כניסה אחידה בפלטפורמה: Google SSO + **סופר-אדמין גלובלי** l023131500@gmail.com (גישה לכל הניהול) + חברות משתמש לפי אתר (`core.app_memberships`).
- סודות מרוכזים ב-`core.secrets` ב-Supabase hub (project `uhnrgujbdxhhmoxcjria`).
- כל מערכת: מבנה **אתר תדמית → כניסה/מנוי**, כפתור כניסה בנווט העליון, ומוצגת בדף הבית (כניסה אם חיה, "בקרוב" + משפט שלב אם לא).
- אל תיגע במוגן: `bkalut-app`, `bkalot-admin`, `zr_*`, `NEDARIM3873`.

---

## 1. שתי המערכות

### א) kesef — פלטפורמת שקיפות תקציבית (Next.js + Supabase)
פלטפורמה שמראה לתושב כמה כסף ציבורי מגיע לרשות המקומית שלו, כמה הגיע בפועל, ומה נתקע. פיילוט: חצור הגלילית (סמל 2034), אח"כ 260 רשויות. שלושה מנועים בנויים ובדוקים: מדדים והשוואה לקבוצת שווים; התראות לקולות קוראים + מייל מוכן; "תיק לפגישה עם ראש הרשות" (PDF). מקורות: data.gov.il, מפתח התקציב, הלמ"ס, ביטוח לאומי.
- **סטאק:** Next.js 15 App Router · React 19 · TypeScript strict · Tailwind · shadcn/ui · Supabase (Postgres) · pnpm workspaces · Vitest · Playwright · Recharts · MapLibre.
- **חבילות:** apps/kesef · packages/kesef-db · kesef-ingest · kesef-normalize · kesef-metrics · kesef-ui.
- **קוד מקור:** מצא בעצמך את הריפו/ענף ב-GitHub של המשתמש (הרץ `gh repo list --limit 100` או `gh search repos kesef`, וחפש ריפו עם חבילות kesef-*). אם לא ברור איזה — רשום ב-NEEDS_USER ושאל אותי.

### ב) kiosk — kioskfleet (שרת Node + SQLite + WebSocket, על Railway)
מערכת ניהול צי מכשירי קיוסק עם שליטה מרחוק בזמן-אמת. אומת עובד מקצה-לקצה: אימות JWT, קישור-אירוע → קוד רישום → רישום סוכן → טוקן מכשיר → heartbeat → פקודה מרחוק (WebSocket) → סטטיסטיקות. קונסולה `/console`, בריאות `/api/health`.
- **קוד מקור:** GitHub ענף `claude/what-do-you-see-gxo5tc`, שורש `kiosk/server`.
- **פריסה נוכחית:** Railway — `kioskfleet-production.up.railway.app`.
- **שרת stateful** עם WebSocket ו-SQLite — **לא מתאים ל-serverless/Vercel.**

---

## 2. אסטרטגיית שילוב — שונה לכל מערכת (חשוב)

### kesef → מתמזג למונו-רפו
- מתווסף כאפליקציה `apps/kesef`, נגיש ב-`more30.com/kesef` (ניתוב-נתיב, לא תת-דומיין).
- מסד: Supabase של הפרויקט הקיים אבל **בסכימה נפרדת `kesef`**, לא ב-public.
- env בתחילית `KESEF_*` / `NEXT_PUBLIC_KESEF_*`.

### kiosk → נשאר על Railway, מקושר לפלטפורמה
- **אל תעביר את הקוד ל-Vercel.** הוא נשאר שירות Railway עצמאי.
- חבר **Railway Volume 1GB** ל-`/app/data` לקביעות ה-SQLite (בלי זה כל דיפלוי מוחק נתונים).
- חשוף למשתמשים דרך `more30.com/kiosk` (rewrite/proxy מהדומיין הראשי לשירות Railway) — כדי שמשתמשי NetFree יגיעו בדומיין הנקי, לא ל-*.up.railway.app.
- env בתחילית `KIOSK_*`.
- החלף את סיסמת האדמין הראשונית (`admin`/הסיסמה שנוצרה בלוג) — לא להשאיר ברירת מחדל.

---

## 3. חוקי שילוב מחייבים
### מסד נתונים
- אין לגעת בסכימת public ובטבלאות של אפליקציות קיימות.
- לפני כל מיגרציה: ספור טבלאות בכל סכימה, הרץ, ספור שוב, השווה. אם מספר בסכימה שאינה `kesef` השתנה — עצור ודווח.
- RLS מופעל על כל טבלה. הסכימה `kesef` צריכה להיות ב-Exposed schemas; אם לא — רשום כמטלה ב-NEEDS_USER, אל תעקוף.
- אזהרת נפח: תוכנית FREE (~500MB לכל הפרויקט). טעינת נתונים בהיקף פיילוט בלבד; אל תרחיב בלי לשאול.

### חבילות משותפות
- אם צריך משהו מחבילה משותפת — **הוסף, אל תשנה**. שינוי חתימת פונקציה משותפת אסור בלי אישור. התנגשות גרסאות → דווח, אל תשדרג לבד.

### בנייה ובדיקות
- job נפרד ב-CI לכל מערכת; כישלון שלה לא יחסום אחרות. פורט dev ייעודי. הבדיקות הקיימות חייבות לעבור באותו מספר כמו לפני.

### פריסה + staging
- `NEXT_PUBLIC_KESEF_ENV=staging` → באנר קבוע "גרסת בדיקות — הנתונים אינם מלאים" + robots.txt חוסם אינדוקס. רק ב-production הבאנר נעלם והאינדוקס נפתח. (אותו עיקרון לקיוסק.)

---

## 4. קווים אדומים במוצר kesef — אל תשנה ואל תשבית בדיקות שאוכפות אותם
1. אין נתונים מומצאים / mock שנראה אמיתי. אין נתון → רכיב `<NoData>` בעברית.
2. כל ערך מספרי מקושר למסמך מקור. `source_document_id` הוא NOT NULL — אל תעקוף.
3. שומר-ניסוח בקוד שחוסם מילים מפלילות + בדיקת יחידה שאוכפת.
4. אין מידע אישי; פרטי בעלי תפקידים רק אם פורסמו רשמית, עם מקור.
5. נגישות תקן ישראלי 5568 חוסמת commit (axe-core ב-CI, מפיל על serious ומעלה).
6. פרופיל משק הבית נשמר בדפדפן בלבד, לא נשלח לשרת (בדיקת Playwright מאמתת).

---

## 5. הצגה בדף הבית + כניסה אחידה
- הוסף את שתי המערכות ל-`core.projects` ולדף הבית: **כניסה לאתר החי** אם מוכנה, אחרת **"בקרוב"** + משפט שלב.
- kesef: החל כניסה אחידה מלאה (Google SSO + סופר-אדמין + חברות לפי אתר).
- kiosk: קישור לאתר החי דרך more30.com/kiosk; שילוב SSO מלא אם אפשרי, אחרת קישור + התחברות פנימית קיימת.

---

## 6. סדר העבודה (מחייב — אל תדלג)
0. **אתר את שני הריפואים ב-GitHub בעצמך** (יש לך הרשאת GitHub של המשתמש): kiosk = ענף `claude/what-do-you-see-gxo5tc`, שורש `kiosk/server`; kesef = ריפו עם חבילות `kesef-*`. השתמש ב-`gh repo list --limit 100` / `gh search repos`. אם ריפו לא מזוהה חד-משמעית — רשום ב-NEEDS_USER ושאל, אל תנחש.
1. **סרוק** את המונו-רפו ואת שתי המערכות. דווח: מבנה חבילות, גרסאות תלויות משותפות, פורטים ופריסה, מיקום env.
2. **הצע תוכנית שילוב מפורטת** — קבצים שישתנו, קבצים שיתווספו, סיכוני התנגשות. **אל תבצע כלום לפני שאישרתי.**
3. אחרי אישור: בצע על **ענף נפרד**, בשלבים, commit לכל שלב.
4. אחרי השילוב: הרץ את **כל** הבדיקות של **כל** האפליקציות והראה שהמספרים לא ירדו.
5. פתח **PR. אל תמזג.**
- תעד הכל ב-`INTEGRATION.md`: מה שולב, מה השתנה, מה נשאר פתוח.

*התחל משלב 1 בלבד: סרוק והצג לי תוכנית. אל תבצע כלום עד אישור.*
