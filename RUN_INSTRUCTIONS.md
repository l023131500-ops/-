# RUN_INSTRUCTIONS — הנחיית הריצה (הטרמינל קורא את זה בכל סבב)

בכל הפעלה בצע **צעד משמעותי אחד** ואז commit+push. הסדר:

1. קרא קודם את `more30-priority.md` ובצע לפי הסדר (§1 → §10).
2. אחר כך `more30-fixes-and-features.txt` (או .md), ואם קיימים: `BKALOT_CLONE_BUILD.md` (שכפול בקלות תפעולי — עדיפות גבוהה), `BKALOT_AUTOMATION_BUILD.md`, `MAATEFET_BUILD.md`.
3. עדכן `SYSTEMS_STATUS.md` ו-`NEEDS_USER.md`.

## כללי ברזל
- **נתוני אמת בלבד.** אין נתון → "לא זמין". לעולם לא נתון מומצא.
- **מוגן = לא לכתוב:** מערכות 08 ("בקלות זכאות"), 09 ("בקלות ניהול"), `bkalut-app`, `bkalot-admin`, `zr_*`, `NEDARIM3873` — **אסור לשנות/למחוק/לפרוס/לשבור**. חריג יחיד ומאושר: **מותר לקרוא** את 08/09 קריאה-בלבד לצורך שכפול לפי `BKALOT_CLONE_BUILD.md`. סכמות `csj`/`csj_src`/`igud` — אל תיגע כלל (ארגונים אחרים).
- **מצב טסט בלבד** לשליחה ולחיוב. אין חיוב/הודעה אמיתית בלי אישור מפורש.
- אחרי כל צעד: **insert heartbeat** ל-`core.run_progress` (phase, task, status, note).
- אמת כל שינוי (בדיקה + צילום מסך) לפני commit. אל תעצור.

## מה חשוב עכשיו (עדכני)
- **מחירים:** נקבעו 2/5/10/12/15 ₪ לחודש. 10 (more30/premium), 12 (nadlan/premium), 15 (nadlan/vip) כבר במסד. הוסף מסלולי 2 ₪ ו-5 ₪ על מערכות קלות חיות בלבד. מצב טסט.
- **מסלול חינמי:** Google **או** הרשמה מיידית (שם·טלפון·מייל·סיסמה) → מיד נכנס לתוך המוצר וממשיך פנימה.
- **שכפול בקלות תפעולי (חדש, מאושר):** בנה עותק עצמאי מלא של בקלות התפעולית (אפליקציה+ניהול+מנוע שליחה+מאגר זכויות) לפי `BKALOT_CLONE_BUILD.md`. מותר לקרוא 08/09 קריאה-בלבד לשכפול נאמן; אסור לשנות אותן; מצב טסט לשליחה. (מערכת 10 + rights.catalog כבר מוצגות לא-נעולות.)
