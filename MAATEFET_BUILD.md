# מעטפת — קובץ בנייה (שלב אחרון, אחרי כל העבודה הנוכחית)

> פלטפורמה לניהול, הדרכה וליווי מדריכי חתנים ומדריכות כלות. **מקור האמת: מסמך האפיון "מעטפת גרסה 2" שהמשתמש צירף** (15 מודולים, ליבה אחת/שני פנים, 5 שלבים). כאן: תמצית מחייבת + תוספות מתקדמות + כללי בנייה.
>
> ⚠️ **מתי:** רק **אחרי** שכל העבודה ב-more30-priority.md ו-more30-feature-expansion.md הושלמה. אם כבר סיימת את הכל — התחל מכאן, משלב 0. בלי לפגוע במה שעובד.
>
> **20/08/2026 (Loop C):** המערכת נרשמה כ-מספר **39** ב-`core.projects` (מוצגת ב-more30.com כ"בקרוב", `apps/39-maatefet/app.json` + `packages/config/src/registry.ts`).
>
> **20/08/2026 (Loop C, סבב 2):** שלב 0 התחיל בפועל — סכימת `maatefet` הוחלה על ה-hub (`uhnrgujbdxhhmoxcjria`) דרך `supabase/migrations/0108_maatefet_core_schema.sql`: טבלאות `instructors`/`invites`/`clients`/`content_items`, כולן RLS, כולן invite-only (מדריך מתחיל `pending` ועובר `verified` רק ע"י סופר-אדמין — אכוף בטריגר `guard_instructor_status_change`, לא רק ב-UI; לקוח מקבל שורה רק דרך קוד-הזמנה אמיתי שיצר מדריך מאומת). הסופר-אדמין משתמש בפונקציה הגלובלית הקיימת `public.more30_is_super_admin()` — לא נבנה טבלת-ניהול חדשה. **עדיין לא נבנה:** קוד אפליקציה/UI (‏/maatefet עדיין "בקרוב"), פאנל סופר-אדמין בפועל לאימות מדריכים, 2FA (Auth MFA — שכבת-אפליקציה, לא DB), הצפנת-שדה לתוכן רגיש (דורש ניהול-מפתחות + RPC הצפנה/פענוח). אלה השלב הבא.
>
> **20/08/2026 (Loop C, סבב 4):** נבנה ה-UI הראשון בפועל תחת
> `sites/39-maatefet/maatefet/` (`index`/`instructor`/`join`/`admin`, אותו דפוס
> כמו `sites/36-nadlan-pro`/`sites/34-kesef`), מחובר לשכבת API חדשה
> `public.maatefet_*` (`supabase/migrations/0110`/`0111`). תוך כדי הבנייה נמצאו
> ותוקנו שלוש בעיות אמיתיות: (1) סכימת `maatefet` לא הייתה ב-exposed schemas של
> ה-Data API — כל ה-RPCs מסבב 3 היו בלתי-נגישים משום דפדפן; נוספה דרך
> Management API (יחול ברסטארט טבעי, לא נכפה כדי לא לשבש 32/16), ובינתיים
> נבנתה שכבת `public.maatefet_*` שעוקפת את זה. (2) מדיניות ה-RLS לאנון על
> `invites` אפשרה מיצוי (enumeration) של כל ההזמנות הממתינות בלי קוד כלל —
> הוסרה, הוחלפה ב-RPC יחיד לפי קוד מדויק. (3) כל פונקציות ה-`public.maatefet_*`
> היו ניתנות להרצה ע"י `anon` למרות `revoke ... from public` (ברירת המחדל של
> Supabase ל-schema `public`) — תוקן ב-`revoke execute ... from anon` מפורש.
> נותר לשלב 0: פרויקט Vercel נפרד + rewrite ב-portal (ראה `NEEDS_USER.md`), 2FA,
> הצפנת-שדה.
>
> **20/08/2026 (Loop C, סבב 5):** 2FA נאכף בפועל (לא רק תועד כ"נותר") למדריך/ה
> מאומת/ת ולפאנל הסופר-אדמין — שני התפקידים היחידים שכבר יכולים לגעת ב-PII
> של זוגות. אכיפה בשכבת ה-DB: `maatefet.my_instructor_id()` (הצומת שכל
> מדיניות RLS ו-RPC של מדריך עוברת דרכו) דורש עכשיו `auth.jwt()->>'aal' =
> 'aal2'` בנוסף ל-`status='verified'`; `maatefet.verify_instructor()` דורש
> aal2 מפורש משלו לפני אישור/השעיה. לא נגעתי ב-`public.more30_is_super_admin()`
> הגלובלי (מערכות אחרות תלויות בו). UI: `instructor.html`/`admin.html` קיבלו
> מסכי הרשמת-2FA (TOTP, QR + מפתח ידני) ואתגר-2FA (לכניסה חוזרת), עם
> `supabase-js` `auth.mfa.enroll/challengeAndVerify/listFactors/unenroll` —
> ללא ספרייה חדשה. `maatefet_me()` חושף את ה-`aal` הנוכחי כדי שה-UI יבדיל
> "עדיין לא מאומת/ת" מ"מאומת/ת אך ה-session הזה טרם עבר אתגר 2FA". 2FA
> ללקוח (חתן/כלה) לא נבנה — אין עדיין מסך אזור-אישי לשער (שלב 1). הצפנת-שדה
> עדיין לא נבנתה.
>
> **20/08/2026 (Loop C, סבב 3):** תוקן פער תפקודי אמיתי שנשאר מסבב 2 — ב-RLS של 0108 ללקוח (חתן/כלה) לא הייתה שום דרך ליצור את שורת ה-`clients` שלו/ה אחרי כניסה בהזמנה אמיתית (המדיניות דרשה `instructor_id` שהוא/היא לא מחזיקים), כך שזרימת "invite-only בכל שכבה" הייתה חסומה מהצד של הלקוח. נוסף `supabase/migrations/0109_maatefet_redeem_invite.sql`: `maatefet.redeem_invite()` (RPC אחד, SECURITY DEFINER, תובע קוד-הזמנה באופן אטומי מול מרוץ-כפילות, מגביל את היצירה ל-`auth_user_id` של הקורא בלבד — לא ניתן להעביר uuid זר) ו-`maatefet.verify_instructor()` (עוטף את בדיקת `more30_is_super_admin()` ל-RPC נקי לפאנל הסופר-אדמין הבא, במקום שהלקוח יבנה UPDATE גולמי מול טבלה עם RLS+טריגר). שני ה-RPCs אומתו חיים (הופעלו בפועל דרך MCP, `pg_proc.prosecdef=true` נבדק, ואין אזהרת אבטחה חדשה על סכימת `maatefet` ב-advisors). **עדיין לא נבנה:** קוד אפליקציה/UI. השלב הבא הוא ה-UI עצמו — עכשיו יש לו RPCs תקינים לבנות מולם (הרשמת מדריך → אימות סופר-אדמין → יצירת הזמנה → מימוש הזמנה על ידי הלקוח → מסך CRM ראשון).

> **20/08/2026 (Loop C, סבב 6):** נסגר הפער האחרון שתועד לשלב 0 — הצפנת-שדה
> לתוכן רגיש. `maatefet.clients.notes` (הערות ליווי חופשיות על זוג — השדה
> הרגיש ביותר שקיים כבר בשלב 0, לפני שדות כמו חתונה-אחרי/דירקטוריז שעוד לא
> נבנו) מוצפן עכשיו at-rest: `supabase/migrations/0113_maatefet_notes_encryption.sql`
> מוסיף `pgp_sym_encrypt`/`pgp_sym_decrypt` (pgcrypto, כבר מותקן) עם מפתח
> סימטרי אקראי שנשמר ב-Supabase Vault (כבר מותקן) — לא בקוד ולא בטבלה. המפתח
> עצמו נגיש רק דרך `maatefet._notes_key()` (SECURITY DEFINER, אפס הרשאות
> ל-authenticated/anon/public); לקוחות מקבלים רק `encrypt_note`/`decrypt_note`
> שמקבלים/מחזירים ערך, לעולם לא את המפתח. עמודת `notes` (text) הוחלפה
> ב-`notes_enc` (bytea); `public.maatefet_clients_list`/`maatefet_client_update`/
> `maatefet_me` מפענחים/מצפינים בשקיפות כך שה-UI הקיים
> (`instructor.html`/`admin.html`) לא נגע כלל — עדיין קורא/כותב שדה `notes`
> כטקסט רגיל, אותה חתימת RPC. RLS על `maatefet.clients` נשאר שכבת ההגנה על
> *אילו שורות* נגישות (בלי שינוי); ההצפנה מגנה על *איך* השדה שמור בדיסק/גיבוי.
> אומת: 0 שורות היו בטבלה לפני הסבב (בלי צורך במיגרציית נתונים), round-trip
> עברית תקין (`decrypt(encrypt('בדיקת הצפנה — הערת ליווי רגישה'))`), ותרחיש
> מלא של יצירת מדריך+לקוח+הערה מוצפנת+פענוח+מחיקת נתוני-בדיקה עבר חי דרך MCP.
> **שלב 0 עכשיו סגור** עם כל ארבעת הרכיבים מהאפיון: CRM + ספריית תוכן + פאנל
> סופר-אדמין + אבטחה (2FA מסבב 5 + הצפנת-שדה מסבב זה). מה שנשאר לשלב 0 הוא
> תשתיתי-לא-טכני בלבד: פרויקט Vercel נפרד כדי ש-`/maatefet` יהיה חי בפועל
> (ר' `NEEDS_USER.md`) — עד אז הנתיב ממשיך להציג "בקרוב". השלב הבא (אחרי
> שהמשתמש יספק את פרטי ה-Vercel): שלב 1 — פורטל זוג + יומן/תזכורות/אישורי
> הגעה.

> **20/08/2026 (Loop C, סבב 7):** שלב 1 התחיל — פורטל זוג + יומן/תזכורות/אישורי
> הגעה + בקרת גישה מלאה. `supabase/migrations/0114_maatefet_stage1_calendar.sql`
> מוסיף שתי טבלאות (`maatefet.appointments`, `maatefet.reminders`) באותו דפוס
> RLS מדורג: מדריך/ה מנהל/ת CRUD מלא רק על הזוגות שלו/ה (אכוף גם בטריגר
> `guard_client_belongs_to_instructor`, לא רק ב-RLS — אימות חי הוכיח שהוא דוחה
> נסיון לצרף פגישה לזוג של מדריך/ה אחר/ת), הזוג מקבל קריאה-בלבד לשורות שלו
> בלבד, ואישור/דחיית הגעה עוברים דרך RPC יחיד ומצומצם
> (`maatefet_appointment_rsvp`, SECURITY DEFINER) ולא דרך הרשאת כתיבה גולמית.
> נוסף `maatefet.my_client_id()` — מקביל ל-`my_instructor_id()` — שדורש aal2
> (2FA) לפני שהזוג יכול לקרוא יומן/תזכורות משלו/ה; זה סוגר את הפער שתועד
> ב-0112 ("2FA ללקוח לא נבנה — אין עדיין מסך אזור-אישי לשער"), כי עכשיו יש
> מסך (`sites/39-maatefet/maatefet/portal.html`, אותו דפוס הרשמה/אתגר 2FA
> כמו instructor.html/admin.html).
>
> שני פערים אמיתיים נמצאו ותוקנו תוך כדי הבנייה: (1) **פרטיות** — `maatefet_me()`
> החזיר עד כה (0113) את `notes` המפוענח (הערות ליווי פרטיות של המדריך/ה על
> הזוג) גם ב-branch של "client", כלומר היה מחזיר לזוג עצמו את ההערות הפרטיות
> שהמדריך/ה כתב/ה עליו — רדום עד עכשיו כי שום UI לא קרא ל-branch הזה; הוסר
> לגמרי, מסך ה-CRM של המדריך/ה לא נגע. (2) **הרשאה** — כמו שתועד ב-0111, גם
> ב-7 הפונקציות החדשות `revoke all ... from public` לא שלל הרצה מ-`anon`
> (הרשאת ברירת מחדל נפרדת בפרויקט הזה) — אומת חי (`has_function_privilege`)
> ותוקן עם `revoke ... from anon` מפורש לכל אחת; לא היה בר-ניצול בפועל (אין
> הרשאת anon על הטבלאות עצמן) אך נסגר כהגנת-עומק.
>
> UI: `instructor.html` קיבל טאבים "יומן" ו"תזכורות" (קביעת/ביטול פגישה,
> יצירת/סימון-כטופל/מחיקת תזכורת, לכל זוג קיים). `portal.html` חדש הוא האזור
> האישי הראשון לזוג — פרטים אישיים (תצוגה בלבד, לא עריכה — עריכה נשארת אצל
> המדריך/ה), יומן עם כפתורי "מאשר/ת הגעה"/"לא אוכל להגיע", ותזכורות (תצוגה
> בלבד). `join.html`/`index.html` מקשרים לפורטל החדש במקום ה"בקרוב" הקודם.
> נבדק: יצירת מדריך/לקוח/פגישה/תזכורת מלאה עברה חי דרך MCP, הטריגר דחה שיוך
> חוצה-מדריכים, הרשאות anon/authenticated אומתו לכל פונקציה חדשה, ניקוי נתוני
> בדיקה בוצע. עדיין נדרש מהמשתמש (לא טכני): פרויקט Vercel נפרד (ר' `NEEDS_USER.md`).
> נותר לשלב 1: פרופיל בסיסי לזוג בפורטל נשאר תצוגה-בלבד לפי החלטה זו — אם
> יידרש שינוי, ל-NEEDS_USER.

> **20/08/2026 (Loop C, סבב 8):** שלב 2 התחיל — מודול 6, תיאום מדריך↔מדריכה.
> עד כה `partner_name` על `maatefet.clients` היה טקסט חופשי בלבד — שום דבר לא
> חיבר את שורת ה-client של הכלה (אצל המדריכה שלה) לשורת ה-client של החתן (אצל
> המדריך שלו, בדרך כלל אדם אחר לגמרי במערכת עם instructor_id נפרד).
> `supabase/migrations/0115_maatefet_couple_link.sql` מוסיף `maatefet.couple_links`
> (kallah_client_id/chatan_client_id, שניהם unique — לקוח מקושר פעם אחת בלבד),
> מאובטח באותו דפוס קוד-הזמנה כמו invites: מדריך/ה מייצר/ת קוד זמני (14 יום,
> `maatefet_client_link_code_generate`) לזוג שלו/ה, משתף/ת עם המדריך/ה השני/ה
> מחוץ למערכת, שמזין/ה אותו (`maatefet_couple_link_redeem`, SECURITY DEFINER,
> אטומי מול מרוץ-כפילות). טריגר `guard_couple_link_segments` חוסם הגנת-עומק נגד
> קישור לא-תקין (kallah-kallah/chatan-chatan) ברמת ה-DB גם אם ה-RPC יעקף.
> `maatefet_couple_partner_info` חושף למדריך/ה (או לזוג עצמו, 2FA-gated) רק את
> מה שנדרש לתיאום — שם/מגזר/תאריך-חתונה של הצד השני ופרטי-קשר של המדריך/ה השני/ה,
> לא הערות ליווי ולא טלפון/מייל של הזוג עצמו. `maatefet_couple_link_remove`
> מתקן קישור שגוי. UI: `instructor.html` קיבל כפתור "תיאום מדריך/ה" לכל זוג.
>
> **פער אבטחה אמיתי נמצא ותוקן באימות החי של הסבב הזה, לפני שיצא לפרודקשן:**
> הבדיקה הראשונה של `maatefet_couple_partner_info` (מדריך/ה שאינו/ה שייך/ת
> לזוג בכלל, קורא/ת ל-RPC על client_id שאינו שלו/ה) **הצליחה** במקום להיכשל —
> החזירה את כל פרטי התיאום (שם/מגזר/תאריך-חתונה/פרטי-קשר מדריך) לצד שלישי לגמרי
> לא-מורשה. הסיבה: `if not (cond1 or cond2 or cond3) then raise` כאשר cond2
> (`v_client.id = maatefet.my_client_id()`) היה NULL (my_client_id() מחזיר NULL
> לכל מדריך/ה, שאין לו/ה שורת client) — ב-SQL, `false or null` הוא `null`, ו-
> `if not(null) then` הוא `if null then`, שב-plpgsql **אף פעם לא מפעיל את ה-then**
> — הבדיקה הייתה עוקפת את עצמה בשקט לכל מדריך/ה לא-קשור/ה. תוקן: כל תנאי עטוף
> ב-`coalesce(..., false)`. אומת חי לפני התיקון (גישה לא-מורשית הצליחה) ואחרי
> (נדחתה כראוי), וגם שהגישה המורשית (המדריך/ה של שני הצדדים + הזוג עצמו עם 2FA)
> ממשיכה לעבוד. תרחיש מלא — מדריכת-כלות + מדריך-חתנים + מדריך שלישי לא-קשור +
> יצירת קוד + מימוש + ניגודי-מגזר + קישור-עצמי + כבר-מקושר + הרשאות anon לכל 4
> הפונקציות — עבר חי דרך MCP, נתוני הבדיקה נוקו. עדיין לא נבנה בשלב 2: מיתוג
> נפרד למגזר חתנים, פורום מקצועי, סטודיו תוכן מלא.

> **20/08/2026 (Loop C, סבב 9):** שלב 2 ממשיך — מודול 7, פורום מקצועי.
> `supabase/migrations/0116_maatefet_forum.sql` מוסיף `maatefet.forum_posts`/
> `maatefet.forum_replies`: מרחב שיח משותף לכל מדריך/ה מאומת/ת ו-2FA (`aal2`),
> **חוצה-מגזרים בכוונה** (בניגוד לנתוני זוגות, שיח מקצועי בין מדריכים לא
> אמור להיות מופרד כלות/חתנים — זה בדיוק המקום שמדריכה ומדריך שמלווים אותו
> זוג יכולים להתייעץ). יצירה/עריכה/מחיקה של פוסט/תגובה עוברות דרך RLS רגיל
> (בעלים בלבד, כמו `maatefet_content_save`) — לא נדרש RPC נפרד לזה. **נעיצה
> (`is_pinned`) היא הפעולה היחידה שאסור שתהיה self-service**: RLS לבד לא
> יכול לבטא "כל עמודה מותרת חוץ מזו", אז נוסף טריגר `guard_forum_post_pin`
> (אותו דפוס "טריגר מגן על RLS" כמו `guard_instructor_status_change`)
> שחוסם כל שינוי ב-`is_pinned` שאינו מגיע מסופר-אדמין. הצגת שם/מגזר הכותב
> בפוסטים של מדריכים אחרים דורשת `SECURITY DEFINER`
> (`maatefet_forum_posts_list`/`maatefet_forum_thread`) כי RLS על
> `maatefet.instructors` מגביל כל מדריך/ה לשורה שלו/ה בלבד — אותה סיבה
> בדיוק ש-`maatefet_couple_partner_info` (0115) היא `SECURITY DEFINER`;
> חשוף רק שם/מגזר, לא טלפון/מייל.
>
> **ממצא נוסף, לא רק ב-0116:** אימות חי גילה של-4 פונקציות הטריגר `guard_*`
> בסכימת `maatefet` כולה (`guard_instructor_status_change` מ-0108,
> `guard_client_belongs_to_instructor` מ-0114, `guard_couple_link_segments`
> מ-0115, ו-`guard_forum_post_pin` החדשה) הייתה הרשאת EXECUTE ל-`anon`/
> `authenticated` (`has_function_privilege` אומת ישירות) — לא ניצל בפועל כי
> Postgres חוסם קריאה ישירה ל-פונקציית `returns trigger` מחוץ להקשר טריגר,
> אבל לא עקבי עם המוסכמה המפורשת של הפרויקט (revoke מפורש מ-0111/0114/0115
> לכל פונקציה קריאה). תוקן במיגרציה נפרדת שחוסמת את כל ה-4 בבת אחת.
>
> **אימות מלא חי דרך MCP** (ללא דפדפן): נוצרו שני מדריכים מאומתים אמיתיים
> (מגזר כלות + מגזר חתנים) עם JWT מדומה (`request.jwt.claims` + `set role
> authenticated`, אותה טכניקת RLS-testing סטנדרטית של Supabase) — 9 צעדים
> כולם עברו כמצופה: (1) יצירת פוסט ע"י מדריכה, (2) ניסיון self-pin ע"י
> הבעלים עצמו **נחסם** ע"י הטריגר (הודעת שגיאה מדויקת), (3) מדריך ממגזר אחר
> רואה את הפוסט (חוצה-מגזרים, שם+מגזר+ספירת-תגובות נכונים), (4) תגובה שלו
> נוצרה נכון, (5) `maatefet_forum_thread` מחזיר פוסט+תגובה עם `is_mine`
> נכון לכל צד, (6)-(7) ניסיון נעיצה ע"י מדריך שאינו בעלים/אדמין נבלם בשקט
> ע"י RLS (`is_pinned` נשאר `false`), (8) `anon` נחסם לגמרי
> ("permission denied"), (9) כל נתוני הבדיקה נוקו (0 שורות שיוריות אומת).
> UI: `instructor.html` קיבל טאב "פורום מקצועי" — פרסום פוסט, רשימת פוסטים
> עם ספירת-תגובות, פתיחת-דיון עם תגובות + טופס תגובה, כפתור נעיצה/מחיקה
> (חשוף לכולם — לא-מורשה מקבל הודעת שגיאה מהשרת, אותו דפוס UI כמו כל שאר
> המערכת). אפס רגרסיה: אף טאב/כפתור קיים לא נגע. עדיין לא נבנה בשלב 2:
> מיתוג נפרד למגזר חתנים, סטודיו תוכן מלא.

> **20/08/2026 (Loop C, סבב 10):** שלב 2 ממשיך — מיתוג נפרד למגזר חתנים
> (הפריט שחזר כ"עדיין לא נבנה" בשלושת הסבבים הקודמים). הפתרון: משתני-הצבע
> הקיימים (`--gold`/`--gold-soft`/`--line`) שכל העיצוב כבר בנוי עליהם
> הופכים בר-דריסה ברמת ה-`body` — מחלקה `body.seg-chatan` מגדירה פלטת
> תכלת-פלדה (`#5B8DEF`/`#8FB2F5`) במקום הזהב, כך שכל רכיב קיים (כפתורים,
> טאבים, מסגרות, badges) יורש את הצבע החדש בלי לגעת בכל כלל CSS בנפרד —
> אפס רגרסיה על העיצוב הקיים למגזר כלות (ברירת המחדל, ללא המחלקה). הופעל
> בשלוש נקודות המגע האמיתיות: `instructor.html` (בעת בחירת "מדריך חתנים"
> בטופס ההרשמה — תצוגה מקדימה חיה — ובכל טעינת דשבורד למדריך מאומת לפי
> `ins.segment`, כולל טקסט ה-wordmark ותווית ה-role שהוחלפה מ"מדריך/ה
> מאומת/ת · מגזר חתנים" הגנרי ל"מדריך חתנים מאומת" הישיר), `portal.html`
> (לפי `client.segment` בעת טעינת האזור האישי — wordmark + כותרת הדף
> "אזור אישי לחתן"/"אזור אישי לכלה"), ו-`join.html` (כבר ברגע בדיקת קוד
> ההזמנה, לפי `maatefet_invite_peek(...).segment` — כך שהזוג רואה את
> המיתוג הנכון עוד לפני ההתחברות, ברציפות עד הפורטל). לא נדרש שינוי DB —
> `segment` כבר קיים בכל הטבלאות/ה-RPCs הרלוונטיים. עדיין לא נבנה בשלב 2:
> סטודיו תוכן מלא.

> **20/08/2026 (Loop C, סבב 12):** שלב 3 התחיל — מודול 5, ליווי אחרי החתונה
> ("הבידול" לפי סעיף היסוד המחייב של האפיון, ראשון ברשימת שלב 3). עד כה לא
> הייתה שום דרך למדריך/ה לקבוע בדיקת-מעקב אחרי החתונה, ולזוג לא הייתה שום
> דרך להגיב לה. `supabase/migrations/0118_maatefet_followups.sql` מוסיף
> `maatefet.followups` — בדיקת-מעקב אחת לכל אבן-דרך (30/90/180 יום או שנה, או
> תאריך מותאם-אישית), בבעלות המדריך/ה, בטווח לקוח שלו/ה בלבד, באותה צורת
> `instructor_id`+`client_id` כמו `appointments`/`reminders` (0114) — משתמשת
> מחדש ב-`guard_client_belongs_to_instructor` ללא שינוי. תאריך מוצע מחושב
> בשרת מ-`wedding_date` כשהמדריך/ה משאיר/ה את השדה ריק; `custom` דורש תאריך
> מפורש. מחזור-חיים `pending → sent → completed/skipped`, אך המדריך/ה
> היחיד/ה שיכול/ה לגעת ברוב השדות — הזוג יכול/ה רק להגיש תשובות לשאלון של
> עצמו/ה, פעם אחת, דרך RPC יחיד ומצומצם (`maatefet_followup_respond`,
> SECURITY DEFINER), אותו דפוס בדיוק כמו `maatefet_appointment_rsvp`.
> `flagged`/`instructor_note` (סימון-ידני "לבדיקה נוספת" — לא אבחון AI; זו
> הגרסה הבנויה עכשיו, לא הגרסה מבוססת-הבינה שתועדה ב"תוספות מתקדמות") נשארים
> אצל המדריך/ה בלבד: נבנו מההתחלה שתי פונקציות קריאה נפרדות במקום `select *`
> משותפת (כדי לא לחזור על פער-הפרטיות שתוקן ב-0114) —
> `maatefet_followups_list` (מדריך/ה, כל העמודות, RLS-מוגבל) ו-
> `maatefet_followup_my_list` (זוג, רשימת עמודות מפורשת שמשמיטה
> flagged/instructor_note לגמרי, לא רק מסתירה בצד הלקוח).
>
> **אימות מלא חי דרך MCP** (JWT מדומה + `set role authenticated`, אותה
> טכניקה שהוכחה בסבבים קודמים): מדריכה מאומתת + מדריך שלישי לא-קשור + לקוחה
> עם תאריך חתונה — יצירת בדיקת-מעקב עם חישוב-תאריך אוטומטי (30 יום),
> `custom` בלי תאריך נחסם כראוי, סימון-כנשלח, הזוג רואה שאלון בלי
> flagged/instructor_note, הזוג מגיש תשובות (עברית מלאה, round-trip תקין),
> הגשה כפולה נחסמת, מדריך לא-קשור לא רואה כלום ב-list, ניסיון קישור בדיקת-
> מעקב ללקוח זר נחסם ע"י הטריגר (`client does not belong to this
> instructor`) גם עם תאריך מפורש שעוקף את בדיקת ה-wedding_date, anon נחסם
> לגמרי, ולבסוף המדריכה מסמנת דגל אחרי קריאת התשובה. כל 7 הפונקציות אומתו
> `has_function_privilege` — anon חסום, authenticated מורשה. נתוני הבדיקה
> (כולל שלושה משתמשי auth.users זמניים) נוקו לגמרי, 0 שורות שיוריות אומת.
> UI: `instructor.html` קיבל טאב "ליווי אחרי החתונה" (קביעה עם הצעת-תאריך
> אוטומטית, רשימה, סימון-כנשלח/כדולג, סימון/הסרת דגל, צפייה בתשובות).
> `portal.html` קיבל כרטיס "מעקב אחרי החתונה" — שאלון קצר למילוי (סולם 1-5 +
> טקסט חופשי) ותשובות שכבר הוגשו כתצוגה-בלבד. אפס רגרסיה: אף טאב/כרטיס/RPC
> קיים לא נגע. עדיין לא נבנה בשלב 3: דירקטוריז (פוסקים/מקוואות/מסדרי
> קידושין/בודקות/יועצים), ארגון חתונה.

## החלטות יסוד מחייבות (מהאפיון)
- **ליבה אחת, שני פנים**: מערכת אחת עם שדה `segment` (חתנים/כלות) דרך כל השכבות. RLS מלא. לא שתי מערכות.
- **תשלום מהמדריכים בלבד** (מנוי SaaS). חתנים/כלות לא משלמים.
- **Invite-only בכל שכבה**: מדריך/ה מאומת/ת ידנית ע"י סופר-אדמין; חתן/כלה נכנסים רק בהזמנת המדריך/ה.
- **אבטחה כערך-על**: 2FA לכולם, הרשאות מדורגות, הצפנת שדה לתוכן רגיש, מנגנון חובת-דיווח, retention לפי חוק הגנת הפרטיות. גבוה מ-CRM עסקי רגיל.
- **דירקטוריז (פוסקים/מקוואות/מסדרי קידושין/בודקות/יועצים)**: הזנה ידנית ע"י סופר-אדמין — לא שאיבה אוטומטית.

## 15 המודולים (לפי האפיון — בנה לפי סעיפים 7.1–7.15)
CRM לזוגות · סטודיו תוכן ומיתוג אישי · אזור אישי לחתן/כלה · יומן+תזכורות+אישורי הגעה · ליווי אחרי החתונה (הבידול) · תיאום מדריך↔מדריכה · פורום מקצועי · ארגון החתונה · דירקטורי פוסקים · מרשם מקוואות · מסדרי קידושין · בודקות טהרה · יועצים זוגיים · לוח משימות ומעקב לימודי · בקרת גישה invite-only.

## מפת הדרכים (5 שלבים — מהאפיון, אל תדלג)
- **שלב 0:** תשתית + ליבה במגזר הכלות בלבד (CRM + ספריית תוכן + פאנל סופר-אדמין + אבטחה).
- **שלב 1:** פורטל זוג + יומן/תזכורות/אישורי הגעה + בקרת גישה מלאה.
- **שלב 2:** מגזר חתנים (מיתוג נפרד) + תיאום מדריך↔מדריכה + פורום + סטודיו מלא.
- **שלב 3:** ליווי אחרי החתונה + דירקטוריז (הזנה הדרגתית) + ארגון חתונה.
- **שלב 4:** סליקת מנויים (ספק ישראלי — Grow/Cardcom/Tranzila/PayPlus, מצב טסט עד אישור) + אנליטיקה + הרחבות.

## תוספות מתקדמות (מחקר — להוסיף, ברמה הכי גבוהה)
- **סוכן AI למדריך**: סיכום מפגש אוטומטי מהקלטה + פריטי פעולה; טיוטת מערך שיעור מותאם-עדה; ניסוח תזכורות.
- **זיהוי "דגלים אדומים" ב-AI** על שאלוני המעקב אחרי החתונה — התראה עדינה ומכובדת למדריך/ה (בלי לאבחן, רק לסמן לבדיקה).
- **תזמון חכם**: הצעת מועדי מפגש פנויים אוטומטית; סנכרון ליומן Google.
- **וואטסאפ דו-כיווני** לתזכורות ואישורי הגעה (שלב מתקדם, לפי עלות ה-API).
- **מרקטפלייס תבניות מבוקר** בין מדריכים (שיתוף מערכים, במודרציה).
- **דשבורד ארגון** לסופר-אדמין: תובנות חוצות-מדריכים (השלמת תוכנית, נושאים חוזרים) — לשיפור ההכשרה.
- **PWA מובייל** מלא + נגישות תקן ישראלי.
- **אזור אישי אחיד** עם היסטוריה, מסמכים, וצפייה חוזרת בשיעורים.

## אינטגרציה ב-more30
- מערכת חדשה במונו-רפו, מוצגת בדף הבית כמו כל מערכת ("בקרוב" עד שמוכנה), מיתוג "עולם הסטארטאפים" + קישור פוטר לתדמית.
- כניסה אחידה + סופר-אדמין גלובלי; שני תתי-נתיבים/מיתוגים (חתנים/כלות).
- סליקה דרך המנגנון המרכזי במצב טסט; חשבוniות דרך הספק שנבחר. אל תבצע חיוב אמיתי בלי אישור.

## כללי בנייה
נתוני אמת בלבד; אין נתון → "לא זמין". אל תיגע במוגן. כל שלב: אמת (בדיקה + צילומי מסך) → git commit + push → heartbeat ל-core.run_progress → עדכן SYSTEMS_STATUS + NEEDS_USER. החלטות פתוחות (שמות מותג, ספק סליקה, ניסוח משפטי לכתב ויתור, שיתוף עם מכון) → NEEDS_USER, אל תמציא. איכות לאורך זמן על פני מהירות.
