# נדל"ן — תוספות לגרסה הבאה (רשום שלא יישכח)

> לביצוע שלב-אחרי-שלב בהמשך, אחרי העדיפויות הנוכחיות. **VIP בלבד. נתוני אמת ומדויקים בלבד — אם לא ודאי, "לא זמין" + הסבר. לעולם לא להמציא.**

## 1. נתוני עירייה — גודל נכס לארנונה + חריגות בנייה
- לשלוף מהעירייה את **גודל הדירה/הנכס לצורך חיוב ארנונה** (מ"ר רשום).
- לבדוק אם ניתן לזהות **תוספת/חריגת בנייה לא חוקית** — השוואה בין המ"ר בהיתר/רישום לבין המצב בפועל.
- הערה: כיסוי משתנה בין רשויות; חלק מהנתונים חשופים ב-GIS עירוני, חלק לא. לבדוק היתכנות עיר-עיר. רק אם הנתון אמיתי.

## 2. התחדשות עירונית — לאמת דיוק
- לפי בדיקות המשתמש, ייתכן שנתוני ההתחדשות העירונית **לא מדויקים**. לאמת מול המקור הרשמי (מבא"ת / הרשות להתחדשות עירונית / תוכניות), ולתקן. אם לא ודאי — לסמן במפורש.

## 3. רישוי זמין — חובה לשלוף
- לשלוף נתונים מ**רישוי זמין** (מערכת הרישוי הארצית). אם הגישה מוגבלת/סגורה לציבור — לתעד מה כן זמין ומה דורש ספק/הרשאה.

## 4. תשריטים והיתרים — הצגה + ניתוח (VIP)
- להציג תשריטים ודוחות היתרים, **ולצידם הסבר וניתוח של התוכנית** (מה מותר, זכויות, מגבלות) — בשפה ברורה.
- רק אם המסמך והניתוח **אמיתיים ומדויקים**. אחרת — קישור למקור הרשמי + "המסמך זמין להורדה רשמית".

## עיקרון על
כל התוספות האלה נכנסות רק כשיש נתון אמת מאומת, ורק ברמת VIP. בלי פדיחות, בלי הערכות שמוצגות כוודאות.
