# מערכת ניהול מתווכים ויועצי נדל"ן — קובץ בנייה מלא (more30)

> קובץ מחייב לטרמינל. מטרה: לבנות את **מערכת הניהול המקצועית** (B2B) של פלטפורמת הנדל"ן — CRM + עסקאות + חשבוניות חוקיות + חוזים וחתימה + הכל במקום אחד. עבודה יסודית, נתוני אמת, אפס פדיחות. **בנה על ענף נפרד, PR — אל תמזג בלי אישור. אל תיגע בקוד/זרימות קיימות של מערכות אחרות.**

---

## 0 · הקשר וכללי ברזל (קרא קודם)
- זו השכבה המקצועית (B2B) של הנדל"ן ב-more30. משתמשת באותו **מנוע אמת** של הדוח הצרכני (nadlan.gov.il, הלמ"ס, GovMap, רמ"י, היתרים) — אל תשכפל מנוע, חבר אליו.
- כניסה אחידה: Google SSO + סופר-אדמין גלובלי (l023131500@gmail.com) + משתמש/משרד לפי אתר (`core.app_memberships`). כפתור כניסה בנווט העליון. מבנה: אתר תדמית → כניסה/מנוי.
- סודות מרוכזים ב-`core.secrets` (Supabase hub `uhnrgujbdxhhmoxcjria`). אל תעתיק מפתחות לפרויקטים.
- מסד בסכימה נפרדת (`crm` או `nadlan_pro`), RLS על כל טבלה, לא ב-public. אל תיגע במוגן: bkalut-app, bkalot-admin, zr_*, NEDARIM3873.
- **נתוני אמת בלבד.** אין נתון → "לא זמין". לעולם אל תמציא לקוח/עסקה/מספר.
- כל שלב: בצע → אמת (Playwright + צילומי מסך) → commit + push → עדכן STATUS + PROGRESS. מלל/תמונה שנטפרי חוסם → דלג + BLOCKED.md.

---

## 1 · החזון: הכל במקום אחד
מתווך/יועץ/שמאי מנהל את כל העסק ממסך אחד: לידים → לקוחות → נכסים → הצגות → הצעות → חוזה → חתימה → חשבונית → עמלה → מעקב אחרי לקוח. בלי לצאת לחמישה כלים. השילוב עם מנוע האמת (דוח נכס מדויק) הוא הבידול — אף CRM ישראלי לא נותן את זה.

---

## 2 · מודולים ופיצ'רים (מה לבנות — לפי מובילי השוק + התאמה לישראל)

### 2.1 CRM ולידים (הליבה)
- ניהול אנשי קשר: קונים, מוכרים, שוכרים, משכירים — עם היסטוריה מלאה.
- **צינור (Pipeline) חזותי**: ליד → יצירת קשר → פגישה → הצעה → חוזה → סגירה. גרירה בין שלבים.
- מקורות ליד: טופס אתר, נחיתה מהדוח הצרכני, ידני, ייבוא. תיוג מקור לכל ליד (למדידת CAC).
- **אוטומציה**: תזכורות מעקב אוטומטיות, המשך שלא נענה, ניתוב ליד למתווך פנוי.
- משימות ולוח שנה: תזכורות, פגישות הצגה, סנכרון ל-Google Calendar.
- הערות, קבצים ותיעוד שיחה לכל לקוח.

### 2.2 ניהול נכסים ומצאי
- כרטיס נכס עשיר: מחובר ל**דוח האמת** (שווי, עסקאות, גיל, היתרים, רקע שכונה).
- סטטוס: פעיל / בהצעה / נמכר / הוקפא. תמונות, מסמכים, בלעדיות + תוקף בלעדיות.
- **התאמת קונה-נכס אוטומטית**: כשנכנס ליד קונה עם קריטריונים — התראה על נכסים תואמים במלאי.

### 2.3 עסקאות (Transaction Management)
- ניהול עסקה מקצה לקצה: צדדים, נכס, מסמכים, אבני דרך, סטטוס.
- **צ'קליסט עסקה**: כל שלב עד סגירה (זיכרון דברים → חוזה → חתימה → תשלום → עמלה).
- **מעקב עמלות**: חישוב עמלה, פיצול בין מתווכים/משרד, סטטוס גבייה.

### 2.4 חשבוניות — חוקי לפי "חשבוניות ישראל" (סעיף 3 המלא)
### 2.5 חוזים + חתימה אלקטרונית (סעיף 4 המלא)

### 2.6 שיווק ופרסום
- פרסום נכס לפורטלים/רשתות ישירות מהמערכת (כשיתאפשר API), עמוד נכס ממותג לשיתוף.
- אוטומציית שיווק: ניוזלטר לקונים לפי קריטריונים (דרך Resend), טיפול בלידים.
- דפי נחיתה לנכס עם טופס ליד שמזין את ה-CRM.

### 2.7 דוחות וניתוח לעסק
- לוח מחוונים: לידים, המרות, עסקאות, הכנסה, עמלות, מקורות ליד מנצחים.
- דוח ביצועי מתווך/משרד, יעדים מול ביצוע.

### 2.8 מובייל
- רספונסיבי מלא + PWA. מתווך בשטח: גישה ללקוח, לנכס, לחוזה ולחתימה מהנייד.

### 2.9 עמידה רגולטורית (אתיקה 2024)
- גילויי מתווך + מספר רישיון מובנים בכל הצעה/חוזה. שמירת הסכם תיווך חתום.

---

## 3 · מודול חשבוניות — חוקי, לפי "חשבוניות ישראל" ⚠️ קריטי

**הרקע החוקי (מאומת, אוגוסט 2026):** רשות המסים דורשת **מספר הקצאה** לכל חשבונית מס מעל סף (לפני מע"מ). הסף יורד בהדרגה: מאי 2024 = 25,000 ₪ · ינואר 2025 = 20,000 ₪ · ינואר 2026 = 10,000 ₪ · **יוני 2026 = 5,000 ₪ (הסף הנוכחי)**. חשבונית מעל הסף בלי מספר הקצאה — המע"מ לא מוכר לקונה. עמלת תיווך על דירה כמעט תמיד מעל הסף → **חובה לתמוך במספר הקצאה.**

**החלטת ארכיטקטורה מחייבת:** **אל תבנה חיבור ישיר לרשות המסים בעצמך** (דורש הסמכה, אבטחה, token שמתחדש כל 3 חודשים, תחזוקה כבדה). במקום זה — **התממשק לספק חשבוניות מורשה קיים שכבר מטפל במספר הקצאה**: iCount / Green Invoice / Morning (חשבונית ירוקה). כולם עם API. זו הדרך החוקית, המהירה והנכונה.

**מה לבנות:**
1. שכבת הפשטה `InvoiceProvider` עם מימוש ראשון לספק אחד (למשל iCount API) — יצירת חשבונית/קבלה, המערכת של הספק דואגת למספר הקצאה מול רשות המסים.
2. יצירת חשבונית מתוך העסקה בלחיצה: מושך צדדים, סכום עמלה, פרטי לקוח.
3. אחסון מספר החשבונית, מספר ההקצאה (אם הוחזר), ה-PDF, וקישור ללקוח/עסקה.
4. מפתחות ה-API של הספק ב-`core.secrets` בלבד.
5. אם אין חיבור ספק פעיל → כפתור חשבונית מציג "נדרש חיבור ספק חשבוניות" ומפנה להגדרות. **לעולם אל תפיק מסמך שנראה כמו חשבונית מס בלי מספר הקצאה חוקי.**

**קו אדום:** אל תייצר "חשבונית מס" מזויפת/עצמאית. או ספק מורשה עם מספר הקצאה — או שאין חשבונית מס (אפשר "דרישת תשלום" לא-רשמית שברור שאינה חשבונית מס).

---

## 4 · מודול חוזים + חתימה אלקטרונית — חוקי

**הרקע החוקי (חוק חתימה אלקטרונית, תשס"א-2001):** שלוש רמות — חתימה אלקטרונית (בסיסית), חתימה אלקטרונית **מאובטחת**, וחתימה אלקטרונית **מאושרת** (עם תעודה מגורם מאשר — התוקף המשפטי החזק ביותר). לרוב מסמכי התיווך חתימה מאובטחת עם תיעוד מספיקה; לחוזים כבדים מומלץ ספק חתימה מוסמך.

**מה לבנות:**
1. **תבניות חוזה/הסכם**: הסכם תיווך, זיכרון דברים, ייפוי כוח — עם שדות דינמיים (צדדים, נכס, עמלה, תאריכים) שנשאבים מהעסקה.
2. **זרימת חתימה**: שליחת מסמך לחתימה במייל (Resend) → הצד חותם דיגיטלית → שמירת עותק חתום + חותמת זמן + לוג ראיות (IP, זמן, זהות).
3. **חתימה מאובטחת מובנית** לשלב ראשון (חתימה על קנבס/הקלדה + audit trail). לחוזים מחייבים — **שכבת הפשטה `SignatureProvider`** שתאפשר בעתיד חיבור לספק מוסמך (Comsign/דומה) לחתימה מאושרת.
4. אחסון מאובטח של מסמכים חתומים, קישור קבוע לכל מסמך בכרטיס העסקה/לקוח.
5. עמידה: כל חוזה כולל גילויי מתווך + מספר רישיון (אתיקה 2024).

**קו אדום:** אל תציג "חתימה מאושרת" אם היא רק חתימה בסיסית. תאר את רמת החתימה בכנות. שמור audit trail לכל חתימה.

---

## 5 · ארכיטקטורת "הכל במקום אחד"
- מסד אחד (סכימה `crm`/`nadlan_pro`) עם ישויות: contacts, properties, deals, activities, documents, invoices, signatures, commissions, memberships.
- כל ליד/לקוח/נכס/עסקה/מסמך/חשבונית מקושרים — מלקוח רואים הכל: נכסים, עסקאות, מסמכים חתומים, חשבוניות, תקשורת.
- מנוע האמת של הדוח משמש בכרטיס הנכס — לא כפילות.
- הרשאות לפי תפקיד: מתווך רואה את שלו, מנהל משרד רואה צוות, סופר-אדמין רואה הכל.

---

## 6 · שלבי ביצוע (מחייב — לפי הסדר, לא לדלג)
**שלב 0 — סריקה ותכנון:** סרוק את המונו-רפו והנדל"ן הקיים. דווח: איפה מנוע האמת, מבנה כניסה אחידה, איפה להוסיף את סכימת ה-CRM. הצע תוכנית קבצים (חדש/משתנה) — **אל תבצע לפני שאישרתי.**
**שלב 1 — תשתית + CRM ליבה:** סכימה `crm` + RLS, ישויות contacts/properties/deals/activities, כניסה אחידה, Pipeline חזותי, כרטיס לקוח/נכס. אמת מקצה לקצה.
**שלב 2 — עסקאות + עמלות:** ניהול עסקה, צ'קליסט, חישוב ומעקב עמלות. חיבור כרטיס הנכס למנוע האמת.
**שלב 3 — חשבוניות חוקיות:** שכבת `InvoiceProvider` + מימוש ספק אחד (iCount/Green Invoice), הפקה מהעסקה, אחסון + קישור. בדיקת סף ומספר הקצאה.
**שלב 4 — חוזים + חתימה:** תבניות, זרימת חתימה מאובטחת + audit trail, אחסון מסמכים חתומים, שכבת `SignatureProvider`.
**שלב 5 — שיווק + דוחות + מובייל:** דפי נחיתה/לידים, אוטומציית מייל (Resend), לוח מחוונים, PWA.
**שלב 6 — אתר תדמית + דף הבית:** תדמית שיווקי למערכת המקצועית, הצגה בדף הבית more30 ("כניסה"/"בקרוב"), תמחור שקוף מדורג.

לכל שלב: Playwright + צילומי מסך + נתוני אמת → commit + push → STATUS/PROGRESS → PR (אל תמזג). משימה שדורשת אותי (מפתח ספק/החלטה) → NEEDS_USER.md.

---

## 7 · אימות — חובה
לכל מודול: זרימת משתמש מלאה עם צילומי מסך. ליד→עסקה→חוזה→חתימה→חשבונית→עמלה עובד מקצה לקצה על נתוני בדיקה אמיתיים. חשבונית לא מופקת בלי מספר הקצאה. חתימה שומרת audit trail. אין נתון אמת → "לא זמין".

---

## 8 · הערות חוקיות (לא ייעוץ משפטי)
מספר הקצאה וחוק חתימה אלקטרונית מבוססים על מקורות רשמיים (רשות המסים; חוק תשס"א-2001). לפני עלייה לאוויר עם הפקת חשבוניות מס וחוזים מחייבים — מומלץ אישור רו"ח/עו"ד. המערכת בנויה נכון (ספק מורשה, audit trail) כדי שהאישור יהיה פורמלי בלבד.
