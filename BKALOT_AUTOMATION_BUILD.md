# אוטומציית בקלות — מסמך בנייה עצמאי (בלי תלות ב-n8n של המתכנתת)

> מסמך מחייב לטרמינל. בונים גרסה **עצמאית משלנו** של כל האוטומציה, שהכל מנוהל מהניהול של כל אתר ומהסופר-אדמין. נתוני אמת בלבד. אל תיגע במוגן: bkalut-app, bkalot-admin, zr_*, NEDARIM3873.
>
> ⚠️ **מצב בטוח ללילה:** בנה ובדוק הכל ב**מצב טסט** — אל תשלח מייל/הקלטה/קמפיין אמיתי ללקוח אמיתי, ואל תבצע חיוב נדרים אמיתי, בלי אישור סופי ממני. שלח רק ליעדי בדיקה. תוכן מנתוני אמת — אבל השליחה בפועל רק לטסט.

## 0. מה כבר קיים (לנצל, לא לבנות מחדש)
- מנוע קול: `apps/27/server/yemot.ts` — ElevenLabs → mp3 → שלוחה בימות.
- מאגר זכויות: `rights.catalog` (~800 שורות) לפי קטגוריות.
- תור שליחה קיים. **בנה גרסה עצמאית משלנו** (טבלה + worker) כדי שלא נהיה תלויים ב-n8n חיצוני.

## 1. מערכת חדשה: אוטומציית תוכן (עצמאית)
- מערכת/מודול חדש במונו-רפו — שלנו, לא ה-n8n של המתכנתת.
- מנוהלת מ**הניהול של כל אתר** + **סופר-אדמין**. אין צורך להיכנס ולהגדיר בכל מערכת בנפרד.

## 2. ערוצי כניסה (טריגרים)
1. **טופס באתר** — לקוח ממלא, בוחר נושא/קטגוריה → נוצר ליד + מופעלת שליחה.
2. **נדרים (webhook עצמאי)** — בנה מקלט callback משלנו. חבר מפתחות כשאתן (מוסד+סיסמה+API). עד אז — סקלטון + NEEDS_USER.
3. **ימות (קול)** — לקוח מתקשר, מקיש מספר נושא → שליחה לשלוחה האישית שלו.
4. **סוכן AI** — יושב במייל ובמערכת הקולית, מזהה מה הלקוח ביקש, ומפעיל את השליחה הנכונה.

## 3. מקורות תוכן
- `rights.catalog` — מידע זכויות לפי קטגוריה/מצב אישי.
- השוואת מחירים (סופרים, זמן אמת).
- השוואת קופות חולים.

## 4. יצירת תוכן
- **מייל**: תבנית HTML מעוצבת לפי מותג האתר, עם התוכן שהתבקש.
- **קול**: טקסט → ElevenLabs (המנוע הקיים) → mp3 → מוטמע בשלוחה האישית של הלקוח בימות. איכות הקלטה גבוהה.

## 5. ערוצי שליחה (מצב טסט בלילה)
- **מייל** (Resend) — מעוצב.
- **הקלטה** לשלוחה האישית של הלקוח בימות + **צינתוק** להודיע לו.
- **קמפיין** — שליחה מרוכזת לפי סגמנט.

## 6. מודל "מספר נושא" (כמו שביקשת)
- באדמין: מגדירים **נושאים לפי מספר** — לכל נושא: טקסט + קטגוריה מ-rights.catalog + הקלטה.
- לקוח מבקש נושא N (בטופס/בהקשה/בשיחה) → אותו נושא נשלח אליו: מייל + הקלטה לשלוחה האישית, או בקמפיין.
- כפתורים באדמין לכל נושא: **שליחה · עדכון טקסט · חידוש הקלטה · תזמון קמפיין**.

## 7. ניהול (כל אתר + סופר-אדמין)
- לכל אתר: ניהול נושאים, תבניות, שלוחות, קמפיינים, רשימת לקוחות, ויומן שליחות.
- סופר-אדמין: תצוגה ושליטה חוצת-אתרים.
- כל הפעולות בכפתורים מסודרים — בלי להיכנס להגדרות בכל מערכת בנפרד.

## 8. עצמאות ומפתחות
- תור/תזמון/ניסיונות-חוזרים — משלנו (טבלת `outbound_queue` + worker), לא תלוי בחיצוני.
- כל המפתחות מ-core.secrets (Yemot, ElevenLabs, Resend, Nedarim). אם חסר מפתח → NEEDS_USER.

## 9. סוכן AI
- סוכן חכם שקורא בקשות (מייל/קול/טופס), מזהה מה הלקוח רוצה, שולף את התוכן הנכון, ומפעיל את השליחה. מבצע פעולות לכל אתר, ומתעד הכל ביומן.

## 10. שלבי ביצוע
1. מודל נתונים: topics, templates, extensions, campaigns, leads, delivery_log, outbound_queue.
2. יצירת תוכן: מנוע תבניות מייל + קול (ElevenLabs→mp3→ימות).
3. ערוצי כניסה: מקלט טופס, מטפל הקשות ימות, מקלט webhook נדרים (סקלטון), קליטת סוכן AI.
4. שליחה (טסט): Resend, העלאה לשלוחת ימות + צינתוק, שולח קמפיין — כולם למצב טסט.
5. אדמין (כל אתר + סופר-אדמין): ניהול נושאים, כפתורים (שליחה/עדכון/חידוש), ניהול לקוחות, יומן.
6. חיווט סוכן AI.
7. אימות מקצה-לקצה ב**נתוני בדיקה** + צילומי מסך.

## 11. מה נשאר לי (NEEDS_USER)
- מפתחות נדרים (מוסד+סיסמה+API+iframe+תיעוד) לחיבור ה-webhook האמיתי.
- אישור סופי לשלוח בפועל ללקוחות אמיתיים ולחייב אמיתי.
- מספרי הנושאים/תכנים הסופיים אם תרצה להגדיר מראש.

## אימות
כל ערוץ (טופס/ימות/נדרים/AI) → תוכן מעוצב → מייל + הקלטה + קמפיין, עובד מקצה-לקצה בטסט, מנוהל מהאדמין בכפתורים. צילומי מסך. שליחה אמיתית וחיוב אמיתי — רק אחרי אישורי.
