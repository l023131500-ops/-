# tooling/new-site

Generator for a new numbered app. Creates `apps/<NN>-<slug>/app.json` and prints
the follow-up steps (register in `packages/config` + `supabase/seed`).

```bash
node tooling/new-site/index.mjs 32 my-new-app torah
```
