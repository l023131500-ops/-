# EVENTS_CAPTURE — לכידה שיטתית של מערכת האירועים הקיימת (הנדסה לאחור לרישוי-עצמי)

> מטרה: למפות **את כל** המערכת שהמשתמש מזמין/בעליה ויש לו גישת ניהול אליה, כדי לבנות ממנה מערכת עצמאית ומשופרת (EVENTS_BUILD.md). המשתמש מאשר — זו המערכת שלו, גישה כדין.
>
> ⚠️ **אבטחה:** הפרטים (משתמש/סיסמה) מוזנים **מקומית בלבד** (env/prompt), **לא נשמרים ולא נדחפים ל-git**. בכל הקבצים הנשמרים — **הסתר טוקנים, סיסמאות, ומידע אישי (PII)**.

## דרישות
- Playwright (כבר מותקן). דפדפן אמיתי עם התחברות.
- כתובת התחברות + פרטים לכל תפקיד (המשתמש נותן מקומית).

## שלבי הלכידה (לא לפספס כלום)
1. **התחבר לכל תפקיד בנפרד:** מנהל-על, בעל אולם, בעל אירוע, משתמש קצה (נותן מתנה). סשן נפרד לכל אחד.
2. **סריקה שיטתית ומלאה:** ספֵּר כל פריט תפריט, כל טאב, כל עמוד, **כל כפתור/פעולה/מודל/טופס**. החזק visited-set; עקוב אחרי כל קישור/טאב/חלון קופץ עד שאין חדש. אל תפספס אף מסך.
3. **לכל מסך:** צילום מסך מלא (ממוספר) + שמירת ה-HTML/DOM המרונדר + רישום ה-route/URL.
4. **הקלטת רשת (ה-API — הכי חשוב):** תפוס **כל** XHR/fetch — method, URL, כותרות (הסתר טוקנים), גוף הבקשה, גוף התשובה. זה משטח ה-API המלא.
5. **נכסי פרונט:** שמור את חבילות ה-JS/CSS כפי שנשלחות (גם אם מכווצות).

## פלטים מובנים (לייצר)
- `capture/SITEMAP.md` — כל route/מסך לכל תפקיד.
- `capture/screens/` — צילומים ממוספרים + DOM לכל מסך.
- `capture/API_INVENTORY.md` — קטלוג כל endpoint: method, path, מטרה, דוגמת בקשה/תשובה.
- `capture/DATA_MODEL.md` — ישויות ושדות שהוסקו מה-API והמסכים (מבנה הנתונים).
- `capture/INTEGRATIONS.md` — נדרים, פיימי, ארנקים/עמלות, חשבוניות — איך נקראים, אילו endpoints.
- `capture/FLOWS.md` — הזרימות המרכזיות (אולם→ליד→אירוע→הזמנה→אישור הגעה→מתנה→חלוקת כספים).

## גבולות (כנות)
- **backend source לא ניתן להשגה** — משוחזר מה-API שנתפס.
- פרונט עשוי להיות מכווץ — משתמשים בו למבנה/זרימה, לא כהעתקה מילולית.

## אחרי הלכידה
- מיזוג עם **ההסבר של המשתמש** (איך זה בנוי + ה-API) → משלים פערים.
- ואז בונים לפי `EVENTS_BUILD.md` מערכת חדשה, נקייה ומשופרת, במונו-רפו — שהמשתמש הבעלים שלה.

## כללים
נתוני אמת בלבד. אל תיגע במוגן. אל תדחוף סודות/PII. תעד ב-capture/. מצב טסט לכל סליקה בבנייה.
