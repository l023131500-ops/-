# KIOSK_BUILD — מפרט בנייה מלא ל"יצרנית קיוסקים" (מערכת 35, KioskFleet)

> ⭐ **המערכת הכי חשובה. איכות ללא פשרות בכלל.** מוצר שיוצג בחנויות ואולמות ולרכישה — **חייב להיות ללא באגים**, יציב, ומקצועי. כל פיצ'ר: אמת בדיקה מלאה (כולל על מכשיר אמיתי כשאפשר) לפני commit.
>
> מקור אמת: המפרט הזה + מה שכבר בנוי במערכת 35. **בנה מעל הקיים — אל תשבור אותו.**

---

## 0. עקרונות איכות (מחייבים)
- **Retail-grade:** אפס קריסות, אפס מסכי לבן/שגיאה גלויים ללקוח, התאוששות אוטומטית מכל תקלה (watchdog).
- **גם אונליין וגם אופליין** — הנעילה נאכפת מקומית ומחזיקה בלי אינטרנט. סנכרון/ניהול קורים כשיש רשת.
- **חד-משמעי ללקוח:** הלקוח בוחר, לוחץ כפתור, ומקבל מוצר עובד — **בלי לדעת איך הקוד עובד**.
- **כל שינוי נבדק** על מכשיר/אמולטור לפני שחרור. תיעוד ב-`KIOSK_METHOD.md`.

---

## 1. מה כבר בנוי (הבסיס — לבנות מעליו)
מערכת 35 "KioskFleet" (live, beta, more30.com/kiosk) כבר עושה:
- נעילת מכשיר אנדרואיד ל**רשימת דומיינים מאושרים** בלבד; אכיפה על המכשיר (מחזיק אופליין).
- **ספריית קישורים**, חזרה אוטומטית לכתובת הראשית.
- **פקודות מרחוק בזמן אמת לפי מספר סידורי**: אתחול, רענון, כיבוי מסך, החלפת כתובת.

השדרוג: להפוך את זה ל**יצרנית קיוסקים מלאה** — ריבוי מסלולי נעילה, ריבוי סוגי מכשירים (אנדרואיד גנרי/GMS/Windows), התקנה בלחיצת כפתור, תשלום, תצוגה מתקדמת, ומצב USB-אופליין מוחלט.

---

## 2. חזון המוצר — הזרימה שהלקוח חווה
בלוח הניהול של הלקוח:
1. **בחר מכשיר** (מהצי שלו / הוסף חדש) — סוג ומידה.
2. **בחר סוג קיוסק:** דפדפן נעול / אפליקציה בודדת / תצוגה (signage) / תשלום.
3. **בחר רמת הגבלה:** רכה (חזרה אוטומטית) / קשיחה (נעילה מלאה, בלי מערכת) / מוחלטת (offline USB).
4. **הגדר קישורים** (ראה §4): קישור ראשי + קישור מדויק + קישורים נוספים מותרים.
5. **הגדר תצוגה** (ראה §5): אורך/רוחב, זום, גודל.
6. **לחיצה על "צור קיוסק"** → המערכת מפיקה את **הפקודה/החבילה הנכונה** לאותו מכשיר, ומציגה **הוראת התקנה מותאמת** (ADB / QR / Windows / USB).
7. הלקוח מבצע את שלב ההתקנה החד-פעמי → המכשיר מופיע בצי, נעול ומנוהל.

**כל היצירה קורית בלחיצת כפתור** — הלקוח לא כותב קוד ולא פקודות. המערכת בונה את הפקודה עבורו.

---

## 2★ זרימת הלקוח המדויקת — **מחייב, גובר על כל השאר** (עודכן לפי בקשת הבעלים)
זו חוויית‑הליבה של המוצר. לבנות בדיוק כך:

### א. בממשק הלקוח (הקונסולה שלנו באתר) — הגדרת מכשיר
לכל מכשיר, מסך הגדרה עם **שני שדות ברורים, זה מתחת לזה**:
1. **"אתר ראשי"** — כאן מכניסים את הקישור ש**נכון לכל המכשירים** (ברירת המחדל / השלד שנועל את המכשיר). זהו האתר שהמכשיר ננעל עליו.
2. **"קישור שיוצג על המכשיר"** — הקישור הספציפי שיוצג במסך של אותו מכשיר.
- כפתור **שמור (Save)** → המערכת יוצרת ללקוח **קישור התקנה** + כפתור **הפעל (Activate)**.

### ב. כפתור "הפעל" → אשף התקנה עם צ'קליסט חי
לחיצה על **הפעל** פותחת רשימת **השלבים שנשארו**, כאשר:
- לכל שלב יש **תיבת סימון** שהלקוח מסמן אם בוצע (✓).
- לכל שלב כתוב **מה בדיוק ללחוץ, באיזה כפתור לאשר, ומה אמור להופיע** על המסך אחרי הלחיצה.
- הכל חד‑משמעי, מסודר, והכי **מהיר** להגיע לתוצאה.
- בסוף הצ'קליסט → **מערכת הקיוסק מופעלת ומנטרלת את הכל**; מרגע זה האתר שלנו שולט על המכשיר (המכשיר "יוצא" מהאתר הרגיל ונכנס למצב קיוסק).

### ג. האפליקציה = גם אתר וגם אפליקציית אנדרואיד (APK) ששולטת במכשיר
- בונים את האתר שלנו **גם כאפליקציית אנדרואיד** (APK/launcher) — היא זו שנועלת ושולטת במכשיר (Device Owner + Lock Task Mode).
- הזרימה: אדם נכנס לאתר → **כלקוח מוריד את האפליקציה** → מקבל **הוראות ברורות והכי מהירות** → לוחץ **כפתור אחד שמתקין את מה שנדרש** → נפתחת לו **ספריית הקישורים** → הוא בוחר קישור **או** מזין **מזהה לקוח** → **המכשיר ננעל אוטומטית על אותו לקוח**.
- מתוך המערכת עצמה אפשר להיכנס ולהפעיל את התוכנה (self‑service מלא).
- הקישור שהלקוח מכניס = מה שמוצג במסך. האפליקציה **לא נותנת גישה לכלום** מלבד המערכת שלנו (אתר/אפליקציה) והקישורים שהלקוח בחר לעצמו.

### ד. מודל "מזהה לקוח" — שני מפלסים (בעל‑עסק → הלקוחות שלו)
- ה**לקוח שלנו** (בעל העסק) יכול **לרשום במערכת את הלקוחות שלו**: לכל אחד **מזהה לקוח** + **קישורים ישירים** + **אתר תדמית** משלו.
- על המכשיר: כשמזינים **מספר מזהה לקוח** → **קופץ אתר התדמית הספציפי של אותו לקוח**, וה**תצוגה חייבת להיות זהה בדיוק** לאותו לקוח (מיתוג/פריסה זהים).
- דוגמה: לבעל‑עסק יש אתר עם חנויות שונות וקישורים. בדף שלו יש "אתר ראשי" שכבר נועל את המכשיר; ברגע שמזינים מזהה לקוח — קופץ אתר התדמית של אותו לקוח. במערכת הוא פשוט רושם לקוחות + קישורים ישירים לכל אחד.

### ה. נעילת יציאה בזמן ריצה (קריטי)
- אחרי הפעלת התוכנה, המשתמש **לא יכול לצאת** מהאפליקציה **ללא רשות הניהול**.
- מותר לו **רק** לחזור ל**מסך הכניסה/בחירה** כדי:
  - לבחור **מספר מזהה לקוח אחר** — **רק** מתוך אלה ש**אישרנו בניהול לאותו מכשיר**.
  - לבחור **קישור אחר** מתוך **הרשימות שאושרו לאותו מכשיר**.
- יציאה מלאה מהאפליקציה / גישה להגדרות המכשיר = **רק עם קוד+סיסמת ניהול** (המחוות המדורגות מ‑§4). בלי זה — אין יציאה.

### ו. סיכום התנהגות
מסך כניסה על המכשיר → בוחר מזהה‑לקוח/קישור מהרשימה המאושרת → נפתח אתר התדמית הזהה של אותו לקוח, נעול → יציאה מותרת רק בחזרה למסך הבחירה (בין המאושרים) → יציאה אמיתית רק בקוד ניהול.

### ז. צורות API להכללה (מתוך תיעוד המתכנת של הלקוח — כמקור מבנה; לבנות אצלנו במונו‑רפו)
> הערה: אצל המתכנת זה ספציפי לאולמות/מתנות ויושב על סופאבייס אחר (`xadihaigjkbvphzphxxk`). אנחנו **מכלילים** לכל לקוח/קישור ובונים ב‑hub שלנו. ה‑webhook של נדרים שייך למערכת האירועים (#38), לא לקיוסק הכללי.
- **`IdentifyDevice`** (public‑api): קלט `serial_number` **או** `client_id` (אצלו `hall_id`) → פלט: פרופיל המכשיר/לקוח + ההקשר הפעיל + **קישורים מוכנים** (`display_url`/`kiosk_url` וכל קישור ייעודי). זהו מנוע הזיהוי שמַזין את מסך המכשיר.
- **`/kiosk-launcher/:code`** (או `/k/:code`): הלקוח (בעל‑העסק) מקבל **קוד גישה ייחודי (6 תווים)** שמוצג בקונסולת הניהול. במכשיר נכנסים ל‑launcher, מקלידים קוד → רואים את **כל הפריטים המאושרים** (מזהי‑לקוח/קישורים) → בחירה פותחת את הקיוסק הנעול. זהו המימוש הקונקרטי של "מסך הבחירה" מ‑§2★ד/ה.
- **קוד גישה למכשיר:** לכל מכשיר/לקוח קוד קצר לשיוך מהיר (כמו zero‑touch רך) — משלים את מסלול B (ADB) לשיוך נוח כשיש רשת.

---

## 3. ארכיטקטורה — ארבעה מסלולים, ממשק אחד
המוצר תומך בכל סוגי המכשירים דרך **מנוע מדיניות אחיד** (Policy) + **מתאם לכל פלטפורמה** (Adapter). הלקוח לא רואה את ההבדל — רק בוחר מכשיר, והמערכת בוחרת מתאם.

### מסלול A — ניהול מנוהל (Android עם GMS + גוגל)
- למכשירים עם Google Play מאושר: **Android Management API / Zero-touch / QR**.
- מתאים למכשירים איכותיים; רישום קל (QR או zero-touch).
- מנוהל בענן; פחות מתאים לאופליין מוחלט.

### מסלול B — DPC עצמאי (Android גנרי/סיני בלי GMS) ⭐ העיקרי לצי שלך
- **אפליקציית Device Owner משלנו** מותקנת דרך **ADB** (`dpm set-device-owner`), אוכפת Lock Task Mode מקומית, ננעלת מחדש בכל הדלקה (BOOT_COMPLETED), עובדת **לגמרי אופליין**.
- זה המסלול לצי המיובא מסין.

### מסלול C — Windows (מחשבים רגילים / מסכי 21"+)
- **Assigned Access** (קיוסק אפליקציה-בודדת) או **Shell Launcher v2** (החלפת ה-shell) לנעילה מלאה.
- מתאים למסכים גדולים באולמות/חנויות.

### מסלול D — USB אופליין מוחלט
- חבילת התקנה על **USB** (ראה §6) — בלי אינטרנט כלל. הכל נצרב מקומית.

---

## 4. מנוע הנעילה והקישורים (הלב — לפי הרעיון של נדרים פלוס)
מודל "קישור אחד לכל לקוח, הכל על מערכת אחת" — בדיוק כמו בית כנסת אחד = קישור אחד בנדרים פלוס.

- **קישור ראשי (main):** כתובת הבית של הקיוסק.
- **קישור מדויק (deep link):** לנעול לעמוד/טופס ספציפי **שלא ניתן לנווט ממנו** לאתר הראשי (למשל טופס תרומה בלבד). הקיוסק חוסם ניווט מחוץ לנתיב שהוגדר.
- **קישורים נוספים מותרים (allowlist):** מעתיקים רשימת כתובות שמותר לפתוח; כל השאר חסום. אחרי העתקת הרשימה → **הפעלת הקיוסק**.
- **חזרה אוטומטית:** אחרי X שניות חוסר פעילות → חזרה לקישור הראשי/המדויק.
- **מחווֹת יציאה מדורגות (מוגדרות מראש):**
  - **הקשה מורכבת** (למשל 5 הקשות בפינה + החזקה) → יציאה מהקישור הספציפי לאתר הראשי.
  - **קוד + סיסמה נוספים** → מעבר לניהול / הגדרת קישור אחר. **זו סיסמת הניהול האמיתית של המכשיר.**
  - הכל **ניתן להגדרה** בלוח (כמה הקשות, איזו פינה, אורך החזקה, קודים).
- **הכל נשמר מקומית** כך שהחסימות והמחוות עובדות **אופליין**.

---

## 5. הגדרות תצוגה (חשוב — זום ואוריינטציה)
- **הגדלת מסך (זום) בהגדרות:** חלק מהאתרים בנויים לפלאפון ונראים קטן על מסך 21". חובה **סליידר זום/scale** בלוח שמגדיל את התצוגה במכשיר (CSS zoom / WebView initial-scale / force-density), כך שאתר-מובייל ימלא מסך גדול יפה.
- **בחירת אוריינטציה:** אורך / רוחב — נכפה על המכשיר (Device Owner יכול לכפות אוריינטציה).
- **"אותו מקום בכל מכשיר":** פריסה ב-dp + ConstraintLayout + תמיכה בצפיפויות; פרופילי פריסה לפי גודל (טלפון/7"/10"/21"+) שנבחרים אוטומטית.
- הכל **לכל מכשיר בנפרד** — נשמר בפרופיל המכשיר.

---

## 6. מצב אופליין / USB-only מוחלט
- **חבילת USB:** קובץ התקנה + מדיניות + הקישורים המותרים, נצרבים על USB. מריצים על המכשיר בלי אינטרנט (או דרך ADB ממחשב בלי רשת).
- הקיוסק פועל, נועל, ומאכף **בלי שום חיבור**. עדכון = USB חדש או חיבור זמני לרשת.
- זה הפתרון ל"אין אינטרנט קבוע".

---

## 7. תשלום ואמצעי קלט (3 אופציות, ללא שמירת מספר כרטיס)
הלקוח בוחר אחת. **שום מספר כרטיס (PAN) לא נשמר באתר/בקיוסק באף מצב:**
1. **הקלדה ידנית מלאה** בשדה/iframe המאובטח של ספק הסליקה (פיימי/PayMe, נדרים וכו').
2. **קורא שמזין רק את 16 הספרות** לשדה + הלקוח משלים ידנית תוקף+CVV (opt-in לפי בחירת הלקוח; חוסך הקלדה; לא נשמר). הצג הערה: מומלץ לוודא מול ספק הסליקה שהאופציה מאושרת.
3. **מסופון מוסמך ומצפין (EMV, semi-integrated)** לכרטיס פיזי — מצפין בראש הקריאה, מדבר ישירות עם הסליקה, מחזיר טוקן+אישור בלבד.
- הקיוסק נועל לטופס התשלום המאובטח בלבד (deep-link). **מצב טסט עד אישור.**
- אמצעי קלט נוספים: סורק ברקוד/QR, מקלדת על-מסך, מדפסת קבלות.

---

## 8. ניהול צי מתקדם
- **דשבורד צי לכל לקוח:** סטטוס חי/אופליין לכל מכשיר, סוללה, גרסה, כתובת נוכחית, זמן פעילות אחרון.
- **פקודות מרחוק:** אתחול, רענון, כיבוי מסך, החלפת כתובת, נעילה/שחרור, צילום מסך מרחוק, עדכון מדיניות — לפי מספר סידורי (כבר קיים, להרחיב).
- **Watchdog:** אם האפליקציה קורסת/נסגרת → מופעלת מחדש אוטומטית; אם המסך תקוע → אתחול.
- **עדכון מרחוק (OTA)** של מדיניות ושל האפליקציה כשיש רשת.
- **התראות:** מכשיר אופליין מעל X, סוללה נמוכה, ניסיון יציאה מהקיוסק.
- **קבוצות/תבניות:** להחיל מדיניות על קבוצת מכשירים בבת אחת.

---

## 9. פיצ'רים מתקדמים נוספים (מהמחקר — להוסיף לרמת מוצר מסחרי)
- **תזמון:** נעילה/פתיחה/כיבוי לפי שעות (שעות פעילות אולם/חנות).
- **מצב תצוגה (Digital Signage):** רוטציית תוכן/מדיה כשאין אינטראקציה.
- **מיתוג לקוח:** מסך פתיחה, לוגו, צבעים לכל לקוח.
- **ניקוי סשן:** מחיקת היסטוריה/עוגיות בין משתמשים (קריטי לקיוסק ציבורי).
- **חסימת הורדות/קבצים/הגדרות/סרגלים** בדפדפן הנעול.
- **אנליטיקה:** כמה שימושים, זמן ממוצע, קישורים פופולריים.
- **מצב תחזוקה** מרחוק, **גיבוי/שחזור** מדיניות, **יומן אירועים** לכל מכשיר.
- **QR התחברות מהיר** להוספת מכשיר (למסלול A).

---

## 10. הוראות התקנה — לכל סוג מכשיר (מוצגות אוטומטית לפי הבחירה)
המערכת מציגה ללקוח **בדיוק** את השלבים לפי המכשיר שבחר:

### Android גנרי/סיני (מסלול B) — דרך מחשב + USB
1. במכשיר: הפעל "אפשרויות מפתח" → "ניפוי USB".
2. חבר למחשב עם ADB (המערכת נותנת קישור הורדה + כפתור "העתק פקודה").
3. הדבק והרץ את הפקודה שהמערכת יצרה (device-owner + מדיניות).
4. סיום — המכשיר ננעל ומופיע בצי.

### Android עם GMS (מסלול A) — QR / Zero-touch
1. איפוס יצרן (או מכשיר חדש).
2. הקשה 6× על המסך → סורק QR.
3. סרוק את ה-QR שהמערכת מציגה. (או Zero-touch: פשוט הדלק.)

### Windows (מסלול C) — מסכים גדולים
1. הורד את חבילת הקיוסק מהמערכת.
2. הרץ כמנהל → בוחר Assigned Access (אפליקציה בודדת) או Shell Launcher (נעילה מלאה).
3. סיום — המחשב נכנס לקיוסק בהפעלה.

### USB אופליין מוחלט (מסלול D)
1. המערכת מייצרת חבילת USB (מוריד קובץ).
2. צריבה ל-USB.
3. חיבור למכשיר והרצה — בלי אינטרנט כלל.

---

## 11. פעולות שנדרשות על המכשיר עצמו (האמת המלאה)
- **תמיד יש הפעלה חד-פעמית פר מכשיר** (איפוס/USB debugging/הרצת פקודה או QR). זו הגנת אנדרואיד — אי אפשר לעקוף בתוכנה.
- אחרי ההפעלה החד-פעמית — **הכל אוטומטי ומרחוק**, כולל אופליין.
- **בדיקת דגם:** חלק מקושחות סין חוסמות device-owner. **בדוק דגם אחד לפני קנייה בכמות**; אם חוסם — דגם אחר/image מותאם.

---

## 12. דרישות איכות ובדיקות (חובה לפני שיווק)
- בדיקה על **מכשיר אמיתי** לכל מסלול (אנדרואיד גנרי, GMS, Windows).
- בדיקת אופליין: לנתק רשת ולוודא שהנעילה מחזיקה, המחוות עובדות, החזרה עובדת.
- בדיקת יציאה: לוודא שאי אפשר לצאת בלי המחווה/הקוד; ושהמחווה+קוד עובדים.
- בדיקת התאוששות: לקרוס בכוונה ולוודא watchdog מרים.
- בדיקת זום/אוריינטציה על מסך גדול (21"+) עם אתר-מובייל.
- **אפס באגים גלויים ללקוח.** כל תרחיש שירות מתועד ונבדק.

---

## 13. קישורים מדויקים (למימוש)
- Lock Task Mode: https://developer.android.com/work/dpc/dedicated-devices/lock-task-mode
- Dedicated devices: https://developer.android.com/work/dpc/dedicated-devices
- Device control: https://developer.android.com/work/dpc/device-management
- Android Management API (מסלול A): https://developers.google.com/android/management
- Zero-touch: https://support.google.com/work/android/answer/7514005
- ADB Platform Tools: https://developer.android.com/tools/releases/platform-tools
- Headwind MDM (קוד פתוח, self-hosted): https://h-mdm.com/kiosk-mode/
- דוגמת COSU: https://github.com/NikhilBhople/COSU_ANDROID_KIOSK_MODE
- Windows Assigned Access: https://learn.microsoft.com/en-us/windows/configuration/assigned-access/
- Windows Shell Launcher: https://learn.microsoft.com/en-us/windows/configuration/kiosk/

## 14. מה דורש אותך (החלטות — ל-NEEDS_USER)
- בחירת ספק סליקה (פיימי/נדרים/אחר) ומסופון מוסמך.
- דגם/י מכשיר סיני לבדיקה מול device-owner.
- (נסגר: בונים גם A וגם B; תשלום ב-3 אופציות ללא שמירת PAN.)

---

## כללי בנייה
נתוני אמת בלבד. מצב טסט לתשלום עד אישור. אל תיגע במוגן. כל שלב: בדיקה + צילום מסך → commit+push → heartbeat → עדכן SYSTEMS_STATUS ו-NEEDS_USER. **איכות לפני מהירות. אפס פשרות.**
