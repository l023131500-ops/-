-- 0046 — לוח המערכות הציע כניסה לניהול של #27, ואין כתובת תחת ההרכבה שמגיעה אליה
--
-- §3 מבקש מלוח הניהול-על קישור "נהל מערכת זו" לכל מערכת. 0024 קבעה את השער
-- (רק admin_auth של own/hub ורק כתובת שאפשר ללחוץ עליה), 0025 העבירה את הכתובת
-- מ-core.projects.admin_url אל הדוח, ו-admin-entry-resolves.mjs (07/08/2026)
-- פתח את שלוש-עשרה הכתובות מול הייצור. שתים-עשרה הגיעו למסך שמבקש להזדהות או
-- שנבדל משורש המערכת. אחת לא: https://more30.com/mechiron/admin.
--
-- המדידה ההיא אמרה שהקישור שגוי. היא לא אמרה אם קיים קישור נכון.
-- scripts/qa/mechiron-admin-address.mjs פתח את ארבע המועמדות מול הייצור וענה:
--
--   /mechiron/          "בקלות — הזכויות וההטבות שמגיעות לכם"   מסך בלי שער
--   /mechiron/admin     אותו מסך בדיוק                          מסך בלי שער
--   /mechiron/#/admin   "404 Page Not Found"                     לא נמצא
--   /mechiron/#/login   "404 Page Not Found"                     לא נמצא
--
--   אפס מתוך ארבע מגיעות למסך שמבקש מאיש להזדהות.
--
-- המקור מסביר את שתי התוצאות, apps/27-bkalut-price/client/src/App.tsx:
--
--   שורה 211  <Router hook={useHashLocation}> — האפליקציה מנתבת על ה-hash ולא
--             על הנתיב. "/admin" כנתיב אינו מסלול כלל: הראוטר קורא hash ריק,
--             מכריע "/", ומצייר את דף הנחיתה הציבורי. הכתובת הרשומה שגויה
--             בצורתה.
--   שורה 198  לאזור הפנימי נכנסים רק כאשר isAdminSubdomain() אמת, והיא קוראת
--             window.location.hostname.startsWith("admin."). תחת
--             more30.com/mechiron שם המארח הוא more30.com, ולכן גם "#/admin"
--             בצורתו הנכונה נופל ל-<NotFound />. אין כתובת נכונה בתוכן.
--
-- כלומר מסך הניהול של #27 קיים במקור — dashboard, admin-login ועוד תריסר
-- מסכים — ואי אפשר להגיע אליו מהכתובת שבה המערכת מוגשת לציבור. זו עובדה על
-- ההרכבה, לא על הקוד, והיא אינה core.issues #86 ("נמדד, לא הונח"): כאן הונח.
--
-- מה שהמיגרציה הזאת עושה: מוחקת את הכתובת שלא מגיעה לשום מקום, ושומרת אותה
-- ואת הסיבה ב-fixed_notes. הלוח מפסיק להציע קישור שנמדד כלא-מגיע, והממצא
-- עצמו נרשם כתקלה פתוחה על #27 במקום להיעלם לתוך קישור שנראה תקין.
--
-- admin_auth נשאר 'own' ולא הופך ל-'none': ל-#27 יש מסך ניהול משלו והוא שומר
-- על עצמו בסיסמה. הצירוף own + admin_url ריק אומר בדיוק את מה שנמדד — המערכת
-- שומרת מסך ניהול משלה, ואין כתובת תחת ההרכבה שמגיעה אליו. ה-21 שורות של
-- admin_auth='none' אומרות דבר אחר לגמרי, ולערבב ביניהן זה לאבד את ההבחנה.
--
-- הדוח more30_admin_systems_report אינו משתנה כאן. הוא כבר מוציא admin_url רק
-- כש-admin_auth הוא own/hub וגם הכתובת ניתנת ללחיצה (0038, שורות 131-136), ולכן
-- מחיקת הכתובת מספיקה כדי שהכרטיס יפסיק לצייר קישור, ו-totals.with_admin_url
-- ירד מ-14 ל-13 — המספר הנכון.
--
-- 14 ולא 13, ובכוונה: ל-core.projects ארבע-עשרה שורות של own/hub עם כתובת,
-- ו-admin-entry-resolves.mjs פתח שלוש-עשרה מהן. ההפרש הוא השורה שה-path שלה
-- ריק (slug=more30-spec-loop), ואי אפשר להרכיב לה כתובת יחסית תחת מערכת שאין
-- לה שם הרכבה. היא נספרת בסך והיא לא נמדדה — שני דברים נכונים בו-זמנית, ולכן
-- הם נאמרים כאן שניהם ולא נבחר מהם המספר הנוח.

begin;

-- ── 1. הכתובת שנמדדה כלא-מגיעה יורדת, עם הסיבה ─────────────────────────────
update core.projects
set    admin_url   = null,
       fixed_notes = coalesce(fixed_notes || E'\n', '') ||
         '07/08/2026 (מיגרציה 0046): admin_url היה "/admin" והוסר. ' ||
         'ארבע כתובות מועמדות נפתחו מול הייצור ' ||
         '(scripts/qa/mechiron-admin-address.mjs): /mechiron/ ו-/mechiron/admin ' ||
         'מציירות את דף הנחיתה הציבורי בלי שום שער, ו-/mechiron/#/admin ' ||
         'ו-/mechiron/#/login מציירות 404. האפליקציה מנתבת על ה-hash ' ||
         '(App.tsx:211 useHashLocation), ולאזור הפנימי נכנסים רק כשהמארח מתחיל ' ||
         'ב-"admin." (App.tsx:198 isAdminSubdomain) — תנאי שכתובת נתיב תחת ' ||
         'more30.com אינה יכולה לקיים. מסך הניהול קיים במקור; אין כתובת ' ||
         'תחת ההרכבה שמגיעה אליו. admin_auth נשאר own.',
       updated_at  = now()
where  path = 'mechiron'
  and  admin_url = '/admin';

-- ── 2. הממצא עצמו — תקלה פתוחה על #27 ──────────────────────────────────────
-- פתוחה ולא סגורה: מה שנסגר כאן הוא רק הקישור השגוי בלוח. להחזיר את מסך
-- הניהול לכתובת דורש שינוי בהרכבה של #27 עצמה, והיא ממתינה להכרעת core.issues
-- #94 (מה בכלל עושים עם #27 — הכתובת מגישה עמוד זכויות והרישום מוכר השוואת
-- מחירים), ולפריסה לייצור שחסומה ב-core.issues #83.
insert into core.issues (app, title, detail, severity, status, owner, blocked_on, evidence)
select
  'mechiron',
  'מסך הניהול של #27 קיים במקור, ואף אחת מארבע הכתובות תחת ההרכבה אינה מגיעה אליו',
  'נמדד מול הייצור 07/08/2026, מנותק. /mechiron/ ו-/mechiron/admin מציירות את ' ||
  'אותו דף נחיתה ציבורי ("בקלות — הזכויות וההטבות שמגיעות לכם"), ואף אחת מהן ' ||
  'אינה מבקשת מאיש להזדהות. /mechiron/#/admin ו-/mechiron/#/login מציירות ' ||
  '"404 Page Not Found". אפס מארבע מגיעות למסך ניהול.' || E'\n\n' ||
  'שתי סיבות, שתיהן ב-apps/27-bkalut-price/client/src/App.tsx: הראוטר הוא ' ||
  'useHashLocation (שורה 211), ולכן "/admin" כנתיב אינו מסלול ומוכרע כ-"/"; ' ||
  'ולאזור הפנימי נכנסים רק כש-isAdminSubdomain() אמת (שורה 198), כלומר ' ||
  'כששם המארח מתחיל ב-"admin.". תחת more30.com/mechiron שם המארח הוא ' ||
  'more30.com, ולכן גם הצורה הנכונה "#/admin" נופלת ל-NotFound. ' ||
  'CLAUDE.md של המערכת מתאר את ההפרדה הזאת כמכוונת: הניהול חי על ' ||
  'admin.bekalut.more30.com, והדומיין הציבורי מציג 404 לנתיבי הניהול.' || E'\n\n' ||
  'מיגרציה 0046 הורידה את admin_url כדי שהלוח יפסיק להציע קישור שנמדד ' ||
  'כלא-מגיע. הממצא עצמו נשאר פתוח: להחזיר כניסה לניהול ל-#27 מ-more30.com ' ||
  'דורש הכרעה על ההרכבה שלה (core.issues #94) ופריסה לייצור (core.issues #83).',
  'normal', 'open', 'agent', 'core.issues #94 · #83',
  'scripts/qa/mechiron-admin-address.mjs · ' ||
  'QA/platform/mechiron-admin-address-0807/{_results.json,root.png,recorded.png,hash-admin.png,hash-login.png} · ' ||
  'QA/platform/admin-entry-resolves-0807/_results.json'
where not exists (
  select 1 from core.issues
  where app = 'mechiron'
    and title = 'מסך הניהול של #27 קיים במקור, ואף אחת מארבע הכתובות תחת ההרכבה אינה מגיעה אליו'
);

-- ── 3. הבאג בלוח — נפתח ונסגר כאן ──────────────────────────────────────────
insert into core.issues (app, title, detail, severity, status, owner, evidence, resolved_at)
select
  'more30',
  'לוח המערכות הציע "ניהול המערכת הזאת" לכתובת שמציירת את דף השיווק של מערכת אחרת',
  'הכרטיס של #27 ב-/admin/systems צייר קישור "ניהול המערכת הזאת ←" אל ' ||
  'https://more30.com/mechiron/admin, כי core.projects רשמה שם "/admin" ' ||
  'ו-admin_auth הוא own — שני התנאים ששער 0024 בודק. השער בודק את צורת ' ||
  'הכתובת, לא את מה שהיא מגישה. הכתובת מחזירה 200 ומציירת דף נחיתה ציבורי, ' ||
  'ולכן היא עברה כל בדיקת סטטוס שנעשתה עליה עד היום.' || E'\n\n' ||
  'admin-entry-resolves.mjs מצא אותה בכך שפתח את שלוש-עשרה הכתובות והשווה את ' ||
  'הטקסט המצויר לזה של שורש המערכת: זו היחידה מהן שמציירת בדיוק את השורש ' ||
  'ואינה מבקשת מאיש להזדהות. 0046 הורידה את הכתובת, ולכן הכרטיס אומר עכשיו ' ||
  'שאין כניסה לניהול במקום להציע כזאת, ו-totals.with_admin_url ירד מ-14 ל-13 ' ||
  '(14 ולא 13: השורה שה-path שלה ריק, slug=more30-spec-loop, נספרת בסך ולא ' ||
  'נמדדה, כי אין לה שם הרכבה להרכיב עליו כתובת יחסית).',
  'normal', 'fixed', 'agent',
  'scripts/qa/admin-entry-resolves.mjs · scripts/qa/mechiron-admin-address.mjs · ' ||
  'QA/platform/mechiron-admin-address-0807/_results.json',
  now()
where not exists (
  select 1 from core.issues
  where app = 'more30'
    and title = 'לוח המערכות הציע "ניהול המערכת הזאת" לכתובת שמציירת את דף השיווק של מערכת אחרת'
);

commit;
