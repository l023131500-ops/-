create policy "potential_submissions_service_role_full" on igud."potential_submissions" as permissive for all to public using ((auth.role() = 'service_role'::text)) with check ((auth.role() = 'service_role'::text));
create policy "prayer_times_tenant_read" on igud."prayer_times" as permissive for select to public using (((tenant_id IS NULL) OR igud.user_in_tenant(tenant_id) OR (EXISTS ( SELECT 1 FROM tenants t WHERE ((t.id = prayer_times.tenant_id) AND (t.status = 'active'::tenant_status))))));
create policy "prayer_times_tenant_write" on igud."prayer_times" as permissive for all to public using ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role))) with check ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role)));
create policy "pc_read" on igud."product_categories" as permissive for select to public using (((is_active = true) OR igud.user_in_tenant(tenant_id)));
create policy "pc_write" on igud."product_categories" as permissive for all to public using (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role)) with check (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role));
create policy "products_read" on igud."products" as permissive for select to public using (((is_active = true) OR igud.user_in_tenant(tenant_id)));
create policy "products_write" on igud."products" as permissive for all to public using (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role)) with check (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role));
create policy "profiles_read_basic" on igud."profiles" as permissive for select to public using (true);
create policy "profiles_self" on igud."profiles" as permissive for all to public using ((id = auth.uid())) with check ((id = auth.uid()));
create policy "rabbi_questions_public_insert" on igud."rabbi_questions" as permissive for insert to "anon" with check ((igud.tenant_accepts_public_intake(tenant_id) AND (answer IS NULL) AND (COALESCE(is_public, false) = false)));
create policy "rabbi_questions_tenant_read" on igud."rabbi_questions" as permissive for select to public using ((igud.user_in_tenant(tenant_id) OR ((is_public = true) AND (answer IS NOT NULL))));
create policy "rabbi_questions_tenant_write" on igud."rabbi_questions" as permissive for all to public using ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role))) with check ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role)));
create policy "rc_announcements_public_read" on igud."rc_announcements" as permissive for select to public using ((is_active AND ((ends_at IS NULL) OR (ends_at > now()))));
create policy "rc_councils_public_read" on igud."rc_councils" as permissive for select to public using (is_active);
create policy "rc_events_public_read" on igud."rc_events" as permissive for select to public using (is_active);
create policy "rc_prayer_times_public_read" on igud."rc_prayer_times" as permissive for select to public using (is_active);
create policy "rc_synagogues_public_read" on igud."rc_synagogues" as permissive for select to public using (is_active);
create policy "bkalut backend full access" on igud."service_submissions" as permissive for all to "anon", "authenticated" using (true) with check (true);
create policy "share_links_public_read" on igud."share_links" as permissive for select to public using ((is_active AND ((expires_at IS NULL) OR (expires_at > now()))));
create policy "study_daily_tenant_read" on igud."study_daily" as permissive for select to public using (igud.user_in_tenant(tenant_id));
create policy "study_daily_tenant_write" on igud."study_daily" as permissive for all to public using ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role))) with check ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role)));
create policy "study_schedules_tenant_read" on igud."study_schedules" as permissive for select to public using (igud.user_in_tenant(tenant_id));
create policy "study_schedules_tenant_write" on igud."study_schedules" as permissive for all to public using ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role))) with check ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role)));
create policy "synagogues_tenant_read" on igud."synagogues" as permissive for select to public using (((tenant_id IS NULL) OR igud.user_in_tenant(tenant_id) OR (EXISTS ( SELECT 1 FROM tenants t WHERE ((t.id = synagogues.tenant_id) AND (t.status = 'active'::tenant_status))))));
create policy "synagogues_tenant_write" on igud."synagogues" as permissive for all to public using ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role))) with check ((igud.is_super_admin(auth.uid()) OR igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'moderator'::app_role) OR igud.has_tenant_role(auth.uid(), tenant_id, 'member'::app_role)));
create policy "teachers_public_read" on igud."teachers" as permissive for select to public using ((is_active AND is_approved));
create policy "tenant_ads_public_read" on igud."tenant_ads" as permissive for select to public using ((is_active AND is_approved AND ((ends_at IS NULL) OR (ends_at > now()))));
create policy "branding_read" on igud."tenant_branding" as permissive for select to public using (true);
create policy "branding_write" on igud."tenant_branding" as permissive for all to public using (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role)) with check (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role));
create policy "features_read" on igud."tenant_features" as permissive for select to public using (true);
create policy "features_write" on igud."tenant_features" as permissive for all to public using (igud.is_super_admin(auth.uid())) with check (igud.is_super_admin(auth.uid()));
create policy "invites_admin" on igud."tenant_invites" as permissive for all to public using (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role)) with check (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role));
create policy "subs_read" on igud."tenant_subscriptions" as permissive for select to public using (igud.has_tenant_role(auth.uid(), tenant_id, 'tenant_admin'::app_role));
create policy "subs_super_admin_write" on igud."tenant_subscriptions" as permissive for all to public using (igud.is_super_admin(auth.uid())) with check (igud.is_super_admin(auth.uid()));
create policy "tenants_admin_update_own" on igud."tenants" as permissive for update to public using (igud.has_tenant_role(auth.uid(), id, 'tenant_admin'::app_role));
create policy "tenants_read_public" on igud."tenants" as permissive for select to public using ((status = 'active'::tenant_status));
create policy "tenants_super_admin_all" on igud."tenants" as permissive for all to public using (igud.is_super_admin(auth.uid())) with check (igud.is_super_admin(auth.uid()));
create policy "tips_read" on igud."tips" as permissive for select to public using (((is_active = true) OR igud.is_super_admin(auth.uid())));
create policy "tips_write" on igud."tips" as permissive for all to public using (igud.is_super_admin(auth.uid()));
create policy "user_roles_self_read" on igud."user_roles" as permissive for select to public using (((user_id = auth.uid()) OR igud.is_super_admin(auth.uid())));
create policy "user_roles_super_admin_write" on igud."user_roles" as permissive for all to public using (igud.is_super_admin(auth.uid())) with check (igud.is_super_admin(auth.uid()));
create policy "bkalut backend full access" on igud."users" as permissive for all to "anon", "authenticated" using (true) with check (true);
create policy "coupons_admin_all" on igud_transcribe."coupon_codes" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "footnotes_admin_all" on igud_transcribe."footnotes" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "jobs_admin_all" on igud_transcribe."jobs" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "profiles_admin_all" on igud_transcribe."profiles" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "profiles_self_read" on igud_transcribe."profiles" as permissive for select to public using ((auth.uid() = id));
create policy "settings_admin_all" on igud_transcribe."settings" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "glossary_admin_all" on igud_transcribe."style_glossary" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "transcripts_admin_all" on igud_transcribe."transcripts" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));
create policy "uploads_admin_all" on igud_transcribe."uploads" as permissive for all to public using ((auth.email() = 'l023131500@gmail.com'::text));