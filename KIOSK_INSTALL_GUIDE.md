# KIOSK_INSTALL_GUIDE — מדריך התקנה מלא "מאפס" (לכל אדם, בלי ידע טכני)

> מקור אמת אחד: מהמדריך הזה נבנה **אשף ההתקנה החי באפליקציה** (§2★ב) — צעדים זהים, תיבות סימון, עברית+English, ותצוגת "מה אמור להופיע". מה שרואה הלקוח באפליקציה = זהה למדריך הזה, בלי שום מחסום.

---

## חלק 0 — מה אנחנו עושים ולמה (בשפה פשוטה)
טאבלט רגיל פותח כל דבר. אנחנו הופכים אותו ל**קיוסק נעול** שפותח רק את האתרים שלנו ואי אפשר לצאת ממנו.
אנדרואיד (מערכת הטאבלט) מגינה על עצמה: היא **לא נותנת לאף אפליקציה שליטה מלאה** אלא אם **אתה, מחזיק את המכשיר פיזית, מאשר זאת פעם אחת**. זו "ההפעלה החד‑פעמית". אחרי זה — הכל אוטומטי ומרחוק, כולל בלי אינטרנט.

**שתי דרכים עיקריות לאותו אישור חד‑פעמי:**
- **שיטה A — QR:** סורקים קוד במסך הפתיחה של הטאבלט. קלה, בלי מחשב. עובדת רק בטאבלטים עם Google Play.
- **שיטה B — מחשב + כבל USB:** מחברים למחשב פעם אחת ומריצים פקודה מוכנה. עובדת כמעט בכל טאבלט, כולל זול/סיני.

בוחרים אחת לפי סוג הטאבלט. שתיהן מוסברות כאן צעד‑צעד.

---

## חלק 1 — מרכישת המכשיר
1. **מה קונים:** טאבלט אנדרואיד (מומלץ אנדרואיד 9 ומעלה).
   - יש בו Google Play (חנות אפליקציות של גוגל)? → **שיטה A (QR)** הכי קלה.
   - זול/סיני בלי Google Play? → **שיטה B (מחשב/USB)**.
2. ⚠️ **חשוב:** לפני שקונים כמות — **קנה מכשיר אחד ובדוק עליו** שהנעילה עובדת. חלק מהטאבלטים הזולים חוסמים את הנעילה בקושחה.
3. אל תוסיף חשבון Google בטאבלט לפני ההתקנה (זה חוסם את שיטה B). אם כבר הוספת — צריך איפוס (חלק 2).

---

## חלק 2 — איפוס להגדרות יצרן (מה זה ולמה)
**למה:** ההפעלה החד‑פעמית עובדת רק על טאבלט "נקי" — בלי חשבונות ובלי נתונים. טאבלט חדש מהקופסה כבר נקי. טאבלט משומש / כזה שהוספת בו חשבון — צריך לאפס.

**דרך 1 — טאבלט חדש מהקופסה:** אין צורך לאפס. פשוט אל תוסיף חשבון Google, והמשך לשיטה A או B.

**דרך 2 — איפוס דרך ההגדרות (הכי פשוט):**
1. פתח **הגדרות / Settings**.
2. **מערכת / System** → **אפשרויות איפוס / Reset options**
   *(בסמסונג: **ניהול כללי / General management** → **איפוס / Reset**)*.
3. **מחיקת כל הנתונים (איפוס להגדרות היצרן) / Erase all data (factory reset)**.
4. **מחק הכל / Erase all** → אשר. הטאבלט נמחק ומדליק מחדש למסך הפתיחה.
   *(השמות משתנים מעט בין יצרנים — חפש "איפוס" בתוך ההגדרות.)*

**דרך 3 — איפוס בכפתורים (אם אי אפשר להיכנס להגדרות):**
כבה את הטאבלט → החזק **כפתור הדלקה + ווליום מעלה** יחד (משתנה בין דגמים) עד שמופיע תפריט → נווט עם כפתורי הווליום ל‑**Wipe data / factory reset** → בחר עם כפתור ההדלקה → **Reboot**. *(צירוף הכפתורים שונה בין דגמים — אם לא בטוח, השתמש בדרך 2.)*

---

## חלק 3 — שיטה A: QR (הכי קלה, בלי מחשב) — לטאבלטים עם Google Play
> ה‑6 הקשות עובדות **רק כאן** — במסך הפתיחה אחרי איפוס, ורק במכשירים שתומכים בזה.

1. אחרי האיפוס, הטאבלט במסך **"Hi / ברוכים הבאים"**. **למה חשוב:** זה הרגע היחיד שהטריק עובד.
2. **הקש 6 פעמים באותה נקודה** על המסך. **למה:** זו "דלת סודית" של אנדרואיד שפותחת התקנת קיוסק. (מובנה באנדרואיד, בלי תוכנה.)
3. אם מבקש — **התחבר ל‑Wi‑Fi** (בחר רשת → הזן סיסמה). **למה:** הטאבלט צריך אינטרנט כדי **להוריד את האפליקציה שלנו**. (ה‑Wi‑Fi נועד רק להורדה — לא לנעילה.)
4. נפתחת **מצלמה** → **סרוק את ה‑QR** שהקונסולה שלנו מציגה. **למה:** ה‑QR אומר לטאבלט **מאיפה להוריד את האפליקציה שלנו + לתת לה שליטה מלאה**. (הסורק = התקנת התוכנה, לא רק רשת.)
5. הטאבלט מוריד, מתקין, קובע את האפליקציה שלנו כ‑"בעלים", ומדליק מחדש. **למה:** עכשיו האפליקציה שלנו שולטת בכל.
6. מופיע **מסך הבחירה של הקיוסק שלנו**. ✅ נעול.

**מה תראה בדרך:** פתיחה → (6 הקשות) → סורק QR → Wi‑Fi → "מגדיר את המכשיר / Setting up your device" → מסך הקיוסק.

**עובד בכל מכשיר?** לא. רק בטאבלטים שאנדרואיד שלהם תומך ב‑QR (רוב מכשירי Google Play, אנדרואיד 7+). טאבלט זול בלי Google — לרוב אין לו את זה → **עבור לשיטה B**.

---

## חלק 4 — שיטה B: מחשב + כבל USB (עובד כמעט בכל טאבלט, כולל זול/סיני)
> במקום QR, המחשב שולח לטאבלט **פקודה אחת מוכנה** שמתקינה את האפליקציה ונותנת לה שליטה. אין QR, והטאבלט אפילו לא צריך Wi‑Fi בשביל הפקודה.

**הכנה במחשב (פעם אחת בחיים):** התקן "ADB / Platform Tools" (הקונסולה שלנו נותנת קישור הורדה + עוזר בלחיצה). **מה זה ADB:** הכלי שמאפשר למחשב "לדבר" עם אנדרואיד דרך כבל USB.

**על הטאבלט:**
1. ודא שהוא **מאופס ובלי חשבון Google** (חלק 2).
2. **הגדרות / Settings** → **אודות הטאבלט / About tablet** → מצא **"מספר גרסה / Build number"** → **הקש עליו 7 פעמים** → מופיע **"אתה עכשיו מפתח / You are now a developer"**. **למה:** זה פותח תפריט מוסתר בשם "אפשרויות מפתח".
3. **הגדרות → מערכת / System → אפשרויות מפתח / Developer options** → הפעל **"ניפוי באגים ב‑USB / USB debugging"** → **אישור / OK**. **למה:** מאפשר למחשב לשלוח פקודות.
4. **חבר את הטאבלט למחשב בכבל USB.** בטאבלט יקפוץ **"לאפשר ניפוי USB? / Allow USB debugging?"** → סמן **"אפשר תמיד / Always allow"** → **אישור / Allow**. **למה:** אתה מאשר את המחשב הספציפי הזה.
5. בקונסולה שלנו לחץ **"העתק פקודה / Copy command"** של אותו מכשיר → הדבק בחלון הפקודה במחשב → **Enter**. **למה:** הפקודה הזו מתקינה את האפליקציה שלנו וקובעת אותה כ‑"בעלים".
6. הטאבלט עובר למצב קיוסק. ✅

**למה צריך מחשב?** כי לטאבלטים האלה אין את תכונת ה‑QR; המחשב הוא הדרך לתת שליטה פעם אחת.

---

## חלק 5 — דרכים נוספות (בקצרה, עם מטרה)
- **Zero‑touch (לכמויות):** אם קונים טאבלטים דרך ספק שתומך ב‑zero‑touch, הם **ננעלים לבד בהדלקה הראשונה** בלי שום צעד. **מטרה:** הזמנות גדולות. דורש הגדרה מול הספק.
- **NFC (נדיר):** הצמדת תג מתוכנת בזמן ההגדרה. רק אם המכשיר תומך.
- **Windows (מסכים גדולים):** הורד חבילה מהקונסולה → הרץ כמנהל → Assigned Access (אפליקציה בודדת) או Shell Launcher (נעילה מלאה).
- **USB אופליין מוחלט:** הקונסולה מייצרת חבילת USB → צריבה → הרצה בלי אינטרנט.
- ⭐ **המומלץ לעסק שמוכר קיוסקים:** **אתה נועל כל טאבלט מראש** (שיטה A או B) לפני המכירה → הלקוח רק מדליק. **אפס צעדים עבורו — אפס מחסום.**

---

## חלק 6 — למה נועד כל דבר (מילון פשוט)
- **איפוס יצרן** = ניקוי הטאבלט כדי שאפשר יהיה "לאמץ" אותו.
- **6 הקשות** = דלת סודית של אנדרואיד שמתחילה התקנת קיוסק.
- **סריקת QR** = הכתובת + אישור ההרשאה שמתקינים את האפליקציה שלנו ונותנים לה שליטה (ולפעמים גם ה‑Wi‑Fi).
- **Wi‑Fi** = נחוץ רק כדי שהטאבלט יוריד את האפליקציה (שיטה A). לא הנעילה עצמה.
- **ADB / ניפוי USB** = "שפת הכבל" שמאפשרת למחשב להגדיר טאבלטים שאין להם QR.
- **Device Owner (בעלים)** = הסטטוס המיוחד שמאפשר לאפליקציה שלנו לנעול הכל. **זו כל מטרת ההפעלה החד‑פעמית.**
- **אחרי ההתקנה** = האפליקציה שלנו מנהלת הכל; אתה שולט מהאתר; עובד אופליין.

---

## חלק 7 — מה רק אתה יכול לעשות מול מה שאוטומטי
- **רק אתה, פעם אחת פר טאבלט (~3 דק'):** איפוס + סריקת QR **או** פקודת USB.
- **כל השאר, לתמיד:** אוטומטי ומרחוק מהקונסולה (החלפת אתר, אתחול, נעילה/שחרור, אופליין).

---

## הוראה לטרמינל (כשהקיוסק חוזר, אחרי גן‑קליק)
1. **אודיט קודם:** בדוק בקוד מה בנוי מ‑§2★ ומ‑KIOSK_BUILD.md מול מה שחסר (קונסולה/APK/IdentifyDevice/launcher/exit‑lock). כתוב ל‑core.issues + STATUS.
2. **בנה APK (Device Owner + Lock Task Mode)** לפי §2★. אם אין Android toolchain מקומי → **צור GitHub Actions workflow שבונה את ה‑APK בענן**.
3. **בנה את אשף ההתקנה החי בקונסולה מהמדריך הזה** — כל השיטות (A/B/C/D + איפוס), צעדים זהים, תיבות סימון, עברית+English, "מה אמור להופיע", והסבר "למה" לכל צעד. יצירת QR + פקודת ADB דינמית לכל מכשיר. זהה בין האתר ללקוח — בלי מחסום.
4. IdentifyDevice + /kiosk‑launcher/:code + מודל מזהה‑לקוח + נעילת יציאה.
5. אמת כל שלב; מה שדורש מכשיר פיזי → NEEDS_USER. איכות לפני מהירות, אפס באגים.
