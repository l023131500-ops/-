# המשימה הגדולה — Phase 1: איחוד, מחלקות, ולוח שליטה מרכזי
### פרויקט more30 · המשך ישיר לשלב 0 (המיפוי כבר הושלם, CONNECTIONS.md קיים)

> **איך לעבוד — עצמאי עם שערי בטיחות:**
> עבוד **ברצף ובאופן עצמאי** והשלם את כל השלבים A→D **בלי לעצור לשאול**, כל עוד מדובר בעבודה מוסיפה ובטוחה (מיפוי, ייבוא לענף, בניית `core` ו-`admin` בפרויקט ה-ops, דוח טוקנים). **אסוף את כל השאלות והחוסרים לסוף** והצג לי אותם ברשימה אחת מסודרת.
> עצור לאישור מפורש **רק** לפני פעולה מסוכנת/בלתי-הפיכה — ראה "שערי עצירה" למטה.

---

## 🧬 עקרון העותק — שכבת-על לא-הרסנית (Non-Destructive Overlay)
Phase 1 בונה **שכבה חדשה מעל** הקיים, ולא נוגעת בו:
- **ייבוא = העתקת קוד בלבד** לתוך המונו-רפו החדש. כל מערכת חיה ממשיכה לרוץ בדיוק כמו שהיא — אותה פריסה, אותו DB, אותם מפתחות — **בלי נגיעה**.
- **הלוח רק קורא סטטוס ומקשר** לעמודי האדמין. הוא **לא דוחף** שום שינוי למערכות חיות.
- **אף פריסה חיה לא מוחלפת.** הלוח נפרס כ**שירות חדש ונפרד** (תת-דומיין/שירות משלו), לא במקום `mor1-main-site` או כל אתר חי.
- **איחוד פריסות בפועל** (העברת מערכת חיה לפרוס מהמונו-רפו) = פייז נפרד בעתיד, ורק באישור מפורש.

---

## ⛔ חוקים קדושים (מחייבים בכל שלב)
1. **לא לשבור חיבורים קיימים.** איחוד = לחבר את מה שקיים, לא לבנות מחדש. כל מערכת נשארת על אותו Supabase project + schema + מפתחות שהיא משתמשת בהם היום.
2. **מפתחות משותפים.** מערכת חדשה או מיובאת משתמשת באותם מפתחות קיימים של המחלקה/הפרויקט שלה. **אין** ליצור מסדי נתונים או מפתחות חדשים שמפצלים עוד.
3. **מוגן — אסור לגעת:** `bkalut-app`, `bkalot-admin`, schema `zr_*`, webhook n8n `NEDARIM3873`.
4. **לארכוב בלבד, לא למחוק:** `luxe-balance-hub-81` (×4), `luxe-ledger-hub`, `lux-manage`, `bklotm`, `pixel-perfect`.
5. **בטיחות.** כל העבודה על ענף `feature/unify-phase1`. הצג לי diff לפני כל מיזוג.
6. **סודות.** לעולם אל תכתוב ערכי סודות בקוד או בקובץ שנכנס ל-git. רק ב-secret store / משתני סביבה של הפריסה.
7. **אין נתוני דמה.** כשאין מקור — מציגים "לא זמין", לא ממציאים.

## 🚦 שערי עצירה — עצור וקבל אישור מפורש לפני:
- מיזוג ל-`main` של מערכת **חיה**.
- כל נגיעה במערכת **מוגנת** (סעיף 3).
- **מחיקה או ארכוב** של מערכת כלשהי.
- כל שינוי שעלול **לנתק חיבור קיים** (מפתח, DB, דומיין) של מערכת חיה.
- **פריסה שמחליפה** גרסה חיה קיימת.
בכל דבר אחר — התקדם לבד בלי לשאול.

---

## שלב A — ייבוא כל המערכות למונו-רפו (גם שנפרסו וגם שלא)
ייבא/שכפל אל `more30.com/apps/` את **כל** המאגרים מחשבון הגיטאב `l023131500-ops` (~41 מאגרים) — deployed, not-deployed, וגם **מה שבאמצע פיתוח** — כאפליקציה ממוספרת לכל אחת (01, 02, ...).
- אל תשמיט אף מאגר. כלול מערכות לא-גמורות (למשל 5 אתרי העורך התורני), שלד/רעיון, ותוכניות לפני-בנייה (הגהה) — אלה ייכנסו עם `README` שמסביר את הסטטוס.
- שמור לכל אפליקציה את החיבורים הקיימים שלה (Supabase project+schema, יעד פריסה) — בלי לשנות.
- **מוגן:** אל תייבא ואל תיגע. **ארכוב:** ייבא לתיקיית `apps/_archive/` בלבד (קפוא, לא נפרס).

---

## שלב B — מבנה מחלקות (Departments)
ארגן את כל האפליקציות ל**מחלקות**. הוסף לכל אפליקציה שדה `department`, לפי הבסיס הבא (התאם והשלם לפי מה שתמצא):

- **עורך תורני** — תמלול (`igud-transcribe`, `chizukim-transcribe`, תמלול אודיו) · המרת כתב יד · יצירת מודעות (`igud-ads`) · עימוד (`imud-torani`) · הגהה (תוכנית) · 5 אתרי העורך התורני · `torah-platform`/`egod` (HUB) · `igud-shiurim-portal`.
- **פיננסי** — `03-financial-marketing-site` · סליקת נדרים פלוס (7016674) · (ארכוב: `luxe-*`, `lux-manage`, `luxe-ledger-hub`).
- **נדל"ן** — `nadlan-berega` / `property-identity` / `smel-ndln`.
- **בריאות וקופות** — `kupot-holim` / `kupot-health-funds`.
- **זכויות** — `get-your-rights` / `bkalot-rights`.
- **קהילה ואזורי** — `chatzor-connect`, `galilee-connect-hub`, `heb-connect-hub`.
- **מותג בקלות (מוגן)** — `bkalut-app` 🔒, `bkalot-admin` 🔒, `bkalut-marketing2`.
- **שונות / מסחר** — `zol`, `bsmachot-plus`, `home-upgrade-magic`, ואחרים.

כל מאגר חייב להשתייך למחלקה. מה שלא ברור — שייך ל"שונות" וסמן לשאלה בסוף.

---

## שלב C — schema `core` + לוח שליטה מרכזי ("הקישור האחד")
זהו הלב: מערכת אחת שרואה ושולטת בכל המערכות.
1. צור schema `core` בפרויקט ה-Supabase הראשי (`uhnrgujbdxhhmoxcjria` — ה-"ops"), **בלי לגעת** בשאר 6 פרויקטי ה-Supabase.
2. טבלה `core.projects` עם עמודות: `number, name, department, category, stage, is_deployed, is_live, repo_url, deploy_target, supabase_project, supabase_schema, live_url, admin_url, bugs (jsonb), tasks_claude (jsonb), tasks_user (jsonb), missing_tokens (jsonb), to_delete (bool), notes`.
   - `stage` = רעיון / בפיתוח / נפרס — כדי שגם מערכות באמצע פיתוח יוצגו.
   - `admin_url` = קישור לעמוד ההתחברות (Admin Login) של אותה מערכת.
3. אכלס את הטבלה מ-`CONNECTIONS.md` **ומכל ~41 המאגרים** — רשומה לכל מערכת, בלי יוצא מן הכלל.
4. בנה אפליקציית `admin/` בתוך המונו-רפו שקוראת וכותבת ל-`core.projects` ומציגה:
   - **רשימה מלאה של כל המערכות** (כולל שבאמצע פיתוח, שלד ורעיון) — שכלום לא יהיה חסר וקל יהיה להמשיך פיתוח.
   - כרטיס לכל מערכת: מחלקה, סטטוס (נפרס/לא · חי/לא · שלב פיתוח), החיבורים שלה, והטוקנים החסרים.
   - **קישור/כפתור "כניסת אדמין"** (`admin_url`) לכל מערכת.
   - **רשימת משימות דו-כיוונית** לכל מערכת: משימות שקלוד מוסיף + משימות שהמשתמש כותב ידנית (עריכה וסימון "בוצע").
   - **סימון למחיקה** (`to_delete`) לכל מערכת.
   - סינון לפי מחלקה, סטטוס ושלב פיתוח.
5. פרוס את הלוח כ**שירות חדש ונפרד** (תת-דומיין `admin.more30.com` או שירות Railway/Vercel חדש) — **לא** במקום `mor1-main-site` ולא מעל אף אתר חי. זהו "הקישור האחד".

---

## שלב D — דוח טוקנים חסרים (עם קישורים מדויקים)
עבור על `CONNECTIONS.md` ועל `core`, ובנה טבלת **טוקנים חסרים**. לכל טוקן חסר תן חמישה פרטים:
1. המערכת שצריכה אותו.
2. שם משתנה הסביבה (למשל `OPENAI_API_KEY`).
3. למה הוא משמש.
4. **קישור מדויק לעמוד שבו מפיקים את המפתח.** לדוגמה:
   - OpenAI → `https://platform.openai.com/api-keys`
   - Google OAuth → `https://console.cloud.google.com/apis/credentials`
   - Anthropic → `https://console.anthropic.com/settings/keys`
   - Supabase `service_role` → Dashboard של הפרויקט → Project Settings → API
5. **קישור מדויק לעמוד שבו מדביקים את הטוקן** — עמוד ה-Variables/Environment של אותו שירות **ספציפי**:
   - Railway → השירות → לשונית **Variables**
   - Vercel → Project → **Settings → Environment Variables**
   - Supabase Edge Functions → Project → **Edge Functions → Secrets**

- התחל מ-`igud-transcribe` (חסרים: `OPENAI_API_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, ו-`SUPABASE_SERVICE_ROLE_KEY` של פרויקט `bieebmnm`).
- **אל תמלא ערכים בעצמך** — רק תכין טבלה של "איפה להשיג" + "איפה להדביק" לכל טוקן, כדי שהמשתמש ישלים.

---

## שלב E — הכנה לשכבת אוטומציות (רישום עכשיו · בנייה ב-Phase 2)
המשתמש מתכנן שכבת אוטומציות. **בשלב זה רק רשום אותן** ב-`core` ובדוח הטוקנים — **בלי לבנות ובלי לגעת בקיים**:
- **ימות המשיח** (טלפוניה / IVR) — רשום כחיבור, וסמן איזה API token / פרטי גישה נדרשים ואיפה מדביקים.
- **שליחת מיילים** — דרך Resend (כבר מחובר אצל המשתמש) או SMTP; רשום איזה מפתח נדרש.
- **N8N** (אורקסטרציה) — כבר קיים אצל המשתמש (webhook `NEDARIM3873` מוגן — לא לגעת). רשום את ה-webhooks/החיבורים הנדרשים בלבד.
הבנייה בפועל של האוטומציות תתבצע ב-**Phase 2**, אחרי ש-Phase 1 יאומת ויאושר.

---

## הפלט הסופי שאני מצפה לו מ-Phase 1 (בבת אחת, בסוף)
1. מונו-רפו עם **כל** ~41 המערכות מיובאות ומאורגנות במחלקות (כולל לא-גמורות).
2. schema `core` מאוכלס + **לוח שליטה חי** (הקישור האחד): רשימת כל המערכות, קישור כניסת-אדמין לכל אחת, משימות דו-כיווניות, וסימוני מחיקה.
3. `CONNECTIONS.md` מעודכן.
4. **טבלת טוקנים חסרים** עם קישור-להשגה + קישור-להדבקה לכל אחד.
5. **רשימת שאלות פתוחות וחוסרים** — כל מה שדורש החלטה שלי או מפתח ממני, במקום אחד.

**התחל עכשיו משלב A והתקדם עד הסוף בעצמך. עצור רק בשערי העצירה. בסוף — הצג הכל + רשימת השאלות.**
