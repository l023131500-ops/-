21 mthbram (#161): every lead was readable from /api by anyone, and the blocker holding the deploy contradicted the audit that wrote it

The fix was written at source three days ago and never shipped. It carried a
blocker in its own words: requireAdmin() gates on has_role, no account passes
has_role today, so deploying before bootstrapping a real admin would lock the
admin panel out of its own data — and the bootstrap needs a secret only the
user can read. That blocker was checked instead of honoured.

It was wrong, and the audit that produced it already said so: nothing in the
app calls those three routes. AdminDashboard.tsx and MatchingTab.tsx read the
lead tables directly over PostgREST under the user session. Re-verified before
touching anything: the only reference to the api function anywhere in
apps/21-mthbram outside node_modules/dist is ApiReferenceSection.tsx, and every
fetch( there sits inside a template string that is rendered and copied, never
called. There was no admin screen to lock out.

Measured first, at 15:44 UTC, with no apikey and no Authorization header at
all: /teachers, /seekers and /contacts each answered 200. The tables are empty
(/stats 0/0/0) so nothing has leaked — but every lead arriving from the web
form, the chatbot or the IVR would have been readable by anyone with the URL.

Deployed through Lovable, not the Supabase CLI — the path opened by #160.
Project f53a4479-1aaf-45ee-810f-e8ea4ed3fbc2, commit 2160a1bb, one file
replaced verbatim with the already-committed local source, 1.9 credits. The
Lovable copy was read first and was byte-for-byte the ungated version, which
is what proves Lovable is what deploys this project.

After, at 15:55 UTC: /teachers, /seekers, /contacts all return 401 with no
headers, and 401 again when the anon key is presented as the Bearer token —
that second leg is the one verify_jwt could never have covered, since the anon
key is a valid JWT shipped in every page load. /lessons, /stats, /cities and
/subjects still return 200. Both halves of the acceptance test published to the
user in NEEDS_USER §0ק pass.

Not measured, and not claimed: the 403 branch for a signed-in non-admin.
Signing in test@more30.com on this project returned 401 today, the same shape
as #172 (an unconfirmed email is indistinguishable from a wrong password). The
branch was verified against this project's primitives during the audit and its
code is unchanged. The POST submission routes were not exercised either — each
one writes a real row into a live lead table, and none of them is touched by
this change.

No source change in this step; the source fix was already committed. What
changed here is the deploy, the measurement, NEEDS_USER §0ק (now closed, with
the false blocker written down so it is not re-inherited) and core.issues #161.

Still open on this project: #165 — chat and search-lessons spend LOVABLE_API_KEY
with no caller check and no rate limit. Worth noting for whoever takes it: 21
has no customer login surface, so the gannenet answer (gate behind the account)
would delete a public feature here. It needs a per-caller rate limit with a
store, which is its own step.

Test mode: no billing, no message sent, no account created, no row written.
Protected systems (08, 09, bkalut-app, bkalot-admin, zr_*, NEDARIM3873)
untouched; csj/csj_src/igud not used.
