mthbram (21): the admin password could not be read back, and the free side was the one the key icon had left

priority §1א asks for a "show password" eye on every password field. A
recursive grep of apps/21-mthbram finds exactly ONE type="password" input in
source — src/pages/AdminLogin.tsx, the admin login — so one edit closes the
system.

The side was measured live before writing, not copied from the systems done
before it. The field carries no dir of its own: it inherits rtl from
<html dir="rtl"> and computes text-align:start, so characters sit flush against
the RIGHT edge — and that is exactly where KeyRound already sits, behind
pr-10 (40px). The left side was the free one at padding-left 12px. left-2 for
the eye, pl-10 to reserve it, so the field now carries a reserved 40px on both
sides and neither icon can be typed over.

type="button" matters more here than on 02/03/04: this field IS inside
<form onSubmit={handleLogin}>, so a button without it would have submitted the
login form on every toggle. Verified rather than assumed — a wrapped
window.fetch counted 0 calls across both toggles with location.href unchanged,
so nothing reached Supabase and no credentials were sent.

Measured after deploy on more30.com: padding-left 40px, button rect
(327,429,28x28) contained in field rect (319,423,382x40), elementFromPoint at
its centre returns the svg the button owns. Round trip both ways:
password->text->password, "הצג סיסמה"->"הסתר סיסמה"->"הצג סיסמה",
eye->eye-off->eye, value More30Admin2026 preserved. 0 console errors; the two
console lines (missing autocomplete, deprecated apple-mobile-web-app-capable)
both predate this change.

vite build passed (3048 modules, index-BEupP8UO.js 1617.50 kB); mirrored into
_deploy/mthbram-more30/mthbram and deployed to mthbram-more30
(dpl_J5qvnV8ReKGBU2njUpcEFmC6puNy, READY). Verified on more30.com with a
cache-buster, not the vercel.app alias.

The deploy also emptied the shelf, same as imud (04) yesterday: production was
still serving index-y-Q5UqSS.js, built before the 10/08 source edits, so those
went out with this change — the meta description off «הפלטפורמה המובילה», the
og:image off the Lovable preview screenshot on a pub-*.r2.dev bucket we do not
own, twitter:card summary_large_image -> summary, the two <link rel=icon>
entries, and the removal of the builder-default public/favicon.ico.

apps/ is gitignored, so the page needed git add -f.

Evidence: QA/mthbram/password-toggle-0812/
