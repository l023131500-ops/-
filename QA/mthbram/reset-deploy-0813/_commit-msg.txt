21 mthbram: מסך האיפוס החזיר 200 וצייר 404 — נפרס ואומת בעיניים (#201)

הפעימה הקודמת דיווחה ששישה משבעת מסכי האיפוס "ענו 200". ה-200 הזה
אינו ראיה: vercel.json של mthbram ממפה כל נתיב תחת /mthbram/ ל-index.html,
ולכן גם נתיב שאינו קיים בחבילה מחזיר 200. בדפדפן, /mthbram/auth/reset
צייר את «404 · Oops! Page not found» של האפליקציה עצמה.

הסיבה נמדדה מול Vercel API ולא נוחשה: הקומיט שבנה את המסך (978684e)
נכתב ב-12/08 21:42Z, והפריסה האחרונה לייצור הייתה 12/08 04:41Z —
שבע-עשרה שעות קודם. אותה פעימה נמנעה מפריסה בכוונה בגלל #204, וכתבה
זאת ב-heartbeat. זהו אותו מצב שתוקן ב-31 גשר בפעימה הקודמת, על מערכת שנייה.

לפני ההעתקה הושוותה החבילה החיה לעותק שב-_deploy (אזהרת ה-HTML שאינו
במקור): זהים באורך, במבנה ובנוכחות auth-button.js — ולכן ההעתקה בטוחה.
נבנה, נפרס מהמקור (לא --prebuilt), ואומת בייצור: /mthbram/ מגישה את
index-DO5Yp880.js; /mthbram/auth/reset בלי טוקן מצייר «הקישור אינו תקף»;
ובמסך admin-login יש «שכחתי סיסמה» וכפתור עין.

מה שנשאר על המשתמש הוא רק #207 — uri_allow_list של aypsqqvfohekxxuqsmrw
אינו מכיל את more30.com, ולכן סיבוב מלא מהמייל אי אפשר לבדוק מכאן.

22 zchuyot נבדקה באותה נשימה ולא נפרסה, בכוונה: ה-HTML שהיא מגישה
בייצור הוא prerendered (161 div של תוכן), ו-vite build על המקור פולט
שורש ריק — פריסת המקור כפי שהוא תמחק את דף הבית שלה. אין שלב prerender
ב-package.json. נרשם ככרטיס משלו במקום להיבלע.
