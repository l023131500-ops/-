kiosk (35): five of the six dialogs were never going to be named by a commit message

apps/35-kioskfleet/STATUS.md item 7 ends on one sentence: promptUrl's label
interpolates a host into a Hebrew sentence, and that is console-rtl-0811's sweep
on a tenth view. A tenth view is the smallest honest step and it is the wrong
one. The devices screen opens eleven dialogs. That run opened three -- the
wizard, the client picker, the access-code dialog. The other six cost one click
each and are the same mechanism, so stopping at the one the last commit happened
to name would have left five views unread for no reason other than which
sentence got written down.

Swept all six: החלף אתר, עריכה, קוד יציאה, קישורים מאושרים, אתחל, מחיקה. Two of
them have a second text branch that only appears on a device with no allowed
host -- promptUrl's label falls back to הדומיין המורשה, and linkApprovals' hint
says the opposite thing -- so device 2 is driven on those as well. Eight views.

No defect. 112 token pairs, every one painted in the order it reads in. The four
lines worth opening these dialogs for: promptUrl's host inside its Hebrew
parenthesis; the exit-code dialog's "מכשיר: … · S/N SN-QA-0001", which is a
bullet with Latin on both sides and so the exact case that forced
console-rtl-0811's second correction (dx=+14.7, stays in group C); its
"⁦(1234, 0000)⁩", the date-time shape that shipped twice, here behind the only
explicit U+2066/U+2069 isolates in the console (dx=+31.8); and "1 מתוך 1
מאושרים" at dx=-43.1. אתחל and מחיקה wrap a Hebrew device name in Hebrew prose
and carry no gradable pair at all -- census 0, recorded rather than passed over.

The probe is console-rtl-0811's, unchanged, three groups and both of its
corrections. Only the driver is new: a dialog is graded against its own .modal,
because .modal-bg is a sibling of #app-view and a body-wide walk with a dialog
open re-grades the screen behind it.

What this run had to change is its own assertion and not the console. Copying
"A > 0 && B > 0 && C > 0" from console-rtl-0811 fails here at A 12 · B 0 · C 100.
That is a correct measurement: group B needs a bare digit run beside a field
mark, which is the device card's "🔋 84% · 📱 Lenovo" and appears in no dialog.
So the assertion is now A and C, with B's zero recorded -- and B is proven live
by a control row rather than quietly switched off, because a group with no rows
and a broken classifier look identical from the outside.

Six control rows per combination, appended to a live .modal so they are read on
the same RTL card as everything else: the known-bad 11.8.2026, 4:40:00 flagged;
S/N in C and increasing; a bullet between two bare digit runs moving to B; and
1 מתוך 2 as A, plus the same string under U+202D to show A can fail.

Verified in QA/kiosk/dialogs-rtl-0811/ -- 170/170 in a real Chromium in both
modes at 390px and 1200px, against the warn-ink-0811 stub, which serves the real
server/public/. Six screenshots.

No source change -- this run found nothing to fix, so node --test is unrun and
unchanged, and there is nothing here to deploy. Not deployed: the Railway
service still serves the previous console, as it has since clients-console-0811.

Found and not fixed, so the claim is not larger than the change: three dialogs on
other screens have still never been walked -- clientModal and confirmDeleteClient
on מזהי לקוח, and the הגדרות → משתמשים set, which needs a role: admin stub user
where warn-ink-0811's is owner. And the input limit stands where input-rtl-0811
left it: a Range cannot reach a value, so the five fields in these dialogs are
graded by declaration, not by measurement. All five carry dir="ltr".

The heartbeat for this step is NOT in core.run_progress. The Supabase MCP server
is not connected in this session and the anon key cannot substitute: PostgREST
answers 406 for the core schema, on run_progress and projects alike. The row is
written out as _heartbeat.sql rather than left unrecorded.

No migration. Protected systems (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873)
untouched. Test mode.
