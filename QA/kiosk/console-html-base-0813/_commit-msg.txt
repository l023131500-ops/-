35 kioskfleet: /kiosk/console.html היה כתובת חיה שאי אפשר להתחבר דרכה (#215 — תת-הפריט נסגר, #216 נפתח)

השרת מגיש את הקונסולה בשתי כתובות ושתיהן מחזירות 200 ואת אותם 8,944 בייט, אבל
js/app.js:15 גזר את קידומת ה-API מהכתובת עצמה בהסרת /console בלבד. בכתובת עם
הסיומת ה-BASE יצא /kiosk/console.html וכל קריאה נשלחה ל-/kiosk/console.html/api/…
שמחזיר 404 — כלומר מסך כניסה מלא, שם וסיסמה נכונים, ו«שגיאה בשרת» בתשובה, בזמן
שהשרת עצמו מחזיר 200 לאותה כניסה בדיוק דרך הנתיב השני. דף הנחיתה מקשר
href="console" בלי סיומת, ולכן המסלול הרגיל מעולם לא נשבר; זו כתובת של סימנייה
או הקלדה ידנית, והיחידה שהשרת מגיש ו-JS שלה אינו עובד.

הפריט הזה נרשם ב-#215 כמלכודת מדידה בלבד («שלא ימדדו את הדבר הלא-נכון»). הוא
נמדד שוב לפני שנגעתי בו והתברר כבאג משתמש ולא כהערה: 404 על
/kiosk/console.html/api/config מול 200 על /kiosk/api/config, על אותו שרת ובאותו רגע.

התיקון שורה אחת — /\/console(?:\.html)?\/?$/ — והקבוצה אופציונלית ולכן תואם
לאחור. נבדק ב-node על עשר כתובות לפני הדחיפה: שלוש הצורות של console נגזרות
ל-/kiosk, שלושתן בשורש נגזרות לריק, ו-console.htmlx ו-myconsole אינם נגזרים כלל.

נכתב כעריכה כירורגית על l023131500-ops/zol לפי ההנחיה התפעולית שב-#215 — 35
נבנית משם ולא מהמונו-רפו, וקומיט כאן לבדו היה משנה אפס בייצור. הקובץ שנשלף
מ-zol הושווה למה שהייצור מגיש בפועל לפני העריכה (35,833 בייט בשניהם, מופע אחד
בדיוק של הביטוי) — כלומר נערך מה שרץ ולא עותק שנחשב זהה. zol ed212eb, Railway
d95f15a6 SUCCESS. העותק שבמונו-רפו הוא בנייה אחרת לגמרי (89,844 בייט, #215) אך
מחזיק את אותה שורה במדויק, ותוקן גם הוא כדי שהענף המקביל לא ישא את הבאג הלאה.

אומת בייצור על התשובה החיה ולא על קובץ: app.js 35,844 בייט עם הביטוי החדש; ואז
הזרימה שהייתה שבורה מקצה לקצה בדפדפן — kf_token נמחק, טעינה נקייה של
/kiosk/console.html, admin + הסיסמה מ-core.secrets, לחיצה על כניסה → הטוקן נשמר,
#login-err ריק, h1 = «המכשירים שלי» עם שישה פריטי ניווט. בלי רגרסיה על
/kiosk/console: 200, basePath "/kiosk", אותם שישה פריטים.

נמצא בדרך ולא נבלע (#216): כדור הכניסה המשותף <more30-auth> מכסה את «➕ הוספת
מכשיר» — הפעולה היחידה במסך הראשון, ובמצב ריק גם ההוראה שהמסך עצמו נותן. נמדד
ולא הונח: הכדור l=16 r=92, הכפתור l=34 r=155, ו-elementFromPoint במרכז החפיפה
מחזיר MORE30-AUTH. זה אותו ליקוי שתוקן ב-03/08 על 11 מסלולים דרך
--more30-auth-inset, ו-/kiosk/console לא היה בהם. תיקונו ברמת הפורטל ולא ב-zol,
ולכן צעד אחר.
