kiosk (35): the eighth screen was graded for everything except its colour

apps/35-kioskfleet/STATUS.md item 7 / the dialogs-rtl-admin-0811 entry ended on a
single line: "the admin screen is graded here for painted order and reflow only.
Its contrast is ungraded — the one screen item 6 believed it had covered is the
one it never saw." That is this step, and the screen was not clean.

מחוברים כעת was 2.28:1. viewAdmin() writes the number as
style="color:var(--accent-2)" inline in its own template, and --accent-2 is
#22c55e — a brand token with one value for both modes: 7.47:1 on the dark card
and 2.28:1 on the white one. At 32px/800 it is large text, so the bar is 1.4.3's
3:1 rather than 4.5:1, and it does not clear that either.

It is the --label-ink / --link-ink shape for the fourth time — a fixed value
beside a surface that inverts — and the same call the console already made once:
the wizard's progress fill is #15803d and not --accent-2, for this reason. What
is new is where it was hiding. An inline style= in a JS template is the one place
a stylesheet sweep cannot look, and the two .stat numbers that carry one are also
the console's only 32px text, i.e. the only place the large-text threshold is
what decides.

--ok-ink: #15803d light (5.02:1, measured rather than quoted), #22c55e dark —
the value already there. The defect was light-mode only, so the dark console is
byte-identical. --accent-2 stays the brand's green and stays right as a surface
(.dot.on); it is wrong as text, which is the whole split, the same one --danger /
--danger-ink made. The light value is #15803d because that is --chip-ok-ink and
the wizard's bar: one green in the console rather than a third.

QA/kiosk/admin-contrast-0811 — 82/82 graded rows in a real Chromium at both
colorScheme values against warn-ink-0811's stub with the admin flag, across the
screen and four dialog views (userModal on both branches, since the edit branch
drops two fields and they are two views, plus resetPw and delUser). Ten
screenshots. Four control rows, three of which fail: two injected greys, and the
"before" re-injected per mode, which fails in light at 2.28 and passes in dark at
7.47 — recorded that way in the source so the dark row cannot read as a second
bug. 151/152 on node --test, the documented baseline, unchanged because this
touches no server code.

Two harness errors are recorded rather than quietly fixed: label:has-text() is
Playwright's selector engine and a SyntaxError inside querySelector, where this
probe runs; and deriving a screenshot slug as view.replace(/[^a-z]/gi, '') from a
Hebrew view name yields the empty string, so the first run wrote four files
called dialog.

Found and not fixed: the .stat border is --line (1.14:1 light, 1.42:1 dark),
reported here and not asserted — that is the console-wide --line question every
contrast step so far has left open on purpose, and four boxes on one screen are
not where it gets decided. And --accent-2 is text in one more place,
.plan li::before on the marketing page, which is a different screen.

Not deployed. The source under apps/** is gitignored, so the CSS token and the
template change are recorded in STATUS.md rather than as a diff.
