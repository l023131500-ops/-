# כל פרויקטי Supabase של כל מערכת חיה — 12/08/2026

**מה נמדד.** 26 הכתובות החיות. לכל אחת: ה-HTML שהייצור מגיש → כל קובצי ה-JS
שהוא טוען → **כל** ההתייחסויות ל-`*.supabase.co` בכל אחד מהם, לא הראשונה.
לכל התייחסות: המפתח הקרוב אליה **באותו קובץ**, מאיזה קובץ היא באה, והאם היא
פנייה ל-API או כתובת של נכס סטטי ב-Storage. לכל פרויקט שמופיע בקובצי המערכת
עצמה — `GET /auth/v1/settings`.

קריאה בלבד: לא נוצר משתמש, לא בוצעה התחברות, לא נכתב דבר לשום מסד.
המערכות המוגנות (08, 09, bkalut-app, bkalot-admin, `zr_*`, NEDARIM3873)
אינן ברשימה.

סקריפט: `scripts/qa/multi-project-sweep.mjs` · תוצאות: `_results.json`

---

## למה נכתב

הסריקות של 12/08 (`autoconfirm-sweep`, `own-form-login-roundtrip`) שלפו מכל
מערכת **זוג אחד** URL+key — הקרוב ביותר זה לזה, בקובץ הראשון שיש בו שניהם —
והצמידו למערכת פרויקט אחד. זו הנחה, לא מדידה. `bkalot` (10) הפריכה אותה:

| קובץ | פרויקט |
| --- | --- |
| `/bkalot/repo.js` | `uhnrgujbdxhhmoxcjria` — מאגר הזכויות המרכזי (hub) |
| `/bkalot/app.js` + ה-HTML | `bieebmnmkffwbqlsfozh` — הפרויקט של המערכת |

`repo.js` נטען ראשון, ולכן הסריקה רשמה ל-bkalot פרויקט אחד, סימנה אותו
"לא תואם ל-`core.projects`" — **ושאלה את `/auth/v1/settings` של הפרויקט הלא
נכון**. הקו הפתוח שנרשם בהארטביט הקודם ("10 bkalot serves a key for a different
project than core.projects records") נסגר כאן: **`core.projects` צדק**, ומה
שטעה הוא כלי המדידה.

## מה נמצא

**1. שתיים מ-26 מדברות עם יותר מפרויקט אחד** — ולכן "פרויקט אחד למערכת"
הוא תיאור שגוי של הרשת הזו:

| מערכת | הפרויקט שלה | ובנוסף | מהיכן |
| --- | --- | --- | --- |
| **10 bkalot** | `bieebmnmkffwbqlsfozh` | `uhnrgujbdxhhmoxcjria` | `repo.js` — טעינת מאגר הזכויות |
| **15 egod** | `hkkkynyoigzlttpynoeo` | `uhnrgujbdxhhmoxcjria` | בתוך אותה חבילה `index-*.js` |

**2. ל-bkalot אין אימות משלה כלל.** `app.js` אינו קורא ל-`/auth/v1` אף פעם
(0 מופעים של `signUp`/`signInWith`/`auth.`); הקריאה היחידה למסד שלה היא
`POST /rest/v1/zr_leads`. לכן העובדה ש-`bieebmnmkffwbqlsfozh` דורש אישור מייל
**אינה נוגעת ללקוח של bkalot** — הוא נכנס דרך ווידג'ט הכניסה המשותף של
הפורטל, שיושב על `uhnrgujbdxhhmoxcjria` (אישור אוטומטי דלוק). המסקנה שנרשמה
ל-10 ב-12/08 נשארת נכונה; הדרך שבה הגענו אליה הייתה שגויה.

**3. 12 מ-26 אינן מגישות מפתח Supabase משלהן בכלל** — 02, 03, 04, 14, 18, 26,
27, 28, 32, 34, 35, 40. המפתח היחיד בכל הדף הוא של `/auth-button.js`, ווידג'ט
הכניסה של הפורטל. מבחינת הדפדפן, מערכות אלה אינן מדברות עם שום פרויקט: כל מה
שאינו כניסה עובר דרך ה-API של המערכת בצד השרת.

**4. ממצא שנפסל לפני שדווח.** הגרסה הראשונה של הסקריפט קבעה ש-**27 מחירון**
היא MISMATCH מול `core.projects` — הדף מפנה ל-`bieebmnmkffwbqlsfozh` בעוד
הרישום אומר `csjekrvukbdznetsrodj`. ההפניה הזו היא
`…/storage/v1/object/public/design/bkalot-theme.css`: **גיליון סגנון**, לא
מסד ולא אימות. הסקריפט מסווג עכשיו כל הפניה לפי מה שבא אחרי הדומיין, ו-27
חוזרת להיות "רק ווידג'ט הפורטל". זה לא באג במחירון ולא ברישום.

## מה לא נמדד, ונרשם ככזה

- **מה שקורה בצד השרת.** הסריקה רואה רק את מה שהדפדפן מקבל. מערכת שקוראת
  ל-Supabase מתוך API משלה (27 מחירון, 04 עימוד, 28 קופות) לא תיראה כאן,
  והרישום ב-`core.projects` עבורה אינו נסתר ואינו מאושר על ידי המדידה הזו.
- **האם ה-CSS של 27 מ-Storage בכלל נטען** — לא נבדק כאן.
- **`csjekrvukbdznetsrodj`** נקרא רק כשם פרויקט בהתייחסות מוגשת; לא נגענו
  בסכמות `csj`/`csj_src`/`igud`.
