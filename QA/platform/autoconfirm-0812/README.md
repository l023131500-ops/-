# §1א/§8ב — אישור המייל, נשאל מכל המערכות החיות בבת אחת · 12/08/2026

`node scripts/qa/autoconfirm-sweep.mjs` → `_results.json`

## למה הסריקה הזו

הצעד הקודם (`QA/platform/own-form-login-0812`) שחזר את §1א והוכיח שהסיבה אינה
אי-התאמת hash אלא הגדרה אחת בפרויקט Supabase — `mailer_autoconfirm=false`.
הוא בדק **שלוש** מערכות, כי רק להן טופס כניסה משלהן, והשאיר בכתב שורה פתוחה:
שאר המערכות "probably fine — probably, not measured".

ההגדרה הזו היא של **הפרויקט**, לא של הטופס. לכן אפשר לשאול אותה מכל מערכת
חיה בבת אחת, בלי ליצור אף משתמש: `GET /auth/v1/settings` היא נקודת קצה
ציבורית שכל דפדפן קורא בטעינת האתר.

## מה נמדד

26 כתובות חיות (כל המערכות הלא-מוגנות עם `live=true` ו-`live_url`).
לכל אחת: ה-HTML שהייצור מגיש → חבילות ה-JS שלו → הזוג `SUPABASE_URL` + מפתח
**מתוך אותו קובץ** (`/auth-button.js`, הווידג'ט המשותף של הפורטל, נבדק אחרון) →
`GET /auth/v1/settings` עם אותו מפתח.

קריאה בלבד. **לא** נוצר אף משתמש, לא בוצעה אף התחברות, לא נכתבה שום שורה
לשום מסד. המערכות המוגנות (08/09/`bkalut-app`/`bkalot-admin`/`zr_*`/
`NEDARIM3873`) אינן ברשימה.

## התוצאה — לפי פרויקט, כי זו יחידת התיקון

| פרויקט Supabase | אישור מייל אוטומטי | הרשמה | מערכות חיות שנשענות עליו |
| --- | --- | --- | --- |
| `uhnrgujbdxhhmoxcjria` | ✅ דלוק | פתוחה | 16 מערכות (02, 03, 04, 10, 14, 16, 18, 26, 27, 28, 32, 33/פורטל, 34, 35, 36, 40) |
| `bieebmnmkffwbqlsfozh` | ❌ **כבוי** | פתוחה | 01 torah |
| `csjekrvukbdznetsrodj` | ❌ **כבוי** | פתוחה | 06 briut · 12 smel · 17 chizukim |
| `hkkkynyoigzlttpynoeo` | ❌ **כבוי** | פתוחה | 15 egod |
| `aypsqqvfohekxxuqsmrw` | ❌ **כבוי** | פתוחה | 21 mthbram |
| `mwljkonwdeuaahsigjdp` | ❌ **כבוי** | פתוחה | 24 galil |
| `jhbeelzvjvhnkxldqvxx` | ❌ **כבוי** | פתוחה | 30 crm |
| `ygaqqnuyfnumezxxmtbh` | ❌ **כבוי** | פתוחה | 31 gesher |
| `trerolyveytzgksawrme` | דלוק | ❌ **סגורה** | 22 zchuyot |

**תשע מערכות חיות דורשות אישור מייל** — 01, 06, 12, 15, 17, 21, 24, 30, 31 —
על שבעה פרויקטים. בכל אחת מהן: הרשמה מחזירה 200 ויוצרת משתמש בלי session,
וההתחברות מיד אחריה נכשלת ב-`email_not_confirmed`, שלקורא עברית נראה בדיוק
כמו "סיסמה שגויה". זו התלונה ב-§1א מילה במילה, וזו גם הפרה של §8ב.

**מערכת אחת — 22 zchuyot — סגורה להרשמה לגמרי** (`disable_signup=true`).
שם §8ב לא יכול לרוץ בכלל: לקוח חדש אינו יכול להיפתח חשבון, ולא משנה מה המסך
מציע. זה עקבי עם `NEEDS_USER §0ש` (משתמש הבדיקה חסום רק ב-22), אבל הוא רחב
ממנו — לא רק משתמש הבדיקה חסום שם, אלא **כל** משתמש חדש.

## השורה הפתוחה של הצעד הקודם — נסגרת

"16 המערכות שנשענות על כניסת הפורטל מעולם לא נבדקו, וכנראה בסדר." **עכשיו זה
נמדד ולא 'כנראה':** כולן יושבות על `uhnrgujbdxhhmoxcjria`, שם האישור האוטומטי
דלוק וההרשמה פתוחה. הסבב הרשמה→התחברות יכול לעבור בכולן.

## מה הסריקה הזו לא אומרת

- **`match=false` איננו "הרישום שגוי".** 11 מערכות (02, 03, 04, 14, 18, 26,
  27, 28, 32, 34, 35, 36, 40) לא נושאות מפתח Supabase משלהן כלל — הזוג נמצא
  רק ב-`/auth-button.js`, הווידג'ט המשותף. כלומר: חשבון הלקוח שלהן חי בפרויקט
  הפורטל, גם אם `core.projects.supabase_project` רושם פרויקט אחר (שם יושבים
  הנתונים שלהן, לא האימות). העמודה `from_shared_widget` ב-`_results.json`
  מפרידה בין השניים.
- **חריג אחד שכן שווה מבט:** 10 bkalot נושא מפתח משלו ב-`/bkalot/repo.js`,
  והוא של `uhnrgujbdxhhmoxcjria` — בעוד `core.projects` רושם לו
  `bieebmnmkffwbqlsfozh`. זו סתירה בין הרישום למה שמוגש, ולא נבדקה כאן.
- **לא נבדק מה כל מסך מציג** כשהאישור נדרש. זה נמדד רק ב-01 ו-15 בצעד הקודם.
  ב-01 ההודעה בעברית **חיה בייצור** — נמשכה היום מ-
  `/torah/assets/authErrors-COw_Oh82.js` והיא מכילה את «המייל עדיין לא אומת…
  הסיסמה עצמה תקינה». (`NEEDS_USER §0א״` אמר קודם שהיא חסרה; זה נכון היה
  לגבי החבילה הראשית, שאינה נושאת את מסך הכניסה.) לשבע המערכות האחרות זה
  **לא נמדד** ונרשם ככזה.
- **לא נבדק אם הרשמה בפועל מצליחה** באף מערכת כאן — הסריקה קוראת הצהרה של
  השרת, לא מריצה סבב. הסבב עצמו רץ בצעד הקודם על שלוש מערכות בלבד.

## בעלוּת

כיבוי Confirm email הוא הגדרה בלוח הבקרה של Supabase, פר-פרויקט. מתוך שמונת
הפרויקטים שאינם `uhnrgujbdxhhmoxcjria`, **אף אחד** אינו נראה מהסביבה הזו
(`list_projects` מחזיר פרויקט אחד). לכן זה של המשתמש — `core.issues #172`,
`NEEDS_USER §0א״`.

מה שכן שלי: אם המשתמש בוחר להשאיר אימות מייל, המסך של כל אחת מתשע המערכות
צריך לומר את האמת במקום "סיסמה שגויה". 01 כבר אומר; השאר לא נמדדו.
