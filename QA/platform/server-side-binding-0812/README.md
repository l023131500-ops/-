# החיבור לצד השרת של 12 המערכות שאינן מגישות מפתח — 12/08/2026

**מה נמדד.** `QA/platform/multi-project-0812` מצא ש-12 מ-26 המערכות החיות
אינן מגישות מפתח Supabase לדפדפן, ורשם במפורש שהרישום ב-`core.projects`
עבורן **אינו נסתר ואינו מאושר** — כי הכל אצלן עובר בצד השרת. כאן נשאלה
סביבת הייצור עצמה: `vercel env pull --environment=production` לכל 11
המערכות שיושבות על Vercel (35 קיוסק יושבת על Railway ונשארת מחוץ).

קריאה בלבד: לא נפרס דבר, לא נכתב דבר ל-Vercel/Supabase/אתר, ולא נוצר משתמש.

סקריפט: `scripts/qa/server-side-binding-sweep.mjs` · תוצאות: `_results.json`

---

## המגבלה שנמדדה, וזו התוצאה העיקרית

**Vercel מסמן את רוב משתני ה-Supabase כ-"sensitive", והערך שלהם אינו ניתן
לקריאה מכאן.** ה-CLI מחזיר `"[SENSITIVE]"` במקום הערך — 13 תווים, זהה בכל
המערכות. זה לא באג בסקריפט ולא הרשאה חסרה; זה מה ש-Vercel מבטיח על משתנה
שנוצר כרגיש: הערך נכתב פעם אחת ולא נקרא שוב, לא ב-CLI ולא ב-API.

**המשמעות ל-`core.projects`:** עבור 9 מ-11 המערכות אפשר למדוד ש**יש** חיבור
Supabase בצד השרת, אבל **אי אפשר למדוד לאיזה פרויקט**. הרישום שלהן נשאר
לא-מאומת, וזה מצב קבוע כל עוד המשתנים מסומנים כרגישים. אין טעם שסבב הבא
ינסה שוב מאותו כיוון.

## הטבלה

| # | מערכת | vars | תוצאה | פרויקט שנמדד | רישום |
| --- | --- | --- | --- | --- | --- |
| 02 | tamlul | 4 | חיבור קיים, ערך חסום | — | `bieebmnmkffwbqlsfozh` |
| 03 | modaot | 4 | חיבור קיים, ערך חסום | — | `bieebmnmkffwbqlsfozh` |
| 04 | imud | 4 | חיבור קיים, ערך חסום | — | `uhnrgujbdxhhmoxcjria` |
| 14 | smachot | **0** | **אין חיבור בכלל** | — | `null` ✔ תואם |
| 18 | orech | 6 | חיבור קיים, ערך חסום | — | `bieebmnmkffwbqlsfozh` |
| 26 | studio | 2 | חיבור קיים, ערך חסום | — | `uhnrgujbdxhhmoxcjria` |
| 27 | mechiron | 2 | חיבור קיים, ערך חסום | — | `csjekrvukbdznetsrodj` |
| 28 | kupot | 3 | חיבור קיים, ערך חסום | — | `uhnrgujbdxhhmoxcjria` |
| 32 | nadlan | 3 | חיבור קיים, ערך חסום | — | `uhnrgujbdxhhmoxcjria` |
| 34 | kesef | **0** | **אין חיבור בכלל** | — | `uhnrgujbdxhhmoxcjria` ✖ |
| 40 | gannenet | 3 | **ניתן לקריאה** | `uhnrgujbdxhhmoxcjria` | `null` ✖ |

## שלושה דברים שכן נמדדו

**1. gannenet (40) — הרישום היה ריק, והחיבור אמיתי.** `SUPABASE_URL` בייצור
אינו מסומן רגיש ונקרא במלואו: `https://uhnrgujbdxhhmoxcjria.supabase.co`.
`core.projects.supabase_project` היה `NULL`. **תוקן ל-`uhnrgujbdxhhmoxcjria`**
מתוך מדידה, לא מתוך הנחה.

**2. kesef (34) — הרישום אומר פרויקט, בייצור אין ולו משתנה אחד.**
`kesef-more30` מחזיק **0** משתני סביבה משלו (רק אלה ש-Vercel מזריק), ויש לו
חמש פריסות production, האחרונה READY. `more30.com/kesef` מחזיר 200 ואינו מגיש
שום מפתח Supabase. במקביל, בפרויקט המרכזי יש סכמת `kesef` עם **38 טבלאות**.
כלומר: הנתונים קיימים באמת, **והאתר החי אינו יכול לקרוא אותם בזמן ריצה** —
מה שמוצג שם מגיע מהבנייה או שאינו מגיע מהסכמה הזו כלל. זה קו פתוח אמיתי
על 34, לא טעות רישום. `core.projects` לא שונה עבורה: המדידה מראה שהחיבור
חסר, לא שהפרויקט שגוי.

**3. orech (18) מדברת עם שני פרויקטים** — יש לה `NEXT_PUBLIC_SUPABASE_URL`
וגם `NEXT_PUBLIC_HUB_SUPABASE_URL` בנפרד. זה מצטרף ל-10 ול-15 מהסריקה
הקודמת: "פרויקט אחד למערכת" שגוי לפחות בשלוש מערכות.

## מה לא נמדד, ונרשם ככזה

- **זהות הפרויקט של 02, 03, 04, 18, 26, 27, 28, 32** — חסומה לצמיתות מכאן
  (ראו למעלה). ניתן לפתיחה רק ע"י המשתמש בדשבורד Vercel.
- **35 kioskfleet** יושבת על Railway ולא נמדדה כאן.
- **מה 34 kesef כן מציגה** — נמדד רק שהעמוד מחזיר 200 ואין בו מפתח; לא נבדק
  אילו נתונים מוצגים בו ומאיפה.
- **מפתח ה-service_role של gannenet** באורך 41 תווים (הפורמט החדש
  `sb_secret_…`, לא JWT). לא נבדק שהוא תקף — רק שהוא קיים.
