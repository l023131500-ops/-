# index-route-recheck-0817 — 30 CRM + 31 gesher, "מסלול index ריק" נבדק מחדש

תאריך: 17/08/2026. שורה בטבלת "מה נשאר לתקן" ב-`SYSTEMS_STATUS.md` (בלי תאריך מדידה):
`30 CRM זכויות | מסלול index ריק + React #418 (hydration mismatch) ב-dashboard | בינוני`
`31 גשר עברית | מסלול index ריק | בינוני`

## מה נבדק
Playwright, 1280×900, cache-buster, שני מסלולים לכל מערכת (`/crm` ו-`/crm/`,
`/gesher` ו-`/gesher/`), console messages (warning+), `document.body.innerHTML.length`.

## תוצאה
| מערכת | כתובת | הפניה בפועל | `<title>` | body length | שגיאות קונסולה |
|---|---|---|---|---|---|
| 30 CRM | `/crm` ו-`/crm/` | → `/crm/auth` | כניסה למערכת \| זכויות פרו | 7,862 | 0 |
| 31 גשר | `/gesher` ו-`/gesher/` | → `/gesher/auth` | התחברות — מערכת CRM שותפים | 8,353 | 0 |

שתי המערכות מציגות מסך כניסה מלא (טופס, כותרת, h1) — לא מסך ריק, ואין שגיאת
hydration (#418 או אחרת) בקונסולה. `#root` אינו קיים בשם הזה ב-CRM (SSR/hydration
אחר), אבל `document.body` מלא ותקין.

## מסקנה
שתי הרשומות אינן פער פעיל כרגע. לא ידוע מתי בין רישומן לעכשיו הן נסגרו — לא
נחקר, כי זה לא משנה את המצב הנוכחי (אותה גישה כמו הרשומות שנבדקו ב-17/08 הקודמות).
לא נבדק: מסך ה-dashboard אחרי כניסה אמיתית (דורש session) — הטענה המקורית על
React #418 הייתה ספציפית ל-dashboard, לא למסלול index/auth שנמדד כאן. אם התקלה
עדיין קיימת היא נמצאת מעבר להתחברות, לא במסלול הריק שהרשומה תיארה.

ראיות: `crm-auth.png`, `gesher-auth.png` (צילומי מסך מלאים, לא ריקים — 200,658
ו-421,576 בייט).
