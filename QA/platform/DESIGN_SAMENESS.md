# §6 — "כל האתרים נראים אותו סגנון של AI": מה נמדד באמת

> נמדד 05/08/2026 מול הפרודקשן, 25 נתיבים, דפדפן אמיתי ב-1280×900.
> הכלי: `scripts/qa/design-fingerprint.mjs` · הנתונים: `QA/platform/_design.json`.

## למה נמדד לפני שנגעו במשהו

התלונה ב-`more30priority.md` §6 היא **התרשמות**, וזו התרשמות סבירה לגמרי.
אבל שכתוב של 24 עיצובים לפי התרשמות הוא הדרך לבזבז שבוע ולא לשנות את מה
שבאמת הפריע. לכן קודם מדדנו מה זהה, ורק אז נקבע במה לגעת.

## מה שהתברר: זה **לא** צבע וזה **לא** טיפוגרפיה

| מדד | תוצאה |
|---|---|
| חתימות עיצוב שונות (גופן + רדיוס + צל) | **24 מתוך 25** |
| גופני כותרת שונים | **7** |
| רדיוס משטח | 12px עד 24px, מפוזר |

כלומר המערכות **כבר** שונות זו מזו בפלטה, בגופן ובצורה. פלטה חדשה לכל אתר
לא הייתה משנה דבר — הבעיה לא שם. זו התוצאה שהפכה את הסעיף הזה ממשימת
"תעצב מחדש" למשימה ממוקדת.

## מה כן חוזר: **ערכת הרכיבים**, לא הסגנון

מה שמזהים לפני שקוראים מילה הוא הקיט — אותם שמות טוקנים של shadcn, אותה
ספריית אייקונים (lucide), אותו משטח `rounded-2xl` עם `backdrop-blur`:

| סימן | כמה מערכות |
|---|---|
| שמות טוקנים של shadcn (3+) | 13 / 25 |
| אייקוני lucide | 14 / 25 |
| `rounded-xl` / `2xl` / `3xl` | 15 / 25 |
| משטחי `backdrop-blur` | 13 / 25 |

**זו התשובה ל-§6, והיא רשימה שמית ולא תחושה.**

### 9 מערכות עם הקיט המלא (4/4) — כאן הריח חזק ביותר

`imud` · `smel` · `egod` · `chatzor` · `chizukim` · `zchuyot` · `mthbram` ·
`galil` · `gesher`

חלקן קיצוניות: `galil` מציגה 104 אייקוני lucide ו-39 פינות `rounded-*`;
`chizukim` 81 אייקונים; `zchuyot` 58 אייקונים ו-39 פינות. ריבוי אייקונים
גנריים הוא בעצמו חתימת "נוצר אוטומטית" — הוא ממלא כל כרטיס בסמל דקורטיבי
שלא נושא מידע.

### 5 מערכות עם קיט חלקי (2–3/4)

`torah` · `studio` · `mechiron` · `kupot` · `crm` · `tamlul` · `nadlan`

### 9 מערכות ללא הקיט כלל (0/4) — אלה כבר בעלות זהות משלהן

`modaot` · `briut` · `bkalot` · `smachot` · `orech` · `kesef` · `kiosk` ·
`tivuch` · `portal`

אין מה "לתקן" בהן לפי §6. `orech` היא דוגמה טובה: גופן אחיד `Frank Ruhl
Libre`, פלטת נייר חמה, אפס אייקוני lucide — היא נראית כמו מערכת עריכה
תורנית ולא כמו תבנית.

## עדכון 05/08/2026 — הקשת נמדדה נכון, והרשימה התקצרה

המדד הראשון ספר **מחרוזות צבע** שונות בצ׳יפים של האייקונים, וזה היה מדד גס
מדי. הוא סימן ארבע מערכות, ושתיים מהן היו תקינות: מה שנראה כמו ארבעה צבעים
היה אותו צבע מותג בארבע שקיפויות. `bg-primary/10` ו-`bg-primary/20` הן שתי
מחרוזות ואותה החלטה עיצובית.

המדד סופר עכשיו **גוונים** (hue, בסלים של 30°), ומדלג על אפור, לבן ושקוף
שאין להם גוון. גוון הוא מה שמפריד בין "מותג אחד בשימוש זהיר" לבין "שמונה
צבעים שאיש לא בחר": גווני מותג חולקים גוון, קשת לא.

| מערכת | לפני | אחרי | מה זה היה |
|---|---|---|---|
| `galil` | 8 מדרגים | **1 גוון** | קשת אמיתית — תוקנה |
| `zchuyot` | 11 מחרוזות | **2 גוונים** | קשת אמיתית — תוקנה. 2 = מותג + הדגשה |
| `egod` | 4 מחרוזות | **1 גוון** | ❌ מעולם לא הייתה קשת — ארבע שקיפויות של צבע אחד |
| `chatzor` | — | 2 גוונים | מותג + הדגשה, תקין |

**אפס מערכות עם 3+ גוונים.** כלומר החלק הזה של §6 הושלם: שתי הקשתות
האמיתיות טופלו, והשאר לא היו בעיה מלכתחילה.

> זו הפעם השביעית בסבב הזה שממצא התברר כשגיאת מדידה. הדפוס עקבי מספיק כדי
> להיות כלל: **לפני שמתקנים 24 אתרים לפי מדד, בודקים את המדד על אתר אחד
> שכבר תוקן.**

---

## עדכון 05/08/2026 — ספירת האייקונים מגזימה בחומרה, לפחות ב-`galil`

`galil` הובילה את סדר העבודה עם הספירה הגבוהה ביותר. נמדד איפה האייקונים
**באמת** יושבים, לפני שנגעו בהם:

| מדד | ערך |
|---|---|
| סה"כ SVG בעמוד | 104 |
| מתוכם בסקשן אחד (`section.container`) | **70** |
| משטחי `rounded-xl/2xl/3xl` | 39 |

הסקשן הזה הוא **מדריך בתי הכנסת**. האייקונים שם אינם קישוט: כל כרטיס מציג
`MapPin` לכתובת, `Clock` לשעות ו-`Phone` לטלפון — אייקון אחד לכל שדה נתונים,
חוזר על פני עשרות רשומות. בקוד כל אחד מהם מופיע **פעם אחת**; הריבוי הוא מספר
הרשומות, לא מספר ההחלטות העיצוביות.

כלומר הכלל "להסיר אייקון דקורטיבי שאינו נושא מידע" פשוט אינו חל כאן. הסרתם
הייתה מורידה מידע ממדריך, לא רעש מתבנית. **ספירת אייקונים גולמית אינה מדד
לרעש עיצובי בדף שהוא בעיקרו רשימת רשומות.**

זו הפעם השמינית בסבב הזה שממצא מבוסס-מדד דרש תיקון לפני פעולה — והפעם הוא
נתפס לפני השינוי ולא אחריו.

**המסקנה לסדר העבודה:** `galil` יורדת מראש הרשימה. מי שממשיך צריך למדוד קודם
כמה מהאייקונים בעמוד הם לכל-רשומה ולא לכל-החלטה, ולהתחיל ממערכת שבה הריבוי
נובע מהתבנית עצמה.

> ✅ נמדד — ראו "עדכון 10/08/2026" למטה. ההוראה הזו בוצעה על שמונה מערכות,
> והדירוג הגולמי התברר כהפוך כמעט לגמרי.

---

## עדכון 10/08/2026 — סדר העבודה המתוקן, נמדד ולא משוער

העדכון הקודם הוריד את `galil` מראש הרשימה והשאיר הוראה מפורשת למי שממשיך:
למדוד קודם כמה מהאייקונים הם **לכל-רשומה** ולא **לכל-החלטה**. זה נמדד עכשיו,
על שמונה מהמערכות ולא על אחת: `scripts/qa/icon-decisions.mjs` ·
`QA/platform/_icon-decisions.json`.

הקיבוץ מבני ולא ויזואלי. לכל SVG נבנית חתימה משם האייקון ומשרשרת האבות שלו
(תג + מחלקות, חמש רמות). מופעים של אותו רכיב בתוך רשימה חולקים שרשרת זהה
ומתקבצים לערך אחד; אייקון שהונח ביד בהירו מחזיק שרשרת שאיש אינו חולק ועומד
לבדו. **`decisions` הוא מספר ההנחות שאדם בחר. `rendered` הוא גודל הנתונים.**

> ⚠️ **המספרים בטבלה הזו מנופחים.** החתימה כללה את שם האייקון, ולכן אייקון
> שמגיע מהנתונים נספר פעם לכל רשומה. ראו "עדכון 10/08/2026 (ב)" למטה —
> המספרים המתוקנים שם, והסדר השתנה.

| מערכת | החלטות | מוצגים | % מהרשומות | הכבדים ביותר |
|---|---|---|---|---|
| `zchuyot` | **43** | 52 | 17% | chevron-down×8 |
| `egod` | **32** | 34 | 6% | quote×3 |
| `galil` | **29** | 68 | 57% | clock×15 · send×7 |
| `mthbram` | **21** | 24 | 13% | filter×3 |
| `chatzor` | 13 | 23 | 43% | map-pin×4 · navigation×4 |
| `smel` | 12 | 20 | 40% | check×5 |
| `chizukim` | 9 | **81** | 89% | clock×25 · coins×25 |
| `imud` | 8 | 8 | 0% | — |
| `gesher` | 1 | 1 | 0% | — |

**הדירוג הגולמי היה הפוך כמעט לגמרי.** שתי המערכות שהובילו אותו נופלות:

- `chizukim` הייתה שנייה עם 81 אייקונים, והיא **שביעית** — תשע החלטות בלבד,
  ‏89% מהמוצג הוא clock/coins/chevron שחוזרים 25 פעם בתוך רשימת רשומות אחת.
  אותה צורה בדיוק שנתפסה ב-`galil`, במערכת אחרת.
- `galil` יורדת לשלישית ולא נעלמת: 29 החלטות זה עדיין מספר גבוה, גם אחרי
  שהמדריך נוכה. ההערכה הקודמת ("מדריך רשומות") הייתה נכונה לגבי 57% ממנה.

ומי שעולה לראש לא היה בשלושת הראשונים קודם:

- **`zchuyot` ראשונה — 43 החלטות.** לא רשימה: 17% בלבד מהמוצג מגיע מחזרה.
  ארבעים ושלוש הנחות נפרדות בעמוד אחד הן בדיוק מה שנקרא "נוצר אוטומטית".
- **`egod` שנייה — 32 החלטות מתוך 34 מוצגים, 6% חזרה.** זו הצורה הקיצונית:
  כמעט כל אייקון בעמוד הוא הנחה נפרדת. `egod` היא גם המערכת שבעדכון הקודם
  התבררה כ**לא** בעלת קשת צבעים — כלומר הבעיה שלה מעולם לא הייתה צבע, וכאן
  היא נמדדת במקום שבו היא באמת יושבת.

`imud` ו-`gesher` יורדות מסדר העבודה הזה לגמרי: שמונה אייקונים ואחד. הן
נספרו 4/4 על הקיט בגלל שמות טוקנים, פינות ובלור — לא בגלל אייקונים, ולכן
כלל ההפחתה של §6 אינו חל עליהן. זה לא אומר שהן שונות זו מזו; זה אומר
שהמדד הזה אינו זה שיפריד ביניהן.

**סדר העבודה ל-§6 מכאן: `zchuyot` → `egod` → `galil` → `mthbram`.**
ארבע אלה מחזיקות 125 מתוך 168 ההחלטות שנמדדו. החמש הנותרות אינן משתלמות.

---

## עדכון 10/08/2026 (ב) — האייקון שמגיע מהנתונים נספר פעם לכל רשומה

העדכון הקודם סיים באזהרה: לפני שמסירים אייקון, לבדוק בעמוד עצמו שהוא באמת
דקורטיבי. הבדיקה הזו נעשתה על `zchuyot`, שהובילה עם 43, **לפני** שנגעו בה —
והיא עצרה את השינוי.

עשרים כרטיסי הקטגוריה ב-`zchuyot` מציירים עשרים גליפים שונים (Shield ·
Landmark · Home · Bus · Briefcase …) דרך **הנחה אחת בקוד**:

```tsx
// apps/22-get-your-rights/src/components/RightsCategories.tsx:210
<cat.icon className="w-7 h-7 text-white" />
```

`rightsData.ts` מחזיק 20 קטגוריות, לכל אחת שדה `icon`. מה שהמעצב בחר כאן הוא
דבר אחד — "לכל קטגוריה סימן משלה" — ומה שמשתנה בין הכרטיסים הוא **נתון**.
זו בדיוק הצורה שהעדכון של 05/08 תפס ב-`galil` (MapPin לכתובת, Clock לשעות),
רק שכאן היא חמקה מהכלי: החתימה כללה את **שם** האייקון, ולכן עשרים גליפים
באותה משבצת נספרו כעשרים החלטות ולא כאחת.

**התיקון בכלי:** החתימה היא שרשרת האבות + **מספר המשבצת** בתוך ההורה, בלי
שם האייקון. אייקון מהנתונים נשאר באותה משבצת ומתקבץ לאחד; שני אייקונים
שהם באמת שתי בחירות יושבים במשבצות שונות (clock במשבצת 0, coins במשבצת 1)
ונשארים שניים. `named` שומר את הספירה הישנה כדי שהפער יהיה גלוי.

| מערכת | החלטות (מתוקן) | היה (`named`) | מוצגים | משבצות מהנתונים |
|---|---|---|---|---|
| `zchuyot` | **24** | 43 | 52 | 7 (הגדולה: 8 שמות במשבצת אחת) |
| `egod` | **17** | 32 | 34 | 4 (הגדולה: 12 שמות במשבצת אחת) |
| `mthbram` | **17** | 21 | 24 | 2 |
| `galil` | 14 | 29 | 68 | 4 (הגדולה: 8 שמות במשבצת אחת) |
| `chatzor` | 11 | 13 | 23 | 2 |
| `smel` | 8 | 12 | 20 | 2 |
| `chizukim` | 6 | 9 | 81 | 3 |
| `imud` | 5 | 8 | 8 | 1 |
| `gesher` | 1 | 1 | 1 | 0 |

**אימות:** עמודת `named` מחושבת בריצה הזו ומחזירה **בדיוק** את תשעת המספרים
של הטבלה הקודמת (43 · 32 · 29 · 21 · 13 · 12 · 9 · 8 · 1). כלומר תוספת
המשבצת לא פיצלה אף קבוצה, וכל ההפרש בין שתי הטבלאות מקורו בקיבוץ לפי שם —
ולא בסחיפה של המדידה.

**מה שהשתנה בסדר העבודה:** `zchuyot` נשארת ראשונה, אבל היתרון שלה נשחק —
היא הייתה 43 מול 32, והיא 24 מול 17. `mthbram` עולה משלישית-נמוכה לשוויון
עם `egod`, כי כמעט כל האייקונים שלה באמת הונחו ביד. `galil` יורדת לרביעית:
מ-29 ל-14, ו-79% ממה שמוצג בה מגיע מרשומות — הקריאה של 05/08 ("מדריך
רשומות") הייתה נכונה יותר ממה שהמספר המתוקן הקודם הראה.

**סדר העבודה ל-§6 מכאן: `zchuyot` → `egod` ≈ `mthbram` → `galil`.**

זו הפעם התשיעית בסבב הזה שממצא מבוסס-מדד דרש תיקון לפני פעולה, והשנייה
שהוא נתפס לפני השינוי ולא אחריו. האזהרה שנכתבה בסוף הסבב הקודם עבדה.

> להמשך: 24 ההחלטות של `zchuyot` הן המספר לעבוד מולו. שבע מהן הן משבצות
> שהגליף בהן מגיע מהנתונים (קטגוריות, ערוצי יצירת קשר, יתרונות) — אלה
> נושאות מידע ואינן מועמדות להסרה. מה שנשאר לבדיקה ידנית הוא ההנחות
> החד-פעמיות, ואת אלה צריך לראות בעמוד לפני שנוגעים.
>
> ✅ בוצע — ראו "עדכון 10/08/2026 (ג)" למטה. חמש-עשרה ההנחות נראו בעמוד,
> ורק שתיים מהן שרדו כמועמדות.

---

## עדכון 10/08/2026 (ג) — חמש-עשרה ההנחות של `zchuyot`, אחת-אחת

ההוראה שנכתבה למעלה הייתה "לראות בעמוד לפני שנוגעים". היא בוצעה מכנית ולא
בעין: `icon-decisions.mjs --list` מדפיס לכל משבצת את **הסקשן שבו היא יושבת
ואת הטקסט שצמוד אליה**, ולכן אפשר לשפוט כל אייקון מול מה שכתוב לידו במקום
מול רשימת ה-import. הגיליון: `QA/platform/_icon-slots-zchuyot.md`.

**המדידה מאשרת את הטבלה הקודמת בדיוק:** 24 החלטות · 52 מוצגים · `named`=43.
שבע משבצות מהנתונים, חמש-עשרה הנחות חד-פעמיות, ושתי חזרות בתוך רשימה
(`chevron-down`×8 על הקטגוריות, `arrow-left`×3) שאינן הנחות נפרדות.

### מה שהתברר: הפוטר, לא הגיבור

הסבב הקודם המליץ להתחיל ב-`HeroSection` וב-`StatsSection`. **שתיהן כבר
נקיות** — לגיבור נשארו שלושה אייקוני ערוץ קשר (טלפון · מעטפה · סיכת מפה),
וב-`StatsSection` שלושת הגליפים יושבים במשבצת אחת מהנתונים. עשר מתוך
חמש-עשרה ההנחות החד-פעמיות יושבות ב-`Footer.tsx` ובשני משגרי הצ׳אט הצפים.

### שלוש עשרה מתוך חמש-עשרה **אינן** מועמדות להסרה

| מה | למה נשאר |
|---|---|
| `search` בשדה החיפוש (23px, בלי טקסט צמוד) | אין תווית אחרת. הסרה = שדה בלי שם |
| `chevron-down` ליד "עוד 12 קטגוריות" | מצב פתוח/סגור, לא קישוט |
| `arrow-left` ×3 על כפתורי הפעולה | סימן כיוון על CTA |
| `map-pin` ×2 ליד "עמדות נדרים פלוס" | מקום פיזי; כבר נשקל ואושר בסבב הגיבור |
| `phone` · `mail` · `send` · `clipboard-list` בפוטר | ערוץ קשר או פעולה, אחד לכל שדה |
| `Heart` ב"נבנה עם ♥ למען הקהילה" (`Footer.tsx:296`) | **הגליף הוא מילה במשפט.** בלעדיו נשאר "נבנה עם למען הקהילה". זה פריט קופי, לא פריט אייקון — ואינו נמחק בלי לכתוב את השורה מחדש |

### שתיים ששרדו

1. **אמוג׳י שמצייר בדיוק את האייקון שלידו.** `AIAgent.tsx:122` מציג
   `<MessageCircle />` בתוך כפתור המשגר, ו-`AIAgent.tsx:126` מסיים את הטקסט
   באותו כפתור ב-💬 — אותו סמל פעמיים, בהפרש של כארבעים פיקסל, באותו פקד.
   ‏DESIGN_STANDARD §2 כבר אוסר אמוג׳י בממשק (אותו כלל שהוחל על הכפתורים
   ב-`HeroSection` ועל שלושת האמוג׳ים ב-`Footer`), וכאן הוא גם כפילות ולא
   רק חריגת סגנון.

2. **שלושה גליפים של טלפון בפוטר אחד.** ‏`PhoneCall` בצ׳יפ "מערכת קולית"
   (`Footer.tsx:61`), `Phone` על המספר (`:74`), ו-`PhoneCall` שוב על אותו
   מספר עצמו ברצועת יצירת הקשר (`:265`). שני האחרונים נושאים את **אותו
   נתון** — 02-3131500 — בשני גליפים שונים. זו החתימה של "כל כרטיס צריך
   סמל" בצורתה הנקייה.

> ✅ בוצע — ראו "עדכון 10/08/2026 (ד)" למטה. שני התיקונים במקור, נבנו ונפרסו.
> **אבל התחזית שכתובה כאן — "לראות את 24 יורד" — לא התקיימה, וזו לא תקלה.**

**הבא בתור:** שני התיקונים האלה במקור, ואז בנייה ופריסה של `/zchuyot`
וריצה חוזרת של `--list` כדי לראות את 24 יורד. אזהרה למי שממשיך: הגיליון
מודד את **הייצור**, ולכן ריצה חוזרת לפני פריסה תחזיר את אותם 24 ותיראה
ככישלון של התיקון.

## עדכון 10/08/2026 (ד) — שני התיקונים נפרסו, והמדד לא זז. הסיבה מבנית.

שני התיקונים שהסבב הקודם בידד בוצעו במקור, נבנו ונפרסו לייצור:

1. `AIAgent.tsx:126` — ה-💬 הוסר מטקסט כפתור המשגר. ה-`<MessageCircle />`
   שארבעים פיקסל ממנו נשאר; עכשיו הסמל מופיע פעם אחת בפקד ולא פעמיים.
2. `Footer.tsx:265` — הגליף על 02-3131500 ברצועת יצירת הקשר הוחלף
   מ-`PhoneCall` ל-`Phone`. אותו מספר נושא עכשיו את אותו גליף בשני
   המקומות שבהם הוא מופיע, ו-`PhoneCall` נשאר רק לצ׳יפ שמדבר על המערכת
   הקולית עצמה — כלומר גליף אחד לכל משמעות, במקום שני גליפים לנתון אחד.

**נמדד בייצור אחרי הפריסה** (DOM חי, `?cb=`, ‏`assets/index-0ikxyjGB.js`):
`lucide-phone-call` ‏3→2 · `lucide-phone` ‏2→3 · 💬 ב-`innerText`: אין.
צילום: `QA/platform/zchuyot-launcher-after.png`.

**ובכל זאת `decisions` נשאר 24, ו-`lucide` נשאר 54.** ריצה חוזרת של
`icon-decisions.mjs /zchuyot` אחרי הפריסה מחזירה `_icon-decisions.json`
זהה בית-בבית לקודם. זה נראה ככישלון של התיקון והוא לא:

- **המדד סופר משבצות, לא מופעים.** החלפה של מופע אחד בין `phone`
  ל-`phone-call` לא מוחקת משבצת — **שתי המשבצות היו בשימוש גם לפני כן**
  (`phone-call` יושב גם בצ׳יפ "מערכת קולית" וגם ב-`contactMethods`).
  סכום ה-54 לא זז מאותה סיבה בדיוק: החלפה אינה הסרה.
- **אמוג׳י אינו lucide.** ה-💬 מעולם לא נספר, ולכן הסרתו לא יכלה להוריד
  את המספר. האזהרה שנכתבה בסבב הקודם ("ריצה לפני פריסה תיראה ככישלון")
  כיוונה למקום הלא נכון — הבעיה לא הייתה תזמון הפריסה אלא זה שהמדד
  פשוט אינו מודד את מה ששני התיקונים תיקנו.

**מה זה אומר להמשך:** `decisions` הוא מד לצפיפות אוצר-המילים של הגליפים,
לא מד לכפילות ולא מד לניקיון. הוא ירד רק כשגליף **יוצא מהמערכת כולה**,
ואת שני הליקויים האלה — סמל כפול באותו פקד, ושני סמלים לאותו נתון — הוא
לא רואה בכלל. מי שממשיך: אל תדרג את העבודה לפי הירידה שלו.

## מה זה אומר על העבודה עצמה

1. **לא לגעת ב-9 שכבר יש להן זהות.** שינוי שם הוא נזק, לא שיפור.
2. **להתחיל מ-9 בעלות הקיט המלא** — אבל **לא** לפי ספירת האייקונים הגולמית.
   ראו העדכון למעלה: ב-`galil` הספירה נבעה ממדריך רשומות ולא מקישוט.
   הסדר המתוקן נמדד ב-10/08 ותוקן שוב באותו יום, אחרי שהתברר שגם ספירת
   ה-`decisions` ספרה אייקון-מנתונים פעם לכל רשומה:
   `zchuyot` → `egod` ≈ `mthbram` → `galil`.
3. **הכיוון הוא הפחתה, לא צביעה.** להסיר אייקון דקורטיבי שאינו נושא מידע,
   לוותר על `backdrop-blur` ששייך לתבנית ולא למותג, ולבחור צורת משטח אחת
   שמתאימה לתחום של אותה מערכת.
4. **הקופי לא נמדד כאן.** "ריח של AI" בטקסט הוא ממד אמיתי שהכלי הזה אינו
   רואה, והוא כנראה חצי מהתלונה. זה דורש קריאה אנושית של הקופי בכל מערכת,
   ולא מדידה.

## איך לשחזר

```bash
node scripts/qa/design-fingerprint.mjs            # כל המערכות
node scripts/qa/design-fingerprint.mjs /galil     # אחת

node scripts/qa/icon-decisions.mjs                # תשע בעלות הקיט המלא
node scripts/qa/icon-decisions.mjs /zchuyot       # אחת
node scripts/qa/icon-decisions.mjs /zchuyot --list  # המשבצות עצמן + הטקסט לידן
```

‏`--list` כותב `QA/platform/_icon-slots-<מערכת>.md` ואינו נוגע ב-JSON —
הספירות שם באות מהריצה המלאה, וריצה מסוננת לא תצמצם אותן.

הכלי מודד גם את הסגנון (גופן, פלטה, רדיוס, צל) וגם את הקיט (טוקנים,
אייקונים, פינות, בלור), כי שני אלה נשברים בנפרד — ומדד אחד היה מסתיר את
השני. זו בדיוק הסיבה שהחתימות נראו שונות בזמן שהאתרים נראים דומים.

## עדכון 10/08/2026 (ה) — שני גלאים לליקויים שהספירה עיוורת אליהם

הסבב הקודם תיקן שני ליקויים ב-`/zchuyot` וכל מספר כאן נשאר זהה בית-בבית. זו לא
הייתה תקלה: `decisions` סופר משבצות, ולכן אמוג׳י באותו פקד (טקסט, לא SVG) מעולם
לא נספר, והחלפת גליף בין שתי משבצות קיימות אינה הסרה. `icon-decisions.mjs` מחזיר
מעכשיו שתי רשימות נוספות — `doubleMark` (סמל כפול באותו פקד) ו-`splitGlyph`
(אותו נתון, שני גליפים). הן רשימות של כתובות לפתוח, לא מספרים, והן **אינן** נכנסות
לדירוג `decisions` כדי שהסדר יישאר בר-השוואה בין סבבים.

`splitGlyph` מקבץ לפי הנתון **בתוך ערוץ**, ולא לפי הנתון לבדו. הריצה הראשונה
סימנה את `egod` על 02-3131600 שנושא `phone` במקום אחד ו-`message-circle`
במקום אחר; קריאת העמוד הראתה ששני אלה יושבים על `tel:` ועל `wa.me` — מספר אחד,
שני ערוצים, והגליף מסמן את הערוץ. זה תקין, ולכן הגלאי צומצם. אחרי הצמצום: אפס
ליקויים בתשע מערכות הקיט המלא.

**אבל אותה קריאה פתחה משהו אמיתי:** ה-href של אותו כפתור WhatsApp הוא
`wa.me/9722313160` — ספרה אחת פחות מ-02-3131600. בדיקה רוחבית מצאה **15 קישורי
wa.me בעץ בארבעה ערכים, ואף אחד מהם אינו הצורה הבין-לאומית של המספר שמוצג לידו**.
הפירוט המלא, שורה-שורה: `QA/platform/WHATSAPP_LINKS.md` · core.issues #137 ו-#138.

**הבא בתור:** #137 — לתקן לכל היותר אפליקציה אחת בסבב (מקור → בנייה → פריסה), כי
בלי פריסה הקישור החי לא זז. `15-egod` הוא ההתחלה הטבעית: שני קישורים, מספר יעד
חד-משמעי, ואותה אפליקציה כבר עמדה שלישית בדירוג §6.

> ✅ #137 נסגר באותו יום — 18 קישורים, 17 תוקנו, האחרון (`01`) הוא #138 וחסום
> על שאלה למשתמש.

---

## עדכון 10/08/2026 (ז) — `egod`: אותה תקלה של `mthbram`, במערכת אחרת

הסבב הקודם מצא ב-`mthbram` את **תקלה A** של `icon-decisions.mjs` — פקד אחד,
אותו סימן פעמיים — והסיר לב אחד מרצועת התרומה. אחריו `mthbram` היא 16
ו-`egod` היא 15, ולכן `egod` היא הבאה בסדר העבודה. **אותה צורה בדיוק נמצאה בה**,
לפי סלוטים 9 ו-10 ב-`_icon-slots-egod.md`, שניהם באותה רצועה:

| # | גליף | גודל | הטקסט לידו |
|---|---|---|---|
| 9 | `lightbulb` | 16px | טיפים וכלים למגיד השיעור |
| 10 | `lightbulb` | 24px | (אין — אריח על כרטיס הטיפ) |

`apps/15-egod/src/components/home/TipsSection.tsx`: שורה 39 ציירה נורה בצ׳יפ
שהמילה הראשונה בטקסט שלו היא **«טיפים»**, ושמונה-עשרה שורות מתחת שורה 57 ציירה
נורה שנייה באריח של הכרטיס המתחלף.

**הנורה של הצ׳יפ הוסרה, זו של הכרטיס נשארה.** הצ׳יפ אומר במילים את מה שהגליף
צייר; הכרטיס מצייר כותרת וגוף של טיפ ואין עליו שום מילה שאומרת שזה טיפ, ולכן שם
הסימן נושא מה שהטקסט אינו נושא. זו אותה הכרעה שנפלה ב-`mthbram` (הצ׳יפ ויתר,
ה-CTA שמר).

**נמדד, לא הונח** — `scripts/qa/egod-tips-lightbulb.mjs`, 14 מתוך 14. הכלי קורא
את הייצור ואת הבנייה החדשה באותה ריצה, כך ששני צדי ההשוואה נמדדים:
ייצור 2 נורות ברצועה (אחת מהן על אריח הכרטיס), בנייה 1 והיא אריח הכרטיס;
מילות הצ׳יפ זהות בית-בבית לייצור; הכותרת, שני חצי הקרוסלה ושמונה הנקודות לא זזו;
אפס נורות בשאר העמוד לפני ואחרי. צילומים: `QA/platform/egod-lightbulb-0810/`.

**`_icon-decisions.json` לא עודכן, ובכוונה.** הוא מודד את הייצור, והייצור לא זז —
ראו עדכון (ד): המדד סופר סלוטים, והוא ממילא אינו רואה את התקלה הזו. הבנייה
(`index-BfpR57e0.js`, 2224 מודולים) לא נפרסה: מכסת הפריסות החינמית של Vercel
(‏`api-deployments-free-per-day`, ‏core.issues #83) סירבה שוב ב-12:14 UTC, והשחרור
הבא — כשהמכסה תשוחרר — שייך ל-`kupot`, שבו שתי כתיבות בדף חי נכשלות.

---

## עדכון 10/08/2026 (ח) — התקלה שנמצאה פעמיים ביד נכנסה לכלי

שני הסבבים האחרונים מצאו את אותה צורה בשתי מערכות: `mthbram` ציירה `heart` 12px
על צ׳יפ ו-`heart` 16px על הקישור מתחתיו באותה רצועה, ו-`egod` ציירה `lightbulb`
16px על צ׳יפ ו-24px על הכרטיס. **שתיהן נמצאו בקריאה ידנית של גיליון
`--list`, בזמן ש-`icon-decisions.mjs` הדפיס «אין».** תקלה A שלו מחפשת SVG לצד
אמוג׳י — הצורה שהייתה במשגר של `zchuyot` — ושתי אלה מצוירות כולן ב-lucide.

`repeatInBand` הוא הגלאי החסר: אותו גליף בשתי משבצות של אותה רצועה. הרצועה
נקבעת לפי הכותרת שקורא רואה, לא לפי מחלקות. הריצה מחזירה את שני הממצאים
הידניים, ומוסיפה שניים שלא נקראו ביד:

| מערכת | גליף | המשבצות | מה זה |
|---|---|---|---|
| `egod` | `lightbulb` ×2 | 16px «טיפים וכלים…» + 24px כרטיס | ידוע, תוקן במקור, ממתין לפריסה |
| `mthbram` | `heart` ×2 | 12px «שותפים בהפצת התורה» + 16px «תרומה דרך נדרים פלוס» | ידוע, תוקן במקור, ממתין לפריסה |
| `mthbram` | `search` ×2 | 16px «הוספת שיעור למאגר» + 40px «לא נמצאו שיעורים מתאימים» | **חדש** — דורש קריאה |
| `zchuyot` | `message-circle` ×2 | 28px משגר + 20px «יש לכם משהו שמעניין אותכם?» | **חדש** — דורש קריאה |

שני החדשים אינם הוראת הסרה. ב-`mthbram` הזכוכית ה-40px היא איור של מצב-ריק
ולא סימן על פקד, וב-`zchuyot` הרצועה נרשמה `—` כי אין לה כותרת — וכל מה שאין לו
כותרת נופל למפתח אחד, כך ששתי המשבצות עשויות לשבת בשתי רצועות שונות. זו מגבלה
ידועה של הגלאי ולא ממצא נגדה: מי שממשיך פותח את שתי הכתובות וקורא, כמו בשתיים
הראשונות.

**מה הגלאי מוציא בכוונה, ואומר בקול:**

- `repeatPerRecord` — גליף שמופיע פעם לכל רשומה הוא סימן רשימה אחד, לא בחירה
  שנייה. `chatzor` מציירת `arrow-left` על ארבעה כרטיסי בית כנסת ושוב על ה-CTA,
  ו-`smel` מציירת `check` על כל שורה בשני מסלולים. ארבעה זוגות כאלה הוצאו,
  ונספרים בפלט הריצה כדי שאפשר יהיה לבקר את הסינון מהריצה עצמה.
- `settledByDatum` — כשאותו **נתון** נושא את אותו גליף פעמיים, תקלה B מנצחת.
  זה בדיוק מה שסבב (ד) יצר במכוון ב-`zchuyot`: 02-3131500 קיבל `phone` בשני
  המקומות במקום פיצול `phone`/`phone-call`. סימון חוזר שלו היה מבקש מהסבב הבא
  לפצל אותו בחזרה.

שלוש הרשימות ממשיכות לשבת **מחוץ** לדירוג `decisions` — גליף כפול הוא גליף
אחד להסרה, לא סיבה לסדר מחדש את העבודה. סדר העבודה לא זז:
`zchuyot 24 → mthbram 17 → egod 15 → galil 14`.

`_icon-decisions.json` כן עודכן הפעם, ולא בסתירה לעדכון (ז): הספירות שם זהות
בית-בבית — הייצור אכן לא זז — והשדות החדשים הם הרשימות בלבד. `egod` ו-`mthbram`
עדיין מופיעות ב-`repeatInBand` מהסיבה הזו: התיקון במקור, הייצור עוד לא קיבל אותו.

---

## עדכון 10/08/2026 (ט) — הרצועה שלא הייתה רצועה: `/zchuyot` מריצה שני משגרי צ׳אט

הסבב הקודם סימן שני ממצאים כ"דורש קריאה", ואחד מהם הוזהר עליו במפורש: הרצועה של
‏`message-circle` ב-`zchuyot` נרשמה `—` **כי אין לה כותרת, וכל מה שאין לו כותרת
נפל למפתח אחד**. הקריאה נעשתה, והתברר שהמגבלה גדולה משנרשמה — ושהממצא עצמו אמיתי.

### המגבלה: "אותה רצועה" הושווה לפי שֵם, ושם אינו זהות

`sectionOf` החזיר את כותרת ה-landmark, ומפתח הזוגות היה **הכותרת** ולא האלמנט.
לכן שני `<section>` בלי כותרת הדפיסו שניהם `section` והתמזגו, וכל סלוט שאין מעליו
‏landmark כלל הדפיס `—` והתמזג עם כל האחרים. מפתח הזוגות הוא מעכשיו **האלמנט
עצמו**: שתי רצועות שחולקות שם — או חולקות את היעדרו — נשארות שתיים.

### מה שנמצא אחרי הפרדת המפתח: לא רצועה, אלא שני פקדים צפים

שני ה-SVG של `message-circle` אינם בשום רצועה — שניהם `position: fixed` מעל
העמוד כולו, בשתי פינות שונות, ושניהם פותחים צ׳אט:

| רכיב | פינה | הגליף | הפקד |
|---|---|---|---|
| `FloatingBot.tsx:496` | שמאל־תחתון (24, 812) · 64×64 | `message-circle` 28px | עיגול, בלי טקסט · `aria-label="פתיחת בודק הזכויות"` |
| `AIAgent.tsx:122` | ימין־תחתון (986, 808) · 270×68 | `message-circle` 20px | גלולה · «יש לכם משהו שמעניין אותכם? מוזמנים לדבר איתי במילים שלכם» |

שניהם `z-index: 50`. צילום: `QA/platform/zchuyot-launchers-0810/` ·
המדידה: `node scripts/qa/zchuyot-two-launchers.mjs`.

**הכלי צדק במקרה.** הוא זיווג את השניים רק מפני ש-`—` היה מפתח משותף — לא מפני
שמדד משהו. `floatingTwice` הוא הגלאי שצודק בכוונה: אותו גליף על שני פקדים צפים
נפרדים. זו לא תקלה A (פקד אחד, שני סימנים) ולא תקלה C (רצועה אחת, שני סלוטים);
זו הצורה השלישית, ולה עמוד שלם ביניהן.

### מה שלא נגרע

- `repeatUnbanded` — זוג בלי landmark משותף ובלי היותו צף יושב בקליפת העמוד,
  שכל סלוט חולק. זה אינו עדות להנחה שנעשתה פעמיים, והוא **מדווח בנפרד ולא
  נזרק**. בתשע המערכות: אפס.
- שלושת הממצאים שנמצאו ביד (`egod` נורה, `mthbram` לב, `mthbram` זכוכית)
  ממשיכים להופיע ב-`repeatInBand` — הפרדת המפתח לא שחקה אף אחד מהם.

**אימות:** תשע המערכות הורצו מחדש, וכל ארבע הספירות בכל אחת מהן (`decisions` ·
`rendered` · `named` · `lucide`) **זהות בית-בבית** לריצה שלפני השינוי, וסדר
העבודה לא זז. השינוי נגע ברשימות הליקויים בלבד, כפי שנועד.

> **הבא בתור — החלטת מוצר, לא החלטת עיצוב.** שני משגרים אינם אייקון מיותר: הם
> שני צ׳אטים על עמוד אחד, ומבקר צריך לבחור בין «בודק הזכויות» ל«לדבר איתי
> במילים שלכם» בלי שום דבר שיסביר את ההבדל. אין למחוק אחד מהם על סמך §6 —
> §6 מודד גליפים, וכאן הגליף הכפול הוא רק הסימן החיצוני לכפילות בתפקוד.
> השאלה למשתמש: איזה מהשניים הוא הצ׳אט של `/zchuyot`.
>
> הממצא השני של הסבב הקודם — `search` ×2 ב-`mthbram` — לא נקרא עדיין. לפי
> הגיליון הזכוכית ה-40px היא איור של מצב ריק («לא נמצאו שיעורים מתאימים»)
> וה-16px היא כפתור, ולכן ההערכה היא שאינו מועמד להסרה — אבל זו הערכה מהגיליון
> ולא קריאה בעמוד, וזה בדיוק ההבדל שעצר את `zchuyot` פעמיים.
>
> ✅ נקרא — ראו «עדכון 10/08/2026 (י)» למטה. המסקנה על שני הגליפים החזיקה,
> **שתי השורות בגיליון שתיארו אותם היו שגויות**, והקריאה פתחה ליקוי אחר.

---

## עדכון 10/08/2026 (י) — `mthbram`: הגליפים נשארו, והכותרת שמעליהם הייתה הממצא

ההוראה הייתה לקרוא את `search` ×2 בעמוד ולא בגיליון. היא בוצעה:
`scripts/qa/mthbram-empty-directory.mjs` · `QA/platform/mthbram-empty-0810/` · 15/15.

**ההערכה על שני הגליפים החזיקה, ושתי השורות שתיארו אותם לא.** מה שהשורה בגיליון
קראה «16px "הוספת שיעור למאגר"» איננו על הכפתור הזה: כפתור «הוספת שיעור למאגר»
מצייר `Plus` בלבד (`LessonsDashboard.tsx:190`), ונמדד כך — **שני** פקדים כאלה
בעמוד, שניהם `lucide-plus`, אפס `lucide-search`. ה-`search` ה-16px הוא הגליף של
**שדה החיפוש עצמו** (`:172`), שכן `aria-label="חיפוש שיעורים"` ויש לו placeholder
ואין לו תווית טקסט נראית. הכלי הלך לטקסט הקרוב ביותר וחצה גבול של פקד — שני
האלמנטים יושבים באותה שורת flex, והוא בחר את השכן הלא נכון.

וגם המספר היה חסר: **בעמוד שלושה גליפי `search`, לא שניים.** השלישי הוא קישור
ניווט שנושא את המילה בעצמו («חיפוש שיעור»), והוא ברצועה אחרת — ולכן
`repeatInBand` מעולם לא ראה אותו. נרשם כאן כדי שהסבב הבא לא יגלה אותו כחדש.

שלושתם נשארים: אחד מסמן שדה בלי תווית, אחד הוא איור של מצב ריק (‏40px, לא בתוך
שום פקד), ואחד יושב על קישור ניווט. **אפס מועמדים להסרה ב-`mthbram` מהזוג הזה.**

### מה שכן נמצא: הכותרת «0 שיעורים» היא נתון, לא תווית

מה שהכלי רשם כשם הרצועה הוא `${filtered.length} שיעורים` ‏(`:217`). כלומר
הייצור מצייר **אפס שיעורים**, וה-`search` ה-40px הוא מצב הריק הזה. הריקנות
אמיתית ולא כשל טעינה — ‏`Public can view approved lessons` היא מדיניות חיה
(מיגרציה `20260712192546`), וספירת ה-REST באנון מחזירה `HTTP 200` ו-**0** גם
בלי סינון וגם על `is_approved=eq.true`. אין לכתוב כאן מספר אחר.

**הליקוי הוא הקופי.** בלי שום סינון פעיל העמוד אומר «לא נמצאו שיעורים
**מתאימים**» — משפט שמדווח על חיפוש שנכשל, בזמן שאיש לא חיפש. הוא מטיל את
הריקנות על המבקר במקום לומר שהמאגר עוד ריק. אותה הבחנה **כבר קיימת בקוד**:
`LessonDirectory.tsx:213` מפריד «עדיין אין שיעורים במאגר» מ«לא נמצאו תוצאות».

התיקון מיישר את הדשבורד לאותה הבחנה, לפי `all.length` ולא לפי הסינון: מאגר ריק
נשאר «עדיין אין שיעורים במאגר» גם כשהמבקר סינן, כי גם אז זו הסיבה האמיתית.

**נמדד, לא הונח** — הבנייה נקראת מול **אותו** Supabase של הייצור, כך שהמאגר
הריק שמפעיל את המחרוזת החדשה הוא החי ולא פיקסצ׳ר: ייצור «לא נמצאו מתאימים»,
בנייה «עדיין אין שיעורים במאגר»; הכותרת נשארה `0 שיעורים` ושלושת הגליפים נשארו
שלושה — **הקופי זז, הנתון והאייקונים לא.** צילומים: `production-before.png` ·
`built-after.png`.

הבנייה (`index-bxKxiOtM.js`, ‏3048 מודולים) לא נפרסה: משבצת הפריסה של החלון
שייכת ל-`kupot` (‏core.issues #83 · #154), ושתי הבניות הממתינות — `15-egod`
ו-`21-mthbram` — עומדות בתור.

> **הבא בתור:** `_icon-decisions.json` מחזיק שתי שורות שגויות — התיאור של
> `mthbram · search ×2` מצביע על הכפתור הלא נכון. השורה נובעת מ`nearestText`
> שחוצה גבול של פקד; הכלי צריך לעצור ב-`closest('input,button,a,[role=button]')`
> לפני שהוא מטפס להורה. **זו תקלת תיוג ולא תקלת ספירה** — `decisions` לא זז
> ממנה, ולכן הסדר `zchuyot 24 → mthbram 17 → egod 15 → galil 14` נשאר.
> זו הפעם העשירית בסבב שממצא מבוסס-מדד דרש תיקון, והשלישית שהוא נתפס לפני
> השינוי ולא אחריו.

---

## עדכון 10/08/2026 (יא) — תקלת התיוג נסגרה: הטיפוס מדלג על פקד של מישהו אחר

ההוראה מהסבב הקודם הייתה לעצור ב-`closest('input,button,a,[role=button]')` לפני
הטיפוס להורה. **היא בוצעה בצורה שונה במכוון, ובגלל מה שהיא הייתה מוחקת:** תקרה
נוקשה הייתה מוחקת גם את הכותרת של כרטיס שכל כולו קישור עם אייקון בלבד — שם
הכותרת היא בדיוק התווית של האייקון. מה ששבור אינו הטיפוס אלא **חציית הגבול**:
טקסט שיושב בתוך פקד שהאייקון אינו בתוכו אינו "הטקסט שלידו".

`labelOf` מחסיר עכשיו מהטקסט של כל אב את תתי-העצים של פקדים שאינם מכילים את
ה-SVG (`a,button,input,textarea,select,label,summary,[role=button],[role=link]`),
ורק אם לא נשאר כלום ממשיך לטפס. אם באותו מכל יושב **שדה בלי תווית נראית**,
התווית היא השם הנגיש שלו (`aria-label` · `placeholder` · `title`) והיא מסומנת
«שדה: …» כדי שהמקור יהיה גלוי בגיליון.

**תשע שורות תווית זזו בתשע המערכות, ואף מספר לא זז.** ‏`decisions` · `rendered` ·
`named` · `per-record` זהים בכל אחת מהתשע, סדר העבודה
(`zchuyot 24 → mthbram 17 → egod 15 → galil 14`) לא זז, ושלוש רשימות הליקויים
החזירו בדיוק את אותם מספרים (mthbram 2 · egod 1 ברצועה, zchuyot 1 צף). זו
תקלת תיוג, כפי שנרשם — ו-`_icon-decisions.json` השתנה בשתי מחרוזות בלבד.

| מערכת | סלוט | היה | עכשיו |
|---|---|---|---|
| `mthbram` | `search` 16px | «הוספת שיעור למאגר» — **כפתור שכן** | «שדה: חיפוש שיעורים» |
| `mthbram` | `filter` 12px | רשימת הצ׳יפים של הנושאים | «שדה: נושא» |
| `chizukim` | `search` 16px | טקסט של כרטיס אחר ברצועה | «שדה: חיפוש בכל התמלולים» |
| `zchuyot` | `search` 24px | «—» (לא הייתה תווית כלל) | «שדה: חיפוש זכות» |
| `imud` | `book-open` 40px | מצב ריק + «ספר חדש» של ה-CTA | מצב ריק בלבד |
| `chatzor` · `smel` | `moon` | «מצא בית כנסת» · «פרימיום» | «—» |
| `zchuyot` · `galil` | `message-circle` צף | שרשור קישורי הניווט | הטקסט של קליפת העמוד |

השורה שפתחה את הסבב הזה — `mthbram · search ×2` — נקראת עכשיו
`16px "שדה: חיפוש שיעורים" + 40px "לא נמצאו שיעורים מתאימים"`, כלומר בדיוק מה
שנמדד בייצור בעדכון (י): שדה בלי תווית נראית, ואיור של מצב ריק. שני הכפתורים
שמצוירים `plus` נשארים `plus` בגיליון (סלוט 6), ואינם נושאים זכוכית מגדלת.

> **הבא בתור:** שתי שורות ה-`moon` אומרות «—» עכשיו, וזה נכון יותר מ«פרימיום»
> אבל עדיין לא התווית שלהן: מתג ערכת נושא הוא פקד בלי טקסט, והשם שלו הוא
> ה-`aria-label` של עצמו. אותו דבר במשגר הצף של `zchuyot`, ששם ה-`aria-label`
> שלו כבר נמדד — «פתיחת בודק הזכויות» (עדכון ט) — בזמן שהגיליון מדפיס לו פרוזה
> של קליפת העמוד. הכלל החסר: **פקד שהאייקון בתוכו, בלי טקסט משלו, מתויג בשם
> הנגיש שלו לפני שמטפסים בכלל.** לא נבדק אם לשני מתגי ה-`moon` יש `aria-label` —
> זו הבדיקה הראשונה של הסבב הבא, ולא הנחה.
>
> ✅ נמדד — ראו «עדכון 10/08/2026 (יב)» למטה. לשניהם **יש** `aria-label`.

---

## עדכון 10/08/2026 (יב) — פקד שקט מתויג בשם של עצמו, ולא בפרוזה שמעליו

הסבב הקודם הסתיים בהוראה **ובאזהרה נגד הנחה**: לא נבדק אם לשני מתגי ה-`moon`
יש `aria-label`, ובלי זה הכלל החדש היה נכתב על סמך ניחוש. הבדיקה נעשתה קודם,
מול הייצור:

| מערכת | הפקד | טקסט נראה | `aria-label` |
|---|---|---|---|
| `smel` | `<button>` · `moon` 16px | אין | «החלף מצב תצוגה» |
| `chatzor` | `<button>` · `moon` 20px | אין | «מעבר למצב כהה» |
| `zchuyot` | `<button>` צף · `message-circle` 28px | אין | «פתיחת בודק הזכויות» |

שלושתם נושאים שם נגיש משלהם, ואף אחד מהם לא ב-`title`, לא ב-`aria-labelledby`
ולא ב-`sr-only`. ההנחה הייתה נכונה — ועכשיו היא מדידה.

**הכלל שנוסף ל-`labelOf`:** אם ה-SVG יושב **בתוך** פקד שאין לו טקסט נראה משלו,
התווית היא השם הנגיש של אותו פקד (‏`aria-labelledby` → `aria-label` → `title`,
בסדר שקורא מסך פותר), והיא מסומנת «פקד: …». רק כשהפקד שקט: כפתור שמציג כיתוב
שומר על הכיתוב, ולכן המשגר ה-16px של `zchuyot` נשאר «וואטסאפ».

**שש שורות תווית זזו, ואף מספר לא זז.** ‏`decisions` · `rendered` · `named` ·
`per-record` זהים בכל תשע המערכות, סדר העבודה
(`zchuyot 24 → mthbram 17 → egod 15 → galil 14`) לא זז, ושלוש רשימות הליקויים
החזירו את אותם מספרים (mthbram 2 · egod 1 ברצועה, zchuyot 1 צף).

| מערכת | סלוט | היה | עכשיו |
|---|---|---|---|
| `smel` | `moon` 16px | «—» | «פקד: החלף מצב תצוגה» |
| `chatzor` | `moon` 20px | «—» | «פקד: מעבר למצב כהה» |
| `chatzor` | `chevron-down` 24px | כותרת+תת-כותרת של ההירו | «פקד: גלול לזמני היום» |
| `zchuyot` | `message-circle` 28px צף | פרוזה של קליפת העמוד | «פקד: פתיחת בודק הזכויות» |
| `galil` | `message-circle` 28px צף | פרוזה של קליפת העמוד | «פקד: פתח צ'אט» |
| `egod` | קרוסלת הטיפים 20px | הטקסט של הטיפ הבא | «פקד: הטיפ הקודם» |

שלוש מהשש לא היו ב-`moon` בכלל, ואחת מהן — `chatzor · chevron-down` — היא
בדיוק הצורה שהכלל נכתב עבורה: חץ בתוך כפתור גלילה שקט, שהגיליון תייג בכותרת
של הרצועה שמעליו.

**איך אפשר לדעת שאף שורה אחרת לא זזה מהשינוי:** הענף החדש מחזיר תמיד מחרוזת
שמתחילה ב«פקד: ». שתי שורות נוספות בגיליונות השתנו בלי הקידומת הזו, ולכן אינן
ממנו אלא מהעמוד עצמו בין שתי הריצות: ב-`galil` שורת ה-`clock` עברה «מנחה קטנה»
→ «פלג המנחה» (זמני היום מתגלגלים לפי השעה), וב-`zchuyot` זכוכית החיפוש נמדדה
26px במקום 24px.

**`floatingTwice` של `zchuyot` נקרא עכשיו** ‏`28px "פקד: פתיחת בודק הזכויות" +
20px "יש לכם משהו שמעניין אותכם? מוזמנים לדבר "` — שני המשגרים בשמותיהם, שזו
בדיוק השאלה שעדכון (ט) הפנה למשתמש.

לא נפרס: הסבב נגע בכלי QA ובתיעוד בלבד, אין בו קוד מוצר.

> **הבא בתור:** שתי הבניות שממתינות מסבבים קודמים — `15-egod` (נורת הטיפים)
> ו-`21-mthbram` (הקופי של המאגר הריק) — עדיין לא בייצור, ושתיהן מופיעות
> ב-`repeatInBand` **מהסיבה הזו בלבד**. כל עוד הן ממתינות, הכלי ימשיך לדווח
> עליהן, וסבב שיתחיל מהרשימה יחשוב שהן פתוחות. משבצת הפריסה של החלון שייכת
> ל-`kupot` (‏core.issues #83 · #154). הליקוי היחיד שנשאר פתוח בקריאה ולא
> בפריסה הוא **שני המשגרים של `zchuyot`** — והוא החלטת מוצר של המשתמש, לא §6.
>
> ✅ **10/08 14:01Z — משבצת הפריסה שוחררה ו-`kupot` נפרס** (‏#154 · ראו
> `QA/platform/kupot-deploy-0810/_results-shipped.json`). התור של §6 התקדם
> בשלב אחד: המשבצת הפנויה הבאה שייכת ל-`15-egod`, ואחריה `21-mthbram`.
> שתיהן ימשיכו להופיע ב-`repeatInBand` עד אז, ומי שקורא את הרשימה לפני כן
> אינו קורא ליקוי פתוח אלא בנייה בתור.

---

## עדכון 10/08/2026 (יג) — `15-egod` הועלה ונדחה, ומי שאוכל את המכסה נמדד

התור אמר `15-egod`, והפעולה נעשתה: `apps/15-egod/dist` (‏`index-BfpR57e0.js`,
הבנייה של עדכון (ז) עם נורת הצ׳יפ שהוסרה) הועתקה ב-`robocopy /MIR` אל
`_deploy/egod-more30/egod`, **אחרי** שהוודא שהתיקייה שהוחלפה זהה לייצור —
‏`more30.com/egod/` הגיש `index-y8vBmSHh.js` ו-`index-CCW5-dLB.css`, בדיוק מה
שישב שם ([[deploy-copy-can-break-live-sites]] הוא הסיבה שזה נבדק ולא הונח).

**ההעלאה עברה (961KB) והשחרור נדחה** ב-14:14Z: `api-deployments-free-per-day`,
ארבע-עשרה דקות אחרי שהמשבצת של `kupot` נוצלה. תיקיית הפריסה של 15 נשארת עם
הבנייה החדשה — הסבב שיקבל משבצת רק מריץ את הפקודה, בלי לבנות ובלי להעתיק.

**מה שכן נסגר בסבב הזה: השאלה למה אין משבצות.** עשרים הפריסות האחרונות של
`more30-portal` נקראו, וכולן `target: null` — תצוגות מקדימה של אינטגרציית Git,
אפס ייצור. שלוש מהן מענף `claude/ganenet-…` וכולן ERROR; בנייה שנכשלה נספרת
במכסה. הקצב: עשרים ב-6 שעות ורבע. הייצור נפרס מה-CLI, ולכן אף אחת מהעשרים
לא הגישה מבקר מעולם. נוצר `vercel.json` בשורש עם `git.deploymentEnabled:false`
(לא היה שם קובץ קודם). מדידה מלאה ותנאי האימות:
`QA/platform/deploy-quota-0810/_results.json` · ‏core.issues #83.

**האימות נוסה מיד, והתברר שהמבחן שנוסח היה חסר ערך.** נכתב «אם הפריסה החדשה
ביותר תישאר 13:41Z — הברז נסגר», ואז נמדד: אפס פריסות מאז 13:41:40Z. אלא ששלוש
דחיפות כבר נחתו אחרי השעה הזו — 46afd6b ב-13:55Z, 059a9fc ב-14:06Z, ו-92a0eef
שנושא את הקובץ — **ושתי הראשונות קדמו ל-`vercel.json`.** התצוגות המקדימות נפסקו
עוד לפני שהדגל היה קיים, כמעט בוודאות מפני שהמכסה המוצה חוסמת גם את אינטגרציית
Git, בדיוק כפי שחסמה את ה-CLI ב-14:14Z. התנאי היה מתקיים בין אם התיקון עובד ובין
אם לא, ולכן אינו מבחן.

> **הבא בתור:** המבחן שכן מפריד בין השניים, ורק אחריו התור: אחרי שהמכסה
> מתאפסת (24 שעות מ-14:14Z ב-10/08) — דחיפה אחת וקריאה חוזרת. נוצרה תצוגה
> מקדימה ⇒ הקובץ אינו נקרא, כלומר ה-rootDirectory של הפרויקט אינו שורש הריפו
> וההגדרה עוברת ללוח של Vercel. לא נוצרה, בזמן ש-`vercel deploy --prod` באותו
> פרק זמן מצליח ⇒ תפס. חלופה זולה שזמינה עוד לפני האיפוס: סטטוס הקומיט של
> 92a0eef ב-GitHub — עם `deploymentEnabled:false` בדיקת Vercel נעדרת לגמרי,
> ועם מכסה מוצה בלבד היא קיימת ונכשלת. לא נבדק כאן: `gh` אינו מותקן, וזה דורש
> קריאת REST עם הטוקן של credential-manager.
> ורק אחרי זה, כשמשבצת מתפנה: `15-egod` (מוכן בתיקייה), ואחריו `21-mthbram`.
>
> ✅ נעשה — ראו «עדכון 10/08/2026 (יד)» למטה. הדגל **מכובד**, וההמתנה של
> 24 שעות שהסבב הקודם דרש לא הייתה נחוצה. מה שכן התברר: המשבצת הבאה רחוקה
> בהרבה משנחשב.

---

## עדכון 10/08/2026 (יד) — הדגל מכובד, וזה נמדד בלי לחכות לאיפוס

הסבב הקודם כתב שהמבחן היחיד שמפריד בין «הדגל תפס» ל«המכסה חסמה» הוא להמתין
לאיפוס המכסה. **זה לא היה נכון, ועלה סבב שלם.** מפריד קיים כבר עכשיו, כי
**סירוב-מכסה והשבתת-אינטגרציה אינם אותה היעדרות**: אפליקציית Vercel שמחוברת
ומסורבת עדיין **מפרסמת סטטוס קומיט** ב-GitHub, והוא נקרא `failure`. אפליקציה
שהופסקה לריפו אינה מפרסמת כלום.

| קומיט | שעה | הדגל | סטטוסי Vercel על הקומיט |
|---|---|---|---|
| `43118dc` | 13:23Z | אין | portal=failure · admin=success |
| `24e0523` | 13:41Z | אין | admin=failure · portal=success |
| `46afd6b` | 13:53Z | אין | **failure · failure** |
| `059a9fc` | 14:05Z | אין | **failure · failure** |
| `92a0eef` | 14:19Z | **יש** | **אין אף אחד** (`pending`) |
| `e968cf0` | 14:21Z | יש | אין אף אחד |
| `7fa6774` | 14:30Z | יש | אין אף אחד |

`46afd6b` ו-`059a9fc` נדחפו תחת **אותה מכסה מוצה בדיוק**, 26 ו-14 דקות לפני
שהקובץ היה קיים, ושניהם נושאים שני סטטוסים נכשלים. שלושת הקומיטים מ-`92a0eef`
והלאה אינם נושאים שום סטטוס — GitHub מחזיר `pending` מפני שאיש לא כתב אליהם.
**המכסה מייצרת סטטוס כושל; הדגל אינו מייצר סטטוס.** ההיעדרות אחרי 14:19Z היא
הדגל. הוא תפס על הקומיט שהכניס אותו עצמו.

וגם מצד Vercel: הפריסה החדשה ביותר בכל הצוות היא 13:59:41Z, כלומר **אפס פריסות
מכל סוג מאז 14:00Z** על פני שלוש דחיפות. שימו לב ש-`46afd6b` ו-`059a9fc` לא
יצרו **שורת פריסה כלל** — סירוב מכסה מפרסם סטטוס בלי ליצור פריסה, ולכן ספירת
פריסות לבדה מעולם לא יכלה להפריד בין שני המקרים.

### מה שנסגר אינו מה שנחסם

עצירת הדליפה אינה מחזירה את מה שהיא כבר בזבזה. בחלון המתגלגל ב-14:38Z:
**132 פריסות** (117 תצוגות Git · 15 CLI) מול תקרה של «יותר מ-100». צריך
**33** להתיישן. הראשונה שמפנה משבצת פגה ב-**11/08 00:11Z**, והחלון יורד מתחת
ל-100 סביב אותה שעה (‏+9h → 114, ‏+10h → 93).

**כלומר: אין פריסה לייצור בעשר השעות הקרובות.** הסבבים הקודמים קראו את ההצלחה
המזדמנת כ«בערך פריסה אחת פנויה לסבב» ובנו על זה תור; זו הייתה דליפה, ועכשיו
כשהיא סגורה אין גם טפטוף. שתי הבניות הממתינות — `15-egod` (כבר מועתקת אל
`_deploy/egod-more30/egod`) ו-`21-mthbram` — נשלחות אחרי שהחלון מתנקז, בסדר הזה.

המדידה המלאה ומה שלא נטען: `QA/platform/deploy-quota-0810/_flag-verified.json` ·
‏core.issues #83.

> **הבא בתור:** לא פריסה — היא חסומה עד ~00:11Z. §6 ממשיך מהקריאה: הליקוי
> היחיד שפתוח בקריאה ולא בפריסה הוא **שני משגרי הצ׳אט של `zchuyot`**, והוא
> החלטת מוצר של המשתמש. אחריו בסדר העבודה `galil 14`, שלא נקראה עדיין.
>
> ✅ `galil` נקראה — ראו «עדכון 10/08/2026 (טו)» למטה.

---

## עדכון 10/08/2026 (טו) — `galil`: גליף אחד, שני יעדים — והמונה לא יכול לראות את זה

`galil` נקראה, והממצא אינו במספר. הכלי הדפיס 14 החלטות ו-79% מהרשומות, ושלושת
הזוגות שנמצאו כאן אינם מזיזים אף אחד מהמספרים האלה.

**הכלל שנמדד לפיו:** גליף רשאי לחזור **כשהיעד חוזר** — שורת ניווט והכרטיס
שמוביל לאותו מקום הם החלטה אחת, ולראות את אותו סמל בשניהם זו עקביות ולא רעש.
הוא אסור לחזור **על שני יעדים שונים**, כי אז עמודת האייקונים מפסיקה לשאת מידע.
נמדד ב-https://more30.com/galil/ (ייצור), 390×844:

| גליף | היעדים שהוא סימן | פסק |
|---|---|---|
| `message-circle` | ניווט «שאל את הרב» · פוטר «התחברו אלינו בוואטסאפ» · פקד צף «פתח צ'אט» | **שלושה יעדים** |
| `settings` | ניווט «פורטל גבאים» · ניווט «ניהול ⚡» · כרטיס «כניסה לפורטל גבאים» | **שניים** (הכרטיס והניווט חולקים יעד — מותר) |
| `book-open` | ניווט «בתי כנסת» · ניווט «דת וקהילה» · כרטיס «מידע דת וקהילה» | **שניים** (הכרטיס ו-`/info` חולקים יעד — מותר) |
| `phone` | ניווט «צור קשר» · כרטיס «צור קשר» | אותו יעד — תקין |
| `chevron-down` | שני אקורדיונים | אפורדנס, לא זהות — תקין |

**למה `repeatInBand` ו-`floatingTwice` שתקו על שלושתם:** הזיווג של `repeatInBand`
הוא לפי כותרת הרצועה, וכל שלושת הזוגות **חוצים רצועות** — ניווט מול פוטר מול פקד
צף. `floatingTwice` מחפש שני פקדים צפים, וכאן רק אחד צף. ולמעלה מזה:
`_icon-slots-galil.md` מונה 14 סלוטים ואף אחד מהם אינו שורת ניווט, כי **גליפי
הניווט מצוירים רק במגירת המובייל** והמדידה רצה ברוחב דסקטופ, שבו הניווט הוא
טקסט בלבד. המונה לא החמיץ אותם — הוא מעולם לא ראה אותם.

**התיקון לקח כל סמל מהיעד עצמו, לא המציא:** «שאל את הרב» → `circle-help`, שהוא
הסמל של `AskRabbiSection` עצמה; הפקד הצף → `Bot`, הסמל שכותרת הפאנל שהוא פותח
כבר נושאת («מחוברים בוט», ChatBot.tsx:471). שני אלה בלבד משחררים את
`message-circle` להיות **הוואטסאפ ותו לא**. «ניהול ⚡» → `shield-check`, ו«בתי
כנסת» → `landmark` (הסמל היחיד שנוסף לאוצר המילים, ובמכוון: `book-open` נשאר
ל-`/info` כי הכרטיס שמוביל לשם מצייר אותו).

**אחרי:** שבע שורות ניווט, שבעה גליפים, והחזרות היחידות שנשארו הן כולן
אותו-יעד. נמדד על הבנייה עצמה ב-`vite preview` ולא בקוד:
`QA/platform/galil-icons-0810/_results.json` · צילומים `before/after-mobile-drawer.png`.

**לא נפרס** — המכסה חסומה. `dist` של galil נבנה ו**לא הועתק** אל
`_deploy/galil-more30`: העתקה בלי לוודא קודם שהתיקייה החיה זהה לבנייה היא בדיוק
הדפוס ששבר אתרים בעבר ([[deploy-copy-can-break-live-sites]]).

> **הבא בתור:** הפריסה נפתחת ~00:11Z, ואז לפי הסדר `15-egod` (מועתק),
> `21-mthbram`, ו-`galil` (יש לוודא זהות מול החי לפני ההעתקה). עד אז §6 ממשיך
> בקריאה: `chatzor 11` ו-`smel 8` לא נקראו סלוט-סלוט. בשתיהן **הצורה הזו היא
> הראשונה לבדוק** — לפתוח את מגירת המובייל ולמפות גליף→יעד — כי אם גם הן מציירות
> ניווט באייקונים במובייל, המונה שרץ בדסקטופ עיוור לו באותה מידה. לא נבדק ולכן
> לא נטען שהן חולקות את הקוד של `galil`.
>
> ✅ `chatzor` נקראה — ראו «עדכון 10/08/2026 (טז)» למטה. הצורה של `galil` **אינה
> קיימת שם**, ומה שכן נמצא הוא ליקוי אחר לגמרי.

---

## עדכון 10/08/2026 (טז) — `chatzor`: הצורה שחיפשנו לא שם, ושורת הכתובת היא כל גוף הכרטיס

התור אמר לפתוח את מגירת המובייל של `chatzor` ולמפות גליף→יעד. זה נעשה
ב-https://more30.com/chatzor/ ב-390×844, המגירה נפתחה בפועל, **והתשובה שלילית**:
שש שורות הניווט — «בתי כנסת · זמני היום · שיעורי תורה · גמ״חים · שאל את הרב ·
צור קשר» — הן **טקסט בלבד, בלי אף גליף**. מה שהעיוור את המונה ב-`galil` פשוט
אינו קיים כאן, ולכן ההשערה שנרשמה בסוף עדכון (טו) נבדקה ונדחתה.

מפקד מלא של העמוד: 24 ‏`svg` גלויים, 13 גליפים שונים, ושלושה מהם חוזרים על יותר
מיעד אחד — `map-pin` ×4, ‏`arrow-left` ×6, ‏`navigation` ×4. **שלושתם לגיטימיים**:
כולם לכל-רשומה (ארבעה כרטיסי בתי כנסת) ולא לכל-החלטה, ו«לך לשם» הוא כיוון ולא
זהות של יעד — אותו פסק שניתן ל-`chevron-down` של האקורדיונים ב-`galil`.

### מה כן נמצא: אותו שדה, שני דינים באותו מוצר

`SynagogueCard.tsx:52` צייר את שורת הכתובת **תמיד**, עם ‏`{s.address ?? "כתובת לא
זמינה"}`. ‏`SynagogueSite.tsx:130` — **אותו שדה, אותו מוצר** — כבר מותנה:
‏`{synagogue.address && …}`. עמוד בית הכנסת מסתיר כתובת חסרה; הכרטיס מכריז עליה.

כל ארבעת בתי הכנסת המפורסמים ב-`chatzor.synagogues` הם `address=null` (נקרא
במסד), ולכן בייצור זה יצא **שמונה פעמים** — ארבע בדף הבית וארבע ב-`/batei-knesset`,
נספר בדפדפן. ובכרטיסים האלה אין `nusach` ואין `description`, ולכן «כתובת לא
זמינה» היה **כל גוף הכרטיס**: השורה היחידה מתחת לשם, ארבע פעמים ברצף. זהו בדיוק
הקופי הממלא-מקום ש-§6 מתלונן עליו.

ולגליף עצמו: `map-pin` סימן את **היעדר** הכתובת, בשעה ש«ניווט» בשורה מתחתיו נושא
קואורדינטות אמיתיות (`32.980094,35.54474` וכו׳, קיימות במסד לכל הארבעה). המוצר
יודע איפה זה — רק שדה הכתובת ריק.

**התיקון:** השורה מותנית ב-`s.address`, כמו ב-`SynagogueSite`. לא הומצאה כתובת
ולא בוצע גיאוקודינג הפוך מהקואורדינטות — אין נתון, אז אין שורה, ו«ניווט» נשאר
ועונה על «איפה זה». שני משטחים מושפעים: `SynagoguesPreview.tsx:31` ו-
`SynagoguesDirectory.tsx:72`.

**אומת על הבנייה עצמה** ב-`vite preview` ולא בקוד: 0 מופעים של המחרוזת ו-0
`map-pin` בשני העמודים, מול 4+4 בייצור, כשארבעת הכרטיסים וארבעת קישורי הניווט
נשארו במקומם. `QA/platform/chatzor-icons-0810/_results.json` · צילומים
`before-cards-production.png` ו-`after-cards-localbuild.png` באותו רוחב.

**לא נפרס** — המכסה חסומה עד ~00:11Z. `dist` נבנה ולא הועתק אל `_deploy`
([[deploy-copy-can-break-live-sites]]).

> **הבא בתור:** הפריסה נפתחת ~00:11Z ואז לפי הסדר `15-egod` (מועתק),
> `21-mthbram`, `galil`, `chatzor` — לשלוש האחרונות יש לוודא זהות מול החי לפני
> ההעתקה. עד אז §6 ממשיך בקריאה: `smel 8` היא היחידה שלא נקראה סלוט-סלוט.
> **מה שכדאי לבדוק בה ראשון אינו עוד מגירת מובייל** אלא הצורה שנמצאה כאן:
> `?? "…לא זמין"` על שדה שעמוד אחר באותו מוצר כבר מסתיר. חיפוש טקסטואלי פשוט
> על `??` ליד מחרוזת עברית מוצא מועמדים בלי דפדפן.
>
> **נתון שחסר למשתמש, לא לי:** לארבעת בתי הכנסת ב-`chatzor` אין כתובת במסד.
> אפשר להזין אותן ב-`/chatzor/admin` (טופס «כתובת» קיים,
> `SynagogueForm.tsx:76`), ואז השורה תחזור מעצמה עם נתון אמיתי.
>
> ✅ `smel` נקראה — ראו «עדכון 10/08/2026 (יז)» למטה. הצורה שהוצע לחפש בה
> (`?? "…לא זמין"`) **אינה שם**, ומה שנמצא במקומה חמור ממנה.

---

## עדכון 10/08/2026 (יז) — `smel`: פסקה שנכתבה בלקוח, זהה לכל כתובת בארץ

התור אמר לחפש ב-`smel` את הצורה של `chatzor` — `??` ליד מחרוזת עברית, שדה
שעמוד אחר באותו מוצר כבר מסתיר. **החיפוש נעשה והתשובה שלילית:** ששת המופעים
ב-`client/src` הם הודעות שגיאה (`err?.message ?? "נסו שוב"`) וכותרת ברירת-מחדל
לשאלון. אין שם ולו ממלא-מקום אחד על שדה נתונים.

מה שכן נמצא, ב-`Report.tsx` בכרטיס «רקע על המקום», הוא **הצורה החזקה יותר של
אותה תלונה**: לא ממלא-מקום שמודה שאין נתון, אלא פסקה שמצהירה שיש.

**המשפט שנמדד בייצור, לשתי כתובות:**

| הוקלד | מה שהכרטיס אמר |
|---|---|
| דיזנגוף 100 · תל אביב | «שכונת **הצפון הישן החלק הדרומי** בתל אביב-יפו היא אזור עם אופי מובהק המשלב נגישות, ביקוש ותמהיל אוכלוסייה ייחודי. הנתונים שאספנו מציירים תמונה של אזור בעל פוטנציאל להשקעה ולמגורים כאחד.» |
| יפו 30 · ירושלים | «שכונת **פלורנטין** בתל אביב-יפו היא אזור עם אופי מובהק המשלב נגישות, ביקוש ותמהיל אוכלוסייה ייחודי. הנתונים שאספנו מציירים תמונה של אזור בעל פוטנציאל להשקעה ולמגורים כאחד.» |

**מילה במילה, אחרי שם השכונה.** המשתנים היחידים בפסקה היו שם השכונה ובינארי
אחד מ-`scoreVerdict(...).tone` («אטרקטיבי במיוחד» מול «בעל פוטנציאל»). כל
כתובת בארץ מקבלת את אותו האזור.

**ושהיא נכתבה בלקוח, לא הוחזרה:** `ResearchProfile` ב-`shared/schema.ts` אינו
נושא שום שדה תיאור או רקע. הפסקה כולה היא מחרוזת ב-`Report.tsx`. המשפט
«**הנתונים שאספנו** מציירים תמונה» הצהיר על נתונים ולא הציג אף אחד מהם.

**למה זה הבלוק היחיד בדוח שנוגד את הכלל של המוצר עצמו:** `StatCard` מדפיס
`—` כשאין ערך, `friendlyInsights` מוסיף שורה רק כשהפרמטר קיים ועובר סף,
רצועת המקורות מוצגת רק כש-`sources.length > 0`, וההירו של `Home.tsx` **כבר
נוקה מאותה שפה בדיוק בסבב קודם** (ההערה בשורה 130 מונה את שלושת הסימנים).
הכרטיס הזה נשאר — אותה תבנית שנמחקה במשטח אחד, שרדה במשטח שכן. זו הצורה
שנמצאה גם ב-`chatzor` (שדה אחד, שני דינים באותו מוצר).

### מה הכרטיס יכול לעמוד עליו

`placeFacts()` ב-`lib/research.ts` בוחר שלושה מפתחות — `dist_central_place` ·
`dist_synagogue` · `synagogues_500m` — לפי שלושה תנאים: ה-payload נושא אותם,
הם **אינם דמוגרפיים** (אלה שייכים לדוח בתשלום, לפי שער הפרימיום עצמו:
«סוג אוכלוסייה (גיל, השכלה, שכר)»), ואף משטח אחר בדוח אינו מדפיס אותם כבר.
מפתח שהמרשם לא ענה עליו **מדולג ולא ממולא**.

**וזה לא היה זהירות תיאורטית:** אותה כתובת בדיוק, בשתי קריאות לפונקציית הקצה,
החזירה **19 פרמטרים בפעם אחת ו-17 בשנייה** — שלושת המפתחות האלה נכחו בחלק
מהקריאות ולא בכולן. סט הפרמטרים אינו קבוע, ולכן הכרטיס חייב לסבול היעדרות.

**אומת על הבנייה** (`vite preview` על `dist/public`, ‏`index-ewZkQnZA.js`), לא
בקוד, ושני הענפים נבדקו:

| הוקלד | שורות עובדה | הודעת היעדר | הפסקה הישנה |
|---|---|---|---|
| דיזנגוף 100 · תל אביב | 2 (‏319 מ׳ · 4) | — | 0 |
| יפו 30 · ירושלים | 0 | «נתוני סביבה נוספים אינם זמינים במרשם לכתובת זו.» | 0 |
| הרצל 20 · חיפה | 0 | אותה הודעה | 0 |

**טעות מדידה שנתפסה תוך כדי ותוקנה בקוד:** הריצה הראשונה ספרה
`[data-testid^="text-place-"]` והחזירה «שורה אחת» על כרטיס בלי אף שורת עובדה —
הקידומת תפסה גם את הודעת ההיעדר. ההודעה נקראת עכשיו `text-noplace`, כדי
ששאילתת הקידומת תישאר נכונה למי שיריץ אותה אחרי.

**לא נפרס** — המכסה חסומה עד ~00:11Z. ‏`dist` נבנה ולא הועתק אל `_deploy`
(‏[[deploy-copy-can-break-live-sites]]).

`QA/platform/smel-place-0810/_results.json` · ארבעה צילומים before/after.

> **באג נכונות שנמצא אגב הבדיקה ולא תוקן כאן — `core.issues #158`:**
> «יפו 30 **ירושלים**» מוחזר כ«דרך יפו-ת״א 30» ב**תל אביב-יפו**, שכונת פלורנטין.
> שם העיר שהוקלד אינו מגביל את ההתאמה, וכל הדוח — ציון, מחיר ממוצע, תשואה,
> הדגשים — מחושב על עיר אחרת בלי שום סימן למשתמש. נמדד גם בייצור וגם בבנייה
> המקומית, ולכן אינו רגרסיה של השינוי הזה. **זה לא §6 ולכן לא נגעתי בו בצעד
> הזה**, אבל הוא חמור יותר מכל מה ש-§6 מצא ב-`smel`.
>
> **הבא בתור:** הפריסה נפתחת ~00:11Z ואז לפי הסדר `15-egod` (מועתק),
> `21-mthbram`, `galil`, `chatzor`, `smel` — לארבע האחרונות יש לוודא זהות מול
> החי לפני ההעתקה. עד אז, ב-`smel` עצמה נשארה **שארית של אותו סבב קופי**:
> ההירו נוקה מ«מחקר נדל״ן חכם» ו«חכם» שרד בחמישה משטחים אחרים של אותו מוצר —
> `Logo.tsx:44` (תגית המותג), `Home.tsx:38` («מקבלים דוח חכם»),
> `Home.tsx:324` (פוטר), ו-`client/index.html:9,10` (‏`<title>` ו-`description`).
> זו בדיוק הצורה שהסבב הזה מצא, בשכבה רדודה יותר. **מה שצריך להחליט לפני
> הנגיעה:** `<title>` ו-`meta description` הם גם SEO, ושינוי שלהם אינו רק קופי.
>
> ✅ חמשת המשטחים נוקו — ראו «עדכון 10/08/2026 (יח)» למטה. ההחלטה על שתי
> שורות ה-SEO התקבלה ונרשמה, ולא נדחתה שוב.

---

## עדכון 10/08/2026 (יח) — `smel`: התואר הריק ששרד את ניקוי ההירו, בחמישה משטחים

הסבב הקודם ניקה את ההירו של `Home.tsx` מ«מחקר נדל״ן חכם וברור» **ורשם במפורש**
שאותו תואר שרד בחמישה משטחים אחרים של אותו מוצר. הפריסה חסומה עד ~00:11Z
(‏core.issues #83), ולכן §6 ממשיך בקריאה — והפעם בפריט שהתור עצמו כבר ניסח.

**נמדד קודם בייצור, ורק אחר כך נגעתי:** ‏`more30.com/smel/` הגיש בדיוק את מה
שהמקור אומר — ‏`<title>` ו-`description` זהים לתו ל-`client/index.html`, ושלושה
מופעים של «חכם» בגוף העמוד. אין כאן פער בין המוגש למקור
(‏[[deploy-copy-can-break-live-sites]] הוא הסיבה שזה נבדק ולא הונח).

| משטח | היה | עכשיו | ההחלטה |
|---|---|---|---|
| `Logo.tsx:44` | «מחקר נדל״ן חכם» | «מחקר נדל״ן מנתוני ממשלה» | **הוחלף ולא נמחק** — תגית המותג רוכבת על הכותרת בכל עמוד, וסלוט ריק הוא שינוי פריסה ולא תיקון קופי |
| `Home.tsx:38` | «מקבלים דוח חכם» | «מקבלים את הדוח» | **נמחק** — גוף אותו כרטיס כבר מונה מה יש בדוח |
| `Home.tsx:324` (פוטר) | «— מחקר נדל״ן חכם.» | «— מחקר נדל״ן.» | **נמחק ולא הוחלף בנוסח התגית** — המשפט הבא באותה פסקה כבר אומר שהנתונים ממשלתיים |
| `index.html:9` ‏`<title>` | «מחקר נדל"ן חכם» | «מחקר נדל"ן מנתוני ממשלה» | ראו ההחלטה למטה |
| `index.html:10` ‏`description` | «מערכת מחקר נדלן **חכם** המבוססת…» | «מערכת מחקר נדלן המבוססת…» | ראו ההחלטה למטה |

### שתי שורות ה-SEO: ההחלטה שהתור דרש, ולא דחייה נוספת

**שתיהן שונו, מינימלית ובמכוון.** הטוקן היחיד שהוסר הוא «חכם», ואף אחד לא
מקליד אותו ליד «נדלן» — כלומר לא נמסר דבר שמדרג. ה-`<title>` **קיבל במקומו**
«מנתוני ממשלה», שהוא גם שאילתה אמיתית וגם טענה שהמוצר עומד בה, ולכן הוא לא רק
התקצר. ואיות: ה-`description` שומר על «נדלן» בלי גרש (וריאציית חיפוש נפוצה)
בעוד הכותרת נושאת «נדל"ן» — שתי הווריאציות נשארות מכוסות בין שני התגים.
**מה שלא נטען:** שהדירוג ישתפר או שלא יזוז. לא נמדד דירוג לא לפני ולא אחרי.

**אומת על הבנייה עצמה** (‏`vite preview` על `dist/public`, ‏`index-DxypDKat.js`)
ולא בקוד: **0 מופעים** של «חכם» בגוף העמוד מול 3 בייצור, שני התגים נושאים את
הנוסח החדש, וההירו ושלושת כרטיסי השלבים נשארו במקומם.

**ארבעה מופעים נשארו במקור ואף אחד מהם אינו מול משתמש:** שתי הערות ⚠️ שמצטטות
את הטקסט שהוסר (‏`Home.tsx:130-131` והחדשה ב-`index.html:10`), ו-`BUILD_BRIEF.md:4`
— מפרט הבנייה הפנימי, שנשאר כפי שהוא בכוונה: הוא תיעוד של מה שהמוצר התבקש
להיות, ושכתוב שלו מוחק את זה.

**לא נפרס** — המכסה חסומה. `dist` נבנה ולא הועתק אל `_deploy`.

`QA/platform/smel-copy-0810/_results.json` · ‏`before-production.png` /
`after-localbuild.png`.

> **הבא בתור:** הפריסה נפתחת ~00:11Z ואז לפי הסדר `15-egod` (מועתק),
> `21-mthbram`, `galil`, `chatzor`, `smel` — לארבע האחרונות לוודא זהות מול החי
> לפני ההעתקה. עד אז, §6 בקריאה: **הצורה שנמצאה כאן היא צורה נמדדת ולא
> התרשמות** — תואר ריק ששרד במשטח אחד אחרי שנוקה במשטח שכן — והיא לא נבדקה
> בשאר המערכות. `zchuyot 24` ו-`mthbram 17` הן הראשונות בסדר העבודה של המונה,
> ובשתיהן אפשר לחפש את אותו הדבר בלי דפדפן: תואר שיווקי (חכם · מתקדם · פשוט ·
> מהפכני · הטוב ביותר) בתגית מותג, בפוטר וב-`<title>`. **לא נבדק ולכן לא נטען
> שהוא שם.**
>
> ✅ שתיהן נבדקו — ראו «עדכון 10/08/2026 (יט)» למטה. `zchuyot` נקייה;
> ב-`mthbram` מה שנמצא אינו תואר ריק אלא שלושה דירוגים.


---

## עדכון 10/08/2026 (יט) — `mthbram`: התור שאל אם התואר הריק שם, והתשובה היא דירוג ולא תואר

הסבב הקודם ניסח משימת קריאה: לחפש ב-`zchuyot` וב-`mthbram` תואר שיווקי בתגית
מותג, בפוטר וב-`<title>`, ו**לא טען שהוא שם**. שתיהן נמדדו בייצור לפני שנגעתי.

**`zchuyot` נקייה, וזה ממצא ולא היעדר בדיקה.** `<title>` הוא
«בקלות — מיצוי זכויות, מענקים והטבות | הזכות שלך, האחריות שלנו» וה-description
מונה גופים בשם — ביטוח לאומי, דיור, מיסים, רווחה. אין שם תואר להסיר.

**`mthbram` נושאת משהו אחר מתואר ריק: שלושה דירוגים.** תואר ריק («חכם») הוא
מילה שלא אומרת כלום; דירוג («המובילה», «הגדול בעולם», «העולמי») הוא **טענה
בדידה שאפשר להפריך**, ואיש לא מדד אף אחת מהן.

| הטענה | משטחים | מה נעשה |
|---|---|---|
| «הפלטפורמה המובילה» | 4 — `description`, `og:`, `twitter:`, `manifest.json` | הוחלפה |
| «מאגר שיעורי התורה הגדול בעולם» | 1 — `HeroSection.tsx:127` | הוחלפה (רכיב מת, ראו למטה) |
| «הארגון העולמי של שיעורי התורה» | 10 | 1 הוחלפה · **9 פתוחות** |

הנוסח שנכנס במקום «המובילה» אינו נוסח שיווקי מרוכך אלא רשימה שכל טוקן בה
מצביע על קוד: «שיעורי תורה, חברותות והרצאות» הם שלושת העמודים של
`ThreePillarsSection`, ו«נושא, עיר, קהל יעד ושפה» הם בדיוק ארבעת הסינונים
ב-`LessonDirectory.tsx:67-83`.

### «הגדול בעולם» אינו רק לא-נמדד — הוא נסתר בעמוד עצמו

`lessons` עם מפתח האנון של האפליקציה (אותו מפתח שהדפדפן שולח) מחזירה
`Content-Range: */0`, והדף מצייר «0 שיעורים · עדיין אין שיעורים במאגר». המשפט
«הגדול בעולם» ישב מעל מאגר שמגיש אפס. לא נבדק אם RLS מסתיר שורות או שהטבלה
ריקה — מה שנמדד הוא מה שהציבור רואה.

### מה שהתגלה תוך כדי, וחשוב יותר מהתיקון עצמו

**`HeroSection.tsx` אינו מורכב לשום מסלול.** הוא מיוצא ואינו מיובא באף קובץ
ב-`src/`, אף אחד מ-20 המסלולים ב-`App.tsx` אינו מגיע אליו, ו-`document.body.innerText`
בבנייה החיה אינו מכיל **לא את הטקסט הישן ולא את החדש**. כלומר שתי ההחלפות שם
נכונות כקופי ו**אינן משנות דבר למשתמש**. זה נרשם בראש הקובץ עצמו.

**ולמה זה כמעט לא נתפס:** `Select-String` על ה-JS הבנוי הראה «0 מופעים» גם
למחרוזת שהוסרה וגם למחרוזת שנוספה — esbuild מבריח חלק מהעברית ל-`\uXXXX`,
ולכן היעדר מחרוזת ב-`dist` **אינו ראיה**. האימות היחיד שסופר כאן הוא DOM חי
ב-`vite preview`. הראיה ל-`description` היא `document.querySelector`, לא grep.

`QA/platform/mthbram-copy-0810/_results.json` · `after-localbuild-directory-empty.png` ·
`before-production-index.html`.

**לא נפרס** — המכסה חסומה עד ~00:11Z. `dist` נבנה ולא הועתק אל `_deploy`.

> **הבא בתור:** תשע ההופעות החיות של «הארגון העולמי של שיעורי התורה» —
> `LessonDetailModal.tsx:20,36`, `lessonImageExport.ts:130`,
> `AdminDashboard.tsx:164`, ופוטר ב-`OrgPortal`, `PublicOrgPage`,
> `PublicRabbiPage`, `PublicSynagoguePage`, `RabbiPortal`. **שתיים מהן אינן
> החלפת מחרוזת:** אחת נצרבת לתוך קנבס מיוצא והשנייה יוצאת בטקסט שיתוף
> לוואטסאפ, ולשתיהן צריך לראות את הפלט לפני ולא רק את השורה. ובנפרד:
> «חיפוש חכם» מופיע ב-12 קבצים ב-`mthbram` כשם של פיצ'ר, לא כתואר בודד —
> זו החלטת מינוח למוצר שלם ולא ניקוי קופי, ולכן לא נגעתי בה כאן.
---

## (כ) mthbram — «הארגון העולמי» ירד מתשעת המשטחים החיים

הסבב הקודם השאיר תור מדויק, והוא נמצא כפי שנרשם: תשע הופעות של
«הארגון העולמי של שיעורי התורה» — `LessonDetailModal.tsx:20,36`,
`lessonImageExport.ts:130`, `AdminDashboard.tsx:164`, ופוטר ב-`OrgPortal`,
`PublicOrgPage`, `PublicRabbiPage`, `PublicSynagoguePage`, `RabbiPortal`.
כולן הוחלפו ב-«שיעורי תורה, חברותות והרצאות».

**למה דווקא זה:** «חברותא» הוא ערך אמיתי ב-`LESSON_STYLES`
(`PortalLessonForm.tsx:22`) ומזהה אפשרות ב-`PublicContactForm.tsx:14`;
«הרצאות» מופיע לצד שיעורים וחברותות ב-`MissionSection.tsx:8`. השורה
מונה מה שהמוצר מחזיק, בלי דירוג ובלי טענת היקף.

**קודם נבדק שהן חיות, ולא הונח.** בניגוד ל-`HeroSection.tsx` מהסבב הקודם —
שאינו מיובא באף קובץ — לכל אחת מחמש העמודים יש `Route` ב-`App.tsx`, והמודאל
ושני המייצאים מגיעים מתוכן.

**שתי ההופעות שאינן החלפת מחרוזת נראו כפלט, לא כשורה.** הרנדור נעשה
בדפדפן אמיתי מול `drawRTL` המקורי (W=800, עוגן ימני 740, `13px Arial`, בלי
`maxWidth`): השורה החדשה היא **159.9px** מול **163.8px** של זו שהוסרה, מרווח
עד המסגרת הפנימית 705px — כלומר ה-PNG המיוצא אינו יכול להיחתך במקום שלא נחתך
קודם. וטקסט השיתוף הורכב מהתבנית האמיתית ונקרא נכון ב-RTL.

**מה שלא אומת חי, ונאמר במפורש:** חמשת הפוטרים נטענים רק מאחורי טוקן פורטל
תקף, וייצוא האדמין מאחורי הזדהות. מפתח האנון של האפליקציה מחזיר **401** על
`synagogue_portals`, `rabbi_portals` ו-`org_portals` (ואותו מפתח מחזיר 404 על
טבלה שאינה קיימת — כלומר אלה RLS ולא שגיאת שם). לכן אין צילום ייצור של פוטר
בסבב הזה, והראיה לשישה מתוך התשעה היא ספירת הבאנדל ולא DOM חי.

הספירה בבאנדל נעשתה אחרי פענוח `\uXXXX`, כי היעדר מחרוזת עברית ב-`dist`
אינו ראיה בפני עצמו: **0 ישנות · 9 חדשות**. (הפעם esbuild לא הבריח, ושתי
הספירות זהות — הפענוח נשמר כדי שהבדיקה לא תסתמך על זה.)

`QA/platform/mthbram-orgclaim-0810/_results.json` · `after-canvas-and-share.png` ·
`canvas-and-share.html`.

**לא נפרס.** `dist` נבנה ולא הועתק אל `_deploy`.

> **הבא בתור:** «חיפוש חכם» ב-12 קבצים ב-`mthbram` — זו החלטת מינוח למוצר
> שלם ולא ניקוי קופי, וצריך להכריע אותה כשם פיצ'ר לפני שנוגעים. ובמקביל:
> אותה סריקת טענות-לא-מדידות טרם נעשתה על `galil` ו-`chatzor`.
>
> ✅ הסריקה נעשתה — ראו «עדכון 10/08/2026 (כא)» למטה. **טענות אין שם**,
> ומה שכן נמצא באותו `<head>` הוא חתימת-בנאי על שלוש מערכות.

---

## עדכון 10/08/2026 (כא) — כרטיס השיתוף של שלושה אתרים חיים הוא צילום מסך של סביבת התצוגה המקדימה של הבנאי

התור אמר לסרוק את `galil` ו-`chatzor` לטענות לא-מדידות, כמו שנעשה ב-`mthbram`.
זה נעשה, **והתשובה שלילית בשתיהן**: חיפוש על `המוביל · הגדול · הטוב ביותר ·
המתקדם · הראשון · המקצועי · העולמי · הכי · אלפי · מאות · הבלעדי` בכל
ה-`tsx/ts/html/json` של `galil` החזיר שלוש שורות, וכולן אינן טענות — placeholder
בשדה קלט («בית הכנסת הגדול»), הכותרת «ניהול סיסמאות מנהל», והערה בקוד. ה-`<head>`
של שתיהן מונה מה שיש: «זמני תפילות, שיעורים ופעילויות קהילתיות». אין מה להסיר.

**מה שכן נמצא באותו `<head>`, ולא חיפשתי אותו:**

| מערכת | `og:image` ‏+ `twitter:image` בייצור | חותמת בשם הקובץ |
|---|---|---|
| `mthbram` | `…lovable.app-1772990456848.png` | 08/03/2026 17:20Z |
| `galil` | `…lovable.app-1773074843204.png` | 09/03/2026 16:47Z |
| `egod` | `…lovable.app-1777595828138.png` | 01/05/2026 00:37Z |

שלושתן על **אותו דלי** — `pub-bb2e103a32db4e198524a2e9ed8f35b4.r2.dev`, שאינו
שלנו. שם הקובץ אומר בדיוק מה זה: `id-preview-…lovable.app` — צילום מסך של
**סביבת התצוגה המקדימה של Lovable**, לא של הייצור. זה המשטח היחיד שנשלח
בוואטסאפ, והוא מציג בנייה מלפני חודשיים עד חמישה חודשים, כולל לפני כל תיקוני
הקופי והאייקונים של היום.

**שלושתן מחזירות 200 היום** (453KB · 1.85MB · 113KB), ולכן זו אינה תמונה שבורה.
הליקוי הוא בעלות ומקור: אי אפשר להחליף אותה בפריסה, אי אפשר לרענן אותה, והיא
נעלמת ביום שהפרויקט ב-Lovable יימחק.

### מה שלא נטען כאן, במפורש

**לא נבדק מה שלוש התמונות מראות.** החיבור כאן מסונן בנטפרי: הורדת זו של `galil`
החזירה 69,866 בתים מול `Content-Length` של 453,140, והבתים מפענחים ל**שלט של
נטפרי** («כעת התמונה נשלחה לנטפרי לבדיקה») ולא לאתר. הקובץ `current-og-image.png`
בתיקייה הוא השלט הזה, נשמר כראיה להחלפה. הממצא נשען רק על מה ששורד את הסינון —
המארח, שם הקובץ, החותמת, וקוד ה-HTTP.

### התיקון — נכס אמיתי איפה שיש, והסרה איפה שאין

`galil` → `more30.com/galil/pwa-512.png` (אותו סמל שה-PWA כבר מתקין),
`mthbram` → `more30.com/mthbram/icon-512.png` (אותו סמל שה-`manifest` כבר מכריז).
שניהם מוגשים 200 בייצור עכשיו (97,813 ו-10,662 בתים), ושניהם ריבועיים — ולכן
`twitter:card` ירד מ-`summary_large_image` ל-`summary`, שזה היחס שלהם באמת.

`egod` — **התגיות הוסרו ולא הוחלפו.** ל-`egod` אין שום תמונה משלה: `public/`
מחזיקה `favicon.ico` ו-`placeholder.svg` בלבד, וה-`favicon.ico` הזה הוא
**אותו קובץ בדיוק** (SHA256 זהה) ב-`egod`, ב-`mthbram`, ב-`zchuyot` וב-`galil` —
ברירת המחדל של הבנאי. הצבעה עליו הייתה מחליפה חתימת-בנאי בחתימת-בנאי. בלי
`og:image` הקישור מוצג ככותרת ותיאור, ואלה נכונים. נרשם ב-`NEEDS_USER`.

**אומת על הבנייה, לא על המקור:** שלוש בניות, ואז ה-`dist/index.html` שנפלט —
‏**0 מופעים של מארח הדלי** בשלושתן, `og:image`/`twitter:image` = 1·1·0,
ו-`twitter:card=summary`. הספירה הראשונה החזירה 1 לכל אחת והתבררה כ**הערת ה-HTML
שזה עתה נכתבה**, שמכילה את המחרוזת `r2.dev` בגוף ההסבר; נספר מחדש על המארח עצמו.
(כלל ה-`\uXXXX` מהסבב הקודם אינו חל כאן — אלה כתובות ASCII ב-HTML סטטי ולא
מחרוזות עברית בתוך באנדל.)

`QA/platform/galil-ogimage-0810/_results.json`.

**לא נפרס** — המכסה חסומה עד ~00:11Z. ושימו לב: `_deploy/egod-more30/egod`
מחזיקה בנייה שקדמה לשינוי הזה, והיא **לא נדרסה**. מי שיקבל את המשבצת חייב לבנות
מחדש ולהעתיק, ולוודא זהות מול החי לפני ([[deploy-copy-can-break-live-sites]]).

> **הבא בתור:** ה-`favicon.ico` הזהה בארבע מערכות — הוא נמדד כאן (SHA256 אחד)
> ולא תוקן, וזו חתימת-בנאי על לשונית הדפדפן של כל אחת מהן. `chatzor` ו-`smel`
> דווקא נקיות ממנו לגמרי (אין להן `favicon.ico` כלל — כדאי לבדוק מה כן מוגש להן
> בייצור לפני שמסיקים שזה טוב יותר). ובנפרד, וללא קשר לאייקונים: «חיפוש חכם»
> ב-12 קבצים ב-`mthbram` עדיין ממתין להכרעת מינוח.
>
> ✅ נמדד ותוקן חלקית — ראו «עדכון 10/08/2026 (כב)» למטה. **חתימת-הבנאי הייתה
> על לשונית אחת מתוך הארבע, לא על ארבע**, ושלוש האחרות הציגו משהו אחר לגמרי.

---

## עדכון 10/08/2026 (כב) — שלוש מתוך ארבע הלשוניות לא הראו את סמל הבנאי, אלא את הסמל של הפורטל

התור אמר: אותו `favicon.ico` יושב בארבע מערכות, וזו חתימת-בנאי על הלשונית של כל
אחת מהן. **החצי הראשון נכון והשני לא.** מה שנבדק לפני שנגעו הוא מה שהדפדפן באמת
מבקש, ולא מה שיושב ב-`public/`: `QA/platform/favicon-0810/_mounts.json`
(לצד `_results.json` באותה תיקייה, שהוא הסבב שבנה את סמל הפורטל עצמו).

הקובץ אכן אחד — ‏`SHA256 dd821076…`, ‏20,373 בתים, מוגש 200 ב-`/egod/`,
‏`/mthbram/`, ‏`/galil/` ו-`/zchuyot/`. אבל **רק ל-`egod` יש
`<link rel="icon">` שמצביע עליו.** לשלוש האחרות אין `rel="icon"` בכלל, ולכן
הדפדפן נופל לברירת המחדל שלו — `/favicon.ico` על ה-**origin**, לא על המונט.
ה-origin הוא `more30.com`, ומה שחוזר משם הוא הסמל של הפורטל (805 בתים,
`bb860cea…`). כלומר שלוש מערכות מציגות בלשונית את הסמל של אתר אחר, וה-`.ico`
של הבנאי שיושב בהן הוא קובץ שאיש לא מבקש.

זו אותה צורה שכבר מתועדת כאן בהקשר אחר — נתיב מוחלט-לשורש במערכת שמורכבת תחת
נתיב יוצא אל הפורטל ([[path-mounted-sites-need-base-href]]) — הפעם בלי שנכתב
נתיב בכלל: **ברירת המחדל של הדפדפן היא בעצמה נתיב מוחלט-לשורש.**

**הנפילה הזו נעשתה נראית רק היום.** עד קומיט `41bc299` (‏03:06 היום) לשורש לא
היה `favicon.ico` כלל, והוא החזיר 404 — כלומר שלוש הלשוניות היו ריקות ולא
שגויות. אותו סבב סיים ברשימת «לא נעשה» עם השורה «המונטים תחת נתיב אינם מצהירים
על סמל משלהם ואינם יורשים דבר מהשורש». הבדיקה כאן מתקנת את חציה השני: הם **כן**
יורשים מהשורש, וברגע שלשורש יש סמל — הוא זה שמצויר.

| מונט | מה הוצהר | מה הלשונית הראתה בפועל |
|---|---|---|
| `egod` | `/egod/favicon.ico` | ברירת המחדל של הבנאי |
| `mthbram` | — | הסמל של הפורטל |
| `galil` | — | הסמל של הפורטל |
| `zchuyot` | — | הסמל של הפורטל |
| `chatzor` | `/chatzor/favicon.svg` | נכס משלה (382b) |
| `smel` | `/smel/favicon.svg` | נכס משלה (628b) |

`chatzor` ו-`smel` נבדקו כפי שהתור ביקש, ולא הונח שהיעדר `.ico` הוא בשורה
טובה: לשתיהן `favicon.svg` משלהן, מוגש 200 ‏`image/svg+xml`. הן נקיות.

### התיקון — שתיים תוקנו, שתיים לא, ומאותה סיבה

`mthbram` ו-`galil` מחזיקות כבר נכס אמיתי משלהן, ומצביעות עליו משני מקומות
אחרים בלבד. הוא נכנס עכשיו גם ללשונית: `mthbram` → `icon-192.png` ו-`icon-512.png`
(שניהם כבר ב-`manifest.json`), `galil` → `pwa-512.png` (אותו סמל שה-PWA מתקין).
ה-`public/favicon.ico` המיותם **הוסר** משתיהן — הוא לא נקרא מאף קובץ בעץ.

`egod` ו-`zchuyot` **לא תוקנו, ובכוונה.** לשתיהן אין שום סמל משלהן:
ה-`public/` של `egod` מחזיקה `favicon.ico` ו-`placeholder.svg` בלבד
(‏`/egod/icon-512.png` מחזיר 200 אבל 3,328 בתים של `text/html` — קליפת ה-SPA),
ושל `zchuyot` מחזיקה `favicon.ico` בלבד. הצבעה על מה שיש הייתה מחליפה סמל
של הפורטל בסמל של הבנאי — כלומר מחמירה. זו אותה הכרעה שנפלה על `og:image`
של `egod` בעדכון (כא), ומאותו טעם. נרשם ב-`NEEDS_USER`.

**אומת על הבנייה, לא על המקור.** ה-`href` נכתב מוחלט-לשורש (`/icon-512.png`)
וה-`base` של ההרכבה כתב אותו מחדש — ולכן ה-`dist` הוא מה שנמדד, ב-DOM חי של
`vite preview`: ‏`mthbram` שני `rel="icon"` שנפתרים ל-`/mthbram/…`, ‏200
‏`image/png`, ‏20,797 ו-10,662 בתים; `galil` אחד, `/galil/pwa-512.png`, ‏200,
‏97,813 בתים. **והדפדפן עצמו הוציא את הבקשה** — `/mthbram/icon-192.png` מופיע
בתעבורה, ולא רק ב-DOM. ‏`/favicon.ico` על ה-origin של התצוגה המקדימה מחזיר 404,
וזו בדיוק הנפילה שכבר לא נלקחת. `favicon.ico` אינו ב-`dist` של אף אחת מהשתיים.

**אין צילום מסך, וזו לא השמטה:** סמל הלשונית יושב בכרום של הדפדפן ולא ב-viewport,
ולכן `screenshot` אינו יכול להראות אותו. הראיה היא ה-`href` שנפתר, הבתים שחזרו,
ובקשת הרשת.

**לא נפרס** — ה-`dist` של שתיהן נבנה ולא הועתק אל `_deploy`.

> **הבא בתור:** ‏`egod` ו-`zchuyot` חסומות על נכס ולא על קוד — צריך סמל אחד לכל
> אחת, ואז זו שורה אחת ב-`index.html` בכל אחת. עד אז הלשונית שלהן מציגה סמל של
> אתר אחר, וזה ידוע ורשום. ובנפרד: «חיפוש חכם» ב-12 קבצים ב-`mthbram` עדיין
> ממתין להכרעת מינוח.
>
> ✅ נמדד על כל 24 המונטים — ראו «עדכון 10/08/2026 (כג)» למטה. **16 מציגים את
> סמל הפורטל ולא 4**, וחמישה מהם עושים זאת דווקא **בזכות** הצהרה שיש להם.

---

## עדכון 10/08/2026 (כג) — 16 מתוך 24 הלשוניות מציירות את סמל הפורטל, וחמש מהן מצהירות על סמל משלהן

הסבב הקודם מדד **שישה** מונטים ומצא צורה: מונט בלי `<link rel="icon">` אינו
מקבל לשונית ריקה — הדפדפן נופל אל `/favicon.ico` על ה-**origin**, וה-origin הוא
`more30.com`. הסבב הזה הריץ את אותה מדידה על **כל 24** המונטים
(`scripts/qa/favicon-mounts.mjs`, ייצור בלבד, לפני שנגעתי).

**16 מתוך 24 מציגים את סמל הפורטל.** ומעניין יותר מהמספר: הם מגיעים לשם בשתי
דרכים שונות, והשנייה לא הייתה ידועה.

| מה | כמה | המונטים |
|---|---|---|
| אין `rel="icon"` כלל | 11 | `tamlul` · `bkalot` · `smachot` · `orech` · `zchuyot` · `mthbram` · `galil` · `crm` · `gesher` · `kesef` · `kiosk` |
| **יש `rel="icon"`, והוא נוחת על ה-origin** | 5 | `torah` · `modaot` (‏`/favicon.svg`) · `imud` · `chizukim` · `studio` (‏`/favicon.png`) |
| נכס משלהם | 8 | `briut` · `smel` · `egod` · `chatzor` · `mechiron` · `kupot` · `nadlan` · `tivuch` |

(‏`mthbram` ו-`galil` תוקנו בסבב הקודם; הבנייה לא נפרסה, ולכן הייצור עדיין מראה
את התקלה. הן נספרות כאן לפי מה שמוגש.)

### חמש ההצהרות שנוחתות על ה-origin

זו **אינה** אותה תקלה בשם אחר. תג קיים, נפתר ל-200, ובכל זאת מצייר את הפורטל:
`href="/favicon.svg"` הוא נתיב מוחלט-לשורש, ומונט שיושב תחת נתיב פותר אותו מול
ה-origin ולא מול המונט. `torah` ו-`modaot` מקבלות את `favicon.svg` של הפורטל
(‏806 בתים), ו-`imud`, `chizukim` ו-`studio` את `favicon.png` שלו (‏4,477 בתים).
זו [[path-mounted-sites-need-base-href]] בשכבה שלישית — הפעם הנתיב **כן** נכתב,
ונכתב כמו שכותבים אותו לאפליקציה שיושבת בשורש.

**וגם זה נעשה נראה רק היום:** עד קומיט `41bc299` (‏03:06) לשורש לא היה סמל כלל,
וחמש ההצהרות האלה החזירו **404**. הן לא התחילו להצביע על הפורטל — הפורטל התחיל
לענות.

### התיקון — שתיים מתוך השש-עשרה, ומאותו קריטריון כמו בסבב הקודם

רק שתיים מהשש-עשרה **מחזיקות כבר נכס משלהן** ולא מצביעות עליו:

| מונט | היה | עכשיו | הנכס שכבר מוגש |
|---|---|---|---|
| `tamlul` (‏`02-igud-transcribe`) | אין שדה `icons` בכלל | `icons: { icon: "/tamlul/favicon.svg" }` | `public/favicon.svg`, ‏505 בתים |
| `modaot` (‏`03-igud-ads`) | `icons: { icon: "/favicon.svg" }` | `icons: { icon: "/modaot/favicon.svg" }` | `public/favicon.svg`, ‏383 בתים |

**למה הקידומת נכתבה ביד, ולא נגזרה:** שתיהן Next 14 עם `basePath` ו-`assetPrefix`
ב-`next.config.mjs`, ו-`metadata.icons` הוא **המקום היחיד ב-Next שאינו מקבל
אותם** — בניגוד ל-`<Image>`, ל-`<Link>` ולמוסכמת הקבצים `app/icon.*`. זו בדיוק
הסיבה שההצהרה הקיימת של `modaot` פספסה: היא נכתבה כמו שכותבים לאפליקציית שורש.

**אומת על רנדור אמיתי ולא בקוד:** `next dev` על כל אחת, טעינה דרך ה-`basePath`,
ואז דפדפן Chromium אמיתי. בשתיהן `rel="icon"` יחיד ב-DOM שנפתר אל המונט,
**והדפדפן עצמו הוציא את הבקשה** (‏`/tamlul/favicon.svg` ו-`/modaot/favicon.svg`
בתעבורה, לא רק ב-DOM), הנכס חוזר 200 `image/svg+xml` ב-505 ו-383 בתים,
ו-`/favicon.svg` על ה-origin של התצוגה המקדימה מחזיר 404 — הנפילה שהייתה כבר
אינה זמינה.

**וספירת הבתים היא גם הראיה שהנכס הוא של המערכת:** 505 ו-383 זהים לקבצים בעץ.
בייצור אי אפשר היה לאשש את זה — נטפרי ממירה SVG ל-WebP בדרך, ואותם שני קבצים
חזרו כ-646 ו-1,370 בתים `image/webp`. המדידה המקומית היא היחידה שסופרת כאן.

**שתים-עשרה נותרו, וכולן חסומות על נכס ולא על קוד** — אין להן שום סמל משלהן, והצבעה
על ברירת המחדל של הבנאי הייתה מחליפה סמל של הפורטל בסמל של הבנאי, כלומר מחמירה.
זו אותה הכרעה שנפלה על `egod` ו-`zchuyot` בעדכון (כב). נרשם ב-`NEEDS_USER`.

**אין צילום מסך** — סמל הלשונית אינו ב-viewport. הראיה היא ה-href שנפתר, הבתים
שחזרו, ובקשת הרשת.

**לא נפרס.** אף אחת מהשתיים לא נבנתה לייצור ולא הועתקה אל `_deploy`.

`QA/platform/favicon-mounts-0810/_results.json` · `_scan.json` (הסריקה הגולמית
של 24 המונטים).

> **הבא בתור:** שתים-עשרה לשוניות ממתינות לנכס אחד כל אחת, וזה פריט למשתמש ולא
> לי. מה שכן אפשר בלי נכס: `imud`, `chizukim` ו-`studio` מצהירות על
> `/favicon.png` — **קובץ שאין להן**, והוא נענה רק במקרה על ידי הפורטל. שווה
> להכריע אם עדיף להשאיר הצהרה שמצביעה על נכס של אתר אחר, או להסיר אותה ולהגיע
> לאותה תוצאה בלי לטעון בעלות. ובנפרד: «חיפוש חכם» ב-12 קבצים ב-`mthbram` עדיין
> ממתין להכרעת מינוח.
>
> ✅ אחת מהשתים-עשרה יצאה — ראו «עדכון 10/08/2026 (כד)» למטה. ‏`zchuyot` **לא**
> הייתה חסרת נכס: יש לה נכס, והוא של הבנאי. עכשיו נמדד ולא מונח.

---

## עדכון 10/08/2026 (כד) — `zchuyot`: «חסום על נכס» היה חצי נכון, והחצי השני נמדד

הסבב הקודם כתב ששתים-עשרה הלשוניות חסומות על נכס, ו**הנמקה** למה לא להצביע על
ברירת המחדל של הבנאי: היא הייתה מחליפה סמל של הפורטל בסמל של הבנאי. זו הייתה
שיפוט ולא מדידה. עכשיו זו מדידה.

**‏SHA-256 של כל `favicon.*` בעץ `apps/`** (בלי `node_modules` ו-`.git`) מחזיר
ארבעה עותקים **זהים לבית** של אותו קובץ, 20,373 בתים, `DD821076A9B0…`:
`22-get-your-rights` · `15-egod` · `_archive/bklotm` · `_archive/lux-manage`.
ארבעה פרויקטי Lovable בלתי-קשורים, שניים מהם בארכיון. זו ברירת המחדל של הבנאי,
וההנמקה החזיקה.

אבל הסיווג לא: `zchuyot` נספרה כמי ש**אין לה נכס**. יש לה — הוא פשוט לא שלה.

### הסמל שנכתב, ומאיפה כל צבע בו הגיע

| רכיב | הערך | מאיפה |
|---|---|---|
| רקע (מעבר) | `#2D7654` → `#1C543A` | `primary` ‏(152 45% 32%) ו-`emerald-dark` ‏(152 50% 22%) ב-`src/index.css` |
| כיוון המעבר | 135° | אותו כיוון של `hero-gradient` |
| האות | `#EEB32B` | `secondary` / `gold` ‏(42 85% 55%) |

**האות היא ב׳ — ולא סמל גרפי — כי זה מה שהמוצר באמת מצייר.** ‏`Navbar.tsx:52-61`
הוא כיתוב טקסט בלבד, «בקלות», בלי שום סימן לצידו.

**ומה שנראה כמו המקור המתבקש הוא קוד מת:**
`src/components/AnimatedLogo.tsx` מצייר טבעת וּוי באותה פלטה בדיוק, והוא **אינו
מיובא באף קובץ** — הליטרל `M20 30 L27 37 L40 22` אינו מופיע בחבילה הבנויה.
סמל שנלקח ממנו היה סימן שאף מבקר לא ראה מעולם. זו אותה מלכודת של עדכון (יז).

**האות מצוירת כנתיב ולא כאלמנט `text` בכוונה:** ‏SVG שמשמש כסמל לשונית נטען
כתמונה ולכן אינו מביא משאב חיצוני — Rubik לא היה מגיע, וגופן המערכת אינו מובטח
לתמוך בעברית בכל מערכת הפעלה.

### תקלה שהסבב הזה ייצר, ונתפסה רק בציור

הגרסה הראשונה של `favicon.svg` **לא צוירה כלל** — תמונה שבורה בכל ארבעת הגדלים.
הסיבה: הערת הפתיחה שלה ציטטה את שמות הטוקנים כפי שהם ב-CSS, כולל שני המקפים
(`--primary`). **הערת XML אינה יכולה להכיל שני מקפים רצופים**, והמסמך כולו נפל
בפענוח.

מה שהופך את זה למעניין הוא מה שכן עבר: הקובץ הוגש `200 image/svg+xml` בספירת
הבתים הנכונה, והדפדפן **הוציא את הבקשה**. כלומר כל בדיקה שקצרה מציור — סטטוס,
טיפוס תוכן, בתים, בקשת רשת — הייתה מכריזה הצלחה. רק ה-`<img>` הראה את השבר.

### האימות

`vite build`, ואז `vite preview` על `base=/zchuyot/`, ואז Chromium אמיתי:
`rel="icon"` יחיד ב-DOM שנפתר אל `http://127.0.0.1:4361/zchuyot/favicon.svg`,
**הדפדפן עצמו הוציא את הבקשה** (בקשה 19, בלי מחרוזת שאילתה — הבקשה של הלשונית),
`naturalWidth/Height` ‏64×64 ו-`decode()` שנפתר בכל ארבעת הגדלים, ו-`/favicon.svg`
על ה-origin מחזיר **404** — הנפילה שהייצור לוקח אינה זמינה כאן.

**ו-Vite לא הכפיל את הקידומת:** ‏`dist/index.html` נושא `href="/zchuyot/favicon.svg"`
בדיוק כפי שנכתב. החבילה לא זזה — `index-0ikxyjGB.js` לפני ואחרי.

**`public/favicon.ico` של הבנאי נשאר במקומו ופשוט אינו מוצהר.** איש אינו מבקש
אותו: נפילת ה-`.ico` של הדפדפן הולכת ל-origin ולא למונט.

**לא נפרס.** הבנייה לא הועתקה אל `_deploy/zchuyot-more30`, ולכן הייצור עדיין
מגיש את סמל הפורטל למונט הזה.

`QA/platform/favicon-zchuyot-0810/_results.json` · `mark-at-four-sizes.png`
(הנכס מצויר כ-`<img>` ב-16/32/64/128 — **לא** צילום של הלשונית; סמל לשונית יושב
בכרום של הדפדפן ואינו ב-viewport).

> **הבא בתור:** ‏`15-egod` נושאת את **אותו קובץ בדיוק** של הבנאי, ולכן היא
> המקרה הזהה הבא — ובה, בניגוד ל-`zchuyot`, יש לבדוק קודם אם הנווט מצייר סימן
> גרפי חי או כיתוב בלבד, כי זה מה שקובע מה הסמל צריך להיות. עשר הנותרות אין להן
> `favicon.*` כלל בעץ, וזו קטגוריה אחרת. ובנפרד, שני פריטים שלא זזו: שלוש
> ההצהרות על `/favicon.png` שאין לו בעלים (‏`imud` · `chizukim` · `studio`),
> ו«חיפוש חכם» ב-12 קבצים ב-`mthbram` שממתין להכרעת מינוח.

## עדכון 10/08/2026 (כה) — ‏`egod`: הלשונית **הצהירה** על סמל, וזה בדיוק מה שהסתיר את הבאג

עדכון (כד) השאיר שאלה מדידה ולא טענה: ‏`15-egod` נושאת את אותו קובץ בדיוק של
הבנאי (‏`DD821076A9B0`, ‏20,373 בתים), ולכן היא המקרה הזהה הבא — אבל בה, בניגוד
ל-`zchuyot`, צריך לבדוק **קודם** אם הנווט מצייר סימן גרפי חי או כיתוב בלבד.

**התשובה: סימן גרפי חי.** ‏`Navbar.tsx:35-37` מצייר אריח מעוגל עם הגליף
`BookOpen` של lucide בתוכו, בתוך `Layout.tsx` שכל עמוד עוטף בו (נבדק
`pages/Index.tsx`). לכן כאן, בשונה מ-`zchuyot`, לא נכתבה אות — הועתק הסימן.

### הבאג שנמצא כאן אינו מה שחיפשנו

`zchuyot` נמנתה בין המונטים **בלי** הצהרת סמל. ‏`egod` **הצהירה**:

```html
<link rel="icon" type="image/x-icon" href="/favicon.ico">
```

ובכל זאת הלשונית ציירה את הסמל של הפורטל. הסיבה היא זו של עדכון (כג): נתיב
מוחלט-לשורש נפתר מול ה-**origin** ולא מול המונט, ולכן מי שנענה הוא
`more30.com/favicon.ico`. הקובץ שהשורה כביכול הצביעה עליו — `public/favicon.ico`
של הבנאי — **לא נטען מעולם**. כלומר שורת ההצהרה לא רק שלא עזרה, היא גם גרמה
לספירה להיראות טוב יותר ממה שהיא.

### היחסים נמדדו, לא נבחרו

`getComputedStyle` על ה-DOM החי: האריח ‏40×40 עם רדיוס 12px ורקע
`rgb(196, 161, 79)`, הגליף ‏24×24 בלבן עם `stroke-width` ‏2. מכאן, לצלע 64:
‏`rx=19.2` (‏12/40), תיבת גליף 38.4 (‏24/40) ממורכזת — ‏`translate(12.8) scale(1.6)`
— וקו שנצייר כ-3.2. שני נתיבי הגליף הועתקו כלשונם מ-
`lucide-react/dist/esm/icons/book-open.js` (‏v0.462.0).

### מה כן הוכרע כאן, ולמה

הזוג החי הוא **לבן על זהב**, ויחס הניגודיות שלו הוא **2.27:1** — מתחת ל-3:1,
המינימום גם לגרפיקה שאינה טקסט, ולשונית נצפית ב-16px. לכן נלקח הזוג ההפוך,
שיושב חי **באותו נווט עצמו**: כפתור «התחברות» הוא `border-secondary
text-secondary` על סרגל `bg-primary`, כלומר זהב על נייבי. האריח נושא את
`gradient-navy` כלשונו (‏135° מ-`#1B2C5A` אל `#213183`) והגליף הוא
`secondary/gold` ‏`#C4A14F` — ‏5.95:1 בקצה הכהה, 5.05:1 בקצה הבהיר. כל ערך הוא
טוקן מ-`src/index.css`; מה שנבחר כאן הוא **איזה** זוג טוקנים, וזה נאמר במפורש
בראש הקובץ.

### האימות

`vite build`, ואז `vite preview` על `base=/egod/`, ואז Chromium אמיתי:
`rel="icon"` יחיד ב-DOM שנפתר אל המונט, **הדפדפן עצמו הוציא את הבקשה**
(‏`initiatorType: "other"`, ‏1,871 בתים, בלי מחרוזת שאילתה), הגשה
`200 image/svg+xml`, ‏`DOMParser` בלי `parsererror` (המלכודת של שני המקפים
מעדכון (כד) נמנעה מראש), ‏`decode()` שנפתר ב-64×64 בארבעה גדלים,
ו-`/favicon.svg` על ה-origin מחזיר **404** — הנפילה שהייצור לוקח אינה זמינה כאן.
‏`dist/index.html` נושא `href="/egod/favicon.svg"` בדיוק כפי שנכתב.

`public/favicon.ico` של הבנאי נשאר על הדיסק ואינו מוצהר עוד.

**לא נפרס.** הבנייה לא הועתקה אל `_deploy`, ולכן הייצור עדיין מגיש את סמל
הפורטל למונט הזה. לא נכתב ICO/PNG, ולכן לקוח שאינו תומך בסמלי SVG עדיין נופל
ל-origin.

`QA/platform/favicon-egod-0810/_results.json` · `mark-vs-navbar.png` — הסימן החי
משובט מה-DOM עם ה-CSS שלו, לצד הסמל החדש בארבעה גדלים ולצד ה-`.ico` של הבנאי
(לב צבעוני), כך שההבדל בין השלושה נראה בתמונה אחת.

> **הבא בתור:** עשר המערכות הנותרות אין להן `favicon.*` כלל בעץ, וזו קטגוריה
> אחרת — בכל אחת צריך קודם לבדוק, כמו כאן, אם הנווט מצייר סימן גרפי חי (ואז יש
> מה להעתיק ואין צורך בנכס ממך) או כיתוב בלבד. שני הפריטים שלא זזו נשארים: שלוש
> ההצהרות על `/favicon.png` שאין לו בעלים (`imud` · `chizukim` · `studio`),
> ו«חיפוש חכם» ב-12 קבצים ב-`mthbram` שממתין להכרעת מינוח.

## עדכון 10/08/2026 (כו) — ‏`torah`: הלשונית הצהירה על קובץ שמעולם לא היה קיים

‏`torah` נמנתה בקבוצת «מצהירה ונוחתת על ה-origin» (עדכון כג), והיא המקרה החמור
בקבוצה: לא רק שהנתיב המוחלט-לשורש נפתר מול ה-origin, אלא ש-`public/` שלה החזיקה
`.gitkeep` ו-`logo.svg.placeholder` בלבד — **‏`favicon.svg` לא היה שם כלל.**
ההצהרה הצביעה על קובץ שלא קיים, נענתה על ידי `more30.com/favicon.svg` של הפורטל,
וגרמה למונט להיספר כ«מצהיר סמל».

‏`Vite` **אינו** מוסיף את ה-`base` לשורה הזו — נבדק על `dist/index.html`, שנשא
`href="/favicon.svg"` כלשונו אחרי בנייה עם `base` ‏`/torah/`. הקידומת נכתבה ביד,
ואחרי הבנייה `dist/index.html` נושא `/torah/favicon.svg` בדיוק — בלי כפילות.

### מה הנווט מצייר, ולמה הסמל בכל זאת אינו האות

`getComputedStyle` על ה-DOM החי של `more30.com/torah`: אין `logo_url` לדייר, ולכן
‏`Navbar.tsx:60-66` נופל לענף השני — אריח `40px` ברדיוס `14px`, רקע
`rgb(26, 47, 91)` (הטוקן `primary`, ‏`222 56% 23%`), אות בלבן. הזוג הוא **13.3:1**,
הרבה מעל 3:1, ולכן — בשונה מ-`egod` — לא הוחלף כאן שום זוג טוקנים.

אבל **האות אינה של המערכת אלא של הדייר**: הענף מצייר `site_name.charAt(0)`,
והדייר שהמונט מגיש היום נקרא «איגוד מגידי השיעורים», ולכן החי הוא **א'** ולא
ה-**ת'** שכתובה בקוד כברירת מחדל. קובץ אחד למונט אין לו גישה לדייר.

### שתי טעויות שהצילום תפס, ולא הקוד

האות א' צוירה ביד כשלושה קווים. היא **יצאה X לטיני** — הזרוע השמאלית-עליונה
והרגל הימנית-תחתונה יצאו כמעט על אותו ישר והתמזגו דרך המרכז. ניסיון שני, אחרי
מדידת הגליף האמיתי בהגדלה ל-400px על רשת של 50px, **יצא X גם הוא**. הציור נזנח
ולא נשלח. מה שתפס את זה בשתי הפעמים היה צילום ההשוואה, לא בדיקה בקוד.

הסמל שנשלח הוא `Mic` של lucide — הסימן שהמערכת עצמה מצהירה עליו
(‏`BrandLogo.tsx:1,20`), ולא ראשי-התיבות של דייר אחד. שני הנתיבים והקו הועתקו
כלשונם מ-`lucide-react/dist/esm/icons/mic.js` (‏v0.462.0). היחסים הם של
`BrandLogo`: אריח `w-9` עם גליף `w-4`, כלומר 44.4% — ‏`translate(17.8)
scale(1.1833)` לצלע 64. ערך אחד במפורש אינו החי: עובי הקו, ‏2 ב-lucide, שהוא
פחות מפיקסל ב-16px, נכתב 2.8.

### באג שנמצא בדרך ולא תוקן

`BrandLogo.tsx` מבקש `bg-gradient-gold` ו-`text-navy`. **שני השמות אינם מוגדרים**
לא ב-`src/index.css` ולא ב-`tailwind.config.ts` — ב-`index.css` יש
`.text-gradient-gold`, שם אחר, ובקונפיג אין צבע `navy` כלל. כלומר סימן המותג של
המערכת מצויר בלי רקע ובלי צבע גליף בכל מקום שבו הוא מורכב: `Footer.tsx:69`
ו-`RoleSelection.tsx:19`. הזוג שנלקח לסמל הוא זה שנמדד **חי בנווט**, ולא זה
שהקוד מבקש ואינו מקבל. בנפרד: האריח בנווט הוא `h-10 w-10` אבל נמדד ברוחב
‏**35.7px** — `flex` מכווץ אותו, כך שהאריח ה«ריבועי» אינו ריבוע.

### האימות

`vite build`, ‏`vite preview` על `base=/torah/`, ואז Chromium אמיתי: `rel="icon"`
יחיד ב-DOM שנפתר אל המונט, **הדפדפן עצמו הוציא את הבקשה** (‏`initiatorType:
"other"`, ‏1,220 בתים, בלי מחרוזת שאילתה — נתפס בטעינה הראשונה של ההצהרה; בבדיקה
החוזרת הוא הוגש מהמטמון ולכן לא הופיעה רשומה חדשה), הגשה `200 image/svg+xml`,
‏`DOMParser` בלי `parsererror`, ‏`decode()` בארבעה גדלים, ו-`/favicon.svg` על
ה-origin מחזיר **404**.

**לא נפרס.** ‏`dist` לא הועתק אל `_deploy`, ולכן הייצור עדיין מגיש את סמל הפורטל
למונט הזה. לא נכתב ICO/PNG. ב-16px ראש המיקרופון והרגל כמעט מתמזגים — זה נקרא
כסימן, לא כמיקרופון קריא.

`QA/platform/favicon-torah-0810/_results.json` · `mark-vs-navbar.png` (הסמל
ב-16/32/64/192 לצד שכפול של אריח הנווט החי) · `_glyph-grid.png` (הגליף האמיתי על
רשת — ההוכחה שהציור ביד נכשל) · `_zoom.png`.

> **הבא בתור:** בקבוצת «מצהירה ונוחתת על ה-origin» נשארו **שלוש**, וכולן על
> `/favicon.png` שאין לו בעלים: `imud` · `chizukim` · `studio`. שלושתן אפליקציות
> `client/` ולא `src/`, ולכן הן מקרה מבני אחר מכל מה שנעשה עד כה — כדאי למדוד
> קודם היכן `index.html` שלהן יושב ומה ה-`base` בפועל. עשר המערכות בלי
> `favicon.*` כלל נשארות קטגוריה שלישית. ובנפרד: `BrandLogo` עם שני שמות מחלקה
> שאינם קיימים, ו«חיפוש חכם» ב-12 קבצים ב-`mthbram` שממתין להכרעת מינוח.
>
> ✅ המדידה נעשתה על שלושתן ואחת יצאה — ראו «עדכון 10/08/2026 (כז)» למטה.
> המבנה זהה בשלוש, אבל **ב-`studio` ה-`base` אינו המונט**, וזו תקלה שנייה.

---

## עדכון 10/08/2026 (כז) — ‏`imud`: המבנה נמדד בשלוש, ובאחת מהן ה-`base` אינו המונט

התור ביקש למדוד קודם היכן `index.html` יושב ומה ה-`base` בפועל, ולא להניח
ששלוש האפליקציות האלה הן כמו קודמותיהן. זה נעשה, **והמבנה אכן זהה בשלושתן**:
‏Vite עם `root: client`, ‏`index.html` ב-`client/index.html`, פלט אל `dist/public`.

| אפליקציה | מונט | ה-`base` בקונפיג | תואם למונט? |
|---|---|---|---|
| `04-imud-torani` | `/imud/` | `/imud/` | ✅ |
| `17-chizukim-transcribe` | `/chizukim/` | `VITE_BASE \|\| "/chizukim/"` | ✅ |
| `26-modaot-studio` | `/studio/` | **`/modaot/`** | ❌ |

**השורה השלישית היא ממצא ולא הערת שוליים.** ‏`studio` נבנית עם `base` של מונט
אחר, וזו תקלה **נפרדת** מזו שהסבב הזה בא לתקן — היא נראתה כאן ולא טופלה, וגם
לא נחקרה. ‏`imud` נלקחה ראשונה בדיוק מהסיבה ההפוכה: אצלה ה-`base` הוא המונט,
ולכן ה-`href` של הסמל הוא המשתנה היחיד.

### מה הנווט מצייר, ומדוע כאן לא הוחלף אף זוג טוקנים

‏`SiteHeader.tsx:14-21` מצייר **ספר פתוח** — סימן גרפי חי, לא כיתוב. ‏`getComputedStyle`
על ה-DOM החי ב-`more30.com/imud`: הגליף `rgb(134, 39, 56)` (הטוקן `primary`,
‏349 55% 34%) על משטח `rgb(250, 248, 245)` (הטוקן `background`, ‏40 33% 97%).
הזוג הזה הוא **8.4:1** — הרבה מעל 3:1 — ולכן, בשונה מ-`egod`, לא נלקח כאן זוג
חלופי. הסמל נושא את הצבעים שהמוצר באמת מצייר.

**ונבדק שהרכיב חי לפני שהועתק ממנו:** ‏`SiteHeader` מיובא ומורכב ב-`Home`,
ב-`Templates` וב-`Wizard`, ו-`Logo` גם ב-`Editor`. זו הבדיקה ששני סבבים קודמים
(‏יז · כד) למדו לעשות אחרי שהעתיקו מרכיב שאיש לא מיובא.

### שלוש סטיות מהחי, כולן בגלל 16px, וכולן מוצהרות

1. **נוסף אריח.** הסימן החי הוא קו בלבד בלי משטח משלו; על לשונית זה יוצא שקוף
   ונבלע גם בכרום בהיר וגם בכהה. האריח נושא את **צבע המשטח שהסימן החי יושב
   עליו**, ולכן הזוג לא השתנה — רק נוסף לו רקע.
2. **עובי הקו** ‏2.2 → 4 לפני קנה מידה 0.86, כלומר 3.44 אפקטיביים. ‏2.2 היה
   יוצא **0.63px** ב-16px, פחות מפיקסל.
3. **האות א' המסוגננת הוסרה** (הנתיב השלישי, עובי 1.4 בשקיפות 0.55). ב-16px
   פנים הספר הוא כ-4px לכל צד, ושלושה קווים חוצים שם סותמים את הצורה במקום
   להיקרא כאות.

**ומה שנבחר ולא נמדד, במפורש:** רדיוס האריח. לסימן החי אין אריח ולכן אין לו
רדיוס למדוד; נלקח הטוקן `radius` ‏(0.5rem = 8px) כ-8 יחידות בתיבה בת 48. זו
פרשנות, לא העתקה, והיא רשומה גם בראש קובץ ה-SVG.

### האימות

`vite build`, ‏`vite preview`, ואז Chromium אמיתי מול הפלט הבנוי: `rel="icon"`
יחיד שנפתר אל `‏/imud/favicon.svg` (‏**Vite לא הכפיל את הקידומת** — `dist/index.html`
נושא בדיוק את מה שנכתב), הגשה `200 image/svg+xml` ב-2,750 בתים **הזהים לקובץ
שעל הדיסק**, ‏`DOMParser` בלי `parsererror`, ‏`decode()` שנפתר ב-16/32/64/192,
ו-`/favicon.svg` · `/favicon.png` · `/favicon.ico` על ה-origin מחזירים **404** —
הנפילה שהייצור לוקח אינה זמינה כאן. `favicon.png` אינו ב-`dist`.

**הפרדת הבדיקות אינה טקס:** עדכון (כד) שלח SVG שהוגש 200 בספירת הבתים הנכונה
ולא צויר כלל. סטטוס, טיפוס תוכן ובתים עוברים על קובץ שבור; רק פענוח וציור תופסים.

### מה לא נטען כאן

**בקשת הלשונית של הדפדפן עצמו לא נתפסה בסבב הזה.** סבבים קודמים רשמו אותה
(‏`initiatorType: "other"`), וכאן יומן הרשת ריק מ-`favicon` בטעינה נקייה — הנכס
כבר היה במטמון מהבדיקות שלמעלה, ולדפדפן ללא ראש אין סרגל לשוניות. הראיה כאן
היא ה-`href` שנפתר, הבתים שהוגשו, הפענוח והציור — **לא** בקשה שנתפסה.
ובנוסף: לא נכתב ICO/PNG, ולכן לקוח שאינו תומך בסמלי SVG עדיין נופל ל-origin.

**אין צילום של הלשונית** — היא בכרום של הדפדפן ולא ב-viewport.
`mark-vs-navbar.png` מצייר את הנכס כ-`<img>` בארבעה גדלים, לצד שכפול של הנווט
החי ולצד `more30.com/favicon.png` — כך שרואים בתמונה אחת שמה שהלשונית הציגה עד
היום הוא סמל של אתר אחר.

**לא נפרס.** ‏`dist` נבנה ולא הועתק אל `_deploy`, ולכן הייצור עדיין מגיש את סמל
הפורטל למונט הזה.

`QA/platform/favicon-imud-0810/_results.json` · `mark-vs-navbar.png` · `mark-vs-navbar.html`.

> **הבא בתור:** ‏`chizukim` — אותו מבנה בדיוק, ה-`base` תואם, ונשאר רק לבדוק מה
> הנווט שלה מצייר. ‏`studio` אחריה, **ובה שתי תקלות ולא אחת**: הסמל, ובנפרד
> ה-`base` שנבנה כ-`/modaot/` בעוד המונט הוא `/studio/` — כדאי להכריע את השנייה
> לפני שנוגעים בראשונה, כי היא קובעת לאן ה-`href` צריך להצביע. עשר המערכות בלי
> `favicon.*` כלל נשארות קטגוריה שלישית. ובנפרד, שני פריטים שלא זזו: `BrandLogo`
> עם שני שמות מחלקה שאינם קיימים, ו«חיפוש חכם» ב-12 קבצים ב-`mthbram`.

## עדכון 19/08/2026 — שתי הנקודות הפתוחות למעלה כבר נסגרו בעבר, בלי שהקובץ הזה עודכן; ונמצא כשל שלישי, נפרד

הרשומה הזאת נכתבה כ"הבא בתור: chizukim, ואחריה studio" — אבל בפועל שתיהן
כבר טופלו בסבבים קודמים שלא חזרו לעדכן את הקובץ הזה: `chizukim` בקומיט
`523cfe58` (favicon.svg נכתב, index.html מצהיר `/chizukim/favicon.svg`
מוחלט-למונט), ו-`studio` בקומיט `10c4be9b` (ה-`base` ב-`vite.config.ts`
תוקן מ-`/modaot/` ל-`/studio/`) וקומיט `662eec5a` (favicon עצמו — Crown
זהב על נייבי, 7.74:1). שתי התקלות שהרשומה הזו חיכתה להן סגורות.

**אבל:** בדיקה של מה שבאמת יושב בעץ העבודה הזה (לא בהיסטוריית git, בעץ
עצמו) גילתה שהקובץ שהופק ונפרס ב-662eec5a **מעולם לא נכנס למקור** —
`git log` על `apps/26-modaot-studio/client/public/favicon.svg` לא מחזיר
אף קומיט, ו-`_deploy/studio-more30` המקומי לא מחזיק תיקיית `public/`
בכלל. זה בדיוק דפוס "אבד בכשל git-add" שתועד ב-`DECISIONS.md` #36-39
עבור 22 קבצי gannenet אחרים — נכס שנכתב ישירות בעץ עבודה של סבב קודם
ולא נשמר. `index.html` עדיין מצהיר את הנתיב הנכון (`/studio/favicon.svg`,
מוחלט-למונט), ולכן הפער לא נראה בייצור החי היום (שנפרס מ-662eec5a) —
הוא ייראה בפעם הבאה שמישהו יריץ `vite build` מהעץ הזה ויפרוס, בדיוק מה
שממתין כ"נשאר לסבב הבא" על CHECKLIST/graphics.md item 13/14.

**התיקון:** `favicon.svg` נכתב מחדש במקור, שחזור נאמן ולא ניחוש —
הגליף `Crown` הועתק מ-`lucide-react` ^0.453.0 (הגרסה הנעוצה ב-
`package.json` של studio, אומתה זהה למותקן בפועל ב-`node_modules` של
מערכת אחרת באותו מונו-רפו), הצבעים `#C9A227` על `#0B1220` נמדדו מ-
`Home.tsx:111,122,130` (לא נבחרו), והניגודיות חושבה מחדש (WCAG relative
luminance): **7.744:1** — תואם בדיוק את ה-7.74:1 שתועד ב-662eec5a, אישוש
עצמאי שהצבעים והגרסה נכונים. אין אריח-מיכל חי למדוד ממנו יחס (הסמל
בסרגל חשוף בלי רקע), ולכן הרדיוס (8px) נלקח ישירות מטוקן ה-radius של
האפליקציה (`index.css:64`, `0.5rem`) ולא מיחס מומצא. אומת רק כ-XML
תקין (`xml.dom.minidom`, ללא שגיאת פענוח) — **לא** `vite build`/`preview`:
`node_modules` אינו קיים בעץ הזה, ולפי כללי הסבב אין להתקין. זה הבדל
מהותי מרמת האימות שכל שאר סמלי הלשונית בקובץ הזה קיבלו (Chromium אמיתי,
פענוח פיקסלים) — נרשם כאן כדי שלא יובן כאילו נבדק באותה רמה.

**כשל שלישי, שנמצא תוך כדי ולא תוקן:** הסריקה שמצאה את זה גם גילתה ש-
`21-mthbram` ו-`24-galilee-connect-hub` מצהירות `<link rel="icon">` אל
`/icon-192.png` ו-`/pwa-512.png` בהתאמה — לא `.svg` כמו כל השאר. לא
נבדק אם אלה תקינים (PWA לרוב מגדיר כמה גדלי אייקון בכוונה) או אם הם
אותו באג בלבוש אחר — לא נגעתי בלי מדידה. **הבא בתור בפועל:** לבדוק
21/24, ואז עשר המערכות בלי `favicon.*` כלל (`10-bkalot-rights`,
`19-igud-shiurim-portal`, `20-igud-portal`, `23-haorech-torani`,
`37-bkalot-clone` ועוד חמש) — קטגוריה שלישית, טרם נמדדה כלל.

## עדכון 19/08/2026 (סבב נוסף) — 21/24 נבדקו: לא באג-בסיס PNG/SVG, אבל נמצא JPEG מתחזה ל-PNG ב-21

בדיקת בתי-הקובץ בפועל (לא רק הסיומת) מצאה ש-PNG של PWA היא קונבנציה
תקנית לגמרי כאן — לא סטייה. אבל `apps/21-mthbram/public/icon-512.png`
התברר כ-JPEG אמיתי (`FF D8 FF`, לא `89 50 4E 47`) שיושב תחת סיומת/
`type="image/png"` שקריים בשלושה מקומות (`<link rel=icon>`, `og:image`/
`twitter:image`, `manifest.json`) — בדיוק המשפחה של הבאג ש-`e4a55b1a`
(אותו קובץ, קומיט קודם) בא לתקן. `icon-192.png` של אותו יישום ו-
`24-galilee-connect-hub/public/pwa-512.png` נמדדו PNG אמיתי — תקינים.

**התיקון:** `git mv` ל-`icon-512.jpg` + `type="image/jpeg"` בשלושת
המקומות (אין כלי המרת תמונה מותקן בעץ הזה כדי להפוך את הבתים בפועל
ל-PNG אמיתי — תיוג נכון של מה שקיים, לא המרת פורמט). פרטים מלאים:
`DECISIONS.md` #53-56.

סריקת magic-bytes שיטתית על כל `apps/*/**/*.png` (לא רק `public/`) מצאה
עוד שתי התאמות-שווא ב-`apps/22-get-your-rights/src/assets/` (JPEG תחת
סיומת PNG, אבל מיובאות דרך צינור ה-assets של Vite ולא `public/` סטטי —
תיקון/אימות שונה מהותית, נדרשת בדיקת התנהגות ה-bundler לפני נגיעה) ושתיים
ב-`apps/_archive/*` (ארכיון בלבד, לא נוגעים לפי `CONNECTIONS.md`). **הבא
בתור בפועל:** `22-get-your-rights/src/assets`, ואז עשר המערכות בלי
`favicon.*` כלל שעדיין לא נבדקו.

## עדכון 19/08/2026 (סבב נוסף 2) — `22-get-your-rights/src/assets` נסגר: `vite.config.ts` נבדק תחילה, אין ולידציית בתים — שינוי שם + עדכון ייבוא בטוח

הבדיקה שהתבקשה כאן (התנהגות ה-bundler לפני נגיעה) בוצעה: `vite.config.ts`
של האפליקציה (`@vitejs/plugin-react-swc`, ללא `imagetools` או כל שלב
שממיר/מאמת תוכן תמונה) מטפל בייבוא נכס לפי **הסיומת בנתיב הייבוא בקוד**
בלבד — לא בודק את הבתים בפועל. המסקנה המעשית: בשונה מ-21-mthbram
(`public/` סטטי, רק ה-`<link>`/`manifest.json` צריכים לזוז), כאן צריך
לזוז **גם שם הקובץ בדיסק וגם שורת הייבוא יחד**, אחרת ה-build שובר על
ייבוא חסר.

שני הקבצים נמדדו שוב ואומתו כ-JPEG אמיתי (`FF D8 FF E0`). `plant-money.png`
מיובא ומשמש בפועל ב-`Footer.tsx:10,185`; `plant-growth.png` אינו מיובא
בשום מקום בעץ המקור (`grep` שיטתי) — נכס מת. תוקן: `git mv` לשני הקבצים
ל-`.jpg` (אותו תוכן בתים) + עדכון שורת הייבוא היחידה שקיימת
(`Footer.tsx:10` → `@/assets/plant-money.jpg`; שורה 185 מפנה למשתנה, לא
לנתיב, לא נגעה). `plant-growth.jpg` שונה שם לתיוג נכון בלבד — אין ייבוא
לעדכן. אין כלי המרת תמונה בעץ הזה, אז זה תיוג נכון של בתים קיימים, לא
המרת פורמט. אומת: `grep` שיטתי על כל `apps/22-get-your-rights` אחרי
העריכה מוצא אפס הפניות ל-שמות ה-`.png` הישנים; בתי הקבצים נבדקו שוב
אחרי ה-`git mv` לוודא זהות. פרטים מלאים: `DECISIONS.md` #57-60.

**הבא בתור בפועל:** עשר המערכות בלי `favicon.*` כלל — `10-bkalot-rights`,
`19-igud-shiurim-portal`, `20-igud-portal`, `23-haorech-torani`,
`37-bkalot-clone` ועוד חמש (טרם זוהו בשם, זקוק לסריקה). זו קטגוריה
שלישית ולא נמדדה כלל עדיין — בשונה מהמשפחה הקודמת (בתים לא תואמים
לסיומת), כאן צריך סבב עיצוב אמיתי: בחירת סמל/צבע ייחודי לכל מערכת,
לא רק בדיקת קובץ.

## עדכון 19/08/2026 — נמדדו שש מהחמש שנותרו (07,10,13,19,20,23,25,29,34,35,37): רק שתיים חיות, שתיהן קיבלו favicon עכשיו

מיפוי מלא מול `core.projects` (לא `registry.ts` — ר' `DECISIONS.md` #61):
`07,13,25,29,34,35` אין להן קוד מווענדר בעץ הזה בכלל (‏`app.json` בלבד,
או טוקנים/שרת) — אין מה לערוך. `19,20,23` הן שלד אמיתי, `live=false`
ב-`core.projects` — אף משתמש לא יכול לפגוש favicon שם היום. **`10-
bkalot-rights` ו-`37-bkalot-clone` בלבד חיות ופרוסות**, ושתיהן קיבלו
`favicon.svg` ייחודי הנגזר מהמותג האמיתי שלהן (‏`10`: אותו סמן ריבוע+וי
שכבר קיים ב-header, בצבע `--teal-d`; `37`: מסמך+וי חדש עקבי עם
"ירוק-אזמרגד עמוק" שכבר מוצהר ב-CSS של האתר). פרטים מלאים, כולל תלות
פריסה שהתגלתה (`37` לא נפרס דרך git-push רגיל — `scripts/stage-
portal.ps1` עודכן): `DECISIONS.md` #61-65. **הבא בתור בפועל:** `19/20/23`
כשייפרסו בפועל; `07/13/25/29/34/35` דורשים הכרעה נפרדת (ונדור מהמקור
או השארה מחוץ לטיפול).

## 19/08/2026 (סבב אוטונומי 2) — «חיפוש חכם» ב-`mthbram` נסגר: נמדד מי באמת LLM, ורק זה נשאר "חכם"

הפריט הזה הופיע כ"ממתין להכרעת מינוח" בכל עדכון מ-10/08/2026 ואילך
(שמונה הפניות למעלה) ומעולם לא נבדק בפועל. נבדק עכשיו מול הקוד: תשעה
מהשנים-עשר מופעי "חיפוש חכם"/"סוכן חכם" הם סינון `.filter()`/`.includes()`
בצד לקוח או שאילתת `ilike` פשוטה בקצה (`ivr-agent/index.ts` — פיצול
מילים, אין קריאת LLM). המופע העשירי, `FloatingChatBot.tsx:202`, **כן**
LLM אמיתי — `search-lessons/index.ts` מזרים תשובה מ-`google/gemini-2.5-flash`
דרך `ai.gateway.lovable.dev` עם קונטקסט מלא של המאגר. תוקנו תשעת
המקומות שאינם LLM (כולל מקום עשירי, `AdminDashboard.tsx:968`, שה-
`grep` המקורי של הרשומות למעלה לא מנה); ה-AI chat האמיתי נשאר "חכם"
ולא נגע. פרטים מלאים: `DECISIONS.md` #66-69.

**הבא בתור בפועל:** שרשור `icon-decisions` המקורי של §6
(`zchuyot 24 → mthbram 17 → egod 15 → galil 14`) — רק `zchuyot` תוקנה
ונפרסה; `mthbram`/`egod`/`galil` עדיין לא נבדקו מול הגלאים
`doubleMark`/`splitGlyph`/`repeatInBand`/`floatingTwice` שנבנו בעדכונים
(ה)–(ט). ובנפרד: `AIIcon` מוצג ליד תיבות החיפוש הלא-LLM ב-`FindLesson.tsx`/
`FeaturedLessons.tsx` — אי-דיוק גרפי מקביל למה שתוקן כאן במילים, טרם נמדד.

## 19/08/2026 (סבב אוטונומי 3) — האי-דיוק הגרפי (`AIIcon`) שסומן למעלה תוקן

שלושה מקומות ב-`mthbram` הציגו את `AIIcon` (מוח+הילה, מתועד כ-"Premium
AI icon"/"AI-grade feel") ליד תכונות סינון רגילות בצד לקוח: תיבת החיפוש
ב-`FindLesson.tsx`, תיבת החיפוש ב-`FeaturedLessons.tsx`, וכרטיס הפעולה
הראשון ב-`HeroSection.tsx` (המפנה ל-`/find-lesson`, החליף אייקון `Search`
רגיל ב-`AIIcon` רק בגלל `featured: true`). שלושתם הוחלפו באייקון פשוט
(`Filter`/`Search` מ-`lucide-react`, שכבר מיובאים באותם קבצים) — אפס
שינוי התנהגות, רק החלפת גרפיקה דקורטיבית. `AIIcon` נשאר ללא שינוי בכל
מקום שבו הוא באמת סמוך ל-LLM אמיתי (`FloatingChatBot.tsx`) או קישוט
מותג כללי לא תלוי-תכונה (`HeroSection.tsx:121`). פרטים מלאים:
`DECISIONS.md` #70-73. **הבא בתור בפועל:** שרשור `icon-decisions`
המקורי (`mthbram`/`egod`/`galil` מול ארבעת הגלאים) — עדיין פתוח.
