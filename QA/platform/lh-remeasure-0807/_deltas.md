# Lighthouse re-measure — 07/08/2026 03:22–03:26Z

מדידה חוזרת של שמונה המערכות שכבר היו ב-`core.lighthouse_runs`, מול הייצור
(`more30.com`), אותה גרסה ואותה מתודה כמו ב-03/08: `scripts/qa/lh-batch.mjs`
(Lighthouse 13.4.1, headless, mobile default) → `scripts/qa/lh-import.mjs`.

הסיבה: כל שמונה הציונים בלוח היו בני 76–102 שעות, `lighthouse_fresh` היה 0/8,
ומאז נחתו פריסות. השאלה לא הייתה אם המספרים ישתנו אלא בכמה.

| מערכת | perf 03/08 | perf 07/08 | Δ | LCP ms | TBT ms | CLS |
|---|---|---|---|---|---|---|
| nadlan   | 79 | **69** | −10 | 3002 → 3293 | 445 → 804 | 0.008 → 0.007 |
| torah    | 82 | **74** | −8  | 3422 → 4396 | 350 → 235 | 0.001 → 0.001 |
| egod     | 55 | **51** | −4  | 6210 → 5599 | 644 → 638 | 0.022 → **0.100** |
| tivuch   | 98 | **95** | −3  | 841 → 1503  | 5 → 259   | 0.005 → 0.004 |
| kupot    | 72 | **70** | −2  | 2877 → 4134 | 745 → 400 | 0.021 → **0.079** |
| mechiron | 67 | **68** | +1  | 4480 → 5519 | 287 → 154 | 0.003 → 0.003 |
| chatzor  | 49 | **59** | +10 | 4744 → 5022 | 1222 → 1038 | 0 → 0 |
| zchuyot  | 48 | **68** | +20 | 6251 → **2610** | 978 → 1234 | 0 → 0 |

a11y/best-practices/seo לא זזו באף מערכת (100/96 · 77 · 100 כמקודם).

שבע מתוך שמונה זזו ביותר מנקודה אחת וחמש ירדו — כלומר להציג את הציון של 03/08
כמצב הנוכחי היה נותן מספר שגוי בכל אחת מהן. זו ההצדקה ל-`stale` ול-`measured_at`
שנוספו במיגרציה 0028, ולא רק זהירות.

מדידה בודדת אינה מגמה: Lighthouse רועד בין ריצות, ובעיקר ב-TBT. מה שכן יציב
מספיק להיאמר — `zchuyot` חתכה LCP ב-3.6 שניות, ו-CLS של `egod` ושל `kupot` עלה
לתחום שמורגש (0.1 ו-0.079 מול סף 0.1).

הדוחות המלאים נשמרים ב-`QA/shots/lh-*.json`. `lh-batch.mjs` דורס אותם בכל ריצה,
ולכן הדוחות של 03/08 קיימים מעכשיו רק בהיסטוריית הגיט (קומיט 6828fab); שורות
`core.lighthouse_runs` עצמן נשמרות במלואן, שורה לכל ריצה.

`lighthouse_fresh` ב-`more30_admin_systems_report`: **0/8 → 8/8**. אומת דרך
הפונקציה המגודרת עם claim של סופר-אדמין; אותה קריאה בלי ה-claim עדיין מחזירה
`42501: super admin only`.

עדיין פתוח (core.issues #90): 22 מערכות מעולם לא נמדדו ומקבלות `null` בלוח.
