# registry.ts מול core.projects — כל השורות, מוכרעות מול הכתובת

נמדד 10/08/2026, אנונימית, `node scripts/qa/registry-vs-projects.mjs`.
‏36 שורות הושוו (08 ו-09 מוגנות — לא נבדקו, לא נגענו).

## מה נמצא בריצה הראשונה

| | |
|---|---|
| מסכימים | 14 |
| חלוקים | 22 |
| **registry.ts סותר את הכתובת** | **15** |
| core.projects סותר את הכתובת | 0 |
| חסרים מ-registry.ts לגמרי | 4 (‏33, 36, 37, 38) |
| מונט שהוא בעצם דף הבית של הפורטל | 0 |

חמש-עשרה שורות ב-`registry.ts` הכריזו `live:false` על מונט שעונה 200 עם חבילת
ה-JS שלו עצמו: ‏06 בריאות, 10 בקלות, 12 סמל, 14 שמחות, 16 חצור, 17 חיזוקים,
18 עורך, 22 זכויות, 24 גליל, 26 סטודיו, 27 מחירון, 28 קופות, 30 CRM, 31 גשר,
34 כסף. אף שורה במסד לא נמצאה שגויה. זה הופך את `core.projects` למקור האמת
על מצב פריסה, ואת הקובץ בעץ לנגזר — וכך הוא מסומן עכשיו בכותרת שלו.

‏36 (נדל"ן פרו, `/tivuch`) הוא המקרה שעולה כסף: מערכת חיה שנעדרה מהקובץ כולו,
כולל מ-`TOPIC_ROUTES`, כך ש-`resolvePath("/tivuch")` החזיר undefined.

## למה 200 לבדו אינו «חי»

הפורטל עונה על נתיב לא-מוכר בדף הבית שלו, ב-200 (‏`unknown-path-serves-home.mjs`).
מונט שמעולם לא נפרס נראה בדיוק כמו מונט עובד אם קוראים רק את שורת הסטטוס.
לכן כל מונט מוטבע לפי ה-assets שה-HTML שלו מושך, ומונט שטביעתו זהה לדף הבית
מסווג `portal-fallback`. בריצה הזאת אף אחד לא נפל לשם — כל 20 המונטים שענו 200
מגישים חבילה משלהם, ו-6 הנתיבים שאינם פרוסים החזירו 404 נקי.

## אחרי התיקון

`registry_wrong: 0` · `missing_from_registry: 0` · `db_wrong: 0`.

## מה שלא הוכרע — ובכוונה

נשארו 9 אי-התאמות שכתובת אינה יכולה להכריע, ולכן לא נגעתי בהן:

- **stage** (‏16, 17, 18, 21, 22, 34) — «wip / beta / live» היא הערכה, לא מדידה.
  ‏GET מוכיח שמשהו מוגש; הוא אינו מוכיח שהמערכת בשלה. צריך הגדרה מוסכמת למה
  מבדיל beta מ-live לפני שמסנכרנים את הצד הזה.
- **deploy_target** (‏21, 33, 37, 38) — ב-33/37/38 זה הבדל ייצוג בלבד: המסד
  מרשה `null`, וב-TypeScript אין null בטיפוס אלא `"unknown"`. ב-21 המסד אומר
  `unknown` והקובץ אומר `vercel`; אין לי מדידה שמכריעה איפה mthbram מתארח.

## קבצים

- `_results.json` — שורה לכל מערכת: שני הצדדים, תשובת הכתובת, ומי סותר אותה.
- `_core-projects.json` — צילום של `core.projects` עם ה-SQL שהפיק אותו.
  ‏core אינו חשוף ב-Data API, ולכן סקריפט אינו יכול לקרוא אותו אנונימית.
