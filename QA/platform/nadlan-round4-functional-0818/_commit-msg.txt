round-4 functional pass: 32 nadlan - core action (free report generation by address, real gov-sourced gush/chelka + street comps)+real data verified, no bug, no deploy needed

Ran the round-4 functional pass on 32 nadlan (נדל"ן ברגע, more30.com/nadlan).
No shared customer session was active this run; the core action (free report
generation) needs no login anyway, same class as mechiron/kupot/zchuyot.

Submitted address form (תל אביב / דיזנגוף / 100) -> /nadlan/report -> real
progress steps -> resolved in ~7s to a real free report: header resolved a
real גוש 7091 חלקה 7 from the address, honest empty-state copy where the
specific parcel has no recorded building sales (with a direct nadlan.gov.il
verification link), real street-level comps (290 real transactions on
דיזנגוף, median 59,607 ILS/sqm, a real per-transaction price table), and a
27-transaction area median (62,620 ILS/sqm, dated 31.1.2026, sourced from
רשות המסים). Real map integration with resolved lat/lng.

Network trace confirmed real backend round-trips, not stubs:
GET /nadlan/api/report?q=...&tier=basic -> 200 OK
GET /nadlan/api/planmap?lat=...&lng=... -> 200 OK

Clears the round-4 bar (core action + real data + deployed 200). No bug
found, no code change, no deploy needed this step.

Evidence: QA/platform/nadlan-round4-functional-0818/_results.md +
nadlan-report.png.

Protected systems untouched (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873).

Next in round-4 ROUTES order per scripts/qa/platform-audit.mjs: kesef (34).
