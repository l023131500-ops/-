# מדידה חוזרת של רשימות ההרשאות — ארבעה ימים אחרי, אפס הבדלים (17/08/2026)

**מה זה:** הרצה חוזרת של `scripts/qa/allowlist-sweep.mjs` על אותם תשעה פרויקטי
Supabase חיים שנמדדו ב-13/08, כדי לענות על שאלה אחת ויחידה: **האם ששת החסמים
שממתינים למשתמש ב-`NEEDS_USER.md` §0תב עדיין חיים, או שהם שרדו את הסיבה שלהם.**

הסעיף הזה נמדד ולא כובד על הנחה. חסם שממתין למשתמש יכול לעמוד בקובץ ימים אחרי
שהמשתמש כבר ביצע את הפעולה, ואז ריצה שלמה מתבזבזת על מעקף של חסם שאינו קיים.
הסקריפט הזה נכתב מלכתחילה כדי שיריצו אותו שוב — ‏`_results.md` של 13/08 אומר
זאת במפורש — וזו ההרצה הזאת.

**מתי נמדד:** `2026-08-16T22:01:16Z` (‏17/08 שעון מקומי).

---

## התוצאה: ⚠️ החסם עומד במלואו

| מערכות | ref | Site URL | reset | mount | root | פסק דין | מול 13/08 |
|---|---|---|---|---|---|---|---|
| 00 פורטל · 16 chatzor · +14 | `uhnrgujbdxhhmoxcjria` | `https://more30.com` | ✅ | ✅ | ✅ | תקין | ללא שינוי |
| 01 torah | `bieebmnmkffwbqlsfozh` | `https://more30.com` | ✅ | ✅ | ✅ | תקין | ללא שינוי |
| 06 briut · 12 smel · 17 chizukim | `csjekrvukbdznetsrodj` | `https://more30.com` | ✅ | ✅ | ✅ | תקין | ללא שינוי |
| 15 egod | `hkkkynyoigzlttpynoeo` | `https://egod.lovable.app` | ❌ | ❌ | ❌ | חסר — #208 | ללא שינוי |
| 21 mthbram | `aypsqqvfohekxxuqsmrw` | `https://mthbram.lovable.app` | ❌ | ❌ | ❌ | חסר — #207 | ללא שינוי |
| 22 zchuyot | `trerolyveytzgksawrme` | `https://get-your-rights.lovable.app` | ❌ | ❌ | ❌ | חסר — #206 | ללא שינוי |
| 24 galil | `mwljkonwdeuaahsigjdp` | `https://galilee-connect-hub.lovable.app` | ❌ | ❌ | ❌ | חסר — #209 | ללא שינוי |
| 30 crm | `jhbeelzvjvhnkxldqvxx` | `https://zchuyot-care-hub.lovable.app` | ❌ | ❌ | ❌ | חסר — #202 | ללא שינוי |
| 31 gesher | `ygaqqnuyfnumezxxmtbh` | `https://heb-connect-hub.lovable.app` | ❌ | ❌ | ❌ | חסר — #205 | ללא שינוי |

**שלוש תקינות, שש חסרות — בדיוק כמו ב-13/08.** אף אחת מששת הפעולות בדשבורד לא
בוצעה עדיין, ובאותה מידה שום דבר לא נשבר בינתיים: הצד השני של אותה מדידה הוא
ששלושת התקינים לא נסוגו.

---

## למה «ללא שינוי» כאן נמדד ולא הוסק בעין

הפסק דין לא נקבע מהשוואת שתי טבלאות בעיניים אלא משוואת שדות מתוך שני קובצי
ה-`_results.json`. תשע שורות, ותשעה שדות בכל שורה — ‏`ref`, `site_url`,
`negative_control_ok`, `positive_control_ok`, `reset_allowed`, `mount_allowed`,
`root_allowed`, `measurement_valid`, `verdict`:

```
total field diffs: 0
```

⚠️ **81 השוואות, אפס הבדלים.** אילו שדה אחד היה זז — למשל `site_url` של פרויקט
אחד — הוא היה נדפס בשמו, ולא היה נבלע ב«נראה אותו דבר».

---

## שתי הבקרות עברו גם בריצה הזאת

בלי הבקרות המספר חסר משמעות, ולכן הן נמדדו מחדש ולא הועתקו מ-13/08:

| בקרה | מה נשלח | מה חייב לקרות | מה זה שולל |
|---|---|---|---|
| שלילית | `https://example.com/_probe` | חייב להיות מוחלף | רשימה פתוחה לכל דומיין |
| חיובית | ה-Site URL עצמו | חייב לחזור כפי שהוא | נקודת קצה שמחליפה הכל ואינה מבחינה |

‏`negative_control_ok` ו-`positive_control_ok` החזירו `true` **בכל תשע השורות**,
וכל תשע הבקרות השליליות ענו `303`. לכן אף שורה בטבלה למעלה לא נפסלה, ו«❌» כאן
פירושו «נמדד ואינו מורשה» ולא «לא הצלחתי למדוד».

---

## שינוי אחד בסקריפט, ולמה הוא היה הכרחי

‏`scripts/qa/allowlist-sweep.mjs` כתב לנתיב קבוע — `QA/platform/allowlist-sweep-0813/`.
כלומר הסקריפט שכל תכליתו להיות מורץ **שוב** היה מוחק בכל הרצה את המדידה שאליה
משווים, ואז לא הייתה נשארת שום עדות לכך שהמצב לא זז. נוסף ארגומנט אופציונלי
לתיקיית פלט:

```
node scripts/qa/allowlist-sweep.mjs QA/platform/allowlist-recheck-0817
```

ברירת המחדל נשארה בדיוק כפי שהייתה, ולכן `node scripts/qa/allowlist-sweep.mjs`
בלי ארגומנט מתנהג היום כמו אתמול. ⚠️ ושהבסיס אכן שרד נמדד ולא הונח: ה-MD5 של
`allowlist-sweep-0813/_results.json` היה `212DB8B85CA283689AC1874C78E57A22` לפני
ההרצה ואותו ערך בדיוק אחריה.

---

## מה זה לא אומר

⚠️ המדידה אומרת מה יש ב-`uri_allow_list` ובַ-`Site URL` — **ולא** שמסך האיפוס של
כל מערכת עובד. אצל 15 עגוד (#208) זה החסם השני מתוך שניים: גם כשההגדרה תתוקן,
נתיב הפריסה שלה עדיין חסום (§0יא/#167). ⚠️ וכן: הפעולה עצמה נשארת של המשתמש —
‏`PATCH /v1/projects/{ref}/config/auth` קיים רק לעשרת הפרויקטים ש-`SUPABASE_ACCESS_TOKEN`
מכסה, וחמישה מששת הנופלים מחזירים עליו 403.

⚠️ ובריצה הזאת גם המסלול הזה לא נבדק בפועל, ולא הונח שהוא זמין: ה-MCP של Supabase
אינו מחובר בסשן הזה כלל, וה-PAT יושב ב-`core.secrets` — כלומר אפילו לעשרת המכוסים
לא הייתה כאן דרך לכתוב. זו בדיוק הסיבה שהצעד הזה נבחר: הוא **קריא-בלבד ובלי מפתח**,
ולכן הוא הדבר היחיד בנתיב הזה שאפשר היה לסגור מכאן היום.

---

## שחזור

```
node scripts/qa/allowlist-sweep.mjs QA/platform/allowlist-recheck-<תאריך>
```

הפלט הגולמי — כל ה-`Location` של כל חמש הבדיקות בכל תשעת הפרויקטים, כולל שתי
הבקרות — יושב ב-`_results.json` באותה תיקייה. הבסיס להשוואה:
`QA/platform/allowlist-sweep-0813/_results.json`.
