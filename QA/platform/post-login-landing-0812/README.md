# §8ב — לאן נוחת הלקוח אחרי התחברות: 21 mthbram, 30 crm, 31 gesher

**12/08/2026 · מדידה בדפדפן על more30.com · מצב טסט · לא נפרס כלום**

הקו הפתוח שנסגר כאן הופיע בשלוש פעימות רצופות (465, 466, 467) באותו נוסח:
> "§8ב after email confirmation has never been walked on 21/30/31"

הפעימה הקודמת (466) הריצה את סבב **ההרשמה** מול שרת האימות והוכיחה
שההרשמה נקייה בשלוש המערכות, ורשמה במפורש מה היא **לא** מדדה: מה קורה
*אחרי* ההתחברות — האם הלקוח נכנס לתוך המוצר או נשאר בתדמית. זה §1א
בניסוחו המילולי ("מי שמתחבר חוזר לדף התדמית עם שמו למעלה") ו-§8ב.
כאן זה נמדד — בדפדפן, עם משתמש הבדיקה הרשמי של §1ב.

**שיטה.** דפדפן אמיתי מול `more30.com` בלבד. לכל מערכת: איתור מסלולי
הניתוב מתוך חבילות ה-JS שהאתר מגיש בפועל → פתיחת מסך הכניסה → הזנת
`test@more30.com` / `More30Test2026` → שליחה → **הכתובת והמסך שאליהם
הלקוח הגיע**, בצילום מסך.

---

## התוצאה: 2 עוברות, ואחת אינה רלוונטית — והסיבה שלה מתקנת סיווג

### 30 crm — ✅ עובר
`/crm/auth` → התחברות → **`/crm/dashboard`**. מסך מוצר אמיתי: תפריט צד
(לקוחות · משימות · שותפים · זכאויות · מסמכים · אנליטיקה · הגדרות),
כרטיסי מדדים, כפתורי פעולה ("לקוח חדש", "הפניות", "מסמכים"), והודעת
"התחברת בהצלחה". כל המונים 0 — מסד ריק, נתון אמת, לא נתון מומצא.
📸 `crm-01-login.png`, `crm-02-after-login.png`

### 31 gesher — ✅ עובר
`/gesher/auth` → התחברות → `/gesher/` → **`/gesher/client/status`**.
אזור אישי אמיתי: "שלום, לקוח בדיקה", ציר התקדמות הבדיקה (נקלט במערכת →
בבדיקת מומחה → הושלם), תיק מסמכים מאובטח, תפריט לקוח ו"התנתק".
כלומר למשתמש הבדיקה יש **רשומת לקוח אמיתית** במערכת, והוא נוחת עליה.
📸 `gesher-02-after-login.png`, `gesher-03-landed.png`

### 21 mthbram — לא רלוונטי, **ואין בה כניסת לקוח בכלל**
זו התוצאה החשובה של הצעד, כי היא **מתקנת סיווג** שנשא שלוש פעימות.

שלוש הפעימות האחרונות מנו את 21 בין "חמש מערכות חיות שיש להן טופס כניסה
משלהן מעל Supabase Auth" ומדדו עליה את §1א כאילו מדובר בכניסת לקוח.
מסלולי הניתוב שהחבילה `index-DheqMcgT.js` מגישה בפועל אומרים אחרת —
**מסלול הסיסמה היחיד הוא `/admin-login`**. כל שאר מסלולי הפורטל הם
מבוססי-טוקן ואין בהם סיסמה כלל:
`/portal/rabbi/:token` · `/portal/org/:token` · `/shul/:accessToken` ·
`/study-days/:token` · `/bulk-upload/:token` · `/view/*/:publicToken`.

והמוצר עצמו **פתוח לגמרי בלי התחברות**: `/mthbram/` הוא מסך החיפוש
במאגר (חיפוש חכם, סינון לפי נושא/עיר/יום, "הוספת שיעור למאגר"), ובניווט
העליון חיפוש שיעור · עדכון שיעור · הקמת שיעור. אין מה "להיכנס אליו".

הכפתור המשותף בפינה הוא `<more30-auth>` — **custom element עם shadow
DOM**, ולכן שאילתת DOM רגילה לא רואה אותו (המדידה הראשונה כאן דיווחה
"אין כפתור כניסה", וזו הייתה טעות שלה, לא של האתר). היעדים שלו:
`more30.com/login?from=…/mthbram/` → ומחזיר בדיוק ל-`/mthbram/`, ומעבר
לכך רק `/me` ו-`/me?upgrade=1` של המרכז. עם session תקף הוא מציג את שם
הלקוח.
📸 `mthbram-01-after-login.png` (מחובר — השם מופיע בפינה)

**המסקנה ל-21:** אין כאן באג §1א ואין כאן מה לתקן בזרימת הכניסה. הטופס
שנמצא בסריקות הקודמות הוא **כניסת אדמין**, לא כניסת לקוח. §8ב אינו חל
עליה כפי שהוא מנוסח.

---

## מה לא נמדד, ונרשם ככזה

- **לא נבדק מה קורה מיד אחרי הרשמה חדשה** — רק אחרי התחברות של משתמש
  קיים ומאושר. הרשמה חדשה על 30/31 עדיין דורשת אישור מייל (#172,
  הגדרת דשבורד ששייכת למשתמש), ולכן לא ניתן להשלים אותה מכאן.
- **לא בוצעה פעולה עסקית אמיתית** בתוך שתי המערכות שעברו — נמדדה
  הנחיתה, לא "פעולה אמיתית עובדת" שבסוף §8ב.
- **`/mthbram/admin-login` לא נפתח ולא נוסה** — זו כניסת אדמין.
- מאגר השיעורים של 21 מציג "0 שיעורים · עדיין אין שיעורים במאגר".
  זה תואם את הבאג הפתוח הידוע על המאגר הריק ואינו חלק מהמדידה הזו.

## מצב טסט

לא נוצר אף משתמש. לא נשלחה שום הודעה. לא נכתב דבר לשום טבלה עסקית —
ההתחברות היחידה היא של משתמש הבדיקה הרשמי `test@more30.com` שכבר קיים
לפי §1ב. המערכות המוגנות (08, 09, bkalut-app, bkalot-admin, zr_*,
NEDARIM3873) אינן ברשימת היעדים, ולא נעשה שימוש ב-csj/csj_src/igud.
