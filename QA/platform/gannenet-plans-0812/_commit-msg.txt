40 gannenet: the plans page sold a different product's price (§8א)

Every light live system got the 2 ₪ / 5 ₪ ladder when §8א was seeded. gannenet
joined the platform afterwards (§0), so it stayed the one publicly visible live
system with no row in core.plans at all.

The page was not empty, which is why nobody caught it: system.html falls back to
the platform ladder. A user who clicked "מסלולים ומחירים" on the gannenet card
was shown "פרימיום · ₪10 לחודש" under the heading "מסלולי הפלטפורמה חלים גם על
השירות הזה" — the price of a different product.

Seeded the three customer-visible rows, worded identically to the other 19 — the
ladder belongs to the platform and is not a place to write a unique promise.
features stays [] and §1בב.3 still holds: everything is free, the paid tiers are
display only until it is defined what each one includes. The hidden billing rows
(charge / one_time / pro) were deliberately left out: they are checkout plumbing,
this is test mode, and more30_checkout does not need them to record a choice.

The same page said "פרוסה בנפרד (Railway)" under "מה השירות עושה". True before
§0, false since — it is served at more30.com/gannenet out of this monorepo
(apps/40-gannenet) and deployed as the Vercel project gannenet-more30. Fixed.

Measured in a browser against production before and after. After: three cards,
חינמי / בסיסי ₪2 / מורחב ₪5, ascending, and the subheading switched to "בוחרים
מסלול אחרי ההתחברות" — the page now knows the system has its own ladder. Recount
over the 20 public live systems: 19 with 2/5, 1 with its own premium (nadlan
12/15), 0 with nothing.

NO DEPLOY this step — both changes are data, and the page reads them at runtime.
No charge, no message sent, no user created; test mode. Protected systems
untouched (08, 09, bkalut-app, bkalot-admin, zr_*, NEDARIM3873); csj/csj_src/igud
unused.

Evidence QA/platform/gannenet-plans-0812/
