round-4 functional pass: 31 gesher - login+core action (consent-management toggle, real server-timestamped write)+real data verified, no bug, no deploy needed

Session start: read RUN_INSTRUCTIONS.md, checked core.run_progress (last id 899,
crm-round4-functional-0818, which queued gesher (31) next in round-4 ROUTES order
per scripts/qa/platform-audit.mjs) and git log (HEAD 622fd7e on fix/nadlan-a11y).

Ran the round-4 functional pass on 31 gesher (Hebrew Bridge CRM / גשר עברית CRM,
own Supabase project ygaqqnuyfnumezxxmtbh, one of the four projects the
Management-API PAT cannot reach): verified live via Playwright against
https://more30.com/gesher.

core.projects note (05/08) said this system "serves a login screen only, no public
product behind it" - that turned out to be stale. With the shared test-customer
session already active, visiting /gesher auto-landed on /gesher/client/status: a
real client-facing portal (case status timeline / secure document vault / consent
management), not just a staff login gate. Network trace confirmed a real session
against gesher's own project: GET .../auth/v1/user -> 200, GET
.../rest/v1/user_roles?select=role&user_id=eq.1458bb7c-... -> 200.

Tested the core action: consent management (/gesher/client/consents). Toggled the
"יועצים פיננסיים" (financial advisors) sharing switch on - copy changed from "not
permitted" to "permitted for the fields below", real server timestamp appeared
("עודכן ב-18 באוג' 2026, 8:50"), proving the write round-tripped through the app's
_serverFn layer into the DB, not a client-only flip. Screenshot:
consent-toggle-success.png. Restored to original (off) state immediately after -
timestamp updated to 8:51, confirm toast shown. No new test data left behind (same
existing consent record already touched in QA/gesher/consents-0812 on 12/08).

Clears the round-4 bar (login + core action + real data + real DB round-trip +
deployed 200). No bug found, no code change, no deploy needed. Corrected the stale
core.projects #31 note to reflect the real client portal. Evidence:
QA/platform/gesher-round4-functional-0818/_results.md + consent-toggle-success.png.

Protected systems untouched (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873) -
ygaqqnuyfnumezxxmtbh is gesher's own project, reached only via its own public
auth+consents API, no protected schema touched.

Next in round-4 ROUTES order per scripts/qa/platform-audit.mjs: nadlan (32).
