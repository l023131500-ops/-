# 13/08 — ה-402 נגמר, ו-31 גשר הייתה המערכת היחידה שהעבודה שלה נשארה מחוץ לייצור

**#204 נסגר (המשתמש שילם), ו-#201 ירד ממערכת שנייה — מסך איפוס הסיסמה של 31 גשר
עלה לאוויר ואומת בדפדפן.**

## 1. מה נמדד ראשון, לפני שנגעתי בכלום

הפעימה הקודמת (13/08 13:31Z) הסתיימה כשכל `more30.com` מחזירה **402 Payment
Required** — softBlock על חשבונית Vercel שלא שולמה (#204). הבדיקה הראשונה כאן הייתה
לשאול אם זה עדיין נכון, כי זה קובע מה בכלל אפשר לעשות בפעימה:

| מדידה | לפני (12–13/08) | עכשיו |
|---|---|---|
| `GET https://more30.com/` | 402, גוף ריק | **200, 3143b** |
| `GET /v2/teams/team_NLONMgS3.../softBlock` | `{"reason":"UNPAID_INVOICE","blockedAt":1786568292853}` | **`null`** |
| `billing.plan` / `billing.status` | `pro` / `canceled` | `pro` / **`active`** |

## 2. סריקה על כל 25 המערכות החיות + 7 מסכי האיפוס

`node scripts/qa/recovery-402-sweep.mjs QA/platform/recovery-402-0813`
(קריאה-בלבד, cache-buster בכל כתובת, `redirect: manual`). תוצאה גולמית:
`_results.json`.

**402 בכל 33 הכתובות: 0.** כל 25 המערכות ענו — 18 ב-200 ישירות, ו-7 ב-307/308
שנבדקו בנפרד ונמצאו הפניות תקינות שנוחתות על 200 (`/tamlul/`→`/tamlul` 23825b,
`/crm/`→`/crm/auth` 11759b, `/gesher/`→`/gesher/auth` 12106b, וכן `/modaot/`,
`/orech/`, `/nadlan/`, `/kesef/`, `/tivuch/`, `/gannenet/`).

הסריקה לא הסתפקה בקוד התשובה: לכל מערכת נבדק שה-HTML מבקש את החבילה של עצמה
(`/<mount>/assets/...`) ולא את קליפת הפורטל — כי מסלול מורכב יכול לענות 200 ועדיין
להגיש את מסך ה-404 של הפורטל.

## 3. מה שהסריקה מצאה — מסך אחד מתוך שבעה שלא היה שם

שבעת מסכי האיפוס שנבנו בחלון ה-402 (#200, #201) נבדקו בייצור בפעם הראשונה — עד
היום אף אחד מהם לא נראה בעיניים, כי בזמן שהם נכתבו כל כתובת החזירה 402:

| מסך | לפני הפעימה |
|---|---|
| פורטל `/auth/reset` | 200 |
| 16 חצור `/chatzor/auth/reset` | 200 |
| 30 crm `/crm/reset-password` | 200 |
| 01 תורה `/torah/auth/reset` | 200 |
| 22 זכויות `/zchuyot/auth/reset` | 200 |
| 21 מתחברים `/mthbram/auth/reset` | 200 |
| **31 גשר `/gesher/reset-password`** | **404** |

גם `/gesher/auth/reset` נבדק כגיבוי — 404 גם הוא, כלומר לא טעות בשם המסלול.

**הסיבה, ולא ניחוש:** הפריסה האחרונה של `gesher-more30` הייתה
`dpl_BPTa33n3X4CB6TeGSSfPV85Dp258` ב-12/08 12:30Z, והקומיט שבנה את המסך
(`44dcf35`) נכתב ב-12/08 21:13Z — תשע שעות אחריה. הפעימה שכתבה אותו רשמה במפורש
"אין פריסה בצעד הזה, ולא בבחירה", כי ה-402 היה בתוקף. הקוד היה בריפו, בייצור הוא
לא היה.

## 4. מה בוצע

עץ העבודה של `apps/31-hebrew-bridge-crm` נבדק כנקי מול HEAD לפני הפריסה, כדי
שמה שנפרס יהיה בדיוק הקוד המקומיט. פריסה מהמקור (לא `--prebuilt` — ראו האזהרה
ב-`vite.config.ts`: `--prebuilt` מתעלם מ-`vercel.json` וכל נכס 404):

```
vercel deploy --prod --yes --scope l023131500-ops-projects
→ dpl_FHQwr2TRvFuSxsJQRcbXYqAh4gdD, READY, production
```

## 5. אימות בייצור אחרי הפריסה

| בדיקה | תוצאה |
|---|---|
| `GET /gesher/reset-password` | **200**, 5987b, `<title>בחירת סיסמה חדשה — מערכת CRM שותפים</title>` |
| `GET /gesher/auth` | 200, 12348b |
| דפדפן: `/gesher/auth` | הכפתור «שכחתי סיסמה» קיים ומוצג בלשונית ההתחברות — `gesher-auth-forgot-link.png` |
| דפדפן: `/gesher/reset-password` בלי טוקן | המסך נטען, בודק את הכתובת, ומסיים ב«הקישור אינו תקף» עם הסבר וקישור חזרה — `gesher-reset-no-token.png` |

המסך בלי טוקן הוא הבדיקה החשובה כאן: הוא מוכיח שהמסלול קיים, שה-JS שלו רץ, ושהוא
מטפל במקרה הרע במקום להיתקע על «בודקים את הקישור מהמייל…».

## 6. מה נשאר פתוח — ולא נסגר כאן

**#205 נשאר על המשתמש.** המסך חי, אבל הקישור שיגיע במייל עדיין ייזרק על ידי
Supabase: `uri_allow_list` של `ygaqqnuyfnumezxxmtbh` אינו מכיל את `more30.com`.
סיבוב מלא מקצה לקצה (מייל → קישור → סיסמה חדשה) אי אפשר לבדוק כאן עד שההגדרה
תשתנה — זה אחד משישה בטבלה של NEEDS_USER §0תב.

**#201 נותרה עליו מערכת אחת בלבד — 15 egod** — והיא חסומה באמת על #167.
