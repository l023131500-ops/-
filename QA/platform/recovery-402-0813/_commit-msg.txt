ה-402 נגמר, ומסך האיפוס של 31 גשר — היחיד משבעה שלא הגיע לאוויר — נפרס ואומת (#204 נסגר, #201 ירד למערכת אחת)

הקו הפתוח שהפעימה הקודמת הותירה היה #201, ועליו נשארה מערכת אחת (15 egod) שחסומה
באמת על #167 — סוכן Lovable של הפרויקט מסרב לכל הודעה, כולל קריאה-בלבד. לפני
שנבחר משהו אחר נבדקה ההנחה הגדולה ביותר שהפעימות האחרונות עבדו תחתיה: מ-12/08
20:58Z כל more30.com החזירה 402 ואי אפשר היה לאמת שום דבר בייצור.

מה נמדד ראשון
- GET https://more30.com/ מחזיר 200 (היה 402, גוף ריק).
- softBlock של team_NLONMgS3DlFsznzcrz0j3OMs חזר null; billing.status = active
  (היה canceled). המשתמש שילם את החשבונית.
- סריקה חדשה על 33 כתובות — 25 המערכות החיות מ-core.projects ו-7 מסכי האיפוס —
  החזירה אפס תשובות 402. שבע מהן ענו 307/308 ונבדקו בנפרד: כולן הפניות תקינות
  שנוחתות על 200. הסריקה לא הסתפקה בקוד התשובה אלא בדקה שכל מערכת מגישה את
  החבילה של עצמה (/<mount>/assets/) ולא את קליפת הפורטל, כי מסלול מורכב יכול
  לענות 200 ועדיין להיות מסך ה-404 של הפורטל.
- #204 נסגר. אף מערכת לא נזקקה לפריסה מחדש — הפריסות היו שלמות כל הזמן, בדיוק
  כפי שנכתב ב-NEEDS_USER §0ר בזמן אמת.

מה שהמדידה הזאת גילתה, וזו העבודה האמיתית כאן
שבעת מסכי איפוס הסיסמה שנבנו בתוך חלון ה-402 (#200, #201) נכתבו, קומיטו ונפרסו
בלי שאיש ראה אף אחד מהם — בזמנו כל כתובת החזירה 402. הם נבדקו כאן בייצור בפעם
הראשונה. שישה החזירו 200. 31 גשר החזירה 404, וגם /gesher/auth/reset החזיר 404,
כלומר לא טעות בשם המסלול.

הסיבה נמדדה ולא נוחשה: הפריסה האחרונה של gesher-more30 הייתה
dpl_BPTa33n3X4CB6TeGSSfPV85Dp258 ב-12/08 12:30Z, והקומיט שבנה את המסך (44dcf35)
נכתב ב-12/08 21:13Z — תשע שעות אחריה. אותה פעימה רשמה במפורש "אין פריסה בצעד
הזה, ולא בבחירה", כי ה-402 היה בתוקף. הקוד היה בריפו; בייצור הוא לא היה.

מה בוצע
- apps/31-hebrew-bridge-crm נבדק כנקי מול HEAD לפני הפריסה, כדי שמה שנפרס יהיה
  בדיוק הקוד המקומיט ולא עץ עבודה מקומי.
- פריסה מהמקור, לא --prebuilt: vite.config.ts של האפליקציה מזהיר במפורש
  ש---prebuilt מתעלם מ-vercel.json ולכן כל נכס 404 והאתר נראה חי אך חסר עיצוב.
  vercel deploy --prod --yes -> dpl_FHQwr2TRvFuSxsJQRcbXYqAh4gdD, READY.

אימות בייצור אחרי הפריסה
- GET /gesher/reset-password -> 200, 5987b, <title>בחירת סיסמה חדשה</title>.
- בדפדפן, מסך הכניסה: הכפתור «שכחתי סיסמה» קיים ומוצג.
- בדפדפן, המסך בלי טוקן: נטען, בודק את הכתובת, ומסיים ב«הקישור אינו תקף» עם
  הסבר וקישור חזרה — לא נתקע על «בודקים את הקישור מהמייל…». זו הבדיקה שמוכיחה
  שהמסלול קיים, שה-JS שלו רץ ושהוא מטפל במקרה הרע.

מה לא נסגר, ובמפורש
סיבוב מלא מהמייל אי אפשר לבדוק כאן: uri_allow_list של ygaqqnuyfnumezxxmtbh אינו
מכיל את more30.com, ולכן Supabase יזרוק את הקישור. זה #205, אחד משישה בטבלה של
NEEDS_USER §0תב, והוא נשאר על המשתמש. #201 נותרה עליו 15 egod בלבד.

ראיות: QA/platform/recovery-402-0813/ (_results.json, _results.md, שני צילומי מסך)
כלי: scripts/qa/recovery-402-sweep.mjs — קריאה-בלבד, ניתן להרצה חוזרת.
