round-4 functional pass: 35 kiosk (KioskFleet) - login+core action (device enrollment code generation, real POST to /kiosk/api/enrollments)+real data verified, no bug, no deploy needed

Session: read RUN_INSTRUCTIONS.md, checked core.run_progress (last row
nadlan-round4-functional-0818, which queued kesef (34) next in round-4
ROUTES order per scripts/qa/platform-audit.mjs) and git log (HEAD c4c6db2
on fix/nadlan-a11y).

34 kesef skipped this step per the anti-drift rule (fully blocked system ->
move to next system): apps/34-kesef contains no source in this monorepo
(app.json marked source: "not-vendored"), the live site is a static
brochure page only, and this has already been exhaustively re-verified and
documented in NEEDS_USER.md / core.projects #34 note across multiple prior
sessions - re-checking it again would not be new work.

Ran the round-4 functional pass on 35 kiosk (KioskFleet, more30.com/kiosk,
Railway-deployed from a different repo l023131500-ops/zol per memory
kiosk-deploys-from-a-different-repo - QA only, no code change needed/made
here). Verified live via Playwright against https://more30.com/kiosk/console
(not the .html URL, which breaks API routing per the same memory).

Login: admin/More30Admin2026 -> POST /kiosk/api/auth/login -> 200, landed
on the devices console as מנהל-על. Core action: device-enrollment code
generation (הוספת מכשיר -> יצירת קוד רישום), filled clearly-marked test
data (name "בדיקת מערכת QA 18/08 - אל תמחק בלי בדיקה", link "אולמות
אירועים"), submitted -> POST /kiosk/api/enrollments -> 200 OK, real 6-char
code FJD33D returned, appeared in the open-enrollments table alongside a
pre-existing code from an earlier QA session (confirms real persisted
state, not a stub). Cleaned up immediately via the console's own delete
button, leaving the console exactly as found.

Clears the round-4 bar (login + core action + real data + deployed 200).
No bug found, no code change, no deploy needed this step.

Evidence: QA/platform/kiosk-round4-functional-0818/_results.md +
kiosk-enrollment-created.png.

Protected systems untouched (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873)
- only kiosk's own public console/API was exercised, no protected schema
touched.

Next in round-4 ROUTES order per scripts/qa/platform-audit.mjs: tivuch (36).
