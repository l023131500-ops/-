# §1א/§8ב — ההרשמה ב-06 · 12 · 17 נכנסת מיד, ולא דרך תיבת המייל · 12/08/2026

הצעד הקודם על הנושא (`QA/platform/autoconfirm-0812/`) מדד את **כל** 26 הכתובות
החיות ובנה את הטבלה שקובעת מי דורש אישור מייל. הוא סיים במשפט בעלוּת אחד:

> "מתוך שמונת הפרויקטים שאינם `uhnrgujbdxhhmoxcjria`, **אף אחד** אינו נראה
> מהסביבה הזו — `list_projects` מחזיר פרויקט אחד בלבד — ולכן אי אפשר לשנות
> את זה מכאן."

**המשפט הזה לא נכון לשניים מתוך השמונה,** וזה נמדד ולא שוער.

## מה נמדד

`GET api.supabase.com/v1/projects` עם ה-`SUPABASE_ACCESS_TOKEN` שכבר יושב
ב-`core.secrets` (scope=global) מחזיר **עשרה** פרויקטים. שלושה מהם
`ACTIVE_HEALTHY`, והגדרת האימות שלהם נקראה ישירות:

| פרויקט | `mailer_autoconfirm` לפני | `site_url` | מערכות |
| --- | --- | --- | --- |
| `uhnrgujbdxhhmoxcjria` | ✅ דלוק | `https://more30.com` | 16 מערכות (ההאב) |
| `bieebmnmkffwbqlsfozh` | ❌ כבוי | `https://more30.com` | 01 torah — **וגם `zr_*` המוגן** |
| `csjekrvukbdznetsrodj` | ❌ כבוי | `http://localhost:3000` | 06 briut · 12 smel · 17 chizukim |

חמשת הפרויקטים האחרים מהטבלה של §0א״ (15, 21, 24, 30, 31) באמת אינם ברשימה
— זה נמדד, ולא נותר עליהם מה לעשות מכאן.

## מה תוקן — `csjekrvukbdznetsrodj`

`PATCH /v1/projects/csjekrvukbdznetsrodj/config/auth` → `mailer_autoconfirm: true`.

לפני שנגעתי, סבב אמיתי מול הייצור (`https://csjekrvukbdznetsrodj.supabase.co`):

* הרשמה → `200`, נוצר משתמש, **בלי `access_token`**.
* התחברות מיד אחריה באותה סיסמה → `400`
  `{"error_code":"email_not_confirmed","msg":"Email not confirmed"}`.

אחרי:

* הרשמה → `200` עם `access_token` ו-`token_type: bearer` **מיד**.
* התחברות מיד אחריה → `200` עם `access_token`.

ובשפה שבה נכתבה הטבלה המקורית — `GET /auth/v1/settings`, עם המפתח שהייצור
עצמו מגיש ב-`/briut/supabase-config.js` — `mailer_autoconfirm: true`.

זה בדיוק מה ש-`RUN_INSTRUCTIONS` דורש מהמסלול החינמי: "הרשמה מיידית
(שם·טלפון·מייל·סיסמה) → מיד נכנס לתוך המוצר".

**להחזיר בדקה:** `PATCH … {"mailer_autoconfirm": false}`.

## נתוני אמת — מה באמת היה שם

לפני השינוי: `auth.users` בפרויקט הזה החזיקה **שורה אחת** (`test@more30.com`,
מאושרת). כלומר אף לקוח אמיתי לא היה תקוע — המלכודת הייתה עתידית, ועכשיו היא
סגורה. בפרויקט 01 יש חמישה משתמשים, שניים לא מאושרים, שניהם מהיום.

שני משתמשי הבדיקה שיצרתי כאן נמחקו (`DELETE /auth/v1/admin/users/{id}` → 200
לשניהם), והטבלה חזרה לשורה אחת בדיוק כפי שהייתה. הקבצים כאן הם התיעוד.

## מה **לא** נגעתי בו, ולמה

`bieebmnmkffwbqlsfozh` נגיש באותה מידה — ולא נגעתי בו. הפרויקט הזה נושא גם את
`zr-loader` ו-`zr-admin-api`, כלומר את `zr_*` **המוגן**. `Confirm email` היא
הגדרה של הפרויקט כולו, ולכן הדלקה שם משנה גם את התנהגות ההרשמה של מערכת
שאסור לי לשנות. זו הכרעה של המשתמש, והיא רשומה ככזו ב-`NEEDS_USER §0א״`.

## מה נמצא בדרך ולא תוקן

`site_url` של `csjekrvukbdznetsrodj` הוא **`http://localhost:3000`**. כל מייל
איפוס סיסמה שהפרויקט הזה שולח מצביע לשם, ולכן אינו ניתן ללחיצה משום מחשב
אמיתי. ההרשמה כבר לא נשענת על קישור כזה (האישור אוטומטי), אבל **איפוס סיסמה
כן** — נפתח ככרטיס נפרד ב-`core.issues` ולא נדחס לצעד הזה.

## מה הצעד הזה לא אומר

* לא נמדד מסך הכניסה בדפדפן, אלא שרת האימות. מה שמוכח הוא שהפרויקט מחזיר
  session מיד; מה שכל מסך עושה עם ה-session הזה נמדד בצעדים אחרים.
* 06, 12 ו-17 נושאות מפתח של הפרויקט הזה **לנתונים**, וחלק מהכניסה בהן עוברת
  בווידג'ט המשותף שיושב על ההאב (`QA/platform/multi-project-0812/`). התיקון
  הזה סוגר את הרגל של הפרויקט שלהן; הרגל של ההאב הייתה פתוחה ממילא.
