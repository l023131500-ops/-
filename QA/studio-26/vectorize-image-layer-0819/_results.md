# QA — 26 studio (modaot-studio): וקטוריזציה לכל שכבת תמונה (item 7a) — 19/08/2026

## מה נבדק
`CHECKLIST/graphics.md` שלב 2 פריט 7 — "וקטוריזציה + הסרת-רקע (Recraft)".
קריאת קוד (Explore agent, read-only) הראתה ש-`POST /api/branding/vectorize`
כבר קיים ופרוס (server + vercel-adapter, מראה byte-identical), אבל נקרא
רק מ-`BrandKitPage.tsx` (וקטוריזציה של קונספט לוגו). לא היה כפתור בעורך
הראשי (`Editor.tsx`) להריץ אותו על שכבת תמונה כלשהי.

## מה נוסף
`Editor.tsx` בלבד (client, לא server/API): פאנל "שכבת תמונה" חדש שמוצג
כשנבחרת שכבה מסוג `image` (לא היה קיים שום פאנל-מאפיינים לשכבת תמונה
קודם — היה ריק) עם כפתור "המר לוקטור SVG (Recraft)". הכפתור ממיר את
`layer.src` הנוכחי ל-base64, קורא ל-`/api/branding/vectorize` הקיים,
ומחליף את `src` ב-data URI של ה-SVG שחזר. שימוש חוזר מלא ב-endpoint —
אין שינוי server/vercel-adapter/סכמה/רינדור/ייצוא.

## איך נבדק
Playwright, 1280×900, `more30.com/studio` (production, cache-buster):
1. נפתחה תבנית "שיעור — חסידי מלכותי" קיימת.
2. **מקרה 1 — קלט וקטורי (לא רלוונטי):** "הוסף אייקון" → `mdi:crown`
   (Iconify SVG) → "המר לוקטור SVG (Recraft)" → `POST .../vectorize`
   החזיר **502** (Recraft דוחה קלט שהוא כבר SVG) — טופל בהודעת שגיאה,
   לא קריסה/רגרסיה.
3. **מקרה 2 — קלט רסטרי אמיתי:** "העלה תמונת הרב" (פיצ'ר קיים, לא נגעתי
   בו) → קובץ PNG אמיתי → נבחרה השכבה → "המר לוקטור SVG (Recraft)" →
   `POST .../vectorize` החזיר **200 OK**, השכבה הוחלפה בפועל לאיור וקטורי
   מסוגנן (לא placeholder), מוצג נכון בקנבס.
4. "הורד PNG" — הקובץ שהורד מכיל את השכבה הממוירת המוקטרת, ברזולוציה
   מלאה — מוכיח שנתיב הייצוא (`exporter.ts` הקיים, ללא שינוי) תומך ב-SVG
   data URI כמו כל שכבת תמונה אחרת.

## תוצאה
1 שגיאת קונסולה בסך הכל לאורך כל הסבב — מה-502 הצפוי במקרה 1 (לא תקלת
קוד). מקרה 2 (השימוש המיועד — תמונה רסטרית) עבר במלואו, 0 שגיאות. שאר
הפאנלים/שכבות/גלריה/רקע/קופי ללא שינוי.

## ידוע, לא תוקן (מחוץ לסקופ הסבב הזה)
קובץ ה-PNG המיוצא הציג גם את מלבן-הבחירה הכחול-מקווקו של השכבה שהייתה
נבחרת בזמן הייצוא. לא בדקתי אם זו התנהגות קיימת-מראש (לא נגעתי כלל
ב-`CanvasStage.tsx`/`exporter.ts`) — סימון עבור סבב עתידי, לא נגרם על
ידי השינוי הזה.

## נשאר לפריט 7 (7b)
הסרת-רקע (Recraft) — אינה קיימת בכלל (לא server, לא adapter, לא UI).
דורשת endpoint חדש (לא רק חשיפת UI כמו 7a) — סבב נפרד.

## פריסה
`vite build --base=/studio/` (2165 מודולים, נקי, `tsc --noEmit` נקי) →
`_deploy/studio-more30/public/studio` (api/ ללא שינוי) →
`dpl_BwSUk8gh9HtbLd3xxmdCNLTnWYJS`, READY, aliased `studio-more30.vercel.app`.
