19 שיעורים: הממצא האחרון של קמפיין «הצג סיסמה» היה הערה, וארבעה שדות אמיתיים היו מחוץ לטווח הסורק (§1א)

הקו הפתוח היה ממצא יחיד — GabaiPortal.tsx:1416 ב-24 גליל. הוא נבדק ואינו
קיים: השורה היא הערה שמסבירה את כפתור הגילוי, והשדה עצמו (1549) כבר כתוב
type={showPassword ? 'text' : 'password'} עם כפתור ו-aria-pressed. הקמפיין
החזיק ממצא פתוח על הפרוזה של עצמו.

מה שהסורק לא ראה חשוב יותר: ה-walk הכיר .html/.jsx/.tsx/.vue/.svelte בלבד,
ושתי מערכות מציירות את כל הממשק שלהן ממחרוזות תבנית בתוך .js רגיל — שישה
שדות שמעולם לא נפתחו. 1,394 קבצים -> 2,624; 6 שדות -> 8.

password-reveal-scan.mjs: הערות מולבנות לפני ההשוואה (תו-בתו, כדי שמספרי
השורות יישארו מדויקים), שמרני במכוון — // הוא הערה רק כשהוא פותח שורה,
אחרת href="https://…" באמצע שורה היה בולע את שארית השורה ושדה שמצויר אחריה
היה נעלם; לעוור את הבדיקה גרוע מהתראת השווא שהיא מסירה. .js/.mjs/.cjs נכנסו
ל-walk ומוכרעים בכללי ה-HTML. scripts/qa יצא ממנו — כל סורק שמחפש אידיום
חייב לאיית אותו, ולכן שני הכלים דיווחו על תבניות החיפוש של עצמם. בקרה
שלישית want:'NONE' על _fixtures/pw-commented.jsx: בלעדיה, חזרה להשוואה
עיוורת-להערות הייתה מדליקה שוב את 24 גליל ונקראת כרגרסיה בגליל.

19 שיעורים — ארבעה שדות בלי שום דרך לראות מה הוקלד: login_password בטופס
ההצטרפות (705), שני api_password בהגדרות נדרים (1340, 1560), והסיסמה של
«כניסת ניהול» (1973) — מסך הכניסה היחיד של המערכת. הכבד הוא 705: אין שם
אימות-חוזר ואין סיסמה קודמת לנסות שוב, ולכן שגיאת הקלדה נשמרת וההתחברות
בעתיד נכשלת בלי שאיש ידע למה.

pwField() אחד וארבעת השדות עברו אליו. המזהה נוצר בכל קריאה כי השדות נושאים
name= ולא id=, והכפתור חייב לנקוב בשדה ב-aria-controls. המאזין delegated על
document ולא נקשר אחרי רינדור: כל מסך נצבע ב-APP.innerHTML=, ומאזין שנקשר
אחרי רינדור מת ברינדור הבא — כולל בשני מסכי הניהול שמצוירים רק מאוחר יותר.
type="button" מפורש (שני השדות בתוך form). ה-CSS בסוף style.css במכוון:
.pw-wrap input ו-.form-field input באותה ספציפיות (0-1-1), רק הסדר קובע.

אומת בדפדפן בלחיצת עכבר אמיתית על שרת מקומי (QA_API_CONFIG נוסף ל-
_serve-portal-public.mjs — המסך מושך /api/config לפני שהוא מצייר): type
password->text->password, aria-pressed false->true->false, ה-label והאייקון
מתחלפים, הערך נשמר, וה-URL זהה לפני ואחרי — הכפתור לא שלח את הטופס.
aria-controls = ה-id, padding-inline-end=42px, הכפתור כולו בתוך גבולות השדה
ו-elementFromPoint במרכזו מחזיר אותו. ב-#/join שרדו name ו-minlength=6.

אין פריסה ואין מה לפרוס: 19 היא live=false, is_deployed=false ב-core.projects.
נשאר פתוח: 35 kioskfleet js/app.js:1293-1294 — בעץ המקומי שאינו הייצור (#215),
והקיוסק הוא מסלול שההנחיה מורה לא להשקיע בו עכשיו (§1בב+ 1).
