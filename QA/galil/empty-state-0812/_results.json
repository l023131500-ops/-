{
  "app": "24-galilee-connect-hub (galil, system 24)",
  "date": "2026-08-12",
  "mount": "https://more30.com/galil/",
  "supabase_project_used_by_production": "mwljkonwdeuaahsigjdp",
  "how_the_project_was_identified": "the VITE_ vars are inlined at build time, so the deployed bundle names the project: regex over https://more30.com/galil/assets/index-pSOB0569.js returned exactly one supabase host, https://mwljkonwdeuaahsigjdp.supabase.co — same ref as apps/24-galilee-connect-hub/.env, so the repo and production agree",
  "measured_before": {
    "GET /rest/v1/synagogues?select=id,name&limit=2 (anon key)": "200 []",
    "GET /rest/v1/gabai_accounts?select=*&is_admin=eq.true (anon key)": "401",
    "rendered": "the section head 'בתי הכנסת בחצור הגלילית' and its subtitle 'לחצו על בית כנסת לפרטים מלאים' are on screen, and below them the page is white for roughly two screens. SynagogueCarousel returned null on an empty list (src/components/SynagogueCarousel.tsx:266) and Index.tsx had no empty branch, while the two side tickers hold the row open at h-[75vh]. PrayersTicker drew its header over an empty scroller.",
    "screenshot": "01-production-blank-before.png"
  },
  "change": [
    "SynagogueCarousel: the `return null` on an empty list is now a stated empty state — 'עדיין לא נרשם כאן אף בית כנסת', one sentence on what fills it, and a link to /gabai.",
    "PrayersTicker: with no prayer rows it says 'אין עדיין זמני תפילה רשומים' instead of an empty 75vh column."
  ],
  "verified_after": {
    "build": "npm run build in apps/24-galilee-connect-hub — built in 1m6s, dist/assets/index-KFiwjeZS.js",
    "served": "npx vite preview --port 4177, base /galil/",
    "note": "the local preview reads the same live project as production, so the empty list under test is the real one, not a fixture",
    "dom_assertions": {
      "'עדיין לא נרשם כאן אף בית כנסת' present in #synagogues": true,
      "'אין עדיין זמני תפילה רשומים' present in #synagogues": true
    },
    "console_errors": 0,
    "screenshot": "02-local-empty-state-after.png"
  },
  "not_done_and_why": {
    "seeding synagogues": "real data only — there is no list of Chatzor Hagelilit synagogues to load, and the gabai form is how the customer fills them.",
    "issue #62 second half": "the gabai/admin login still cannot succeed: gabai_accounts is 401 to anon and the password is compared in the browser. Fixing that needs mwljkonwdeuaahsigjdp, which the SUPABASE_ACCESS_TOKEN in core.secrets does not cover (10 projects, verified 12/08, this ref not among them). The project is Lovable-owned (galilee-connect-hub, 506805d0-89de-4112-84ff-741553892b34), which is a candidate path, not a measured one.",
    "the ?auto=admin bypass": "already closed and now verified in production — the deployed bundle initialises view as 'login' and the query param only preselects the admin tab."
  },
  "test_mode": "no charge, no message sent, no AI credit spent. Untouched: 08, 09, bkalut-app, bkalot-admin, zr_*, NEDARIM3873."
}
