שכפול בקלות שכבה 3: שלוש העמודות של 0074 לא הגיעו לשום מסך (§5ב, 0075)

הקו הפתוח שנוסח ב-3f96141, מילה במילה: «השדות אינם מגיעים למסך —
bkalot_clone_admin_case מונה את עמודות המסמך אחת-אחת ולכן עמודה חדשה אינה מופיעה
בתשובה מאליה, והמנהל עדיין רואה «מסמך #72» בלי לדעת איזה מכתב יושב בו; זו הלבנה
הבאה (RPC ואז UI ואז פריסה)». נלקח כפי שנוסח ולא הורחב: מיגרציה בלבד — apps/37 לא
נגעה, bkalot-clone-admin נשארה v6, supabase/functions לא נגעה ואין פריסת פורטל.

מה היה, ומדוע זו תקלת מוצר ולא הערה על סכמה: 0074 הוסיפה template_key,
template_fallback ו-updated_at ל-documents, ו-render כותב אותן בכל הפקה. שתי
פונקציות הניהול בונות את המסמך ב-jsonb_build_object ומונות את העמודות אחת-אחת,
ולכן עמודה חדשה אינה מופיעה בתשובה מאליה. התוצאה: הנתון נכתב למסד בכל הפקה, ואינו
קיים בשום מסך. המנהל רואה מזהה וחותמת יצירה — ומאז fb607a0 שתי הפקות מאותה פנייה
בשני מכתבים שונים מציגות שורה זהה לחלוטין.

מה נבנה: admin_case (בלוק documents) ו-admin_document מחזירות את שלוש העמודות
ולצידן template_name_he ו-overwritten. אין שינוי סכמה, אין שינוי כתיבה, ואין נגיעה
ב-render.

חמש הכרעות, ושתיים הן העיקר. (1) template_name_he ב-LEFT JOIN, ו-null כששורת
התבנית אינה קיימת: 0074 הכריעה ש-template_key הוא text ואינו FK, כלומר מפתח בלי
שורה הוא מצב חוקי; מיפוי בלקוח מרשימת templates שחוזרת באותה תשובה היה נכשל בשקט
בדיוק במקרה שבגללו נכתבה אותה הכרעה — תבנית שנמחקה. (2) template_fallback חוזר
כפי שהוא, כולל null: coalesce(...,false) היה כותב על כל מסמך שקדם ל-0074 «הופק
מתבנית אמיתית», בדיוק הקביעה חסרת-המקור שהכרעה (5) של 0074 סירבה לכתוב למסד, ואין
היגיון בכך שהמסד יאמר «לא ידוע» וה-RPC יהפוך את זה לקביעה בדרך החוצה. (3)
overwritten נגזר בשרת — השוואת timestamptz ולא השוואת שתי מחרוזות אחרי סריאליזציה.
(4) גם admin_document ולא רק admin_case: זו הפונקציה שמחזירה את גוף המכתב, כלומר
המסך היחיד שבו קוראים את מה שייצא, ולתקן אחת מהן היה משאיר את הנתון חסר בדיוק שם.
(5) תוספת בלבד — אין שינוי במיון, בסינון או בשם של שדה קיים.

נמדד על פניות שנוצרו דרך bkalot_clone_intake ולא בהזרקה ל-cases: #84 (treatment,
single_parent, 42 זכויות) נבדקה, #85 היא הבקרה שלא נגעו בה כלל ונשארה בלי מסמך.

המדידה שקונה את הפעימה, בשתי טרנזקציות נפרדות: הפקה ראשונה בלי מפתח → מסמך #75,
rights_treatment_reply, «מענה לפניית טיפול — רשימת הזכויות», fallback=false,
2,508 תווים, created_at=updated_at=03:39:12.549537, overwritten=false. הפקה שנייה
עם general_inquiry_reply → אותו מסמך #75, «מענה לפנייה כללית», 240 תווים,
created_at קפוא ו-updated_at 03:39:28.104807 — 15.555 שניות, overwritten=true.
240 ולא 2,508: מכתב אחר ולא תווית אחרת. עד היום המסך היה מציג את שתי השורות האלה
זהות לחלוטין.

שלוש בקרות, וכל אחת קונה הכרעה. (1) template_key הוחלף ל-deleted_letter_0075
(0 שורות ב-templates) → template_name_he=null והמפתח נשאר כפי שהוא. (2) השורה
הועמדה בצורת מסמך מלפני-0074 → jsonb_typeof על template_fallback, updated_at
ו-overwritten הוא null בשלושתם, ו-j ? 'template_name_he' וגם j ? 'template_fallback'
מחזירים true: null אמיתי ב-JSON והמפתח קיים, ולא שדה חסר. (3) והבקרה שבלעדיה
הכרעה (1) הייתה מסוכנת: rights_treatment_reply נוטרלה ואז הפקה בלי מפתח —
template_key=rights_treatment_reply, template_name_he נושא את השם המלא של המכתב,
template_fallback=true, 96 תווים ולא 2,508. השורה נושאת שם מכתב אמיתי והגוף אינו
בא ממנו, ורק הדגל מבדיל. התבנית הוחזרה ל-enabled=true עם updated_at=updated_at
בשני הכיוונים.

הרשאות נמדדו ולא הונחו, כי create or replace שומר ACL: שתי הפונקציות, לפני ואחרי —
anon=false, authenticated=false, service_role=true. בלי זה SECURITY DEFINER כאן
היה חושף כל פנייה וכל גוף מכתב לכל מחזיק מפתח anon.

סטייה שנרשמת ולא נבלעת: המדידה רצה ב-RPC ולא ב-edge function מעל HTTP, כמו ב-0073.
נתיב /case מעביר את הגוף כפי שהוא, אך המדידה הזאת אינה מוכיחה את המעבר — הוא ייסגר
בפעימת ה-UI, שבה ממילא נמדד הדפדפן מול הכתובת החיה.

מצב טסט נמדד ולא הוצהר: outbound_queue נשאר 8 ו-delivery_log נשאר 3, אפס מסמכים עם
queue_id, אפס פניות ב-sent, אפס שורות עם sent_at, אפס שורות mode=live. שתי
הפונקציות קוראות בלבד — אין net.http, אין pg_net ואין ולו קריאה יוצאת אחת. אין
מייל, אין הודעה ואין מסמך שיצא החוצה.

המקור לא נגע, ונמדד ולא הונח: 8 שורות app_key=bkalot נשארו 8, טביעת אצבע
da28ec6315535ec0bfbebad4d57752a7 (id:status:attempts) זהה לפני ואחרי.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (מסמך, 42 case_rights, 2 פניות, 2 אנשי
קשר). אחרי: cases=0, case_rights=0, documents=0, contacts=4, admin_users=1,
admin_sessions=5, templates=2 (שתיהן enabled), catalog=888, outbound_queue=8,
delivery_log=3 — זהה בדיוק לבסיס. שתי הפונקציות נשארו.

אין נגיעה בשום נתיב מוגן: git status על apps/08-bkalut-app, apps/09-bkalot-admin,
supabase/functions ו-apps/37-bkalot-clone ריק, ואין נגיעה ב-bkalut-app/
bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

מה שנשאר פתוח ולא נבלע: אין מי שקורא את השדות — admin.html מציג «מסמך #75» ואת
created_at בלבד, וחמשת השדות חוזרים ואיש אינו קורא אותם; זו הלבנה הבאה (UI ואז
פריסה), אותו פיצול כמו 0073 → 978ab68 → fb607a0. template_key is null נמדד בצורתו
בלבד, בעדכון ישיר, ולא על שורה אמיתית מלפני 0074 — אין ולו שורה כזו במסד. «מי
הפיק» אינו נכתב לשום מקום, וגם לא «מי שינה ומתי». sent בלתי-ניתן-להשגה בכוונה;
bkalot_clone_admin_create אינה חשופה ב-HTTP; pdf/audio → channel_unsupported.
public_visible ו-show_in_showcase של #37 נשארים false.
