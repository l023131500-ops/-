שכפול בקלות שכבה 1: הטופס היה קיים ונמדד, ולא הייתה ולו כתובת אחת שמשתמש יכול לפתוח (§5ב, #223 נסגר)

more30.com/bkalot-studio חי — עם ובלי לוכסן, 200 בשניהם.

לא נוצר פרויקט Vercel נפרד: apps/37-bkalot-clone/index.html הוא קובץ אחד בלי
build ובלי נכס יחסי אחד, ולכן אין לו את הבעיה שמצדיקה פרויקט משלו וגם לא את
מלכודת ה-base href. הוא מוגש מהפורטל — stage-portal.ps1 מעתיק את המקור אל
portal/dist/bkalot-studio/ בכל staging (throw ולא warning אם המקור חסר: פריסה
בלי הקובץ מגישה 404 בנתיב שה-rewrite כבר מכריז עליו), ושני rewrites
ב-vercel.dist.json. אין עותק שני של הקובץ בריפו.

נמדד לפני הפריסה: portal/dist בעץ הוא מ-10/08 ופריסה ממנו יכולה להחזיר את
הפורטל אחורה — הושווה מול הייצור קודם, אותם hash-ים לשני הנכסים, והדיף השורתי
היחיד הוא הזרקת NetFree.

אומת בדפדפן על הכתובת החיה: «טיפול מלא» פותח את «המצב שלכם» (25 אפשרויות)
והדוא״ל הופך לחובה; שליחה מלאה החזירה «הפנייה נקלטה» עם 59 זכויות — אותו מספר
שנמדד ב-SQL, בקצה ה-HTTP ובמדידה המקומית. במסד: הטלפון נשמר מנורמל 0509998877
וההערה העברית חזרה בלי כפל-קידוד.

מצב טסט אומת בכך שהתור לא זז (outbound_queue=8, delivery_log=3, catalog=888
לפני ואחרי), והבדיקה התגלגלה אחורה במלואה ל-cases=0, case_rights=0, contacts=4.

נשאר פתוח: אין מי שרואה את הפנייה — שכבה 2. public_visible ו-show_in_showcase
נשארו false.

עדות: QA/bkalot-clone/mount-0813/
