# 0077 — השורה יודעת מי סגר את הפנייה, ולמסך אין דרך לשאול

**§5ב — שכפול בקלות שכבה 3 · 14.08.2026**

## הקו שנסגר, מילה במילה

נכתב ב-f1ba90c (heartbeat 572), בסופו: «bkalot_clone_admin_case עדיין אינה מחזירה את שתי
העמודות ולכן admin.html אינו יכול לומר מי סגר (הלבנה הבאה)». נלקח כפי שנוסח ולא הורחב:
מיגרציה אחת — `bkalot_clone_admin_case` בלבד. אין שינוי סכמה, אין נגיעה ב-`set_status`,
`bkalot-clone-admin` נשארת v7, `apps/37` לא נגעה ואין פריסת פורטל.

## מה שהתגלה לפני שנכתבה ולו שורה אחת

קובץ המיגרציה `0077_the_row_knows_who_closed_the_case_and_the_screen_cannot_ask.sql` **היה
כבר על הדיסק** — 11,290 בתים, נוסח מלא עם חמש הכרעות — **ולא היה בגיט ולא הורץ על המסד**.
`git log` עליו החזיר אפס commits, `git status` הראה `??`, ובמסד החי `position('decided_by' in
prosrc)` על הפונקציה החזיר **0**. הריצה הקודמת כתבה אותו ונקטעה לפני שהחילה אותו. זה בדיוק
המצב שאין לו סימפטום: הקובץ קיים, קריא, משכנע — והמוצר אינו מכיל אותו.

**הוא לא הוחל על סמך שהוא נראה נכון.** מה שנמדד קודם: גוף הפונקציה בקובץ פורק ב-node, שש
השורות החדשות (שני הערות, שלושה שדות, ושורת ה-`left join`) הוסרו ממנו, וה-md5 של מה שנשאר
הושווה ל-`md5(prosrc)` של הפונקציה החיה.

| | |
|---|---|
| גוף הקובץ | 4,439 בתים · `8241c57160085eab23431f1ac43a087f` |
| הקובץ פחות שש השורות | 4,087 בתים · `7ebff6716d8f43bc9ba70763954d5907` |
| הפונקציה החיה, לפני | 4,087 בתים · `7ebff6716d8f43bc9ba70763954d5907` |

זהה בייט-בבייט. כלומר הקובץ הוא הפונקציה החיה **בתוספת** שש שורות ולא גרסה מקבילה שנכתבה
מעל עץ ישן — `create or replace` על גרסה כזו היה מחזיר אחורה שדות של 0075 בלי ששום דבר ידווח
שגיאה. אחרי ההחלה: `md5(prosrc)` = `8241c57160085eab23431f1ac43a087f`, בדיוק md5 של גוף
הקובץ.

## שתי טענות שהקובץ מסתמך עליהן, נמדדו ולא הונחו

- **הכרעה (2)** נשענת על כך ש-`full_name` הוא NOT NULL — אחרת אותו null היה מכסה שני מצבים.
  נמדד על הסכמה החיה: `is_nullable = NO`.
- **הכרעה (1)** נשענת על כך ש-`decided_by` אינו FK. נמדד: **0** אילוצי מפתח זר על העמודה.

**הרשאות** — `create or replace` שומר ACL ולכן קל להניח. נמדד לפני ואחרי, זהה:
`anon=false authenticated=false service_role=true`. חתימה אחת בלבד לפונקציה (לא שתיים —
זו התקלה של 0076, שם ה-DROP היה הכרחי).

## המדידה — מעל HTTP, על נתיב הקליטה הציבורי

שתי פניות נוצרו דרך `bkalot-clone-intake` עם מפתח anon כמו מהטופס, ולא בהזרקה למסד:
**#101** treatment/single_parent עם 42 זכויות, **#102** בקרה. כניסה לניהול מעל HTTP, שני
מנהלים (26, 27). כל קריאה דרך `/case` החי — כלומר נמדד מה שהמסך מקבל, לא מה שהמסד יודע.

שלושת המפתחות נבדקו ב-`Object.hasOwn` ולא ב-`?? null`: שדה חסר ושדה שנושא null נראים זהים
בקריאה רגילה, וזו ההבחנה שהפעימה עומדת עליה. **בכל שש הקריאות שלושת המפתחות קיימים.**

| מה | status | decided_by | decided_at | decided_by_name |
|---|---|---|---|---|
| #101 לפני כל הכרעה | new | null | null | null |
| #101 אחרי לחיצת מנהל 26 | closed | **26** | 04:57:30.822948 | **QA 0077** |
| #101 אחרי לחיצת מנהל 27 | rejected | **27** | 04:57:33.266842 | **QA 0077 שני** |
| #101 אחרי מחיקת חשבון 27 | rejected | **27** | 04:57:33.266842 | **null** |
| #102 (בקרה) | new | null | null | null |

השורה הרביעית היא הבקרה שקונה את הכרעות (1) ו-(2), והיא נמדדה על **שורה אמיתית ולא בעדכון
ישיר של העמודה**: חשבון המנהל השני נמחק בין שני שלבי המדידה, והפנייה נשארה שלמה — `ok:true`,
הפנייה חוזרת, 42 הזכויות במקומן. `INNER JOIN` היה מעלים את הפנייה כולה מהמסך ברגע שהמכריע
עזב. הזהות נשארה 27 והחותמת לא זזה — רק השם נעלם, ומכיוון ש-`full_name` הוא NOT NULL הוא
אומר דבר אחד בדיוק: «החשבון שהכריע כאן אינו קיים עוד».

השורות השנייה והשלישית הן מה שקונה את הפעימה: הזהות היא **נתון ולא קבוע** — שני מנהלים, שני
ערכים, ושלושת השדות מתחלפים יחד. החותמות שחוזרות מ-`/case` זהות בדיוק לאלו שהחזיר
`/set-status` באותה לחיצה.

**בלי רגרסיה** (הכרעה (5) — תוספת בלבד): באותה תשובה `rights=42`, `documents=0`,
`templates=2`, `contact.full_name` = «בדיקה 0077 א», `contact_email_differs=false`,
`kind=treatment`, `to_email=test@more30.com`.

## 🚫 מצב טסט — נמדד ולא הוצהר

`outbound_queue` נשאר 8, `delivery_log` נשאר 3, אפס שורות עם `sent_at`, אפס שורות
`mode=live`, אפס פניות ב-`status=sent`, אפס מסמכים עם `queue_id`. הפונקציה קוראת בלבד —
אין בה UPDATE, אין INSERT ואין ולו קריאה יוצאת אחת. **לא יצא מייל, לא יצאה הודעה ולא יצא
מסמך.**

## המקור לא נגע

8 שורות `app_key=bkalot` נשארו 8, טביעת אצבע `da28ec6315535ec0bfbebad4d57752a7` זהה לפני
ואחרי. `git status` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`, `apps/37-bkalot-clone`
ו-`supabase/functions` ריק. אין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא
בסכמות csj/csj_src/igud.

## הבדיקה התגלגלה אחורה — כולל מה שהריצה הקודמת השאירה

הבסיס שנמדד בתחילת הפעימה **לא** היה הבסיס המתועד: cases=3 (98/99/100), admin_users=3,
contacts=7, admin_sessions=8. הריצה שנקטעה השאירה אותם. הגלגול אחורה כאן מכסה גם אותם:

| | לפני הפעימה | אחרי |
|---|---|---|
| cases · case_rights · documents | 3 · 42 · 0 | **0 · 0 · 0** |
| admin_users · admin_sessions | 3 · 8 | **1 · 5** |
| contacts | 7 | **4** |
| templates (enabled) · catalog | 2 (2) · 888 | **2 (2) · 888** |
| outbound_queue · delivery_log | 8 · 3 | **8 · 3** |

זהה בדיוק לבסיס המתועד. הפונקציה נשארה (`8241c571…`).

## מה שנשאר פתוח ואינו נבלע

- **אין מי שמציג את השלושה.** התשובה כוללת אותם מעכשיו, ו-`admin.html` אינו קורא אותם —
  מסך הפנייה עדיין אומר `status` ו-`updated_at` בלבד. זו הלבנה הבאה (UI), ואחריה פריסת
  פורטל, אותו פיצול כמו 0074 → 0075 → e9e949c → 530fb44.
- **`bkalot_clone_admin_cases` — רשימת העבודה — לא נגעה.** שורה ברשימה אינה אומרת מי הכריע.
  הוצהר בקובץ המיגרציה עצמו כדי שלא ייקרא כהשלמה.
- **אין היסטוריה.** #101 עברה 26 → 27, ומ-26 לא נשאר דבר. הכרעה קודמת נדרסת.
- **`email` של המנהל אינו חוזר** — הכרעה (3), במכוון.
- «מי הפיק» על `documents` עדיין אינו נכתב לשום מקום.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP (משתמשי
  הבדיקה נוצרו ב-SQL ישיר בריצה הקודמת); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
