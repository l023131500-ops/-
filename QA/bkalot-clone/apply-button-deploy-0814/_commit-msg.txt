שכפול בקלות שכבה 3: «הצג» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח נכתב ב-d7c5757 (heartbeat 609), מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין מגיש «סינון», והמדידה של המתוקן רצה מול
שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר; זו הפעימה הבאה». נלקח כפי
שנוסח ולא הורחב — פריסה בלבד: אין שינוי קוד, אין מיגרציה ואין פריסת edge
function; bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin v7.
apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-d7c5757 (95,580 בתים,
git status ריק על apps/37-bkalot-clone). מה שנכנס ל-commit הוא ראיות בלבד.

חמשת מסומני NEW נפסלו מול הקובץ שבייצור לפני שנקבעו — המלכודת של 530fb44,
29d6ac6 ו-e12acf9: מסומן שמחזיר true על שתי הגרסאות אינו מודד דבר. כל אחד 0
בייצור ו-1 במקור. אחד המסומנים הוא ההפך משאר הפריסות כאן: ">סינון</button>"
נספר 1 לפני ו-0 אחרי — זו החלפת נוסח ולא תוספת, ולכן היא נמדדת גם בהיעלמות.
prev הועתק מ-portal/dist לפני ה-staging: 95,108 בתים MD5 141DB33F — בדיוק מה
ש-6557313 רשם כנפרס, כלומר ה-dist שעל הדיסק היה הגרסה שבייצור ולא קובץ ישן
יותר. 38 קבצים נפרסו, מ-portal/dist ובלי --prebuilt (prebuilt-deploy-drops-vercel-json).

HTTP לפני ואחרי: מסך הניהול 95,344 → 95,816 בשתי הכתובות (עם לוכסן ובלעדיו,
שני rewrites נפרדים), חמשת המסומנים 0 → 1, ">סינון</button>" 1 → 0. הדלתא מול
המקור 236 בדיוק — דלתת NetFree הידועה. שתי בקרות שמראות שזו החלפה ולא סחף:
המילה «סינון» נשארה 14 בדיוק (הכפתור ויתר עליה, ההערות החדשות החזירו אותה)
ו«אין פניות שתואמות את הסינון» נשארה 2 — ריקנות היא עדיין תולדה של סינון
בלבד. הטופס נשאר 32,558 ודף הבית 3,517. אפס תווי החלפה בארבע הכתובות, לפני
ואחרי. נכתב ב-node ולא ב-PowerShell בכוונה (ps1-without-bom-parsed-as-cp1255).

המדידה שקונה את הפעימה אינה טקסט אלא התנהגות על הכתובת החיה: כפתור ששמו שונה
ואינו מחיל עוד את שתי השאלות הוא בדיוק התקלה שהחלפת הנוסח יכולה להכניס. נמדד
על more30.com/bkalot-studio/admin עם cache-buster ולא על הכתובת החשופה (תקלת
f2001e0), בדפדפן אמיתי 1280x900, בהתחברות אמיתית דרך המסך. הכפתור נקרא מתוך
ה-DOM החי: textContent «הצג» ו-title «מחיל את הסינון ואת המיון יחד, ומחזיר
לעמוד הראשון». תיבת המיון נקראה מתוך ה-select החי ולא מה-HTML — היא נבנית בזמן
ריצה — ושתי האפשרויות שלה שלמות. גוף כל בקשה נלכד כפי שיצא מ-fetch ולא שוחזר
מהמסך: חמש בקשות, לא אחת יותר.

הזריעה היא היפוך מלא ולא שלוש שורות: שלוש פניות דרך bkalot-clone-intake מעל
HTTP עם מפתח anon, כמו מהטופס (195-197, שלושה טלפונים שונים בכוונה — הקליטה
מאתרת איש קשר לפי טלפון), ואז הכרעה בסדר הפוך — 196 ואז 195 — בשתי קריאות
נפרדות, כי now() קפוא בתוך עסקה וקריאה אחת הייתה מטביעה בשתיהן את אותו
decided_at (14:31:20.906 מול 14:31:26.492 — נמדד ולא הונח). #197 לא הוכרעה
כלל. ההכרעה עברה בנתיב של המוצר עצמו ולא ב-update ידני:
bkalot_clone_admin_set_status, אותה פונקציה שהמסך קורא לה, והיא שכתבה
decided_at ו-decided_by=52.

מה שנמדד על החי, לפי הסדר: טעינה ראשונה sort:"" → 197,196,195 ו«3 פניות».
שינוי המיון בלי לחיצה — אפס בקשות יצאו והסדר לא זז, בדיוק ההתנהגות שנמדדה
בייצור לפני הפריסה, כלומר הפריסה לא הפכה את המסך למגיב-לבחירה. לחיצה על «הצג»
→ sort:"decided_at" ו-195,196,197: היפוך מלא, ו-#197 מהראש לזנב — nulls last
על המסך. המונה נשאר «3 פניות», כלומר מיון אינו מזיז את הקבוצה הנמנית. בקרה
שהכפתור מחיל את שתיהן ולא רק אחת: decided=no עם sort=decided_at באותו גוף
מחזיר את #197 לבדה ו«פנייה אחת». רגרסיה: חזרה לברירת המחדל מחזירה גוף עם
decided:"" ו-sort:"" ואת 197,196,195 — הבחירה אינה נדבקת.

זו החלפת נוסח ולא שינוי התנהגות, ואמרנו זאת ולא בלענו: גוף הבקשה שנמדד כאן על
החי זהה בייט-בייט לזה שנמדד בייצור ב-d7c5757 לפני השינוי. הכפתור לא קיבל
תפקיד חדש — רק שם שמתאר את שני התפקידים שכבר היו לו.

שלושה צילומים, שלושה MD5 שונים (DB806DAE / 921A402E / F6D10BE5) — נבדקו ולא
הונחו (screenshot-evidence-below-the-fold). אפס שגיאות ואפס אזהרות בקונסולה;
ארבע הודעות INFO הן טעינת גופנים של NetFree מ-netfree.link ולא מהאפליקציה.

מצב טסט נמדד ולא הוצהר: documents=0, אפס פניות ב-sent, כל קליטה החזירה
queued=false, outbound_queue נשאר 8 ואפס שורות עם sent_at (טביעת אצבע
bf57d69edaf6725ff7f52ad3007c4170 זהה בבסיס ואחרי), delivery_log נשאר 3. אין
מייל, אין הודעה ואין מסמך שיצא החוצה.

המנהל נוצר דרך הנתיב של המוצר ולא בהזרקת hash: bkalot_clone_admin_create
החזירה id=52 והסיסמה עברה crypt/bf12. התגלגל אחורה בחמש פקודות נפרדות לפי id
ולא CTE אחד — תקלת 124cfc2 — ואנשי הקשר לפי id (202-204) ולא לפי טלפון שנשמר
מנורמל — תקלת ac6e523; כל מחיקה החזירה מספר והמספר נבדק (case_rights 0,
cases 3, contacts 3, admin_sessions 1, admin_users 1). אחרי: cases=0,
case_rights=0, documents=0, admin_users=1, admin_sessions=5, templates=2,
contacts=4, outbound_queue=8, delivery_log=3, catalog=888 — בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

פתוח: נתיב המיון סגור מארבעת צדדיו — מסד (0082), מסך (da1e3a6), פריסה
(6557313) ונוסח הכפתור (d7c5757 + כאן) — ואין בו קו פתוח שנשאר. אין כיוון עולה
ואין מפתח dir. שתי שורות עם אותו decided_at לא נמדדו. ה-title נמדד כתכונה
ב-DOM ולא כטולטיפ שנפתח בריחוף — לא נבדק שהוא נראה בעכבר, ובמסך מגע אין לו
דרך להיראות כלל. קווים שממשיכים ולא נגעו: אין היסטוריית הכרעות; הקליטה מחברת
42 זכויות ל-treatment ואפס ל-info/reminder על אותו situation; «מי הפיק» על
documents אינו נכתב לשום מקום; ${out.text_chars} ו-${d.html_chars} נשארו
במספר; כותרות «זכויות (N)» ו«מסמכים (N)» לא נגעו. sent בלתי-ניתן-להשגה
בכוונה; bkalot_clone_admin_create אינה חשופה ב-HTTP; pdf/audio →
channel_unsupported. public_visible ו-show_in_showcase של #37 נשארים false.
