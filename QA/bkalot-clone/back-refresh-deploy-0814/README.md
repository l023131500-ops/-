# «חזרה לרשימה» מרעננת את הרשימה — נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (13516f0) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה שבה החזרה אינה מרעננת,
והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא את המוצר
(ה-rewrite, ההעתקה ל-dist ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה
הבאה, בדיוק כמו 1abad72 → f2001e0».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-13516f0 (86,588 בתים,
MD5 `0F72749FA8162820B5F86E3AAB1937A4`, `git status` ריק) ו-`bkalot-clone-admin`
נשארה v7. אותה חלוקה כמו 1abad72 → f2001e0, e9e949c → 530fb44 ו-2a067be → 188bd04.

## המסומנים נמדדו מול הגרסה הקודמת לפני שהונחו

13516f0 היא **שורת קוד אחת** (`load()` בכפתור «חזרה») ו-15 שורות הסבר, ולכן רוב
המסומנים כאן הם טקסט הערה. זו אינה חולשה: `admin.html` מוגש כפי שהוא, בלי minify
ובלי build — 236 הבייטים היחידים שנוספים בדרך הם דלתת NetFree — ולכן ההערה
שנמצאת בייצור היא בדיוק ההערה שבקובץ. המסומן שקונה את הפעימה הוא שורת הקוד עצמה;
ההערות הן בקרה שזה אותו קובץ ולא קובץ אחר באותו גודל.

`show("screen-list")` **לבדו** נמדד true בשתי הגרסאות והועבר ל-OLD — ההבדל אינו
בקריאה אלא במה שבא אחריה. זו בדיוק התקלה של 530fb44 («נוסח נפילה») ושל 29d6ac6
(«החשבון אינו קיים עוד»), והיא נתפסת רק כשמודדים לפני שמניחים.

ארבעת המסומנים נבדקו על ארבעה קבצים ב-`marker-check.mjs`:

| | ארבעת המסומנים החדשים | שנים-עשר הישנים |
|---|---|---|
| `git show 13516f0^` (הגרסה שבייצור) | **כולם false** | כולם true |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | **כולם false** | כולם true |
| `portal/dist/…/admin.html` אחרי ה-staging | כולם true | כולם true |

`prev-admin.html` חולץ ב-`execFileSync` עם `encoding:"buffer"` ולא דרך צינור
PowerShell — צינור מוסיף BOM והופך בייטים, ואז ההשוואה מודדת את הצינור ולא את
הקובץ. 85,043 בתים, בדיוק הקובץ ש-29d6ac6 פרסה.

## הבדיקה שרצה לפני הפריסה ולא הונחה

`portal/dist` יושב על הדיסק, ופריסה מ-dist ישן מחזירה את כל הפורטל אחורה בלי
ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו לייצור — `index-C2AHwa-z.js`
ו-`index-B35ByRD-.css`, זהים בשני הצדדים לפני ואחרי.

`dist/bkalot-studio/admin.html` היה 85,043 (הקובץ של 29d6ac6,
MD5 `6FD618CDFB0C8A04924B1F88667A7B57`) והמקור 86,588; אחרי ה-staging שניהם
86,588 עם אותו MD5 בדיוק. 38 קבצים ב-staging, כמו בפריסה הקודמת.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 85,279 | **86,824** |
| `/bkalot-studio/admin/` | 85,279 | 86,824 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 845 | **881** |

הדלתא מול המקור היא **236 בדיוק** (86,824 − 86,588) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים — כלומר
המדידה מראה גם רגרסיה ולא רק את התוספת.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin`, על שתי פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא בהזרקה למסד:
**#116** תיגע, **#117** בקרה. כניסה לניהול נעשתה במסך עצמו, מנהל 37
(«QA 0081 פריסה»). הסקריפט לא הפיק ולא הכריע דבר — שתי השורות נוצרו `new`, וכל
שינוי שנראה אחר כך הוא תוצאת לחיצה בדפדפן.

**המדידה שקונה את הפעימה.** #116 נפתחה, «הפק מסמך» נלחץ, ואז «חזרה לרשימה» —
ותו לא. בלי «סינון», בלי טעינה מחדש של העמוד:

| | לפני | אחרי «חזרה לרשימה» |
|---|---|---|
| סטטוס #116 | חדשה | **בטיפול** |
| `span` בשורה | 8 | **10** |
| מסמכים | — | **1 מסמכים** |
| סימן | — | **«לא הוכרע מהמסך»**, `rgb(168, 99, 31)` |

ה-title על הסימן: «הפנייה אינה «חדשה» ולא נרשמה עליה לחיצה — הטריגר קידם אותה
בהפקת מסמך». הצבע וה-title נקראו מ-`getComputedStyle` ומה-DOM ולא מהצילום.

**מה שהמסד אמר באותו רגע**, ולכן זו תקלת מוצר ולא הערה על רענון: #116 הייתה כבר
`in_progress` עם מסמך אחד ו-`updated_at` 06:32:34 — הטריגר `documents_advance_case`
קידם אותה **בשרת**, בלי שהלקוח יודע. לפני הפריסה השורה הייתה ממשיכה לומר «חדשה»
ובלי ולו סימן אחד, כפי ש-13516f0 מדדה מקומית (8 span מול 10) — כלומר נתון שנמדד
בעוד שהוא ישן. `decided_by` ו-`decided_at` נשארו null, ולכן «לא הוכרע מהמסך» הוא
הנוסח הנכון: הפנייה זזה ואיש לא לחץ.

**הבקרה שאין לה תחליף:** #117 נוצרה באותה ריצה ולא נגעו בה — 8 `span`, «חדשה»,
בלי סימן, לפני «חזרה לרשימה» ואחריה, ובמסד `created_at` שווה בדיוק ל-`updated_at`.
הרענון אינו ממציא שינוי בשורה שאיש לא נגע בה.

בלי רגרסיה: «2 פניות» בשני השלבים, אחרי החזרה גלוי `screen-list` בלבד
(`screen-case: none`) ו-`list-error` מוסתר.

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — 0 מתוך 0 הודעות, לעומת
`favicon.ico` 404 של השרת הסטטי במדידה המקומית של 13516f0.

שני הצילומים נבדקו ב-`Get-FileHash`, והפעם הם **שונים** — ב-13516f0 שני הצילומים
הראשונים היו זהים ב-MD5, וזו הייתה הראיה עצמה (הרשימה שלא זזה):

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-both-new.png` | 38,938 | `857A665D0B64A6CF0FB9DB6D9BB0357F` |
| `02-live-back-fresh.png` | 41,086 | `124C9410C7E8092577DC807818ADF722` |

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, שורות `outbound_queue` עם `sent_at` = 0, שורות `mode=live`
= 0, מסמכים עם `queue_id` = 0, `delivery_log` נשאר 3, ו-`outbound_queue` נשאר 8
גם אחרי הפקת המסמך — «הפק מסמך» כותב שורה ב-`documents` ואינו מכניס לתור. טביעת
האצבע של התור (`046ae375ade6f929ecc61b91aaf39cf5`) זהה בבסיס, בזמן המדידה
ואחריה. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## גלגול אחורה

מסמך אחד, 84 `case_rights`, 2 פניות (116/117), 2 אנשי קשר, סשן אחד ומשתמש הניהול
37 — כולם נמחקו בסדר הנכון. אחרי: `cases`=0, `case_rights`=0, `documents`=0,
`admin_users`=1, `admin_sessions`=5, `contacts`=4, `templates`=2, `catalog`=888,
`outbound_queue`=8, `delivery_log`=3 — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` ריק, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- **רענון שנכשל משאיר את השורות הישנות על המסך** — `count` אומר «—» ויש שורת
  שגיאה, ולכן המצב אינו שקט, אבל השורות עצמן נשארות. זהה בדיוק לסינון ולדפדוף,
  ולא נמדד כאן בייצור: אין נתיב HTTP שיכול להפיל את `/cases` בבקשה אחת.
- `await load()` שרץ אחרי «שינוי סטטוס» הפך למיותר ולא הוסר — הסרה היא שינוי
  התנהגות בפני עצמו.
- «חזרה לרשימה» מרעננת את **אותה** רשימה — הסינון ו-`offset` אינם מאופסים. זה
  מכוון, ולא נמדד כאן על עמוד שני: שתי פניות אינן ממלאות עמוד.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`.
- אין היסטוריה: הכרעה קודמת נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום, ולכן «1 מסמכים» אינו אומר מי
  הפיק אותו; והמונה עצמו אינו סופר עברית כמו «2 פניות» שמעליו.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP
  (משתמש הבדיקה נוצר ב-RPC ישיר); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
