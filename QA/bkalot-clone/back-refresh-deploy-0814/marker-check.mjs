// בודק את המסומנים של 13516f0 מול קובץ מקומי. הטעם: מסומן שהיה true גם לפני
// הפריסה אינו מעיד על הפעימה — ב-530fb44 «נוסח נפילה» היה כזה, וב-29d6ac6
// «מנהל #${c.decided_by} · החשבון אינו קיים עוד» היה כזה. שניהם נתפסו רק מפני
// שנמדדו מול הגרסה הקודמת ולא הונחו.
//
// 13516f0 היא שורת קוד אחת (`load()` בכפתור «חזרה») ו-15 שורות הסבר, ולכן רוב
// המסומנים כאן הם טקסט הערה. זו אינה חולשה: admin.html מוגש כפי שהוא, בלי
// minify ובלי build — 236 הבייטים היחידים שנוספים בדרך הם דלתת NetFree — ולכן
// ההערה שנמצאת בייצור היא בדיוק ההערה שבקובץ. המסומן שקונה את הפעימה הוא
// שורת הקוד עצמה; ההערות הן בקרה שהיא אותו קובץ ולא קובץ אחר באותו גודל.
import { readFileSync } from "node:fs";
const NEW = [
  'show("screen-list"); load();',
  "חזרה לרשימה מרעננת אותה, ולא רק מחליפה מסך",
  "ורשימת מה-שעלול-לשנות אינה ידועה ללקוח מראש",
  "show לפני load ולא אחריה",
];
const OLD = [
  'show("screen-list")',
  "decidedBit",
  "לא הוכרע מהמסך",
  "fillDecidedLine",
  "לא נרשמה הכרעה ידנית מהמסך",
  "החשבון אינו קיים עוד",
  "<th>מכתב</th>",
  "fillTemplateCell",
  "tmpl-sel",
  "שינוי סטטוס",
  "עבד עכשיו",
  "הכנס לתור",
];
const t = readFileSync(process.argv[2], "utf8");
console.log(process.argv[2], t.length, "chars", Buffer.byteLength(t, "utf8"), "bytes");
console.log("new:", NEW.map((m) => m + "=" + t.includes(m)).join(" | "));
console.log("old:", OLD.map((m) => m + "=" + t.includes(m)).join(" | "));
