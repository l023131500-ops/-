# שכפול בקלות שכבה 3 — הפריסה: ההפקה נמדדת על הכתובת החיה (14/08/2026)

הקו הפתוח שהפעימה הקודמת (`c2a0a36`) הותירה נוסח בה עצמה במפורש, גם בהודעת
ה-commit וגם ב-`QA/bkalot-clone/render-ui-0814/README.md`:

> **הקובץ טרם נפרס.** `more30.com/bkalot-studio/admin` עדיין מגיש את הגרסה בלי
> הכפתור. הפריסה היא הצעד הבא.

הצעד הזה הוא **הפריסה בלבד** — אין שינוי קוד, אין מיגרציה ואין פריסת edge
function. `apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-`c2a0a36`.

## למה זה צעד בפני עצמו ולא זנב של הקודם

הפעימה הקודמת נמדדה מול `http://127.0.0.1:8137/admin.html` — שרת סטטי מקומי
שמגיש את הקובץ מהריפו, מול ה-edge והמסד **החיים**. זו מדידה נכונה של הקוד ולא
של המוצר: כל מה שבין הדפדפן לקובץ — ה-rewrite ב-`vercel.dist.json`, ההעתקה
ב-`stage-portal.ps1`, ו-NetFree שמזריק 236 בייט לכל תשובה — לא נבדק בה כלל.

## הבדיקה שרצה **לפני** הפריסה, ולא הונחה

`portal/dist` נבנה ב-13/08 ויושב על הדיסק מאז. פריסה מ-`dist` ישן הייתה מחזירה
את **כל הפורטל** אחורה בלי ששום דבר ידווח שגיאה. הבדיקה היא השוואת שמות הנכסים
המגובבים בין `dist/index.html` לבין הייצור:

| | נכסים |
|---|---|
| `portal/dist/index.html` | `index-C2AHwa-z.js` · `index-B35ByRD-.css` |
| `more30.com/` (לפני) | `index-C2AHwa-z.js` · `index-B35ByRD-.css` |

זהים — כלומר הפריסה אינה מחזירה את הפורטל אחורה. `dist/bkalot-studio/admin.html`
היה 32,594 בייט (הגרסה הישנה), והמקור 40,850.

## הפריסה

```
pwsh scripts/stage-portal.ps1        # מעתיק vercel.dist.json, .vercel/project.json, ו-37
cd portal/dist
vercel deploy --prod --yes --scope l023131500-ops-projects
```

`dpl_7PfSdPmJbhde1488gmMAovchVFjk` · `READY` · `Aliased https://more30.com`.

**מ-`portal/dist` ובלי `--prebuilt`** — שתי מלכודות ידועות: בלי
`.vercel/project.json` בתוך `dist` ה-CLI מסיק את שם הפרויקט משם התיקייה ויוצר
פרויקט חדש בשם "dist" (הפריסה מצליחה ו-more30.com לא מתעדכן), ו-`--prebuilt`
מפיל את `vercel.json` — כלומר את מפת ה-rewrites של כל המערכות.

## נמדד אחרי הפריסה — HTTP

| כתובת | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 200 · 32,830 | **200 · 41,086** |
| `/bkalot-studio/admin/` | 200 | **200 · 41,086** — זהה |
| `/bkalot-studio` (הטופס) | 200 · 21,272 | 200 · 21,272 — ללא רגרסיה |
| `more30.com/` | 200 · 3,517 | 200 · 3,517, אותם שני נכסים |

41,086 − 40,850 = **236** — בדיוק דלתת NetFree הידועה, ולא מספר אחר. ובגוף
שחזר: `showDocument` ו-`setAttribute("sandbox", "")` שניהם נוכחים.

## נמדד אחרי הפריסה — דפדפן אמיתי על הכתובת החיה

Playwright, 1280×900, `https://more30.com/bkalot-studio/admin`. נתוני בדיקה
נוצרו דרך נתיב הקליטה האמיתי (`bkalot_clone_intake`) ולא בהזרקה למסד: פנייה 23,
`situation=disability`, 59 זכויות, `queued:false`.

| # | מה נמדד | תוצאה |
|---|---|---|
| 1 | מסך הכניסה | **הקופי החדש** — «מציג פניות… ומפיק מהן את מכתב התשובה. אין ממנו שום שליחה» |
| 2 | «הצג סיסמה» | קיים (#226 עדיין סגור אחרי הפריסה) |
| 3 | כניסה | «מחובר: בודק פריסה» — עברית נקייה, בלי כפל-קידוד |
| 4 | הרשימה | «פנייה אחת» · #23 · טיפול מלא · **59 זכויות** |
| 5 | הפנייה | 59 שורות, `B100 B111 B114 B124 B144 B195` — אותו מספר ואותו סדר |
| 6 | הכפתור | **«הפק מסמך»** — נמצא בייצור, וזה מה שהצעד קנה |
| 7 | לחיצה | `render` הצליח — `document_id=14` |
| 8 | שורת המדידה | `מסמך #14 הופק · תבנית rights_treatment_reply · 3439 תווי טקסט · 59 זכויות (כל הרשימה) · לא נשלח — מצב טסט` |
| 9 | אחרי ההפקה | הכפתור → **«הפק מחדש»**, «מסמכים (1)» |
| 10 | «הצג» | «בקלות — 59 זכויות שאותרו עבורך (פנייה 23)» |
| 11 | שורת המטא | `מסמך #14 · email · 3439 תווי טקסט · 5247 תווי HTML · לא נשלח — מצב טסט` |
| 12 | אורך הטקסט שהגיע ל-DOM | **3439 — שווה בדיוק למה ש-`render` דיווח בשורה 8** |
| 13 | ה-iframe | `sandbox=""` (ריק), גובה 420px; `contentDocument` **null** — כלומר ה-sandbox אוכף באמת ולא רק כתוב במאפיין |
| 14 | תוכן המסמך | עברית תקינה, RTL, שמות אמיתיים מ-`rights.catalog` |
| 15 | שגיאות קונסולה | **0** לאורך כל הזרימה |
| 16 | יציאה | חזרה למסך הכניסה |

צילומים: `live-render-note.png` (הכפתור ושורת המדידה על הכתובת החיה),
`live-document-panel.png` (המסמך המעוצב ב-iframe מעל גרסת הטקסט).

ההבדל מהמדידה המקומית — 59 זכויות ו-3439 תווים במקום 42 ו-2485 — אינו שינוי
התנהגות: `situation` שונה (`disability` במקום `family_1_3`), והמסמך נגזר ממנו.
מה שחוזר על עצמו הוא היחס: **אורך הטקסט בשורת ההפקה = אורך הטקסט שהגיע ל-DOM**,
בשתי המדידות.

## מצב טסט — נמדד, לא הוצהר

`outbound_queue=8`, `delivery_log=3`, `catalog=888` — זהים לפני ואחרי.
`documents.queue_id` של מסמך 14 נשאר **null**, ו-`queued=false` הופיע בשני
מקורות בלתי-תלויים (שורת ההפקה ושורת המטא). אין מייל, אין הודעה ואין מסמך
שיצא החוצה.

## ניקוי

הבדיקה התגלגלה אחורה במלואה, ובסדר הנכון — הפנייה לפני איש הקשר, כי
`cases_contact_id_fkey` הוא `ON DELETE SET NULL` והסדר ההפוך היה מייתם אותה
בשקט: מסמך 14, 59 `case_rights`, פנייה 23, סשן אחד, משתמש ניהול 6, איש קשר 29.

הבסיס חזר בדיוק למה שהיה לפני הצעד: `cases=0`, `case_rights=0`, `documents=0`,
`admin_users=1`, `admin_sessions=5`, `contacts=4`, `outbound_queue=8`,
`delivery_log=3`, `catalog=888`.

## מה שנשאר פתוח ולא נבלע

- **מסלול השליחה** — `outbound_queue`, `delivery_log` וערוץ. הלבנה הבאה של §5ב.
  `queue_id` נשאר null בכוונה ואין ולו נתיב יוצא אחד.
- **#232** — למצב אין שם עברי במסד; התוויות יושבות במילון הלקוח.
- **בחירת תבנית** — `render` מקבל `template_key` והמסך אינו שולח אותו.
- `public_visible` ו-`show_in_showcase` של #37 נשארים `false`.

🚫 שום נגיעה ב-08 / 09 / bkalut-app / bkalot-admin / zr_* / NEDARIM3873, ובסכמות
csj / csj_src / igud. `git status` על `apps/08-bkalut-app` ו-`apps/09-bkalot-admin`
ריק.
