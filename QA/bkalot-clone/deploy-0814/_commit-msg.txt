שכפול בקלות שכבה 3: הכפתור נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הפעימה הקודמת (c2a0a36) בנתה את «הפק מסמך» ומדדה אותו בדפדפן אמיתי — מול שרת
סטטי מקומי שמגיש את הקובץ מהריפו. היא נסגרה עם קו פתוח שנוסח בה במפורש:
«הקובץ טרם נפרס». הצעד הזה הוא הפריסה בלבד — אין שינוי קוד, אין מיגרציה ואין
פריסת edge function, ו-admin.html זהה לזה שנכנס ל-c2a0a36.

מדידה מול 127.0.0.1 היא מדידה של הקוד ולא של המוצר: ה-rewrite ב-vercel.dist.json,
ההעתקה ב-stage-portal.ps1 ו-236 הבייטים של NetFree לא נבדקו בה כלל.

לפני הפריסה: portal/dist נבנה ב-13/08 ויושב על הדיסק מאז, ופריסה מ-dist ישן
מחזירה את כל הפורטל אחורה בלי ששום דבר ידווח שגיאה. שמות הנכסים המגובבים
הושוו לייצור — index-C2AHwa-z.js ו-index-B35ByRD-.css, זהים.

אחרי הפריסה (dpl_7PfSdPmJbhde1488gmMAovchVFjk):
- /bkalot-studio/admin  200 · 32,830 -> 41,086  (40,850 ועוד 236 של NetFree)
- /bkalot-studio        200 · 21,272 ללא שינוי
- more30.com/           200 · 3,517 ואותם שני נכסים — בלי רגרסיה

ובדפדפן אמיתי על הכתובת החיה, על פנייה שנוצרה דרך נתיב הקליטה ולא בהזרקה:
הקופי החדש במסך הכניסה, «הפק מסמך» נמצא בייצור, render החזיר document_id=14,
3439 תווי טקסט · 59 זכויות · לא נשלח, ואורך הטקסט שהגיע ל-DOM דרך document
שווה בדיוק לאותו מספר. ה-iframe הוא sandbox ריק ו-contentDocument שלו null —
כלומר ה-sandbox נאכף ולא רק כתוב במאפיין. אפס שגיאות קונסולה.

מצב טסט נמדד ולא הוצהר: outbound_queue=8, delivery_log=3, catalog=888 לפני
ואחרי, ו-queue_id של מסמך 14 נשאר null. נתוני הבדיקה נמחקו בסדר הנכון — הפנייה
לפני איש הקשר, כי ה-FK הוא ON DELETE SET NULL — והבסיס חזר בדיוק.

מסלול השליחה (outbound_queue וערוץ) הוא הלבנה הבאה. QA/bkalot-clone/deploy-0814/
