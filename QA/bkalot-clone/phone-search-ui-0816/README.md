# שורת «חופש לפי» ברשימת העבודה — מדידה (16/08)

הקו נכתב ב-bed3cdb מילה במילה: «`apps/37-bkalot-clone/admin.html` שולח את המונח
כפי שהוקלד ואינו אומר דבר על `q_phone` — התיקון עובד מהשרת כבר עכשיו, אבל המנהל
אינו רואה למה נמצאה שורה שהטלפון בה נראה אחרת ממה שהקליד». נלקח ולא הורחב: קובץ
אחד, `apps/37-bkalot-clone/admin.html`. אין מיגרציה, אין פריסת edge function ואין
פריסת פורטל — `bkalot-clone-admin` נשארת v8, `bkalot-clone-intake` v1, ו-`supabase/`
לא נגעה.

## מה שנבנה

`<p class="qnote" id="q-phone">` מתחת למונה, נצבע ב-`q_phone` שחוזר מ-0093, ומוסתר
כשהמפתח `null`.

חמש הכרעות:

1. **המספר מוצג ולא מתואר.** «נורמלנו את המונח» אינו אומר למנהל לפי מה חופש בפועל,
   והוא זה שצריך להחליט אם זה הטלפון שהתכוון אליו.
2. **השורה נשארת גם כשהרשימה ריקה** — ושם היא שווה יותר מאשר מעל שורות. «אין פנייה
   כזו» ו«הקלדתי עם מקפים» נראים על המסך אותו הדבר בדיוק, והמספר הזה הוא הדבר היחיד
   שמכריע ביניהם. זה גם המצב שבו המנהל פותח רישום כפול לאדם שכבר פנה.
3. **אינה `.err` ואינה נצבעת באדום.** לא קרתה תקלה; החיפוש עשה בדיוק את מה שהתבקש.
4. **המספר בסוף המשפט ובלי סימן פיסוק אחריו.** הוא רץ משמאל לימין בתוך שורה עברית
   (`.code`, `direction:ltr`), ונקודה אחריו נוחתת בצד שאינו הצד שבו המשפט נגמר.
5. **נסגרת יחד עם השורות במסלול השגיאה.** היא מתארת חיפוש שתוצאתו אינה על המסך עוד,
   ומעל שורת שגיאה הייתה נקראת כאילו אותו חיפוש כן רץ והחזיר את מה שרואים.

## מה שנמדד

הקליטה קיבלה את הטלפון **עם מקפים** — `054-812-3491` — ושמרה `0548123491` (contact
279, case 272). כלומר הצורה שהמנהל מקליד היא הצורה שהמערכת עצמה מקבלת, והפער היחיד
הוא בקריאה.

שלושה מצבים, 1280x900, התחברות אמיתית דרך המסך בחשבון 99, ושלוש לחיצות «הצג»
אמיתיות — אין כאן ולו קריאת `cases` אחת מסקריפט ואין הזרקה כלשהי:

| מצב | מונח | מונה | שורות | `q-phone` |
|---|---|---|---|---|
| `01-dashed-note.png` | `054-812-3491` | פנייה אחת | 1 | גלויה, `.code` = `0548123491` |
| `02-plain-no-note.png` | `0548123491` | פנייה אחת | 1 | מוסתרת, טקסט ריק |
| `03-empty-with-note.png` | `052-000-0000` | אין פניות | 0 | גלויה, `.code` = `0520000000` |

`01` מראה את שלושת הדברים בפריים אחד: התיבה עם המקפים, השורה עם `טלפון 0548123491`,
והמשפט שמסביר את ההפרש ביניהם. `02` הוא הבקרה — אותה פנייה בדיוק נמצאת, והשורה
אינה מופיעה: היא נצבעת מהנרמול ולא מהחיפוש. `03` הוא המצב של הכרעה (2) — הרשימה
ריקה, `empty` גלויה, ומתחת למונה כתוב לפי איזה מספר נשאלה השאלה.

המיקומים נמדדו ולא הונחו — תקלת `screenshot-evidence-below-the-fold`: השורה ב-342
והתוכן שמתחתיה ב-373, בתוך חלון של 900. שלושת הצילומים בשלושה MD5 שונים
(`315FDFB1`, `744AC5B8`, `189FC1F1`). קונסולה: שגיאה אחת ויחידה — `favicon.ico` 404
מהשרת הסטטי המקומי, כמו בכל מדידה מקומית קודמת.

`list-error` נשאר מוסתר בשלושת המצבים — הכרעה (5) לא נבדקה כאן מצידה השני (אין דרך
להפיל את `cases` מהמסך בלי הזרקה), והיא מוצהרת ולא נבלעת.

## מצב טסט

נמדד ולא הוצהר: `queued=false` בקליטה, אפס מסמכים, `outbound_queue` נשאר 8 ואפס שורות
`sent_at`, `delivery_log` נשאר 3, אפס פניות ב-`sent`, אפס קריאות יוצאות. הקובץ
שהשתנה הוא HTML ואין בו ולו קריאת כתיבה אחת חדשה — `cases` היא קריאה בלבד.

## התגלגלות אחורה

בפקודות נפרדות לפי טבלה — תקלת `cte-delete-sees-prestatement-snapshot`; ואיש הקשר
לפי `id` ולא לפי טלפון מנורמל — תקלת `ac6e523`. נמחקו: `case_status_log` 0,
`case_rights` 42, `cases` 1, `contacts` 1, `admin_sessions` של 99, `admin_users` 1.
אחרי: `cases` 0, `case_status_log` 0, `case_rights` 0, `documents` 0, `admin_users` 1,
`admin_sessions` 5, `templates` 2, `contacts` 4, `outbound_queue` 8, `delivery_log` 3,
`rights_catalog` 888 — בסיס בדיוק.

## פתוח

**הפריסה.** מה שנמדד כאן הוא הקוד שבמקור; בייצור השורה עדיין אינה קיימת, והמנהל
שמחפש עם מקפים מקבל שורה בלי הסבר. זו הלבנה הבאה — אותו פיצול כמו
`aff6455 → 9c96135`, `b1fcca7 → 81e3307` ו-`53a3e5a → c806151`.

**קו שנרשם ולא נלקח (ממשיך מ-bed3cdb):** `v_q` נכנס ל-`ilike` בלי escape, ולכן `%`
ו-`_` במונח החיפוש הם תווי הצבה ולא תווים.

מה שלא נבנה: אין אינדקס על `ct.phone`; אין נרמול בכיוון ההפוך; אין חיפוש בטקסט
הנימוקים; אין סינון ואין מיון לפי מספר מעברים; אין `status_last_change_at`; אין שמות
ברשימה; אין חובת נימוק על `rejected`; אין עריכה ואין מחיקה ביומן.

קווים שממשיכים ולא נגעו: טעינה מחדש של הכתובת בייצור מנתקת את הסשן (הכרעה מוצהרת,
`admin.html`); הקליטה מחברת זכויות ל-`treatment` ואפס ל-`info`/`reminder` על אותו
`situation`. `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה
ב-HTTP ונקראת ב-SQL בארגומנט `jsonb` יחיד; `pdf`/`audio` → `channel_unsupported`.
`public_visible` ו-`show_in_showcase` של #37 נשארים `false`.
