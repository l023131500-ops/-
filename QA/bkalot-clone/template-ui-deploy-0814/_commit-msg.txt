שכפול בקלות שכבה 3: בורר המכתב נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (978ab68) הותירה נוסח בה עצמה: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין מגיש את הגרסה בלי הבורר, והמדידה כאן רצה מול
שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר (ה-rewrite, ההעתקה ל-dist ו-236
הבייטים של NetFree לא נבדקו בה), וזו הפעימה הבאה».

נלקח כפי שנוסח ולא הורחב: אין שינוי קוד, אין מיגרציה ואין פריסת edge function.
apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-978ab68 (git diff ריק)
ו-bkalot-clone-admin נשארה v6. אותה חלוקה כמו c2a0a36 → 205c940, 37235a3 →
e95a0ef ו-2a067be → 188bd04.

הבדיקה שרצה לפני הפריסה ולא הונחה: portal/dist יושב על הדיסק, ופריסה מ-dist ישן
מחזירה את כל הפורטל אחורה בלי ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו
לייצור — index-C2AHwa-z.js ו-index-B35ByRD-.css, זהים בשני הצדדים.
dist/bkalot-studio/admin.html היה 64,708 והמקור 70,064; אחרי ה-staging שניהם
70,064 עם אותו MD5.

נמדד ב-HTTP לפני ואחרי: /bkalot-studio/admin היה 64,944 בתים וחמשת המסומנים
החדשים כולם false, ועלה ל-70,300 — הדלתא מול המקור היא 236 בדיוק, דלתת NetFree
הידועה ולא מספר אחר; /bkalot-studio/admin/ מחזיר את אותם בתים; הטופס נשאר 21,272
ו-more30.com/ נשאר 3,517 עם אותם שני נכסים — בלי רגרסיה. שנים-עשר המסומנים חזרו
בגוף, כולם true: החמישה החדשים («ברירת מחדל לפי סוג הפנייה», tmpl-sel, «מנוטרלת,
אינה מפיקה», «ערוץ התבנית», «נקבע לפי סוג הפנייה») והששה של הפעימות הקודמות
(«שינוי סטטוס», «עבד עכשיו», <th>תור</th>, «כתובת המכתב», «עובד בבדיקה — לא
נשלח», «הכנס לתור») — כדי שהמדידה תראה גם רגרסיה ולא רק את התוספת.

template_key נרשם ונפסל כמסומן: הוא היה true גם לפני הפריסה, כי טבלת ERRORS נושאת
את הנוסחים של 0063 מאז. נשאר בפלט כדי שלא ייקרא ככזה בדיעבד.

שפיות קידוד נמדדה: 546 → 614 אל״פים אמיתיים, אפס תווי החלפה ואפס סימני כפל-קידוד.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ .ps1 בלי BOM נקרא כ-cp1255
והעברית שבתבנית הייתה נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

נמדד בדפדפן אמיתי 1280x900 על הכתובת החיה, על פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (bkalot-clone-intake, מפתח anon מהטופס) ולא בהזרקה למסד: #80
treatment עם 59 זכויות ו-#81 info, שני טלפונים שונים בכוונה. «הצג סיסמה» קיים
(#226 נשאר סגור). הבורר קיים במסך החי עם שלוש האפשרויות.

המדידה שקונה את הפעימה: #80 היא treatment, כלומר ברירת המחדל שלה היא
rights_treatment_reply. נבחר general_inquiry_reply והופק — «מסמך #70 הופק · תבנית
general_inquiry_reply · 222 תווי טקסט». 222 ולא 3,505: מכתב אחר ולא תווית אחרת,
וזהו ערך שברירת המחדל אינה יכולה להפיק על הפנייה הזו בשום מצב. הבחירה עברה
מהדפדפן החי, דרך ה-rewrite, אל ה-edge.

הבקרה, ובלעדיה הפעימה הייתה מוכיחה רק ש-<select> נפרס: אותה פנייה, חזרה לערך
הריק, הפקה מחדש — «מסמך #70 הופק · תבנית rights_treatment_reply · 3505 תווי
טקסט». אותו מזהה מסמך (upsert על case_id,kind), והכלל של render לא זז ולא שוכפל.

הכרעה (5) נמדדה בייצור משני הכיוונים: אחרי ההפקה הבורר נשאר על
general_inquiry_reply — keepTmpl שורד את הרענון שההפקה גרמה לו; ופתיחת #81
מהרשימה התחילה בערך הריק — הבחירה אינה דולפת בין פניות. הכיתוב עוקב אחרי הסוג:
#80 על ריק «טיפול מלא» מקבל את המכתב עם רשימת הזכויות, אחרי בחירה מפורשת הנושא
והערוץ של התבנית, ו-#81 (מידע) «מקבל את המענה הכללי». אפס שגיאות ואפס אזהרות
בקונסולה (0 מתוך 0 הודעות).

מצב טסט נמדד ולא הוצהר: אפס שורות עם sent_at, אפס שורות mode=live, אפס פניות
ב-status=sent, outbound_queue נשאר 8 ו-delivery_log נשאר 3. הנתיב מפיק בלבד — לא
נכנס דבר לתור, לא נכתבה שורת יומן ולא יצאה ולו קריאה אחת. אין מייל, אין הודעה
ואין מסמך שיצא החוצה.

המקור לא נגע, ונמדד ולא הונח: 8 שורות app_key=bkalot נשארו 8, טביעת אצבע
da28ec6315535ec0bfbebad4d57752a7 (id:status:attempts) זהה לפני ואחרי.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (מסמך אחד, 59 case_rights, 2 פניות, 2
אנשי קשר, סשן אחד ומשתמש ניהול אחד). אחרי: cases=0, case_rights=0, documents=0,
contacts=4, admin_users=1, admin_sessions=5, templates=2 (שתיהן enabled),
catalog=888, outbound_queue=8, delivery_log=3 — זהה בדיוק לבסיס.

אין נגיעה בשום נתיב מוגן: git status על apps/08-bkalut-app, apps/09-bkalot-admin
ו-supabase/functions ריק, ואין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873
ולא בסכמות csj/csj_src/igud.

מה שנשאר פתוח ולא נבלע: template_disabled נשאר בלתי-נגיש מהמסך במכוון לפי הכרעה
(2), ושתי התבניות היו enabled לאורך המדידה — כלומר המסומן «מנוטרלת, אינה מפיקה»
מוכיח כאן שהקוד שמייצר אותה נפרס, לא שהוא רץ; template_unknown אינו נגיש כלל כי
הבורר בונה את הערכים מהרשימה שהשרת החזיר; documents אינה שומרת מאיזו תבנית הופק
המסמך, ומסמך #70 החזיק כאן את שני הנוסחים בזה אחר זה ו-upsert דרס; «מי שינה
ומתי» אינו נכתב לשום מקום; sent בלתי-ניתן-להשגה בכוונה; bkalot_clone_admin_create
עדיין אינה חשופה ב-HTTP (משתמש הבדיקה נוצר ב-SQL ישיר); pdf/audio →
channel_unsupported. public_visible ו-show_in_showcase של #37 נשארים false.
