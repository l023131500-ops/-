# בורר המכתב נמדד מדפדפן ולא היה בייצור — הפריסה (§5ב)

הקו הפתוח שהפעימה הקודמת (978ab68) הותירה נוסח בה עצמה, גם בהודעת הקומיט וגם
ב-`QA/bkalot-clone/template-ui-0814/README.md`: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין מגיש את הגרסה בלי הבורר, והמדידה כאן רצה
מול שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר; ה-rewrite ב-vercel.dist.json,
ההעתקה ב-stage-portal.ps1 ו-236 הבייטים של NetFree לא נבדקו בה כלל. זו הפעימה
הבאה, כמו c2a0a36 → 205c940».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-978ab68 (נמדד: `git diff`
מול 978ab68 ריק), ו-`bkalot-clone-admin` נשארה v6. אותה חלוקה כמו
c2a0a36 → 205c940, 18006b1 → 55efb75, c3338c5 → 3750c2e, 37235a3 → e95a0ef
ו-2a067be → 188bd04.

## למה זו פעימה בפני עצמה

המדידה הקודמת רצה מול שרת סטטי מקומי שמגיש את הקובץ מהריפו מול ה-edge והמסד
החיים. שלושה דברים לא נבדקו בה כלל: ה-rewrite ב-`vercel.dist.json`, ההעתקה
ב-`stage-portal.ps1`, ו-236 הבייטים ש-NetFree מזריק לכל תשובה.

## הבדיקה שרצה לפני הפריסה ולא הונחה

`portal/dist` יושב על הדיסק, ופריסה מ-dist ישן מחזירה את **כל** הפורטל אחורה בלי
ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו לייצור — `index-C2AHwa-z.js`
ו-`index-B35ByRD-.css`, זהים בשני הצדדים. `dist/bkalot-studio/admin.html` היה
64,708 (הקובץ של 188bd04) והמקור 70,064; אחרי ה-staging שניהם 70,064 עם אותו
MD5 בדיוק (`3E943EE5…`).

## מה נמדד ב-HTTP, לפני ואחרי

`/bkalot-studio/admin` היה 64,944 בתים וחמשת המסומנים החדשים כולם false, ועלה
ל-70,300 — הדלתא מול המקור היא **236 בדיוק**, דלתת NetFree הידועה ולא מספר אחר.
`/bkalot-studio/admin/` מחזיר את אותם בתים; הטופס נשאר 21,272 ו-more30.com/ נשאר
3,517 עם אותם שני נכסים — בלי רגרסיה.

שנים-עשר המסומנים חזרו בגוף, כולם true: החמישה החדשים («ברירת מחדל לפי סוג
הפנייה», `tmpl-sel`, «מנוטרלת, אינה מפיקה», «ערוץ התבנית», «נקבע לפי סוג הפנייה»)
והששה של הפעימות הקודמות («שינוי סטטוס», «עבד עכשיו», `<th>תור</th>`, «כתובת
המכתב», «עובד בבדיקה — לא נשלח», «הכנס לתור») — כדי שהמדידה תראה גם רגרסיה ולא רק
את התוספת.

**`template_key` נרשם ונפסל כמסומן.** הוא היה true גם *לפני* הפריסה — טבלת
ERRORS נושאת את הנוסחים של 0063 מאז, ולכן הוא אינו מעיד על הפעימה הזו. נשאר בפלט
כדי שלא ייקרא ככזה בדיעבד.

שפיות קידוד נמדדה: 546 → 614 אל״פים אמיתיים, אפס תווי החלפה ואפס סימני כפל-קידוד.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית הייתה נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## מה נמדד בדפדפן, על הכתובת החיה

בדפדפן אמיתי 1280x900 על `https://more30.com/bkalot-studio/admin`, על פניות
שנוצרו דרך נתיב הקליטה הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon
מהטופס) ולא בהזרקה למסד: #80 treatment עם 59 זכויות ו-#81 info, שני טלפונים
שונים בכוונה. «הצג סיסמה» קיים (#226 נשאר סגור).

הבורר קיים במסך החי עם שלוש האפשרויות, ושתיהן enabled.

**המדידה שקונה את הפעימה.** #80 היא `treatment`, כלומר ברירת המחדל שלה היא
`rights_treatment_reply`. נבחר `general_inquiry_reply` והופק — «מסמך #70 הופק ·
תבנית general_inquiry_reply · **222 תווי טקסט**». 222 ולא 3,505: מכתב אחר ולא
תווית אחרת, וזהו ערך שברירת המחדל אינה יכולה להפיק על הפנייה הזו בשום מצב.
הבחירה עברה מהדפדפן החי, דרך ה-rewrite, אל ה-edge.

**הבקרה, ובלעדיה הפעימה הייתה מוכיחה רק ש-`<select>` נפרס.** אותה פנייה, חזרה
לערך הריק, הפקה מחדש — «מסמך #70 הופק · תבנית rights_treatment_reply · **3505
תווי טקסט**». אותו מזהה מסמך (upsert על `case_id,kind`), והכלל של render לא זז
ולא שוכפל.

**הכרעה (5) נמדדה בייצור משני הכיוונים.** אחרי ההפקה הבורר נשאר על
`general_inquiry_reply` — כלומר `keepTmpl` שורד את הרענון שההפקה גרמה לו; ופתיחת
#81 מהרשימה התחילה בערך הריק — כלומר הבחירה אינה דולפת בין פניות.

**הכיתוב עוקב אחרי הסוג ולא אחרי הפנייה שנפתחה אחרונה.** #80 (טיפול מלא) על ערך
ריק: «נקבע לפי סוג הפנייה — «טיפול מלא» מקבל את המכתב עם רשימת הזכויות.»; אחרי
בחירה מפורשת: «נושא: בקלות — קיבלנו את פנייתך (פנייה {{caseId}}) · ערוץ התבנית:
email»; #81 (מידע): «נקבע לפי סוג הפנייה — «מידע» מקבל את המענה הכללי.»

אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה — 0 מתוך 0 הודעות.

## מצב טסט — נמדד ולא הוצהר

אפס שורות עם `sent_at`, אפס שורות `mode=live`, אפס פניות ב-`status=sent`,
`outbound_queue` נשאר 8 ו-`delivery_log` נשאר 3. הנתיב הזה מפיק בלבד: לא נכנס
דבר לתור, לא נכתבה שורת יומן ולא יצאה ולו קריאה אחת. אין מייל, אין הודעה ואין
מסמך שיצא החוצה.

## המקור לא נגע

8 שורות `app_key=bkalot` נשארו 8, טביעת אצבע `da28ec6315535ec0bfbebad4d57752a7`
(id:status:attempts) זהה לפני ואחרי. `git status` על `apps/08-bkalut-app`,
`apps/09-bkalot-admin` ו-`supabase/functions` ריק, ואין נגיעה ב-`bkalut-app`/
`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות `csj`/`csj_src`/`igud`.

## גלגול אחורה

בסדר הנכון (מסמכים לפני שורות התור, פניות לפני אנשי הקשר —
`cases_contact_id_fkey` הוא ON DELETE SET NULL): נמחקו מסמך אחד, 59
`case_rights`, 2 פניות, 2 אנשי קשר, סשן אחד ומשתמש ניהול אחד. אחרי: cases=0,
case_rights=0, documents=0, contacts=4, admin_users=1, admin_sessions=5,
templates=2 (שתיהן enabled), catalog=888, outbound_queue=8, delivery_log=3,
src_fp זהה — זהה בדיוק לבסיס שנמדד לפני.

## מה שנשאר פתוח ולא נבלע

- **`template_disabled` נשאר בלתי-נגיש מהמסך** — במכוון, לפי הכרעה (2) של
  978ab68. נמדד ב-RPC ב-0073. `template_unknown` אינו נגיש כלל מהמסך, כי הבורר
  בונה את הערכים מהרשימה שהשרת החזיר. **הערה על הפעימה הזו:** שתי התבניות היו
  enabled לאורך כל המדידה, ולכן האפשרות המנוטרלת לא נראתה כאן בייצור — היא נמדדה
  בדפדפן ב-978ab68 מול שרת מקומי, והמסומן «מנוטרלת, אינה מפיקה» מוכיח כאן שהקוד
  שמייצר אותה נפרס, לא שהוא רץ.
- **`documents` אינה שומרת מאיזו תבנית הופק המסמך**, וזה חד יותר מהיום: מסמך #70
  החזיק כאן את שני הנוסחים בזה אחר זה, ו-upsert על `(case_id,kind)` דרס בלי
  שנשאר רישום שהיה בו נוסח קודם.
- «מי שינה ומתי» אינו נכתב לשום מקום; `sent` בלתי-ניתן-להשגה בכוונה;
  `bkalot_clone_admin_create` עדיין אינה חשופה ב-HTTP (משתמש הניהול לבדיקה נוצר
  ב-SQL ישיר); pdf/audio → `channel_unsupported`. `public_visible`
  ו-`show_in_showcase` של #37 נשארים false.

## הקבצים כאן

| קובץ | מה |
| --- | --- |
| `probe-http.mjs` | מדידת ה-HTTP (בתים, מסומנים, קידוד) |
| `probe-http-pre.txt` | הפלט לפני הפריסה — חמשת המסומנים false |
| `probe-http-post.txt` | הפלט אחרי — שנים-עשר true, דלתא 236 |
| `seed.mjs`, `seed-out.txt` | יצירת שתי הפניות דרך נתיב הקליטה מעל HTTP |
| `_measurements.json` | כל המספרים והנוסחים כפי שנקראו |
| `01-live-selector-default.png` | הבורר בייצור, פנייה של טיפול, ערך ברירת מחדל |
| `02-live-picked-general.png` | אחרי בחירת «מענה לפנייה כללית» — הנושא והערוץ |
| `03-live-after-render-general.png` | 222 תווים על פנייה שברירת המחדל שלה 3,505 |
| `04-live-control-default.png` | הבקרה — אותו מסמך, 3,505 תווים |
| `05-live-info-case-hint.png` | #81 (מידע) — הכיתוב עוקב אחרי הסוג, הבחירה לא דלפה |
