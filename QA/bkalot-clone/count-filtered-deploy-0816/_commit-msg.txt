שכפול בקלות שכבה 3: המונה שאומר «אין פניות» על תור מלא נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נלקח כפי שנוסח ב-68785e9 (heartbeat 665), מילה במילה: «פתוח: הפריסה. מה
שנמדד כאן הוא הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) המונה עדיין
אומר «אין פניות» על תור שיש בו פנייה. זו הלבנה הבאה». נלקח ולא הורחב: פריסה
בלבד — אין שינוי קוד, אין מיגרציה, אין פריסת edge function. bkalot-clone-admin
נשארת v9, bkalot-clone-intake v1, schema_migrations נשארה 20260816131352,
supabase/ ו-apps/37 לא נגעו. מה שנכנס ל-commit הוא ראיות בלבד.

הריצה התחילה מבסיס נקי ולא מחצי עבודה — נמדד לפני הכל ולא הונח, תקלת
heartbeat-lags-a-crashed-run: cases 0, case_status_log 0, case_rights 0,
documents 0, admin_users 1, admin_sessions 5, templates 2, contacts 4,
outbound_queue 8, delivery_log 3, rights_catalog 888. ו-HEAD היה 68785e9 וגם
origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 164,141 בייט ו-MD5 A135478A במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
160,897 (3CEDD39D) — מה ש-8851cb5 פרס — כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (תקלת prebuilt-deploy-drops-vercel-json),
ו-vercel.json ו-.vercel/project.json נמדדו כנוכחים לפני הפקודה.
dpl_4osCGxo4Xz4S2YXCFGW8AcnPtWtk, READY, production, aliased ל-more30.com.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (candidates.mjs: src מול dist),
בלי שהכתובת החיה נפגשה ולו פעם אחת, ו-probe.mjs נכתב אחרי ש-classify.mjs נחתם —
תקלת 0087. ⚠️ ומועמד אחד עבר קבוצה בגלל המדידה הזו ולא נשאר איפה שהונח: «אין
פניות שתואמות את הסינון» — המשפט החדש עצמו — נראה NEW מובהק וחזר src 5 / dist 3,
מפני שהצירוף כבר ישב שלוש פעמים ב-dist בתוך הערות שדנו במצב לפני שנסגר. הוא
סווג DIFF ולא NEW. מסומן שנראה חדש ואינו חדש הוא בדיוק מה שהשלב הזה קיים כדי
לתפוס, ו-NEW שבנוי עליו היה עובר גם בלי פריסה.

⚠️ הפעימה הזו אינה מוסיפה טקסט בלבד אלא מחליפה שורת קוד, ולכן יש כאן קבוצת
REMOVED: שתי השורות שציירו את המונה יוצאות. NEW לבדה מוכיחה שהקוד החדש הגיע —
ולא שהישן הסתלק, ועמוד שמגיש את שניהם (קאש שמיזג, שני עותקים של הסקריפט) עובר
NEW ונופל ב-REMOVED. «$("count").textContent = total === 0» ו-«(total ===
cases.length ? many(total)» החזירו 1 בייצור לפני ו-0 אחרי, בשתי הכתובות.

⚠️ הבקרה האמיתית של שרשרת הפריסה היא dist_equals_live: לפני הפריסה כל 35
המסומנים החזירו מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת. כלומר מה
שהכתובת מגישה נמדד ולא הונח, ואילו נפרס קובץ אחר — הטענה הייתה נופלת שם ולא כאן.

אחד-עשר מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי
בשתי הכתובות (עם לוכסן ובלעדיו): «const filtered = !!(lastFilters.status»
ו-«const head = total === 0» — הקוד עצמו — ושלוש פסקאות ה-title («המספר הוא של
הפניות שעונות על הסינון ולא של התור כולו», «השרת מחזיר את המניין שאחרי הסינון»,
«המיון אינו מוציא שורות ואינו משפיע על המספר»), וארבע הכרעות ההערה. שנים-עשר
מסומני CONTROL לא זזו, ובהם «מספר פנייה או נימוק ביומן ההכרעות» — בדיוק מה
ש-8851cb5 פרסה בפעימה הקודמת — ו-id="count" ו-id="empty", האלמנטים שבתוכם כל
השינוי מתרחש: הם מוכיחים שהאלמנט לא נכפל ולא נעלם אלא שרק מה שנכתב לתוכו הוחלף.
עשרה מסומני DIFF החזירו בייצור בדיוק את ספירת המקור.

⚠️ dead_markers ריק ב-before ומחזיר את שני מסומני REMOVED ב-after — זה נכון ולא
תקלה: אחרי הפריסה src ו-dist הם אותו קובץ, ומסומן שיצא הוא 0 בשניהם. הטענה
שלהם נמדדה ב-before, שם dist היה 1.

live 161,133 בייט לפני ו-164,377 אחרי, בשתי הכתובות ובאותו MD5 — 236 בייט מעל
הקובץ שנפרס בשתי המדידות, כלומר הפרש קבוע של הזרקה ולא של תוכן. אפס תווי החלפה
ואפס «×» בשתיהן — תקלת deployed-html-double-encoded-cp1255.

ומעל הכול, המדידה שאינה על ספירת מחרוזות אלא על המסך: דפדפן אמיתי, מהכתובת החיה
עם cachebuster, התחברות דרך המסך (חשבון 122, נוצר ב-bkalot_clone_admin_create
ולא בהזרקת hash), והסינון נבחר ונלחץ «הצג» כמו מנהל. שלושה מצבים על אותה פנייה:
בלי סינון «פנייה אחת» ו-title null; סינון «חדשה» «פנייה אחת תואמת את הסינון»;
סינון «נשלחה» «אין פניות שתואמות את הסינון». ⚠️ המצב בלי סינון הוא הבקרה והוא
לא זז — מה שהשתנה הוא המסלול שבו יש סינון בלבד. בקרה שנייה: מספר השורות מה-DOM
1/1/0, כלומר המונה נקרא מאותה טעינה שציירה את השורות. ומצב רביעי, «back»:
nomatch → match מחזיר את המשפט של match, כלומר המצב מתהפך חזרה ואינו נדבק.
שני צילומים בשני MD5 (4287BF9D, 26391D3D) — תקלת screenshot-evidence-below-the-fold;
הגלילה אל הטופס ולא אל ראש הדף, מפני שהמונה יושב מתחת לתיבות הסינון. הקונסולה
נלכדה ולא נרשמה כחסרה: אפס הודעות ואפס תגובות שאינן 2xx/3xx.

הפנייה נזרעה דרך הכתובת החיה של bkalot-clone-intake מעל HTTP עם מפתח anon בדיוק
כמו מהטופס: #387 (contact 394), kind=info, queued=false.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, outbound_queue נשאר 8, אפס
שורות sent_at, delivery_log נשאר 3, אפס קריאות יוצאות. kind=info ולא treatment,
ולכן case_rights 0 לאורך כל הריצה.

התגלגל אחורה בפקודות נפרדות לפי טבלה — תקלת cte-delete-sees-prestatement-snapshot;
ואיש הקשר לפי id ולא לפי טלפון מנורמל. ⚠️ contacts יושבת ב-bkalot_auto ולא
ב-bkalot_clone. נמחקו: cases 1 (387), contacts 1 (394), admin_sessions של 122,
admin_users 1 (122); case_status_log החזירה 0 — לא נעשה ולו מעבר סטטוס אחד
בריצה. אחרי: cases 0, case_status_log 0, case_rights 0, documents 0,
admin_users 1, admin_sessions 5, templates 2, contacts 4, outbound_queue 8,
delivery_log 3, rights_catalog 888 — בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin ו-supabase,
נמדד מהשורש לפני ה-add; אין נגיעה ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.
ל-git add נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה ב-commit -F — תקלת
here-string-commit-fails-silently.

פתוח: הלבנה הבאה היא UI ולא פריסה. מרשימת «מה שלא נבנה» שנרשמה שוב ב-68785e9
ולא זזה: אין «מתוך N בסך הכול» — השרת מחזיר total אחד ומספר כזה היה מומצא; אין
הדגשת המילה בתוך הנימוק ואין ציטוט; אין חיפוש ב-cases.note; אין סימון מקביל על
שמות המכריעים; אין סינון «יש נימוק / אין»; אין ספירה ברמת השורה ברשימה; אין
אינדקס ואין חיפוש טקסט מלא; אין סינון ומיון לפי מספר מעברים; אין עריכה ומחיקה
ביומן; אין סינון «איש קשר נמחק / קיים»; אין שמירת מיקום הפנייה בעמוד החדש; אין
אינדקס על ct.phone; pager.hidden = total <= PAGE נשאר כפי שהוא. ⚠️ שורה 90
ב-0096 עדיין אומרת «מה שלא נמדד» ולא נערכה. קווים שממשיכים ולא נגעו: טעינה
מחדש של הכתובת מנתקת את הסשן (admin.html:312, הכרעה מתועדת ולא חוסר); sent
בלתי-ניתן-להשגה בכוונה; bkalot_clone_admin_create אינה חשופה ב-HTTP; pdf/audio →
channel_unsupported; public_visible ו-show_in_showcase של #37 נשארים false.
