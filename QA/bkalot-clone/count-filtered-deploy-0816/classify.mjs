// classify.mjs — הסיווג נחתם כאן, ונקבע כולו מ-candidates.json: השוואה של שני
// קבצים מקומיים (src מול dist), בלי שהכתובת החיה נפגשה ולו פעם אחת. probe.mjs
// נכתב אחרי הקובץ הזה. תיקון סיווג אחרי שרואים את התוצאה הוא תקלת 0087.
//
// ארבע קבוצות, וכל אחת מהן טענה נפרדת:
//
//   NEW     — 0 ב-dist, ≥1 ב-src. הבייטים שהפריסה אמורה להביא.
//   REMOVED — ≥1 ב-dist, 0 ב-src. הפעימה הזו אינה מוסיפה טקסט בלבד אלא מחליפה
//             שורת קוד: שתי השורות שציירו את המונה יוצאות. NEW לבדה מוכיחה
//             שהקוד החדש הגיע — ולא שהישן הסתלק, ועמוד שמגיש את שניהם (קאש
//             שמיזג, שני עותקים של הסקריפט) עובר NEW ונופל כאן.
//   DIFF    — ≥1 בשני הצדדים ובמספרים שונים. ראיה חלשה מ-NEW, ולכן בנפרד.
//   CONTROL — שווה ב-src וב-dist, וחייב להיות שווה גם בייצור. מסומן בקרה שזז
//             הוא הסימן שנפרס קובץ אחר ממה שנמדד.
//
// ⚠️ «אין פניות שתואמות את הסינון» אינו NEW אלא DIFF (src 5, dist 3): הצירוף
// כבר הופיע שלוש פעמים ב-dist בתוך הערות שדנו במצב הזה לפני שנסגר. מסומן שנראה
// חדש ואינו חדש הוא בדיוק מה שהשלב הזה קיים כדי לתפוס, ולכן הוא לא הועבר
// ל-NEW אחרי שנמדד.
//
// ⚠️ «מספר פנייה או נימוק ביומן ההכרעות» הוא 1/1 והוא הבקרה הצמודה ביותר על
// שרשרת הפריסה עצמה: זה בדיוק הטקסט ש-8851cb5 פרסה לייצור בפעימה הקודמת. אם
// הוא חוזר 0 מהכתובת החיה לפני הפריסה — מה שמוגש אינו מה שנפרס, וכל השאר
// חסר משמעות.
//
// ⚠️ «id="count"» ו-«id="empty"» הם 1/1 והם הבקרה על האלמנט עצמו: כל השינוי
// מתרחש בתוכו, והם מוכיחים שהוא לא נכפל ולא נעלם אלא שרק מה שנכתב לתוכו הוחלף.
export const MARKERS = {
  new: [
    "המונה מדבר על התור כולו בזמן שהוא סופר את הסינון",
    "המספר הוא של הפניות שעונות על הסינון ולא של התור כולו", // ה-title החדש
    "המיון אינו סינון ואינו נספר כאן",                        // הכרעה (1)
    "const filtered = !!(lastFilters.status",                 // התנאי עצמו
    "const head = total === 0",                               // השורה שהחליפה
    "השרת מחזיר את המניין שאחרי הסינון",                      // ה-title, הכרעה (2)
    "המיון אינו מוציא שורות ואינו משפיע על המספר",            // ה-title, סוף
    "היה מייחס למספר",
    "היה מספר מומצא",                                          // הכרעה (2)
    "ותיבת ה-empty אינה אזור סטטוס ואינה מוקראת כלל",         // הכרעה (3)
    "הוא מתאר את חיתוך העמוד",                                 // הכרעה (4)
  ],
  removed: [
    '$("count").textContent = total === 0',
    "(total === cases.length ? many(total)",
  ],
  diff: [
    "אין פניות שתואמות את הסינון", // 3 → 5
    'removeAttribute("title")',    // 7 → 8
    "את הסינון",                    // 5 → 10
    "אין פניות",                    // 5 → 9
    "מוצגות ",                      // 2 → 3
    "lastFilters",                 // 7 → 11
    'role="status"',               // 3 → 4
    "a795108",                     // 2 → 4
    "0081",                        // 3 → 4
    "מיון",                         // 7 → 10
  ],
  control: [
    "מספר פנייה או נימוק ביומן ההכרעות", // מה ש-8851cb5 פרסה — בקרת שרשרת
    'id="count"',                        // האלמנט שבתוכו כל השינוי
    'id="empty"',
    "פנייה אחת",
    "countHe",
    "bkalot_clone_admin_cases",
    "פנייה #",
    "matched_in_note",
    "איש הקשר נמחק",
    "רצף ההכרעות",
    "<th>למה</th>",
    "מי הכריע",
  ],
};

export function count(hay, needle) {
  let n = 0, i = 0;
  for (;;) {
    const j = hay.indexOf(needle, i);
    if (j < 0) return n;
    n++;
    i = j + needle.length;
  }
}
