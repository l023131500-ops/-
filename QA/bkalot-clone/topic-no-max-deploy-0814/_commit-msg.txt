שכפול בקלות שכבה 1: החסימה של מספר נושא גדול מהעמודה נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו הפתוח נכתב ב-67555e3 (heartbeat 599), מילה במילה: «המדידה כאן רצה מול שני
שרתים סטטיים מקומיים מול ה-edge והמסד החיים — מדידה של הקוד ולא של המוצר.
more30.com/bkalot-studio עדיין מגיש 31,164 בתים ועדיין שולח את «999999999999»
ברשת; ה-rewrite ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים
ש-NetFree מזריק לא נבדקו כאן כלל. הפריסה היא הפעימה הבאה». נלקח כפי שנוסח ולא
הורחב — פריסה בלבד: אין שינוי קוד, אין מיגרציה ואין פריסת edge function.
bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin נשארה v7.
apps/37-bkalot-clone/index.html זהה בדיוק לזה שנכנס ל-67555e3 (32,322 בתים,
MD5 E6E39BF0F0D5FFD03F8A0ADC95F81078, git status ריק).

REMOVED כאן הוא סמיכות ולא מחרוזת, ההפך מ-cd5340e: 67555e3 היא תוספת טהורה —
12 שורות נוספו ואפס נמחקו — ולכן אין מחרוזת שנעלמה, והמרחק בין החסימה הקודמת
לבין סוף preflight() נמדד ישירות: 2 שורות בייצור, 14 במתוקן. העוגן נלקח כמופע
האחרון ולא הראשון, מלכודת ac6e523.

שני מסומנים נפסלו לפני שנקבעו, כי היו מחזירים true על הגרסה שבייצור — המלכודת של
530fb44, 29d6ac6 ו-e12acf9: TOPIC_NO_MAX לבדו, שהקבוע כבר יושב בקובץ מאז
1bd2ff5; ו«גדול מכל מספר נושא שקיים במערכת» לבדו, שכבר בייצור בתוך
whyTopicNoInvalid. מה שנספר הוא הצירוף why: "…" }; — התשובה של preflight ולא של
נתיב השרת — והמחרוזת עצמה ירדה ל-OLD ונמדדה כספירה, 1 בייצור מול 2 במתוקן.
ארבעת המסומנים ששימשו הם קוד או הערה, וכל אחד 0 בייצור ו-1 במקור.

prev הועתק מ-portal/dist לפני ה-staging: 31,164 בתים, MD5 4F7257379E3D1C88D86EB4CEEA96CDA8
— בדיוק מה ש-598 ו-599 רשמו כנפרס, כלומר ה-dist שעל הדיסק היה הגרסה שבייצור ולא
קובץ ישן יותר. 38 קבצים נפרסו.

dpl_EQuttzcSUmrVFJ7sE2eTdGHDZaAm, READY, aliased ל-more30.com. מ-portal/dist
ובלי --prebuilt: בלי .vercel/project.json ה-CLI יוצר פרויקט בשם "dist"
ו-more30.com לא מתעדכן, ו---prebuilt מפיל את vercel.json כלומר את מפת ה-rewrites
של כל המערכות.

HTTP לפני ואחרי: הטופס 31,400 → 32,558 בשתי הכתובות (עם לוכסן ובלעדיו, שני
rewrites נפרדים), אל״פים 304 → 327, ארבעת המסומנים 0 → 4, הסמיכות 2 → 14.
הדלתא מול המקור היא 236 בדיוק — דלתת NetFree הידועה ולא מספר אחר. מסך הניהול
נשאר 90,056 ודף הבית 3,517 ואותם שני נכסים מגובבים, כלומר המדידה מראה גם רגרסיה
ולא רק את התוספת. אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
נכתב ב-node ולא ב-PowerShell בכוונה: .ps1 בלי BOM נקרא כ-cp1255 והעברית שבתבנית
נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

המדידה שקונה את הפעימה אינה טקסט על המסך אלא בקשה שלא יצאה, על הכתובת החיה,
בדפדפן אמיתי 1280x900, ו-«999999999999» הוקלד בהקשות מקלדת ולא הושם ב-value:
אפס בקשות אל נתיב הקליטה ו-88 מילישניות עד התשובה, מול בקשה אחת ו-2,528
מילישניות שנמדדו בייצור ב-599. האזרח כבר אינו ממתין לשרת כדי לשמוע מה שהמסך ידע
ברגע ההקלדה, והשרת כבר אינו נשאל שאלה שהתשובה לה ידועה מראש.

מה שהמסך אמר: «הפנייה לא נשלחה» ומתחתיה «מספר נושא — גדול מכל מספר נושא שקיים
במערכת. תקנו ושלחו שוב.», «999999999999» עומד בשדה הממוקד ומסומן
aria-invalid=true (אלמנט אחד בדף), וכל שאר הטופס עומד כפי שהוקלד — שום שדה לא
מתאפס, מפני שאין פנייה שנשמרה.

שתי בקרות. «2147483647» — הערך הגדול ביותר ש-0080 עדיין מקבלת, כלומר מה שחסימה
ב->= או עם עיגול הייתה עוצרת בטעות: הבקשה יצאה, 200, פנייה #165 עם 42 זכויות,
ו-topic_no נשמר במסד כ-2147483647 בדיוק — הערך עשה את כל הדרך אל תוך עמודת
ה-integer; הטופס התאפס וחזר ל-info, אפס aria-invalid, המיקוד לא נחטף (BODY).
«1-5» — badInput=true, אפס בקשות, «מספר נושא — צריך מספר שלם. תקנו ושלחו שוב.»,
מיקוד וסימון; החסימה המוקדמת הקודמת לא זזה. הוכחה דו-צדדית מהמסד: פנייה אחת
בלבד לאורך שלוש הזרימות — הבקרה — ולשני הניסיונות החסומים אין שורה באמצע.

שלושה צילומים, שלושה MD5 שונים (957027F4 / F7161AC2 / 80136F68). שורת התוצאה
נמדדה ב-705 והשדה ב-272 בחלון של 900, שניהם בו-זמנית מעל הקו; תקלת הראיות של
e12acf9 נבדקה ולא חזרה. אפס הודעות בקונסולה לאורך שלוש הזרימות.

מצב טסט נמדד ולא הוצהר: documents=0, אפס פניות ב-sent, אפס שורות outbound_queue
עם sent_at, outbound_queue נשאר 8, delivery_log נשאר 3, וטביעת האצבע
050e54ab59235d105af3a10c785dbaea זהה בבסיס ואחרי; הבקרה שנקלטה הציגה בעצמה «מצב
טסט: הפנייה נשמרה, ולא יצאה ממנה שום הודעה». אין מייל, אין הודעה ואין מסמך שיצא
החוצה.

התגלגל אחורה בשלוש פקודות נפרדות לפי id ולא ב-CTE אחד (case_rights → cases →
contacts) — תקלת 124cfc2 — ואיש הקשר נמחק לפי id ולא לפי טלפון, שנשמר מנורמל
לספרות — תקלת ac6e523. אחרי: cases=0, case_rights=0, documents=0, admin_users=1,
templates=2, contacts=4, outbound_queue=8, delivery_log=3, catalog=888 — זהה
בדיוק לבסיס.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

ראיות: QA/bkalot-clone/topic-no-max-deploy-0814.

פתוח: קווים שממשיכים ולא נגעו — הקליטה מחברת 42 זכויות ל-treatment ואפס ל-info
על אותו situation; await load() שאחרי «שינוי סטטוס» מיותר ולא הוסר; אין סינון
«לא הוכרעה» ואין מיון לפי decided_at; אין היסטוריית הכרעות; «מי הפיק» על
documents אינו נכתב לשום מקום; ${out.text_chars} ו-${d.html_chars} נשארו במספר;
כותרות «זכויות (N)» ו«מסמכים (N)» לא נגעו. sent בלתי-ניתן-להשגה בכוונה;
bkalot_clone_admin_create אינה חשופה ב-HTTP; pdf/audio → channel_unsupported.
public_visible ו-show_in_showcase של #37 נשארים false.
