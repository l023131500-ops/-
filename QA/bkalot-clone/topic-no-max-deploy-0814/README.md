# topic-no-max-deploy-0814 — הפריסה של 67555e3

הקו הפתוח נכתב ב-67555e3 (heartbeat 599), מילה במילה: «המדידה כאן רצה מול שני
שרתים סטטיים מקומיים מול ה-edge והמסד החיים — מדידה של הקוד ולא של המוצר.
more30.com/bkalot-studio עדיין מגיש 31,164 בתים ועדיין שולח את «999999999999»
ברשת; ה-rewrite ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים
ש-NetFree מזריק לא נבדקו כאן כלל. הפריסה היא הפעימה הבאה».

נלקח כפי שנוסח ולא הורחב — **פריסה בלבד**: אין שינוי קוד, אין מיגרציה ואין
פריסת edge function. `bkalot-clone-intake` נשארה v1 ו-`bkalot-clone-admin` v7.
`apps/37-bkalot-clone/index.html` זהה בדיוק לזה שנכנס ל-67555e3
(32,322 בתים, MD5 `E6E39BF0F0D5FFD03F8A0ADC95F81078`, `git status` ריק).

## למה זו פעימה בפני עצמה

המדידה הקודמת רצה מול שני שרתים סטטיים מקומיים מול ה-edge והמסד החיים. ה-rewrite
ב-`vercel.dist.json`, ההעתקה ב-`stage-portal.ps1` ו-236 הבייטים ש-NetFree מזריק
לא נבדקו בה כלל.

## REMOVED כאן הוא סמיכות ולא מחרוזת

67555e3 היא תוספת טהורה — 12 שורות נוספו ואפס נמחקו — ולכן אין מחרוזת שנעלמה,
ו-REMOVED נמדד כמרחק בין החסימה הקודמת (`why: "צריך מספר שלם" };`) לבין סוף
`preflight()`: **2 שורות בייצור, 14 במתוקן**. העוגן נלקח כמופע האחרון ולא הראשון —
מלכודת ac6e523, שם עוגן שחוזר בקובץ החזיר מרחק שאינו הסמיכות הנמדדת.

שני מסומנים נפסלו לפני שנקבעו, כי היו מחזירים true על הגרסה שבייצור — המלכודת של
530fb44, 29d6ac6 ו-e12acf9:

- `TOPIC_NO_MAX` לבדו — הקבוע כבר יושב בקובץ מאז 1bd2ff5.
- `גדול מכל מספר נושא שקיים במערכת` לבדו — כבר בייצור בתוך `whyTopicNoInvalid`
  («מספר הנושא גדול מכל…»). מה שנספר הוא הצירוף `why: "…" };` — התשובה של
  `preflight()` ולא של נתיב השרת. הוא נשאר ב-OLD כספירה: 1 בייצור, 2 במתוקן.

ארבעת המסומנים ששימשו הם קוד או הערה, וכל אחד 0 בייצור ו-1 במקור.

## prev הוא באמת הייצור

`portal/dist/bkalot-studio/index.html` הועתק לפני ה-staging: 31,164 בתים,
MD5 `4F7257379E3D1C88D86EB4CEEA96CDA8` — בדיוק מה שheartbeat 598/599 רשמו כנפרס,
כלומר ה-dist שעל הדיסק היה הגרסה שבייצור ולא קובץ ישן יותר. 38 קבצים נפרסו.

## הפריסה

`dpl_EQuttzcSUmrVFJ7sE2eTdGHDZaAm`, READY, aliased ל-more30.com. מ-`portal/dist`
ובלי `--prebuilt`: בלי `.vercel/project.json` ה-CLI יוצר פרויקט בשם "dist"
ו-more30.com לא מתעדכן, ו-`--prebuilt` מפיל את `vercel.json` כלומר את מפת
ה-rewrites של כל המערכות.

## HTTP לפני ואחרי

הטופס 31,400 → 32,558 בשתי הכתובות (עם לוכסן ובלעדיו — שני rewrites נפרדים),
אל״פים 304 → 327, ארבעת המסומנים 0 → 4, הסמיכות 2 → 14. הדלתא מול המקור היא
**236 בדיוק** — דלתת NetFree הידועה ולא מספר אחר. מסך הניהול נשאר 90,056 ודף
הבית 3,517 עם אותם שני נכסים מגובבים, כלומר המדידה מראה גם רגרסיה ולא רק את
התוספת. אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.

נכתב ב-node ולא ב-PowerShell בכוונה: `.ps1` בלי BOM נקרא כ-cp1255 והעברית
שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## המדידה שקונה את הפעימה

אינה טקסט על המסך אלא **בקשה שלא יצאה, על הכתובת החיה**. כל בקשה אל ENDPOINT
נספרה ב-`page.on("request")`. דפדפן אמיתי 1280x900, ו-«999999999999» הוקלד
בהקשות מקלדת ולא הושם ב-value.

| | בייצור לפני (נמדד ב-599) | על הכתובת החיה עכשיו |
|---|---|---|
| בקשות אל נתיב הקליטה | 1 | **0** |
| זמן עד התשובה | 2,528ms | **88ms** |

המסך: «הפנייה לא נשלחה» ומתחתיה «מספר נושא — גדול מכל מספר נושא שקיים במערכת.
תקנו ושלחו שוב.», «999999999999» עומד בשדה הממוקד ומסומן `aria-invalid=true`
(אלמנט אחד בדף), וכל שאר הטופס עומד כפי שהוקלד — שום שדה לא מתאפס, מפני שאין
פנייה שנשמרה.

## שתי בקרות

**«2147483647»** — הערך הגדול ביותר ש-0080 עדיין מקבלת, כלומר מה שחסימה ב-`>=`
או עם עיגול הייתה עוצרת בטעות: הבקשה יצאה, 200, פנייה #165 עם 42 זכויות,
ו-`topic_no` נשמר במסד כ-2147483647 בדיוק — הערך עשה את כל הדרך אל תוך עמודת
ה-integer. הטופס התאפס וחזר ל-info, אפס `aria-invalid`, המיקוד לא נחטף (BODY).

**«1-5»** — `badInput=true`, אפס בקשות, «מספר נושא — צריך מספר שלם. תקנו ושלחו
שוב.», מיקוד וסימון; החסימה המוקדמת הקודמת לא זזה.

הוכחה דו-צדדית מהמסד: פנייה אחת בלבד לאורך שלוש הזרימות — הבקרה — ולשני
הניסיונות החסומים אין שורה באמצע.

## ראיות

שלושה צילומים, שלושה MD5 שונים (957027F4 / F7161AC2 / 80136F68). שורת התוצאה
נמדדה ב-705 והשדה ב-272 בחלון של 900, שניהם בו-זמנית מעל הקו; תקלת הראיות של
e12acf9 נבדקה ולא חזרה. אפס הודעות בקונסולה לאורך שלוש הזרימות.

## מצב טסט — נמדד ולא הוצהר

`documents=0`, אפס פניות ב-`sent`, אפס שורות `outbound_queue` עם `sent_at`,
`outbound_queue` נשאר 8, `delivery_log` נשאר 3, וטביעת האצבע
`050e54ab59235d105af3a10c785dbaea` זהה בבסיס ואחרי. הבקרה שנקלטה הציגה בעצמה
«🚫 מצב טסט: הפנייה נשמרה, ולא יצאה ממנה שום הודעה». אין מייל, אין הודעה ואין
מסמך שיצא החוצה.

## התגלגלות אחורה

שלוש פקודות נפרדות לפי id ולא CTE אחד (`case_rights` → `cases` → `contacts`) —
תקלת 124cfc2 — ואיש הקשר לפי id ולא לפי טלפון (הטלפון נשמר מנורמל, `0501234567`)
— תקלת ac6e523. אחרי: `cases=0, case_rights=0, documents=0, admin_users=1,
templates=2, contacts=4, outbound_queue=8, delivery_log=3, catalog=888` — זהה
בדיוק לבסיס.

## נתיבים מוגנים

`git status --porcelain` ריק על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions`; אין נגיעה ב-bkalut-app /
bkalot-admin / `zr_*` / NEDARIM3873 ולא בסכמות `csj`/`csj_src`/`igud`.

## קבצים

| קובץ | מה |
|---|---|
| `prev/index.html` | הגרסה שבייצור לפני הפריסה (31,164 / 4F725737) |
| `marker-check.mjs` | ספירת מסומנים + מדידת הסמיכות |
| `marker-check-prev.txt` / `-src.txt` / `-dist-post.txt` | הצדדים |
| `http-check.mjs`, `http-before.json`, `http-after.json` | ארבע הכתובות, לפני ואחרי |
| `measure-live.mjs`, `measure-out.json` | שלוש הזרימות בדפדפן על הכתובת החיה |
| `01`–`03`*.png | הצילומים |
| `stage-out.txt`, `deploy-out.txt` | ה-staging והפריסה |
