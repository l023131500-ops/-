// המסומנים של 9f9dbb8, במקום אחד, כדי שהבדיקה המקומית וה-HTTP ימדדו בדיוק את
// אותו דבר. הפיצול לשלוש רשימות הוא העיקר:
//
// NEW     — חייבים להיות false בגרסה שבייצור ו-true אחרי. אלה שקונים את הפעימה.
// REMOVED — ההפך: true לפני, false אחרי. בלעדיהם «נפרס» יכול להיות גם «נפרסו
//           שתי הגרסאות זו על גבי זו», וזה בדיוק מה ששני קבצים באותו גודל
//           נראים כמו.
// OLD     — true בשתי הגרסאות. אלה אינם ראיה לפעימה אלא בקרת רגרסיה: פריסה
//           מ-dist ישן מחזירה מסכים אחורה בלי ששום דבר ידווח שגיאה.
//
// המלכודת כאן חדה מבכל פעימה קודמת: «זכות אחת» לבדו הוא **OLD** ולא NEW — הוא
// נכנס למסך ב-e3e1af1, בשורת הרשימה, ונפרס ב-2842cf4. מי שיחפש אותו לבדו יקבל
// true על הגרסה שבייצור ויקרא אותו כאילו הוא מעיד. ההבדל היחיד הוא מי מקבל
// אותו — `out.rights_count` (שורת אישור ההפקה) ולא `c.rights_count` (הרשימה) —
// ולכן שלושת המסומנים החדשים נושאים את `out.` בתוכם או את ההסבר שמעליו.
// זו אותה מלכודת ש-530fb44 ו-29d6ac6 נתפסו בה, ופעם אחת על אותו טקסט ממש.
export const NEW = [
  'countHe(out.rights_count, "זכות אחת", "זכויות")',
  "// אותו countHe של שורת הרשימה ולא ניסוח שני על אותו נתון",
  "הסוגריים נשארות כפי שהן: הן אומרות מאיפה בא המספר",
];

export const REMOVED = [
  "${out.rights_count} זכויות (",
];

export const OLD = [
  "const countHe = (n, one, plural) =>",
  'countHe(c.rights_count ?? 0, "זכות אחת", "זכויות")',
  'countHe(n, "פנייה אחת", "פניות")',
  "זכות אחת",
  "מסמך אחד",
  "כל הרשימה",
  "נבחרו",
  "תווי טקסט",
  "נוסח נפילה",
  "לא הוכרע מהמסך",
  "<th>מכתב</th>",
  "הכנס לתור",
  "עבד עכשיו",
];

export const report = (t) => ({
  new: NEW.map((m) => m + "=" + t.includes(m)).join(" | "),
  removed: REMOVED.map((m) => m + "=" + t.includes(m)).join(" | "),
  old: OLD.map((m) => m + "=" + t.includes(m)).join(" | "),
});
