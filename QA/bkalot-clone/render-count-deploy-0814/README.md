# «זכות אחת» בשורת אישור ההפקה נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (9f9dbb8) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה שאומרת «1 זכויות»
באישור ההפקה, והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא
את המוצר (ה-rewrite, ההעתקה ל-dist ו-236 הבייטים של NetFree לא נבדקו בה); זו
הפעימה הבאה, בדיוק כמו e3e1af1 → 2842cf4».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-9f9dbb8 (88,696 בתים,
MD5 `17DA929FDF9DD91A162AA15F5041473A`, `git status` ריק) ו-`bkalot-clone-admin`
נשארה v7. אותה חלוקה כמו e3e1af1 → 2842cf4, 13516f0 → b03d393 ו-1abad72 → f2001e0.

## המלכודת שהפעימה הזו יושבת בתוכה

`«זכות אחת»` הוא **OLD** ולא NEW, וזה חד יותר מבכל פעימה קודמת: המחרוזת הזו
נכנסה למסך ב-e3e1af1 — בשורת **הרשימה** — ונפרסה ב-2842cf4. מי שיחפש אותה לבדה
כדי לאמת את הפריסה יקבל `true` על הגרסה שכבר בייצור ויקרא אותו כאילו הוא מעיד.

ההבדל היחיד הוא **מי מקבל את המספר**: `out.rights_count` (שורת אישור ההפקה) ולא
`c.rights_count` (הרשימה). שלושת מסומני ה-NEW נושאים את `out.` בתוכם או את
ההסבר שמעליו, ושניהם — `countHe(c.rights_count …)` ו-`«זכות אחת»` הערומה — יושבים
ב-OLD ונמדדו `true` בארבעת הקבצים, כצפוי. זו אותה מלכודת ש-530fb44 («נוסח נפילה»)
ו-29d6ac6 («החשבון אינו קיים עוד») נתפסו בה, ופה על אותו טקסט ממש.

| | NEW | REMOVED | OLD |
|---|---|---|---|
| `git show 9f9dbb8^` (הגרסה שבייצור) | **כולם false** | כולם true | כולם true |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true | **כולם false** | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | **כולם false** | כולם true | כולם true |
| `portal/dist/…/admin.html` אחרי ה-staging | כולם true | **כולם false** | כולם true |

`prev/admin.html` חולץ ב-`execFileSync` עם `encoding:"buffer"` ולא דרך צינור
PowerShell — צינור מוסיף BOM והופך בייטים, ואז ההשוואה מודדת את הצינור ולא את
הקובץ. 88,067 בתים, MD5 `9055A63E89E3004D84D8B464B3C7F5BE` — **בדיוק** מה
ש-`portal/dist` החזיק לפני ה-staging, כלומר ה-dist שעל הדיסק היה הקובץ שבייצור
ולא קובץ ישן יותר. אחרי ה-staging שניהם 88,696 עם אותו MD5, ו-38 קבצים כמו
בפריסה הקודמת.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 88,303 | **88,932** |
| `/bkalot-studio/admin/` | 88,303 | 88,932 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 909 | **927** |

הדלתא מול המקור היא **236 בדיוק** (88,932 − 88,696) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים
(`index-C2AHwa-z.js`, `index-B35ByRD-.css`) — כלומר המדידה מראה גם רגרסיה ולא רק
את התוספת.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin?cb=qa0084b`, על שתי פניות שנוצרו דרך
נתיב הקליטה הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא
בהזרקה למסד: **#126** תיגע, **#127** בקרה. כניסה לניהול נעשתה במסך עצמו, מנהל 42
(«QA 0084 פריסה»). המסמכים הופקו בלחיצה על «הפק מסמך» בדפדפן.

**המדידה שקונה את הפעימה** — שורת אישור ההפקה, המשפט היחיד שאדם קורא אחרי הפעולה
שבשבילה המסך קיים, בייצור:

> מסמך #105 הופק · תבנית rights_treatment_reply · 394 תווי טקסט · **זכות אחת**
> (כל הרשימה) · לא נשלח — מצב טסט

לפני הפריסה אותה שורה בדיוק הייתה אומרת **«1 זכויות»** — זה מה ש-9f9dbb8 מדדה
מקומית על שתי גרסאות באותה ריצה, וזה מה שכל אדם שהפיק מסמך ראה עד עכשיו. מאז
e3e1af1 זה נאמר בשני נוסחים באותו מסך: הרשימה מעל אמרה «זכות אחת» והאישור מתחתיה
«1 זכויות».

**הבקרה, ושני מצבי הסוגריים, שניהם על אותה פנייה:** #127 עם 42 זכויות ואפס
נבחרות אומרת «2503 תווי טקסט · **42 זכויות** (כל הרשימה)», ואחרי שזכות אחת סומנה
`chosen` — «398 תווי טקסט · **זכות אחת** (נבחרו)». אותה פנייה אמרה «42 זכויות»
רגע לפני כן, ולכן היחיד נגזר מהמספר ולא מהמקור, ושני מצבי הסוגריים נמדדו ולא
אחד. «מסמך #106» בשני הצדדים — 0063 עושה `on conflict (case_id, kind) do update`,
ולכן זה אותו מסמך ולא שניים, וההבדל היחיד בשורה הוא צורת הריבוי. הבקרה גם מוכיחה
שהריבוי לא נשבר: מספר גדול מ-1 עדיין נקרא במספר.

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — ארבע הודעות `INFO`, כולן
של NetFree על טעינת גופנים מ-`netfree.link` ולא של המסך.

## תקלת ראיות שנתפסה בריצה ואינה נבלעת

הריצה הראשונה (פניות 124/125, מנהל 41) הפיקה **שני צילומים זהים ב-MD5** —
`BF54F0B76B4887484E04A2F8C19C3706` פעמיים, על «42 זכויות (כל הרשימה)» ועל «זכות
אחת (נבחרו)». הסיבה: שורת האישור יושבת **מתחת לקו הקיפול** ב-1280x900, והצילום
תיעד את החלק העליון של המסך — שאינו משתנה. הטקסט עצמו נקרא מה-DOM והיה נכון בשתי
המדידות, אבל הצילומים לא הראו אותו, כלומר הראיה הייתה של האזור הלא נכון.

זה **ההפך** מהתקלה של 29d6ac6, שבה שני צילומים זהים היו הראיה עצמה, ולכן נאמר
כאן במפורש. התיקון: `scrollIntoView({block:"center"})` על שורת הסטטוס ואימות
`inViewport` לפני כל צילום — השורה נמדדה ב-`y=438` בשלושת הצילומים. הריצה חזרה
מאפס על פניות 126/127 ומנהל 42, וצילומי הריצה הראשונה נמחקו: אילו נשמרו, הם היו
נראים כמו ראיה.

שלושת הצילומים החדשים נבדקו ב-`Get-FileHash` והם שונים:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-one-right.png` | 59,014 | `C3C20CF6BBA806F3D8C36D6179421F51` |
| `02-live-control-42.png` | 72,684 | `87F436376B63C5C5619C35EA4A12E224` |
| `03-live-chosen-one.png` | 74,391 | `06E81305779875C5C157C588CBF243C0` |

## מה שנרשם ולא נבלע

`rights_count=1` אינו מצב שנתיב HTTP יכול לייצר — הקליטה מחברת כל שורת קטלוג
שתואמת למצב (42 כאן) — ולכן #126 קוצצה ל-`case_rights` אחת ב-`DELETE` ישיר, כמו
ב-9f9dbb8. `rights_source="chosen"` אינו נגיש מהמסך כלל (אין בו בורר זכויות),
ולכן שורה אחת של #127 סומנה ב-`UPDATE` ישיר. `bkalot_clone_admin_create` אינה
חשופה ב-HTTP, ולכן משתמש הבדיקה נוצר ב-RPC ישיר; הכניסה עצמה נעשתה במסך.

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, שורות `outbound_queue` עם `sent_at` = 0, שורות `mode=live`
= 0, מסמכים עם `queue_id` = 0, `delivery_log` נשאר 3, ו-`outbound_queue` נשאר 8
גם אחרי ארבע הפקות בשתי הריצות. טביעת האצבע של התור
(`b23dd82344fcd07a64e2a7037872869f`) זהה בבסיס, בזמן המדידה ואחריה. «הכנס לתור»
ו«עבד עכשיו» היו על המסך ולא נלחצו. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## גלגול אחורה

שתי הריצות התגלגלו אחורה במלואן ובסדר הנכון — בכל אחת: 2 מסמכים, 43
`case_rights`, 2 פניות, 2 אנשי קשר, סשן אחד ומשתמש ניהול (41 ו-42). אחרי:
`cases`=0, `case_rights`=0, `documents`=0, `admin_users`=1, `admin_sessions`=5,
`contacts`=4, `templates`=2 (שתיהן enabled), `catalog`=888, `outbound_queue`=8,
`delivery_log`=3, אפס שורות `chosen` — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status --porcelain` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` החזיר שורות אפס, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- `${out.text_chars}` תווי טקסט ו-`${d.html_chars}` תווי HTML במסך המסמך נשארו
  במספר. «1 תווי טקסט» פירושו מכתב באורך תו אחד, וזה אינו מצב שנתיב התבניות
  מייצר — 394 ו-2503 התווים כאן הם המדידה של זה.
- «0 זכויות» באישור ההפקה לא נמדד בייצור — נתיב הקליטה אינו יכול לייצר פנייה
  בלי זכויות, ואותו `countHe` מייצר אותו.
- כותרות «זכויות (N)» ו«מסמכים (N)» במסך הפנייה לא נגעו: מספר בסוגריים אינו
  צורת ריבוי.
- רענון שנכשל משאיר את השורות הישנות על המסך; אין נתיב HTTP שיכול להפיל את
  `/cases` בבקשה אחת.
- `await load()` שרץ אחרי «שינוי סטטוס» הפך למיותר ולא הוסר.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריה — הכרעה קודמת
  נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום.
- `sent` בלתי-ניתן-להשגה בכוונה; pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
