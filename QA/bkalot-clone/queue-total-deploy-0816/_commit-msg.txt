שכפול בקלות שכבה 3: «בתור N» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נלקח כפי שנוסח ב-f9df2e2 (heartbeat 673), מילה במילה: «פתוח: הפריסה. מה
שנמדד כאן הוא הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) המונה עדיין
אינו אומר «בתור N» ולו בתו אחד, וה-title שם עדיין אומר שהמספר אינו ידוע. זו
הלבנה הבאה». נלקח ולא הורחב: פריסה בלבד — אין שינוי קוד, אין מיגרציה, אין
פריסת edge function. bkalot-clone-admin נשארת v9, schema_migrations נשארה
20260816174554, ו-git status על apps/37-bkalot-clone ועל supabase/ החזיר אפס
שורות לפני ה-add ואחריו. מה שנכנס ל-commit הוא ראיות בלבד.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816174554. ו-HEAD היה f9df2e2 וגם
origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 176,004 בייט ו-MD5 282DF8EC במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
172,296 (4D03D58B) — מה ש-d4ce4ed פרסה — כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (prebuilt-deploy-drops-vercel-json),
ו-vercel.json ו-.vercel/project.json נמדדו כנוכחים לפני הפקודה.
dpl_6XQMtuBfG3hZQmAHjDrLJXpRANSY, target=production, Ready.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist), בלי
שהכתובת החיה נפגשה ולו פעם אחת — 0087. DEAD ריק. ⚠️ ומה שהיה בסבב הקודם הערה
בלבד הוא מעכשיו שמירה (6849803): classify.mjs יוצא בקוד 2 ואינו כותב את
classify.json כשה-dist כבר זהה ל-src, כלומר כשה-staging כבר רץ — ההרצה החוזרת
שנראית תקינה ואינה מודדת דבר אינה יכולה עוד לדרוס את הראיה.

⚠️ הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה כל 23 הסימנים
החזירו מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת.

12 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי בשתי
הכתובות (עם לוכסן ובלעדיו): «let queueTotal;», «queueTotal = Object.hasOwn(data,
"queue_total")», queueTotal (4), queue_total (3), «const queueBit = typeof
queueTotal === "number"», queueBit (3), «· בתור ${many(queueTotal)}», ה-title
החדש, «if (queueBit) $("count").title» ושלוש שורות ההערה. ⚠️ 2 מסומני REMOVED —
«$("count").textContent = total === cases.length» וה-«if (filtered)» שקדם
ל-title הישן — עמדו 1 לפני ו-0 אחרי; שני אלה הם הראיה שהקוד הישן לא נשאר לצד
החדש, ובפרט שה-title הישן הוא מעכשיו ענף else ולא ענף ראשון. 2 מסומני DIFF הגיעו
בדיוק לספירת המקור: $("count").title 1→2 ו«המיון אינו מוציא שורות» 1→2 — הענף
השני של ה-title, בלי שהתנהגות המסך תשתנה במקום אחר. 7 מסומני CONTROL לא זזו,
והצמודים שבהם ctl_filtered ו-ctl_noneText — שני הערכים שהשורה שהשתנתה קוראת
מהם ולא נגעה בהם.

live 172,532 בייט לפני ו-176,240 אחרי, בשתי הכתובות ובאותו MD5 — 236 בייט מעל
הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של תוכן. אפס תווי החלפה ואפס
«×» — deployed-html-double-encoded-cp1255.

הטענה עצמה נקראה מה-DOM בכתובת החיה ולא מקובץ. הזריעה: ארבע פניות דרך
public.bkalot_clone_intake (source=form, queued=false, rights_linked 0) — 400
info «אברהם ישראלי» 0501230041, 401 reminder «שרה כהן» 0501230042, 402 info
«יוסף לוי» 0501230043, 403 reminder «רבקה מזרחי» 0501230044. ⚠️ שתיים info
ושתיים reminder בכוונה, כדי שסינון יצמצם באמת ולא בתיאוריה. ⚠️ ונאמר ולא נבלע:
הקליטה נעשתה ב-SQL ולא מעל HTTP — פיגום ולא הטענה. התחברות אמיתית דרך הטופס,
חשבון 128 שנוצר ב-bkalot_clone_admin_create ולא בהזרקת hash, ושלושת המצבים
באותה טעינה ובלי refresh — טעינה מחדש מנתקת את הסשן (admin.html:312).

שלושה מצבים, וכל שורה נקראה מ-#count ב-DOM ולא הוסקה:

  A  בלי סינון — «4 פניות», בלי title, ארבע שורות. הבקרה: queue_total null אינו
     מצויר, והשינוי אינו נוגע במסך שאין בו שאלה.
  B  kind=info — «2 פניות תואמות את הסינון · בתור 4 פניות», שתי שורות, ועמו
     ה-title החדש: «המספר הראשון הוא של הפניות שעונות על הסינון, ו«בתור» הוא כל
     הפניות שבמערכת — כולל אלה שהסינון הוציא».
  C  ⚠️ status=sent — «אין פניות שתואמות את הסינון · בתור 4 פניות», אפס שורות.
     זה הכשל שנסגר: עד הפריסה הזו אמר המסך בייצור «אין פניות שתואמות את הסינון»
     ותו לא, ולא הבחין בין «יש תור ואף פנייה בו אינה נשלחה» ל«התור ריק».

בקרות שלא זזו: מספר השורות בשלוש הקריאות (4/2/0) כמו ב-total, התיבה הריקה נדלקת
ב-C בלבד ואומרת «אין פניות שתואמות את הסינון.» כמו לפני, pager מוסתר בשלושתן,
ואפס גלישה אופקית ב-1280x900. הקונסולה נלכדה ולא נרשמה כחסרה: אפס הודעות ואפס
תגובות שאינן 2xx. שלושה צילומים בשלושה MD5 (24300357, 9513314C, 2B82DDED) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize, והגלילה אל #count עצמו.

⚠️ נאמר ולא נבלע: מצבי A/B/C לפני הפריסה לא צולמו מהייצור — הפריסה כבר קרתה.
שהייצור הריץ את הקוד הישן נמדד מהסימנים (old_count_line 1, old_title_if 1, ואפס
מכל 12 החדשים) ולא מקריאת DOM. מצבים D (מיון בלבד) ו-F (decided=no) נמדדו
מקומית ב-f9df2e2 ואינם חוזרים כאן. הענף total>0 עם אפס שורות («בעמוד הזה לא
נשארו פניות.») נפרס ולא נמדד מדפדפן. ולא נמדד ב-390x844.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, case_rights 0,
outbound_queue נשאר 8, delivery_log נשאר 3, אפס שורות sent_at, אפס קריאות
יוצאות. מה שנפרס הוא HTML ואין בו כתיבה חדשה.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot;
ונמדד לפני המחיקה ולא הונח שאין שורות בנות: log 0, case_rights 0, documents 0.
⚠️ contacts יושבת ב-bkalot_auto ולא ב-bkalot_clone. נמחקו cases 4 (400–403),
contacts 4 (407–410), admin_sessions של 128, admin_users 1 (128). אחרי: בסיס
בדיוק, ו-admin_cases על פרמטרים ריקים מחזירה total 0 ו-queue_total null.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add; אין נגיעה
ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. portal/dist gitignored. ל-git
add נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה ב-commit -F —
here-string-commit-fails-silently.

פתוח: נתיב «המונה אומר מתוך כמה» סגור מקצה לקצה — המסד (0099), השער (v9, בלי
שינוי), המסך (f9df2e2) והפריסה כאן. הלבנה הבאה היא UI ולא פריסה. מה שלא נבנה
ולא זז: אין פירוק של המספר — «כמה נשארו מחוץ לסינון» ו«איזה מהסננים צמצם» אינם
נענים ממספר אחד; אין הדגשה ואין ציטוט של המילה בנימוק; אין חיפוש ב-cases.note;
אין סימון על שמות המכריעים; אין סינון «יש נימוק / אין»; אין סינון ומיון לפי
מספר הנימוקים או המעברים; אין אינדקס על case_status_log(note) ואין חיפוש טקסט
מלא; אין עריכה ומחיקה ביומן; אין סינון «איש קשר נמחק / קיים»; אין שמירת מיקום
הפנייה; אין אינדקס על ct.phone. ⚠️ שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
