שכפול בקלות שכבה 3: טבלת המסמכים קיימת מאז 0057, וארבע מיגרציות נבנו סביבה בלי שיהיה מי שכותב לתוכה (§5ב, 0063, #231 נסגר)

מסך הניהול הציג «מסמכים (0)» על כל פנייה מאז שנבנה, ו-0 היה נכון ולא תקלה:
קליטה (0058), קריאה (0060) וכניסה (0061/0062) נבנו כולן סביב טבלה שאין לה
רנדרר. זו הלבנה הראשונה של שכבה 3, והיא הפקה בלבד — אין בה שליחה.

מה נבנה: bkalot_clone.templates — התבנית כנתון ולא כקוד, לפי BKALOT_METHOD §3
(במקור automation_configs.configJson) — ושתי תבניות מוזרעות. העברית של המסמך
יושבת בשורות הטבלה ולא בגוף הפונקציה, כדי שהניהול יערוך נוסח בלי מיגרציה.
public.bkalot_clone_render(jsonb) בוחרת ביניהן לפי סוג הפנייה.

נמדד על שתי פניות שנוצרו דרך נתיב הקליטה האמיתי ולא בהזרקה למסד:

  info (0 זכויות)          233 תווי טקסט, 465 HTML, fallback=false
  treatment (disability)   59 זכויות, 3420 טקסט, 5228 HTML, 59 פריטי רשימה
  ששת הראשונים            B100 B111 B114 B124 B144 B195 — המדידה השביעית
                           הבלתי-תלויה שמסכימה על אותו מספר ואותו סדר

שתי הכרעות שאינן העתקה מהמקור. (1) גוף ה-HTML עובר escape על כל ערך שמגיע
מהפונה. שם מלא הוא קלט חופשי של אדם זר; נמדד על שם שהוא ממש תגית — תגית img
עם onerror יצאה מקודדת ב-body_html ונשארה גולמית ב-body_text. בלי זה היא
הייתה יושבת בתוך המסמך שמסך הניהול מציג.
(2) case_rights.chosen הוא בחירת הזכויות: בחר מישהו — המסמך מכיל את הנבחרות;
לא בחר איש — את כל הרשימה המדורגת. סימון שלוש זכויות החזיר את אותו
document_id (upsert ולא צבירה), rights_count=3, rights_source=chosen.

חמישה מסלולי שגיאה ושני שומרים: case_id_required (חסר ולא-מספרי),
case_not_found, template_unknown עם רשימת המותרות, template_disabled;
תבנית ברירת-מחדל מנוטרלת נופלת לנוסח נפילה (fallback=true) ואינה מפילה את
ההפקה, ותבנית שמתרוקנת ברינדור מוחזרת כ-render_empty במקום להיכתב כמסמך ריק.
מציין-מקום שאינו מוכר מדווח ב-placeholders_unresolved ואז מוסר — במקור הוא
נבלע, וכאן הוא נראה, אחרת תבנית עם שגיאת כתיב שולחת נוסח קטוע ואיש לא יודע.

מצב טסט, נמדד ולא הוצהר: outbound_queue=8 ו-delivery_log=3 לפני ואחרי,
queue_id של כל מסמך שנוצר נשאר null, ולפונקציה אין שום מסלול יוצא.
הרשאות אחרי: anon=false, authenticated=false, service_role=true — אותה מלכודת
שנמדדה ב-0058/0060/0061, שבה פונקציית SECURITY DEFINER חדשה ב-public מקבלת
EXECUTE ל-PUBLIC כברירת מחדל ומפתח ה-anon יושב גלוי בקוד הטופס הציבורי.
templates: RLS דלוק, אפס policies, אפס grants פרט לבעלים.

הבדיקה התגלגלה אחורה במלואה: cases=0, case_rights=0, documents=0, contacts=4,
templates=2 ושתיהן דלוקות.

נשאר פתוח: לרנדרר אין כתובת HTTP ואין כפתור — bkalot-clone-admin לא נגעה,
ומסך הניהול עדיין אינו יכול לקרוא לו. זו הסיבה שאין בצעד הזה צילום מסך.
ו-#232: למצב (situation) אין שם עברי במסד — rights.situation_map מחזיקה מפתח
אנגלי בלבד — ולכן המציין זמין ואינו מופיע באף תבנית מוזרעת.

אין נגיעה במוגן (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873, csj/csj_src/igud).
עדות: QA/bkalot-clone/render-0814/_results.json
