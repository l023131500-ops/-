שכפול בקלות שכבה 3: «אין פניות» על תור מלא נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נלקח כפי שנוסח ב-a795108 (heartbeat 644), מילה במילה: «הפריסה. מה שנמדד כאן
הוא הקוד שבמקור; בייצור המסך עדיין אומר «אין פניות» על תור מלא. זו הלבנה הבאה —
אותו פיצול כמו aff6455 → 9c96135, b1fcca7 → 81e3307, 53a3e5a → c806151
ו-02fc51c → 2346adb». נלקח ולא הורחב: פריסה בלבד — אין שינוי קוד, אין מיגרציה,
bkalot-clone-admin נשארת v8 ו-bkalot-clone-intake v1, supabase/ ו-apps/37 לא נגעו.
מה שנכנס ל-commit הוא ראיות בלבד.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 136,547 בייט ו-MD5 96D642E3 במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
134,160 (9D4E4787) — מה ש-2346adb פרס, כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (הדגל מפיל את vercel.json ואיתו את מפת
ה-rewrites). dpl_3Eu6RtqSuASoJqJnWNCEj8CKfvT8, READY, production, aliased
ל-more30.com.

שמונה מסומני NEW החזירו 0 בייצור ו-≥1 במקור לפני, והתהפכו אל שוויון אחרי בשתי
הכתובות (עם לוכסן ובלעדיו): reclamped 0→3, load(true) 0→1, «הרשימה מתקצרת בשרת»
0→1, «ואינו רודף אחריו» 0→1, «מעבר לסוף הרשימה» 0→1, const cases = data.cases
0→1, «ראה הכרעה (4) למטה» 0→1, offset > 0 0→1. ⚠️ offset > 0 יושב ב-NEW ולא
ב-DIFF מפני שנמדד 0 בייצור ולא הונח: זהו התנאי היחיד במסך שבודק אם המשתמש מעבר
לעמוד הראשון, והוא נולד בלבנה הזו. שני ה-DIFF גדלו אל המקור:
Math.ceil(total / PAGE) 1→2 ו-render(cases) 1→2, שניהם יושבים בייצור מזמן
(חישוב «עמוד N מתוך M» והכרזת הפונקציה) והחיתוך משתמש בשניהם שוב, ולכן הם גדלים
ואינם נולדים.

עשרים ואחד מסומני הבקרה לא זזו לפני ולא אחרי. חמשת הראשונים הם מסומני ה-NEW של
2346adb — q-phone 3, qPhone 6, «חופש לפי» 1, q_phone 1, 0093 2 — כלומר מסומני
ה-NEW של הלבנה הקודמת הם הבקרה של זו. ⚠️ data.cases 1 ו-function load 1 יושבים
בבקרה אף שהם צמודים לשורה שהשתנתה — data.cases עבר מ-render(data.cases || [])
אל const cases = data.cases, ו-function load מ-load() אל load(reclamped) — אותו
מספר בדיוק בשתי הגרסאות, ולכן הם הבקרה הצמודה ביותר שיש. ועוד: list-error 4,
decider_required 1, note_too_long 5, why-input 2, syncNoteCount 4, td.why 1,
«רצף ההכרעות» 4, handsBit 2, producedBit 2, fillQueueCell 2, fillTemplateCell 4,
fillCreatedCell 4, «מי הכריע» 13, fillHistoryNoteCell 2.

⚠️ בריצה הזו אף מסומן לא סווג מחדש — כל 21 מסומני הבקרה נמדדו שווים בדיוק, כמו
ב-81e3307, ב-c806151 וב-2346adb ובשונה מ-8a577bc, מ-24f8a7d ומ-9c96135. הסיווג
נקבע ב-classify.mjs לפני הריצה הראשונה, ושתי הריצות רצו מ-probe.mjs בדיוק כפי
שהוא — תקלת 0087. שתי כתובות בקרה לא זזו: 32,558 ו-3,517. אפס תווי החלפה ואפס
כפל-קידוד. 134,396 → 136,783 = 136,547 + 236, דלתת NetFree הידועה.

הרצף נמדד בייצור ב-1280x900 אחרי התחברות אמיתית דרך המסך בחשבון 102, ואין ולו
הזרקה אחת ואין ולו קריאת cases אחת מסקריפט — בשונה מ-53a3e5a ומ-c806151, שבהם
אין דרך לגרום לשרת החי להחזיר את הקוד; כאן החיתוך יושב במסך עצמו והשרת החי מחזיר
את הכל בעצמו. סינון «חדשה» → 20 שורות (#318…#299), «21 פניות · מוצגות 20»,
«עמוד 1 מתוך 2»; «הבא» → שורה אחת (#298), «21 פניות · מוצגות 1», «עמוד 2 מתוך 2»;
«בטיפול» וחזרה → 20 שורות, «20 פניות», «עמוד 1 מתוך 1», empty מוסתרת. לפני
הפריסה השורה האחרונה אמרה שלושה דברים בבת אחת: «20 פניות · מוצגות 0», «אין פניות
שתואמות את הסינון» ו«עמוד 2 מתוך 1».

גופי הבקשות נמדדו כפי שיצאו: חמש קריאות cases ב-offset 0, 0, 20, 20, 0.
החמישית היא החיתוך; אין שישית, כלומר הכרעה (4) — פעם אחת בלבד — נמדדה בייצור ולא
הונחה.

בקרה על הכרעה (3) בריצה שנייה: סינון «נשלחה» (אפס פניות) — «אין פניות», empty
גלויה ב-342, pager מוסתר, ושתי בקשות בלבד יצאו, כלומר החיתוך לא נכנס על ריקנות
אמיתית. ⚠️ מה שנמדד שם הוא total=0 עם offset=0; המצב total=0 עם offset>0 אינו
ניתן להגעה מהמסך בזריעה הזו — הוא נמנע מהבנייה בתנאי total>0, וזה נאמר ולא נבלע.

המיקומים נמדדו מה-DOM אחרי הציור ולא הונחו — תקלת screenshot-evidence-below-the-fold:
empty ב-342 ו-pager ב-446 בעמוד השני, בתוך חלון של 900. ⚠️ בעמוד הראשון ה-pager
יושב ב-2188, מתחת לקיפול — נמדד ונאמר, ואינו מה שהלבנה קונה. ארבעת הצילומים
בארבעה MD5 שונים. קונסולה: אפס הודעות ואפס שגיאות בשתי הריצות. ⚠️ החלון שונה
פעם אחת לפני הצילום הראשון — תקלת playwright-blank-screenshot-until-resize.

מצב טסט נמדד ולא הוצהר: queued=false בכל 21 הקליטות, אפס מסמכים, outbound_queue
נשאר 8 ואפס שורות sent_at, delivery_log נשאר 3, אפס פניות ב-sent, אפס קריאות
יוצאות. מה שנפרס הוא HTML, ושתי הריצות הן קריאות למעט מעבר סטטוס אחד. kind=info
ולא treatment: info אינו מחבר זכויות, ולכן 21 קליטות אינן גוררות ~880 שורות
case_rights לגלגול אחורה — case_rights נמדד 0 לאורך כל הריצה.

21 הפניות נזרעו דרך הכתובת החיה של bkalot-clone-intake מעל HTTP עם מפתח anon
בדיוק כמו מהטופס (#298–#318), והמנהל דרך bkalot_clone_admin_create (102) ולא
בהזרקת hash. התגלגל אחורה בפקודות נפרדות לפי טבלה — תקלת
cte-delete-sees-prestatement-snapshot; ואנשי הקשר לפי id ולא לפי טלפון מנורמל —
תקלת ac6e523. ⚠️ contacts, outbound_queue ו-delivery_log יושבות ב-bkalot_auto
ולא ב-bkalot_clone. נמחקו: case_status_log 1, cases 21, contacts 21,
admin_sessions 2, admin_users 1. אחרי: cases 0, case_status_log 0, case_rights 0,
documents 0, admin_users 1, admin_sessions 5, templates 2, contacts 4,
outbound_queue 8, delivery_log 3, rights_catalog 888 — בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש; אין נגיעה
ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.
ל-git add נמסר נתיב מפורש אחד ולא נקודה.

פתוח: נתיב החיתוך סגור מקצה לקצה — הקוד (a795108) והפריסה כאן; מי שמסיים לעבוד
את התור בייצור רואה את 20 הפניות שנשארו ולא מסך שאומר שהתור ריק.

מה שלא נבנה: pager.hidden = total <= PAGE נשאר כפי שהוא — אחרי החיתוך יש עמוד
אחד באמת, ושורת דפדוף על עמוד יחיד היא רעש. אין הודעה שמסבירה שהעמוד נחתך —
המסך מראה את הרשימה הנכונה, ומשפט «העמוד שהיית בו כבר אינו קיים» מתאר תקלה שאין.
אין שמירת מיקום הפנייה שטופלה בתוך העמוד החדש. אין status_last_change_at; אין
שמות ברשימה; אין חיפוש בטקסט הנימוקים; אין סינון ואין מיון לפי מספר מעברים; אין
חובת נימוק על rejected; אין עריכה ואין מחיקה ביומן; אין אינדקס על ct.phone; אין
backfill.

קווים שממשיכים ולא נגעו: טעינה מחדש של הכתובת בייצור מנתקת את הסשן — הכרעה
מוצהרת ב-admin.html:296 ולא תקלה; הקליטה מחברת זכויות ל-treatment ואפס
ל-info/reminder על אותו situation — הכרעה מוצהרת ב-intake. sent
בלתי-ניתן-להשגה בכוונה; bkalot_clone_admin_create אינה חשופה ב-HTTP ונקראת
ב-SQL בארגומנט jsonb יחיד; pdf/audio → channel_unsupported. public_visible
ו-show_in_showcase של #37 נשארים false.
