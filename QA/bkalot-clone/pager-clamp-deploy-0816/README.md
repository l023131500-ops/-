# פריסה — רשימת העבודה מודיעה «אין פניות» כשהתור מלא

הקו נכתב ב-`a795108` (heartbeat 644), מילה במילה: «הפריסה. מה שנמדד כאן הוא הקוד
שבמקור; בייצור המסך עדיין אומר «אין פניות» על תור מלא. זו הלבנה הבאה — אותו פיצול
כמו aff6455 → 9c96135, b1fcca7 → 81e3307, 53a3e5a → c806151 ו-02fc51c → 2346adb».

נלקח ולא הורחב: **פריסה בלבד**. אין שינוי קוד, אין מיגרציה, אין פריסת edge
function — `bkalot-clone-admin` נשארת v8, `bkalot-clone-intake` v1, `supabase/`
לא נגעה ו-`apps/37-bkalot-clone` לא נגעה. מה שנכנס ל-commit הוא ראיות בלבד.

## הקובץ שנפרס הוא הקובץ שבמקור

| | בייטים | MD5 |
|---|---|---|
| מקור `apps/37-bkalot-clone/admin.html` | 136,547 | `96D642E3…` |
| `portal/dist` **לפני** staging | 134,160 | `9D4E4787…` |
| `portal/dist` **אחרי** staging | 136,547 | `96D642E3…` |

מה שישב ב-dist לפני ה-staging הוא בדיוק מה ש-`2346adb` פרס — כלומר שרשרת הפריסות
נמדדה ולא הונחה. 38 קבצים, מ-`portal/dist` ובלי `--prebuilt` (הדגל מפיל את
`vercel.json` ואיתו את מפת ה-rewrites).

`dpl_3Eu6RtqSuASoJqJnWNCEj8CKfvT8`, READY, production, aliased ל-more30.com.

## המסומנים

הסיווג נקבע ב-`classify.mjs` **לפני** ש-`probe.mjs` נכתב, ושתי הריצות רצו מ-`probe.mjs`
בדיוק כפי שהוא — תקלת 0087.

- **NEW (8)** — 0 בייצור לפני, `≥1` במקור, ושוויון אחרי, בשתי הכתובות (עם לוכסן
  ובלעדיו): `reclamped` 0→3, `load(true)` 0→1, `הרשימה מתקצרת בשרת` 0→1,
  `ואינו רודף אחריו` 0→1, `מעבר לסוף הרשימה` 0→1, `const cases = data.cases` 0→1,
  `ראה הכרעה (4) למטה` 0→1, `offset > 0` 0→1.
  ⚠️ `offset > 0` יושב ב-NEW ולא ב-DIFF מפני שנמדד 0 בייצור ולא הונח: זהו התנאי
  היחיד במסך שבודק אם המשתמש מעבר לעמוד הראשון, והוא נולד בלבנה הזו.
- **DIFF (2)** — גדלו אל המקור: `Math.ceil(total / PAGE)` 1→2 ו-`render(cases)` 1→2.
  שניהם יושבים בייצור מזמן (חישוב «עמוד N מתוך M» והכרזת הפונקציה), והחיתוך
  משתמש בשניהם שוב — ולכן הם גדלים ואינם נולדים.
- **בקרה (21)** — לא זזו לפני ולא אחרי. חמשת הראשונים הם מסומני ה-NEW של
  `2346adb`: `q-phone` 3, `qPhone` 6, `חופש לפי` 1, `q_phone` 1, `0093` 2 —
  **מסומני ה-NEW של הלבנה הקודמת הם הבקרה של זו.** ⚠️ `data.cases` 1 ו-`function load` 1
  יושבים בבקרה אף שהם צמודים לשורה שהשתנתה (`render(data.cases || [])` →
  `const cases = data.cases`, ו-`load()` → `load(reclamped)`) — אותו מספר בדיוק
  בשתי הגרסאות, ולכן הם הבקרה הצמודה ביותר שיש. ועוד: `list-error` 4,
  `decider_required` 1, `note_too_long` 5, `why-input` 2, `syncNoteCount` 4,
  `td.why` 1, `רצף ההכרעות` 4, `handsBit` 2, `producedBit` 2, `fillQueueCell` 2,
  `fillTemplateCell` 4, `fillCreatedCell` 4, `מי הכריע` 13, `fillHistoryNoteCell` 2.

⚠️ בריצה הזו אף מסומן לא סווג מחדש — כל 21 מסומני הבקרה נמדדו שווים בדיוק, כמו
ב-`81e3307`, ב-`c806151` וב-`2346adb`, ובשונה מ-`8a577bc`, מ-`24f8a7d` ומ-`9c96135`.

שתי כתובות בקרה לא זזו: 32,558 ו-3,517. אפס תווי החלפה ואפס כפל-קידוד.
134,396 → 136,783 = 136,547 + 236, דלתת NetFree הידועה.

## הרצף בייצור

1280x900, התחברות אמיתית דרך המסך (חשבון 102), ואין ולו הזרקה אחת ואין ולו
קריאת `cases` אחת מסקריפט — השרת החי מחזיר את הכל בעצמו.

| | שורות | מונה | דפדוף | empty |
|---|---|---|---|---|
| (א) סינון «חדשה» | 20 (#318…#299) | «21 פניות · מוצגות 20» | «עמוד 1 מתוך 2» | מוסתרת |
| (ב) «הבא» | 1 (#298) | «21 פניות · מוצגות 1» | «עמוד 2 מתוך 2» | מוסתרת |
| (ג+ד) «בטיפול» וחזרה | **20** (#318…#299) | **«20 פניות»** | **«עמוד 1 מתוך 1»** | **מוסתרת** |

לפני הפריסה השורה האחרונה אמרה שלושה דברים בבת אחת: «20 פניות · מוצגות 0»,
«אין פניות שתואמות את הסינון» ו«עמוד 2 מתוך 1».

גופי הבקשות נמדדו כפי שיצאו: חמש קריאות `cases`, ב-offset `0, 0, 20, 20, 0`.
החמישית היא החיתוך; **אין שישית**, כלומר הכרעה (4) — פעם אחת בלבד — נמדדה בייצור
ולא הונחה.

## בקרה על הכרעה (3)

ריצה שנייה, סינון «נשלחה» (אפס פניות): «אין פניות», `empty` גלויה ב-342, `pager`
מוסתר, ו**שתי בקשות בלבד** יצאו — כלומר החיתוך לא נכנס על ריקנות אמיתית.
⚠️ מה שנמדד שם הוא `total=0` עם `offset=0`; המצב `total=0` עם `offset>0` אינו
ניתן להגעה מהמסך בזריעה הזו — הוא נמנע מהבנייה בתנאי `total>0`, וזה נאמר ולא נבלע.

המיקומים נמדדו מה-DOM אחרי הציור ולא הונחו — תקלת `screenshot-evidence-below-the-fold`:
`empty` ב-342 ו-`pager` ב-446 (עמוד 2), בתוך חלון של 900. ⚠️ ב-(א) ה-`pager` יושב
ב-2188, מתחת לקיפול — נמדד ונאמר, ואינו מה שהלבנה קונה. ארבעת הצילומים בארבעה
MD5 שונים. קונסולה: אפס הודעות ואפס שגיאות בשתי הריצות.
⚠️ החלון שונה פעם אחת לפני הצילום הראשון — תקלת `playwright-blank-screenshot-until-resize`.

## מצב טסט — נמדד ולא הוצהר

`queued=false` בכל 21 הקליטות, אפס מסמכים, `outbound_queue` נשאר 8 ואפס שורות
`sent_at`, `delivery_log` נשאר 3, אפס פניות ב-`sent`, אפס קריאות יוצאות. מה שנפרס
הוא HTML, ושתי הריצות הן קריאות למעט מעבר סטטוס אחד.

`kind=info` ולא `treatment`: `info` אינו מחבר זכויות, ולכן 21 קליטות אינן גוררות
~880 שורות `case_rights` לגלגול אחורה — `case_rights` נמדד 0 לאורך כל הריצה.

## גלגול אחורה

בפקודות נפרדות לפי טבלה — תקלת `cte-delete-sees-prestatement-snapshot`; ואנשי
הקשר לפי id ולא לפי טלפון מנורמל — תקלת `ac6e523`. ⚠️ `contacts`, `outbound_queue`
ו-`delivery_log` יושבות ב-`bkalot_auto` ולא ב-`bkalot_clone`.

נמחקו: `case_status_log` 1, `cases` 21, `contacts` 21, `admin_sessions` 2, `admin_users` 1.
אחרי: cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888 — **בסיס בדיוק**.

## מוגן

`git status` ריק על `apps/08-bkalut-app`, `apps/09-bkalot-admin`, `supabase`
ו-`apps/37-bkalot-clone`, נמדד מהשורש; אין נגיעה ב-`bkalut-app`/`bkalot-admin`/`zr_*`/
`NEDARIM3873` ולא בסכמות `csj`/`csj_src`/`igud`. ל-`git add` נמסר נתיב מפורש אחד
ולא נקודה.

## פתוח

נתיב החיתוך סגור מקצה לקצה — הקוד (`a795108`) והפריסה כאן; מי שמסיים לעבוד את
התור בייצור רואה את 20 הפניות שנשארו ולא מסך שאומר שהתור ריק.

מה שלא נבנה: `pager.hidden = total <= PAGE` נשאר כפי שהוא. אין הודעה שמסבירה
שהעמוד נחתך. אין שמירת מיקום הפנייה שטופלה בתוך העמוד החדש. אין `status_last_change_at`;
אין שמות ברשימה; אין חיפוש בטקסט הנימוקים; אין סינון ואין מיון לפי מספר מעברים;
אין חובת נימוק על `rejected`; אין עריכה ואין מחיקה ביומן; אין אינדקס על `ct.phone`;
אין backfill.

קווים שממשיכים ולא נגעו: טעינה מחדש של הכתובת בייצור מנתקת את הסשן — הכרעה מוצהרת
ב-`admin.html:296` ולא תקלה; הקליטה מחברת זכויות ל-`treatment` ואפס ל-`info`/`reminder`
על אותו situation — הכרעה מוצהרת ב-intake. `sent` בלתי-ניתן-להשגה בכוונה;
`bkalot_clone_admin_create` אינה חשופה ב-HTTP ונקראת ב-SQL בארגומנט jsonb יחיד;
`pdf`/`audio` → `channel_unsupported`. `public_visible` ו-`show_in_showcase` של #37
נשארים false.

## קבצים

- `classify.mjs` / `classify.json` — הסיווג המוקדם.
- `probe.mjs` — רץ פעמיים בדיוק כפי שהוא → `http-before.json`, `http-after.json`.
- `seed.mjs` / `seed.json` — 21 הקליטות דרך הכתובת החיה.
- `measure.mjs` / `measure-out.json` — הרצף בייצור (`live`) והבקרה (`ctl`).
- `live-01-filtered.png`, `live-02-page2.png`, `live-03-back.png`, `ctl-01-true-empty.png`.
