שכפול בקלות שכבה 3: כפתורי «שינוי סטטוס» נמדדו מדפדפן ולא היו בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (2a067be) הותירה נוסח בה עצמה, גם בהודעת הקומיט וגם
ב-QA/bkalot-clone/set-status-ui-0814: «הקובץ טרם נפרס — more30.com/bkalot-studio/admin
עדיין מגיש את הגרסה בלי הכפתורים, והמדידה כאן רצה מול שרת סטטי מקומי, כלומר
מדדה את הקוד ולא את המוצר (ה-rewrite, ההעתקה ל-dist ו-236 הבייטים של NetFree לא
נבדקו בה כלל) — וזו הפעימה הבאה, כמו 37235a3 → e95a0ef». נלקח כפי שנוסח ולא
הורחב: אין שינוי קוד, אין מיגרציה ואין פריסת edge function.
apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-2a067be (64,708 בתים,
git diff ריק) ו-bkalot-clone-admin נשארה v6. אותה חלוקה כמו c2a0a36 → 205c940,
18006b1 → 55efb75, c3338c5 → 3750c2e ו-37235a3 → e95a0ef.

למה זו פעימה בפני עצמה: המדידה הקודמת רצה מול שרת סטטי מקומי שמגיש את הקובץ
מהריפו מול ה-edge והמסד החיים. ה-rewrite ב-vercel.dist.json, ההעתקה
ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק לא נבדקו בה כלל.

הבדיקה שרצה לפני הפריסה ולא הונחה: portal/dist יושב על הדיסק, ופריסה מ-dist ישן
מחזירה את כל הפורטל אחורה בלי ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו
לייצור — index-C2AHwa-z.js ו-index-B35ByRD-.css, זהים בשני הצדדים.
dist/bkalot-studio/admin.html היה 57,636 (הקובץ של e95a0ef) והמקור 64,708.

נמדד ב-HTTP לפני ואחרי: /bkalot-studio/admin היה 57,872 בתים וששת המסומנים
החדשים כולם false, ועלה ל-64,944 — הדלתא מול המקור היא 236 בדיוק, דלתת NetFree
הידועה ולא מספר אחר; /bkalot-studio/admin/ מחזיר את אותם בתים; הטופס נשאר 21,272
ו-more30.com/ נשאר 3,517 עם אותם שני נכסים — בלי רגרסיה. שנים-עשר המסומנים חזרו
בגוף, כולם true: הששה החדשים (שינוי סטטוס, status_required, status_not_settable,
«אין מה לשנות», «משנה…», «הפנייה כבר הייתה ב») והששה של הפעימות הקודמות (עבד
עכשיו, <th>תור</th>, «כתובת המכתב», queue_body_empty, «עובד בבדיקה — לא נשלח»,
«הכנס לתור») — כדי שהמדידה תראה גם רגרסיה ולא רק את התוספת. שפיות קידוד נמדדה:
546 אל״פים אמיתיים, אפס תווי החלפה ואפס סימני כפל-קידוד. המדידה נכתבה ב-node
ולא ב-PowerShell בכוונה — קובץ .ps1 בלי BOM נקרא כ-cp1255 והעברית שבתבנית הייתה
נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

נמדד בדפדפן אמיתי 1280x900 על הכתובת החיה, על פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (bkalot-clone-intake, מפתח anon מהטופס) ולא בהזרקה למסד —
75 ו-76, שני טלפונים שונים בכוונה. «הצג סיסמה» קיים (#226 נשאר סגור).

הפנייה נפתחה: h3 «שינוי סטטוס» קיים במסך החי, שלושת הכפתורים פעילים (new אינו
אחד מהיעדים), לכל אחד נימוק ב-title. לחיצה על «נסגרה» בייצור — «הסטטוס שונה
מ«חדשה» ל«נסגרה» · לא נשלחה שום הודעה»; «סטטוס» ו«עודכנה» זזו יחד (05:32→05:33),
אזור השגיאה נשאר מוסתר, והרשימה שמאחור התרעננה בלי שנגעו בה. אפס שגיאות ואפס
אזהרות בקונסולה לאורך כל הזרימה — 0 מתוך 0 הודעות, בניגוד למדידה המקומית של
2a067be שבה היה favicon.ico 404 של השרת הסטטי.

הבדיקה שמפרידה בין מצב לקוח למצב שרת, ובלעדיה הפעימה הייתה מוכיחה רק שהכפתור
מכבה את עצמו: «חזרה לרשימה» ואז פתיחה מחדש של #75 — openCase בונה את המסך מאפס
וקורא ל-bkalot_clone_admin_case — והכפתור נשאר כבוי עם אותו נימוק בדיוק
(«הפנייה כבר ב«נסגרה», ולכן אין מה לשנות»), וה-note ריק ומוסתר.

והמסנן: «נסגרה» + «סינון» → «פנייה אחת», #75 בלבד, והבקרה אינה ברשימה. זהו ערך
מסנן שעד 0071/0072 לא יכול היה להחזיר ולו שורה אחת בשום מצב — ועכשיו הוא מחזיר,
מהמסך החי.

מצב טסט נמדד ולא הוצהר: documents=0, outbound_queue=8, delivery_log=3, אפס
פניות ב-sent, אפס שורות עם sent_at, אפס שורות mode=live. הנתיב נגע בעמודה status
ותו לא — לא הופק מסמך, לא נכנסה שורה לתור, לא נכתבה שורת יומן ולא יצאה ולו
קריאה אחת. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

הבקרה: #76 נוצרה באותה ריצה ולא נגעו בה — נשארה new, ו-updated_at שלה שווה
בדיוק ל-created_at. אצל #75 ההפרש 55.7 שניות, והלחיצה היחידה היא ההפרש כולו.

המקור לא נגע, ונמדד ולא הונח: 8 שורות app_key=bkalot נשארו 8, טביעת אצבע
da28ec6315535ec0bfbebad4d57752a7 (id:status:attempts) זהה לפני ואחרי.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (פניות לפני אנשי הקשר —
cases_contact_id_fkey הוא ON DELETE SET NULL; ואז הסשן ומשתמש הניהול qa0075dep):
נמחקו 118 case_rights, 2 פניות, 2 אנשי קשר, סשן אחד ומשתמש ניהול אחד. אחרי:
cases=0, case_rights=0, documents=0, admin_users=1, admin_sessions=5, templates=2,
catalog=888, contacts=4, outbound_queue=8, delivery_log=3 — זהה בדיוק לבסיס.

אין נגיעה בשום נתיב מוגן: git status על apps/08-bkalut-app, apps/09-bkalot-admin
ו-supabase/functions ריק, ואין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873
ולא בסכמות csj/csj_src/igud.

מה שנשאר פתוח ולא נבלע: changed:false ו-status_not_settable אינם נגישים מהמסך
כי הכפתור של הסטטוס הנוכחי כבוי, ושני המסלולים נמדדו ב-HTTP ב-v6; «מי שינה
ומתי» אינו נכתב לשום מקום, ו-admin שחוזר בתשובה אינו מוצג במכוון; 'sent'
בלתי-ניתן-להשגה בכוונה עד שיהיה מסלול שליחה אמיתי; bkalot_clone_admin_create
עדיין אינה חשופה ב-HTTP; pdf/audio → channel_unsupported; בחירת תבנית — render
מקבל template_key והמסך אינו שולח אותו. public_visible ו-show_in_showcase של #37
נשארים false.
