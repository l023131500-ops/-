שכפול בקלות שכבה 3: הכפתור «הכנס לתור» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (18006b1) הותירה נוסח בה עצמה, גם בהודעת הקומיט וגם
ב-QA/bkalot-clone/queue-ui-0814: «הקובץ טרם נפרס — more30.com/bkalot-studio/admin
עדיין מגיש את הגרסה בלי הכפתור, וזו הפעימה הבאה». הוא נלקח כפי שנוסח ולא הורחב:
אין שינוי קוד, אין מיגרציה ואין פריסת edge function, ו-apps/37-bkalot-clone/admin.html
זהה בדיוק לזה שנכנס ל-18006b1. אותה חלוקה כמו c2a0a36 → 205c940.

למה זו פעימה בפני עצמה: המדידה הקודמת רצה מול שרת סטטי מקומי שמגיש את הקובץ
מהריפו מול ה-edge והמסד החיים. זו מדידה של הקוד ולא של המוצר — ה-rewrite
ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק לא
נבדקו בה כלל.

הבדיקה שרצה לפני הפריסה ולא הונחה: portal/dist נבנה ב-13/08 ויושב על הדיסק מאז,
ופריסה מ-dist ישן מחזירה את כל הפורטל אחורה בלי ששום דבר ידווח שגיאה. שמות הנכסים
המגובבים ב-dist/index.html הושוו לייצור — index-C2AHwa-z.js ו-index-B35ByRD-.css,
זהים בשני הצדדים. dist/bkalot-studio/admin.html היה 40,850 (בלי הכפתור) והמקור 45,982.

נמדד ב-HTTP אחרי הפריסה (dpl_9dZeZ4cWGKv3eAC98GEBtrx34jyc, READY, more30.com):
/bkalot-studio/admin עלה מ-35,205 ל-39,055 תווים ו-46,218 בתים, והדלתא מול המקור
היא 236 בדיוק — דלתת NetFree הידועה ולא מספר אחר; /bkalot-studio/admin/ מחזיר את
אותם בתים; הטופס /bkalot-studio נשאר 21,272 ו-more30.com/ נשאר 3,517 עם אותם שני
נכסים — בלי רגרסיה. בגוף שחזר: תווית «הכנס לתור», queueNote, queueErr, ששת קודי
השגיאה של 0067 (no_consent, no_contact, no_address, channel_unsupported,
document_empty, queue_row_missing), queue_status, queue_mode, showDocument
ו-sandbox. שפיות קידוד נמדדה ולא הונחה: 289 אל״פים אמיתיים ואפס סימני כפל-קידוד.

נמדד בדפדפן אמיתי 1280x900 על הכתובת החיה, לא על קובץ מקומי, על נתונים שנוצרו דרך
נתיב הקליטה האמיתי מעל HTTP ולא בהזרקה למסד: פניות 39 ו-40, disability, 59 זכויות
כל אחת, שני טלפונים שונים בכוונה (intake מאתר איש קשר לפי טלפון — #235).

מסך הכניסה מציג את הקופי המתוקן של 18006b1 («מפיק מהן את מכתב התשובה ומכניס אותו
לתור. אין ממנו שום שליחה», ו«אפשר להכניס אותו לתור — התור נעצר שם»), כלומר הקובץ
שבייצור הוא החדש ולא רק גדול יותר; «הצג סיסמה» קיים גם אחרי הפריסה (#226 נשאר
סגור); הכפתורים בשורת המסמך הם ["הצג", "הכנס לתור"] — וזה מה שהצעד קנה.

יעד ברשימת הבדיקה: «שורת תור #16 · נכנס עכשיו · במצב queued · יעד
qa.bkalot@more30.com · מצב test · 7697 בתים». לחיצה שנייה: אותו #16 ו«כבר היה בתור
— לא נוצרה שורה שנייה» — בלי זה שתי לחיצות היו שני מכתבים לאותו אדם. הקורא מסכים
עם הכותב: «הצג» החזיר «מסמך #34 · email · 3541 תווי טקסט · 5355 תווי HTML · בתור
#16 · queued · מצב test», ואורך הטקסט שהגיע ל-DOM הוא 3541 — שווה בדיוק למה
ש-render דיווח. יעד שאינו ברשימה: «שורת תור #17 · חסום — לא יישלח · יעד
qa.blocked.0814@more30.com», ובנוסח של המקור מילה במילה. אזור השגיאה מוסתר בשני
המסלולים — שורה חסומה אינה שגיאה. אפס שגיאות קונסולה לאורך כל הזרימה.

מה שנמדד כאן ולא נמדד במדידה המקומית: contentDocument של ה-iframe הוא null, כלומר
sandbox="" נאכף באמת ולא רק כתוב במאפיין. מאפיין שנקרא בחזרה מוכיח שהוא נכתב, לא
שהדפדפן מכבד אותו.

הבידוד ממנוע המקור נמדד אחרי הכתיבה מהדפדפן החי ולא הונח: max_attempts=0 בשתי
שורות השכפול, והפרדיקט המדויק של bkalot_auto.queue_due ו-queue_process_dryrun
מחזיר [6] בלבד — שורת המקור — ולא את 16 ואת 17. המעבד עצמו לא הורץ בכוונה: הרצתו
הייתה מעבדת את שורה 6 של המקור ומוסיפה שורת delivery_log. התוכן שנכנס לתור הוא
התוכן שהופק: q.body = d.body_html, q.subject = d.title ו-q.content_bytes =
octet_length(d.body_html), שלושתם true בשתי השורות.

מצב טסט נמדד ולא הוצהר: delivery_log=3 ללא שינוי, אפס שורות mode=live, אפס שורות
עם sent_at. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

המקור לא נגע: 8 שורות app_key=bkalot נשארו 8, וטביעת האצבע שלהן זהה לפני ואחרי —
aa6929ee28d13b34a01b5806d2598254.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (מסמכים לפני שורות התור — documents.queue_id
הוא FK; והפניות לפני אנשי הקשר — cases_contact_id_fkey הוא ON DELETE SET NULL
והסדר ההפוך היה מייתם פניות בשקט): 2 מסמכים, 2 שורות תור, 118 case_rights,
2 פניות, סשן אחד, משתמש ניהול 9, 2 אנשי קשר. אחרי: cases=0, case_rights=0,
documents=0, admin_users=1, admin_sessions=5, contacts=4, outbound_queue=8,
delivery_log=3, catalog=888 — זהה בדיוק לבסיס שלפני.

אין נגיעה בשום נתיב מוגן: git status על apps/08-bkalut-app ו-apps/09-bkalot-admin
ריק, ואין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות
csj/csj_src/igud.

מה שנשאר פתוח ולא נבלע: אין שולח (#234 סעיף 2) — השורה נכנסת לתור ונשארת שם, וזו
הלבנה הבאה; #235 (כתובת הנמען נקראת מאיש הקשר בזמן ההכנסה); cases.status נשאר new;
documents.kind=pdf/audio → channel_unsupported; ומסך הפנייה אינו יודע אילו מסמכים
כבר בתור לפני לחיצה. public_visible ו-show_in_showcase של #37 נשארים false.
