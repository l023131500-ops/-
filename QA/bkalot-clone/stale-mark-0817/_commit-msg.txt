שכפול בקלות שכבה 1: שדה תקין נשאר מסומן שגוי אחרי שהנפילה הבאה הייתה של משהו אחר (§5ב, UI)

‏markInvalid מסירה סימון קודם לפני שהיא מסמנת, ולכן כל עוד לנפילה **יש** שדה
הסימן נכון. מה שלא כוסה הוא נפילה בלי שדה, ואלה אינן מקרה קצה אחד אלא שתי
משפחות: network ו-bad_response חוזרות ב-`return fail(...)` מתוך ה-catch לפני
שהבדיקה על CODE_FIELD בכלל מגיעה, ו-rpc_failed, rpc_unreachable, body_too_large,
kind_invalid, source_invalid וכל קוד שאינו במפה מגיעים אליה ויוצאים ריקים. בכל
אחת מהן markInvalid אינה נקראת ואין מי שיסיר — והסימן של השליחה **הקודמת** נשאר
עומד.

מי שנפסל על הטלפון, תיקן אותו, ונפל בשליחה הבאה על משהו אחר לגמרי ראה את הטלפון
המתוקן מסומן כשגוי ליד הודעה שאינה מדברת עליו. ⚠️ וזה לא נשאר בגדר aria: מאז
8ef394c ‏(שנפרס ב-56bac47) הכלל צובע את aria-invalid, ולכן מהפריסה ההיא והלאה זה
שדה **אדום** על המסך. הפעימה שהביאה את הצבע היא זו שהפכה את הדליפה מהערה לתקלה
נראית, ולכן היא נסגרת מיד אחריה.

התיקון הוא חילוץ ולא כלל חדש: `clearInvalid()` היא בדיוק הלולאה שכבר הייתה בתוך
markInvalid ובתוך focusDropped, ושתיהן קוראות לה עכשיו במקום לחזור עליה. מה
שנוסף הוא קריאה אחת ב-`fail()` — ולא בכל ענף בנפרד, מפני ש-fail היא הנקודה
היחידה שכל נפילה עוברת בה, וכל סימון חדש נקבע תמיד **אחריה** (שלושת הענפים
נבדקו: 555, 561, 576 ו-580). החסימה המוקדמת אינה עוברת ב-fail והיא קוראת
ל-markInvalid, שמנקה ממילא.

⚠️ זו פעימת UI בלבד — אין פריסה. הייצור מגיש עדיין את גרסת HEAD; הפריסה היא
הפעימה הבאה. אין שינוי סכמה, אין מיגרציה, אין פריסת edge function ואין נגיעה
במסד — git status על supabase/ החזיר אפס שורות לפני ה-add ואחריו.

⚠️ טענה על «הסימן הקודם» אי אפשר למדוד בשליחה אחת, ולכן כל תרחיש כאן הוא בן שני
שלבים, ושלב 1 זהה בשלושתם (phone_invalid מהשרת). שלושה תרחישים, ושניים מהם הם
שתי משפחות הנפילה השונות ולא אותה אחת פעמיים:
  A — נפילה שנייה מהשרת בלי שדה: הטלפון מתוקן והערה בת 20,000 תווים ← 413
      body_too_large, שחוזר מה-edge function בשורה 58.
  C — נפילה שנייה מהלקוח: הבקשה מבוטלת ב-route.abort ← ה-catch מחזיר
      fail("network") ב-return, מסלול שאינו מגיע ל-CODE_FIELD בכלל.
  B — ⚠️ הבקרה שמכריעה שהתיקון לא שינה התנהגות היכן ש**יש** שדה: הטלפון מתוקן
      והשם רוקן ← full_name_required, והסימן חייב לעבור אל full_name.

מדידה, לא הנחה: בשתי הגרסאות שלב 1 סימן phone וצבע rgb(155,28,28) — זהה, ולכן
שלב 2 נמדד על אותו בסיס. ב-A וב-C לפני התיקון marked=["phone"] ו-rgb(155,28,28)
בעוד ערך השדה הוא 0500000000 התקין; אחרי — marked=[] ו-rgb(220,218,209), שהוא
‏--line, גבול השדה הרגיל. ⚠️ ההודעה עצמה זהה מילה במילה בשתי הגרסאות בשני
התרחישים — התיקון הוריד את הסימן ולא שינה מה שנאמר. ב-B ‏marked=["full_name"],
המיקוד על full_name וההודעה «צריך שם מלא.» — זהים לפני ואחרי, אות באות.

⚠️ הצבע נקרא מ-getComputedStyle על השדה שנמדד באמת ולא מתוך ה-CSS, וההמתנה היא
עד שהערך מפסיק לזוז ולא זמן קבוע — computed-style-measures-a-transition. גם
ההמתנה לתשובה אינה קצובה: היא עד שטקסט התוצאה **התחלף** ביחס לשלב הקודם ולכן
אינה יכולה למדוד את מסך שלב 1 בטעות.

⚠️ שני שרתים סטטיים באותה ריצה, מול אותו endpoint חי ואותו מסד. 8148 מגיש את
head/index.html — 36,127 בייט ו-MD5 30B03F39, בדיוק הקובץ ש-56bac47 מדד כנפרס
לייצור, ולכן «לפני» כאן הוא מה שמבקר רואה עכשיו ולא שחזור שלו. 8147 מגיש את
apps/37-bkalot-clone עצמו ולא עותק (BB48BD92). ENDPOINT הוא כתובת מוחלטת, ולכן
ההבדל היחיד בין הריצות הוא index.html. no-store בשרת —
gannenet-dev-served-by-stale-sw.

⚠️ מצב טסט נמדד ולא הוצהר. אף פנייה לא נוצרה, ולא מפני שנזהרנו: phone_invalid
ו-full_name_required הם `return` שיושבים ב-0080 בשורות 108 ו-99, מעל
`insert into bkalot_auto.contacts` שבשורה 162; body_too_large חוזר לפני ה-fetch
אל ה-RPC (שורה 58 מול 73); ו-C אינו יוצא לרשת כלל. 10 תשובות נקלטו וכל גוף נכתב
לקובץ: any_ok=false, any_case_id=false, והקודים בדיוק שלושה —
200:phone_invalid, 413:body_too_large, 200:full_name_required. אפס שורות, אפס
הודעות יוצאות, אין זריעה ואין מיגרציה.

⚠️ הקונסולה אינה ריקה, וזה נמדד ולא נבלע: A מפילה 413 ו-C מבטלת בקשה, והדפדפן
רושם שורת רשת על כל אחת. אלה התרחישים עצמם, ולכן הבדיקה אינה «אפס הודעות» אלא
שלוש: אפס pageerror (חריג JS שלא נתפס), כל שורה שנרשמה היא אחת משתי המושרות,
והרשימה זהה 2 מול 2 בשתי הגרסאות — התיקון לא הוסיף ולא הסתיר שורה.

26 בדיקות, PASS, אפס fails. שני צילומים בשני MD5 (A12CDC4D, EB958F37) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize; והגלילה כוונה אל הטלפון, מפני שהטענה
יושבת עליו. בצילום «לפני» נראים באותו מסך השדה 0500000000 באדום וההודעה «הפנייה
ארוכה מדי. קצרו את ההערה ונסו שוב.» — שני הדברים יחד הם התקלה.

⚠️ ה-heartbeat לא נכתב ל-core.run_progress. ה-MCP של Supabase אינו זמין בריצה
הזאת — ההארנס מדווח על המחבר כדורש הרשאה מחדש, מה שריצה לא-אינטראקטיבית אינה
יכולה לעשות. השורה נכתבה כקובץ: `_heartbeat-pending.sql`. ⚠️ הוא **השביעי**
בתור, ויש להריץ את כולם בסדר הקומיטים — `case-hit-row-deploy-0817`,
`allowlist-recheck-0817`, `which-field-0817`, `which-field-deploy-0817`,
`invalid-visible-0817`, `invalid-visible-deploy-0817`, ואז זה.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin ו-supabase,
נמדד לפני ה-add ואחרי ה-push; אין נגיעה ב-bkalut-app/bkalot-admin/zr_*/
NEDARIM3873 ולא בסכמות csj/csj_src/igud. ל-git add נמסרו שני נתיבים מפורשים ולא
נקודה. ההודעה נמסרה ב-commit -F — here-string-commit-fails-silently.

פתוח: התיבה (consent) עדיין אינה מכוסה בכלל הצבע — ⚠️ ונמדד למה זה עדיין אינו
מגיע לעין: 0080 מכניסה ל-dropped[] שלושה מפתחות בלבד (situation, topic_no,
documents, שורות 149/153/156) ו-consent אינו ביניהם, ואינו ב-CODE_FIELD, ולכן
אין היום מסלול שמסמן אותו. אם קוד עתידי יסמן — הוא יישאר בלי צבע, וגם
`el.value` ב-focusDropped אינו מסמן תיבה. ⚠️ וענף topic_no_invalid שמגיע מהשרת
עדיין לא נמדד בייצור — החסימה המוקדמת תופסת אותו לפני השליחה; שורה פתוחה
מ-3cff166 שגם הפעימה הזאת לא נגעה בה.

ראיות: `QA/bkalot-clone/stale-mark-0817/` — `measure.mjs` + `_measured.json` +
`measure-out.txt`, `head/index.html` (30B03F39), ושני צילומים:
`before-stale-phone-red.png`, `after-no-stale-mark.png`.
