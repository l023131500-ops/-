שכפול בקלות שכבה 1: «קוד: topic_no_invalid» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח נכתב ב-1bd2ff5 (heartbeat 597), מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio עדיין מגיש «קוד: topic_no_invalid», והמדידה כאן רצה
מול שרתים סטטיים מקומיים; זו הפעימה הבאה». נלקח כפי שנוסח ולא הורחב: פריסה
בלבד. אין שינוי קוד, אין מיגרציה, אין פריסת edge function —
bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin נשארה v7.

למה זו פעימה בפני עצמה: המדידה של 1bd2ff5 רצה מול שני שרתים סטטיים מקומיים
(8154/8155) מול ה-edge והמסד החיים — מדידה של הקוד ולא של המוצר. ה-rewrite
ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק
לא נבדקו בה כלל.

הפריסה: stage-portal.ps1 ואז npx vercel deploy --prod --yes מ-portal/dist
ובלי --prebuilt — בלי .vercel/project.json ה-CLI יוצר פרויקט בשם dist
ו-more30.com לא מתעדכן, ו---prebuilt מפיל את vercel.json כלומר את מפת
ה-rewrites של כל המערכות. dpl_6Gmiodb7CQJAhAgQ4FPD248UDqgW, production.

prev הועתק מ-portal/dist לפני ה-staging: 28,825 בתים, MD5 432A2D93 — בדיוק מה
ש-597 רשם כגרסה שבייצור, כלומר ה-dist שעל הדיסק היה החי ולא קובץ ישן. הנפרס:
31,164, 4F725737.

כל מסומן נספר בשתי הגרסאות לפני שנקבע — המלכודת של 530fb44, 29d6ac6 ו-e12acf9.
«צריך מספר שלם» נפסל כמסומן NEW (כבר בייצור מ-193da4a ומ-124cfc2) וירד לספירה:
4 בייצור → 5 במתוקן. ששת המסומנים ששימשו: 0 בייצור לפני, 1 במקור כל אחד.
1bd2ff5 אינה תוספת טהורה — שלוש שורות נמחקו כשרוכזו ל-markInvalid — ולכן
REMOVED נמדד ישירות: stop.el.setAttribute("aria-invalid","true") ו-stop.el.focus(),
שניהם 1 בייצור לפני ו-0 במקור.

HTTP לפני ואחרי: הטופס 29,061 → 31,400 בשתי הכתובות (עם לוכסן ובלעדיו — שני
rewrites נפרדים), אל״פים 272 → 304. הדלתא ב-HTTP היא 2,339 — בדיוק דלתת המקור
(31,164 − 28,825), ודלתת NetFree נשארה 236 בדיוק. מסך הניהול נשאר 90,056
(MD5 של admin.html זהה בשני הצדדים) ודף הבית 3,517 עם אותם שני נכסים מגובבים,
כלומר נמדדה גם רגרסיה ולא רק התוספת. אפס תווי החלפה ואפס סימני כפל-קידוד
בארבע הכתובות, לפני ואחרי. ששת המסומנים 0 לפני → 6 אחרי בשתי כתובות הטופס,
0 בניהול ובדף הבית; שני מסומני REMOVED 2 לפני → 0 אחרי. נכתב ב-node ולא
ב-PowerShell בכוונה: .ps1 בלי BOM נקרא כ-cp1255 והעברית שבתבנית נהרסת בזמן
הפירוק ומדווחת «לא נמצא» על טקסט קיים.

מה שקונה את הפעימה — דפדפן אמיתי 1280x900 על הכתובת החיה, «999999999999»
בהקשות מקלדת: בייצור לפני «אירעה תקלה. נסו שוב בעוד רגע.» ומתחתיה «קוד:
topic_no_invalid», מיקוד BODY, אפס aria-invalid. בייצור עכשיו «מספר הנושא אינו
תקין. תקנו ושלחו שוב.» ומתחתיה «מספר הנושא גדול מכל מספר נושא שקיים במערכת.»,
המיקוד על topic_no והוא מסומן. השרת החזיר 200 עם ok:false ו-detail עברי שלא
הוצג — הניסוח נקבע במסך, כפי ש-1bd2ff5 הכריע.

שתי בקרות בייצור: «1-5» — אפס בקשות אל ENDPOINT, badInput=true, «מספר נושא —
צריך מספר שלם», מיקוד וסימון; הריכוז ל-markInvalid לא שינה את החסימה המוקדמת,
ונמדד בייצור ולא רק מקומית. «7» — הבקשה יוצאת, פנייה #163 עם 42 זכויות,
«הפנייה נקלטה», אפס aria-invalid, המיקוד לא נחטף והטופס מתאפס. הוכחה דו-צדדית:
במסד נוצרה פנייה אחת בלבד — הבקרה — ולשני הניסיונות שנפסלו אין שורה באמצע.

שורת התוצאה ב-705 והשדה ב-272 בחלון של 900 — יחד מעל הקו; תקלת הראיות של
e12acf9 נבדקה ולא חזרה. שלושה צילומים, שלושה MD5 שונים. קונסולה: אפס הודעות.

מצב טסט נמדד ולא הוצהר: documents=0, אפס שורות outbound_queue עם sent_at,
outbound_queue נשאר 8, delivery_log נשאר 3, וטביעת האצבע 681dde58 זהה בבסיס,
בזמן המדידה ואחריה. התגלגל אחורה בשלוש פקודות נפרדות לפי id ולא CTE אחד
(case_rights 42 → cases 1 → contacts 1) — תקלת 124cfc2 — ואיש הקשר לפי id ולא
לפי טלפון — תקלת ac6e523. אחרי: cases=0, case_rights=0, documents=0,
admin_users=1, templates=2, contacts=4, outbound_queue=8, delivery_log=3,
catalog=888 — בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

ראיות: QA/bkalot-clone/topic-no-invalid-deploy-0814 (כולל verify-live.mjs —
אימות עצמאי חוזר מול הכתובת החיה: 31,400 בתים, 6 מסומנים חדשים, 0 מסומני
REMOVED, ניהול 90,056 ודף הבית 3,517 ללא תזוזה, אפס כפל-קידוד).

פתוח: החסימה המוקדמת שבמסך עדיין מעבירה מחרוזת ספרות ארוכה מכפי שהעמודה
מחזיקה — נמדד כאן בייצור: «999999999999» יצא ברשת ונפסל בשרת. עכשיו הוא נפסל
בעברית ועם מיקוד, ולכן זו שאלה של סיבוב מיותר ולא של נתון שאובד; חסימה מוקדמת
גם על הגבול הזה (TOPIC_NO_MAX כבר יושב בקובץ) היא הפעימה הבאה המתבקשת.
