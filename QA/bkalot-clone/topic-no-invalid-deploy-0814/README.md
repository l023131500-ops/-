# שכפול בקלות שכבה 1 — «קוד: topic_no_invalid» ירד מהמסך החי, ונמדד בייצור

הקו הפתוח נכתב ב-`1bd2ff5` (heartbeat 597), מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio עדיין מגיש «קוד: topic_no_invalid», והמדידה כאן רצה מול
שרתים סטטיים מקומיים; זו הפעימה הבאה, בדיוק כמו a6eb793 → cd5340e ו-124cfc2 →
4746bf3». נלקח כפי שנוסח ולא הורחב — **פריסה בלבד**: אין שינוי קוד, אין מיגרציה,
אין פריסת edge function. `bkalot-clone-intake` נשארה v1 ו-`bkalot-clone-admin`
נשארה v7.

## למה זו פעימה בפני עצמה

המדידה של `1bd2ff5` רצה מול שני שרתים סטטיים מקומיים (8154/8155) מול ה-edge
והמסד החיים — מדידה של הקוד ולא של המוצר. ה-rewrite ב-`vercel.dist.json`,
ההעתקה ב-`stage-portal.ps1` ו-236 הבייטים ש-NetFree מזריק לא נבדקו בה כלל.

## הקובץ שהיה בייצור הוא זה שנמדד כ-prev

`prev` הועתק מ-`portal/dist` **לפני** ה-staging: 28,825 בתים, MD5
`432A2D93D21581DACC5E835324D44091` — בדיוק מה ש-heartbeat 597 רשם כגרסה שבייצור,
כלומר ה-dist שעל הדיסק היה הגרסה החיה ולא קובץ ישן יותר. הנפרס: 31,164 בתים,
MD5 `4F7257379E3D1C88D86EB4CEEA96CDA8`.

## המסומנים

כל מסומן נספר **בשתי הגרסאות לפני שנקבע**, בדיוק המלכודת של `530fb44`, `29d6ac6`
ו-`e12acf9`. «צריך מספר שלם» נפסל כמסומן NEW — הוא כבר יושב בייצור מ-`193da4a`
ומ-`124cfc2` והיה מחזיר `true` על הגרסה שבייצור בזכות המופע האחר; הוא ירד ל-OLD
ונמדד כספירה (4 בייצור → 5 במתוקן).

ששת המסומנים ששימשו הם קוד או טקסט מסך, וכל אחד **0 בייצור לפני ו-1 במקור**:
`function whyTopicNoInvalid(sent) {`, `const TOPIC_NO_MAX = 2147483647;`,
`function markInvalid(el) {`, `if (code === "topic_no_invalid" && "topic_no" in
payload) {`, «מספר הנושא גדול מכל מספר נושא שקיים במערכת.» ו«מספר הנושא אינו
תקין. תקנו ושלחו שוב.».

`1bd2ff5` **אינה** תוספת טהורה — שלוש שורות נמחקו מהחסימה המוקדמת כשרוכזו
ל-`markInvalid` — ולכן REMOVED נמדד ישירות ולא כסמיכות: `stop.el.setAttribute
("aria-invalid", "true");` ו-`stop.el.focus();`, שניהם **1 בייצור לפני ו-0 במקור**.

## הפריסה

`stage-portal.ps1` (הוא עצמו מעתיק `apps/37-bkalot-clone/*.html` אל
`dist/bkalot-studio/`) ואז `npx vercel deploy --prod --yes` מ-`portal/dist`
**ובלי `--prebuilt`**: בלי `.vercel/project.json` ה-CLI יוצר פרויקט בשם `dist`
ו-more30.com לא מתעדכן, ו-`--prebuilt` מפיל את `vercel.json` כלומר את מפת
ה-rewrites של כל המערכות. `dpl_6Gmiodb7CQJAhAgQ4FPD248UDqgW`, production.

## HTTP לפני ואחרי

הטופס 29,061 → 31,400 בשתי הכתובות (עם לוכסן ובלעדיו — שני rewrites נפרדים),
אל״פים 272 → 304. **הדלתא ב-HTTP היא 2,339 — בדיוק דלתת המקור** (31,164 −
28,825), ודלתת NetFree נשארה 236 בדיוק ולא מספר אחר. מסך הניהול נשאר 90,056
(וה-MD5 של `admin.html` זהה בשני הצדדים) ודף הבית 3,517 עם אותם שני נכסים
מגובבים, כלומר המדידה מראה גם רגרסיה ולא רק את התוספת. אפס תווי החלפה ואפס
סימני כפל-קידוד בארבע הכתובות, לפני ואחרי. ששת המסומנים: **0 לפני, 6 אחרי**
בשתי כתובות הטופס, 0 במסך הניהול ובדף הבית; שני מסומני REMOVED: **2 לפני, 0
אחרי**.

נכתב ב-node ולא ב-PowerShell בכוונה: `.ps1` בלי BOM נקרא כ-cp1255 והעברית
שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## המדידה שקונה את הפעימה

דפדפן אמיתי 1280x900 על הכתובת החיה, «999999999999» בהקשות מקלדת ולא בהשמה
ל-`value`, ואותה תשובת שרת בדיוק שהתקבלה במדידה המקומית:

| | בייצור לפני (heartbeat 597) | בייצור עכשיו |
|---|---|---|
| שורה 1 | אירעה תקלה. נסו שוב בעוד רגע. | מספר הנושא אינו תקין. תקנו ושלחו שוב. |
| שורה 2 | **קוד: topic_no_invalid** | מספר הנושא גדול מכל מספר נושא שקיים במערכת. |
| מיקוד | BODY | `topic_no` |
| aria-invalid בדף | 0 | 1 (על `topic_no`) |

השרת החזיר `200` עם `{"ok": false, "error": "topic_no_invalid", "detail": "מספר
הנושא גדול מכל מספר נושא שקיים במערכת"}` — בקשה אחת יצאה, ה-`detail` העברי לא
הוצג והניסוח נקבע במסך, כפי ש-`1bd2ff5` הכריע. הערך «999999999999» עומד בשדה
אחרי הכישלון (`fail` אינו מאפס), והכותרת «הפנייה לא נשלחה» נכונה: לא נוצרה שורה.

**שתי בקרות בייצור:**

* **02 — «1-5»:** אפס בקשות אל ENDPOINT, `badInput=true`, «מספר נושא — צריך מספר
  שלם. תקנו ושלחו שוב.», מיקוד ו-`aria-invalid` על השדה. הריכוז ל-`markInvalid`
  לא שינה את החסימה המוקדמת — נמדד בייצור ולא רק מקומית.
* **03 — «7»:** הבקשה יוצאת, פנייה **#163** עם 42 זכויות, «הפנייה נקלטה», אין
  שורת שגיאה, אפס `aria-invalid`, המיקוד לא נחטף (BODY) והטופס מתאפס במלואו.
  התיקון אינו נדבק למסלול התקין.

**הוכחה דו-צדדית:** במסד נוצרה פנייה אחת בלבד — הבקרה #163 — ולשני הניסיונות
שנפסלו אין שורה באמצע.

שורת התוצאה נמדדה ב-705 והשדה ב-272 בחלון של 900 — שניהם בו-זמנית מעל הקו;
תקלת הראיות של `e12acf9` נבדקה ולא חזרה. שלושה צילומים, שלושה MD5 שונים
(438C2D41 / 80136F68 / C20E12BD). קונסולה: **אפס הודעות** לאורך שלוש הזרימות.

## מצב טסט — נמדד ולא הוצהר

`documents=0`, אפס שורות `outbound_queue` עם `sent_at`, `outbound_queue` נשאר 8,
`delivery_log` נשאר 3, וטביעת האצבע של התור `681dde58ed67b2094d0cbbd7c68e0fb6`
זהה בבסיס, בזמן המדידה ואחריה. המסך עצמו מצהיר: «🚫 מצב טסט: הפנייה נשמרה, ולא
יצאה ממנה שום הודעה». אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## התגלגל אחורה

שלוש פקודות נפרדות לפי id ולא CTE אחד (`case_rights` 42 → `cases` 1 →
`contacts` 1), וכל אחת בדקה את המספר שחזר — התקלה שנתפסה ב-`124cfc2`, שבה CTE
אחד קרא את התצלום שלפני הפקודה ודיווח הצלחה על 0 מחיקות. איש הקשר נמחק **לפי id
ולא לפי טלפון** (הטלפון נשמר מנורמל לספרות) — התקלה של `ac6e523`. אחרי:
`cases=0, case_rights=0, documents=0, admin_users=1, templates=2, contacts=4,
outbound_queue=8, delivery_log=3, catalog=888` — זהה בדיוק לבסיס שנמדד לפני.

נתיבים מוגנים: `git status` ריק על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions`; אין נגיעה ב-`bkalut-app`/
`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות `csj`/`csj_src`/`igud`.

## מה שנשאר פתוח

החסימה המוקדמת שבמסך עדיין מעבירה מחרוזת ספרות ארוכה מכפי שהעמודה מחזיקה —
נמדד כאן בייצור: «999999999999» יצא ברשת ונפסל בשרת. עכשיו הוא נפסל בעברית ועם
מיקוד, ולכן זו שאלה של סיבוב מיותר ולא של נתון שאובד; חסימה מוקדמת גם על הגבול
הזה (`TOPIC_NO_MAX` כבר יושב בקובץ) היא הפעימה הבאה המתבקשת.

קווים שממשיכים ולא נגעו: הקליטה מחברת 42 זכויות ל-`treatment` ואפס ל-`info` על
אותו `situation`; `await load()` שאחרי «שינוי סטטוס» מיותר ולא הוסר; אין סינון
«לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריית הכרעות; «מי הפיק» על
`documents` אינו נכתב לשום מקום; `${out.text_chars}` ו-`${d.html_chars}` נשארו
במספר; כותרות «זכויות (N)» ו«מסמכים (N)» לא נגעו. `sent` בלתי-ניתן-להשגה
בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP; pdf/audio →
`channel_unsupported`. `public_visible` ו-`show_in_showcase` של #37 נשארים false.
