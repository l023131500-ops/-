# 0076 — שינוי הסטטוס אומר מתי, ולעולם לא מי הכריע (§5ב)

**14.08.2026 · מיגרציה בלבד.** apps/37 לא נגעה, `bkalot-clone-admin` נשארה v6,
`supabase/functions` לא נגעה, אין פריסת פורטל.

## הקו שנסגר, ומי כתב אותו
0072 עצמה, בסעיף «מה שאין כאן ואינו נשכח»: «לא הומצאה כאן עמודה ולא נוסף
ארגומנט admin_id שאין לו לאן ללכת». תשע פעימות מאז חזרו על הקו הזה ולא נגעו בו.
כאן נבנה המקום שאליו הארגומנט הולך — שתי עמודות על `cases`, ו-set_status כותבת
אותן.

## מה נמדד

### הבסיס לפני
cases=0, case_rights=0, documents=0, contacts=4, admin_users=1, admin_sessions=5,
templates=2 (שתיהן enabled), catalog=888, outbound_queue=8, delivery_log=3.
טביעת אצבע המקור (app_key=bkalot, id:status:attempts): `e491999e1492c0dcf1cf88288bdec60a`.

### הרשאות — לפני ואחרי
ה-DROP מוחק ACL, ולכן זו אינה בדיקת טקס אלא הדבר היחיד שמפריד בין הפונקציה לבין
כל מחזיק מפתח anon:

| | חתימה | anon | authenticated | service_role |
|---|---|---|---|---|
| לפני | `bkalot_clone_admin_set_status(jsonb)` | false | false | true |
| אחרי | `bkalot_clone_admin_set_status(jsonb,bigint)` | false | false | true |

חתימה אחת בלבד אחרי — לא שתיים. שתי חתימות באותו שם היו הופכות קריאה עם ארגומנט
אחד ל-`function is not unique`, כלומר שוברות את נתיב /set-status החי.

### הנתיב החי שרד את ה-drop+create (HTTP, `probe.mjs` → `http.json`)
פניות שנוצרו דרך נתיב הקליטה הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon
מהטופס) ולא בהזרקה למסד: **#96** treatment עם 42 זכויות, **#97** בקרה.
כניסה לניהול מעל HTTP → admin id 25.

| קריאה | תשובה |
|---|---|
| set-status #96 → closed | 200, `changed:true`, `previous:new`, `decided_at:04:30:02.656066`, `decided_by:null` |
| set-status #96 → closed (שוב) | 200, `changed:false`, `decided_at:04:30:02.656066` — **אותה חותמת בדיוק** |
| set-status #96 → rejected, **`admin_id:1` בגוף** | 200, `changed:true`, `decided_by:null` |

**המדידה שקונה את הכרעה (1):** גוף הבקשה נשא `admin_id:1` והעמודה נשארה null.
נתיב /set-status מעביר את הגוף כפי שהוא ל-`p`, ו-`p_admin_id` הוא ארגומנט שני
שה-edge בונה בעצמו — ולכן הדפדפן אינו יכול לחתום הכרעה בשם מנהל אחר. באותה
תשובה עצמה יושב `admin:{id:25}` מהשער: הזהות קיימת, ואינה מגיעה לעמודה. זה
בדיוק מה שנשאר פתוח ל-v7.

### שלושת המצבים שעמודה אחת לא הייתה מבדילה ביניהם (הכרעה 2)

| פנייה | status | decided_at | decided_by | הקריאה |
|---|---|---|---|---|
| #94 אחרי הפקת מסמך (טריגר `documents_advance_case`) | in_progress | **null** | null | מכונה קידמה, אדם לא הכריע |
| #96 אחרי לחיצה מהמוצר (שער v6) | rejected | 04:30:04.616960 | **null** | אדם הכריע, זהותו אינה ידועה |
| #94 אחרי קריאה עם ארגומנט שני (`p_admin_id=25`) | closed | 04:30:38.615352 | **25** | אדם ידוע הכריע, ומתי |
| #97 (בקרה, לא נגעו בה) | new | null | null | לא הייתה הכרעה כלל |

#94 הוא המקרה שבגללו נכתבה העמודה: `updated_at` שלה זזה ב-58 שניות כשהטריגר
קידם אותה, ו-`decided_at` נשארה null. `updated_at` לבדו אינו מסוגל לומר את
ההבדל הזה.

הפרדת ה-now(): שלוש הקריאות רצו בטרנזקציות נפרדות. שאילתה אחת עם CTE-ים הייתה
מודדת אפס — כולן רואות אותו snapshot ו-now() קפוא בתוכן. (נמדד בדרך הקשה כאן:
קריאת `render` ובדיקת השורה באותה שאילתה החזירו את השורה לפני הטריגר.)

### הכרעה (4) — אין FK, ונמדד ולא הונח
משתמש הניהול 25 נמחק בזמן שהפנייה עדיין קיימת: `decided_by` נשאר **25**,
והשם שנפתר ב-LEFT JOIN חזר null. `ON DELETE SET NULL` היה מוחק כאן את זהות
המכריע מכל פנייה שהכריע בה ברגע שהחשבון נסגר.

### מצב טסט — נמדד ולא הוצהר
documents=1 בשיא הריצה ואפס מהם עם `queue_id`, אפס פניות ב-`sent`, אפס שורות עם
`sent_at`, אפס שורות `mode=live`, outbound_queue נשאר 8 ו-delivery_log נשאר 3.
הפונקציה נוגעת ב-`status`/`decided_by`/`decided_at`/`updated_at` ותו לא — אין בה
`net.http`, אין `pg_net` ואין ולו קריאה יוצאת אחת. אין מייל, אין הודעה ואין
מסמך שיצא החוצה.

### המקור לא נגע
8 שורות `app_key=bkalot` נשארו 8, טביעת אצבע `e491999e1492c0dcf1cf88288bdec60a`
זהה לפני ואחרי.

### הבדיקה התגלגלה אחורה במלואה
נמחקו: מסמך אחד, 84 case_rights, 4 פניות (94–97, כולל 94/95 שנוצרו בריצה ראשונה
שנפלה על שם נתיב שגוי), 2 אנשי קשר, סשן אחד ומשתמש ניהול אחד.
אחרי: cases=0, case_rights=0, documents=0, contacts=4, admin_users=1,
admin_sessions=5, templates=2 (שתיהן enabled), catalog=888, outbound_queue=8,
delivery_log=3 — **זהה בדיוק לבסיס**. שתי העמודות נשארו.

### אין נגיעה בשום נתיב מוגן
`git status` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`, `supabase/functions`
ו-`apps/37-bkalot-clone` ריק. אין נגיעה ב-bkalut-app / bkalot-admin / zr_* /
NEDARIM3873 ולא בסכמות csj / csj_src / igud.

## מה שנשאר פתוח ולא נבלע
- **השער אינו מעביר את הזהות.** `bkalot-clone-admin` נשארה v6 ומעבירה `{p: payload}`
  בלבד, ולכן כל לחיצה מהמוצר החי כותבת `decided_by:null`. הפונקציה מחזירה
  `admin:{id:25}` באותה תשובה — הזהות קיימת בשער ואינה עוברת הלאה. זו הלבנה
  הבאה (v7), ואחריה החזרת השדות בקריאה והצגתם במסך.
- **`decided_by=25` נמדד ב-RPC ולא מעל HTTP**, מאותה סיבה בדיוק — אין היום נתיב
  HTTP שיכול למלא את הארגומנט השני. זו סטייה מוצהרת ולא השמטה.
- **אין היסטוריה** (הכרעה 6): שתי העמודות אומרות את ההכרעה האחרונה בלבד, והכרעה
  קודמת נדרסת. «מי הכריע קודם» אינו נשמר.
- **«מי הפיק» על `documents`** עדיין אינו נכתב לשום מקום — קו נפרד, לא נסגר כאן.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP
  (משתמש הבדיקה נוצר ב-SQL ישיר); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
