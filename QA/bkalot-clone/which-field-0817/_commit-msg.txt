שכפול בקלות שכבה 1: הטופס אומר מה נפסל ואינו אומר איזה שדה זה (§5ב, UI)

‏0058 פוסלת פנייה ומחזירה קוד, והמסך תרגם את הקוד לעברית נכונה — «מספר הטלפון
אינו תקין», «צריך שם מלא», «בחרו מצב». מה שלא נאמר הוא **היכן**: בטופס בן שלושה
fieldset-ים ההודעה יושבת בתחתית העמוד, ומי שקרא אותה חיפש בעצמו את השדה שהיא
מדברת עליו. ומי שקורא מסך לא קיבל אפילו את זה — אף שדה לא נשא aria-invalid,
כלומר לא היה שום סימן שיש בכלל שדה שנפסל.

הפער היה חלקי ולא כללי, וזה מה שהופך אותו לפער ולא להחלטה: הפונקציה markInvalid
כבר קיימת בקובץ מ-1bd2ff5, וקוד אחד בדיוק — topic_no_invalid — כבר קרא לה.
חמשת הקודים האחרים של 0058 שנופלים כל אחד על שדה אחד ידוע לא קראו לה.

**מה נמדד, ולא הונח.** שני שרתים סטטיים באותה ריצה מול אותו endpoint חי ואותו
מסד: 8148 מגיש את HEAD (93ad034, 32,322 בתים, MD5 E6E39BF0) ו-8147 את עץ
העבודה. ‏ENDPOINT הוא כתובת מוחלטת, ולכן ההבדל היחיד בין הריצות הוא index.html
עצמו. שישה מקרים, כל אחד קוד אחר, וכל אחד נקרא מה-DOM בשלושה סימנים באותה
קריאה — מי מסומן aria-invalid, מי ממוקד בפועל (activeElement), ומה המסך אומר:

| קוד | שדה | HEAD | עץ העבודה |
|---|---|---|---|
| `full_name_required` | `full_name` | סימון: — · מיקוד: BODY | ✅ מסומן וממוקד |
| `phone_invalid` | `phone` | סימון: — · מיקוד: BODY | ✅ מסומן וממוקד |
| `email_invalid` | `email` | סימון: — · מיקוד: BODY | ✅ מסומן וממוקד |
| `email_required_for_treatment` | `email` | סימון: — · מיקוד: BODY | ✅ מסומן וממוקד |
| `situation_required_for_treatment` | `situation` | סימון: — · מיקוד: BODY | ✅ מסומן וממוקד |
| `topic_no_invalid` (בקרה) | `topic_no` | ✅ מסומן וממוקד | ✅ ללא שינוי |

הבקרה היא החצי השני של הטענה: הקוד היחיד שכבר עבד ב-HEAD מתנהג אחריה אות באות
זהה, כלומר הפעימה הוסיפה ולא החליפה. וההודעות עצמן לא זזו — `same_message`
אמת בשישה מתוך שישה, כלומר מה שהשתנה הוא היכן ולא מה.

**המיפוי נגזר ולא נבחר.** ‏CODE_FIELD נקרא מ-0058/0080 שורה-בשורה: כל קוד שבמפה
הוא ה-`return jsonb_build_object('ok', false, …)` שיושב מיד אחרי הבדיקה של אותו
שדה בדיוק. ‏`kind_invalid` ו-`source_invalid` **אינם** במפה — הטופס שולח בהם ערך
קבוע מתוך רשימה סגורה ואין להם שדה שמקלידים בו. ‏`dropped[]` לא נגעתי בו:
‏`focusDropped` נשארה במקומה כרשת שמתחת, ואינה עוסקת בפנייה שנפסלה אלא בפנייה
שנשמרה חסרה.

**⚠️ סימון רק לשדה שנראה.** שדות «טיפול מלא» יושבים ב-fieldset מוסתר בשני הסוגים
האחרים, ו-`focus()` על אלמנט ב-display:none נכשל בשקט ומשאיר aria-invalid על שדה
שאיש אינו רואה. התנאי `offsetParent !== null` מונע בדיוק את זה, ואינו מבטל אף
מקרה אמיתי: שני הקודים האלה מוחזרים ממילא רק כש-kind='treatment'.

**⚠️ מצב טסט — נמדד, לא הוצהר.** אף פנייה לא נוצרה, ולא בגלל זהירות אלא מפני
שכל חמשת הקודים הם `return` שיושב **מעל** יצירת ה-contact ומעל יצירת ה-case
ב-0058/0080 — כלומר הפסילה קודמת ל-insert הראשון. 12 תשובות נקלטו משתי הריצות:
`any_ok=false`, `any_case_id=false`, וחמישה קודים שונים בדיוק כמצופה. אפס שורות,
אפס הודעות יוצאות. אין כאן זריעה, אין גלגול-אחורה ואין מיגרציה.

**מה נמדד ולא תוקן, ונרשם ככזה.** לתכונה `aria-invalid` אין כלל CSS בקובץ, ולא
היה גם קודם — כלומר הסימן לעין הוא טבעת המיקוד בלבד. זה נכון היה גם ל-topic_no
לפני הפעימה, ולכן זו אינה רגרסיה אלא פער קיים; שינוי ויזואלי דורש מדידת ניגודיות
משלו והוא פעימה נפרדת. ‏POLISH.

**⚠️ ה-heartbeat לא נכתב ל-core.run_progress.** ה-MCP של Supabase אינו קיים
בריצה הזאת כלל — חיפוש כלים מפורש החזיר אפס התאמות, וההארנס מדווח על המחבר
כדורש הרשאה מחדש, מה שריצה לא-אינטראקטיבית אינה יכולה לעשות. זו התקלה השנייה
מבין השתיים שתועדו ב-3f32f63, וזו שאינה מתבהרת מעצמה. השורה נכתבה כקובץ:
`_heartbeat-pending.sql`. ⚠️ הוא **השלישי** בתור, ויש להריץ את שלושתם בסדר
הקומיטים — `case-hit-row-deploy-0817` ואז `allowlist-recheck-0817` ואז זה —
אחרת הפריסה תתוארך אחרי מדידה שבאה אחריה.

**זו פעימת UI בלבד — אין פריסה.** הייצור מגיש עדיין את גרסת HEAD; הפריסה היא
הפעימה הבאה, כמו בכל זוג UI/פריסה בשכבה הזאת.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin ו-supabase;
אין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.
ל-git add נמסרו שני נתיבים מפורשים ולא נקודה. ההודעה נמסרה ב-commit -F.

ראיות: `QA/bkalot-clone/which-field-0817/` — `measure.mjs` (ניתן להרצה חוזרת),
`_measured.json`, `head/index.html` (עותק ה-HEAD שנמדד מולו),
`after-phone-marked.png`, `after-situation-marked.png`.
