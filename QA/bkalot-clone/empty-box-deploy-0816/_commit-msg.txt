שכפול בקלות שכבה 3: התיבה שאמרה «שתואמות את הסינון» על מסך בלי סינון נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו נלקח כפי שנוסח ב-847ae58 (heartbeat 670), מילה במילה: «פתוח: הפריסה. מה
שנמדד כאן הוא הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) התיבה עדיין
אומרת «שתואמות את הסינון» על מסך בלי סינון. זו הלבנה הבאה». נלקח ולא הורחב:
פריסה בלבד — אין שינוי קוד, אין מיגרציה, אין פריסת edge function.
bkalot-clone-admin נשארת v9, bkalot-clone-intake v1, schema_migrations נשארה
20260816162924, ו-git status על apps/37-bkalot-clone החזיר אפס שורות לפני
ה-add ואחריו. מה שנכנס ל-commit הוא ראיות בלבד.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816162924. ו-HEAD היה 847ae58 וגם
origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 172,296 בייט ו-MD5 4D03D58B במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
169,253 (84442C69) — מה ש-4dd30e2 פרסה — כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (prebuilt-deploy-drops-vercel-json),
ו-vercel.json ו-.vercel/project.json נמדדו כנוכחים לפני הפקודה.
dpl_AWUGzYRLcSUHAPuNWiet1nJFKPSP, READY, production, aliased ל-more30.com.

הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist), בלי
שהכתובת החיה נפגשה ולו פעם אחת — 0087. dead_markers ריק בשתי הריצות.

הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה כל 20 הסימנים החזירו
מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת.

11 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי בשתי
הכתובות (עם לוכסן ובלעדיו): noneText (4), «const noneText = filtered ?»,
emptyBox (4), «const emptyBox = $("empty");», «emptyBox.hidden = cases.length
> 0;», «emptyBox.textContent = total === 0», «בעמוד הזה לא נשארו פניות.»
וארבע שורות ההערה. 2 מסומני REMOVED — «$("empty").hidden = cases.length > 0;»
והטרינרי הישן שבתוך head — עמדו 1 לפני ו-0 אחרי; שני אלה הם הראיה שהקוד הישן
לא נשאר לצד החדש. 2 מסומנים נשקלו כבקרות ונמדדו כ-DIFF, ונרשמים כפי שנמדדו
ולא כפי שתוכננו: «אין פניות שתואמות את הסינון» 5→7 ו«· מוצגות » 2→3 — ההערה
החדשה מצטטת את שני הנוסחים, ולכן הספירה עולה בלי שהתנהגות המסך תשתנה. 5
מסומני CONTROL לא זזו, והצמוד שבהם ctl_box_literal — המחרוזת ב-admin.html:304
שהתיבה מדפיסה כשהיא מוסתרת, והיא שנשארת במקומה מילה במילה.

live 169,489 בייט לפני ו-172,532 אחרי, בשתי הכתובות ובאותו MD5 — 236 בייט מעל
הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של תוכן. אפס תווי החלפה
ואפס «×» — deployed-html-double-encoded-cp1255.

הטענה עצמה נקראה מה-DOM בכתובת החיה ולא מקובץ. שני המצבים רצים על תור ריק,
ולכן לא נזרעה ולו פנייה אחת — הבסיס עצמו (cases 0) הוא מצב A. התחברות אמיתית
דרך הטופס, חשבון 126 שנוצר ב-bkalot_clone_admin_create ולא בהזרקת hash, ושני
המצבים באותה טעינה ובלי refresh — טעינה מחדש מנתקת את הסשן (admin.html:312).

  A  בלי סינון כלל — המונה «אין פניות» והתיבה «אין פניות.». שני המשפטים
     מסכימים, וזהו הכשל שנסגר: עד הפריסה הזו התיבה אמרה «אין פניות שתואמות
     את הסינון.» מעל אותו מונה בדיוק.
  B  q=zzzz — המונה והתיבה שניהם «שתואמות את הסינון». הבקרה הצמודה: המשפט
     המסנן לא נעלם כשהוא נכון, ו-count_title נדלק כאן ולא ב-A.

בקרות שלא זזו בשתי הקריאות: אפס שורות, pager מוסתר, אפס גלישה אופקית
ב-1280x900. הקונסולה נלכדה ולא נרשמה כחסרה: אפס הודעות ואפס תגובות שאינן 2xx.
שני צילומים בשני MD5 (B406C038, D64402CF) — screenshot-evidence-below-the-fold;
החלון שונה פעם אחת לפני הכל, playwright-blank-screenshot-until-resize, והגלילה
אל התיבה עצמה.

מה שנאמר ולא נבלע: מצב A לפני הפריסה לא צולם מהייצור — הפריסה כבר קרתה.
שהייצור הריץ את הקוד הישן נמדד מהסימנים (old_ternary 1, old_empty_line 1,
ואפס מכל 11 החדשים) ולא מקריאת DOM. מצבים C ו-D (תור עם פנייה) נמדדו מקומית
ב-847ae58 ואינם חוזרים כאן, כי הם דורשים זריעה. והענף total>0 עם אפס שורות
(«בעמוד הזה לא נשארו פניות.») נפרס ולא נמדד מדפדפן.

מצב טסט נמדד ולא הוצהר: אפס פניות נזרעו, אפס אנשי קשר, אפס שורות יומן,
outbound_queue נשאר 8, delivery_log נשאר 3, אפס שורות sent_at, אפס קריאות
יוצאות. מה שנפרס הוא HTML ואין בו כתיבה חדשה.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot.
נמחקו: admin_sessions של 126, admin_users 1 (126). לא נוצרו פניות, אנשי קשר או
שורות יומן ולכן אין מה למחוק שם. אחרי: בסיס בדיוק, ו-admin_cases על פרמטרים
ריקים מחזירה total 0.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add; אין נגיעה
ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. portal/dist gitignored.
ל-git add נמסר נתיב מפורש אחד. ההודעה נמסרה ב-commit -F —
here-string-commit-fails-silently.

פתוח: נתיב «התיבה הריקה אומרת את האמת» סגור מקצה לקצה — המסך (847ae58)
והפריסה כאן. הלבנה הבאה היא UI ולא פריסה. מה שלא נבנה ולא זז: אין «מתוך N
בסך הכול»; אין הדגשה ואין ציטוט של המילה בנימוק; אין חיפוש ב-cases.note; אין
סימון על שמות המכריעים; אין סינון «יש נימוק / אין»; אין סינון ומיון לפי מספר
הנימוקים או המעברים; אין אינדקס על case_status_log(note) ואין חיפוש טקסט מלא;
אין עריכה ומחיקה ביומן; אין סינון «איש קשר נמחק / קיים»; אין שמירת מיקום
הפנייה; אין אינדקס על ct.phone. שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
