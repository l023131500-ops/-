שכפול בקלות שכבה 3: classify.mjs נקרא כאילו הוא ניתן להרצה חוזרת, ואינו (§5ב, ראיות)

תיקון על d4ce4ed שנמצא מיד אחריו ואינו נוגע בפריסה. שני דברים:

(1) שני מפתחות ב-classify.json נשאו קידומת ctl_ בעוד שהסיווג המחושב שלהם הוא
    DIFF — הם נוסחו כבקרות ונמדדו כמשהו אחר. השמות ב-markers.mjs כבר תוקנו
    ל-phr_ לפני ה-commit, ו-classify.json נשאר עם הישנים; המספרים זהים ורק
    השמות הותאמו. הסיווג עצמו מחושב ואינו נטען, ולכן שם מטעה בקובץ ראיות הוא
    בדיוק מה שקורא עתידי יאמין לו במקום למספר.

(2) והחמור מהשניים: classify.mjs משווה את apps/37-bkalot-clone/admin.html מול
    portal/dist/bkalot-studio/admin.html, ו-stage-portal.ps1 מעתיק את הראשון
    אל השני. מרגע ה-staging שני הקבצים זהים והסיווג נמחק — הרצה חוזרת מחזירה
    18 CONTROL ו-2 DEAD ואפס NEW, כלומר פלט שנראה תקין לחלוטין ואינו מודד
    דבר. זה נמדד ולא הונח: הסקריפט הורץ שוב אחרי הפריסה והחזיר בדיוק את זה,
    ו-classify.json נדרס. הקובץ שוחזר ב-git checkout אל ההרצה שלפני
    ה-staging — היחידה שמכריעה — והאזהרה נכתבה בראש הסקריפט עצמו ולא בהערת
    commit שאיש לא יקרא בפעם הבאה.

לא נמדד ולא נטען: לא נבנתה כאן שמירה שתמנע את ההרצה החוזרת (למשל השוואת
mtime או דגל --pre-stage), רק הערה. הפריסה עצמה, הסימנים ושתי קריאות ה-DOM
מ-d4ce4ed לא נגעו ולא נמדדו מחדש.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase. ל-git add נמסרו שני נתיבים מפורשים.
ההודעה נמסרה ב-commit -F — here-string-commit-fails-silently.
