שכפול בקלות שכבה 3: «הופק על ידי» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח נכתב ב-042e60f (heartbeat 616), מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין אינו מציג את השורה, והמדידה כאן רצה מול שרת
סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר; זו הפעימה הבאה, אותו פיצול כמו
0083 → v8 → 0084». נלקח כפי שנוסח ולא הורחב — פריסה בלבד: אין שינוי קוד, אין
מיגרציה ואין פריסת edge function; bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin
v8. apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-042e60f (99,924 בתים,
MD5 85124B6E, git status ריק). מה שנכנס ל-commit הוא ראיות בלבד.

stage-portal.ps1 מעתיק את admin.html מהמקור בכל staging, וה-MD5 של הקובץ שב-dist
נבדק מול המקור ולא הונח — כדי שלא ייפרס עותק שני שנפרד מהראשון. 38 קבצים נפרסו,
מ-portal/dist ובלי --prebuilt (--prebuilt מפיל את vercel.json ואיתו את כל מפת
ה-rewrites). dpl_3g9SuzEuSGef7w9NT46RixnaWRVq, READY, aliased ל-more30.com.

תשעת המסומנים הם בדיוק אלה של 616, במכוון: שם חמשת ה-NEW החזירו 0 בייצור ו-≥1
במקור, וכאן הם התהפכו — «הופק על ידי: » 0→1, fillProducedCell 0→2,
produced_by_name 0→2, «הזהות נשמרה על המסמך בזמן ההפקה» 0→1, prodLine 0→5, בשתי
הכתובות (עם לוכסן ובלעדיו, שני rewrites נפרדים). ארבעת ה-OLD הם בקרה: שניים לא זזו
(fillCreatedCell 3=3, «תווי טקסט» 4=4) ושניים זזו בדיוק כצפוי (fillDecidedLine 4→5,
«החשבון אינו קיים עוד» 3→4). מסומן שמחזיר true על שתי הגרסאות אינו מודד דבר —
המלכודת של 530fb44, 29d6ac6 ו-e12acf9.

HTTP: מסך הניהול 96,802 → 100,160 בשתי הכתובות. הדלתא מול המקור 236 בדיוק — דלתת
NetFree הידועה, אותו הפרש כמו ב-612. הטופס נשאר 32,558 ודף הבית 3,517 כבקרה. אפס
תווי החלפה ואפס כפל-קידוד. נכתב ב-node ולא ב-PowerShell בכוונה.

הגרסה שהוגשה נקראה מתוך ה-DOM החי ולא רק ב-HTTP, ואז נמדדו שלושת המצבים על אותו
מסמך #130 בדיוק, על הכתובת החיה, בהתחברות אמיתית דרך המסך ועם cache-buster ולא על
הכתובת החשופה (תקלת f2001e0):
(1) לחיצת «הפק מסמך» על המסך → produced_by=61 במסד ו«QA 0816 מפיק בייצור» על המסך,
    עם מזהה המנהל ב-title. הזהות שיושבת ב-g.admin נסעה בשער v8 אל העמודה ומשם אל
    המסך, בייצור.
(2) bkalot_clone_render בארגומנט אחד — צורת v7, בדיוק מה שהפיק מסמכים לפני השער →
    produced_by=null ו«אינו ידוע».
(3) render עם p_admin_id=62 ואז מחיקת חשבון 62, ולא UPDATE ידני → produced_by=62,
    אפס שורות ל-62, ו«מנהל #62 · החשבון אינו קיים עוד». זו המדידה שקונה את ה-LEFT
    JOIN בייצור: המסמך שרד את מחיקת המפיק (produced_by אינו FK) והגוף נשאר שלם
    (2520 תווים אחרי המחיקה).

⚠️ הקריאה הראשונה של המצב השני החזירה span ריק בלי class ובלי title, ונראתה כמו
כישלון של המחלקה .stale. היא לא: fillProducedCell מקבלת את pspan ומוסיפה לתוכו span
פנימי, ולכן querySelector("span") מחזיר את החיצוני. במצב השם ה-title יושב על pspan
עצמו, ובשני המצבים ה«ישנים» על הפנימי. נמדד שוב עם querySelectorAll ונרשם ולא נבלע.

השורה נמדדה ב-top=681 בחלון של 900 — בתוך המסך אך בקצהו — וגוללה למרכז (438) לפני
כל צילום; תקלת e12acf9 נבדקה ולא חזרה. שלושה צילומים, שלושה MD5 שונים (0F14B902 /
7579E06B / 567B104A) — נבדקו ולא הונחו. אפס הודעות בקונסולה על הכתובת החיה.

מצב טסט נמדד ולא הוצהר: הקליטה החזירה queued=false, כל שלוש ה-render החזירו
queued=false, queue_id של המסמך null, אפס פניות ב-sent, outbound_queue נשאר 8 ואפס
שורות עם sent_at (טביעת אצבע 8cf33788a36d5f9618858835c89e30c1 זהה בבסיס ואחרי),
delivery_log נשאר 3. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

הפנייה נזרעה דרך bkalot-clone-intake מעל HTTP עם מפתח anon כמו מהטופס (#205,
contact 212, queued=false, 42 זכויות), ושני המנהלים נוצרו דרך
bkalot_clone_admin_create (61, 62) ולא בהזרקת hash. התגלגל אחורה בשש פקודות נפרדות
לפי id ולא CTE אחד — תקלת 124cfc2 — ואיש הקשר לפי id ולא לפי טלפון שנשמר מנורמל —
תקלת ac6e523; כל מחיקה החזירה מספר והמספר נבדק (documents 1, case_rights 42,
cases 1, contacts 1, admin_sessions 1, admin_users 2). אי-קיומו של חשבון 62 נמדד
בפקודה נפרדת משלו ולא באותו משפט שמחק אותו — תקלת cte-delete-sees-prestatement-snapshot.

אחרי: cases=0, case_rights=0, documents=0, contacts=4, admin_users=1,
admin_sessions=5, templates=2, outbound_queue=8, delivery_log=3, catalog=888 —
בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. ל-git add נמסר נתיב מפורש אחד ולא
נקודה — העץ מלא בקבצים לא-מנוהלים שאינם שייכים לפעימה.

פתוח: נתיב «מי הפיק» סגור מארבעה צדדים — עמודה (0083), שער (v8), קורא (0084), מסך
(042e60f) ופריסה (כאן) — ואין בו קו פתוח שנשאר. bkalot_clone_admin_case (בלוק
documents) ו-bkalot_clone_admin_cases אינן יודעות על produced_by, ולכן שורת המסמך
ברשימה עדיין שותקת — רק מסך המסמך מדבר. אין היסטוריית מפיקים ואין produced_at.
ה-title נמדד כתכונה ב-DOM ולא כטולטיפ שנפתח בריחוף. קווים שממשיכים ולא נגעו: אין
היסטוריית הכרעות; הקליטה מחברת 42 זכויות ל-treatment ואפס ל-info/reminder על אותו
situation. sent בלתי-ניתן-להשגה בכוונה; bkalot_clone_admin_create אינה חשופה
ב-HTTP; pdf/audio → channel_unsupported. public_visible ו-show_in_showcase של #37
נשארים false.
