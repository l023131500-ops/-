שכפול בקלות שכבה 3: כל ההופעות בנימוק נמדדו מדפדפן ולא היו בייצור (§5ב, פריסה)

הקו נלקח מ-f3304ec (heartbeat 679), מילה במילה: «פתוח: הפריסה. מה שנמדד כאן
הוא הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) «טופס» עדיין מקבל
סימן אחד מתוך שלושה. זו הלבנה הבאה». נלקח ולא הורחב: פריסה בלבד — אין שינוי
קוד, אין מיגרציה, אין פריסת edge function. bkalot-clone-admin נשארת v9,
schema_migrations נשארה 20260816192140, ו-git status על apps/37-bkalot-clone
ועל supabase/ החזיר אפס שורות לפני ה-add ואחריו. מה שנכנס ל-commit הוא ראיות
בלבד.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816192140. ו-HEAD היה f3304ec וגם
origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 185,393 בייט ו-MD5 4B887ECA במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
181,758 (6C7B27B3) — מה ש-6412e7f פרסה — כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (prebuilt-deploy-drops-vercel-json),
ו-vercel.json ו-.vercel/project.json נמדדו כנוכחים לפני הפקודה. target=production,
Ready.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist),
בלי שהכתובת החיה נפגשה ולו פעם אחת — 0087. DEAD ריק. ו-classify.mjs רץ לפני
ה-staging ולא אחריו: השמירה שנוספה ב-6849803 (יציאה בקוד 2 כש-dist זהה ל-src)
עמדה בדרך של הרצה חוזרת שנראית תקינה ואינה מודדת דבר.

⚠️ הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה כל 52 הסימנים
החזירו מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת. _verify.mjs
מכריע משלוש ראיות ולא מאחת — dist_equals_live לפני, src_equals_live אחרי,
ובקרות שלא זזו — ו-PASS על 52 סימנים בשתי הכתובות, עם לוכסן ובלעדיו.

24 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי:
note_match_pos_all (2), «const all = r.note_match_pos_all;», ה-lenOk, ה-inside,
«let spots = null, complete = false;», שמירת הסדר העולה
(all.every((p, k) => ...)), «spots = all; complete = true;», הנפילה חזרה
«spots = [r.note_match_pos];», «if (!spots)», «const title = !complete»,
שני נוסחי ה-title (יחיד ורבים), הלולאה «for (const p of spots)»,
«cur = i + len;», הזנב, «m.title = title;», וששת בלוקי ההערות (8)–(11)
וההערות שתוקנו. ⚠️ 10 מסומני REMOVED עמדו 1 לפני ו-0 אחרי — «const pos =
r.note_match_pos, len = r.note_match_len;», «const fits =», השמירה הישנה,
«if (!fits)», «const i = pos - 1;», ה-append המשולש, «להתפצל לשלושה צמתים»,
«הצומת האמצעי הוא היחיד שנעטף» ושתי ההערות (2) ו-(6) הישנות; אלה הראיה
שהקוד הישן לא נשאר לצד החדש. 2 מסומני DIFF הגיעו בדיוק לספירת המקור:
«כאן נמצא מה שהוקלד בחיפוש» 1→3 ו-note_match_pos 2→6.

⚠️ ונרשם כפי שנמדד ולא כפי שתוכנן: «מסומנת ההופעה הראשונה בלבד» נשקל כ-REMOVED
ונמדד כ-CONTROL, 1 לפני ו-1 אחרי. הכרעה (9) משאירה אותו במכוון — הוא ה-title
של מסלול הנפילה חזרה, כששרת מלפני 0101 אינו מחזיר את המערך. הסימן הועבר
בקובץ הסימנים אל מקומו הנכון, ולא נמחק כדי שהתוצאה תיראה נקייה. 16 מסומני
CONTROL לא זזו בכלל, ובהם document.createTextNode 19 לפני ו-19 אחרי — הפונקציה
נכתבה מחדש ולא נוסף בה ולו צומת אחד שאינו טקסט.

live 181,994 בייט לפני ו-185,629 אחרי, בשתי הכתובות ובאותו MD5 — 236 בייט מעל
הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של תוכן. אפס תווי החלפה ואפס
«×» — deployed-html-double-encoded-cp1255.

הטענה עצמה נקראה מה-DOM בכתובת החיה ולא מקובץ. הזריעה: פנייה אחת ושלוש שורות
יומן, באותם שלושה טקסטים שעליהם נמדדו 0101 ו-f3304ec. #410 (contact 417)
«אברהם ישראלי» 0501230101, דרך public.bkalot_clone_intake (kind=info,
source=form, queued=false, rights_linked 0), ואחריה שלוש הכרעות ב-
bkalot_clone_admin_set_status (מנהל 4): log 144 «Email נשלח לפונה, ואחריו
EMAIL נוסף בטעות» (41 תווים), log 145 «טופס, טופס ועוד טופס נשלחו בטעות» (32),
log 146 «המסמכים שצורפו שייכים לשנת המס הקודמת» (37). ⚠️ שלוש, שתיים ואחת
במכוון: מסך שמצייר «כולן» חייב להיבדק גם היכן שאין לו מה להוסיף, ו-146 היא
הבקרה הזו. ⚠️ ונאמר ולא נבלע: הקליטה נעשתה ב-SQL ולא מעל HTTP — פיגום ולא
הטענה. התחברות אמיתית דרך הטופס, חשבון 132 שנוצר ב-bkalot_clone_admin_create
ולא בהזרקת hash.

⚠️ המדידה שקדמה למסך, מהמסד: note_match_pos_all «email» → [1,26], «טופס» →
[1,7,17], «המס הקודמת» → [28] — כלומר השדה שהמסך צריך עונה גם בייצור.

ארבעה מצבים, וכל שורה נקראה מה-DOM ולא הוסקה. ⚠️ «טופס» → שלושה סימנים ותשעה
צמתים, [ריק, MARK, «, », MARK, « ועוד », MARK, « נשלחו בטעות», BR, SPAN],
ו-title «3 הופעות בטקסט הזה, וכולן מסומנות» — זה הכשל שנסגר; עד הפריסה הזו
צייר הייצור סימן אחד מתוך שלושה. «email» → שני סימנים, «Email» ו«EMAIL», שתי
צורות אותיות ושתיהן אות באות כפי שהוקלדו על מונח שהוקלד קטן. «המס הקודמת» →
סימן אחד ו-title «הופעה אחת בטקסט הזה, והיא מסומנת» — הכרעה (11) בעברית ולא
«1 הופעות», וההתאמה בסוף המחרוזת ולכן הצומת האחרון ריק. ⚠️ ובקרה בלי מונח
כלל: שלוש שורות, אפס סימנים, אפס «זה הנימוק שהתאים», וצומת טקסט אחד בכל תא.

בקרות שלא זזו: td.textContent שווה לנימוק השמור בכל התאים שנקראו — הפיצול
ל-2N+1 צמתים אינו מוסיף ואינו גורע תו; ה-title של התא נשאר «41/32/37 תווים,
כפי שנשמרו»; class="hit" בכל הסימנים; ואפס גלישה אופקית ב-1280x900
(scrollWidth 1265 = clientWidth). הקונסולה נלכדה ולא נרשמה כחסרה: אפס הודעות,
אפס שגיאות ואפס אזהרות.

שלושה צילומים בשלושה MD5 (588D90FA, 7CB226CC, DD017C01) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize, והגלילה אל הסימן עצמו.

⚠️ נאמר ולא נבלע: מצבי הדפדפן שלפני הפריסה לא צולמו מהייצור — הפריסה כבר קרתה.
שהייצור הריץ את הקוד הישן נמדד מ-52 הסימנים (live-before.json) ולא מקריאת DOM.
ו-gannenet-dev-served-by-stale-sw: כל קריאה נעשתה עם cachebust, והכתובת החשופה
לא נמדדה לבדה.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, case_rights 0,
outbound_queue נשאר 8, delivery_log נשאר 3, אפס קריאות יוצאות. מה שנפרס הוא
HTML שמצייר טקסט — הנתיב שהשתנה אינו קורא ואינו כותב.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot;
ונמדד לפני המחיקה ולא הונח שאין שורות בנות: log 3, case_rights 0, documents 0,
admin_sessions של 132 שתיים. ⚠️ contacts יושבת ב-bkalot_auto ולא ב-bkalot_clone.
נמחקו case_status_log 3 (144–146), cases 1 (410), contacts 1 (417),
admin_sessions 2, admin_users 1 (132), בסדר הזה. אחרי: בסיס בדיוק,
ו-admin_cases על פרמטרים ריקים מחזירה total 0.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add; אין נגיעה
ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. portal/dist gitignored.
ל-git add נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה ב-commit -F —
here-string-commit-fails-silently.

פתוח: נתיב «כל ההופעות מסומנות» סגור מקצה לקצה — המסד (0101), השער (v9, בלי
שינוי), המסך (f3304ec) והפריסה כאן. הלבנה הבאה היא UI ולא פריסה. מה שלא נבנה
ולא זז: אין ציטוט חתוך של הקטע; אין ספירת ההתאמות ברשימת העבודה (0098 סופרת
נימוקים, לא התאמות בתוכם); אין סימון על note של הפנייה עצמה; אין פירוק של
מספר התור; אין חיפוש ב-cases.note; אין סימון על שמות המכריעים; אין סינון «יש
נימוק / אין»; אין סינון ומיון לפי מספר הנימוקים או המעברים; אין אינדקס על
case_status_log(note) ואין חיפוש טקסט מלא; אין עריכה ומחיקה ביומן; אין סינון
«איש קשר נמחק / קיים»; אין שמירת מיקום הפנייה; אין אינדקס על ct.phone.
⚠️ שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
