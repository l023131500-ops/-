# מספר נושא ארוך מכפי שהעמודה מחזיקה — 502 עם נוסח המנוע בתוכו (0080)

**מי כתב את הקו הזה.** 41ccad1 (0079), בשורה האחרונה שלה: `'^[0-9]+$'` מקבל
מחרוזת ארוכה מכפי שנכנסת ל-`integer`, וה-cast זורק 22003 שאיש אינו תופס.

**מה נמדד לפני המיגרציה.** POST אל `/functions/v1/bkalot-clone-intake` עם מפתח
`anon` (PowerShell, לא דפדפן), `kind=treatment`, `situation=young_single`,
`topic_no="999999999999"`:

```
HTTP 502
{"ok":false,"error":"rpc_failed","status":400,
 "detail":"{\"code\":\"22003\",\"details\":null,\"hint\":null,
            \"message\":\"value \\\"999999999999\\\" is out of range for type integer\"}"}
```

שתי תקלות בתשובה אחת: הקורא מקבל **502** — «השרת נפל» — על שדה שהוא עצמו מסר
וטעה בו; ובתוך התשובה יוצאת **הודעת השגיאה הפנימית של המנוע**, קוד SQLSTATE
ונוסח באנגלית, לקורא ציבורי.

אפס פניות נוצרו גם לפני התיקון — החריגה מפילה את כל הפונקציה — ולכן זו תקלת
תשובה ולא תקלת נתונים.

## מה נמדד אחרי — שש בקשות, אותו קורא

| | בקשה | תשובה |
|---|---|---|
| **A** | `treatment` + `topic_no="999999999999"` | **200** `ok:false`, `topic_no_invalid`, detail «מספר הנושא גדול מכל מספר נושא שקיים במערכת» |
| **B** | `treatment` + `topic_no="2147483647"` | 200 `ok:true`, case 158, `dropped []`, 36 זכויות — **הגבול עצמו מתקבל** |
| **C** | `treatment` + `topic_no="2147483648"` | 200 `ok:false`, `topic_no_invalid` — **אחד מעל הגבול נפסל** |
| **D** | `treatment` + `topic_no="שבע"` | 200 `ok:false`, `topic_no_invalid`, detail «מספר הנושא הוא מספר שלם» — 0079 לא זזה |
| **E** | `treatment` + `topic_no="7"` | 200 `ok:true`, case 159, `dropped []`, 36 זכויות |
| **F** | `info` + `topic_no="999999999999"` | 200 `ok:true`, case 160, `dropped ["topic_no"]` — נתיב ההשמטה נשאר |

B ו-C יחד הן הבדיקה האמיתית: הגבול הוא **גבול העמודה** (`bkalot_clone.cases.topic_no`
היא `integer` — נמדד ולא הונח), ולא מספר "סביר" שהומצא כאן. השורה של B במסד יצאה
`topic_no = 2147483647` — כלומר הערך שהתקבל אכן נשמר ולא נחתך.

F היא הבקרה שמראה שהמיגרציה לא זלגה: בסוגים שאינם טיפול מלא השדה אינו נשאל,
והוא ממשיך ליפול אל `dropped[]` בדיוק כשהיה.

**הרשאות לא זזו:** `anon` false, `authenticated` false, `service_role` true —
לפני ואחרי, נמדד ולא הונח.

**מצב טסט:** `documents` 0, `outbound_queue` 8, אפס `sent_at`, `delivery_log` 3,
`rights.catalog` 888 — חמישתם זהים למה ש-heartbeat 595 רשם. אין שינוי סכמה,
אין נגיעה ב-edge function, אין נגיעה ב-`apps/37`.

**התגלגל אחורה** בשלוש פקודות נפרדות לפי id (`case_rights` → `cases` →
`contacts`), לא ב-CTE אחד: ב-CTE יחיד כל ענף קורא את הצילום שלפני הפקודה,
ומחיקת ההורה מדווחת הצלחה בעוד שורות הבן נשארות. אחרי: `cases` 0,
אפס `case_rights` יתומות, `contacts` 4 — בדיוק מצב הבסיס.

## מה שנשאר פתוח

`MESSAGES` ב-`apps/37-bkalot-clone` עדיין אינו מכיר `topic_no_invalid` (קו פתוח
מאז 0079). דפדפן ששולח `"999999999999"` עובר את החסימה המוקדמת — הרגקס שלה מקבל
מחרוזת ספרות באורך כלשהו — ומעכשיו יראה «קוד: topic_no_invalid» במקום 502 אטום.
שתיהן הודעות שאינן ראויות למסך; זה צעד במסך ולא במסד.
