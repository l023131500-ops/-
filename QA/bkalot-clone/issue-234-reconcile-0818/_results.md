# #234 reconcile — 18/08/2026

## מה נבדק
core.issues #234 ("שכפול בקלות שכבה 3: למסמך יש נתיב כניסה לתור ואין לו ולו כתובת
HTTP אחת, ואין שולח") נשאר `status=open` במסד, למרות שהקוד וההיסטוריה מראים
ששני הסעיפים שנפתחו בו נסגרו במלואם, כולל פריסה, בסבב הקומיטים:

- ab63177 — נתיב `queue` מאחורי שער הסשן ב-`bkalot-clone-admin` v5.
- 3e8b1fd — `bkalot_clone_dispatch` (0070), עיבוד מצב-בדיקה, service_role בלבד.
- 37235a3 — כפתור «עבד עכשיו» ב-`apps/37-bkalot-clone/admin.html` (dispatch-ui-0814).
- e12acf9, 3750c2e, c3338c5, 2346adb, 8e8bce4 ועוד — סבב תיקוני UI+פריסה נוספים
  לשכבה 3 שקרו אחרי 14/08.

## מה אומת עכשיו מול הייצור (לא מדפדפן מקומי, לא מקובץ)
`Invoke-WebRequest https://more30.com/bkalot-studio/admin?cachebust=<rand>`:

- אורך תגובה: 140,815 תווים.
- מכיל `הכנס לתור` (#234 סעיף 1 — נתיב queue + כפתור) — קיים.
- מכיל `עבד עכשיו` (#234 סעיף 2 — נתיב dispatch + כפתור) — קיים.

כלומר הפער היחיד שנרשם בכרטיס תחת "[14/08 0070] נשאר פתוח" — "הקובץ טרם נפרס" —
כבר לא קיים: הגרסה החיה מגישה את שני הכפתורים.

## מה נשאר פתוח בכוונה, לא באג
- `cases.status` נשאר `'new'` גם אחרי עיבוד מוצלח — הכרעת מוצר מתועדת במפורש
  בכרטיס ("'in_progress' הוא החלטת מוצר ולא תיקון"), לא קוד חסר.
- `documents.kind='pdf'/'audio'` → `channel_unsupported` — אין ערוץ לסוגים האלה
  בשכפול, מגבלה מתועדת, לא נסיגה.
- מצב טסט: `mode='test'` קשיח בקוד; אין נתיב live. זה בכוונה (§5ב, אין הודעה
  אמיתית בלי אישור מפורש).

## מסקנה
שני הסעיפים שהיו פתוחים בכרטיס (נתיב HTTP+כפתור queue, נתיב HTTP+כפתור dispatch)
סגורים ומאומתים בייצור. אין שינוי קוד/פריסה בצעד הזה — בדיקת-רישום בלבד, אותו
דפוס בדיוק כמו #237 (17/08): העבודה כבר נעשתה ונפרסה, והרישום ב-core.issues פיגר.
