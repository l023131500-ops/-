# «זכות אחת» ו«מסמך אחד» נמדדו מדפדפן ולא היו בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (e3e1af1) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה שאומרת «1 מסמכים»,
והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא את המוצר
(ה-rewrite, ההעתקה ל-dist ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה
הבאה, בדיוק כמו 13516f0 → b03d393».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-e3e1af1 (88,067 בתים,
MD5 `9055A63E89E3004D84D8B464B3C7F5BE`, `git status` ריק) ו-`bkalot-clone-admin`
נשארה v7. אותה חלוקה כמו 13516f0 → b03d393, 1abad72 → f2001e0 ו-e9e949c → 530fb44.

## שלוש רשימות מסומנים ולא אחת

`markers.mjs` מחזיק את השלוש במקום אחד, כדי שהבדיקה המקומית וה-HTTP ימדדו בדיוק
את אותו דבר:

- **NEW** — false בגרסה שבייצור, true אחרי. אלה שקונים את הפעימה.
- **REMOVED** — ההפך: true לפני, false אחרי. בלעדיהם «נפרס» יכול להיות גם
  «נפרסו שתי הגרסאות זו על גבי זו», וזה בדיוק מה ששני קבצים באותו גודל נראים כמו.
  כאן זה ממשי: `const many = (n) => (n === 1 ? "פנייה אחת" : ` ו-
  `${c.rights_count ?? 0} זכויות` הן השורות שהוחלפו, ואם הן עדיין בייצור —
  משהו בדרך החזיר את הקובץ הישן.
- **OLD** — true בשתי הגרסאות. אינם ראיה לפעימה אלא בקרת רגרסיה: פריסה מ-dist
  ישן מחזירה מסכים אחורה בלי ששום דבר ידווח שגיאה.

`«פנייה אחת»` **לבדו** יושב ב-OLD ולא ב-NEW: הוא היה במסך מאז שהמונה נכתב,
וההבדל הוא בשתי השורות שמתחתיו. זו בדיוק המלכודת ש-530fb44 («נוסח נפילה»)
ו-29d6ac6 («החשבון אינו קיים עוד») נתפסו בה — מסומן שהיה true גם לפני ונקרא
כאילו הוא מעיד. המסומן הזה נמדד true בארבעת הקבצים, כצפוי.

| | NEW | REMOVED | OLD |
|---|---|---|---|
| `git show e3e1af1^` (הגרסה שבייצור) | **כולם false** | כולם true | כולם true |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true | **כולם false** | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | **כולם false** | כולם true | כולם true |
| `portal/dist/…/admin.html` אחרי ה-staging | כולם true | **כולם false** | כולם true |

`prev/admin.html` חולץ ב-`execFileSync` עם `encoding:"buffer"` ולא דרך צינור
PowerShell — צינור מוסיף BOM והופך בייטים, ואז ההשוואה מודדת את הצינור ולא את
הקובץ. 86,588 בתים, MD5 `0F72749FA8162820B5F86E3AAB1937A4` — **בדיוק** מה
ש-`portal/dist` החזיק לפני ה-staging, כלומר ה-dist שעל הדיסק היה הקובץ שבייצור
ולא קובץ ישן יותר. אחרי ה-staging שניהם 88,067 עם אותו MD5, ו-38 קבצים כמו
בפריסה הקודמת. שמות הנכסים המגובבים — `index-C2AHwa-z.js`, `index-B35ByRD-.css`
— זהים בשני הצדדים לפני ואחרי.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 86,824 | **88,303** |
| `/bkalot-studio/admin/` | 86,824 | 88,303 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 881 | **909** |

הדלתא מול המקור היא **236 בדיוק** (88,303 − 88,067) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים — כלומר
המדידה מראה גם רגרסיה ולא רק את התוספת.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin?cb=qa0083`, על שתי פניות שנוצרו דרך
נתיב הקליטה הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא
בהזרקה למסד: **#120** תיגע, **#121** בקרה. כניסה לניהול נעשתה במסך עצמו, מנהל 39
(«QA 0083 פריסה»). הסקריפט לא הפיק ולא הכריע דבר — שתי השורות נוצרו `new`, וכל
שינוי שנראה אחר כך הוא תוצאת לחיצה בדפדפן.

**המדידה שקונה את הפעימה.** אותה שורה, שני חלקיה, בייצור:

| | #120 (תיגע) | #121 (בקרה) |
|---|---|---|
| לפני הלחיצה | **«זכות אחת»**, 8 `span`, «חדשה» | «42 זכויות», 8 `span`, «חדשה» |
| אחרי «הפק מסמך» ו«חזרה לרשימה» | **«זכות אחת · מסמך אחד»**, 10 `span`, «בטיפול», «לא הוכרע מהמסך» | «42 זכויות», 8 `span`, «חדשה» |

לפני הפריסה אותה שורה בדיוק הייתה אומרת «1 זכויות» ו«1 מסמכים» — זה מה
ש-e3e1af1 מדדה מקומית על שתי גרסאות באותה ריצה, וזה מה שכל אדם שנכנס למסך ראה
עד עכשיו. המסמך הופק בדפדפן ב«הפק מסמך», כלומר זה מה שרואים אחרי הפעולה הרגילה
ביותר במסך, ולא מצב שהוזרק.

**הבקרה שאין לה תחליף:** #121 נוצרה באותה ריצה ובאותה צורה בדיוק ולא נגעו בה —
«42 זכויות», 8 `span`, «חדשה», זהה בשני הצילומים. ההבדל הוא בין מצב לניסוח שלו
ולא בין שתי פניות. הבקרה גם מוכיחה שהריבוי לא נשבר: מספר גדול מ-1 עדיין נקרא
במספר.

בלי רגרסיה: «2 פניות» בשני השלבים, אחרי החזרה גלוי `screen-list` בלבד
(`screen-case: none`), והסימן «לא הוכרע מהמסך» — התוספת של 073f08b — עדיין
מופיע. ה-`bits` נקראו מה-DOM ולא מהצילום.

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — ארבע הודעות `INFO`,
כולן של NetFree על טעינת גופנים מ-`netfree.link` ולא של המסך.

שני הצילומים נבדקו ב-`Get-FileHash` והם שונים:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-one-right.png` | 38,012 | `BAC41C844A2B574CECA9B289A88A2FD8` |
| `02-live-one-doc.png` | 40,050 | `6F3D02EBFC6180A506BAB9E5099E9187` |

## מה שנרשם ולא נבלע

`rights_count=1` אינו מצב שנתיב HTTP יכול לייצר — הקליטה מחברת כל שורת קטלוג
שתואמת למצב (42 כאן) — ולכן #120 קוצצה ל-`case_rights` אחת ב-`DELETE` ישיר,
כמו ב-e3e1af1. `bkalot_clone_admin_create` אינה חשופה ב-HTTP, ולכן משתמש הבדיקה
נוצר ב-RPC ישיר; הכניסה עצמה נעשתה במסך.

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, שורות `outbound_queue` עם `sent_at` = 0, שורות `mode=live`
= 0, מסמכים עם `queue_id` = 0, `delivery_log` נשאר 3, ו-`outbound_queue` נשאר 8
גם אחרי הפקת המסמך — «הפק מסמך» כותב שורה ב-`documents` ואינו מכניס לתור. טביעת
האצבע של התור (`51f64bdd93798e8f15a26581f1dea677`) זהה בבסיס, בזמן המדידה
ואחריה. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## גלגול אחורה

מסמך אחד, 43 `case_rights`, 2 פניות (120/121), 2 אנשי קשר, סשן אחד ומשתמש הניהול
39 — כולם נמחקו בסדר הנכון. אחרי: `cases`=0, `case_rights`=0, `documents`=0,
`admin_users`=1, `admin_sessions`=5, `contacts`=4, `templates`=2, `catalog`=888,
`outbound_queue`=8, `delivery_log`=3 — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status --porcelain` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` החזיר שורות אפס, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- «${out.rights_count} זכויות» בשורת אישור הפקת המסמך, ו«${out.text_chars} תווי
  טקסט» לצידה, נשארו במספר — `countHe` כבר יושב בהיקף המודול, והראשון מגיע
  ליחיד בשורה אחת.
- כותרות «זכויות (N)» ו«מסמכים (N)» במסך הפנייה לא נגעו: מספר בסוגריים אינו
  צורת ריבוי.
- «0 זכויות» לא נמדד בייצור — נתיב הקליטה אינו יכול לייצר פנייה בלי זכויות,
  והמצב נמדד מקומית ב-e3e1af1.
- רענון שנכשל משאיר את השורות הישנות על המסך; אין נתיב HTTP שיכול להפיל את
  `/cases` בבקשה אחת.
- `await load()` שרץ אחרי «שינוי סטטוס» הפך למיותר ולא הוסר.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריה — הכרעה קודמת
  נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום.
- `sent` בלתי-ניתן-להשגה בכוונה; pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
