// המסומנים של e3e1af1, במקום אחד, כדי שהבדיקה המקומית וה-HTTP ימדדו בדיוק
// את אותו דבר. הפיצול לשלוש רשימות הוא העיקר:
//
// NEW     — חייבים להיות false בגרסה שבייצור ו-true אחרי. אלה שקונים את הפעימה.
// REMOVED — ההפך: true לפני, false אחרי. בלעדיהם «נפרס» יכול להיות גם «נפרסו
//           שתי הגרסאות זו על גבי זו», וזה בדיוק מה ששני קבצים באותו גודל
//           נראים כמו.
// OLD     — true בשתי הגרסאות. אלה אינם ראיה לפעימה אלא בקרת רגרסיה: פריסה
//           מ-dist ישן מחזירה מסכים אחורה בלי ששום דבר ידווח שגיאה.
//
// «פנייה אחת» יושב ב-OLD ולא ב-NEW בכוונה — הוא היה במסך מאז שהמונה נכתב,
// וההבדל הוא בשתי השורות שמתחתיו. זו בדיוק המלכודת ש-530fb44 ו-29d6ac6 נתפסו
// בה: מסומן שהיה true גם לפני, ונקרא כאילו הוא מעיד.
export const NEW = [
  "const countHe = (n, one, plural) =>",
  'countHe(n, "פנייה אחת", "פניות")',
  '"זכות אחת"',
  '"מסמך אחד"',
  "צורת זוגי הייתה מחייבת גם מין דקדוקי",
];

export const REMOVED = [
  'const many = (n) => (n === 1 ? "פנייה אחת" : ',
  "${c.rights_count ?? 0} זכויות",
  "ריח של AI. עברית סופרת אחרת.",
];

export const OLD = [
  "פנייה אחת",
  'show("screen-list"); load();',
  "decidedBit",
  "לא הוכרע מהמסך",
  "fillDecidedLine",
  "החשבון אינו קיים עוד",
  "<th>מכתב</th>",
  "fillTemplateCell",
  "tmpl-sel",
  "שינוי סטטוס",
  "עבד עכשיו",
  "הכנס לתור",
];

export const report = (t) => ({
  new: NEW.map((m) => m + "=" + t.includes(m)).join(" | "),
  removed: REMOVED.map((m) => m + "=" + t.includes(m)).join(" | "),
  old: OLD.map((m) => m + "=" + t.includes(m)).join(" | "),
});
