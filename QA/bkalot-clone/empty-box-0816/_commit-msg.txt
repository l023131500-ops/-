שכפול בקלות שכבה 3: התיבה אומרת «שתואמות את הסינון» על מסך בלי סינון (§5ב, UI)

תור ריק בלי סינון כלל: המונה אומר «אין פניות» והתיבה שמתחתיו אומרת «אין פניות
שתואמות את הסינון.» — שני משפטים סותרים זה מעל זה, באותה טעינה ובאותו צילום.
המסך מייחס את הריקנות לבחירה שהמנהל לא עשה, ומי שקורא את התיבה מחפש סינון
לנקות שאינו קיים.

הסיבה נמדדה ולא הונחה: הטקסט של התיבה נכתב פעם אחת ב-HTML (admin.html:304)
ומאז לא זז. 68785e9 תיקנה את המונה כך שיבחין בין «אין פניות» ל«אין פניות
שתואמות את הסינון», והכרעה (3) שלה אמרה במפורש שהמונה חוזר על מה שכתוב בתיבה
שמתחת — אבל התיבה נשארה מחרוזת קבועה, ומאז השתיים אינן מסכימות.

שלוש הכרעות:

(1) מחושב פעם אחת ונקרא בשני המקומות. שתי מחרוזות באותו נוסח בשני מקומות —
    אחת ב-HTML ואחת ב-JS — הן בדיוק מה שהוליד את הסתירה, ותיקון שהיה מעתיק את
    התנאי במקום לחלוק אותו היה מוליד אותה שוב בפעימה הבאה. noneText הוא המקור
    היחיד לשני המשפטים, והמונה משתמש בו מעכשיו במקום בטרינרי שהיה לו.

(2) מצב שלישי ולא שניים. התיבה נדלקת על אפס שורות בעמוד ולא על תור ריק:
    total>0 עם אפס שורות הוא מסלול החיתוך של a795108 אחרי שגם הטעינה השנייה
    חזרה ריקה, ושם «אין פניות» הוא שקר מול המונה שאומר באותו רגע «N פניות ·
    מוצגות 0».

(3) הטקסט ב-textContent ובנקודה בסוף, כמו שהיה. התיבה אינה role="status"
    ואינה מוקראת (הכרעה (3) של 68785e9), ולכן היא נשארת מה שהייתה — הד נראה —
    ורק מפסיקה לומר דבר שאינו נכון.

נמדד מדפדפן אמיתי על שני שרתים מקומיים מול אותו מסד ואותו edge, וההפרש היחיד
בין הריצות הוא admin.html: 8138 מגיש את HEAD (169,253 בייט, MD5 84442C69)
ו-8137 את עץ העבודה. עותק HEAD הוצא ב-cmd ולא בהפניית PowerShell — הפניית PS
מוסיפה BOM. התחברות אמיתית דרך הטופס, חשבון 125 שנוצר ב-
bkalot_clone_admin_create ולא בהזרקת hash.

ארבעה מצבים × לפני/אחרי, וכל שורה נקראה מה-DOM ולא הוסקה:
  A  תור ריק, בלי סינון      — לפני: מונה «אין פניות», תיבה «אין פניות שתואמות
                                את הסינון.»; אחרי: תיבה «אין פניות.». זהו הכשל.
  B  תור ריק, q=zzzz          — «אין פניות שתואמות את הסינון.» לפני ואחרי.
                                בקרה: המשפט לא נעלם כשהוא נכון.
  C  תור עם פנייה, q=zzzz     — זהה אות באות ל-B לפני ואחרי. תור מלא ורשימה
                                ריקה, וזו בדיוק הסיבה שהמשפט מדבר על הסינון.
  D  תור עם פנייה, בלי סינון  — התיבה מוסתרת, שורה אחת, «פנייה אחת». בקרה.

בקרות שלא זזו בשמונה הקריאות: title המונה, מספר השורות, pager מוסתר, אפס
גלישה אופקית ב-1280x900. הקונסולה: אפס הודעות ואפס תגובות שאינן 2xx.

שמונה צילומים. ארבעת צילומי B/C חולקים MD5 אחד (45144201) וזו הראיה עצמה ולא
תקלת screenshot-evidence-below-the-fold: המסך בשני המצבים זהה, וההפרש היחיד
ביניהם הוא מצב התור במסד. D נבדל ב-13 בייט בעוד קריאת ה-DOM זהה אות באות ואין
הפרש נראה — ההפרש בקידוד ה-PNG ולא בתוכן, והטענה היא קריאת ה-DOM. החלון שונה
פעם אחת לפני הכל — playwright-blank-screenshot-until-resize; הגלילה אל התיבה
עצמה, וכשהיא מוסתרת אל הרשימה שבמקומה.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816162924. HEAD היה 4dd30e2 וגם
origin, 0/0.

הזריעה דרך הכתובת החיה של bkalot-clone-intake מעל HTTP עם מפתח anon כמו
מהטופס: #391 (contact 398) «אברהם ישראלי» 0501230014, kind=info, source=form,
queued=false. הניסיון הראשון למחוק את הפנייה חזר 502 מה-proxy ונשלח שוב —
הספירה שאחריו היא זו שמכריעה ולא התשובה שחזרה.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, case_rights 0,
outbound_queue נשאר 8, delivery_log נשאר 3, אפס שורות sent_at, אפס קריאות
יוצאות. הנתיב שהשתנה מצייר טקסט — הוא אינו קורא ואינו כותב.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot;
איש הקשר לפי id, ו-contacts יושבת ב-bkalot_auto. נמחקו: cases 1 (391),
contacts 1 (398), admin_sessions של 125, admin_users 1 (125). לא נוצרו שורות
יומן. אחרי: בסיס בדיוק, ו-admin_cases על פרמטרים ריקים מחזירה total 0.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin ו-supabase,
נמדד מהשורש לפני ה-add; אין נגיעה ב-zr_*/NEDARIM3873 ולא בסכמות
csj/csj_src/igud. ל-git add נמסרו שני נתיבים מפורשים ולא נקודה. ההודעה נמסרה
ב-commit -F — here-string-commit-fails-silently.

פתוח: הפריסה. מה שנמדד כאן הוא הקוד שבמקור; בייצור
(more30.com/bkalot-studio/admin) התיבה עדיין אומרת «שתואמות את הסינון» על מסך
בלי סינון. זו הלבנה הבאה.

לא נמדד: הענף total>0 עם אפס שורות («בעמוד הזה לא נשארו פניות.») נכתב ולא נמדד
מדפדפן — הוא דורש שהרשימה תתקצר פעמיים בין שתי טעינות.

מה שלא נבנה ולא זז: אין «מתוך N בסך הכול»; אין הדגשה ואין ציטוט של המילה
בנימוק; אין חיפוש ב-cases.note; אין סימון על שמות המכריעים; אין סינון «יש
נימוק / אין»; אין סינון ומיון לפי מספר הנימוקים או המעברים; אין אינדקס על
case_status_log(note) ואין חיפוש טקסט מלא; אין עריכה ומחיקה ביומן; אין סינון
«איש קשר נמחק / קיים»; אין שמירת מיקום הפנייה; אין אינדקס על ct.phone.
שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
