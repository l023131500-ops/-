שכפול בקלות שכבה 3: ההדגשה בתוך הנימוק נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו נלקח כפי שנוסח ב-c1598aa (heartbeat 676), מילה במילה: «פתוח: הפריסה. מה
שנמדד כאן הוא הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) הנימוק עדיין
מצויר כמחרוזת אחת ואין בו ולו mark אחד. זו הלבנה הבאה». נלקח ולא הורחב: פריסה
בלבד — אין שינוי קוד, אין מיגרציה, אין פריסת edge function. bkalot-clone-admin
נשארת v9, schema_migrations נשארה 20260816182726, ו-git status על
apps/37-bkalot-clone ועל supabase/ החזיר אפס שורות לפני ה-add ואחריו. מה שנכנס
ל-commit הוא ראיות בלבד.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816182726. ו-HEAD היה c1598aa וגם
origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 181,758 בייט ו-MD5 6C7B27B3 במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
176,004 (282DF8EC) — מה ש-06a5707 פרסה — כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (prebuilt-deploy-drops-vercel-json),
ו-vercel.json ו-.vercel/project.json נמדדו כנוכחים לפני הפקודה.
dpl_EkDNrQSgwxHPKzRbHsTvF16bccmo, target=production, READY.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist), בלי
שהכתובת החיה נפגשה ולו פעם אחת — 0087. DEAD ריק, והסקריפט רץ לפני ה-staging
ולא אחריו (השמירה שנבנתה ב-6849803 מחזירה קוד 2 אם dist כבר זהה ל-src).

⚠️ הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה החזירו שתי
הכתובות, עם לוכסן ובלעדיו, בדיוק את ספירת ה-dist שנפרס בפעם הקודמת — 30 מתוך
30 הסימנים.

16 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי בשתי
הכתובות: mark.hit{, ה-box-shadow שמתחתיו, «function fillNoteText(td, r) {»,
fillNoteText (5), הקריאה עצמה, שורת ה-pos/len, note_match_pos (2),
note_match_len (2), שמירת הגבולות, יצירת ה-mark, m.className, ה-title החדש
וארבע שורות הערה. ⚠️ 3 מסומני REMOVED — «td.textContent = r.note;», «הטקסט
נכנס כ-textContent ולא כ-HTML» ו«אין הדגשה של המילה בתוך הנימוק ואין ציטוט של
הקטע שהתאים» — עמדו 1 לפני ו-0 אחרי; שלושת אלה הם הראיה שהקוד הישן לא נשאר
לצד החדש, ובפרט שהסירוב שהוסר אינו יושב עוד לצד הפונקציה שסותרת אותו.
⚠️ 2 מסומני DIFF נרשמים כפי שנמדדו ולא כפי שתוכננו: createTextNode נכתב כסימן
NEW ונמדד כ-DIFF (15 שימושים כבר היו ב-dist, 19 במקור), ו-fillHistoryNoteCell
נשקל כבקרה ונמדד כ-DIFF (4→5) — שמו מצוטט בהערות החדשות. שניהם הגיעו אחרי
לספירת המקור בדיוק. 9 מסומני CONTROL לא זזו, ובהם «זה הנימוק שהתאים» (3),
« תווים, כפי שנשמרו» ו«· בתור ${many(queueTotal)}» — הפריסה הקודמת לא נגעה.

live 176,240 בייט לפני ו-181,994 אחרי, בשתי הכתובות ובאותו MD5 — 236 בייט מעל
הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של תוכן. אפס תווי החלפה ואפס
«×» — deployed-html-double-encoded-cp1255.

הטענה עצמה נקראה מה-DOM בכתובת החיה ולא מקובץ. הזריעה: פנייה אחת ושלוש שורות
יומן, באותו תוכן שבו נבנתה 0100 — כדי שהמסך ייבדק בייצור בדיוק על הטקסטים
שהמיגרציה נמדדה עליהם. #407 (contact 414) «אברהם ישראלי» 0501230071, דרך
public.bkalot_clone_intake (kind=info, source=form, queued=false,
rights_linked 0), ואחריה שלוש הכרעות ב-bkalot_clone_admin_set_status (מנהל 4,
חשבון שהיה במסד ולא נוצר כאן): log 135 «המסמכים שצורפו שייכים לשנת המס הקודמת»
(37 תווים), log 136 «Email נשלח לפונה, ואחריו EMAIL נוסף בטעות» (41), log 137
«הטופס נשלח בדואר רשום על ידי הפונה» (34). ⚠️ 136 היא הסיבה שהיא נבחרה: המונח
יושב בה פעמיים (1 ו-26) ובשתי צורות אותיות, והראשונה פותחת את השורה. ⚠️ ונאמר
ולא נבלע: הקליטה נעשתה ב-SQL ולא מעל HTTP — פיגום ולא הטענה. התחברות אמיתית
דרך הטופס, חשבון 130 שנוצר ב-bkalot_clone_admin_create ולא בהזרקת hash, וכל
המצבים באותה טעינה ובלי refresh — טעינה מחדש מנתקת את הסשן (admin.html:312),
והחזרה לרשימה נעשתה בכפתור ולא ב-goto.

⚠️ המדידה שקדמה למסך: שלוש קריאות ל-admin_case(407, q) במסד החזירו
substr(note, pos, len) שהוא המונח עצמו — «המס הקודמת» (135, 28/10), «Email»
(136, 1/5 על מונח שהוקלד קטן), «בדואר רשום» (137, 12/10). ⚠️ ותקלה בתוך המדידה
הזו נרשמת ואינה נבלעת: שאילתת הסיכום הראשונה כוונה ל-'case'->'status_history'
והחזירה מערך ריק — status_history יושבת ברמה העליונה של התשובה; תוקן ונמדד שוב.

חמישה מצבים, וכל שורה נקראה מה-DOM בייצור ולא הוסקה. שלושת המונחים: mark אחד
בדיוק בכל מסך פנייה, ותוכנו שווה אות באות ל-substr שהמסד החזיר, ו-class="hit"
וה-title כפי שנכתב. ⚠️ ורצף הצמתים נקרא ולא רק הטקסט: ב-135
[טקסט, MARK, טקסט ריק, BR, SPAN] — ההתאמה בסוף המחרוזת; ב-136
[טקסט ריק, MARK, טקסט, BR, SPAN] — ההתאמה פותחת אותה; ב-137
[טקסט, MARK, טקסט, BR, SPAN] — באמצע. ⚠️ וב-136 «EMAIL» השני (מיקום 26) אינו
מסומן בייצור: 0100 הכרעה (4) נראית במסך החי ואינה נטענת. שתי בקרות: בלי מונח
כלל — שלוש שורות ואפס mark; ו-zzzz — אפס שורות ברשימה, ולכן אין מסך פנייה.

בקרות שלא זזו: הטקסט מורכב מחדש מצמתי התא ושווה לנימוק השמור בכל 12 התאים
שנקראו — הפיצול לשלושה צמתים אינו מוסיף ואינו גורע תו; ה-title של התא נשאר
«37/41/34 תווים, כפי שנשמרו»; «זה הנימוק שהתאים» נדלק על השורה שהתאימה בלבד;
ואפס גלישה אופקית ב-1280x900. הקונסולה נלכדה ולא נרשמה כחסרה: אפס הודעות ואפס
תגובות שאינן 2xx.

ארבעה צילומים בארבעה MD5 (321890B7, A2A2669C, 0EE58F85, 046B473D) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize, והגלילה אל התא עצמו.

⚠️ נאמר ולא נבלע: מצבי המסך לפני הפריסה לא צולמו מהייצור — הפריסה כבר קרתה.
שהייצור הריץ את הקוד הישן נמדד מהסימנים (שלושת הישנים 1, ואפס מכל 16 החדשים)
ולא מקריאת DOM. הענף שבו pos/len אינם נכנסים למחרוזת (הכרעה (4)) נפרס ולא נמדד
מדפדפן — אין שרת כזה להריץ מולו; שורה שבה note_matched true ו-pos null (0100
הכרעה (2)) נפרסה ולא נמדדה; לא נמדד ב-390x844; ומראה ה-mark עצמו (רקע, משקל,
קו תחתון) נמצא בצילומים ולא נמדד כערכי CSS מחושבים.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, case_rights 0,
outbound_queue נשאר 8, delivery_log נשאר 3, אפס קריאות יוצאות. מה שנפרס הוא
HTML ואין בו כתיבה חדשה.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot;
ונמדד לפני המחיקה ולא הונח שאין שורות בנות: log 3, case_rights 0, documents 0,
admin_sessions של 130 אחת. ⚠️ contacts יושבת ב-bkalot_auto ולא ב-bkalot_clone.
נמחקו case_status_log 3 (135–137), cases 1 (407), contacts 1 (414),
admin_sessions 1, admin_users 1 (130), בסדר הזה. אחרי: בסיס בדיוק, ו-admin_cases
על פרמטרים ריקים מחזירה total 0.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add; אין נגיעה
ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. portal/dist gitignored.
ל-git add נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה ב-commit -F —
here-string-commit-fails-silently.

פתוח: נתיב «המסך מסמן היכן בנימוק» סגור מקצה לקצה — המסד (0100), השער (v9, בלי
שינוי), המסך (c1598aa) והפריסה כאן. הלבנה הבאה היא UI ולא פריסה. מה שלא נבנה
ולא זז: אין ההתאמה השנייה ואילך; אין ציטוט חתוך של הקטע; אין פירוק של מספר
התור; אין חיפוש ב-cases.note; אין סימון על שמות המכריעים; אין סינון «יש נימוק /
אין»; אין סינון ומיון לפי מספר הנימוקים או המעברים; אין אינדקס על
case_status_log(note) ואין חיפוש טקסט מלא; אין עריכה ומחיקה ביומן; אין סינון
«איש קשר נמחק / קיים»; אין שמירת מיקום הפנייה; אין אינדקס על ct.phone.
⚠️ שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
