שכפול בקלות שכבה 3: הכפתור «עבד עכשיו» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח ש-37235a3 הותירה נוסח בה עצמה: «הקובץ טרם נפרס — המדידה רצה מול שרת
סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר». נלקח כפי שנוסח ולא הורחב: אין שינוי
קוד, אין מיגרציה ואין פריסת edge function. admin.html זהה בדיוק לזה שנכנס
ל-37235a3, ו-bkalot-clone-admin נשארה v5.

לפני הפריסה נבדק ש-portal/dist אינו ישן: שמות הנכסים המגובבים זהים לייצור
(index-C2AHwa-z.js, index-B35ByRD-.css). פריסה מ-dist ישן מחזירה את הפורטל אחורה
בלי ששום דבר ידווח שגיאה.

נמדד ב-HTTP: /bkalot-studio/admin עלה מ-50,484 ל-57,872 בתים, והדלתא מול המקור
היא 236 בדיוק — דלתת NetFree הידועה ולא מספר אחר. הטופס נשאר 21,272 ו-more30.com/
נשאר 3,517 עם אותם נכסים. שנים-עשר המסומנים חזרו בגוף, כולם true, ושפיות הקידוד
נמדדה — 456 אל״פים, אפס תווי החלפה, אפס כפל-קידוד. נכתב ב-node ולא ב-PowerShell:
.ps1 בלי BOM נקרא כ-cp1255 ומדווח «לא נמצא» על טקסט קיים.

נמדד בדפדפן 1280x900 על הכתובת החיה, על פניות שנוצרו דרך נתיב הקליטה הציבורי מעל
HTTP (62/63/64, שלושה טלפונים שונים בכוונה). שלושת המצבים נמדדו בייצור: A queued —
פעיל, «מאמת את התוכן ואת היעד. אינו שולח.»; B blocked — כבוי, «השורה אינה ממתינה
בתור»; C בלי שורת תור — כבוי, «המסמך לא הוכנס לתור».

לחיצה על A: «שורת תור #34 · עובד במצב בדיקה — לא נשלחה שום הודעה · יעד
qa.bkalot@more30.com · מצב test · מסמך #61 · 7608 בתים אומתו · נשלח בפועל: 0».
אפס שגיאות קונסולה.

הבדיקה שמפרידה מצב לקוח ממצב שרת: הפנייה נסגרה ונפתחה מחדש, המסך נבנה מאפס,
והכפתור נשאר כבוי עם אותה סיבה — המצב מגיע מ-queue_status שבמסד ולא מהלחיצה.

מצב טסט נמדד ולא הוצהר: אפס sent_at, אפס mode=live, delivery_log 3→4 — שורה אחת
בדיוק על שורת התור 34, outcome=dry_run; השורה החסומה לא כתבה כלום.

המקור לא נגע: 8 שורות app_key=bkalot נשארו 8, טביעת אצבע
da28ec6315535ec0bfbebad4d57752a7 זהה לפני ואחרי, והפרדיקט של המעבד של המקור
מחזיר [6] בלבד ולא את 34/35.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון. אחרי: cases=0, case_rights=0,
documents=0, contacts=4, admin_users=1, admin_sessions=5, templates=2, catalog=888,
outbound_queue=8, delivery_log=3 — זהה בדיוק לבסיס.

אין נגיעה בנתיב מוגן (08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873, csj/csj_src/igud).

נשאר פתוח: already_processed אינו נגיש מהמסך; cases.status נשאר new; pdf/audio →
channel_unsupported; בחירת תבנית אינה נשלחת; אין שליחה אמיתית. #37 נשאר מוסתר.
