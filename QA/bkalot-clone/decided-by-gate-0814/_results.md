# v7 — הזהות עוברת מהשער אל decided_by (§5ב)

**מה נמדד:** `bkalot-clone-admin` נפרסה כגרסה **7** (HTTP 201, `verify_jwt:true`,
23,900 בייט מקור). כל פנייה נוצרה דרך נתיב הקליטה הציבורי `bkalot-clone-intake`
במפתח anon, וכל לחיצה נעשתה דרך הנתיב החי `/set-status` מאחורי שער הסשן — לא
בהזרקה למסד.

## הקו שנסגר

0076 בנתה את `decided_by`/`decided_at` ואת הארגומנט השני `p_admin_id`, וכתבה
במפורש שהעברת הזהות מהשער היא הלבנה הבאה. עד הפריסה הזו v6 קראה עם ארגומנט
אחד, ולכן **כל לחיצה בייצור כתבה `decided_at` מלא ו-`decided_by` null** — המצב
השני מתוך שלושת המצבים של הכרעה (2): «אדם הכריע ואיננו יודעים מי».

הראיה למצב הקודם נמדדה בפעימה הקודמת ויושבת ב-
`QA/bkalot-clone/decided-by-0814/http.json`, «set-status #1»: `changed:true`,
`decided_at` מלא, `decided_by: null` — ובאותה תשובה עצמה `admin:{id:25}`
מהשער. הזהות הייתה קיימת ולא הגיעה לעמודה.

## המדידות (probe-out.txt · http.json)

פניות: **#98** (treatment, 42 זכויות) · **#99** (בקרה, info).
מנהלים: **26** «QA 0077» · **27** «QA 0077 שני» — שניים בכוונה, כדי שהזהות
תהיה נתון ולא קבוע.

| # | מה נעשה | תוצאה |
|---|---------|-------|
| 1 | `closed` מטוקן של 26 | `changed:true`, `previous:new`, `decided_by:26`, `decided_at 04:41:32.379729` |
| 2 | לחיצה שנייה, אותו ערך | `changed:false`, `decided_by:26`, `decided_at` **04:41:32.379729** — אותה חותמת בדיוק |
| 3 | `rejected`, והגוף נושא `admin_id:1` | `changed:true`, **`decided_by:26`** — לא 1 |
| 4 | `closed` מטוקן של 27 | `changed:true`, **`decided_by:27`** |
| — | בקרה #99, איש לא נגע | `status:new` |

### המדידה שקונה את הכרעה (1) של 0076

שורה 3. הגוף מועבר ל-`p` כפי שהוא ובכוונה בלי סינון מפתחות (כך גם ב-`queue`
וב-`dispatch`), ולכן `admin_id` בתוכו **מגיע מהדפדפן** — זו בדיוק הדרך שבה
מחזיק טוקן היה חותם הכרעה בשם מנהל אחר. הערך 1 נשלח, השורה השתנתה, והעמודה
קיבלה 26. `p_admin_id` הוא ארגומנט שני שה-edge בונה בעצמו מ-`g.admin`, והגוף
אינו יכול להגיע אליו.

### מה שהעמודה עצמה אומרת, ולא רק התשובה

נקרא מהמסד אחרי הריצה, עם ה-LEFT JOIN של הכרעה (4):

| id | status | decided_by | שם | decided_at |
|----|--------|-----------|-----|-----------|
| 98 | closed | **27** | QA 0077 שני | 04:41:35.332341 |
| 99 | new | null | null | null |

השם נפתר ב-`LEFT JOIN` ל-`admin_users` ולא ב-FK — 0076 הכרעה (4).
#99 מדגימה את המצב הראשון: שתיהן null, לא הייתה הכרעת אדם.

## מה לא נגעה בו הפעימה

מיגרציה לא נוספה (0076 היא האחרונה) · `apps/37` לא נגעה · אין פריסת פורטל ·
`bkalot-clone-intake` לא נגעה · שום נגיעה ב-08/09/`bkalut-app`/`bkalot-admin`/
`zr_*`/`NEDARIM3873` ובסכמות `csj`/`csj_src`/`igud`.

🚫 **מצב טסט:** הנתיב נוגע ב-`status`, `decided_by`, `decided_at`, `updated_at`
של `bkalot_clone.cases` ותו לא. אין נגיעה ב-`outbound_queue`, ב-`delivery_log`
ולא בשום ערוץ שליחה, ולא יצאה ולו הודעה אחת.

## מה שאין כאן ואינו נשכח

- **המסך עדיין אינו מציג את זה.** `bkalot_clone_admin_case` אינה מחזירה
  `decided_by`/`decided_at`, ולכן `admin.html` אינו יכול לומר מי סגר. זו הלבנה
  הבאה — אותו פיצול בדיוק כמו 0074 → 0075 → UI.
- **אין היסטוריה.** שתי העמודות אומרות את ההכרעה האחרונה בלבד, וזה נמדד כאן
  במפורש: #98 עברה 26 → 26 → 27, ומ-26 לא נשאר דבר. «מי הכריע קודם» נשאר קו
  פתוח, כפי שהוצהר ב-0076 הכרעה (6).
