# «הופק על ידי» בשורת רשימת העבודה — פריסה

הקו הפתוח נלקח כפי שנוסח ב-commit `4b7293d`, מילה במילה: «פתוח: הפריסה. מה
שנמדד כאן הוא הקוד שבמקור מול ה-DB החי, ובייצור שורת הרשימה עדיין אינה אומרת
דבר; זו הלבנה הבאה, אותו פיצול כמו 0085 → 3da9647 → a6e8844».

פריסה בלבד: אין שינוי קוד, אין מיגרציה, `bkalot-clone-admin` נשארת v8
ו-`bkalot_clone_admin_cases` לא נגעה. מה שנכנס ל-commit הוא ראיות בלבד.

## הקובץ שנפרס הוא הקובץ שבמקור

| | בייטים | MD5 |
|---|---|---|
| מקור `apps/37-bkalot-clone/admin.html` | 105,128 | `DCE69974…` |
| `portal/dist` אחרי `stage-portal.ps1` | 105,128 | `DCE69974…` |
| `portal/dist` לפני — מה ש-a6e8844 פרס | 101,368 | `3E0CFD8B…` |

38 קבצים נפרסו מ-`portal/dist` ובלי `--prebuilt` (`--prebuilt` מפיל את
`vercel.json` ואיתו את כל מפת ה-rewrites). `dpl_DMPZysc2GPcVw3NHd2vhXTK7Age5`,
READY, production.

## המסומנים התהפכו

אותם שנים־עשר מסומנים של `produced-by-list-ui-0816/http-probe.mjs`, מילה במילה —
הצד השני של אותה מדידה בדיוק. שם ארבעת ה-NEW החזירו 0 בייצור ו-≥1 במקור, וכאן
הם התהפכו אל שוויון, בשתי הכתובות (עם לוכסן ובלעדיו):
`function producedBit(c)` 0→1, `const produced = producedBit(c);` 0→1,
`last_produced_by_name` 0→2, «מי הפיק אינו ידוע» 0→1.

ארבעת ה-DIFF גדלו אל המקור: «הופק על ידי» 3→4, `fillProducedCell` 4→6,
`Object.hasOwn` 2→4, `documents_count` 2→6. ארבעת ה-OLD לא זזו כלל:
`fillDecidedLine` 5, `fillQueueCell` 2, `fillTemplateCell` 4, `decidedBit(c)` 2.

«הופק על ידי» אינו NEW כאן אלא DIFF: `a6e8844` כבר פרסה אותו ככותרת עמודה במסך
הפנייה, ומסומן שמחזיר true על שתי הגרסאות אינו מודד דבר (המלכודת של 530fb44,
29d6ac6, e12acf9). `documents_count`, שנכשל כבקרה ב-UI (6 מול 2) והועבר ל-DIFF
בכתב, נשאר ב-DIFF ולא חזר בשקט ל-OLD.

שתי כתובות בקרה לא זזו: `bkalot-studio` 32,558 ו-`more30.com` 3,517. אפס תווי
החלפה ואפס כפל-קידוד. 101,604 → 105,364 = 105,128 + 236, דלתת NetFree הידועה.

## ארבעה מצבים בייצור, בטעינה אחת

התחברות אמיתית דרך המסך בחשבון 74, ואז ארבע השורות נקראו מה-DOM החי בטעינה אחת.

| פנייה | איך הגענו אליו | המסד | הסימן בשורה |
|---|---|---|---|
| #217 | לחיצת «הפק מסמך» על המסך החי, דרך השער v8 | `produced_by=74` | `הופק על ידי QA 0088 מפיק ברשימה בייצור` |
| #218 | `bkalot_clone_render` בארגומנט אחד — צורת v7 | `produced_by=null` | `מי הפיק אינו ידוע` |
| #219 | render בחשבון 75, ואז מחיקת חשבון 75 | `produced_by=75`, אפס שורות ל-75 | `הופק בידי מנהל #75 · החשבון אינו קיים עוד` |
| #220 | לא הופק עליה מסמך כלל | — | שותקת |

#220 היא מה שקונה את התנאי הראשון בפונקציה: `documents_count` ולא
`last_produced_by`. פנייה שאיש עוד לא נגע בה ופנייה שהופק עליה בצורת v7 מקבלות
שתיהן null/null מהשרת, ורק המונה מפריד ביניהן — בלעדיו #220 הייתה נושאת «מי
הפיק אינו ידוע», סימן שנקרא כמו נתון שנמדד.

#219 לא הוזרק ב-`UPDATE` אלא נוצר בנתיב האמיתי, והוא שקונה את ה-LEFT JOIN של
0086 בייצור — כאן בחומרה גדולה יותר מאשר במסך הפנייה: `INNER` לא היה מרוקן שדה
אחד אלא **מעלים את שורת הפנייה מרשימת העבודה**, כלומר מוחק מהרשימה פנייה של
אזרח מפני שעובד התפטר. נמדד: ארבע שורות חזרו, והגוף שלם (2,505 תווים אחרי
המחיקה).

## מה שנמדד ולא הונח

- קונסולה: אפס הודעות, אפס שגיאות ואפס אזהרות על הכתובת החיה.
- הכתובת נמדדה עם cache-buster ולא חשופה (תקלת f2001e0).
- ה-`title` נקרא כתכונה ב-DOM ולא כטולטיפ שנפתח בריחוף. במצב השם המחלקה ריקה,
  ובשני ה«ישנים» היא `stale` — אותו נוסח בדיוק של `fillProducedCell`.

> ⚠️ `typeof producedBit` החזיר `false` ב-DOM החי. זה אינו היעדר: הפונקציה יושבת
> בתוך תחום בלוק ואינה גלובלית. נמדד במקום זאת בטקסט הסקריפט המוגש — ארבעת
> מסומני NEW נמצאו (1/1/2/1), זהים למקור.

> ⚠️ שני צילומי מסך יצאו עם אותו MD5 בדיוק (`49E70A74`). העמוד אינו נגלל
> (`scrollHeight` 900 = חלון 900), ולכן `scrollIntoView` לא הזיז דבר והצילום
> השני היה אותו viewport. נמדד שארבע השורות נמצאות כולן בתוך הקיפול
> (342→701 בחלון 900), ולכן **צילום אחד** הוא הראיה הנכונה; הכפול נמחק ולא
> נשמר כמדידה שנייה — תקלת `screenshot-evidence-below-the-fold`.

## מצב טסט

ארבע הקליטות `queued=false`, שלוש ה-render `queued=false`, אפס מסמכים עם
`queue_id`, אפס פניות ב-`sent`, `outbound_queue` נשאר 8 ואפס שורות עם `sent_at`
(טביעת אצבע `050e54ab59235d105af3a10c785dbaea` זהה בבסיס ואחרי),
`delivery_log` נשאר 3. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## התגלגלות אחורה

שש פקודות נפרדות לפי id, כל אחת סופרת את מה שהיא עצמה מחקה (תקלת
`cte-delete-sees-prestatement-snapshot`); אנשי הקשר לפי id (224–227) ולא לפי
טלפון מנורמל (תקלת ac6e523). documents 3, case_rights 168, cases 4, contacts 4,
admin_sessions 2, admin_users 1 — חשבון 75 נמחק כבר בזמן המדידה כחלק ממצב C,
ולכן 1 ולא 2.

אחרי: cases=0, case_rights=0, documents=0, contacts=4, admin_users=1,
admin_sessions=5, templates=2, outbound_queue=8, delivery_log=3, catalog=888 —
בסיס בדיוק.

## פתוח

נתיב «מי הפיק» סגור עכשיו משני המסכים ומהפריסה — עמודה (0083), שער (v8), קורא
מסמך (0084), מסך מסמך (042e60f + 7d7dcf2), קורא פנייה (0085), שורת פנייה
(3da9647 + a6e8844), קורא רשימה (0086), שורת רשימה (4b7293d) והפריסה כאן — ואין
בו קו פתוח שנשאר.

מה שלא נבנה: אין סינון ואין מיון לפי מפיק, אין `last_produced_at`, ואין
היסטוריית מפיקים — העמודה אומרת את ההפקה האחרונה בלבד.

קווים שממשיכים ולא נגעו: אין היסטוריית הכרעות; הקליטה מחברת 42 זכויות
ל-`treatment` ואפס ל-`info`/`reminder` על אותו `situation`. `sent`
בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP;
`pdf`/`audio` → `channel_unsupported`. `public_visible` ו-`show_in_showcase` של
#37 נשארים false.
