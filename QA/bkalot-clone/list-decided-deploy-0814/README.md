# הסימן «מי הכריע» ברשימת העבודה נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (073f08b) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הרשימה בלי הסימן, והמדידה כאן
רצה מול שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר (ה-rewrite, ההעתקה ל-dist
ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה הבאה, בדיוק כמו 1abad72 →
f2001e0».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-073f08b (85,043 בתים,
MD5 `6FD618CDFB0C8A04924B1F88667A7B57`, `git status` ריק) ו-`bkalot-clone-admin`
נשארה v7. אותה חלוקה כמו 1abad72 → f2001e0, e9e949c → 530fb44 ו-2a067be → 188bd04.

## המסומנים נמדדו מול הגרסה הקודמת, ואחד מהם נפל בבדיקה הזו

`מנהל #${c.decided_by} · החשבון אינו קיים עוד` נראה ייחודי לרשימה — הוא הנוסח
שהשורה החדשה מציגה — ונמדד **true גם על `073f08b^`**, הגרסה שהייתה בייצור:
073f08b העתיקה אותו מילה במילה מ-`fillDecidedLine` (1abad72). לולא נמדד מול
הגרסה הקודמת הוא היה נקרא כראיה לפעימה הזו, בעוד שהוא יושב בקובץ כבר יממה. הועבר
ל-OLD; זו בדיוק התקלה של 530fb44, והיא נתפסת רק כשמודדים לפני שמניחים.

חמשת המסומנים שנשארו נבדקו על ארבעה קבצים ב-`marker-check.mjs`:

| | חמשת המסומנים החדשים | אחד־עשר הישנים |
|---|---|---|
| `git show 073f08b^` (הגרסה שבייצור) | **כולם false** | כולם true |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | **כולם false** | כולם true |
| `portal/dist/…/admin.html` אחרי ה-staging | כולם true | כולם true |

## הבדיקה שרצה לפני הפריסה ולא הונחה

`portal/dist` יושב על הדיסק, ופריסה מ-dist ישן מחזירה את כל הפורטל אחורה בלי
ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו לייצור — `index-C2AHwa-z.js`
ו-`index-B35ByRD-.css`, זהים בשני הצדדים לפני ואחרי.

`dist/bkalot-studio/admin.html` היה 80,786 (הקובץ של f2001e0,
MD5 `FBD4962E4F1FC1A7507AE57BBB7A3203`) והמקור 85,043; אחרי ה-staging שניהם
85,043 עם אותו MD5 בדיוק.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 81,022 | **85,279** |
| `/bkalot-studio/admin/` | 81,022 | 85,279 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 777 | **845** |

הדלתא מול המקור היא **236 בדיוק** (85,279 − 85,043) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים — כלומר
המדידה מראה גם רגרסיה ולא רק את התוספת.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin`, על שתי פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא בהזרקה למסד:
**#112** תוכרע מהמסך, **#113** תזוז בלי לחיצה. כניסה לניהול נעשתה במסך עצמו,
מנהל 35 («QA 0080 פריסה»). הסקריפט לא הכריע דבר — שתי השורות נוצרו `new`, וכל
שינוי שנראה אחר כך הוא תוצאת לחיצה בדפדפן.

| # | מצב | מה הוצג בייצור |
|---|------|--------|
| 01 | שתיהן חדשות, איש לא נגע | אפס סימנים בשתי השורות |
| 02 | #112 נסגרה בלחיצה **בדפדפן** | «הוכרעה 14.08.2026, 09:04 · QA 0080 פריסה», title «מזהה מנהל 35» |
| 03 | #113 קודמה ב«הפק מסמך» | «לא הוכרע מהמסך», `rgb(168, 99, 31)`, ולצידה «1 מסמכים» |

**המדידה שקונה את הפעימה:** שתי הדרכים ל«בטיפול»/הכרעה נמדדו **באותה רשימה
בייצור**, בלחיצות בדפדפן שעברו את השער — כלומר נמדד המסלול המלא (rewrite →
הקובץ שנפרס → edge → v7 → 0078 → הרשימה) ולא כתיבה למסד. במסד אחרי הלחיצות:
#112 `status=closed`, `decided_by=35`, `decided_at` 06:04:26.982576+00 — בדיוק
09:04 בשעון ישראל, מה שהשורה הציגה; #113 `status=in_progress`, `decided_by=null`,
`decided_at=null`, `updated_at` 06:04:55. לפני הפריסה #113 הייתה אומרת «בטיפול»
ותו לא, אות באות כמו שורה שאדם הכריע.

ה-title והצבע נקראו מ-`getComputedStyle` ולא מהצילום, ומספר ה-`span` בשורה נמדד:
9 ב-#113 מול 8 ב-#112 — כלומר סימן אחד בדיוק נוסף, ולא שניים.

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — 0 מתוך 0 הודעות, לעומת
`favicon.ico` 404 של השרת הסטטי במדידה המקומית של 073f08b.

שלושת הצילומים נבדקו ב-`Get-FileHash`:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-list-both-new.png` | 36,418 | `4DCBB95660DCDD855AF5D3656DA701EA` |
| `02-live-decided-by-human.png` | 38,584 | `147E0795F406953855FDF8705D08670E` |
| `03-live-advanced-by-trigger.png` | 40,718 | `BED74ED2B28639D46CBB97DA8EC7079D` |

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, שורות `outbound_queue` עם `sent_at` = 0, שורות `mode=live`
= 0, `delivery_log` נשאר 3, ו-`outbound_queue` נשאר 8 גם אחרי הפקת המסמך —
«הפק מסמך» כותב שורה ב-`documents` ואינו מכניס לתור. טביעת האצבע של התור
(`8cf33788a36d5f9618858835c89e30c1`) זהה בזמן המדידה ואחריה. אין מייל, אין הודעה
ואין מסמך שיצא החוצה.

## גלגול אחורה

מסמך אחד, 2 פניות (112/113), 2 אנשי קשר, סשן אחד ומשתמש הניהול 35 — כולם נמחקו
בסדר הנכון. אחרי: `cases`=0, `case_rights`=0, `documents`=0, `admin_users`=1,
`admin_sessions`=5, `contacts`=4, `templates`=2, `catalog`=888,
`outbound_queue`=8, `delivery_log`=3 — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` ריק, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- **תקלה שנתפסה במדידה הזו ולא תוקנה בה:** «חזרה לרשימה» אינה מרעננת את הרשימה.
  אחרי «הפק מסמך» השורה עדיין אמרה «חדשה» ובלי סימן, בעוד שבמסד היא כבר הייתה
  `in_progress` עם מסמך — ורק «סינון» הביא את המצב האמיתי. זו התצוגה היחידה
  שראיתי כאן שיכולה להיקרא כנתון שנמדד בעוד שהוא ישן. הקו הבא.
- מצב **(0)** («המפתח אינו קיים בתשובה») ומצב «החשבון אינו קיים עוד» נמדדו
  ב-073f08b מקומית; כאן המסומנים בלבד מוכיחים שהקוד שמייצר אותם נפרס, לא שהוא רץ.
- מצב (2) של `fillDecidedLine` — «מי הכריע אינו ידוע» — עדיין ללא ולו שורה
  אמיתית מלפני v7 במסד ואין נתיב HTTP שיכול לייצר אותה.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`, כך שהפניות שהטריגר קידם
  עדיין מפוזרות בעמודים.
- אין היסטוריה: הכרעה קודמת נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום, ולכן «1 מסמכים» אינו אומר מי
  הפיק אותו; והמונה עצמו אינו סופר עברית כמו «פנייה אחת» שברשימה.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP
  (משתמש הבדיקה נוצר ב-RPC ישיר); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
