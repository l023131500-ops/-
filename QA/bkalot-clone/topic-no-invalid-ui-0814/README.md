# מסך הפנייה אמר לאזרח «קוד: topic_no_invalid» — 14/08

הקו הפתוח נכתב ב-72dbaa8 (heartbeat 596) ובתחתית מיגרציה 0080, מילה במילה:
«MESSAGES ב-apps/37-bkalot-clone עדיין אינו מכיר topic_no_invalid — דפדפן
ששולח "999999999999" עובר את החסימה המוקדמת כי הרגקס שלה מקבל מחרוזת ספרות
באורך כלשהו, ומעכשיו יראה «קוד: topic_no_invalid» במקום 502 אטום; שתיהן אינן
ראויות למסך, וזה צעד במסך ולא במסד». נלקח הקו הזה ורק הוא.

`apps/37-bkalot-clone/index.html` בלבד. אין מיגרציה, אין פריסת edge function,
`supabase/functions` לא נגעה, ואין פריסת פורטל — הקובץ טרם נפרס.

## מה השתנה

1. `MESSAGES.topic_no_invalid` — «מספר הנושא אינו תקין. תקנו ושלחו שוב.»
2. `whyTopicNoInvalid(sent)` — 0079 ו-0080 מחזירות **קוד אחד** לשתי סיבות
   שונות (אינו ספרות / ספרות רבות מכפי שהעמודה מחזיקה). ההבחנה ביניהן היא פרט
   מימוש של טיפוס העמודה, ולכן הקוד אחד והניסוח נקבע במסך — מהערך שהטופס באמת
   שלח, באותן שתי בדיקות ובאותו סדר של 0080. `TOPIC_NO_MAX = 2147483647` הוא
   גבול העמודה כפי ש-0080 בודקת אותו, ולא מספר "סביר" שהומצא כאן. ערך שאינו
   נופל תחת אף אחת מהן מוצג בלי סיבה — אותו כלל של `whyDropped`.
3. `markInvalid(el)` — מסיר סימון קודם, מסמן `aria-invalid` ומחזיר מיקוד.
   נקרא משני מקומות: החסימה המוקדמת (שלוש שורות שהיו שם הוחלפו בקריאה אחת)
   והנתיב החדש. אין שינוי התנהגות בחסימה המוקדמת — ראו בקרה 04.

## איך נמדד

דפדפן אמיתי 1280x900, שני שרתים סטטיים באותה ריצה מול אותו edge חי ואותו מסד:
8154 מגיש `git show HEAD` ו-8155 את המתוקן. `ENDPOINT` ב-index.html הוא כתובת
מוחלטת, ולכן ההבדל שנמדד יכול להיות רק בין שתי גרסאות של המסך.

prev: 28,825 בתים, MD5 432A2D93D21581DACC5E835324D44091 — בדיוק הקובץ
ש-heartbeat 594 רשם כנפרס ב-more30.com/bkalot-studio, כלומר 8154 הוא הגרסה
שבייצור ולא קובץ ישן יותר. המתוקן: 31,164 בתים, MD5
4F7257379E3D1C88D86EB4CEEA96CDA8.

«999999999999» הוקלד בהקשות מקלדת ולא הושם ב-value. נמדד לפני השליחה בשתי
הגרסאות: `value` מחזיק את הערך, `badInput` false, ו-`checkValidity` על השדה
ועל הטופס שניהם true — האימות הילידי אינו חוסם, וגם החסימה המוקדמת אינה
חוסמת (הרגקס שלה מקבל ספרות באורך כלשהו). זו הסיבה שהערך מגיע לשרת בכלל.

## מה שקונה את הפעימה

אותה הקלדה בדיוק, אותה תשובת שרת בדיוק (`ok:false`, `topic_no_invalid`,
בקשה אחת יצאה בשתי הגרסאות), שני מסכים:

| | בייצור (8154) | מתוקן (8155) |
|---|---|---|
| שורה 1 | אירעה תקלה. נסו שוב בעוד רגע. | מספר הנושא אינו תקין. תקנו ושלחו שוב. |
| שורה 2 | **קוד: topic_no_invalid** | מספר הנושא גדול מכל מספר נושא שקיים במערכת. |
| מיקוד | BODY | topic_no |
| aria-invalid בדף | 0 | 1 (על topic_no) |

הכותרת «הפנייה לא נשלחה» זהה בשתיהן והיא נכונה בשתיהן: 0080 פוסלת לפני
`insert`, ולכן לא נוצרת שורה. הערך «999999999999» עומד בשדה בשתי הגרסאות —
`fail` אינו מאפס — ומה שחסר בייצור הוא איזה שדה נפל, למה, ולאן ללכת.

## שתי בקרות

* **03 — «7»:** הבקשה יוצאת, פנייה #162 נוצרת עם 42 זכויות, אין שורת שגיאה,
  אפס `aria-invalid`, המיקוד לא נחטף (BODY) והטופס מתאפס. התיקון אינו נדבק
  למסלול התקין ואינו ממציא שורה.
* **04 — «1-5»:** אפס בקשות אל ENDPOINT, «מספר נושא — צריך מספר שלם. תקנו
  ושלחו שוב.», מיקוד ו-`aria-invalid` על השדה. הצילום כאן זהה בייט-בייט
  (MD5 0677E461) לצילום החסימה המוקדמת מריצת badinput-topic-no-0814 — הרפקטור
  ל-`markInvalid` לא שינה ולו פיקסל אחד באזור שנמדד. (האזור אינו כולל את שדה
  השם, ששתי הריצות מילאו בטקסט שונה; זה נאמר ולא נבלע.)

שורת התוצאה נמדדה ב-705 והשדה ב-272 בחלון של 900 — שניהם בו-זמנית מעל הקו,
ולכן הצילום מראה את ההודעה ואת השדה המסומן יחד. תקלת הראיות של e12acf9 נבדקה
ולא חזרה. ארבעה צילומים, ארבעה MD5 שונים: 69CBF368 / 5B1187E3 / A3E405E8 /
0677E461. אפס הודעות בקונסולה לאורך ארבע הזרימות — לא שגיאה, לא אזהרה ולא INFO.

## מצב טסט — נמדד ולא הוצהר

`documents=0`, אפס שורות `outbound_queue` עם `sent_at`, `outbound_queue` נשאר
8, `delivery_log` נשאר 3, וטביעת האצבע של התור
`8cf33788a36d5f9618858835c89e30c1` זהה בבסיס ואחרי. אין מייל, אין הודעה ואין
מסמך שיצא החוצה.

## התגלגל אחורה

שלוש פקודות נפרדות לפי id ולא CTE אחד (`case_rights` → `cases` → `contacts`);
מחיקת איש הקשר לפי id ולא לפי טלפון — הטלפון נשמר מנורמל לספרות. אחרי:
`cases=0`, `case_rights=0`, `documents=0`, `contacts=4`, `outbound_queue=8`,
`delivery_log=3`, `catalog=888` — זהה בדיוק לבסיס שנמדד לפני.

## פתוח

הקובץ טרם נפרס — more30.com/bkalot-studio עדיין מגיש את הגרסה שאומרת
«קוד: topic_no_invalid», והמדידה כאן רצה מול שרתים סטטיים מקומיים; זו הפעימה
הבאה, בדיוק כמו a6eb793 → cd5340e ו-124cfc2 → 4746bf3.

קו נוסף שנרשם כאן ולא נלקח: החסימה המוקדמת שבמסך עדיין מעבירה מחרוזת ספרות
ארוכה מכפי שהעמודה מחזיקה — הבקשה יוצאת ונפסלת בשרת. עכשיו היא נפסלת בעברית
ועם מיקוד, ולכן זו שאלה של סיבוב מיותר ולא של נתון שאובד; חסימה מוקדמת גם על
הגבול הזה היא צעד נפרד.
