שכפול בקלות שכבה 3: סינון «מי לא נלקח» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח נלקח כפי שנוסח בתחתית ac527e0, מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין מגיש מסך בלי התיבה, והמדידה כאן רצה מול
שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא את המוצר; זו הפעימה הבאה».

פריסה בלבד. אין שינוי קוד, אין מיגרציה ואין פריסת edge function;
bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin v7.
apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-ac527e0 — 92,899 בתים,
MD5 AF35D4991BA6E2B8756D0CF00A8ADE2A, git status ריק לפני הצעד.

הגרסה שבייצור נמדדה ולא הונחה: portal/dist הועתק ל-prev לפני ה-staging,
90,920 בתים ו-MD5 BFB19E3E, וה-HTTP לפני הפריסה החזיר 91,156 = 90,920 + 236 —
דלתת NetFree הידועה — כלומר זה הקובץ שהרשת החזירה ולא קובץ ישן יותר.

stage-portal.ps1 (38 קבצים) ואז vercel deploy --prod --yes מ-portal/dist ובלי
--prebuilt: dpl_4XLykapJBnpxeoFeyUJcAWQJURG3, READY, production, aliased
ל-more30.com.

HTTP: מסך הניהול 91,156 → 93,135 בשתי הכתובות (עם לוכסן ובלעדיו — שני rewrites
נפרדים, נמדדו בנפרד), דלתת HTTP 1,979 = בדיוק דלתת המקור, דלתת NetFree נשארה
236; /bkalot-studio 32,558 ודף הבית 3,517 לא זזו ואותם שני נכסים מגובבים.
ששת מסומני NEW 0 → 6, מסומן REMOVED 1 → 0, עשרת מסומני OLD 10 → 10, אפס תווי
החלפה ואפס כפל-קידוד בארבע הכתובות לפני ואחרי.

המלכודת נספרה במפורש: המסך אומר «לא נרשמה הכרעה ידנית מהמסך» מאז 0077, ולכן
«לא נרשמה» ו«הכרעה ידנית» כמסומנים בפני עצמם מחזירים true על שתי הגרסאות —
2 לפני ו-2 אחרי. שניהם תחת TRAP ואינם משמשים כראיה. זו תקלת 530fb44, 29d6ac6
ו-e12acf9. ששת המסומנים ששימשו הם קוד: id="f-decided", הקבוע DECIDED, לולאת
האפשרויות, המפתח הנשלח, משפט decided_unknown השלם, ועמודת ה-CSS.

בדפדפן חי 1280x900 על הכתובת החיה עם cache-buster (ולא על הכתובת החשופה —
תקלת f2001e0): has_new_control=true ו-has_removed_lastfilters=false נמדדו מתוך
הדפדפן ולא רק ב-HTTP. ה-select נבנה בזמן ריצה, והאפשרויות נקראו ממנו —
["","הכול"], ["no","לא נרשמה"], ["yes","נרשמה"] — כלומר לולאת DECIDED רצה
בייצור, ולא רק שההערה הגיעה ברשת. התווית «הכרעה ידנית», התיבה שלישית מארבע
(f-status, f-kind, f-decided, f-q), וה-CSS שחל הוא החדש: 1fr 1fr 1fr 2fr auto,
חמש עמודות. רגרסיה: מסך ההתחברות נראה, הרשימה והפנייה מוסתרות, הכותרת «בקלות —
ניהול פניות», העברית בצילום תקינה. אפס הודעות בקונסולה ואפס בקשות שנכשלו.

מצב טסט — נמדד ולא הוצהר: הריצה לא נגעה במסד כלל. אפס פניות נזרעו, אפס מסמכים
הופקו, אפס שורות תור, אפס הודעות, ולא הייתה התחברות. אין מה לגלגל אחורה, ולכן
אין כאן סעיף התגלגלות אחורה — ולא מפני שנשכח.

מה שלא נמדד ונאמר במפורש: הרשימה המסוננת עצמה לא נמדדה על הכתובת החיה. אישורי
המנהל נמחקו בהתגלגלות אחורה של ac527e0, bkalot_clone_admin_create אינה חשופה
ב-HTTP בכוונה, וה-MCP של Supabase אינו מחובר בסשן — אין SQL ואין דרך ליצור
מנהל. לכן מה שנמדד כאן הוא שהשאלה קיימת בייצור ונגישה למי שיושב מול המסך, ולא
שהתשובה עליה נכונה; החלוקה 2+1=3 נמדדה ב-ac527e0 מול אותו edge ואותו מסד.
getComputedStyle החזיר את הערך המוצהר ולא פיקסלים, מפני ש-#screen-list מוסתר
ואין פריסה — מה שנמדד הוא שכלל ה-CSS החדש הוא זה שחל, לא רוחב עמודה.

heartbeat לא הוחל ונשמר ב-_heartbeat.sql: אין MCP של Supabase בסשן (המחובר
היחיד הוא playwright), SUPABASE_ACCESS_TOKEN ריק בשלושת ההיקפים, ומפתח anon
מקבל 406 על סכמת core. אותו מצב כמו a45cdf5.

מוגן לא נגע: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

פתוח: אימות הרשימה המסוננת על הכתובת החיה (טעון אישורי מנהל דרך SQL) — הבקרה
היחידה שהפעימה הזו לא סגרה. וכן: אין מיון לפי decided_at, אין היסטוריית
הכרעות, הקליטה מחברת 42 זכויות ל-treatment ואפס ל-info על אותו situation,
«מי הפיק» על documents אינו נכתב לשום מקום, ${out.text_chars} ו-${d.html_chars}
נשארו במספר. sent בלתי-ניתן-להשגה בכוונה; pdf/audio → channel_unsupported.
public_visible ו-show_in_showcase של #37 נשארים false.
