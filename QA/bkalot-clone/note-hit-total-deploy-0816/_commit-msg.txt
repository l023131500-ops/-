שכפול בקלות שכבה 3: מספר ההתאמות נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נלקח מ-6aefa6a (heartbeat 682), מילה במילה: «פתוח: הפריסה. מה שנמדד כאן הוא
הקוד שבמקור; בייצור (more30.com/bkalot-studio/admin) השורה עדיין אומרת «המונח
ב-1 מתוך 3 הנימוקים» בלי ולו תו אחד על כמה פעמים. זו הלבנה הבאה». נלקח ולא
הורחב: פריסה בלבד — אין שינוי קוד, אין מיגרציה, אין פריסת edge function.
bkalot-clone-admin נשארת v9, schema_migrations נשארה 20260816200600, ו-git status
על apps/37-bkalot-clone ועל supabase/ החזיר אפס שורות לפני ה-add ואחריו. מה
שנכנס ל-commit הוא ראיות בלבד.

הריצה התחילה מבסיס נקי — נמדד לפני הכל ולא הונח, heartbeat-lags-a-crashed-run:
cases 0, case_status_log 0, case_rights 0, documents 0, admin_users 1,
admin_sessions 5, templates 2, contacts 4, outbound_queue 8, delivery_log 3,
rights_catalog 888, schema_migrations 20260816200600. שורה בשורה כפי שנרשמו
ב-heartbeat 682, ו-HEAD היה 6aefa6a וגם origin, 0/0.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 188,257 בייט ו-MD5 47C25DBD במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
185,393 (4B887ECA) — מה ש-f41628b פרסה — כלומר שרשרת הפריסות נמדדה ולא הונחה,
ושני המספרים זהים לְמה שנרשם ב-heartbeat 682 כקובץ ה-HEAD. 38 קבצים, מ-portal/dist
ובלי --prebuilt (prebuilt-deploy-drops-vercel-json), ו-vercel.json ו-.vercel/project.json
נמדדו כנוכחים לפני הפקודה. target=production, READY, ו-Aliased ל-more30.com.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist), בלי
שהכתובת החיה נפגשה ולו פעם אחת — 0087. 41 סימנים, DEAD ריק. ו-classify.mjs רץ
לפני ה-staging ולא אחריו — classify-self-destructs-after-staging: אחרי ה-staging
שני הקבצים זהים, הסקריפט יוצא בקוד 2 ואינו כותב את classify.json מחדש.

⚠️ הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה כל 41 הסימנים
החזירו מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת. _verify.mjs מכריע
משלוש ראיות ולא מאחת — dist_equals_live לפני, src_equals_live אחרי, ובקרות שלא
זזו — ו-PASS על 41 סימנים בשתי הכתובות, עם לוכסן ובלעדיו, אפס fails.

15 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי: קריאת
note_hit_total, ביטוי ה-hits, השמירה total ≥ hit, הקריאה ל-countHe, פתיחת הסוגר
ב-textContent, «הנימוקים`) + hits;», שני נוסחי ה-title החדשים, וששת בלוקי
ההערות ((6)–(9), הכותרת והפער). ⚠️ 3 מסומני REMOVED עמדו 1 לפני ו-0 אחרי —
שורת ה-textContent הישנה ושני חלקי ה-title הישן («המספר נספר… ולכן הוא נדלק»);
אלה הראיה שהקוד הישן לא נשאר לצד החדש. 3 מסומני DIFF הגיעו בדיוק לספירת המקור:
«ארבע הכרעות נוספות:» 1→2, countHe( 10→11, ו-Object.hasOwn 23→25.

20 מסומני CONTROL לא זזו בכלל, ובהם השמירות שמעל השורה שהשתנתה (hasOwn,
null-guard, hit===0), «המונח בנימוק היחיד», «המכנה הוא הנימוקים שנכתבו ולא
המעברים», ו-span.stale 22 לפני ו-22 אחרי — התוספת נכנסה לתוך ה-span הקיים ולא
יצרה אחד חדש.

live 185,629 בייט לפני ו-188,493 אחרי, בשתי הכתובות ובאותו MD5 (C4CFF382 →
7B86D885) — 236 בייט מעל הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של
תוכן, ואותם 236 שנמדדו ב-heartbeat 680. אפס תווי החלפה ואפס «×» —
deployed-html-double-encoded-cp1255.

הטענה עצמה נקראה מה-DOM בכתובת החיה ולא מקובץ. הזריעה: פנייה אחת ושלוש שורות
יומן, באותם שלושה טקסטים שעליהם נמדדו 0102 ו-6aefa6a. #413 (contact 420) «אברהם
ישראלי» 0501230131, דרך public.bkalot_clone_intake (kind=info, source=form,
queued=false, rights_linked 0), ואחריה שלוש הכרעות ב-bkalot_clone_admin_set_status
(מנהל 4): log 153 «Email נשלח לפונה, ואחריו EMAIL נוסף בטעות» (41 תווים),
log 154 «טופס, טופס ועוד טופס נשלחו בטעות» (32), log 155 «המסמכים שצורפו שייכים
לשנת המס הקודמת» (37). ⚠️ שלוש הופעות בנימוק אחד, שתיים באחר ואחת בשלישי, ומונח
אחד («נשלח») שיושב פעם אחת בכל אחד משניים — בלי הצירוף הזה אי אפשר להבחין בין
שני המספרים. ⚠️ ונאמר ולא נבלע: הקליטה נעשתה ב-SQL ולא מעל HTTP — פיגום ולא
הטענה. התחברות אמיתית דרך הטופס, חשבון 134 שנוצר ב-bkalot_clone_admin_create
ולא בהזרקת hash.

⚠️ המדידה שקדמה למסך, מהמסד בייצור: «טופס» → 1/3 ו-total 3; «email» → 1/3 ו-2;
«נשלח» → 2/3 ו-2; «המס הקודמת» → 1/3 ו-1; «אברהם» → 0/3 ו-0 עם
matched_in_note=false; ובלי מונח — null והמפתח קיים. כלומר השדה שהמסך צריך עונה
גם בייצור.

שישה מצבים אחרי, וכל שורה נקראה מה-DOM ולא הוסקה: «טופס» → «המונח ב-1 מתוך 3
הנימוקים · 3 פעמים» — זה הכשל שנסגר; עד הפריסה הזו צייר הייצור את אותה מחרוזת
בדיוק בלי הסיפא. «email» → «· 2 פעמים». ⚠️ «נשלח» → «ב-2 מתוך 3 · 2 פעמים»,
הבקרה שבה שני המספרים שווים — הכרעה (9) — ובלעדיה «1 מתוך 3 · 3 פעמים»
ו«2 מתוך 3 · 2 פעמים» לא היו נבדלים בהתנהגות. «המס הקודמת» → «· פעם אחת» ולא
«1 פעמים», אותו countHe של השורה עצמה. «אברהם» → אין תיבה כלל (hit 0, הכרעה (3)
לא זזה). ובלי מונח כלל — אין תיבה.

בקרות שלא זזו: «נמצאה לפי הנימוק» נדלק על אותם ארבעה מונחים בדיוק שבהם
matched_in_note=true; המונה «ב-N מתוך M הנימוקים» זהה אות באות למה שהיה לפני
הפריסה — התוספת מתארת ואינה מחליפה; שורת המונה («פנייה אחת תואמת את הסינון ·
בתור פנייה אחת») זהה בכל חמש הקריאות המסוננות; שורה אחת בכל קריאה בכל שישה
המצבים; ואפס גלישה אופקית ב-1280x900 (scrollWidth 1280 = clientWidth). הקונסולה
נלכדה ולא נרשמה כחסרה: אפס שגיאות ואפס אזהרות; ארבע הודעות INFO, כולן טוען
הגופנים של netfree.link ולא האפליקציה.

שלושה צילומים בשלושה MD5 (9B1E8ACE, DC5BC108, 21BFA966) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize, והגלילה אל השורה עצמה.

⚠️ נאמר ולא נבלע: מצבי הדפדפן שלפני הפריסה לא צולמו מהייצור — הפריסה כבר קרתה.
שהייצור הריץ את הקוד הישן נמדד מ-41 הסימנים (live-before.json) ולא מקריאת DOM.
ו-gannenet-dev-served-by-stale-sw: כל קריאה נעשתה עם cachebust. ⚠️ ותקלת תזמון
בבדיקה עצמה נרשמת ואינה נבלעת: הקריאה הראשונה אחרי רינדור מחדש חזרה ריקה
ב-1600ms ונמדדה שוב ב-3000ms — מרוץ של הבדיקה ולא מצב של המסך.

מצב טסט נמדד ולא הוצהר: queued=false, documents 0, case_rights 0, outbound_queue
נשאר 8, delivery_log נשאר 3, אפס קריאות יוצאות. מה שנפרס הוא HTML שמצייר טקסט —
הנתיב שהשתנה אינו קורא ואינו כותב.

התגלגל אחורה בפקודות נפרדות לפי טבלה — cte-delete-sees-prestatement-snapshot;
ונמדד לפני המחיקה ולא הונח שאין שורות בנות: log 3, case_rights 0, documents 0,
admin_sessions של 134 אחת. ⚠️ contacts יושבת ב-bkalot_auto ולא ב-bkalot_clone.
נמחקו case_status_log 3 (153–155), cases 1 (413), admin_sessions 1,
admin_users 1 (134), contacts 1 (420), בסדר הזה. אחרי: בסיס בדיוק,
ו-admin_cases על פרמטרים ריקים מחזירה total 0.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add; אין נגיעה
ב-zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud. portal/dist gitignored. ל-git add
נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה ב-commit -F —
here-string-commit-fails-silently.

פתוח: נתיב «כמה פעמים בתוכם» סגור מקצה לקצה — המסד (0102), השער (v9, בלי שינוי),
המסך (6aefa6a) והפריסה כאן. הלבנה הבאה היא UI ולא פריסה. מה שנאמר ולא נבלע: ענף
המפתח החסר (הכרעה (7)) וענף המחלוקת (הכרעה (8)) נכתבו ולא נמדדו — אין כאן שרת
מלפני 0102 להריץ מולו, ו-0102 מחזירה null בדיוק במקרה של מחלוקת; ולא נמדד
ב-390x844. מה שלא נבנה ולא זז: אין סינון ואין מיון לפי המספר החדש; אין ציטוט
חתוך של הקטע; אין סימון על note של הפנייה עצמה; אין פירוק של מספר התור; אין
חיפוש ב-cases.note; אין סימון על שמות המכריעים; אין סינון «יש נימוק / אין»; אין
אינדקס על case_status_log(note) ואין חיפוש טקסט מלא; אין עריכה ומחיקה ביומן; אין
סינון «איש קשר נמחק / קיים»; אין שמירת מיקום הפנייה; אין אינדקס על ct.phone.
⚠️ שורה 90 ב-0096 עדיין אומרת «מה שלא נמדד».
