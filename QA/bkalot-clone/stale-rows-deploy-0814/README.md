# «רענון שנכשל השאיר את השורות הישנות על המסך» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (a3a3405) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין משאיר את השורות הישנות אחרי רענון
שנכשל, והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא את המוצר
(ה-rewrite, ההעתקה ל-dist ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה הבאה,
בדיוק כמו e3e1af1 → 2842cf4, והכשל עצמו יימדד בה באותה עטיפת fetch שכן אינו נגיש
מ-HTTP».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-a3a3405 (89,820 בתים,
MD5 `B9AF28E5286362FD84EBC8DDDBE2110B`) ו-`bkalot-clone-admin` נשארה v7. אותה
חלוקה כמו e3e1af1 → 2842cf4, 13516f0 → b03d393 ו-1abad72 → f2001e0.

## מדוע REMOVED כאן הוא סמיכות ולא מחרוזת

הקושי הפוך מזה של e12acf9. שם המחרוזת שקונה את הפעימה הייתה כבר בייצור והמלכודת
הייתה לקרוא OLD כ-NEW. כאן a3a3405 היא **תוספת טהורה** — שלוש שורות קוד ותשע
שורות הסבר, ולא נמחקה ולו שורה אחת. אין מחרוזת שנעלמה, ולכן REMOVED חייב להיות
**סמיכות**: לפני התוספת השורה שאחרי `toLogin(data.error);` הייתה מיד
`$("count").textContent = "—";`, ואחרי התוספת חוצצות ביניהן שתים-עשרה שורות. זה
מה שנמדד, והוא ניתן להפרכה בדיוק כמו מחרוזת שנמחקה. בלעדיו «נפרס» יכול להיות גם
«נפרסו שתי הגרסאות זו על גבי זו».

שלושת מסומני ה-NEW הם **קוד ולא הסבר**, ושלושתם נבדקו כמופע יחיד בקובץ לפני
שנקבעו: `$("empty").hidden = true;` מול `$("empty").hidden = cases.length > 0;`
בנתיב ההצלחה, ו-`$("pager").hidden = true;` מול `$("pager").hidden = total <= PAGE;`.
מסומן שמופיע פעמיים היה מחזיר `true` על הגרסה שבייצור בזכות המופע האחר — זו
המלכודת ש-530fb44, 29d6ac6 ו-e12acf9 נתפסו בה.

| | NEW | REMOVED | OLD |
|---|---|---|---|
| `git show a3a3405^` (הגרסה שבייצור) | **כולם false** | כולם true | כולם true |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true | **כולם false** | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | **כולם false** | כולם true | כולם true |
| `portal/dist/…/admin.html` אחרי ה-staging | כולם true | **כולם false** | כולם true |

`prev/admin.html` חולץ ב-`execFileSync` עם `encoding:"buffer"` ולא דרך צינור
PowerShell — צינור מוסיף BOM והופך בייטים, ואז ההשוואה מודדת את הצינור ולא את
הקובץ. 88,696 בתים, MD5 `17DA929FDF9DD91A162AA15F5041473A` — **בדיוק** מה
ש-`portal/dist` החזיק לפני ה-staging, כלומר ה-dist שעל הדיסק היה הקובץ שבייצור
ולא קובץ ישן יותר. אחרי ה-staging שניהם 89,820 עם אותו MD5, ו-38 קבצים כמו
בפריסה הקודמת.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 88,932 | **90,056** |
| `/bkalot-studio/admin/` | 88,932 | 90,056 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 927 | **956** |

הדלתא מול המקור היא **236 בדיוק** (90,056 − 89,820) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים
(`index-C2AHwa-z.js`, `index-B35ByRD-.css`) — כלומר המדידה מראה גם רגרסיה ולא רק
את התוספת.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin?cb=qa0086live`, על שתי פניות שנוצרו דרך
נתיב הקליטה הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא
בהזרקה למסד: **#131** מסוג «טיפול מלא» ו-**#132** מסוג «מידע». ההבדל בסוג הוא מה
שהופך את הסינון שנכשל למדיד. כניסה לניהול נעשתה במסך עצמו, מנהל 44.

**הכשל מוזרק ולא נמצא**, וזה נאמר ולא נבלע: `window.fetch` נעטף בדף וזורק
`TypeError` על `/cases` בלבד, ו-`call()` תופסת ומחזירה `{ok:false,error:"network"}`
— נתיב הכשל האמיתי של המסך ולא אובייקט שהוזרק ל-`load()`. שאר הקריאות עברו לשרת
החי; הקריאה היחידה שנחסמה נרשמה ונבדקה
(`bkalot-clone-admin/cases`, מופע אחד). אין נתיב HTTP שיכול להפיל את `/cases`
בבקשה אחת — זו הסיבה שהקו נישא פתוח מ-29d6ac6 — ולכן זו הדרך היחידה למדוד אותו,
גם בייצור.

**המדידה שקונה את הפעימה** — אותה לחיצה בדיוק על «סינון», אותו כשל, בייצור:
המונה אומר «—», שורת השגיאה אומרת «אין חיבור לשירות. בדקו את האינטרנט ונסו שוב.
network», ומתחתיה **אפס שורות**. לפני הפריסה אותה לחיצה הותירה **שתי שורות** על
המסך, ובהן שורת «טיפול מלא» — פנייה שהסינון «מידע» שעל המסך מוציא במפורש. זה מה
ש-a3a3405 מדדה מקומית על שתי גרסאות באותה ריצה, וזה מה שכל אדם שסינון שלו נכשל
ראה עד עכשיו.

**שתי הבקרות, שתיהן באותה לחיצה אחת אחרי הסרת הכשל:** אותו סינון «מידע» מחזיר
«פנייה אחת» ושורה אחת — #132 — ושורת השגיאה נסגרת. כלומר הסינון הרגיל לא נשבר,
והמחיקה אינה קבועה: הריפוי הוא הרענון המוצלח הבא ולא טעינה מחדש של הדף.

**אפס הודעות בקונסולה** לאורך כל הזרימה — לא שגיאה, לא אזהרה ולא INFO.

שלושת הצילומים נבדקו ב-`Get-FileHash` והם שונים, ושורת השגיאה אומתה
`inViewport` ב-`y=302` לפני הצילום — תקלת הראיות של e12acf9 (שורה מתחת לקו
הקיפול) נבדקה ולא חזרה:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-loaded.png` | 38,937 | `3BA169B67B5EDAF95C6105488916BF50` |
| `02-live-failed-filter.png` | 25,149 | `E55539D0F8958C67B3B9EAC027143A56` |
| `03-live-recovered.png` | 30,213 | `9E528D1A59B6981B60C10ACAD7EC2C7C` |

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, `documents` = 0, שורות `outbound_queue` עם `sent_at` = 0,
`delivery_log` נשאר 3 ו-`outbound_queue` נשאר 8. טביעת האצבע של התור
(`bf57d69edaf6725ff7f52ad3007c4170`) זהה בבסיס, בזמן המדידה ואחריה. «הפק מסמך»,
«הכנס לתור» ו«עבד עכשיו» היו על המסך ולא נלחצו. אין מייל, אין הודעה ואין מסמך
שיצא החוצה.

## גלגול אחורה

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון: 42 `case_rights`, 2 פניות, סשן אחד,
משתמש ניהול 44 ושני אנשי קשר. אחרי: `cases`=0, `case_rights`=0, `documents`=0,
`admin_users`=1, `admin_sessions`=5, `contacts`=4, `templates`=2, `catalog`=888,
`outbound_queue`=8, `delivery_log`=3 — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status --porcelain` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` החזיר שורות אפס, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- **נמדד כאן ולא היה מתועד:** הקליטה מחברת 42 זכויות לפנייה מסוג `treatment`
  ואפס לפנייה מסוג `info`, על אותו `situation` בדיוק (`single_parent`). #132
  הציגה «0 זכויות» בשורה. לא נבדק אם זה מכוון.
- `await load()` שרץ אחרי «שינוי סטטוס» הפך למיותר ולא הוסר.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריה — הכרעה קודמת
  נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום.
- `${out.text_chars}` תווי טקסט ו-`${d.html_chars}` תווי HTML נשארו במספר.
- כותרות «זכויות (N)» ו«מסמכים (N)» במסך הפנייה לא נגעו.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP;
  pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
