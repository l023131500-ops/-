שכפול בקלות שכבה 3: עמודת התור נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (c3338c5) הותירה נוסח בה עצמה, גם בהודעת הקומיט וגם
ב-QA/bkalot-clone/queue-column-0814: «הקובץ טרם נפרס — more30.com/bkalot-studio/admin
עדיין מגיש את הגרסה בלי העמודה, וזו הפעימה הבאה». הוא נלקח כפי שנוסח ולא הורחב:
אין שינוי קוד, אין מיגרציה ואין פריסת edge function, ו-apps/37-bkalot-clone/admin.html
זהה בדיוק לזה שנכנס ל-c3338c5. bkalot-clone-admin נשארה v4.

למה זו פעימה בפני עצמה: המדידה הקודמת רצה מול שרת סטטי מקומי שמגיש את הקובץ
מהריפו מול ה-edge והמסד החיים. זו מדידה של הקוד ולא של המוצר — ה-rewrite
ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק לא
נבדקו בה כלל.

הבדיקה שרצה לפני הפריסה ולא הונחה: portal/dist יושב על הדיסק מאז 13/08, ופריסה
מ-dist ישן מחזירה את כל הפורטל אחורה בלי ששום דבר ידווח שגיאה. שמות הנכסים
המגובבים הושוו לייצור — index-C2AHwa-z.js ו-index-B35ByRD-.css, זהים בשני
הצדדים. dist/bkalot-studio/admin.html היה 45,982 והמקור 50,248.

נמדד ב-HTTP אחרי הפריסה (dpl_6Dgbig7NDAEC6dp6fDm46bGUVTZr, READY, more30.com):
/bkalot-studio/admin עלה מ-46,218 ל-50,484 בתים, והדלתא מול המקור היא 236 בדיוק
— דלתת NetFree הידועה ולא מספר אחר; /bkalot-studio/admin/ מחזיר את אותם בתים;
הטופס נשאר 21,272 ו-more30.com/ נשאר 3,517 עם אותם שני נכסים — בלי רגרסיה.
אחד-עשר המסומנים של c3338c5 חזרו בגוף, כולם true: fillQueueCell, QUEUE_STATUS,
QUEUE_BAD, queue_missing, contact_email_differs, <th>תור</th>, «בתור — טרם נשלח»,
«חסום — לא יישלח», «כתובת המכתב», «הסכמה בפנייה», «שורת התור חסרה». שפיות קידוד
נמדדה: 344 אל״פים אמיתיים, אפס סימני כפל-קידוד ואפס תווי החלפה.

נמדד בדפדפן אמיתי 1280x900 על הכתובת החיה, על נתונים שנוצרו דרך נתיב הקליטה
האמיתי מעל HTTP ולא בהזרקה למסד. פניות 51 ו-52 באותו טלפון בכוונה — שתיהן
contact_id=57, ואחרי 52 המייל של איש הקשר הוא later.overwrite.0071@more30.com.

מסך הכניסה מציג את הקופי «ומכניס אותו לתור», כלומר הקובץ שבייצור הוא החדש ולא
רק גדול יותר; «הצג סיסמה» קיים (#226 נשאר סגור); וכותרת העמודה «תור» נמצאת
בטבלה — וזה מה שהצעד קנה.

פנייה 51: התא «#25 · בתור — טרם נשלח · מצב test», לא צבוע; «כתובת המכתב»
qa.bkalot@more30.com מול «דוא״ל» later.overwrite.0071; והאזהרה «⚠ המייל של איש
הקשר אינו הכתובת של הפנייה הזו. המכתב ילך אל qa.bkalot@more30.com.» — הפרש
שהיה שקט בין שני מסכים הוא עכשיו משפט. פנייה 52: «#26 · חסום — לא יישלח · מצב
test», צבוע, ואפס אזהרות (differs=false, ובכוונה אינו מוצג). פנייה 53 (info,
בלי מייל): כתובת «—», הסכמה «כן», אפס אזהרות (differs=null), ואין טבלת מסמכים.
שלושת מסלולי fillQueueCell נמדדו בייצור.

לחיצה על «הכנס לתור» בפנייה 51: התא נשאר #25, הכפתור «הכנס לתור שוב», שורה אחת
בלבד בטבלה, ושורת המדידה «שורת תור #25 · כבר היה בתור — לא נוצרה שורה שנייה ·
במצב queued · יעד qa.bkalot@more30.com · מצב test · 7610 בתים». אפס שגיאות
קונסולה.

מה שנתפס תוך כדי ונרשם — כשל מדידה ולא כשל מוצר: פנייה 53 «לא נפתחה» בשלוש
מדידות רצופות והמסך נראה כאילו נזרק למסך הכניסה. document.querySelector("main")
מחזיר את ה-main הראשון ב-DOM — זה של מסך הכניסה — ו-textContent מחזיר את הטקסט
שלו גם כשהוא hidden. שלוש מדידות בלתי-תלויות סתרו את המסקנה לפני שנרשמה: /case
על 53 החזיר http=200 ok=true, רשימת הבקשות הראתה case → 200, ו-screen-case.hidden
היה false בזמן שהמסך «נראה» כמסך כניסה. אין כאן באג. נרשם כי מדידה שקוראת
אלמנט מוסתר מדווחת תקלה על מוצר תקין — כשל שנראה בדיוק כמו ממצא.

מצב טסט נמדד ולא הוצהר: delivery_log=3 ללא שינוי, אפס שורות mode=live, אפס
שורות עם sent_at. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

בידוד ממנוע המקור נמדד אחרי הכתיבה: max_attempts=0 בשורות 25 ו-26, והפרדיקט
המדויק של queue_due מחזיר [6] בלבד. המעבד לא הורץ בכוונה.

המקור לא נגע: 8 שורות app_key=bkalot נשארו 8, טביעת אצבע
aa6929ee28d13b34a01b5806d2598254 זהה לפני ואחרי.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (מסמכים לפני שורות התור —
documents.queue_id הוא FK; פניות לפני אנשי הקשר — cases_contact_id_fkey הוא
ON DELETE SET NULL): 2 מסמכים, 2 שורות תור, 118 case_rights, 3 פניות, 5 סשנים,
משתמש ניהול 11, 2 אנשי קשר. אחרי: cases=0, case_rights=0, documents=0,
admin_users=1, admin_sessions=5, templates=2, contacts=4, outbound_queue=8,
delivery_log=3, catalog=888 — זהה בדיוק לבסיס.

שני צילומים ב-QA/bkalot-clone/queue-column-deploy-0814, לצד ההרצות עצמן
(probe-http.mjs, seed.mjs, case53.mjs).

אין נגיעה בשום נתיב מוגן: git status על apps/08-bkalut-app ו-apps/09-bkalot-admin
ריק, ואין נגיעה ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות
csj/csj_src/igud.

מה שנשאר פתוח ולא נבלע: אין שולח (#234 סעיף 2) — השורה נכנסת לתור עם
max_attempts=0 ונשארת שם, וזו הלבנה הבאה; cases.status נשאר new; pdf/audio →
channel_unsupported; בחירת תבנית — render מקבל template_key והמסך אינו שולח
אותו. public_visible ו-show_in_showcase של #37 נשארים false.
