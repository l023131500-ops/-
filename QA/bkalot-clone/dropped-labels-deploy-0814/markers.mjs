// המסומנים של a6eb793, במקום אחד, כדי שהבדיקה המקומית וה-HTTP ימדדו בדיוק את
// אותו דבר. הפיצול לשלוש רשימות הוא העיקר:
//
// NEW     — חייבים להיות false בגרסה שבייצור ו-true אחרי. אלה שקונים את הפעימה.
// REMOVED — ההפך: true לפני, false אחרי. בלעדיהם «נפרס» יכול להיות גם «נפרסו
//           שתי הגרסאות זו על גבי זו», וזה בדיוק מה ששני קבצים באותו גודל
//           נראים כמו.
// OLD     — true בשתי הגרסאות. אינם ראיה לפעימה אלא בקרת רגרסיה: פריסה מ-dist
//           ישן מחזירה מסכים אחורה בלי ששום דבר ידווח שגיאה.
//
// כאן, בניגוד ל-a3a3405 (תוספת טהורה שחייבה סמיכות), a6eb793 באמת **מחקה שורה**:
// `body.dropped.join(", ")` הוחלף ב-`body.dropped.map(...)`. לכן REMOVED יכול
// להיות מחרוזת ממש, והיא הקריאה החזקה ביותר — היא מופיעה בייצור, אינה מופיעה
// במקור, ואי אפשר לזייף אותה בפריסה כפולה.
//
// שלושת מסומני ה-NEW נספרו בקובץ לפני שנקבעו וכל אחד מהם מופע יחיד. מסומן
// שמופיע פעמיים היה מחזיר true על הגרסה שבייצור בזכות המופע האחר — זו המלכודת
// ש-530fb44, 29d6ac6 ו-e12acf9 נתפסו בה. שים לב במיוחד לרווח הכפול ב-`topic_no:`
// ולכך ש«מספר נושא» לבדו הוא תווית שדה שקיימת גם בגרסה שבייצור, ולכן אינו מסומן.
export const NEW = [
  "const DROPPED = {",
  'topic_no:  "מספר נושא",',
  'body.dropped.map((k) => DROPPED[k] || k).join(", ")',
];

// מחרוזת שנמחקה ממש — לא סמיכות.
export const REMOVED = [
  'lines.push("שדות שלא נשמרו: " + body.dropped.join(", "));',
];

// בקרת רגרסיה על הטופס הציבורי עצמו: הכותרת, שתי שורות האישור (החיה ומצב
// הטסט), הקידומת שלא זזה, ומפת ההודעות שהתיקון בנוי לצידה.
export const OLD = [
  "שדות שלא נשמרו: ",
  "הפנייה נקלטה",
  "הודעת אישור נשלחה אליכם.",
  "🚫 מצב טסט: הפנייה נשמרה, ולא יצאה ממנה שום הודעה.",
  "bad_response:",
  "שליחת הפנייה",
];

export const report = (t) => ({
  new: NEW.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
  removed: REMOVED.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
  old: OLD.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
});
