# «שדות שלא נשמרו: topic_no» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (`a6eb793`) הותירה נוסח בה עצמה, מילה במילה:

> **הקובץ טרם נפרס** — more30.com/bkalot-studio עדיין מגיש את הגרסה שאומרת
> topic_no, והמדידה כאן רצה מול שרתים סטטיים מקומיים; זו הפעימה הבאה.

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge
function.** `apps/37-bkalot-clone/index.html` זהה בדיוק לזה שנכנס ל-`a6eb793`
(22,105 בתים, MD5 `66CA2D677A9DA86E79C716426499D771`). אותה חלוקה כמו
`e3e1af1` → `2842cf4`, `13516f0` → `b03d393` ו-`a3a3405` → `94af723`.

## למה זו פעימה בפני עצמה

המדידה של `a6eb793` רצה מול שני שרתים סטטיים מקומיים (8148/8149) שמגישים את
הקובץ מהריפו מול ה-edge והמסד החיים. זו מדידה של הקוד ולא של המוצר: ה-rewrite
ב-`vercel.dist.json`, ההעתקה ב-`stage-portal.ps1` ו-236 הבייטים ש-NetFree מזריק
לא נבדקו בה כלל.

## REMOVED כאן הוא מחרוזת ולא סמיכות

הפוך מ-`530fb44`. שם `a3a3405` הייתה תוספת טהורה ולא נמחקה ולו שורה אחת, ולכן
REMOVED נאלץ להיות סמיכות. כאן `a6eb793` **מחקה שורה ממש** —
`body.dropped.join(", ")` הוחלף ב-`body.dropped.map(...)` — ולכן REMOVED הוא
מחרוזת שנעלמה, והיא הקריאה החזקה ביותר: מופיעה בייצור, אינה מופיעה במקור, ואי
אפשר לזייף אותה בפריסה שהניחה שתי גרסאות זו על גבי זו.

שלושת מסומני ה-NEW הם **קוד ולא הסבר**, וכל אחד נספר בקובץ כמופע יחיד לפני
שנקבע — מסומן שמופיע פעמיים היה מחזיר `true` על הגרסה שבייצור בזכות המופע האחר,
המלכודת ש-`530fb44`, `29d6ac6` ו-`e12acf9` נתפסו בה. שים לב במיוחד:
**«מספר נושא» לבדו אינו מסומן** — זו תווית השדה, והיא קיימת גם בגרסה שבייצור.
המסומן הוא `topic_no:  "מספר נושא",` על שני הרווחים שלו.

| | NEW | REMOVED | OLD |
|---|---|---|---|
| `git show a6eb793^` (הגרסה שבייצור) | **כולם false** | כולם true | כולם true |
| `apps/37-bkalot-clone/index.html` (המקור) | כולם true | **כולם false** | כולם true |
| `portal/dist/…/index.html` לפני ה-staging | **כולם false** | כולם true | כולם true |
| `portal/dist/…/index.html` אחרי ה-staging | כולם true | **כולם false** | כולם true |
| HTTP על הכתובת החיה, לפני | **כולם false** | כולם true | כולם true |
| HTTP על הכתובת החיה, אחרי | כולם true | **כולם false** | כולם true |

`prev/index.html` חולץ ב-`execFileSync` עם `encoding:"buffer"` ולא דרך צינור
PowerShell — צינור מוסיף BOM והופך בייטים, ואז ההשוואה מודדת את הצינור ולא את
הקובץ. 21,036 בתים, MD5 `2535CA364F7AA18E7E74835FB3BA0BE4` — **בדיוק** מה
ש-`portal/dist` החזיק לפני ה-staging, כלומר ה-dist שעל הדיסק היה הקובץ שבייצור
ולא קובץ ישן יותר. אחרי ה-staging שניהם 22,105 עם אותו MD5, ו-38 קבצים כמו
בפריסה הקודמת.

## הפריסה

```
powershell -File scripts/stage-portal.ps1
cd portal/dist
npx vercel deploy --prod --yes --scope l023131500-ops-projects
```

`dpl_ELw9cUAeWXhVvfz1qR7AuVxFp4Fd` · `READY` · aliased ל-`more30.com`.

**מ-`portal/dist` ובלי `--prebuilt`** — שתי מלכודות ידועות: בלי
`.vercel/project.json` בתוך `dist` ה-CLI מסיק את שם הפרויקט משם התיקייה ויוצר
פרויקט חדש בשם "dist" (הפריסה מצליחה ו-more30.com לא מתעדכן), ו-`--prebuilt`
מפיל את `vercel.json` — כלומר את מפת ה-rewrites של כל המערכות.

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/` (הטופס) | 21,272 | **22,341** |
| `/bkalot-studio` (בלי לוכסן) | 21,272 | 22,341 |
| `/bkalot-studio/admin` | 90,056 | 90,056 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים בטופס | 134 | **151** |

הדלתא מול המקור היא **236 בדיוק** (22,341 − 22,105) — דלתת NetFree הידועה ולא
מספר אחר. מסך הניהול ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים
(`index-C2AHwa-z.js`, `index-B35ByRD-.css`) — כלומר המדידה מראה גם רגרסיה ולא רק
את התוספת. שתי הכתובות (עם לוכסן ובלעדיו) נמדדו כי שני ה-rewrites נפרדים.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/?cb=qa0087live`. הפנייה נוצרה **דרך הטופס
עצמו** ולא בהזרקה: «טיפול מלא», מצב `single_parent`, ו-`1e3` **הוקלד לשדה
בשלוש הקשות מקלדת** ולא הושם ב-`value`. הדפדפן קורא לזה תקין —
`topic_no.checkValidity()` ו-`intake.checkValidity()` שניהם `true`, כי `1e3` הוא
מספר נקודה-צפה תקין ב-HTML גם על `type=number min=1 step=1`. זו הסיבה שאדם מגיע
לשורה הזו בכלל.

**המדידה שקונה את הפעימה** — «שליחת הפנייה» נלחצה, והשורה על המסך החי אומרת:

> שדות שלא נשמרו: **מספר נושא**

`topic_no` אינו מופיע על המסך כלל. השורה אומתה `inViewport` ב-`y=574` לפני
הצילום — תקלת הראיות של `e12acf9` (שורה מתחת לקו הקיפול) נבדקה ולא חזרה. עד
הפריסה הזו כל מי שהקליד מספר נושא שאינו מספר שלם קיבל את המילה `topic_no` בתוך
מסך עברי שלם.

**מה שהמסד אמר, ולכן זו תקלת מוצר ולא ניסוח:** #136 נשמרה עם `topic_no=null`
בעוד ש-`raw->>'topic_no'` הוא `"1e3"` — מה שהאזרח הקליד יושב ב-raw ולא בעמודה.

**הבקרה**, אותו מסך חי, מספר נושא תקין (7, #137): **אין שורת «שדות שלא נשמרו»
כלל**, הכותרת «הפנייה נקלטה», ו-`topic_no=7` במסד. כלומר התיקון אינו ממציא
שורה. שתי הפניות 42 זכויות כל אחת — נתיב הזכויות לא זז.

**אפס הודעות בקונסולה** לאורך שתי הזרימות — לא שגיאה, לא אזהרה ולא INFO.

שלושת הצילומים נבדקו ב-`Get-FileHash` והם שונים:

| קובץ | MD5 |
|---|---|
| `01-live-form-typed.png` | `FCC8F1758D221E719A7FE86493173C90` |
| `02-live-dropped-hebrew.png` | `E22856C7C21A6BDF19859257257512F9` |
| `03-live-control-no-line.png` | `17C2EF6DDD0C52002827E7A98F8AD827` |

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, `documents` = 0, שורות `outbound_queue` עם `sent_at` = 0,
`outbound_queue` נשאר 8 ו-`delivery_log` נשאר 3. טביעת האצבע של התור
(`050e54ab59235d105af3a10c785dbaea`) זהה בבסיס, בזמן המדידה ואחריה. אין מייל,
אין הודעה ואין מסמך שיצא החוצה.

## גלגול אחורה

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון: 84 `case_rights`, 2 פניות (#136, #137)
ושני אנשי קשר (142, 143). אחרי: `cases`=0, `case_rights`=0, `documents`=0,
`contacts`=4, `outbound_queue`=8, `delivery_log`=3, `catalog`=888 — זהה בדיוק
לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status --porcelain` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` החזיר שורות אפס, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- **הנתון עצמו עדיין הולך לאיבוד.** #136 מוכיחה את זה בייצור: `1e3` ב-`raw`
  ו-`null` בעמודה. הטופס אומר עכשיו **איזה** שדה לא נשמר, ועדיין לא אומר **למה**
  ואינו מחזיר מיקוד לשדה. זה נוגע ב-0058 ולכן נשקל בנפרד.
- הקליטה מחברת 42 זכויות לפנייה מסוג `treatment` ואפס לפנייה מסוג `info`, על
  אותו `situation` — נמדד ב-`stale-rows-deploy-0814` ועדיין לא נבדק אם מכוון.
- «מי הפיק» על `documents` אינו נכתב לשום מקום.
- אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריה.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
