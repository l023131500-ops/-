שכפול בקלות שכבה 1: «שדות שלא נשמרו: topic_no» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח ש-a6eb793 הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio עדיין מגיש את הגרסה שאומרת topic_no, והמדידה כאן רצה
מול שרתים סטטיים מקומיים; זו הפעימה הבאה». נלקח כפי שנוסח ולא הורחב: אין שינוי
קוד, אין מיגרציה ואין פריסת edge function. apps/37-bkalot-clone/index.html זהה
בדיוק לזה שנכנס ל-a6eb793 (22,105 בתים, MD5 66CA2D677A9DA86E79C716426499D771).

למה זו פעימה בפני עצמה: המדידה הקודמת רצה מול שני שרתים סטטיים מקומיים
(8148/8149) מול ה-edge והמסד החיים — מדידה של הקוד ולא של המוצר. ה-rewrite
ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק לא
נבדקו בה כלל.

REMOVED כאן הוא מחרוזת ולא סמיכות, הפוך מ-530fb44: a6eb793 מחקה שורה ממש —
body.dropped.join(", ") הוחלף ב-body.dropped.map(...) — ולכן «נפרס» אינו יכול
להיות גם «נפרסו שתי הגרסאות זו על גבי זו». שלושת מסומני ה-NEW הם קוד ולא הסבר
וכל אחד נספר כמופע יחיד לפני שנקבע. «מספר נושא» לבדו במפורש אינו מסומן — זו
תווית השדה והיא קיימת גם בגרסה שבייצור; המסומן הוא topic_no:  "מספר נושא", על
שני הרווחים שלו. שש הגרסאות נמדדו ונופלות לשני צדדים נקיים: הגרסה שבייצור,
המקור, dist לפני ואחרי ה-staging, ו-HTTP לפני ואחרי.

prev/index.html חולץ ב-execFileSync עם encoding:"buffer" ולא דרך צינור
PowerShell: 21,036 בתים, MD5 2535CA364F7AA18E7E74835FB3BA0BE4 — בדיוק מה
ש-portal/dist החזיק לפני ה-staging, כלומר ה-dist שעל הדיסק היה הקובץ שבייצור
ולא קובץ ישן יותר. 38 קבצים נפרסו.

dpl_ELw9cUAeWXhVvfz1qR7AuVxFp4Fd, READY, aliased ל-more30.com. מ-portal/dist
ובלי --prebuilt: בלי .vercel/project.json ה-CLI יוצר פרויקט בשם "dist"
ו-more30.com לא מתעדכן, ו---prebuilt מפיל את vercel.json כלומר את מפת ה-rewrites
של כל המערכות.

HTTP לפני ואחרי: הטופס 21,272 → 22,341 בשתי הכתובות (עם לוכסן ובלעדיו, שני
rewrites נפרדים), אל״פים 134 → 151. הדלתא מול המקור היא 236 בדיוק — דלתת NetFree
הידועה ולא מספר אחר. מסך הניהול נשאר 90,056 ודף הבית 3,517, ואותם שני נכסים
מגובבים (index-C2AHwa-z.js, index-B35ByRD-.css) — המדידה מראה גם רגרסיה ולא רק
את התוספת. אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.

נמדד בדפדפן אמיתי על הכתובת החיה, 1280x900. הפנייה נוצרה דרך הטופס עצמו ולא
בהזרקה, ו-«1e3» הוקלד לשדה בשלוש הקשות מקלדת ולא הושם ב-value. הדפדפן קורא לזה
תקין — checkValidity מחזיר true על השדה ועל הטופס, כי 1e3 הוא מספר נקודה-צפה
תקין ב-HTML גם על type=number min=1 step=1; זו הסיבה שאדם מגיע לשורה הזו בכלל.

המדידה שקונה את הפעימה: השורה על המסך החי אומרת «שדות שלא נשמרו: מספר נושא»,
ו-topic_no אינו מופיע על המסך כלל. השורה אומתה inViewport ב-y=574 לפני הצילום.

מה שהמסד אמר, ולכן זו תקלת מוצר ולא ניסוח: #136 נשמרה עם topic_no=null בעוד
ש-raw->>'topic_no' הוא "1e3" — מה שהאזרח הקליד יושב ב-raw ולא בעמודה.

הבקרה, אותו מסך חי, מספר נושא תקין (7, #137): אין שורת «שדות שלא נשמרו» כלל,
הכותרת «הפנייה נקלטה», ו-topic_no=7 במסד — התיקון אינו ממציא שורה. שתי הפניות
42 זכויות כל אחת; נתיב הזכויות לא זז. אפס הודעות בקונסולה לאורך שתי הזרימות.
שלושה צילומים, שלושה MD5 שונים.

מצב טסט נמדד ולא הוצהר: sent=0, documents=0, שורות outbound_queue עם sent_at=0,
outbound_queue נשאר 8 ו-delivery_log נשאר 3; טביעת האצבע
050e54ab59235d105af3a10c785dbaea זהה בבסיס, בזמן המדידה ואחריה. אין מייל, אין
הודעה ואין מסמך שיצא החוצה.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (84 case_rights, 2 פניות, 2 אנשי קשר).
אחרי: cases=0, case_rights=0, documents=0, contacts=4, outbound_queue=8,
delivery_log=3, catalog=888 — זהה בדיוק לבסיס.

נתיבים מוגנים: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה
ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

מה שנשאר פתוח: הנתון עצמו עדיין הולך לאיבוד, ו-#136 מוכיחה את זה בייצור. הטופס
אומר עכשיו איזה שדה לא נשמר, ועדיין לא אומר למה ואינו מחזיר מיקוד לשדה; זה נוגע
ב-0058 ולכן נשקל בנפרד.
