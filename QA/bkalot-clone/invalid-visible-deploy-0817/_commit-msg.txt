שכפול בקלות שכבה 1: הצבע של השדה שנפסל נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נלקח מ-8ef394c, מילה במילה: «זו פעימת UI בלבד — אין פריסה. הייצור מגיש
עדיין את גרסת HEAD; הפריסה היא הפעימה הבאה». נלקח ולא הורחב: פריסה בלבד — אין
שינוי קוד, אין מיגרציה, אין פריסת edge function ואין נגיעה במסד. מה שנכנס
ל-commit הוא ראיות בלבד, ו-git status על apps/37-bkalot-clone ועל supabase/
החזיר אפס שורות לפני ה-add ואחריו.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 36,127 בייט ו-MD5 30B03F39 במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
34,622 (183921EE) — בדיוק הקובץ ש-3cff166 רשם כקובץ שנפרס, ובדיוק זה ש-8ef394c
הגיש משרת 8148 כ-HEAD. 38 קבצים, מ-portal/dist ובלי --prebuilt
(prebuilt-deploy-drops-vercel-json), ו-vercel.json (9,254 · 2CDA4F21)
ו-.vercel/project.json (118 · EE7AFD40) נמדדו כנוכחים אחרי ה-staging.
target=production, READY, ו-Aliased ל-more30.com
(dpl_7iy7vLhgpNN2PCFP1FUNqXjokkjs).

⚠️ הפעימה נוגעת ב-index.html ולא ב-admin.html, ונמדד ולא הונח: admin.html עמד
על 197,514 ו-0343B025 גם במקור וגם ב-dist לפני ה-staging וגם אחריו —
stage-portal מעתיק את שניהם בכל ריצה, ולכן «לא נגעתי» אינו מספיק והמספר הוא
הראיה.

⚠️ הסיווג נקבע כולו מהשוואת שני קבצים מקומיים (classify.mjs: src מול dist), בלי
שהכתובת החיה נפגשה ולו פעם אחת — 0087. 33 סימנים, DEAD ריק. ו-classify.mjs רץ
לפני ה-staging ולא אחריו — classify-self-destructs-after-staging; השמירה שנוספה
ב-6849803 עומדת גם כאן: הרצה אחרי staging יוצאת בקוד 2 בלי לכתוב מחדש.

⚠️ הפעימה הזו היא תוספת טהורה ואין בה שורה שנמחקה, ולכן קבוצת REMOVED **ריקה**
— ואין כאן את «return fail(code, extra);» שהכריע ב-3cff166 בין «נוסף»
ל«הוחלף». זה נאמר ולא נבלע, ומה שמחליף אותו כראיה הוא ctl_focus_rule: הכלל
input:focus,select:focus,textarea:focus{…box-shadow:var(--ring)} עמד 1 לפני
ו-1 אחרי, בשתי הכתובות. אילו הכלל החדש היה מחליף אותו במקום לשבת מתחתיו הסימן
היה יורד ל-0 וה-verify היה נופל; וכך גם ctl_ring_light/ctl_ring_dark ושני
קווי ה---danger המקוריים, שכולם הם ה-CSS שהכלל החדש נשען עליו ולא מחליף.

⚠️ הבקרה האמיתית של השרשרת היא dist_equals_live: לפני הפריסה כל 33 הסימנים
החזירו מהכתובת החיה בדיוק את ספירת ה-dist שנפרס בפעם הקודמת — אפס אי-התאמות,
בשתי הכתובות. _verify.mjs מכריע משלוש ראיות ולא מאחת — dist_equals_live לפני,
src_equals_live אחרי, ובקרות שלא זזו — ו-PASS על 33 סימנים בשתי הכתובות, עם
לוכסן ובלעדיו, אפס fails.

12 מסומני NEW החזירו 0 בייצור לפני ו-≥1 במקור, והתהפכו אל שוויון אחרי: שני
טוקני --danger-ring (בהיר וכהה), ארבעת חלקי שני הכללים — בורר וגוף בנפרד לכל
אחד, מפני שבורר בלי גוף הוא כלל ריק וגוף בלי בורר נדבק למשהו אחר — וארבע שורות
ההערה, ובנוסף phr_aria_invalid_true 0→6 ו-phr_danger_ring 0→3. 2 מסומני DIFF
הגיעו בדיוק לספירת המקור: aria-invalid 9→16 ו---danger-soft 3→5. 19 מסומני
CONTROL לא זזו, ובהם כל מה ש-3cff166 פרס — CODE_FIELD, bad_const, bad_guard,
markInvalid, ENDPOINT, ההודעות — כלומר הפריסה לא גלגלה את הקובץ אחורה אל גרסה
ישנה שאליה נוסף רק ה-CSS.

live 34,858 בייט לפני ו-36,363 אחרי, בשתי הכתובות ובאותו MD5 (1473D798 →
76D43A6F) — 236 בייט מעל הקובץ שנפרס בשתי המדידות, הפרש קבוע של הזרקה ולא של
תוכן, ואותם 236 שנמדדו ב-680, ב-683, ב-685, ב-687 ובפריסה הקודמת. אפס תווי
החלפה ואפס «×» — deployed-html-double-encoded-cp1255.

⚠️ ספירת מחרוזת בגוף ה-HTML אומרת שה-CSS **נשלח**, לא שהוא **צויר**, וזו לא אותה
טענה. הצבעים נקראו מ-getComputedStyle על השדה שנמדד באמת בכתובת החיה, ולא מתוך
ה-CSS. שלושה מקרים בשתי סכמות צבע, בדיוק אלה של 8ef394c: phone_invalid (שדה
טקסט, נתיב שרת), situation_required_for_treatment (בורר — select הוא אלמנט אחר
ולכן הוא במפורש בכלל), ו-topic_no דרך החסימה המוקדמת (נתיב לקוח שאינו יוצא
לרשת). שישה מתוך שישה: הגבול בייצור rgb(155,28,28) בבהיר ו-rgb(240,138,138)
בכהה, המילוי והטבעת בהתאמה.

⚠️ ההשוואה כאן אינה מול «לפני» חי, וזה נאמר ולא הונח: ברגע שהפריסה עברה אין
יותר ייצור-לפני להצביע עליו. שתי הטענות נמדדות שתיהן מול המספרים שנרשמו
ב-8ef394c: live_equals_local_after (הייצור צובע בדיוק כפי שהקובץ נמדד) אמת
בשישה מתוך שישה, ו-live_differs_from_old (הייצור כבר אינו rgb(15,95,76) בבהיר
ו-rgb(75,191,159) בכהה — --brand, הירוק של שדה תקין שנוגעים בו) אמת בשישה מתוך
שישה. ניגודיות מחושבת מהערכים שנמדדו, מול שני הצבעים שצמודים לגבול ולא מול
אחד: בהיר 8.15 מול הכרטיס ו-7.01 מול המילוי, כהה 7.12 ו-6.73 — כולן מעל סף
3:1 של WCAG 1.4.11.

⚠️ בקרה שלא זזה: marks_same_field ו-same_message אמת בשישה מתוך שישה — אותו
שדה מסומן, אותו שדה ממוקד, ואותה הודעה מילה במילה כמו שנמדדה מהקובץ. הפריסה
הביאה את הצבע ולא שינתה את ההתנהגות. אפס הודעות קונסולה ואפס שגיאות בשתי
הריצות, וכל טעינה עם cachebust — gannenet-dev-served-by-stale-sw. ההמתנה היא
עד שהערך מפסיק לזוז ולא זמן קבוע שהומצא — computed-style-measures-a-transition.

⚠️ 390x844 נמדד ולא רק צולם: השדה מסומן וממוקד, אותו גבול ואותה ניגודיות, ואפס
גלישה אופקית. כך גם ב-1280x901, ובשני הרוחבים נמדד scrollWidth מול clientWidth
ולא הונח.

⚠️ מצב טסט נמדד ולא הוצהר. אף פנייה לא נוצרה, ולא מזהירות אלא מפני ששני הקודים
שיוצאים לרשת הם return שיושב מעל יצירת ה-contact ומעל יצירת ה-case ב-0058,
והשלישי נעצר בחסימה המוקדמת ואינו נשלח. 6 תשובות נקלטו: any_ok=false,
any_case_id=false, כולן 200, ושני קודים בדיוק כמצופה. אפס שורות, אפס הודעות
יוצאות, אין זריעה, אין גלגול-אחורה ואין מיגרציה.

שלושה צילומים בשלושה MD5 (555E84E4, B0B3EBC6, B2359BD1) —
screenshot-evidence-below-the-fold; החלון שונה פעם אחת לפני הכל,
playwright-blank-screenshot-until-resize; והגלילה כוונה אל השדה המסומן ולא אל
תיבת התוצאה, מפני שמה שנטען כאן יושב על השדה.

⚠️ ה-heartbeat לא נכתב ל-core.run_progress. ה-MCP של Supabase אינו קיים בריצה
הזאת כלל — חיפוש כלים החזיר אפס התאמות, וההארנס מדווח על המחבר כדורש הרשאה
מחדש, מה שריצה לא-אינטראקטיבית אינה יכולה לעשות. השורה נכתבה כקובץ:
`_heartbeat-pending.sql`. ⚠️ הוא **השישי** בתור, ויש להריץ את כולם בסדר
הקומיטים — `case-hit-row-deploy-0817`, `allowlist-recheck-0817`,
`which-field-0817`, `which-field-deploy-0817`, `invalid-visible-0817`, ואז זה —
אחרת הפריסה תתוארך לפני הפעימה שהיא פורסת.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase, נמדד מהשורש לפני ה-add ואחרי ה-push; אין נגיעה
ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.
portal/dist gitignored. ל-git add נמסר נתיב מפורש אחד ולא נקודה. ההודעה נמסרה
ב-commit -F — here-string-commit-fails-silently.

פתוח: הפער «הסימן הגיע ל-aria ולא לעין» סגור מקצה לקצה — הכלל (8ef394c) והפריסה
כאן, ומבקר בייצור רואה היום את השדה שנפסל באדום. ⚠️ ומה שלא נסגר, ונרשם ולא
נמחק: הכלל מכסה input/select/textarea ולא את תיבת הסימון — היא אינה ב-CODE_FIELD
ואינה מסומנת היום, ואם קוד עתידי יסמן אותה היא תישאר בלי צבע. ⚠️ וענף
topic_no_invalid שמגיע מהשרת עדיין לא נמדד בייצור — החסימה המוקדמת תופסת אותו
לפני השליחה, וזו שורה פתוחה מ-3cff166 שהפעימה הזאת לא נגעה בה.

ראיות: `QA/bkalot-clone/invalid-visible-deploy-0817/` — `markers.mjs`,
`classify.mjs` + `classify.json`, `probe-live.mjs` + `live-before.json` /
`live-after.json`, `_verify.mjs` + `_results.json`, `measure-live.mjs` +
`_measured-live.json`, `_context.json`, ושלושה צילומים:
`live-light-phone.png`, `live-dark-situation.png`, `live-mobile-topic-390.png`.
