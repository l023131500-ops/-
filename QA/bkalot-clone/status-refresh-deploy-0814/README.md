# status-refresh-deploy-0814 — הפריסה של 643ccf2

הקו הפתוח נלקח כפי שנוסח ב-heartbeat 601 ובתחתית 643ccf2, מילה במילה:
«הקובץ טרם נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה שפונה לשרת
אל מסך מוסתר, והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא
את המוצר; זו הפעימה הבאה».

**פריסה בלבד.** אין שינוי קוד, אין מיגרציה ואין פריסת edge function;
`bkalot-clone-intake` נשארה v1 ו-`bkalot-clone-admin` v7.
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-643ccf2 —
90,920 בתים, MD5 `BFB19E3E15688423A0FEB54644D958E7`, `git status` ריק.

## למה זו פעימה בפני עצמה

המדידה של 643ccf2 רצה מול שני שרתים סטטיים מקומיים מול ה-edge והמסד החיים —
מדידה של הקוד ולא של המוצר. ה-rewrite ב-`vercel.dist.json`, ההעתקה ב-
`stage-portal.ps1` ו-236 הבייטים ש-NetFree מזריק לא נבדקו בה כלל.

## המסומנים

כאן `REMOVED` הוא **מחרוזת שנעלמה ולא סמיכות**, ההפך מ-67555e3: 643ccf2 מחקה
שורה ממש. `await load();` **לבדו נפסל כמסומן** — הוא מופיע בקובץ גם במקומות
אחרים והיה מחזיר true על שתי הגרסאות, בדיוק המלכודת של 530fb44, 29d6ac6
ו-e12acf9. מה שנספר הוא הסמיכות המדויקת שנמחקה,
`await openCase(c.id, undefined, msg);` ואחריה `await load();` באותו מטפל.
`await load();` נשאר ברשימת `OLD` כ**ספירה ולא נוכחות**: 2 בייצור, 1 במתוקן.

ארבעת מסומני `NEW` הם שורות ההערה שנוספו, כל אחד נספר בשתי הגרסאות לפני שנקבע
ונמצא 0 בייצור ו-1 במקור. שמונת מסומני `OLD` true בשני הצדדים.

## הפריסה

`prev/admin.html` הועתק מ-`portal/dist` **לפני** ה-staging: 89,820 בתים,
MD5 `B9AF28E5286362FD84EBC8DDDBE2110B` — בדיוק מה ש-643ccf2 רשם כגרסה שבייצור,
כלומר ה-dist שעל הדיסק היה הגרסה שבייצור ולא קובץ ישן יותר. 38 קבצים נפרסו.

`dpl_GN5zvDdND1sPDXpS5PAW7ZrYRXDT`, READY, aliased ל-`more30.com`; מ-`portal/dist`
ובלי `--prebuilt` (בלי `.vercel/project.json` ה-CLI יוצר פרויקט בשם "dist"
ו-more30.com לא מתעדכן, ו-`--prebuilt` מפיל את `vercel.json` כלומר את מפת
ה-rewrites של כל המערכות).

## HTTP לפני ואחרי

| כתובת | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 90,056 | 91,156 |
| `/bkalot-studio/admin/` | 90,056 | 91,156 |
| `/bkalot-studio` | 32,558 | 32,558 (רגרסיה — לא זז) |
| `/` | 3,517 | 3,517, אותם שני נכסים מגובבים |

שתי כתובות הניהול נמדדו בנפרד — עם לוכסן ובלעדיו הם שני rewrites נפרדים.
הדלתא מול המקור 236 בדיוק — דלתת NetFree הידועה. `NEW` 0 → 4, `REMOVED` 1 → 0,
`await load();` 2 → 1. אפס תווי החלפה ואפס כפל-קידוד בארבע הכתובות, לפני ואחרי.
נכתב ב-node ולא ב-PowerShell בכוונה: `.ps1` בלי BOM נקרא כ-cp1255 והעברית
שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## המדידה שקונה את הפעימה

דפדפן אמיתי 1280x900 על הכתובת החיה, עם cache-buster ולא על הכתובת החשופה
(שניות אחרי פריסה, דפדפן טרי קיבל את ה-HTML הישן על הכתובת החשופה בעוד ש-fetch
על אותה מכונה קיבל את החדש). הגרסה שהוגשה נמדדה **מתוך הדפדפן** ולא רק ב-HTTP:
`has_new_comment=true`.

לחיצה על «בטיפול»: **set-status 1, case 1, cases 0 — שתי בקשות**, מול
**שלוש** שנמדדו על הגרסה שבייצור ב-643ccf2 (set-status 1, case 1, cases 1).
חלון שקט של 2.5 שניות אחרי שהשורה כבר על המסך, כדי שבקשה שיוצאת מאוחר תיספר.
שורת האישור זהה מילה במילה למה ש-643ccf2 מדד:
«הסטטוס שונה מ«חדשה» ל«בטיפול» · לא נשלחה שום הודעה» — לא ירד דבר ממה שהמנהל קורא.

**הצד שלא נמדד כאן ונאמר במפורש:** הגרסה שבייצור לפני הפריסה לא נמדדה בדפדפן על
הכתובת החיה — היא כבר לא שם. מה שכן נמדד עליה כאן הוא HTTP: `REMOVED` נוכח
ו-`NEW` נעדר, על קובץ שהוא בדיוק prev, ושלוש הבקשות שלה נמדדו ב-643ccf2 מול
אותו edge ואותו מסד.

## הבקרות

- «חזרה לרשימה»: בקשת `cases` אחת — הרענון לא אבד אלא נמצא במקום שבו רואים
  אותו; והשורה מציגה «בטיפול» ואת «הוכרעה 14.08.2026, 15:05 · QA רענון פריסה».
- #170 לא נלחצה ונשארה «חדשה» בלי «הוכרעה» — הרשימה לא זזה מעצמה.
- הוכחה דו-צדדית מהמסד: 169 → `in_progress` `decided_by=46`, 170 → `new`
  `decided_by=null`. ההסרה לא שינתה את מה שנכתב, רק את מי שנשאל.

שני צילומים, שני MD5 שונים (69738139 / B69F8DF3). אפס הודעות בקונסולה.

## מצב טסט — נמדד ולא הוצהר

`documents=0`, אפס פניות ב-`sent`, `outbound_queue` נשאר 8, אפס שורות עם
`sent_at`, `delivery_log` נשאר 3, וטביעת האצבע `bf57d69edaf6725ff7f52ad3007c4170`
זהה בבסיס ואחרי. הנתיב שנמדד נוגע בעמודת `status` ותו לא.

## התגלגלות אחורה

חמש פקודות נפרדות לפי id ולא CTE אחד (`case_rights` → `cases` → `contacts` →
`admin_sessions` → `admin_users`) — תקלת 124cfc2 — ואנשי הקשר לפי id ולא לפי
טלפון, שנשמר מנורמל — תקלת ac6e523; מזהי אנשי הקשר (175, 176) נלקחו **לפני**
מחיקת הפניות, כי `cases_contact_id_fkey` הוא `ON DELETE SET NULL`. אחרי:
`cases=0, case_rights=0, documents=0, admin_users=1, admin_sessions=5,
templates=2, contacts=4, outbound_queue=8, delivery_log=3, catalog=888` —
זהה בדיוק לבסיס.

## נתיבים מוגנים

`git status` ריק על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions`; אין נגיעה ב-
`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות `csj`/`csj_src`/`igud`.

## פתוח

- נתיב «שינוי סטטוס» סגור משני צדדיו ואין בו קו פתוח שנשאר.
- קווים שממשיכים ולא נגעו: הקליטה מחברת 42 זכויות ל-`treatment` ואפס ל-`info`
  על אותו `situation`; אין סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין
  היסטוריית הכרעות; «מי הפיק» על `documents` אינו נכתב לשום מקום;
  `${out.text_chars}` ו-`${d.html_chars}` נשארו במספר.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP;
  pdf/audio → `channel_unsupported`. `public_visible` ו-`show_in_showcase` של #37
  נשארים false.
