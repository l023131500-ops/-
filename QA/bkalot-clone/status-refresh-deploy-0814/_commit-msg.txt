שכפול בקלות שכבה 3: הסרת הרענון אל המסך המוסתר נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו הפתוח נכתב ב-643ccf2 (heartbeat 601), מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio/admin עדיין מגיש את הגרסה שפונה לשרת אל מסך מוסתר,
והמדידה כאן רצה מול שרתים סטטיים מקומיים, כלומר מדדה את הקוד ולא את המוצר; זו
הפעימה הבאה». נלקח כפי שנוסח ולא הורחב — פריסה בלבד: אין שינוי קוד, אין מיגרציה
ואין פריסת edge function; bkalot-clone-intake נשארה v1 ו-bkalot-clone-admin v7.
apps/37-bkalot-clone/admin.html זהה בדיוק לזה שנכנס ל-643ccf2 (90,920 בתים,
MD5 BFB19E3E, git status ריק).

REMOVED כאן הוא מחרוזת שנעלמה ולא סמיכות, ההפך מ-67555e3: 643ccf2 מחקה שורה
ממש. «await load();» לבדו נפסל כמסומן לפני שנקבע — הוא מופיע בקובץ גם במקומות
אחרים והיה מחזיר true על שתי הגרסאות, בדיוק המלכודת של 530fb44, 29d6ac6
ו-e12acf9; מה שנספר הוא הסמיכות המדויקת שנמחקה, openCase ואחריה load באותו
מטפל. «await load();» נשאר ב-OLD כספירה ולא נוכחות: 2 בייצור מול 1 במתוקן.
ארבעת מסומני NEW הם שורות ההערה שנוספו, כל אחד 0 בייצור ו-1 במקור; שמונת
מסומני OLD true בשני הצדדים.

prev הועתק מ-portal/dist לפני ה-staging: 89,820 בתים MD5 B9AF28E5 — בדיוק מה
ש-643ccf2 רשם כגרסה שבייצור, כלומר ה-dist שעל הדיסק היה הגרסה שבייצור ולא קובץ
ישן יותר. 38 קבצים נפרסו. dpl_GN5zvDdND1sPDXpS5PAW7ZrYRXDT, READY, aliased
ל-more30.com; מ-portal/dist ובלי --prebuilt.

HTTP לפני ואחרי: מסך הניהול 90,056 → 91,156 בשתי הכתובות (עם לוכסן ובלעדיו,
שני rewrites נפרדים), NEW 0 → 4, REMOVED 1 → 0, await load() 2 → 1. הדלתא מול
המקור 236 בדיוק — דלתת NetFree הידועה. הטופס נשאר 32,558 ודף הבית 3,517 ואותם
שני נכסים מגובבים — רגרסיה ולא רק תוספת. אפס תווי החלפה ואפס כפל-קידוד בארבע
הכתובות, לפני ואחרי. נכתב ב-node ולא ב-PowerShell בכוונה.

המדידה שקונה את הפעימה אינה טקסט אלא בקשה שלא יצאה, על הכתובת החיה, בדפדפן
אמיתי 1280x900, עם cache-buster ולא על הכתובת החשופה. הגרסה שהוגשה נמדדה מתוך
הדפדפן ולא רק ב-HTTP. לחיצה על «בטיפול»: set-status 1, case 1, cases 0 — שתי
בקשות, מול שלוש שנמדדו על הגרסה שבייצור ב-643ccf2. חלון שקט של 2.5 שניות אחרי
שהשורה כבר על המסך, כדי שבקשה שיוצאת מאוחר תיספר. שורת האישור זהה מילה במילה
(«הסטטוס שונה מ«חדשה» ל«בטיפול» · לא נשלחה שום הודעה») — לא ירד דבר ממה שהמנהל
קורא.

הצד שלא נמדד ונאמר במפורש: הגרסה שבייצור לפני הפריסה לא נמדדה בדפדפן על הכתובת
החיה — היא כבר לא שם. מה שנמדד עליה כאן הוא HTTP, על קובץ שהוא בדיוק prev, ושלוש
הבקשות שלה נמדדו ב-643ccf2 מול אותו edge ואותו מסד.

שתי בקרות: «חזרה לרשימה» — בקשת cases אחת, כלומר הרענון לא אבד אלא נמצא במקום
שבו רואים אותו, והשורה מציגה «בטיפול» ואת «הוכרעה 14.08.2026, 15:05 · QA רענון
פריסה»; ו-#170, שלא נלחצה, נשארה «חדשה» בלי «הוכרעה». הוכחה דו-צדדית מהמסד:
169 → in_progress decided_by=46, 170 → new decided_by=null — ההסרה לא שינתה את
מה שנכתב, רק את מי שנשאל.

שני צילומים, שני MD5 שונים (69738139 / B69F8DF3). אפס הודעות בקונסולה.

מצב טסט נמדד ולא הוצהר: documents=0, אפס פניות ב-sent, אפס שורות outbound_queue
עם sent_at, outbound_queue נשאר 8, delivery_log נשאר 3, טביעת אצבע
bf57d69edaf6725ff7f52ad3007c4170 זהה בבסיס ואחרי.

התגלגל אחורה בחמש פקודות נפרדות לפי id ולא CTE אחד (case_rights → cases →
contacts → admin_sessions → admin_users) — תקלת 124cfc2 — ואנשי הקשר לפי id ולא
לפי טלפון שנשמר מנורמל — תקלת ac6e523; מזהי אנשי הקשר נלקחו לפני מחיקת הפניות,
כי cases_contact_id_fkey הוא ON DELETE SET NULL. אחרי: cases=0, case_rights=0,
documents=0, admin_users=1, admin_sessions=5, templates=2, contacts=4,
outbound_queue=8, delivery_log=3, catalog=888 — בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה ב-bkalut-app/bkalot-admin/
zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

ראיות: QA/bkalot-clone/status-refresh-deploy-0814.

פתוח: נתיב «שינוי סטטוס» סגור משני צדדיו ואין בו קו פתוח שנשאר. קווים שממשיכים
ולא נגעו: הקליטה מחברת 42 זכויות ל-treatment ואפס ל-info על אותו situation; אין
סינון «לא הוכרעה» ואין מיון לפי decided_at; אין היסטוריית הכרעות; «מי הפיק» על
documents אינו נכתב לשום מקום; ${out.text_chars} ו-${d.html_chars} נשארו במספר.
sent בלתי-ניתן-להשגה בכוונה; bkalot_clone_admin_create אינה חשופה ב-HTTP;
pdf/audio → channel_unsupported. public_visible ו-show_in_showcase של #37
נשארים false.
