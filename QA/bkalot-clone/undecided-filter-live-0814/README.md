# undecided-filter-live-0814 — הבקרה שנשארה פתוחה ב-98dbef1

הקו נלקח כפי שנוסח בתחתית `98dbef1`, מילה במילה: «**הרשימה המסוננת בייצור** —
הבקרה היחידה שהפעימה הזו לא סגרה, וטעונה אישורי מנהל (`bkalot_clone_admin_create`
דרך SQL). זו הפעימה הבאה **אם יהיה MCP**». יש MCP, ולכן יש מנהל, ולכן אפשר
למדוד את התשובה ולא רק את קיום השאלה.

**אין שינוי קוד, אין מיגרציה, אין פריסה.** `bkalot-clone-intake` נשארה v1
ו-`bkalot-clone-admin` v7; `apps/37-bkalot-clone` לא נגעה כלל (`git status` ריק
עליה לפני ואחרי). זו פעימת מדידה, ומה שנכנס ל-commit הוא ראיות בלבד.

## למה זו פעימה בפני עצמה ולא חזרה על ac527e0

ac527e0 מדד את **החלוקה** מול אותו edge ואותו מסד — אבל מהמסך שישב על שרת
סטטי מקומי. 98dbef1 מדד את **המסך שבייצור** — אבל רק את קיום התיבה, כי לא היו
אישורי מנהל. אף אחת מהשתיים לא מדדה את מה שהמנהל באמת רואה: תיבה שנשלחת
מהמסך שבייצור, ורשימה שחוזרת בגללה. `bkalot_clone_admin_create` אינה חשופה
ב-HTTP בכוונה, ולכן ה-MCP הוא מה שחסר, ולא הקוד.

## המנהל — נוצר דרך SQL ונמחק אחרי

`bkalot_clone_admin_create` דרך MCP → `{"ok":true,"admin":{"id":48}}`.
הסיסמה נכנסת ל-`crypt(..., gen_salt('bf',12))` באותו נתיב שהמוצר משתמש בו, ולא
הוזרק hash. המנהל, ה-session שההתחברות פתחה, וכל מה שנזרע — נמחקו בסוף.

## הזריעה — דרך נתיב הקליטה הציבורי

שלוש פניות דרך `bkalot-clone-intake` מעל HTTP עם מפתח anon, כמו מהטופס, ולא
בהזרקה ל-`cases`. שלושה טלפונים שונים בכוונה: הקליטה מאתרת איש קשר לפי טלפון,
ושלוש פניות מאותו טלפון היו נותנות איש קשר אחד.

| # | תפקיד | לפני המדידה |
|---|---|---|
| 177 | איש לא נגע בה | `new`, `decided_at` null |
| 178 | תוכרע **בלחיצה מהמסך החי** | `new`, `decided_at` null |
| 179 | הופק עליה מסמך (`render`, `document_id` 110, `queued=false`) | `in_progress`, `decided_at` **null** |

**179 היא הבקרה שקונה את הפעימה.** הטריגר `documents_advance_case` קידם אותה
ל-`in_progress` בלי שאיש לחץ עליה, ולכן ברשימה היא נראית בדיוק כמו פנייה שאדם
לקח. סינון על `status=new` היה מחמיץ אותה לגמרי ומחזיר 1 במקום 2 — כלומר שולח
את המנהל לפנייה שכבר בטיפול ומשאיר את זו שאיש לא הכריע מחוץ לרשימה. זו הסיבה
שהשאלה החדשה אינה כפילות של הסינון הקיים.

## המדידה — על הכתובת החיה, בדפדפן אמיתי

`https://more30.com/bkalot-studio/admin` עם cache-buster ולא הכתובת החשופה
(שניות אחרי פריסה דפדפן טרי קיבל HTML ישן על הכתובת החשופה בעוד ש-`fetch` על
אותה מכונה קיבל את החדש — f2001e0). דפדפן 1280x900, התחברות אמיתית דרך המסך.

התיבה נקראה **מתוך ה-`select` החי** ולא מה-HTML — היא נבנית בזמן ריצה, ולכן
נוכחות מחרוזת בקובץ אינה מוכיחה שהלולאה רצה: `«הכרעה ידנית»`, ושלוש אפשרויות
`["","הכול"]`, `["no","לא נרשמה"]`, `["yes","נרשמה"]`.

גוף כל בקשת `cases` נלכד כפי שיצא (`r.postData`) ולא שוחזר מהמסך: מסך שמציג
תיבה ואינו שולח את ערכה הוא בדיוק התקלה שנבדקה, ואין דרך להבחין ביניהן
מהמסך בלבד.

| צעד | `decided` שיצא ברשת | מה חזר למסך | המונה |
|---|---|---|---|
| ברירת מחדל | `""` | 179, 178, 177 | «3 פניות» |
| «לא נרשמה» | `no` | **179, 177** | «2 פניות» |
| «נרשמה» | `yes` | **178** | «פנייה אחת» |
| חזרה ל«הכול» | `""` | 179, 178, 177 | «3 פניות» |

**חלוקה מלאה ולא חופפת: 2 + 1 = 3**, ואף מזהה אינו בשתי הקבוצות. החזרה ל«הכול»
היא רגרסיה ולא קישוט — הסינון אינו נדבק, והרשימה חוזרת בדיוק למה שהייתה.

## ההכרעה על 178 — לחיצה, לא UPDATE

הפעולה היחידה בריצה כולה שכתבה `decided_at` היא לחיצת «בטיפול» על המסך שבייצור.
שורת האישור כלשונה: **«הסטטוס שונה מ«חדשה» ל«בטיפול» · לא נשלחה שום הודעה»**.

הוכחה דו-צדדית מהמסד, והיא מה שהופך את זה למדידה ולא לאמון במסך: **178 ו-179
שתיהן `in_progress`**, ורק ל-178 יש `decided_at` ו-`decided_by=48` — מזהה
המנהל שנוצר כאן, כלומר האדם שלחץ. שתי שורות עם אותו סטטוס בדיוק, ורק אחת מהן
חזרה תחת «נרשמה». סינון שמסתמך על `status` לא יכול היה להפריד ביניהן.

## מצב טסט — נמדד ולא הוצהר

`outbound_queue` נשאר 8 ואפס שורות בו עם `sent_at`, `delivery_log` נשאר 3,
אפס פניות ב-`sent`. מסמך אחד הופק (110) ולא יצא לשום מקום — `queued=false`.
אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## התגלגלות אחורה

שש פקודות **נפרדות** לפי id ולא CTE אחד — כל CTE קורא את התצלום שלפני הפקודה,
וה-`not exists` שנועד להגן מוחק 0 ומדווח הצלחה (תקלת 124cfc2). אנשי הקשר לפי id
(183, 184, 185) ולא לפי טלפון, שנשמר מנורמל (תקלת ac6e523). כל מחיקה החזירה
מספר והמספר נבדק: `documents` 1, `case_rights` 84, `cases` 3, `contacts` 3,
`admin_sessions` 1, `admin_users` 1.

אחרי: `cases`=0, `case_rights`=0, `documents`=0, `admin_users`=1,
`admin_sessions`=5, `templates`=2, `catalog`=888, `contacts`=4,
`outbound_queue`=8, `delivery_log`=3 — **בסיס בדיוק**.

## Heartbeat שהיה תקוע

`QA/bkalot-clone/undecided-filter-deploy-0814/_heartbeat.sql` נשמר כקובץ מפני
שלא היה MCP באותו סשן. הוא **הוחל כאן** ונכנס כ-`run_progress` 604 — הנוסח
נלקח מהקובץ ולא הומצא מחדש מתוך הודעת ה-commit.

הקובץ ציפה ל-605 בהנחה ש-604 שייך ל-ac527e0. **ל-ac527e0 אין שורה ואין קובץ**
— לא נכתב לו heartbeat באותו סשן, ולכן אין נוסח שנכתב בזמן אמת. הוא לא הומצא
כאן, והפער נרשם במפורש במקום להיסגר בהמצאה.

## מה שלא נמדד — ונאמר במפורש ולא נבלע

- **`decided_unknown` לא נמדד בדפדפן.** התיבה סגורה ואין ממנה דרך לשלוח ערך
  שלישי, וה-token יושב בסגור של המסך. הוא נמדד ב-a0e6a39 מול המסד.
- **המיון לא נמדד** — הוא לא קיים; ראו «מה שנשאר פתוח».
- 42 הזכויות לכל פנייה נמדדו כספירה (`case_rights` 84 = 42×2) ולא נפתחו
  זכות-זכות; ל-179 (`reminder`) אפס — הקו הפתוח הידוע, ולא ממצא חדש.

## קבצים

| קובץ | מה |
|---|---|
| `seed.mjs` / `seed.json` | שלוש הפניות דרך הקליטה הציבורית |
| `measure-live.mjs` / `measure-out.json` | הדפדפן על הכתובת החיה, כולל גופי הבקשות |
| `01-live-login.png` | מסך ההתחברות שבייצור |
| `02-default.png` | הרשימה בלי סינון — 3 |
| `03-decided-no.png` | «לא נרשמה» — 179 ו-177 |
| `04-decided-yes.png` | «נרשמה» — 178 |
| `05-back-to-all.png` | חזרה ל«הכול» — 3, רגרסיה |
| `_results.json` | סיכום מדיד |

חמישה צילומים, חמישה MD5 שונים (49B5458E / F4AA6303 / 06DB2FE0 / C114F29B /
871B70BA) — נבדקו ולא הונחו (תקלת screenshot-evidence-below-the-fold).
אפס הודעות בקונסולה ואפס בקשות שנכשלו.

## נתיבים מוגנים

`git status` ריק על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions`; אין נגיעה ב-
`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות `csj`/`csj_src`/`igud`.

## מה שנשאר פתוח

- **אין מיון לפי `decided_at`** — הוא מחליף את סדר ברירת המחדל של המסך והוא
  הכרעת מוצר נפרדת ולא תוספת; זו הפעימה הבאה אם תילקח.
- **אין heartbeat ל-ac527e0** — פער רישום, לא פער מוצר.
- אין היסטוריית הכרעות — הכרעה קודמת נדרסת, וגם הסינון יראה רק את האחרונה.
- הקליטה מחברת 42 זכויות ל-`treatment` ואפס ל-`info`/`reminder` על אותו
  `situation`; «מי הפיק» על `documents` אינו נכתב לשום מקום;
  `${out.text_chars}` ו-`${d.html_chars}` נשארו במספר; כותרות «זכויות (N)»
  ו«מסמכים (N)» נבדקו ולא שונו.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP;
  `pdf`/`audio` → `channel_unsupported`. `public_visible` ו-`show_in_showcase`
  של #37 נשארים `false`.
