# «מי הכריע ומתי» נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (1abad72) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה בלי השורה, והמדידה כאן
רצה מול שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר (ה-rewrite, ההעתקה ל-dist
ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה הבאה, כמו e9e949c → 530fb44».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-1abad72 (80,786 בתים,
MD5 `FBD4962E4F1FC1A7507AE57BBB7A3203`, `git diff` ריק) ו-`bkalot-clone-admin`
נשארה v7. אותה חלוקה כמו e9e949c → 530fb44, 978ab68 → fb607a0 ו-2a067be → 188bd04.

## הבדיקה שרצה לפני הפריסה ולא הונחה

`portal/dist` יושב על הדיסק, ופריסה מ-dist ישן מחזירה את כל הפורטל אחורה בלי
ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו לייצור — `index-C2AHwa-z.js`
ו-`index-B35ByRD-.css`, זהים בשני הצדדים לפני ואחרי.

`dist/bkalot-studio/admin.html` היה 76,528 (הקובץ של 530fb44,
MD5 `02F0FF9742BEA68AEE6F4AB0F224E116`) והמקור 80,786; אחרי ה-staging שניהם
80,786 עם אותו MD5 בדיוק (`FBD4962E4F1FC1A7507AE57BBB7A3203`).

## המסומנים נמדדו מול הגרסה הקודמת ולא הונחו

בפעימה הקודמת (530fb44) «נוסח נפילה» היה `true` **גם לפני** הפריסה — הנוסח ישב
בקובץ מאז 0063, ולולא נמדד מול הגרסה הקודמת הוא היה נקרא כראיה לפעימה שלא היה לה
שום קשר אליה. לכן כאן ששת המסומנים נבדקו קודם מקומית ב-`marker-check.mjs` על
שלושה קבצים:

| | `fillDecidedLine` ועוד חמישה |
|---|---|
| `git show 1abad72^` (הגרסה שבייצור) | **כולם false** |
| `apps/37-bkalot-clone/admin.html` (המקור) | כולם true |
| `portal/dist/…/admin.html` לפני ה-staging | כולם false |

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 76,764 | **81,022** |
| `/bkalot-studio/admin/` | 76,764 | 81,022 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 718 | **777** |

הדלתא מול המקור היא **236 בדיוק** (81,022 − 80,786) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים — כלומר
המדידה מראה גם רגרסיה ולא רק את התוספת. 718 → 777 אל״פים הוא בדיוק מה שנמדד
מקומית ב-1abad72.

ששת המסומנים של 1abad72 היו **כולם false לפני** ו-**כולם true אחרי**:
`fillDecidedLine`, «לא נרשמה הכרעה ידנית מהמסך», «מי הכריע אינו ידוע»,
«החשבון אינו קיים עוד», ``הוכרעה ${fmtDate``, ``מזהה מנהל $``.
שמונת המסומנים של הפעימות הקודמות (`<th>מכתב</th>`, `fillTemplateCell`,
`fillCreatedCell`, «ברירת מחדל לפי סוג הפנייה», `tmpl-sel`, «שינוי סטטוס»,
«עבד עכשיו», «הכנס לתור») היו true בשני הצדדים.

שפיות קידוד: אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.
המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM נקרא כ-cp1255,
והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin`, על פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon כמו מהטופס) ולא בהזרקה למסד:
**#105** treatment/single_parent עם 42 זכויות, **#106** בקרה. כניסה לניהול מעל
HTTP במסך עצמו, מנהל 30 («QA 0078 פריסה»).

| # | מצב | מה הוצג בייצור |
|---|------|--------|
| 01 | #105 לפני כל הכרעה | «לא נרשמה הכרעה ידנית מהמסך», אפס `span.stale` |
| 02 | לחיצה על «נסגרה» **בדפדפן** | «הוכרעה 14.08.2026, 08:22 · QA 0078 פריסה», title «מזהה מנהל 30» |
| 03 | #106, לא נגעו בה | «לא נרשמה הכרעה ידנית מהמסך» |

**המדידה שקונה את הפעימה:** הלחיצה ב-02 נעשתה בדפדפן על הכתובת החיה ועברה את
השער — כלומר נמדד המסלול המלא (rewrite → הקובץ שנפרס → edge → v7 → 0077 → המסך)
ולא כתיבה למסד. במסד אחרי הלחיצה: `decided_by=30`, `decided_at`
05:22:42.748479+00 — בדיוק 08:22 בשעון ישראל, מה שהשורה הציגה.

**הבקרה:** #106 נוצרה באותה ריצה ולא נגעו בה — `status=new`, ו-`created_at`
שווה בדיוק ל-`updated_at`. אצל #105 ההפרש הוא הלחיצה, ותו לא.

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — 0 מתוך 0 הודעות, בניגוד
למדידה המקומית של 1abad72 שבה היה `favicon.ico` 404 של השרת הסטטי.

שלושת הצילומים צולמו כשהשורה מוגללת למרכז ונבדקו ב-`Get-FileHash`:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-no-decision.png` | 60,797 | `550B4F6B7AB88FE7977A62FAE55140D4` |
| `02-live-decided-by-name.png` | 60,988 | `1D7F7E519CD052286CC7CD93224BF020` |
| `03-live-control-untouched.png` | 56,499 | `8E2173EE842FD6720853F7CB2778E6F9` |

## 🚫 מצב טסט — נמדד ולא הוצהר

`cases` ב-`sent` = 0, `documents` = 0, `outbound_queue` נשאר 8, שורות עם
`sent_at` = 0, שורות `mode=live` = 0, `delivery_log` נשאר 3. אין מייל, אין הודעה
ואין מסמך שיצא החוצה.

**המקור לא נגע, ונמדד ולא הונח:** 8 שורות `app_key=bkalot` נשארו 8, טביעת אצבע
`da28ec6315535ec0bfbebad4d57752a7` זהה לפני ואחרי.

## גלגול אחורה

42 `case_rights`, 2 פניות (105/106), 2 אנשי קשר, סשן אחד ומשתמש הניהול 30 — כולם
נמחקו בסדר הנכון. אחרי: `cases`=0, `case_rights`=0, `documents`=0,
`admin_users`=1, `admin_sessions`=5, `contacts`=4, `templates`=2 (שתיהן enabled),
`catalog`=888, `outbound_queue`=8, `delivery_log`=3 — זהה בדיוק לבסיס שנמדד לפני.

## אין נגיעה בשום נתיב מוגן

`git status` על `apps/08-bkalut-app`, `apps/09-bkalot-admin`,
`apps/37-bkalot-clone` ו-`supabase/functions` ריק, ואין נגיעה
ב-`bkalut-app`/`bkalot-admin`/`zr_*`/`NEDARIM3873` ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה נשאר פתוח ולא נבלע

- **מצבים (2) ו-(3) לא נמדדו כאן בייצור** — «מי הכריע אינו ידוע» ו«החשבון אינו
  קיים עוד». הם נמדדו ב-1abad72 מקומית, במחיקת מנהל ובעדכון ישיר של העמודה;
  כאן המסומנים בלבד מוכיחים שהקוד שמייצר אותם נפרס, לא שהוא רץ בייצור. אין ולו
  שורה אמיתית מלפני v7 במסד, ואין נתיב HTTP שיכול לייצר אותה.
- **הרשימה אינה יודעת** — `bkalot_clone_admin_cases` אינה מחזירה את השדות, ולכן
  רשימת העבודה עדיין אינה מבחינה בין פנייה שאדם סגר לפנייה שהטריגר קידם. זה
  הקו הבא.
- אין היסטוריה: הכרעה קודמת נדרסת.
- «מי הפיק» על `documents` אינו נכתב לשום מקום.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP
  (משתמש הבדיקה נוצר ב-RPC ישיר); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
