# #237 — בורר תבנית בשכפול בקלות: רישום פיגר אחרי המציאות

תאריך: 18/08/2026

## מה נמדד
core.issues #237 ("שכפול בקלות: למסך הניהול שני נוסחי שגיאה על תבנית שאינו
יכול לבחור") עמד status=open, עם detail שמתאר "נשאר: בורר תבנית ב-admin.html
+ פריסת פורטל". בפועל שני החלקים כבר נעשו ונמדדו ב-14/08/2026:

- **קוד**: `3fd1524` (0073 — templates בתשובת bkalot_clone_admin_case),
  `978ab68` (בורר "מכתב" ב-`apps/37-bkalot-clone/admin.html`).
- **פריסה**: `fb607a0` — staging ל-`portal/dist/bkalot-studio/admin.html` +
  `vercel deploy --prod`, עם מדידת דפדפן מלאה מול הייצור (12 מסומנים,
  שינוי תבנית בפועל שהופק 3505→222 תווים, keepTmpl שרד רענון, אפס שגיאות
  קונסולה).

## אימות חוזר היום (18/08)
```
GET https://more30.com/bkalot-studio/admin?cachebust=<random>
```
- `tmplSel` נמצא בגוף התשובה.
- "בורר המכתב" נמצא בגוף התשובה.
- "הכנס לתור" (#234, פעימה נפרדת) גם נמצא — פרוס.

## מסקנה
אין עבודת קוד/פריסה נדרשת. `core.issues` #237 עודכן ל-`status='fixed'`,
`resolved_at=now()`, עם detail שמסביר את הפער בין הרישום למציאות. שיעור
לסבבים הבאים: לפני שבוחרים כרטיס "פתוח, לא חסום" מ-core.issues, לבדוק את
הייצור החי — הרישום יכול לפגר אחרי עבודה שכבר בוצעה ונפרסה.

מוגן: לא נגעתי ב-08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873. לא בוצע
שינוי קוד או פריסה בצעד הזה.
