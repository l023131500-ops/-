שכפול בקלות שכבה 1: «צריך מספר שלם» והמיקוד שחוזר לשדה נמדדו מדפדפן ולא היו בייצור (§5ב, פריסה)

הקו הפתוח ש-193da4a הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם נפרס —
more30.com/bkalot-studio עדיין מגיש את הגרסה בלי סיבה ובלי מיקוד, והמדידה כאן רצה
מול שרתים סטטיים מקומיים; זו הפעימה הבאה». נלקח כפי שנוסח ולא הורחב: אין שינוי
קוד, אין מיגרציה ואין פריסת edge function. apps/37-bkalot-clone/index.html זהה
בדיוק לזה שנכנס ל-193da4a (25,483 בתים, MD5 B24A10595849FA24B1795F23C3F2A9F6).

למה זו פעימה בפני עצמה: המדידה הקודמת רצה מול שני שרתים סטטיים מקומיים
(8148/8149) מול ה-edge והמסד החיים — מדידה של הקוד ולא של המוצר. ה-rewrite
ב-vercel.dist.json, ההעתקה ב-stage-portal.ps1 ו-236 הבייטים ש-NetFree מזריק לא
נבדקו בה כלל.

REMOVED כאן הוא מחרוזת ולא סמיכות: 193da4a מחקה שורה ממש — הביטוי
body.dropped.map((k) => DROPPED[k] || k).join(", "), שהוא בדיוק מה ש-cd5340e
פרסה, הוחלף בגוף פונקציה שמחזיר name + " — " + why — ולכן «נפרס» אינו יכול להיות
גם «נפרסו שתי הגרסאות זו על גבי זו». חמשת מסומני ה-NEW הם קוד ולא הסבר, וכל אחד
נספר בקובץ לפני שנקבע ונמצא מופע יחיד; marker-check.mjs מדפיס ספירה ולא רק
includes. «מספר נושא» לבדו במפורש אינו מסומן — זו תווית השדה והיא קיימת גם בגרסה
שבייצור; המסומן הוא return "צריך מספר שלם";, כלומר הסיבה כקוד. שש הגרסאות נמדדו
ונופלות לשני צדדים נקיים: המקור, prev, dist לפני ואחרי ה-staging, ו-HTTP לפני
ואחרי. שבעת מסומני ה-OLD true בכל השש.

prev/index.html הועתק מ-portal/dist לפני ה-staging: 22,105 בתים,
MD5 66CA2D677A9DA86E79C716426499D771 — בדיוק מה ש-cd5340e פרסה, כלומר ה-dist שעל
הדיסק היה הקובץ שבייצור ולא קובץ ישן יותר. 38 קבצים נפרסו.

dpl_Ef8qCdpWqnJWoV3yAcRG7uMEjSrM, READY, aliased ל-more30.com. מ-portal/dist ובלי
--prebuilt: בלי .vercel/project.json ה-CLI יוצר פרויקט בשם "dist" ו-more30.com לא
מתעדכן, ו---prebuilt מפיל את vercel.json כלומר את מפת ה-rewrites של כל המערכות.

HTTP לפני ואחרי: הטופס 22,341 → 25,719 בשתי הכתובות (עם לוכסן ובלעדיו, שני
rewrites נפרדים), אל״פים 151 → 211. הדלתא מול המקור היא 236 בדיוק — דלתת NetFree
הידועה ולא מספר אחר. מסך הניהול נשאר 90,056 ודף הבית 3,517, ואותם שני נכסים
מגובבים (index-C2AHwa-z.js, index-B35ByRD-.css) — המדידה מראה גם רגרסיה ולא רק
את התוספת. אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.

נמדד בדפדפן אמיתי על הכתובת החיה, 1280x900. הפנייה נוצרה דרך הטופס עצמו ולא
בהזרקה, ו-«1e3» הוקלד לשדה בהקשות מקלדת ולא הושם ב-value. הדפדפן קורא לזה תקין —
checkValidity מחזיר true על השדה, כי 1e3 הוא מספר נקודה-צפה תקין ב-HTML גם על
type=number min=1 step=1; זו הסיבה שאדם מגיע לשורה הזו בכלל.

המדידה שקונה את הפעימה (#141): השורה על המסך החי אומרת «שדות שלא נשמרו: מספר
נושא — צריך מספר שלם», המיקוד על topic_no, הערך «1e3» עומד בשדה והוא מסומן
aria-invalid=true — אלמנט אחד בדף ולא יותר. השורה נמדדה ב-822–851 והשדה ב-272
בחלון של 900, שניהם בו-זמנית מעל הקו, ולכן הצילום מראה את הסיבה ואת המיקוד יחד;
תקלת הראיות של e12acf9 נבדקה ולא חזרה. שאר הטופס ריק אחרי ההחזרה, ולכן שליחה
חוזרת מיידית אינה מייצרת פנייה כפולה אלא נופלת על full_name_required.

מה שהמסד אמר, ולכן זו תקלת מוצר ולא ניסוח: #141 נשמרה עם topic_no=null בעוד
ש-raw->>'topic_no' הוא "1e3" — מה שהאזרח הקליד יושב ב-raw ולא בעמודה, והשורה
החדשה אומרת לו למה.

הבקרה (#142), אותו מסך חי, מספר נושא תקין (7): אין שורת «שדות שלא נשמרו» כלל,
אפס אלמנטים עם aria-invalid בדף, המיקוד לא נחטף (BODY) והטופס מתאפס במלואו וחוזר
ל-info עם ה-fieldset מוסתר — התיקון אינו ממציא שורה ואינו נדבק למסלול התקין.
topic_no=7 במסד. שתי הפניות 42 זכויות כל אחת; נתיב הזכויות לא זז. אפס הודעות
בקונסולה לאורך שתי הזרימות. שלושה צילומים, שלושה MD5 שונים.

מצב טסט נמדד ולא הוצהר: documents=0, outbound_queue נשאר 8, אפס שורות עם sent_at,
delivery_log נשאר 3, וטביעת האצבע של התור 050e54ab59235d105af3a10c785dbaea זהה
בבסיס, בזמן המדידה ואחריה. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (84 case_rights, 2 פניות, 2 אנשי קשר).
אחרי: cases=0, case_rights=0, documents=0, contacts=4, outbound_queue=8,
delivery_log=3, catalog=888 — זהה בדיוק לבסיס.

נתיבים מוגנים: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase/functions; אין נגיעה
ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

מה שנשאר פתוח: הנתון עצמו עדיין הולך לאיבוד, ו-#141 מוכיחה את זה בייצור. הטופס
אומר עכשיו מה נפל, למה, ומחזיר מיקוד — ואינו מונע את הנפילה מראש; חסימה לפני
השליחה נוגעת בחוזה של 0058 ולכן נשקלת בנפרד.

ראיות: QA/bkalot-clone/dropped-why-deploy-0814
