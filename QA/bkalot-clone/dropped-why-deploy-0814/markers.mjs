// המסומנים של 193da4a, במקום אחד, כדי שהבדיקה המקומית וה-HTTP ימדדו בדיוק את
// אותו דבר. הפיצול לשלוש רשימות הוא העיקר:
//
// NEW     — חייבים להיות false בגרסה שבייצור ו-true אחרי. אלה שקונים את הפעימה.
// REMOVED — ההפך: true לפני, false אחרי. בלעדיהם «נפרס» יכול להיות גם «נפרסו
//           שתי הגרסאות זו על גבי זו», וזה בדיוק מה ששני קבצים באותו גודל
//           נראים כמו.
// OLD     — true בשתי הגרסאות. אינם ראיה לפעימה אלא בקרת רגרסיה: פריסה מ-dist
//           ישן מחזירה מסכים אחורה בלי ששום דבר ידווח שגיאה.
//
// כמו ב-cd5340e וללא הרחבה: 193da4a מחקה שורה ממש — הביטוי
// `body.dropped.map((k) => DROPPED[k] || k).join(", ")` שנפרס ב-cd5340e הוחלף
// בגוף פונקציה שמחזיר `name + " — " + why`. לכן REMOVED הוא מחרוזת ולא סמיכות,
// והיא הקריאה החזקה ביותר: היא מופיעה בייצור, אינה מופיעה במקור, ואי אפשר
// לזייף אותה בפריסה כפולה.
//
// כל מסומן נספר בקובץ לפני שנקבע וכל אחד מהם מופע יחיד. מסומן שמופיע פעמיים
// היה מחזיר true על הגרסה שבייצור בזכות המופע האחר — המלכודת של 530fb44,
// 29d6ac6 ו-e12acf9. שים לב: «צריך מספר שלם» לבדו הוא מופע יחיד, אבל הוא נכתב
// כאן עם `return` שלפניו כדי שיימדד כקוד ולא כטקסט שעלול להופיע בהערה.
export const NEW = [
  "function whyDropped(key, sent) {",
  'return "צריך מספר שלם";',
  "function focusDropped(dropped, sent, sentKind) {",
  'el.setAttribute("aria-invalid", "true");',
  '}).join("; "));',
];

// מחרוזת שנמחקה ממש — לא סמיכות. זו בדיוק השורה ש-cd5340e פרסה לייצור.
export const REMOVED = [
  'body.dropped.map((k) => DROPPED[k] || k).join(", ")',
];

// בקרת רגרסיה על הטופס הציבורי עצמו: מפת התוויות שהפעימה הקודמת הכניסה ושאסור
// שתיעלם, הכותרת, שתי שורות האישור (החיה ומצב הטסט), והקידומת שלא זזה.
export const OLD = [
  "const DROPPED = {",
  'topic_no:  "מספר נושא",',
  "שדות שלא נשמרו: ",
  "הפנייה נקלטה",
  "הודעת אישור נשלחה אליכם.",
  "🚫 מצב טסט: הפנייה נשמרה, ולא יצאה ממנה שום הודעה.",
  "שליחת הפנייה",
];

export const report = (t) => ({
  new: NEW.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
  removed: REMOVED.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
  old: OLD.map((m) => JSON.stringify(m) + "=" + t.includes(m)).join(" | "),
});

// ספירת מופעים — לא includes. מסומן NEW שמופיע פעמיים בגרסה שבייצור בזכות
// המופע האחר הופך את כל המדידה לירוקה על מוצר שלא נפרס.
export const counts = (t) =>
  [...NEW, ...REMOVED, ...OLD].map((m) => JSON.stringify(m) + "=" + t.split(m).length - 1);
