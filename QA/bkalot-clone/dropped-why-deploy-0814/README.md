# «מספר נושא — צריך מספר שלם» והמיקוד שחוזר לשדה — פריסה לייצור (§5ב, 14/08)

הקו הפתוח ש-193da4a הותירה, מילה במילה: «הקובץ טרם נפרס — more30.com/bkalot-studio
עדיין מגיש את הגרסה בלי סיבה ובלי מיקוד, והמדידה כאן רצה מול שרתים סטטיים מקומיים;
זו הפעימה הבאה». נלקח כפי שנוסח ולא הורחב.

**אין שינוי קוד.** `apps/37-bkalot-clone/index.html` זהה בדיוק לזה שנכנס ל-193da4a —
25,483 בתים, MD5 `B24A10595849FA24B1795F23C3F2A9F6`.

## למה זו פעימה בפני עצמה

המדידה של 193da4a רצה מול שני שרתים סטטיים מקומיים (8148/8149) מול ה-edge והמסד
החיים — מדידה של הקוד ולא של המוצר. ה-rewrite ב-`vercel.dist.json`, ההעתקה
ב-`stage-portal.ps1` ו-236 הבייטים ש-NetFree מזריק לא נבדקו בה כלל.

## המסומנים, ולמה REMOVED הוא מחרוזת

193da4a מחקה שורה ממש: `body.dropped.map((k) => DROPPED[k] || k).join(", ")` — בדיוק
מה ש-cd5340e פרסה — הוחלף בגוף פונקציה שמחזיר `name + " — " + why`. לכן «נפרס» אינו
יכול להיות גם «נפרסו שתי הגרסאות זו על גבי זו». חמשת מסומני ה-NEW הם קוד ולא הסבר,
וכל אחד נספר בקובץ לפני שנקבע והוא **מופע יחיד** — מסומן שמופיע פעמיים היה מחזיר
true על הגרסה שבייצור בזכות המופע האחר, המלכודת של 530fb44, 29d6ac6 ו-e12acf9.
`marker-check.mjs` מדפיס ספירה ולא רק `includes`.

«מספר נושא» לבדו אינו מסומן — זו תווית שדה שקיימת גם בגרסה שבייצור. מה שמסומן הוא
`return "צריך מספר שלם";`, כלומר הסיבה כקוד.

שש הגרסאות נופלות לשני צדדים נקיים:

| גרסה | NEW | REMOVED |
|---|---|---|
| מקור | 5/5 | 0/1 |
| prev (מה שהיה בייצור) | 0/5 | 1/1 |
| `portal/dist` לפני staging | 0/5 | 1/1 |
| `portal/dist` אחרי staging | 5/5 | 0/1 |
| HTTP לפני | 0/5 | 1/1 |
| HTTP אחרי | 5/5 | 0/1 |

שבעת מסומני ה-OLD true בכל השש — בקרת רגרסיה, לא ראיה.

`prev/index.html` הועתק מ-`portal/dist` **לפני** ה-staging: 22,105 בתים,
MD5 `66CA2D677A9DA86E79C716426499D771` — בדיוק הקובץ ש-cd5340e פרסה, כלומר ה-dist
שעל הדיסק היה הקובץ שבייצור ולא קובץ ישן יותר.

## הפריסה

`dpl_Ef8qCdpWqnJWoV3yAcRG7uMEjSrM`, READY, aliased ל-more30.com. 38 קבצים נפרסו.
מ-`portal/dist` ובלי `--prebuilt`: בלי `.vercel/project.json` ה-CLI יוצר פרויקט בשם
"dist" ו-more30.com לא מתעדכן, ו-`--prebuilt` מפיל את `vercel.json` כלומר את מפת
ה-rewrites של כל המערכות.

## HTTP לפני ואחרי

הטופס 22,341 → 25,719 בשתי הכתובות (עם לוכסן ובלעדיו, שני rewrites נפרדים), אל״פים
151 → 211. הדלתא מול המקור היא 236 בדיוק — דלתת NetFree הידועה ולא מספר אחר. מסך
הניהול נשאר 90,056 ודף הבית 3,517, ואותם שני נכסים מגובבים
(`index-C2AHwa-z.js`, `index-B35ByRD-.css`) — המדידה מראה גם רגרסיה ולא רק את התוספת.
אפס תווי החלפה ואפס סימני כפל-קידוד בארבע הכתובות, לפני ואחרי.

## מה נמדד בדפדפן, על הכתובת החיה

1280x900. הפנייה נוצרה דרך הטופס עצמו ולא בהזרקה, ו-«1e3» הוקלד בהקשות מקלדת ולא
הושם ב-value. הדפדפן קורא לזה תקין — `checkValidity` מחזיר true על השדה, כי `1e3`
הוא מספר נקודה-צפה תקין ב-HTML גם על `type=number min=1 step=1`; זו הסיבה שאדם מגיע
לשורה הזו בכלל.

**המדידה שקונה את הפעימה (#141):** השורה על המסך החי אומרת
«שדות שלא נשמרו: מספר נושא — צריך מספר שלם», המיקוד על `topic_no`, הערך «1e3» עומד
בשדה, והוא מסומן `aria-invalid=true` — אלמנט אחד בדף ולא יותר. השורה נמדדה ב-822–851
והשדה ב-272 בחלון של 900, שניהם בו-זמנית מעל הקו, ולכן הצילום מראה את הסיבה ואת
המיקוד יחד; תקלת הראיות של e12acf9 נבדקה ולא חזרה.

שאר הטופס ריק אחרי ההחזרה (`full_name`/`phone`/`situation`), ולכן שליחה חוזרת מיידית
אינה מייצרת פנייה כפולה — היא נופלת על `full_name_required`. הסוג נשאר `treatment`,
אחרת השדה היה יושב ב-fieldset מוסתר ו-`focus()` היה נכשל בשקט.

**מה שהמסד אמר, ולכן זו תקלת מוצר ולא ניסוח:** #141 נשמרה עם `topic_no=null` בעוד
ש-`raw->>'topic_no'` הוא `"1e3"` — מה שהאזרח הקליד יושב ב-raw ולא בעמודה. השורה
החדשה אומרת לו למה.

**הבקרה (#142), אותו מסך חי, מספר נושא תקין (7):** אין שורת «שדות שלא נשמרו» כלל,
אפס אלמנטים עם `aria-invalid` בדף, המיקוד לא נחטף (BODY), והטופס מתאפס במלואו וחוזר
ל-info עם ה-fieldset מוסתר — התיקון אינו ממציא שורה ואינו נדבק למסלול התקין.
`topic_no=7` במסד. 42 זכויות בשתיהן; נתיב הזכויות לא זז.

אפס הודעות בקונסולה לאורך שתי הזרימות. שלושה צילומים, שלושה MD5 שונים.

## מצב טסט — נמדד ולא הוצהר

`documents=0`, `outbound_queue` נשאר 8, אפס שורות עם `sent_at`, `delivery_log` נשאר 3,
וטביעת האצבע של התור `050e54ab59235d105af3a10c785dbaea` זהה בבסיס, בזמן המדידה
ואחריה. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

## התגלגלות אחורה

84 `case_rights`, 2 פניות, 2 אנשי קשר — בסדר הנכון. אחרי: `cases=0`, `case_rights=0`,
`documents=0`, `contacts=4`, `outbound_queue=8`, `delivery_log=3`, `catalog=888` —
זהה בדיוק לבסיס.

## נתיבים מוגנים

אין נגיעה ב-08/09/`bkalut-app`/`bkalot-admin`/`zr_*`/NEDARIM3873 ולא בסכמות
`csj`/`csj_src`/`igud`.

## מה שנשאר פתוח

הנתון עצמו עדיין הולך לאיבוד, ו-#141 מוכיחה את זה בייצור: הטופס אומר מה נפל, למה,
ומחזיר מיקוד — ואינו מונע את הנפילה מראש. חסימה לפני השליחה נוגעת בחוזה של 0058
ונשקלת בנפרד.

קווים שממשיכים ולא נגעו כאן: הקליטה מחברת 42 זכויות ל-treatment ואפס ל-info על אותו
`situation` ולא נבדק אם מכוון; `await load()` שאחרי «שינוי סטטוס» מיותר ולא הוסר; אין
סינון «לא הוכרעה» ואין מיון לפי `decided_at`; אין היסטוריית הכרעות; «מי הפיק» על
`documents` אינו נכתב לשום מקום; `${out.text_chars}` ו-`${d.html_chars}` נשארו במספר;
כותרות «זכויות (N)» ו«מסמכים (N)» לא נגעו. `sent` בלתי-ניתן-להשגה בכוונה;
`bkalot_clone_admin_create` אינה חשופה ב-HTTP; pdf/audio → `channel_unsupported`.
`public_visible` ו-`show_in_showcase` של #37 נשארים false.

## הקבצים כאן

- `markers.mjs` — שלוש רשימות המסומנים, מקור יחיד לבדיקה המקומית ול-HTTP
- `marker-check.mjs` — בדיקת קובץ יחיד + ספירת מופעים
- `marker-check-{src,prev,dist-pre,dist-post}.txt` — ארבע הגרסאות המקומיות
- `probe-http.mjs`, `probe-http-{pre,post}.txt` — ארבע כתובות, לפני ואחרי
- `stage-out.txt` — 38 הקבצים ש-staging העמיד
- `deploy-out.txt` — פלט הפריסה
- `prev/index.html` — הקובץ שהיה בייצור, כפי שנלקח מ-`portal/dist` לפני ה-staging
- `01..03-*.png` — שלושת הצילומים מהכתובת החיה
- `_results.json` — כל המספרים
