שכפול בקלות שכבה 3: הנוסח העברי נמדד מדפדפן ולא היה בייצור (§5ב, פריסה)

הקו נכתב ב-53a3e5a (heartbeat 638), מילה במילה: «הפריסה. מה שנמדד כאן הוא הקוד
שבמקור; בייצור המסך עדיין מציג את הקוד הגולמי. אותו פיצול כמו aff6455 → 9c96135
וכמו b1fcca7 → 81e3307». נלקח ולא הורחב: פריסה בלבד — אין שינוי קוד, אין מיגרציה,
bkalot-clone-admin נשארת v8 ו-bkalot-clone-intake v1, supabase/ לא נגעה. מה שנכנס
ל-commit הוא ראיות בלבד.

הקובץ שנפרס הוא הקובץ שבמקור ולא עותק שלו: 131,108 בייט ו-MD5 3D4BEF6F במקור,
ואותו MD5 בדיוק ב-portal/dist אחרי stage-portal.ps1; לפני ה-staging ישב שם
129,476 (7106F46C) — מה ש-81e3307 פרס, כלומר שרשרת הפריסות נמדדה ולא הונחה.
38 קבצים, מ-portal/dist ובלי --prebuilt (הדגל מפיל את vercel.json ואיתו את מפת
ה-rewrites). dpl_9GxA7KUCVqqwwgGK8Yihj844j6z6, Ready, production, aliased
ל-more30.com.

שבעה מסומני NEW החזירו 0 בייצור ו-≥1 במקור לפני, והתהפכו אל שוויון אחרי בשתי
הכתובות: decider_required 0→1, «הסשן לא מסר מי מכריע» 0→1, «לא נרשמה שורת יומן»
0→2, «צאו והתחברו מחדש ונסו שוב» 0→1, «0092 הכרעות» 0→1, «מספר שלם בטוח» 0→1,
«שערי הקלט» 0→1. ארבעת ה-DIFF גדלו אל המקור — invalid_session 2→3,
token_required 1→2, 0076 3→4, «הסשן פג» 1→2 — כולם יושבים בייצור מזמן וההערה
החדשה מזכירה את כולם שוב, שכן זו בדיוק הכרעה (2) של הנוסח: מדוע אינו נבלע בהם.
שנים-עשר ה-OLD לא זזו לפני ולא אחרי: note_too_long 5, why-input 2, syncNoteCount
4, td.why 1, «רצף ההכרעות» 4, handsBit 2, producedBit 2, fillQueueCell 2,
fillTemplateCell 4, fillCreatedCell 4, «מי הכריע» 13, fillHistoryNoteCell 2. שתי
כתובות בקרה לא זזו: 32,558 ו-3,517. אפס תווי החלפה ואפס כפל-קידוד.
129,712 → 131,344 = 131,108 + 236, דלתת NetFree הידועה.

⚠️ בריצה הזו אף מסומן לא סווג מחדש — שנים-עשר מסומני הבקרה נמדדו שווים בדיוק, כמו
ב-81e3307 ובשונה מ-8a577bc, מ-24f8a7d ומ-9c96135 שבהם בקרה נפלה ל-DIFF מפני
שהערה חדשה הזכירה נוסח קיים בשמו. הסיווג נקבע ב-classify.mjs לפני הריצה הראשונה,
ושתי הריצות רצו מ-probe.mjs בדיוק כפי שהוא — תקלת 0087.

שני הצדדים נמדדו על אותה פנייה (269) ולא הונחו מ-0092 ומ-53a3e5a. השרת:
set_status(…, null) החזירה decider_required, ובפקודה נפרדת אחריה נמדד שהפנייה
נשארה new עם decided_at/decided_by ריקים ואפס שורות יומן — סאב-סלקט לצד הקריאה
הוא צילום שלפני המשפט, תקלת cte-delete-sees-prestatement-snapshot. המסך בייצור:
1280x900, התחברות אמיתית דרך המסך בחשבון 98, הפנייה נפתחה מהרשימה, «בטיפול»
נלחץ — «הסשן לא מסר מי מכריע, ולכן הסטטוס לא שונה ולא נרשמה שורת יומן. צאו
והתחברו מחדש ונסו שוב.», ומתחתיו decider_required בשורת .code. בפריים אחד גם
התיבה הריקה (211–310), גם השגיאה (440–459) והקוד (466–486), וגם «רצף ההכרעות»
(525) שאומר «לא נרשם ולו מעבר אחד מהמסך» — הנוסח מבטיח «לא שונה ולא נרשם»,
והדבר היחיד שמתחתיו מאשר את שניהם. הסטטוס נשאר «חדשה». המיקומים נמדדו אחרי
הגלילה ולא הונחו — תקלת screenshot-evidence-below-the-fold. קונסולה: אפס הודעות
ואפס שגיאות.

⚠️ ההזרקה מוצהרת ואינה נבלעת: אין דרך לגרום לשרת החי להחזיר את הקוד מהדפדפן,
ובכוונה — השער ממלא את p_admin_id מהטוקן תמיד ושולח null רק כשהערך אינו מספר שלם
בטוח. התשובה הוזרקה ב-window.fetch על הנתיב /set-status בלבד, פגיעה אחת שנספרה
(‎…/bkalot-clone-admin/set-status), גוף {"case_id":269,"status":"in_progress"};
login, cases ו-case נסעו אל השרת החי כרגיל. התנאי על ה-URL ולא על הגוף, שכן
call() בונה ${BASE}/${action} — תקלת ההתקנה שתוקנה ב-53a3e5a. אותה עטיפה בדיוק
כמו 53a3e5a וכמו 74dbf7d.

מצב טסט נמדד ולא הוצהר: queued=false בקליטה, אפס מסמכים, outbound_queue נשאר 8
ואפס שורות sent_at, delivery_log נשאר 3, אפס פניות ב-sent, אפס קריאות יוצאות. מה
שנפרס הוא HTML; שער ההכרעה נקרא פעם אחת ב-SQL וחזר ok=false לפני שנגע בשורה,
והלחיצה בדפדפן לא יצאה אל השרת כלל.

הפנייה נזרעה דרך bkalot-clone-intake מעל HTTP עם מפתח anon כמו מהטופס (269;
contact 276), והמנהל דרך bkalot_clone_admin_create (98) ולא בהזרקת hash.
התגלגל אחורה בפקודות נפרדות לפי טבלה — תקלת cte-delete-sees-prestatement-snapshot;
ואיש הקשר לפי id ולא לפי טלפון מנורמל — תקלת ac6e523. נמחקו: case_status_log 0,
case_rights 42, cases 1, contacts 1, admin_users 1. אחרי: cases 0,
case_status_log 0, case_rights 0, documents 0, admin_users 1, admin_sessions 5,
templates 2, contacts 4, outbound_queue 8, delivery_log 3, rights_catalog 888 —
בסיס בדיוק.

מוגן: git status ריק על apps/08-bkalut-app, apps/09-bkalot-admin,
apps/37-bkalot-clone ו-supabase; אין נגיעה ב-bkalut-app/bkalot-admin/zr_*/
NEDARIM3873 ולא בסכמות csj/csj_src/igud. ל-git add נמסר נתיב מפורש אחד ולא נקודה.

פתוח: נתיב ההודעה סגור מקצה לקצה — השער (0092), הנוסח (53a3e5a) והפריסה כאן. מה
שלא נבנה: אין ניסיון שני אוטומטי ואין רענון סשן שקט — «צאו והתחברו מחדש» הוא מה
שהמסך אומר ולא מה שהוא עושה, מפני שסשן שמתחדש לבדו על שגיאת זהות מסתיר בדיוק את
המצב שכדאי שייראה. אין חובת נימוק על rejected; אין עריכה ואין מחיקה ביומן; אין
חיפוש בטקסט הנימוקים; אין סינון ואין מיון לפי מספר מעברים; אין
status_last_change_at; אין שמות ברשימה; ואין backfill. קווים שממשיכים ולא נגעו:
טעינה מחדש של הכתובת בייצור מנתקת את הסשן; הקליטה מחברת 42 זכויות ל-treatment
ואפס ל-info/reminder על אותו situation. sent בלתי-ניתן-להשגה בכוונה;
bkalot_clone_admin_create אינה חשופה ב-HTTP ונקראת ב-SQL בארגומנט jsonb יחיד;
pdf/audio → channel_unsupported. public_visible ו-show_in_showcase של #37 נשארים
false.
