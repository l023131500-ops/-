# עמודת «מכתב» נמדדה מדפדפן ולא הייתה בייצור (§5ב, פריסה)

הקו הפתוח שהפעימה הקודמת (e9e949c) הותירה נוסח בה עצמה, מילה במילה: «הקובץ טרם
נפרס — more30.com/bkalot-studio/admin עדיין מגיש את הגרסה בלי העמודה, והמדידה כאן
רצה מול שרת סטטי מקומי, כלומר מדדה את הקוד ולא את המוצר (ה-rewrite, ההעתקה ל-dist
ו-236 הבייטים של NetFree לא נבדקו בה); זו הפעימה הבאה, כמו 978ab68 → fb607a0».

נלקח כפי שנוסח ולא הורחב: **אין שינוי קוד, אין מיגרציה ואין פריסת edge function.**
`apps/37-bkalot-clone/admin.html` זהה בדיוק לזה שנכנס ל-e9e949c (76,528 בתים,
MD5 `02F0FF9742BEA68AEE6F4AB0F224E116`, git diff ריק) ו-bkalot-clone-admin נשארה
v6. אותה חלוקה כמו 978ab68 → fb607a0, 2a067be → 188bd04 ו-37235a3 → e95a0ef.

## הבדיקה שרצה לפני הפריסה ולא הונחה

`portal/dist` יושב על הדיסק, ופריסה מ-dist ישן מחזירה את כל הפורטל אחורה בלי
ששום דבר ידווח שגיאה. שמות הנכסים המגובבים הושוו לייצור — `index-C2AHwa-z.js`
ו-`index-B35ByRD-.css`, זהים בשני הצדדים לפני ואחרי.

`dist/bkalot-studio/admin.html` היה 70,064 (הקובץ של fb607a0, MD5 `3E943EE5…`)
והמקור 76,528; אחרי ה-staging שניהם 76,528 עם אותו MD5 בדיוק
(`02F0FF9742BEA68AEE6F4AB0F224E116`).

## נמדד ב-HTTP לפני ואחרי

| | לפני | אחרי |
|---|---|---|
| `/bkalot-studio/admin` | 70,300 | **76,764** |
| `/bkalot-studio/admin/` | 70,300 | 76,764 |
| `/bkalot-studio/` (הטופס) | 21,272 | 21,272 |
| `more30.com/` | 3,517 | 3,517 |
| אל״פים במסך הניהול | 614 | 718 |

הדלתא מול המקור היא **236 בדיוק** (76,764 − 76,528) — דלתת NetFree הידועה ולא
מספר אחר. הטופס ודף הבית לא זזו ולו בבית אחד, ואותם שני נכסים מגובבים — כלומר
המדידה מראה גם רגרסיה ולא רק את התוספת.

שבעת המסומנים של e9e949c היו **כולם false לפני** ו-**כולם true אחרי**:
`<th>מכתב</th>`, `fillTemplateCell`, `fillCreatedCell`, «התבנית אינה קיימת»,
`נדרס ${fmtDate`, «אינו ידוע». ששת המסומנים של הפעימות הקודמות
(«ברירת מחדל לפי סוג הפנייה», `tmpl-sel`, «שינוי סטטוס», «עבד עכשיו»,
«כתובת המכתב», «הכנס לתור») היו true בשני הצדדים.

**מסומן שנרשם ונפסל, ולא נבלע:** «נוסח נפילה» היה true גם לפני הפריסה. הנוסח
יושב ב-admin.html מאז 0063 בשורת תוצאת ההפקה
(``תבנית ${out.template_key}${out.fallback ? " (נוסח נפילה)" : ""}``) — נמדד
ב-`git show e9e949c^` ולא הונח. הוא אינו מעיד על הפעימה הזו, ונשאר בפלט כדי
שלא ייקרא ככזה בדיעבד.

שפיות קידוד נמדדה: 614 → 718 אל״פים אמיתיים, אפס תווי החלפה ואפס סימני
כפל-קידוד. המדידה נכתבה ב-node ולא ב-PowerShell בכוונה — קובץ `.ps1` בלי BOM
נקרא כ-cp1255, והעברית שבתבנית נהרסת בזמן הפירוק ומדווחת «לא נמצא» על טקסט קיים.

## נמדד בדפדפן אמיתי על הכתובת החיה

1280x900, `more30.com/bkalot-studio/admin`, על פניות שנוצרו דרך נתיב הקליטה
הציבורי מעל HTTP (`bkalot-clone-intake`, מפתח anon מהטופס) ולא בהזרקה למסד:
**#92** treatment עם 59 זכויות, ו-**#93** היא הבקרה שלא נגעו בה כלל.

הכותרות בייצור: `# · סוג · כותרת · **מכתב** · נוצר · תור` — שבע עמודות ולא שש.

### המדידה שקונה את הפעימה, שתי הפקות על אותו מסמך #87

| | הפקה ראשונה (ברירת מחדל) | הפקה שנייה (general_inquiry_reply) |
|---|---|---|
| עמודת «מכתב» | מענה לפניית טיפול — רשימת הזכויות | **מענה לפנייה כללית** |
| ה-title שבתא | מפתח `rights_treatment_reply` | מפתח `general_inquiry_reply` |
| עמודת «נוצר» | 14.08.2026, 07:14 — בלי סימון | 07:14 + **«נדרס 14.08.2026, 07:15»** |
| תווי טקסט | 3,506 | **223** |

223 ולא 3,506: **מכתב אחר ולא תווית אחרת**. ו-#92 היא treatment — כלומר ברירת
המחדל שלה אינה יכולה להפיק את השורה הזו בשום מצב. במסד: מסמך #87, `created_at`
קפוא על 04:14:51.718016 ו-`updated_at` 04:15:26.767098 — **35.049 שניות**, בדיוק
מה שהעמודה מציגה.

הפאנל שמאחורי «הצג» אומר את אותם שני דברים בדיוק, ומאותן שתי פונקציות עזר:
«מכתב: מענה לפנייה כללית» ו«נוצר: 14.08.2026, 07:14 / נדרס 14.08.2026, 07:15»,
עם אותו title מילה במילה («הפק מחדש» כותב על אותה שורה, upsert על case_id
ו-kind…).

**אפס שגיאות ואפס אזהרות בקונסולה לאורך כל הזרימה** — 0 מתוך 0 הודעות, בניגוד
למדידה המקומית של e9e949c שבה היה `favicon.ico` 404 של השרת הסטטי.

### תקלת הראיות של e9e949c לא חזרה

שם הסדרה הראשונה יצאה זהה בבתים ושני קבצים חלקו MD5. כאן שלושת הצילומים צולמו
כשטבלת המסמכים מוגללת למרכז, ושלושתם נבדקו ב-`Get-FileHash`:

| קובץ | בתים | MD5 |
|---|---|---|
| `01-live-default-letter-row.png` | 85,375 | `188E7A681D13E9891A7870981072439F` |
| `02-live-other-letter-overwritten.png` | 87,409 | `40845C7D235CD835BD1F5661AC2F2473` |
| `03-live-panel-same-two-facts.png` | 50,785 | `921F1F4BB96DDC50129AAA1604DF5CE5` |

## מצב טסט — נמדד ולא הוצהר

`queue_id` של המסמך נשאר null, אפס פניות ב-`sent`, אפס שורות עם `sent_at`, אפס
שורות `mode=live`, `outbound_queue` נשאר 8 ו-`delivery_log` נשאר 3. הנתיב מפיק
וקורא בלבד. אין מייל, אין הודעה ואין מסמך שיצא החוצה.

**המקור לא נגע, ונמדד ולא הונח:** 8 שורות `app_key=bkalot` נשארו 8, טביעת אצבע
`da28ec6315535ec0bfbebad4d57752a7` זהה לפני ואחרי.

## הבקרה והגלגול אחורה

#93 נוצרה באותה ריצה ולא נגעו בה — נשארה `new`, `updated_at` שווה בדיוק
ל-`created_at`, ובלי ולו מסמך אחד. אצל #92 ההפרש הוא ההפקה, ותו לא.

הבדיקה התגלגלה אחורה במלואה ובסדר הנכון (מסמך, case_rights, פניות, אנשי קשר,
ואז הסשן ומשתמש הניהול qa0076dep). אחרי: `cases=0, case_rights=0, documents=0,
admin_users=1, admin_sessions=5, templates=2 (שתיהן enabled), catalog=888,
contacts=4, outbound_queue=8, delivery_log=3` — זהה בדיוק לבסיס שנמדד לפני.

**אין נגיעה בשום נתיב מוגן:** `git status` על `apps/08-bkalut-app`,
`apps/09-bkalot-admin` ו-`supabase/functions` ריק, ואין נגיעה
ב-bkalut-app/bkalot-admin/zr_*/NEDARIM3873 ולא בסכמות csj/csj_src/igud.

## מה שנשאר פתוח ולא נבלע

- בקרות (ב) ו-(ג) של e9e949c — מפתח בלי שורה, ושלושת השדות null — **לא נמדדו
  כאן בייצור**. הן נמדדו מקומית בעדכון ישיר של העמודות, ואין ולו שורה אמיתית
  אחת במסד שתדגים «לא ידוע»; המסומנים בלבד מוכיחים שהקוד שמייצר אותן נפרס, לא
  שהוא רץ בייצור.
- «נוסח נפילה» לא נמדד כאן בייצור מאותה סיבה — שתי התבניות היו enabled לאורך כל
  המדידה, ולא נוטרלה אף אחת מהן על המסד החי.
- «מי הפיק» ו«מי שינה ומתי» אינם נכתבים לשום מקום.
- `sent` בלתי-ניתן-להשגה בכוונה; `bkalot_clone_admin_create` אינה חשופה ב-HTTP
  (משתמש הבדיקה נוצר ב-SQL ישיר); pdf/audio → `channel_unsupported`.
- `public_visible` ו-`show_in_showcase` של #37 נשארים false.
