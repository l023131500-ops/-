# תוכנית אוטומציית בקלות — מבנה (טיוטה לאישור)

> העיקרון המוביל: לא לפגוע באיכות ולא במה שכבר חי באוויר עם לקוחות אמיתיים וסליקה.

## עיקרון-על
- לא נוגעים במערכות המוגנות/החיות: **bkalut-app, bkalot-admin, zr_*, NEDARIM3873** — רק קוראים ומבינים.
- בונים ב**מערכת חדשה נפרדת**, על ענף נפרד, PR בלי מיזוג בלי אישור.
- כל שלב מאומת לפני הבא. נתוני אמת בלבד. סודות מרוכזים ב-core.secrets.

## שלב 0 — לסיים את מה שבתהליך (עכשיו)
לסגור באיכות מלאה את עבודת הפלטפורמה הפתוחה: prerender לעמודי הנחיתה, מצב כהה ל-13 מערכות, וסריקת 33 המערכות (כל כפתור / UX / עיצוב / קופי). **לא מתחילים בקלות עד שזה סגור.**

## שלב 1 — חקירה ומיפוי (קריאה בלבד)
מיפוי מדויק, בלי לשנות כלום:
- מערכות בקלות: איך לקוח נרשם, איפה נשמר, איך נשלח לו תוכן היום, ומהי מערכת הזכויות הקיימת (התחלת CRM/שיתופי פעולה).
- נדרים פלוס: כתובת ה-callback הקיימת, אילו נתונים, לאן נכתבים.
- ימות המשיח: הקלטות, הקשות, צינתוק (callback), שלוחות, ומי מחזיק את מפתחות ה-API.
- מקורות התוכן: זכויות לפי מצב אישי (קטגוריות במאגר בקלות), השוואת מחירים בסופרים בזמן אמת, השוואת קופות חולים.
- מה כבר מחובר (ע"י המפתחת) מול מה שחסר וצריך לבנות עצמאית.

**תוצר:** `BKALOT_AUTOMATION_DISCOVERY.md` — מפת ארכיטקטורה, טבלת קיים/חלקי/חסר, וזרימות.

## שלב 2 — אפיון וארכיטקטורה (אני, מתוך הדו"ח)
- ניהול לקוחות מאובטח (CRM) + מסמכים + ניהול שיתופי פעולה.
- זרימה רב-ערוצית: לקוח נרשם (טופס / נדרים / ימות + הקשות / סוכן AI) → מזוהה מה ביקש → נשלף התוכן הרלוונטי → **תוכן מעוצב** → נשלח בכל הדרכים: מייל מעוצב + הקלטה/צינתוק בימות בשלוחת הלקוח + סוכן AI על המייל.
- קבלת ה-callbacks הקיימים מנדרים ומימות אל המערכת החדשה.
- אבטחה, הרשאות, יומן ראיות.

**תוצר:** קובץ בנייה מדויק + רשימת "מה דורש אותך" (מפתחות/גישות/החלטות).

## שלב 3 — בנייה מדורגת (מערכת חדשה)
בנייה שלב-שלב, אימות לכל שלב, איכות עיצוב והקלטות ברמה הגבוהה ביותר. שכפול הפונקציונליות למערכת חדשה — לא נגיעה במקור.

## שלב 4 — בדיקות קצה-לקצה + עלייה לאוויר
כל ערוץ (מייל, ימות/צינתוק, נדרים, סוכן AI) נבדק עם לקוח בדיקה אמיתי מקצה לקצה לפני הפעלה חיה.

## שער איכות בכל שלב
אימות (בדיקות + נתוני אמת + צילומי מסך) · לא נוגעים במוגן · סודות מרוכזים · PR בלי מיזוג · תיעוד מלא.
