# דו"ח בוקר — 03/08/2026

## מה הכי חשוב לדעת

**כל 30 הנתיבים חיים ומגישים תוכן אמיתי.** אפס מסכים ריקים, אפס נכסים
שבורים חוץ מאחד שכבר תוקן וממתין לפריסה.

**שני דברים שהיו סיכון אמיתי ונסגרו:**

1. **2,706 קבצי מקור חיו רק על הדיסק הזה.** `NEEDS_USER` דיווח על 21;
   הבדיקה מצאה שכל קוד המערכות היה מחוץ לכל ריפו, כי `/apps/**` מוחרג
   ב-`.gitignore`. הכול נשמר ונדחף. מפתחות (`.env*`) הוחרגו, וכל קובץ
   נסרק לאישורי גישה לפני השמירה.

2. **`briut` מגיש `<base href="/briut/">` שאינו קיים במקור.** כל נכס
   בעמוד הוא נתיב יחסי, ולכן העתקה שגרתית מהמקור הייתה מפילה כל CSS ו-JS
   ל-404 ומגישה **עמוד ריק מאחורי 200** — בדיוק מה שהפיל את 30 ו-31.
   נמצא בהשוואת שורות מול העמוד החי, לפני שהעתקתי. הוחזר למקור.

---

## מוכנות לשיווק — נמדד, לא הוערך

| מוכנה | הערה |
|---|---|
| **36 נדל"ן פרו** (`/tivuch`) | Lighthouse 98/100/100. חוזים, חתימה, יומן ראיות, שליחה במייל |
| **32 נדל"ן ברגע** (`/nadlan`) | נגישות 100. מצב כהה בדרך |
| **34 כסף** (`/kesef`) · **35 KioskFleet** (`/kiosk/`) | נגישות 100 בשתיהן |
| **06 לידים קופות** (`/briut`) · **28 השוואת קופות** (`/kupot`) | תוכן עשיר, אפס שגיאות |
| **10 מימוש זכויות בקלות** (`/bkalot`) · **22 מימוש זכויות** (`/zchuyot`) | 22 עדיין מתחת לסף ביצועים |
| **33 אתר התדמית** (`more30.com`) | 30 קישורים, נקי |

**קרובות אבל לא שם:** 01 torah (Lighthouse 69 — דורש SSR מלא) ·
15 egod · 16 chatzor · 27 mechiron (כולן חסומות על אותו סף ביצועים).

---

## מה תוקן בסבב הזה

- **כפתור הכניסה כיסה פקד אמיתי ב-10 מתוך 26 נתיבים** — לא סגנון, פקדים
  שאי אפשר היה ללחוץ עליהם. `auth-button.js` מפרסם עכשיו את הרוחב שהוא
  באמת תופס. **6 אומתו נקיים בפרודקשן**, 4 ממתינים לפריסה.
- **מצב כהה** נסגר ב-7 מערכות; `mthbram` התברר ככהה מלכתחילה ולא כחסרה.
- **zchuyot: 9 שגיאות קונסולה** — הידרציה שנכשלה (#418×8 → #423). הוסרה.
- **mechiron: ה-404 של הצ'אטבוט** — נסגר בכנות. המסלול עונה ומדווח
  `enabled:false`, כי מקור הנתונים אינו נגיש מהפריסה הזו.
- **נגישות:** 4 יעדי מגע ושני פקדים בלי שם נגיש.
- **ניקוי:** פרויקט ה-Vercel היתום `dist` נמחק.
- **באג שנתפס לפני שנפרס:** ב-nadlan הטוקנים החדשים החזיקו hex, ולכן
  Tailwind השמיט **36 מחלקות שקיפות בשקט**. תוקן ואומת בפלט הבנוי.

---

## 🔴 מה חוסם עכשיו — פריסה בלבד

**מכסת Vercel (100/יום) נגמרה אחרי 7 פריסות.** 13 פריסות ממתינות, הכול
בנוי ומוכן ב-staging. הרשימה המדויקת + שלוש פקודות האימות שאחריה:
`NEEDS_USER.md` §0ג.

---

## מה דורש אותך

| # | מה | למה אני לא יכול |
|---|---|---|
| 1 | **RLS על `recordings` בפרויקט `csjekrvu`** — ה-anon key מוחק 1,138 הקלטות | נבדק שוב: `list_projects` מחזיר פרויקט אחד (ההאב). `csjekrvu` בחשבון Supabase אחר |
| 2 | **`GREENINVOICE_API_KEY` + `_SECRET`** | חוסם הפקת חשבוניות ב-36 |
| 3 | **רשומת DNS `kiosk` ב-Cloudflare** + **חיבור ה-Volume ב-Railway** | בלי הראשונה אין WebSocket, בלי השנייה כל דיפלוי מוחק נתונים |
| 4 | **הכרעת `.gitignore`** — ריפו לכל מערכת, או ניהול הכול כאן | הסיכון המיידי הוסר, ההכרעה נשארה |
| 5 | **מקור האתר החי של `smachot`** ושל `nihul` | אינו על הדיסק. חוסם יעד מגע וחפיפת כניסה |
| 6 | **שדרוג תוכנית Vercel** (או המתנה יומית) | 100 פריסות ליום נגמרות בסבב עבודה אחד |

---

## הבא בתור, לפי הסדר

1. איפוס המכסה → 13 הפריסות → `authbutton-overlap` · `dark-probe` ·
   `console-probe`.
2. מצב כהה ל-5 הנותרות (03, 06, 10, 35 — 02 כבר נבנה).
3. רינדור מראש ל-22, 27, 28 → Lighthouse ≥ 90. (01, 15, 16 כבר נאפו.)
