# BKALOT_CLONE_BUILD — שכפול תפעולי מלא של "בקלות" (עותק עצמאי של המשתמש)

> **המטרה (מאושר ע"י המשתמש):** ליצור **עותק עצמאי ומלא** של מערכת בקלות התפעולית הפעילה — האתר ש**קולט פניות ושולח אוטומטית מיילים/מסמכים** — כדי שהמשתמש יוכל להפעיל אותו, ללמוד את שיטת העבודה, ולהמשיך לפתח בעצמו, **בלי תלות במפתח החיצוני ובלי n8n חיצוני**.
>
> זה **שונה** ממערכת 10 (מימוש זכויות) שכבר מוצגת בדף הבית — 10 היא האתר הצרכני לקריאה בלבד. כאן בונים את **הכלי התפעולי** על כל שכבותיו.

## ⚠️ גבול ברזל — קריאה מותרת, כתיבה אסורה
המשתמש אישר במפורש:
- ✅ **מותר לקרוא** קריאה-בלבד את **08 (bkalut-app "בקלות זכאות")** ו-**09 (bkalot-admin "בקלות ניהול")** — קוד, מבנה סכמה, קונפיגורציות — **כדי לשכפל את שיטת העבודה נאמנה**.
- 🚫 **אסור בהחלט** לשנות / למחוק / לכתוב / לפרוס-מחדש / לשבור את המקור החי 08/09, את `bkalut-app`, `bkalot-admin`, `zr_*`, `NEDARIM3873`.
- 🚫 **אסור** להשתמש בסכמות של ארגונים אחרים (`csj`, `csj_src`, `igud`) כמקור — אלה מערכות חיות של גורמים אחרים עם נתונים רגישים. אם שיטת השליחה של בקלות אינה קריאה מ-08/09 בלבד — **בנה מ-`BKALOT_AUTOMATION_BUILD.md`** ורשום ב-`NEEDS_USER.md`, אל תיגע בסכמה של ארגון אחר.
- 🚫 **מצב טסט בלבד** לכל שליחה/חיוב. אין הודעה/מסמך אמיתי החוצה בלי אישור מפורש.

## מבנה העותק (הכל חדש ונפרד — לא נוגע במקור)
- **סכמה ייעודית:** `bkalot_clone` (טבלאות חדשות). מנוע השליחה יכול לשבת על `bkalot_auto` שכבר קיים (contacts, intake_log, outbound_queue, delivery_log, topics, test_targets) — הרחב אותו, אל תמחק.
- **מאגר זכויות:** צור עותק קריא של `rights.catalog` (+ `rights.situation_map`, `rights.reference`) לתוך `bkalot_clone` **או** קרא ממנו חי — לפי מה שנקי יותר. 888 הנושאים חייבים להופיע בעותק.
- **פרויקט חדש ב-core.projects:** slug `bkalot-clone`, name_he "בקלות — עותק עבודה", path משלו (למשל `bkalot-studio`), is_protected=false, live אחרי אימות.
- **נתיבים משלו** תחת more30.com — נפרדים לגמרי מ-08/09/10.

## שלוש השכבות שצריך לשכפל
1. **אפליקציה (מקור: 08 זכאות):** קליטת פנייה/זכאות — הטופס והזרימה שבהם אזרח/לקוח נכנס ומזין פרטים, בדיוק כשיטת המקור.
2. **ניהול (מקור: 09 ניהול):** ממשק ניהול פנימי — רשימת פניות, לקוחות, נושאים, שליטה על מה שנשלח, לוגים. כולל **כניסה לניהול** (auth) כמו כל אתר.
3. **מנוע השליחה (מקור: שיטת בקלות + BKALOT_AUTOMATION_BUILD):** קליטת פנייה → הפקת **מסמך/מייל מעוצב** → שליחה (מייל דרך Resend / ואטסאפ / הקלטה לשלוחת ימות) → קמפיין. מודל "מספר נושא". הכל **עצמאי**, בלי n8n חיצוני. מצב טסט: השליחה נכתבת ל-`outbound_queue`/`test_targets` ולא יוצאת החוצה עד אישור.

## שיטת העבודה שצריך ללמוד ולתעד
תוך כדי הקריאה מ-08/09, **תעד ב-`BKALOT_METHOD.md`**: איך פנייה הופכת למסמך, מאיפה שולפים את נוסח הזכות, איך נבחר ערוץ השליחה, מה הטריגרים. זה מה שהמשתמש רוצה "להעתיק את שיטת העבודה" — שיהיה מסמך ברור של השיטה, לא רק קוד.

## אימות (חובה לפני live)
- העותק עולה, נטען, מאגר הזכויות מציג 888 נושאים.
- כניסה לניהול עובדת (משתמש בדיקה).
- זרימת פנייה → מסמך → תור שליחה **במצב טסט** עובדת מקצה לקצה, וההודעה **לא** יצאה החוצה.
- המקור החי 08/09 **לא נגעו בו** — ודא (git diff ריק על נתיבי המקור).
- צילומי מסך + heartbeat + עדכון SYSTEMS_STATUS ו-NEEDS_USER.

## מתי
זו עדיפות גבוהה של המשתמש. שבץ אחרי §1–§5 הנוכחיים ב-more30-priority.md, לפני §9 (הרחבת פיצ'רים). אל תעצור באמצע שכבה — סיים שכבה, אמת, commit+push, ואז המשך.
