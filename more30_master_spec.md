# 📘 מפרט אב — more30: נדל"ן, מרכז שליטה, ומפתחות

> מסמך אפיון מקיף. הטרמינל בונה לפיו. עקרונות-על: **נתוני אמת בלבד**, **שפה מדוברת וברורה** (בלי ז'רגון טכני באתרי הלקוח), **כל נתון מסומן במקור ובוודאות** (אמת / מקורב / הערכה), ואיכות גבוהה לפני מהירות.

---

## חלק א — מפתחות וחיבורים (משותף לכל המוצרים)

יש להזין את המפתחות הבאים כמשתני סביבה (secret store / env), **לא בקוד**, ולהשתמש בהם בכל המערכות הרלוונטיות:

- `GOOGLE_MAPS_API_KEY` — מפות, Places, Street View (צילום בניין), Distance Matrix (מרחקים), Geocoding, GTFS/תחבורה.
- `APIFY_TOKEN` — שליפת דירות מוצעות בזמן אמת (Yad2 / Madlan) דרך סקרייפרים של Apify, כולל היסטוריה.

מפתחות שכבר קיימים ומשמשים את המערכות: OpenAI, Anthropic, Gemini, Recraft, ElevenLabs, Yemot, DATAGOV, Supabase.

---

## חלק ב — נדל"ן ברגע: הדוח המלא + אתר תדמית

**בסיס:** להישען על האתר שנבנה לאחרונה ב-Lovable (Property Pulse / נדל"ן), ולשדרג אותו לפי המפרט הזה.

### 2.1 כניסה
כל הפקת כתובת → מעבר ישיר למסך תוצאות הדוח.

### 2.2 כותרת הדוח (בראש)
- הרחוב, העיר והמספר שנבדקו.
- הכתובת המדויקת + **גוש/חלקה**.

### 2.3 רקע כללי (לפני הפרטים)
- רקע על השכונה והיישוב.
- **אפיון סוג אוכלוסייה** מתוך תוצאות הבחירות לפי קלפי (data.gov.il) — מסומן "מקורב".
- כתוב בשפה מדוברת, קצר וברור.

### 2.4 ניווט לפי קטגוריות (אחרי הרקע)
לחיצה על כל קטגוריה פותחת את פרטיה:
1. **הנכס** — פרטים, שטח, מחיר, מחיר למ"ר.
2. **הבניין והכניסה** — קומות, כניסות, דירות, גודל קומה, פוטנציאל. (מסומן "הערכה" איפה שאין נתון ודאי.)
3. **עסקאות שנמכרו** — פירוט מלא לכל דירה: **רחוב + מספר**, תאריך רכישה, מחיר, מחיר למ"ר. **מיון מהקרוב ביותר לבניין והלאה.** בדיוק כמו ב-nadlan.gov.il.
4. **דירות מוצעות כרגע** (למכירה/השכרה) — דרך Apify (Yad2/Madlan), כולל היסטוריה. אם אין מפתח — "דורש מקור מורשה", בלי המצאות.
5. **סביבה ומוסדות** — לא "39 מוסדות", אלא פירוט: **סוג המוסד + שם + מרחק מדויק** (בית כנסת X — 120 מ', בי"ס Y — 300 מ').
6. **תחבורה** — קווים, תחנות אוטובוס, ומרחק/זמן לכל אחת.
7. **פוטנציאל** — מגמת מחירים לאורך זמן (גרף), השוואה לממוצע שכונה/עיר, הערכת תשואת שכירות, וזכויות בנייה/תב"ע (פוטנציאל עתידי).

### 2.5 מסך VIP (כפתור נפרד, פרימיום)
מחקר עמוק רב-כלי: כל השכבות + צילום הבניין (Street View) + מפה אזורית מלאה + **מצגת חיה** מסודרת מוכנה להצגה ללקוח + **גרסת PDF מעוצבת** להדפסה. עשוי לקחת יותר זמן ולהשתמש בכמה כלי AI — ומצדיק תשלום.

### 2.6 מקורות נתונים
- `nadlan.gov.il` — עסקאות + היסטוריה + מחיר למ"ר (חינם, רשמי).
- `data.gov.il` — בתי ספר, תחבורה (GTFS), תוצאות בחירות לפי קלפי.
- Google Maps — Places / Street View / מרחקים / תחבורה.
- Apify — Yad2 / Madlan (דירות מוצעות + היסטוריה).

### 2.7 שפה ועיצוב
הכל בעברית מדוברת וברורה, לכל נושא כותרת + הסבר קצר + "שורה תחתונה". מסודר, מבודד לפי נושאים, מתאים גם לאתר וגם להדפסה. **בלי הגדרות תכנותיות/טכניות באתר הלקוח.**

---

## חלק ג — מרכז שליטה בניהול (Admin)

בעמוד הניהול, לכל מערכת ובאופן מרוכז, להציג:

### 3.1 מפתחות וחיבורים
- כל מפתחות ה-env של המערכת (**מוסתרים** — מציגים רק 4 ספרות אחרונות), עם שם ומטרה.
- כל קישורי האתרים של המערכת (אתר חי, אדמין, more30.com/<שם>).

### 3.2 כלי ה-AI — לוח ספקים
טבלה מרכזית לכל ספק AI:
| ספק | מה עושה | קרדיטים שנותרו | הוסף קרדיטים |
|---|---|---|---|
| OpenAI | תמלול / ניסוח / GPT | (אוטומטי אם ניתן) | https://platform.openai.com/account/billing |
| Anthropic (Claude) | עיבוד טקסט / סוכנים | (ידני) | https://console.anthropic.com/settings/billing |
| Gemini | יצירת מודעות / ניסוח | (ידני) | https://aistudio.google.com |
| ElevenLabs | הודעות קוליות | ✅ אוטומטי (יש API ליתרה) | https://elevenlabs.io/app/subscription |
| Recraft | רקעים ותמונות מודעות | (אם ניתן) | https://www.recraft.ai |
| Apify | דירות מוצעות (נדל"ן) | (אם ניתן) | https://console.apify.com/billing |
| Google Maps | מפות/מרחקים/צילום | לפי חיוב | https://console.cloud.google.com/billing |

- קרדיטים: אוטומטי איפה שיש API ליתרה (ElevenLabs, OpenAI); איפה שאין — שדה ידני להזנה.
- לכל דוח/פעולה יקרה — לרשום את **עלות ההפקה** (סכימת קריאות API + טוקנים).

### 3.3 סטטוס מערכות
לכל מערכת: חי/לא, נתוני אמת כן/לא, נדרש/לא-נדרש, ומה עוד חסר לרמה גבוהה.

---

## חלק ד — עקרונות איכות (מחייב)
- נתוני אמת בלבד; כשאין — "לא זמין", בלי המצאות.
- כל נתון מסומן במקור ובדרגת וודאות.
- שפה מדוברת, ברורה, מסודרת.
- אל תיגע במוגן (bkalut-app, bkalot-admin, zr_*, NEDARIM3873); אל תשבור מערכת חיה; אין סודות בקוד.
- git commit אחרי כל שלב; דו"ח בסוף.
