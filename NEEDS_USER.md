# NEEDS_USER.md — כל מה שדורש אותך, במכה אחת

> נוצר לפי חוק 4 ב-MASTER_PLAN. **עודכן: 06/08/2026** (השלמת לוח התקלות).
> לכל פריט: מה בדיוק חסר · מאיפה משיגים · לאן זה נכנס.
> מסודר לפי מה שמשחרר הכי הרבה עבודה.

---

## 📋 הקובץ הזה יושב עכשיו גם על המסך — `more30.com/admin/issues`

הלוח הזה קיים מ-04/08, אבל הרשומה האחרונה שנכתבה בו הייתה מ-**05/08 22:06**.
מאז נרשמו 14 פעימות עבודה ואף אחת לא הגיעה אליו, כולל **שתי ההכרעות שחוסמות
אותך ופתוחות בקובץ הזה** — §0ח (`?auto=admin` בגליל) ו-§0ז (הגובה ודקות הדלקת
הנרות בחצור ובגליל). מי שהיה פותח את הלוח באותו רגע היה מסיק שלא קרה דבר.

**הושלם.** הלוח מחזיק עכשיו 16 פריטים ב"דורש אותך", 3 מהם קריטיים, כל אחד עם
מה שנמדד ומה בדיוק חוסם אותו. **ולמניעת הישנות** — המסך מדווח על הפיגור של
עצמו: כמה פעימות עבודה נרשמו מאז הפעם האחרונה שהלוח זז. מעל שלוש, מופיעה
אזהרה בראש העמוד. (הסף הוא שלוש ולא אחת כדי שהאזהרה לא תדלוק תמיד ותהפוך
לרעש.)

**מה זה משנה לך:** שתי ההכרעות למטה — §0ח ו-§0ז — אינן שאלות תיאורטיות. אחת
פותחת קונסולת ניהול בלי סיסמה, והשנייה מזיזה את כניסת השבת ב-13 דקות בשני
אתרים. שתיהן ממתינות לך, ושתיהן עכשיו גלויות גם במסך ולא רק בקובץ הזה.

---

## 🟠 0ס. עימוד (04) — הפונקציה נכתבה; חסרים שני מפתחות בפרויקט Vercel (10/08/2026)

**מה קרה:** ל-`more30.com/imud` לא הייתה פונקציית API כלל. כל קריאה ל-
`/imud/api/*` נבלעה ב-catch-all של ה-SPA וחזרה 200 `text/html`, ולכן כל
שמירה וטעינה במוצר נכשלה על `JSON.parse` — לא על סטטוס, ולכן שום דבר לא
נראה כשבור מבחוץ. ‏`core.issues` #153.

**מה נעשה:** הפונקציה נכתבה ונבדקה — אותם מסלולים כמו האפליקציה המקורית,
8/8 בבדיקת הניתוב, ו-`/api/meta` מחזיר נתונים אמיתיים (32 תבניות, 13
גופנים, 15 פונקציות, 4 שאלות). `QA/platform/imud-api-0810/`.

**מה נדרש ממך — שני משתני סביבה בפרויקט `imud-more30` ב-Vercel:**

* `SUPABASE_URL`
* `SUPABASE_SERVICE_KEY`

**למה דווקא service_role ולא anon:** הטבלה `public.otvedaf_books` מוגנת
ב-RLS **ללא policies** (נמדד: `rowsecurity=true`, 0 policies), כלומר anon
חסום ממנה לגמרי. יש בה 2 שורות אמיתיות. אפשר במקום זה לכתוב policies
ל-anon — אבל הבידוד בין משתמשים במערכת הזאת נשען על כותרת `X-Visitor-Id`
שהדפדפן שולח, כלומר על משהו שכל אחד יכול לכתוב, ולכן פתיחת הטבלה ל-anon
היא פתיחתה לכולם. לא עשיתי את זה לבד.

**ואז — פריסה.** עד שהשניים יוגדרו ותיעשה פריסה, הייצור עונה כמקודם.
בלי המשתנים הפונקציה אינה מתרסקת: היא מחזירה 500 עם שמות המשתנים החסרים
בגוף, כך שאפשר לאבחן את זה מהדפדפן.

---

## 🟡 0נ. עמוד המסלולים — הדלת נפתחה בקוד; נשארה הכרעה אחת (08/08/2026)

> **עודכן 08/08 — הכיוון הראשון בוצע.** כל כרטיס פעיל בדף הבית מחזיק עכשיו
> קישור שני, "מסלולים ומחירים", אל `/system.html?app=<שם>` — 19 קישורים
> (המערכת ה-20 היא האתר הזה עצמו, בלי `path`). השרשרת נמדדה מקצה לקצה מול
> ה-dist הבנוי: כרטיס → עמוד המסלולים → `/subscribe?app=torah&plan=basic`,
> עם מדרגות ה-2 ₪ וה-5 ₪ של §8א. `subscribe-funnel-entry.mjs` עבר מ-3/2
> ל-**5 passed · 0 failed**. הניתוב לא זז, ו-`/nadlan` עדיין מגיש את המערכת.
> `QA/platform/plans-link-0808/`. בייצור — רק אחרי הפריסה הבאה (§0ט).
>
> **מה שנשאר להכרעתך הוא רק השורה השנייה בטבלה למטה** — האם להחזיר את הפרדת
> הסמלוש, כלומר `more30.com/<שם>` לתדמית ו-`/<שם>/` למערכת. זה מזיז את הכתובת
> של 19 מערכות חיות, ולכן לא אעשה אותו בלי אישור. אם התשובה היא "להשאיר כמו
> שזה" — אין כאן עוד עבודה, והסעיף נסגר.

**זו הכרעה קטנה שלך, והיא שווה כסף.** `core.issues #120`.

הצינור של §8 שלם: `core.plans` מלא, `portal/public/system.html` מצייר לכל מערכת
את כרטיסי המסלול, וכל כרטיס מקשר אל `/subscribe`. הכל בנוי, והכל **חי בייצור
עכשיו** — `more30.com/system.html?app=nadlan` מחזיר 200 ועמוד גמור: שם המערכת,
מה השירות עושה, שבע יכולות, שלושה מסלולים (חינם · פרימיום 12 ₪ · VIP 15 ₪),
והמשפט שהסליקה לא נפתחה ושבחירת מסלול רושמת בקשה ואינה מחייבת. הצילום המלא:
`QA/platform/subscribe-entry-0808/orphan-system-page-nadlan.png`.

**מה שאין הוא הדלת.** סריקת 2,425 קבצי מקור (כולל untracked — מקורות אפליקציות
נמצאים ב-`.gitignore`, ו-`git grep` שותק עליהם): **אפס** מפנים אל `system.html`
בכל הריפו, ושני מפנים אל `/subscribe` — שניהם קובץ ניתוב והעמוד היתום עצמו.
כלומר אף עמוד שלקוח יכול לפגוש אינו מוביל לבחירת מסלול.

**למה זה קרה:** `docs/NIGHT_PROGRESS.md` מתאר סמלוש אחד —
`more30.com/<שם>` = תדמית, `more30.com/<שם>/` = המערכת — ו-`system.html` נכתב
בדיוק לכך (גוזר את המערכת מ-`location.pathname`, מכריז canonical על
`more30.com/<שם>`). אבל `portal/vercel.dist.json` מרכיב **גם** `/<שם>` וגם
`/<שם>/` על הפריסה של המערכת. הכתובת שהעמוד נכתב עבורה תפוסה.

| הכיוון | מה הוא נוגע בו | מה הוא עולה |
|---|---|---|
| **קישור שני בכרטיס בדף הבית** אל `/system.html?app=<שם>` | `portal/src/App.tsx` בלבד | שינוי ויזואלי ב-20 כרטיסים, טעון אימות משלו. הניתוב לא זז |
| **להחזיר את הפרדת הסמלוש** — `/<שם>` לתדמית | הניתוב של **20 מערכות חיות** | לא צעד קטן, ולא אעשה אותו בלי אישור |

**מה אני צריך ממך:** אחד מהשניים. לא בחרתי לבד כי השני מזיז את הכתובת של כל
מערכת חיה. הרגרסיה כבר נעולה בכל מקרה —
`node scripts/qa/subscribe-funnel-entry.mjs` נכשל היום על שתי טענות, ואחת
מטענותיו הירוקות היא ש-`/nadlan` עדיין מגיש את המערכת ולא עמוד שיווקי.

⚠️ בכל מקרה זה תלוי בפריסה (§0ט / `#83`), והתור מחזיק לפניו את שער
`/torah/legacy/*`.

---

## 🟡 0מ. מה לעשות עם #27 (mechiron) — הכרטיס מכר השוואת מחירים, והכתובת מגישה עמוד זכויות (07/08/2026)

**הכרטיס כבר ירד מדף הבית. מה שנשאר הוא הכרעה אחת שלך.** `core.issues #94`.

> **עודכן 07/08 09:0x —** ההסתרה הייתה חצי. `public_visible` נקרא בדף הבית ובשום
> מקום אחר, ולכן `/subscribe?app=mechiron` המשיך להציע **2 ₪ / 5 ₪** על אותה
> מערכת, ו-`more30_subscribe` רשם את הבקשה והחזיר `ok`. מיגרציה **0033** סוגרת
> את זה לכל 11 המערכות המוסתרות/הלא-חיות: קריאה מחזירה אפס מסלולים עם הסיבה,
> כתיבה נדחית. **השימוש לא נחסם** — מי שכבר בפנים ממשיך; נחסמה רק המכירה.
> ההכרעה למטה לא השתנתה, והיא עדיין שלך.

#27 ישב בדף הבית כאחת מ-21 המערכות הגלויות, ומכר את עצמו כ"השוואת מחירים
בקלות · אותו מוצר, מחיר חכם יותר". נמדד על הייצור — `/mechiron` מחזיר 200 ו-31,585
בייט, ובתוכם:

| מחרוזת | מופעים |
|---|---|
| "השוואת מחירים" | **0** |
| "מחיר" | **0** |
| "₪" | **0** |
| `price` | **0** |
| `compare` | **0** |

אפס, ולא "מעט". מה שמוגש שם הוא עמוד נחיתה של בקלות לזכויות והטבות
(`h1` = "בקלות — הזכויות וההטבות שמגיעות לכם"), שמשכפל את #10 ואת #22, ואחד
מקישורי הניווט שלו הוא #28. ה-`<title>` אומר את זה בעצמו: **"מאגר בקלות — כלי
פנימי לצוות"**. שכבת ה-API מתה: `/api/health`, `/api/search`, `/api/rights`,
`/api/prices`, `/api/compare`, `/api/items` — כולם 404, ו-`/api/` מחזיר 200 רק
כנפילה אל ה-SPA של הפורטל.

לפי §4 ("לא-עובדות → מוסתרות") הורדתי את הכרטיס: `public_visible=false`,
מיגרציה `0032`. **המאונט לא נגעתי בו** — `/mechiron` עדיין 200 — וההסתרה הפיכה
בשורה אחת.

**מה נדרש ממך — אחת משלוש, וכל אחת מהן משנה היקף מוצר:**

1. **לבנות** את השוואת המחירים בפועל על המאונט, ואז להחזיר `public_visible=true`.
2. **לשנות רישום** כך שיתאר את מה שבאמת מוגש — אבל אז #27 הוא כפילות של #10/#22,
   וצריך להחליט מי מהם נשאר.
3. **לפרוש** את #27: להוריד את המאונט ולסמן `to_delete`.

**לא נגעתי ב-`apps/27-bkalut-price`** — ה-`CLAUDE.md` שלו אומר שהוא נפרס אל
`/var/www/bkalut-app/`, שנמצא ברשימת המוגנות שלך.

ראיות: `QA/platform/mechiron-0807/_results.json` · `live-mechiron.png` ·
`home-after.png`.

---

## 🔴 0ל. מה כולל 2 ₪ ומה כולל 5 ₪ — 19 מערכות מציעות את זה כבר היום (07/08/2026)

**הכרעת מוצר אחת, ואני ממשיך לבד.** `core.issues #92`.

19 מערכות ציבוריות חיות מציעות היום את אותו סולם: **חינמי · בסיסי 2 ₪ ·
מורחב 5 ₪**. שלוש עובדות שנמדדו, ואינן מתיישבות זו עם זו:

1. **אין שער.** חיפוש על כל הריפו ועל כל פונקציות ה-`SECURITY DEFINER`:
   המחרוזות `basic` ו-`extended` אינן מופיעות באף תנאי שפותח יכולת.
   `plan_code` נקרא רק ב-checkout, subscribe, my_subscription ובלוחות
   הניהול — אף פעם כשער. מי שישלם 5 ₪ יקבל בדיוק את מה שמקבל מי שלא שילם.
2. **הקופי סותר את עצמו.** תגית החינמי: "גישה מלאה למה שהמערכת מציעה, בלי
   הגבלת זמן". תגית ה-2 ₪: "גישה מלאה למערכת, בלי הגבלת שימוש". אותה הבטחה
   בדיוק, פעם בחינם ופעם בתשלום.
3. **תגית ה-5 ₪ מבטיחה יכולת שאינה קיימת** — "ובנוסף שמירת היסטוריה
   וייצוא". אין מימוש כזה באף אחת מ-19 המערכות. אותן הבטחות מופיעות גם
   ברשימת הפרימיום ב-`portal/public/me.html`.

**מה נדרש ממך:** להכריע מה כל אחד משני המסלולים כולל בפועל. אחרי ההכרעה אני
כותב את השער, את ה-`features` ואת הקופי בעצמי. **לא שיניתי את התגיות לבד**
כי כל ניסוח שהייתי בוחר הוא המצאה של היקף מוצר, ולא נתון.

הסליקה סגורה (`core.billing_settings.mode='off'`), ולכן איש לא חויב על
ההבטחה הזו — אבל היא מוצגת ללקוחות היום.

---

## 🟡 0כ. שורת בדיקה אחת ב-`kupot_leads` שאני לא יכול למחוק (07/08/2026)

**קטן, ולכן כתוב כאן ולא נבלע.** כדי להכריע מה מסך הניהול של briut יכול
באמת לקרוא, מדדתי את ארבע הפעולות שהאתר מבצע מול הפרויקט החי
`csjekrvukbdznetsrodj`. מדידת ה-`POST` **כותבת שורה אמיתית** — אין דרך למדוד
"האם הטופס הציבורי עדיין עובד" בלי לשלוח פנייה. השורה מסומנת בבירור:

| עמודה | ערך |
|---|---|
| `source` | `qa-anon-probe-0807` |
| `full_name` | `QA probe more30` |
| `phone` | `000` |

**למה לא ניקיתי אחריי:** מפתח ה-`anon` — היחיד שיש לאתר — **אינו יכול למחוק**
(נמדד: `DELETE` → 401), ואין `service_role` לפרויקט הזה לא ב-`core.secrets`
ולא ב-MCP שלי. **מה צריך ממך:** מחיקה אחת מלוח ה-Table Editor.
למניעת הישנות, ההוספה בסקריפט נמצאת מעכשיו מאחורי `--insert`.

**מה המדידה הזאת גילתה, וזה החלק שמשנה תוכנית עבודה:** המפתח ש-briut נושא הוא
**insert-only** — `POST` 201, ו-`GET`/`PATCH`/`DELETE` כולם 401. כלומר
**הלידים מוגנים** (אף מבקר לא קורא, משנה או מוחק אותם), ובאותה נשימה **מסך
הניהול של briut אינו יכול לעבוד כלל** — לא בגלל השער אלא כי אין לו דרך לקרוא.
הוא היה מציג רק פניות שהוקלדו באותו דפדפן. briut יורדת מהכותרת "הפריט הזול
ברשימה" ודורשת נתיב צד-שרת כמו שאר השבע. `core.issues #86`.

---

## 🔴 0י. סיסמת אדמין אחת, בטקסט גלוי, בשני קבצים בריפו הציבורי (07/08/2026)

**נמצא תוך כדי עבודה על §2, ולא חיפשתי אותו.** `core.issues #86` סימן את מסך
הניהול של briut כ"הפריט הזול ביותר ברשימה" — פשוט להגיש קובץ שכבר קיים במקור.
קראתי את הקובץ לפני שהגשתי אותו, וטוב שכך:

| קובץ | מה היה שם |
|---|---|
| `apps/06-kupot-holim/site/admin.js:8-9` | `ADMIN_USER` ו-`ADMIN_PASS` כליטרלים, מושווים **בדפדפן** |
| `apps/27-bkalut-price/server/auth.ts:12` | **אותה סיסמה בדיוק**, בהערת התיעוד |

הריפו ציבורי — אומת מול ה-API של GitHub (`private: false`), לא הונח. שני
הקבצים נכנסו בקומיט `cb6e4f5`, קומיט ההצלה של 03/08 שהוסיף בכוח את כל `apps/`.
כלומר הסיסמה **פורסמה**, לא רק "ניתנת לקריאה".

**מה שמצמצם את החומרה, ונמדד ולא הונח: אין חשיפה בייצור.**
`more30.com/briut/admin.html` מחזיר 200 עם **14,759 בייט — בדיוק כמו**
`more30.com/briut/`, כלומר זו נפילת ה-SPA ולא הקובץ. אותו דבר ב-`/kupot`
(4,497 בייט בשניהם). המסך הזה פשוט אינו מוגש. *(שים לב: שום דבר לא מחזיר 404
במסלולים האלה, ולכן בדיקת סטטוס לבדה הייתה עוברת בשקט.)*

**מה שכבר עשיתי:** הליטרלים הוסרו מ-`admin.js` והשער נכשל סגור. אומת על
ה-build כי הפריסות חסומות על §0ט: session מזויף ב-`localStorage` כבר לא פותח
את הלוח — *וזה היה החור האמיתי, לא הסיסמה: ה"session" היה מפתח בלי חתימה,
כך שהקלדת שורה אחת בקונסולה פתחה את הלוח בלי לראות סיסמה מעולם* — הסיסמה
הישנה נדחית, והמחרוזת אינה ב-DOM. `QA/platform/kupot-admin-gate-0807/`.

**`apps/27` לא נגעתי בו במכוון.** ה-`CLAUDE.md` שלו אומר שהוא נפרס אל
`/var/www/bkalut-app/`, ו-`bkalut-app` נמצא ברשימת המוגנות שלך. זו שורת הערה
אחת — ולא אכריע לבד על גבול מוגן. **תגיד "מותר לגעת ב-27" ואסיר אותה בדקה.**

**מה צריך ממך — שני דברים, ושניהם רק אתה יכול:**

| מה | למה |
|---|---|
| **לסובב את הסיסמה** בכל מקום שמשתמש בה | הסרה מעץ העבודה **אינה** מוחקת מההיסטוריה. הנח שהיא נקראה |
| **להכריע על ההיסטוריה** — ריפו פרטי, או `git filter-repo` | זהה להכרעה שכבר פתוחה ב-§0 על נדרים פלוס. שכתוב היסטוריה הרסני ולא אעשה אותו בלי אישור |

**למניעת הישנות — והפעם זה נמדד:** `secret-scan.mjs` סרק 2,987 קבצים והחזיר
2 ממצאים, **בלי אף אחד משני אלה**, משתי סיבות נפרדות: `NAME` תפס
`password|passwd` אבל לא `pass` אחרי מפריד, ו-`ASSIGN`/`FALLBACK` דורשים
שניהם `שם <אופרטור> "ליטרל"` — בעוד ברירת מחדל בהערה היא פרוזה בלי אופרטור
ובלי מרכאות. שתי התבניות נוספו, והסריקה מחזירה עכשיו את שניהם.
`core.issues #88`.

**עדכון 09/08 — הסריקה חזרה להיות קריאה, והממצא של `apps/27` חזר להיראות.**
מאז שהתבניות נוספו הסריקה החזירה 29 ממצאים ויצאה באדום בכל ריצה, ו-28 מהם היו
אותה מחרוזת אחת: `access_token: 'qa.fixture.token'` ש-14 בדיקות Playwright
שותלות ב-`localStorage` כדי לעבור את השער בצד הלקוח (השער האמיתי הוא
`more30_is_admin` בשרת). הממצא היחיד שהוא באמת סיסמה — `apps/27` — היה השורה
הראשונה מתוך 29, ו-`MORNING_REPORT` רשם על אותה סריקה **"0 ממצאים"**.

עכשיו: פיקסטורות `qa.fixture` הן `BENIGN`; דליפה מוכרת-ופתוחה נכנסת לרשימת
`KNOWN` נפרדת מ-`ACCEPTED` — *`ACCEPTED` אומרת "קראתי, זה לא סוד", `KNOWN`
אומרת "זה כן סוד, הוא כן פורסם, וההכרעה להסיר אינה שלי"* — ומודפסת בכל ריצה
עם מספר הבאג, אבל אינה מאדימה את הריצה. ועוד שומר: כל השתקה שלא התאימה לשום
דבר בריצה מאדימה את הריצה, כי השתקה מתה נראית כמו הבנה שאין לסריקה. הוא כבר
מצא שתיים כאלה, ושתיהן נמחקו.

**מה שלא השתנה: `apps/27` עדיין מפרסם את הסיסמה, ושתי השורות בטבלה למעלה
עדיין מחכות לך.** הסריקה עכשיו אומרת את זה בקול בכל ריצה במקום לטבוע.

---

## 🟠 0ט. מכסת הפריסות של Vercel — mthbram ירד, הפורטל נכנס לתור (07/08/2026)

> **עודכן 08/08 — הפורטל נפרס, והתור שלו ריק.** נמדד פעמיים מול `more30.com`
> כאנונימי, על כל 14 המסלולים ש-`portal/vercel.dist.json` מגיש מ-`portal/public/`
> ועל `auth-button.js` שכולם טוענים. ב-22:59 **12 מתוך 15 היו ישנים**, ואיתם
> ארבעה קומיטים שנמדדו בשמם ולא היו בייצור — `e95b47e` (סרגל אחים ל-/admin/pricing),
> `f12dd5b` (שני המסכים שלא היו על אף סרגל), `5c0c007` (סרגל מלא בתשעת המסכים),
> `7d25bea` (שם וטלפון בטבלת הלקוחות). במדידה החוזרת **כל 15 זהים בייט-בבייט**
> וכל ארבעת הסימנים חיים. הפער נסגר. `scripts/qa/portal-deploy-drift.mjs`,
> `QA/platform/portal-deploy-drift-0808/`.
>
> **עודכן 08/08 (מאוחר יותר) — לפורטל שוב יש שינוי שאינו בייצור.** התיקון של
> `core.issues #119` (כתובת בלי מערכת אומרת "לא נמצאה" במקום להגיש את דף הבית)
> נמצא ב-`portal/src/App.tsx`, נבנה ונמדד מקומית 20/20 —
> `node scripts/qa/unknown-path-renders-404.mjs`. **הוא לא נפרס בכוונה:** התור
> מחזיק גם את שער `/torah/legacy/*` (חשיפת ניהול בלי סיסמה, `#87`), וזה קודם.
> כשתרוץ פריסת פורטל, הרצה חוזרת של אותה בדיקה מול `https://more30.com` מאמתת.
>
> **מה שנשאר פתוח בסעיף הזה:** המכסה עצמה (`core.issues #83`) — היא לא השתנתה,
> ולכן הפריסה הבאה עדיין תלויה בחלון. השורה על שלוש קונסולות הניהול של מערכת 01
> למטה **לא נמדדה בסבב הזה** ואינה מסומנת כסגורה.

> **מצב קודם (07/08 08:06):** הפריט שעמד בתור, `mthbram`, **נפרס** — ראה בסוף
> הסעיף. **הבא בתור הוא `portal`**, ולא בהערכה: `more30.com/subscribe` נמדד
> עכשיו ומחזיר 15,940 בייט שעדיין מכילים `<ul>${feats}</ul>` ובלי
> `.plan .btn{margin-top:auto}` — כלומר תיקון כרטיסי המסלול (`25a47ce`) ושאר
> שינויי התצוגה של הסבבים 0025–0031 חיים בגיט ולא בייצור.
> `core.issues #83` נשאר פתוח כי המכסה עצמה לא השתנתה.

`vercel deploy --prod` נעצר בשלב האחרון, אחרי שההעלאה כבר הצליחה:

```
"reason": "deploy_failed",
"message": "Resource is limited - try again in 24 hours
            (more than 100, code: \"api-deployments-free-per-day\")"
```

זו מגבלת חשבון ולא תקלה בקוד: התוכנית החינמית מרשה 100 פריסות ל-24 שעות,
והריצות של 06–07/08 עברו אותה.

**מה זה משנה בפועל:** כל צעד שנגמר בפריסה נעצר בסנטימטר האחרון — הקוד נבנה,
נמדד ונכנס לגיט, אבל `more30.com` ממשיך להגיש את הגרסה הקודמת. לכן גם כל מדידה
מול הייצור תמשיך לדווח את המצב הישן, ולוחות ה-QA ישארו על ❌ בדברים שכבר תוקנו.
הפריט הראשון שנתקע כך הוא `mthbram` (‎core.issues #82‎): הבנייה מוכנה ומאומתת
ב-`_deploy/mthbram-more30`, וממתינה רק להרצה חוזרת אחת. ✅ **נפרס ב-08:06 —
ראה בסוף הסעיף.**

**הפריט השני דחוף יותר, ולכן הוא כתוב כאן ולא רק ב-#87:** שלוש קונסולות הניהול
הישנות של מערכת 01 פתוחות כרגע בייצור לכל מי שיודע את הכתובת, בלי סיסמה —
`/torah/legacy/admin` (לוח השיעורים על שמות הרבנים והערים, עריכה, גיבוי ZIP,
ייצוא לנדרים), `/torah/legacy/ivr` ו-`/torah/legacy/nedarim`. השער נכתב, נמדד
על ה-build ונדחף (קומיט `ba2c41b`), **והייצור ימשיך להגיש את הגרסה הפתוחה עד
שתרוץ פריסה אחת**. תיבות הלידים עצמן סגורות — ה-RLS מחזיק אותן מול מפתח ה-anon,
ולכן החשיפה היא המסך והפקדים. `core.issues #87`.

**מה צריך ממך:** או להמתין לאיפוס חלון ה-24 שעות ואז להריץ פריסה אחת, או
לשדרג את החשבון ל-Vercel Pro אם הקצב הזה הוא הקצב הקבוע. `core.issues #83`.

> **עודכן 07/08 04:40 — נוסתה שוב ונדחתה.** ההעלאה הצליחה והשלב האחרון החזיר את
> אותה הודעה בדיוק, כלומר החלון טרם התאפס. **מה שכן הוכן בינתיים:** הבנייה
> המגודרת של torah הועברה אל `_deploy/torah-more30` **ונאפתה מחדש עם ה-seed**
> דרך `scripts/prerender-all.mjs` — בלי הצעד הזה הפריסה הייתה מחזירה את רגרסיית
> ה-CLS של 03/08 (0.001 → 0.578). כלומר הפריסה הבאה היא **פקודה אחת** מהתיקייה
> הזאת, בלי בנייה נוספת:
>
> ```
> ~~cd _deploy/torah-more30~~   ← ✅ בוצע ב-05:25. אל תריץ. ראה למטה.
> ```
>
> ⚠️ **הפקודה הזאת כבר רצה** — הבלוק נשאר כאן כתיעוד של הרצף בלבד. הפקודה
> החיה היחידה בסעיף הזה היא זו שבסוף, ב-"הפריסה הבאה בתור".

### ✅ עודכן 07/08 05:25 — **הקונסולות הפתוחות נסגרו בייצור. הפקודה האחת רצה.**

הפקודה שלמעלה הורצה והפעם עברה: `dpl_CrB5iFg7RidHqhGQfPfhZ5XZQ4C7`, מצב
`READY`, aliased ל-`torah-more30.vercel.app`. מיד אחריה נמדד הייצור עצמו דרך
`more30.com`, ושלושת הנתיבים שהיו פתוחים — `/torah/legacy/admin`,
`/torah/legacy/ivr`, `/torah/legacy/nedarim` — מפנים עכשיו למסך הכניסה ושומרים
את הדרך חזרה. **`core.issues #87` נסגר.** ראיות:
`QA/platform/spa-admin-torah-0807/`.

**מה עדיין דורש אותך, ולמה זה לא נפתר:** החלון אינו מתאפס בבת אחת אלא מתגלגל
ומשחרר מקומות בודדים. הפריסה השנייה שניסיתי באותה דקה — `mthbram`, שהבנייה שלה
מוכנה ומאומתת ב-`_deploy/mthbram-more30` — **נדחתה עם אותה הודעה בדיוק**
(`api-deployments-free-per-day`). כלומר יש כרגע כ-פריסה אחת פנויה לכל סבב, וכל
צעד שנגמר בפריסה ימשיך להמתין בתור. סדר התור שקבעתי לבד: קונסולה פתוחה בייצור
לפני מצב כהה. **ההכרעה שלך:** לשדרג ל-Vercel Pro אם הקצב הזה קבוע, או להשאיר
כך ולתת לכל סבב לשחרר פריט אחד. `core.issues #83` נשאר פתוח.

הפריסה הבאה בתור, כשיתפנה מקום — בלי בנייה:

```
~~cd _deploy/mthbram-more30~~   ← ✅ בוצע ב-08:06. אל תריץ. ראה בסוף הסעיף.
```

⚠️ **הפקודה הזאת כבר רצה.** הבלוק נשאר כתיעוד של הרצף בלבד.
הפקודה החיה היחידה בסעיף הזה היא זו שבסוף, תחת "הבא בתור".

### ⚠️ עודכן 07/08 06:05 — חלון חדש נפתח, והמקום היחיד בו בוזבז על פריסה כפולה

החלון אכן מתגלגל: ב-06:03 `_deploy/torah-more30` נפרס והצליח
(`dpl_5J7mzdrie6yPMMnEZJkFTCfd3xRe`, `READY`). **אבל torah כבר נפרס ב-05:25** —
זו הייתה אותה תיקייה, אותו תוכן, פריסה כפולה שלא שינתה דבר בייצור. מיד אחריה
`_deploy/mthbram-more30`, שהוא באמת הבא בתור, **נדחה שוב** באותה הודעה
(`api-deployments-free-per-day`). כלומר המקום היחיד שהתפנה בחלון הזה הלך לריק,
ו-`core.issues #82` ממתין לחלון הבא.

**למה זה קרה, וזה תוקן:** בסעיף הזה עמדו **שני בלוקי "פקודה אחת"** — הראשון
(`cd _deploy/torah-more30`) עשרים שורות מעל ההודעה שהוא כבר רץ. קריאה מלמעלה
למטה מגיעה לפקודה הישנה קודם. הבלוק הישן סומן עכשיו כבוצע, ונשאר בדיוק בלוק
פקודה חי אחד בסעיף.

**מה כן נמדד ברווח הזה:** הייצור נמדד שוב דרך `more30.com` וארבעת הנתיבים
המגודרים של torah — `/legacy/admin`, `/legacy/ivr`, `/legacy/nedarim`, `/admin` —
כולם מפנים למסך כניסה עם שדה סיסמה, 4/4 נמדדו, אפס קונסולות פתוחות. זה מאשר
שהסגירה של 05:25 מחזיקה. ראיות: `QA/platform/route-gate-torah-more30-com/`.

### ✅ עודכן 07/08 08:06 — **mthbram נפרס. תור הפריסות ריק.**

החלון התגלגל שוב ושחרר מקום, והפעם הוא הלך לפריט שבאמת עמד בתור:
`_deploy/mthbram-more30` נפרס והצליח — `dpl_6PcRHMH3473rq1SjKtMLmYSv2Ddx`,
מצב `READY`, aliased ל-`mthbram-more30.vercel.app`.

**נמדד לפני הפריסה, על התיקייה עצמה:** `assets/index-CIaqlLS4.css` מכיל
`color-scheme:dark`, וחבילת ה-JS זהה בייט-לבייט לזו שהייתה בייצור
(sha256 `DE489454…BEEA96`) — כלומר הפריסה שינתה גיליון סגנון בלבד ולא נגעה
בהתנהגות.

**נמדד אחרי, על הייצור עצמו דרך `more30.com`** (‎`scripts/qa/color-scheme-probe.mjs`‎,
עם מצב מערכת ההפעלה מאולץ ל-light — המצב שבו החוסר היה מזיק):

| | לפני | אחרי |
|---|---|---|
| computed `color-scheme` | `"normal"` | **`"dark"`** |
| רקע אפקטיבי | `rgb(9,26,32)` (0.009) | `rgb(9,26,32)` (0.009) |
| `dark-by-design` | ❌ NO | ✅ **YES** |

זה מסיים את הפער האחרון מתוך 30 הנתיבים ב-`QA/platform/SUMMARY.md` שדיווח
"אין מצב כהה", והוא גם התיקון עצמו: בלי ההצהרה הדפדפן צבע שדות קלט, `select`
ופסי גלילה בברירת המחדל הבהירה על גבי עמוד כהה. ראיות:
`QA/platform/colorscheme-mthbram-0807/`. **`core.issues #82` נסגר.**

**הבא בתור — `portal`, ונמדד ולא שוער.** לפני שסימנתי את התור כריק בדקתי,
והייצור הכחיש: `more30.com/subscribe` מחזיר 200 עם 15,940 בייט שעדיין מכילים
`<ul>${feats}</ul>` ואין בהם `.plan .btn{margin-top:auto}`. כלומר תיקון כרטיסי
המסלול (`25a47ce`) ואיתו שאר שינויי התצוגה של 0025–0031 — הבאנר של
`/admin/pricing`, אריח ה-KPI ב-`/admin/systems`, המסלול ב-`/me` — כולם בגיט
ולא בייצור. זו **אינה** פקודה אחת: הפורטל דורש בנייה לפני פריסה.

```
npx pnpm --filter @more30/portal build
.\scripts\stage-portal.ps1
cd portal/dist ; npx vercel deploy --prod --yes --scope l023131500-ops-projects
```

> `pwsh` אינו מותקן על המכונה הזאת — הבלוק הקודם נפתח ב-`pwsh scripts/stage-portal.ps1`
> ומחזיר "is not recognized". החלון של PowerShell 5.1 מריץ את הסקריפט ישירות.
> נמדד 07/08 09:0x: הבנייה והערימה עוברות (`dist` מלא, `vercel.json` ו-`api/`
> במקומם), והפריסה עצמה עדיין נדחית ב-`api-deployments-free-per-day`.

**מה עדיין דורש אותך:** `core.issues #83` נשאר פתוח — המכסה לא בוטלה, החלון רק
מתגלגל ומשחרר כמקום אחד לסבב, ולכן כל צעד שנגמר בפריסה ימשיך להמתין תור.
**ההכרעה שלך:** לשדרג ל-Vercel Pro, או להשאיר כך.

---

## 🔴 0ח. פורטל הגבאים של גליל — הדלת פתוחה, והמפתח לא עובד (06/08/2026)

**נמצא בסריקת יומני המערכות, ואומת חי — לא בקוד בלבד.** שלושה ממצאים נפרדים
ב-`more30.com/galil/gabai`, ורק הראשון תוקן:

| מה | מצב |
|---|---|
| **הסיסמה שהוקלדה נכתבה לקונסולה** של הדפדפן בכל ניסיון כניסה, יחד עם כל שורות חשבונות המנהלים | ✅ **תוקן ונפרס היום.** אומת: אותו ניסיון בדיוק כבר לא מדפיס דבר |
| **`?auto=admin` נכנס לניהול בלי סיסמה** — והנווט של האתר עצמו מקשר לשם ("ניהול ⚡") | 🔴 **פתוח — דורש הכרעה שלך** |
| **כניסת מנהל/גבאי לא יכולה להצליח** — הדפדפן מקבל 401 על `gabai_accounts` | 🔴 **פתוח** |

**מה נמדד בפועל** (`QA/platform/galil-gabai-auto-admin.png`): הכתובת
`more30.com/galil/gabai?auto=admin` נוחתת ישר בקונסולת הניהול — ניהול בתי כנסת,
ייבוא CSV, פרסום באנרים, הוספת מנהל — **בלי להקליד סיסמה**.

**מה שמצמצם את החומרה, ונבדק ולא הונח:** מסד הנתונים מסרב לכתיבה מאנונימי.
`POST /rest/v1/synagogues` חזר `42501 — new row violates row-level security
policy`, ו-`gabai_accounts` מחזיר 401 לאנונימי. כלומר **המסך נפתח, הנתונים לא
נפגעים.** זו חשיפה ובלבול — לא דליפת מידע.

**ולכן גם הצד השני:** הכניסה האמיתית שבורה מאותה סיבה. הקוד קורא את
`gabai_accounts` **מהדפדפן** ומשווה את הסיסמה שהוקלדה מול העמודה
`password_hash` ב-JavaScript. אנונימי חסום מהטבלה (וטוב שכך — אחרת כל מבקר היה
קורא את סיסמאות המנהלים), ולכן הגבאי מקבל "שגיאת חיבור למסד הנתונים. נסה שוב."
ולא ייכנס לעולם. **הפורטל הזה דקורטיבי בפרודקשן.**

**למה לא הכרעתי לבד:** להסיר את `?auto=admin` זו שורה אחת — אבל היא **הדרך
היחידה שבה מישהו נכנס לניהול היום**. הסרה בלי לתקן את הכניסה = נעילת הלקוח
מחוץ למערכת שלו.

**מה אני צריך ממך — בחר אחד:**
1. **לסגור עכשיו** — אסיר את הבייפס ואת קישור "ניהול ⚡" מהנווט. הניהול ייחסם עד שתיבנה כניסה אמיתית.
2. **לבנות כניסה אמיתית** — אימות בצד השרת (Edge Function / RPC ב-`SECURITY DEFINER`) עם סיסמאות מגובבות במקום השוואת טקסט גלוי בדפדפן. זו עבודה אמיתית, ואני צריך גישת ניהול לפרויקט `mwljkonwdeuaahsigjdp` (הוא **לא** הפרויקט הראשי `uhnrgujbdxhhmoxcjria` ואינו מופיע ב-MCP שלי).
3. **להשאיר כמו שזה** — אז זה ייכתב במפורש כהחלטה ב-`SYSTEMS_STATUS`, לא כפער ששכחנו.

*(הערה נלווית: `synagogues` בפרויקט הזה **ריקה** — 0 שורות. רשימת בתי הכנסת
שהאתר מציג מגיעה מקובץ בקוד, לא מהמסד. לא נגעתי בזה בסבב הזה.)*

---

## 🔴 0ז. חצור — שני מספרים שמזיזים את זמן כניסת השבת (06/08/2026)

**המנוע תקין. שני הנתונים שהוא מקבל אינם מאומתים.** `/chatzor` מפרסם זמני
היום, תאריך עברי ופרשה למועצה הדתית חצור הגלילית. בדקתי את המנוע עצמו
(`scripts/qa/chatzor-zmanim-verify.mjs`): החישוב אמיתי — `@hebcal/core`
אסטרונומי מתוך תאריך + קואורדינטות, לא נגרד ולא מקודד קשיח, ואחד־עשר הזמנים
יוצאים בסדר עולה ובשעות סבירות לגליל. הבעיה אינה בחישוב אלא בשני קלטים
שנכתבו כהנחה ומוצגים כעובדה:

| מה | הערך כיום | מי קבע | כמה זה מזיז |
|---|---|---|---|
| **גובה מעל פני הים** | `350` מ׳, מסומן `(approx.)` בקוד | הנחה | שקיעה **מאוחרת ב-3 דק׳** מגובה פני הים |
| **דקות הדלקת נרות** | `40` לפני השקיעה | הנחה, עם הערה "כמו ברוב הגליל" | 40 הוא המנהג **הירושלמי**; בצפת ובחיפה נהוג **30** |

**למה זה לא "באג שאתקן":** הגובה דולק (`Zmanim(..., true)`), ולכן הוא דוחה
את השקיעה — והדלקת הנרות נגזרת מהשקיעה, כך שההנחה מזיזה את **כניסת השבת
לכיוון הפחות מגן**. בין שתי הקריאות (350 מ׳ + 40 דק׳ מול פני הים + 30 דק׳)
יש פער של **13 דקות**. זו הכרעה הלכתית של המועצה, לא החלטה של מתכנת, ושינוי
שקט שלה גרוע מהשארתה.

**עדכון 06/08 — זה נוגע עכשיו לשני אתרים.** `/galil` הציג עד היום **טבלת
זמנים קפואה** שנכתבה בקוד וסומנה "(approximate)" — אחת עשרה שעות קבועות תחת
הכותרת "זמני היום". טבלה קבועה אינה יכולה להיות זמני היום: בינואר היא אמרה
שקיעה 19:05 כשהשקיעה בפועל 16:56 — **הפרש של 129 דקות**. היא הוחלפה במנוע
המחושב של chatzor, עם **אותן קואורדינטות בדיוק**, כדי ששני האתרים של אותה
עיירה לא יוכלו לסתור זה את זה. המשמעות: התשובות שלך למטה משנות עכשיו את
`/chatzor` **וגם** את `/galil`, ויש לשנות את שניהם יחד.

**מה אני צריך ממך — שתי שאלות למועצה הדתית:**
1. האם לחשב את השקיעה לפי גובה חצור או לפי פני הים? ואם לפי הגובה — מה הגובה
   המדויק שהמועצה עובדת לפיו (היום כתוב 350 מ׳ בקירוב, בלי מקור).
2. הדלקת נרות בחצור — 40 דקות או 30?

**למה לא בדקתי לבד:** גרסת `@hebcal/core` שמותקנת אינה נושאת דקות הדלקת נרות
על `Location` כלל, ואין בה ערך לחצור, לצפת או לראש פינה — רק טבריה, חיפה
וירושלים. כלומר **לא זמין** מהמכונה הזאת; זה חייב להגיע מהמועצה.

---

## 🆕 0−. מה נבנה בלילה 04/08 ומה נדרש ממך כדי להפעיל

| נבנה | כתובת | מה נשאר ממך |
|---|---|---|
| ~~**שליטת מחירים**~~ ✅ | `/admin/pricing` | **הוכרע 05/08/2026** — ראה §0₪ למטה |
| **דוח מערכות** | `/admin/systems` | — |
| **ניהול לקוחות** | `/admin/customers` | — |
| **עולם הסטארטאפים** | `/showcase` | **לסמן אילו מערכות להציג.** ברירת המחדל: מוסתר |
| **תור האוטומציה של בקלות** | סכימת `bkalot_auto` | ראה §0= למטה |

### ✅ 0₪. המחירים הוכרעו — מה נכנס בפועל (05/08/2026)

המספרים הגיעו ב-`RUN_INSTRUCTIONS.md`: **2 / 5 / 10 / 12 / 15 ₪ לחודש**.
זה שחרר פריט שהיה חסום, והנה מה שנעשה איתו בדיוק:

| מדרגה | על מי | מצב |
|---|---|---|
| 10 ₪ `premium` | `more30` (הפלטפורמה) | היה כבר במסד |
| 12 ₪ `premium` · 15 ₪ `vip` | `nadlan` | היה כבר במסד |
| **2 ₪ `בסיסי` · 5 ₪ `מורחב`** | **16 מערכות קלות חיות** | **נוסף עכשיו** |
| חינמי (ברירת מחדל) | כל מערכת שיש לה מחיר | **נוסף עכשיו** |

**מי נחשבה "קלה חיה":** `live` ו-`public_visible` — שנקבעו מסריקת דפדפן מול
הפרודקשן ולא מדגל ישן — פחות מי שכבר יש לו מחיר (`nadlan`, `more30`) ופחות
שלוש פלטפורמות תפעוליות: `tivuch`, `kiosk`, `studio`. השלוש האלה אינן חסרות
מחיר — הן נופלות למחיר הפלטפורמה (10 ₪) דרך נפילת ברירת המחדל שקיימת ממילא.

**שתי המדרגות נוצרו לכל מערכת ולא אחת מהן.** ההנחיה נתנה שני מספרים ולא
אמרה איזו מערכת מקבלת איזה; לבחור עבור 16 מערכות היה להמציא החלטה מסחרית.
אפשר לכבות מדרגה לכל מערכת ב-`/admin/pricing` בלי מיגרציה.

> **🔴 מה שעדיין דורש אותך:** מחיר מוצג ≠ חיוב. `core.billing_settings.mode`
> הוא `off`, והעמודה מקבלת `off`/`test` בלבד — `live` אינו ערך חוקי, ולכן
> גבייה אמיתית מחייבת מיגרציה מכוונת ולא מתג. `billing-safety.mjs` 6/6.

---

### 🔴 0=. שני אישורים שחוסמים שליחה וחיוב אמיתיים

**1. שליחה אמיתית ללקוחות.** התור בנוי, אבל `mode='live'` **זורק שגיאה
בכוונה**. אין דגל להפוך בקוד — הפעלה אמיתית היא מיגרציה מכוונת, וזה
נועד למנוע בדיוק את התאונה של שליחה המונית בטעות. במצב הנוכחי נשלח רק
ליעדים ב-`bkalot_auto.test_targets`, וכל נמען אחר נרשם כ-`blocked` עם
הסיבה. **תגיד "מאשר שליחה אמיתית" ואפתח.**

**2. חיוב אמיתי.** אין ספק סליקה מחובר, ואף מסלול אינו גובה
(`charged:false` תמיד). גם אחרי שתקבע מחירים — החיבור לסליקה חי דורש
אישור נפרד ממך.

> **עודכן 06/08 — הצנרת עצמה כבר מחוברת ואומתה.** עד היום עמוד המסלולים
> נעצר ברישום הבקשה ומעולם לא קרא ל-`more30_checkout`, כלומר המחירים ישבו
> בקטלוג בלי נתיב אל ההכרעה. עכשיו הוא קורא לה, ו**שני המצבים אומתו חי עם
> לקוחות בדיקה** (17/17 בכל אחד, `scripts/qa/checkout-flow.mjs`):
> `off` → "הסליקה סגורה"; `test` → "כוונת תשלום לבדיקה, אל תשתמש בכרטיס
> אמיתי". `charged=false` בשניהם.
>
> **הפרודקשן הושאר ב-`off` במכוון:** `provider` הוא `NULL`, ולכן `test`
> היה מבטיח למבקר סביבת בדיקה שאינה קיימת. **שתי החלטות שלך, ושתיהן
> קטנות:**
>
> | מה | איך זה מתבצע | למה שתרצה |
> |---|---|---|
> | **לפתוח `test` בפרודקשן** | `UPDATE` יחיד על `core.billing_settings.mode` — בלי פריסה, בלי קוד | להראות ללקוח את מסך הבדיקה כמו שהוא |
> | **לחבר ספק סליקה בסביבת טסט** (נדרים פלוס) | אישורי גישה של **סביבת הבדיקה** בלבד → `core.secrets` | זה מה שחסר כדי שיהיה **לאן** להמשיך מהמסך הזה |
>
> ⚠️ שתי אלה **אינן** חיוב אמיתי ואינן מתקרבות אליו: `live` אינו ערך חוקי
> בעמודה כלל, ופתיחתו דורשת מיגרציה מכוונת — לא מתג ולא מסך ניהול.

---

## 🔴🔴 0. **דחוף ביותר — לסובב את אישורי הסליקה של נדרים פלוס**

`apps/03-igud-ads/lib/nedarim.ts` החזיק את ה-**ApiValid וה-ApiPassword** של
המוסד כערכי ברירת מחדל אחרי `||`, והערה מעליהם חזרה עליהם בטקסט גלוי.
**קומיט ההצלה שלי (03/08) הוסיף בכוח את כל `apps/` לריפו — והריפו ציבורי.**
אומת מול ה-API של GitHub (`private: false`), לא הונח. כלומר הם פורסמו.

**מה כבר עשיתי:** הגדרתי `NEDARIM_MOSAD_ID`, `NEDARIM_API_VALID` ו-
`NEDARIM_API_PASSWORD` כמשתני סביבה ב-`modaot-more30` (הם **לא** היו מוגדרים
שם כלל — הסליקה החיה רצה כולה על אותם ליטרלים, ולכן מחיקה שלהם קודם הייתה
שוברת חיוב), ורק אז הסרתי אותם מהמקור. הקוד זורק עכשיו לפי שם אם משתנה חסר.

**מה שאני לא יכול לעשות:** להסיר אותם מהיסטוריית הגיט, ולסובב אותם מול
נדרים פלוס.

| מה צריך ממך | למה |
|---|---|
| **לסובב את ApiValid ו-ApiPassword** מול נדרים פלוס, ולעדכן את הערכים החדשים ב-Vercel (`modaot-more30`) וב-`core.secrets` | מחיקה מעץ העבודה אינה מוחקת מההיסטוריה. ריפו ציבורי יש להניח שנקרא |
| **להחליט על הריפו** — להפוך אותו לפרטי, או לשכתב היסטוריה (`git filter-repo`) | שכתוב היסטוריה הוא פעולה הרסנית ולא אעשה אותה בלי אישור מפורש |

**למניעת הישנות:** `node scripts/qa/secret-scan.mjs` מחפש **שמות** שנראים
כמו סוד שמקבלים ליטרל — כולל דרך `||`, שזו בדיוק הצורה שחמקה — ומפענח
JWT של Supabase כדי ש-19 מפתחות ה-anon שבריפו ידווחו כפומביים-בכוונה במקום
להטביע את הממצא. כדאי להריץ אותו לפני כל `git add -f` רחב.

---

## 🔴 0ג. מכסת הפריסות של Vercel נגמרה — **חוסם כל אימות נוסף**

> ## עודכן 06/08 18:50 — **שתי הפריסות של הסבב הזה עברו. לא נשאר כאן שום דבר שממתין לך.**
>
> המכסה נגמרה אחרי `_deploy/briut-more30` (`dpl_9jP76Dzz12fEa4BmwF7PTTNaCSgC`),
> התאפסה כעבור כשעתיים, ו-`_deploy/smachot-more30` נפרס ב-18:43 ואומת חי.
>
> | תיקייה | מה נכנס איתה | מצב |
> |---|---|---|
> | `_deploy/briut-more30` | תיקון הריפוד ל-`.header-inner` — כולל **רגרסיה שאני יצרתי הבוקר** ב-1280 | ✅ נפרס ואומת: 5/5 רוחבים נקיים |
> | `_deploy/smachot-more30` | ריפוד ל-`.topbar` — כפתור החלפת מצב התצוגה לא היה לחיץ **באף רוחב** | ✅ נפרס ואומת: 5/5 רוחבים, `reaches:true` |
>
> **מה זה אומר לך:** שום דבר, וזו הנקודה — הפער נסגר מעצמו ולא נדרש ממך דבר.
> קובץ המקור של smachot בגיט ומה שחי תואמים שוב. החסם הזה יחזור בכל סבב פריסות
> גדול; אם תרצה לסגור אותו לתמיד, שדרוג ל-Vercel Pro מסיר את התקרה.
>
> ## עודכן 03/08 אחה"צ — **11 פריסות עברו ואומתו חיות. 5 ממתינות.**
>
> המכסה התאפסה, נוצלו 11 פריסות שכולן אומתו במדידה חיה, ואז נגמרה שוב.
>
> **מה כבר חי ואומת:**
> `portal` · `torah` · `egod` · `chatzor` · `galil` · `imud` · `smel` ·
> `zchuyot` · `tamlul` · `nadlan` · `crm` · `gesher`
>
> | אימות | תוצאה |
> |---|---|
> | `console-probe /zchuyot` | **9 שגיאות → 0** |
> | `console-probe /mechiron` | **404 של הצ'אטבוט → 0** |
> | `authbutton-overlap` | **20 מתוך 26 נתיבים נקיים** (הבוקר: 16 נכשלו) |
> | `dark-probe` | ✅ imud · egod · galil · mechiron · nadlan · crm · gesher · tamlul · zchuyot |
>
> **מה ממתין לאיפוס הבא** — הכול בנוי ומוכן; חסר רק
> `vercel deploy --prod --yes --scope l023131500-ops-projects` מכל תיקייה:
>
> | תיקייה | מה נכנס איתה |
> |---|---|
> | 🔴 **`_deploy/torah-more30`** | **תיקון רגרסיה שאני גרמתי לה והיא חיה עכשיו.** ראה למטה — הכי דחוף בתור |
> | `_deploy/mthbram-more30` | פינוי לכפתור הכניסה (הנמדד היחיד שנשאר שם) |
> | `_deploy/bkalot-more30` | הדלקת מצב כהה · תווית ל-`<select>` · פינוי בנווט |
> | `_deploy/briut-more30` | מצב כהה מלא + תיקון ניגודיות של כפתור הפעולה |
> | `_deploy/mechiron-more30` | **רינדור מראש** (2,976 תווים) + כפתור כניסה שהיה חסר |
> | `apps/17-chizukim-transcribe` | פינוי מעל "העלאת הקלטה" |
>
> **`apps/03-igud-ads` (modaot) — בנוי ומוכן, ובכוונה לא נפרס.** ראה §4:
> זו מערכת עם סליקה חיה, ויש שם בקשת אישור פתוחה ממך מזה כמה סבבים.
> מצב כהה מלא נכתב ו-`next build` עובר. **תגיד "כן" ואפרוס בדקה.**
>
> ## עודכן שוב 03/08 (סבב הפריסות) — **17 פריסות עברו ואומתו**
>
> | אימות חי | תוצאה |
> |---|---|
> | `/torah` CLS | **0.578 → 0** · perf 47 → 62. הרגרסיה נסגרה |
> | `authbutton-applied` | **10/11 מפנים מקום בפועל** (בדיקה חדשה — ראה למטה) |
> | `/mthbram` | **נקי בשני הרוחבים**, 82px מרווח בפועל |
>
> **בדיקה חדשה שהייתה חסרה:** `authbutton-overlap` עונה "האם הכדור מכסה
> פקד עכשיו", אבל אינו מבחין בין נווט שפינה מקום לבין נווט שהפקד האחרון
> שלו פשוט יושב במקום אחר. `scripts/qa/authbutton-applied.mjs` קורא את
> הערך המחושב. הוא גילה ש-**שניים מתוך אחד-עשר מעולם לא פינו כלום**:
> ב-`mthbram` ה-CSS של המערכת מכיל `.container{padding-left:1rem!important}`,
> ו-`!important` בגיליון מחבר גובר על סגנון inline רגיל — ולכן ה-`style`
> של React נבלע בשקט.
>
> ## ✅ עודכן שוב — **כל התור נפרס. 24 פריסות היום, כולן אומתו.**
>
> `portal` (עם לוח המשתמשים) · `bkalot` · `briut` · `mechiron` · `modaot` ·
> `chizukim` · `torah` · `mthbram` — כולן חיות.
>
> | אימות | תוצאה |
> |---|---|
> | `/admin/activity` | **200, הלוח חי** ומגודר לסופר-אדמין |
> | `modaot` — סליקה | **5/5, זהה לפני ואחרי.** ראה למטה |
> | `chizukim` | **נקי בשני הרוחבים** |
>
> **על הפריסה של `modaot` (סליקה חיה):** `scripts/qa/modaot-payment-smoke.mjs`
> רץ **לפני ואחרי** ומחזיר את אותן חמש התוצאות. הפריסה שינתה ערכת צבע
> והוסיפה את כפתור הכניסה — ותו לא.
> *(ממצא נלווה: `/modaot/api/payments/create` מחזיר 404 בפרודקשן **גם**
> כשהמסלול קיים בבנייה. זה מבני ולא בנייה מיושנת. סומן ולא הופעל בשקט.)*
>
> ### 🟡 שלוש חפיפות שנשארו — כולן נגיעות של פיקסלים בודדים
>
> | נתיב | מה נמדד | מצב |
> |---|---|---|
> | `/modaot` | 54×4 → **3×4** אחרי הפינוי | הפינוי חל; הנווט פשוט רחב ממה שנשאר |
> | `/mechiron` | 14×36 · 45×36 — **לא זז** | `/mechiron` מרנדר משהו אחר מ-`public-landing.tsx`. טרם אותר |
> | `/bkalot` | 39×1 | ראה למטה — **לא נפתר, והמדידה סותרת את הקוד** |
>
> **`/bkalot` שווה קריאה לפני שמישהו ינסה שוב:** ה-`style.css` שנפרס
> **כן מכיל** את `--more30-auth-inset`, ובכל זאת הריפוד המחושב הוא 0,
> ה-`min-height` 0, גובה הסרגל 94px במקום 64, ו-`.nav` מחושב
> `display:block` בעוד המקור אומר `flex`. זה לא חוק שלי שמפסיד בקסקדה —
> זה **גיליון אחר שמנצח**, ולא זיהיתי אותו. רשום עם המספרים ולא כניחוש.
>
> ### 🔴 רגרסיה שאני גרמתי לה, והיא חיה כרגע ב-`/torah`
>
> **CLS עלה מ-0.001 ל-0.578** (הסף 0.1). `cls-why.mjs` מצביע על אלמנט
> אחד: "מערכות נוספות מבית איגוד השיעורים" — הסעיף שמעליו הוא רשת
> יכולות שהכרטיסים בה מותנים בשורת הטננט.
>
> **הסיבה:** torah נאפית **עם seed** — שורת הטננט נשלפת בזמן הבנייה,
> נכתבת ל-HTML כ-`window.__TENANT__` ונקראת סינכרונית ברינדור הראשון.
> בבנייה מחדש הבוקר הרצתי את `prerender-spa.mjs` **בלי `--seed-url`**.
> שום דבר לא נכשל, העמוד נראה תקין — אבל ה-HTML האפוי צייר את הרשת
> המלאה, הרינדור הראשון בלקוח היה בלי טננט וצייר פחות כרטיסים, וכל מה
> שמתחת קפץ.
>
> **תוקן ונאפה מחדש עם ה-seed.** ולמניעת חזרה: הדגלים עברו ל-
> `scripts/prerender-all.mjs`, שמסרב לאפות את torah בכלל אם לא הצליח
> לבנות את ה-seed. **חסרה רק הפריסה.**
>
> **אחרי הפריסה, ארבע בדיקות אימות:**
> `node scripts/qa/authbutton-overlap.mjs` (יעד: אפס OVERLAP) ·
> `node scripts/qa/dark-probe.mjs` (יעד: `CHANGED` בכל אחת) ·
> `node scripts/qa/probe-all.mjs` ·
> `node scripts/qa/lh-batch.mjs /torah` ואז `node scripts/qa/cls-why.mjs torah`
> (יעד: CLS חוזר מתחת ל-0.1).
>
> **אחרי הפריסה, שלוש בדיקות אימות:**
> `node scripts/qa/authbutton-overlap.mjs` (יעד: אפס OVERLAP) ·
> `node scripts/qa/dark-probe.mjs` (יעד: `CHANGED` בכל אחת) ·
> `node scripts/qa/console-probe.mjs https://more30.com/zchuyot` (יעד: נקי).

```
Resource is limited - try again in 24 hours
(more than 100, code: "api-deployments-free-per-day")
```

התוכנית החינמית מוגבלת ל-**100 פריסות ליום**, והמכסה נוצלה במלואה בסבב הזה
(תיקוני crm, gesher, mthbram, galil, egod, torah, nadlan, tivuch, kesef —
כל אחד דרש בנייה→פריסה→מדידה, לעיתים כמה סבבים עד שהמדידה אישרה).

**מה זה חוסם:** אי אפשר לפרוס ולכן אי אפשר **לאמת** שום תיקון נוסף. אני לא
מסמן תיקון כ"עובד" בלי למדוד אותו חי.

**שני תיקונים כתובים בקוד וממתינים לפריסה** (לא נפרסו, לא אומתו):

| קובץ | תיקון |
|---|---|
| `sites/36-nadlan-pro/tivuch/index.html` | `.footer a` → אזור מגע 24px+ |
| `sites/34-kesef/kesef/index.html` | זהה |

**מה צריך ממך:** או להמתין לאיפוס המכסה, או לשדרג את התוכנית ב-Vercel.
אחרי האיפוס — `vercel deploy --prod` בשתי התיקיות, ואז
`node scripts/qa/small-targets.mjs` לאימות.

---

## ❓ 0ב. הודעה שלא הצלחתי לפענח — `3/.9.5`

התקבלה **שמונה פעמים** במהלך הרצת 03/08, פעמיים כהודעה עצמאית.
שאלתי פעמיים במילים מה הכוונה וקיבלתי בחזרה את אותו טקסט בדיוק.

לא ניחשתי ולא פעלתי לפיה — המשכתי לפי סדר העדיפות הקיים. הקריאות שעלו בדעתי
ונפסלו כי אין להן עוגן: מספר גרסה (3.9.5), תאריך (3/9), או הקשה בשוגג.

**אם זו פקודה — כתוב אותה במילים ואבצע.** אם זו הקשה בשוגג, אפשר למחוק את
הסעיף הזה.

---

## ✅ 0א. **הצלת הקבצים — נסגרה 03/08 ערב. נותרה הכרעה ארכיטקטונית בלבד.**

> **הסעיף הזה היה "הכי דחוף" וכבר אינו סיכון.**
>
> הרשימה שהייתה כאן מנתה 21 קבצים. **הבדיקה בפועל מצאה שהבעיה הייתה גדולה
> בהרבה:** `/apps/**` מוחרג ב-`.gitignore`, ולכן **2,706 קבצי מקור** —
> כלומר כל קוד המערכות, לא רק מה שנזכרנו לתקן — חיו על הדיסק הזה בלבד.
> פורמט אחד או מחיקה אחת היו מוחקים אותם.
>
> **כולם נשמרו ונדחפו** (`git add -f`). מה שהוחרג בכוונה: `.env*` (מפתחות
> חיים), lockfiles, `node_modules`, `dist`, `.next`, `.vercel`, `.output`
> ובינאריים מעל 3MB. **כל קובץ שנוסף נסרק לאישורי גישה לפני השמירה** —
> ששת ה-JWT של Supabase שנמצאו פוענחו ואומתו כ-`role=anon` (הם ממילא
> נשלחים לדפדפן בכל טעינה), ולא נשמר שום `service_role`.
>
> גם `_deploy/**` היה מוחרג, והמקור חסר-השרת שנכתב ביד תחת `_deploy/*/api/`
> נשמר גם הוא.
>
> **מה שנשאר אצלך זו עדיין ההכרעה למטה** — היכן הקוד חי לטווח ארוך. אבל
> היא כבר לא מרוץ נגד אובדן.

---

## 🟡 0א. **תיקוני מקור — נשמרו בגיט 03/08. נותרה הכרעה ארכיטקטונית**

> מסבב 03/08. **דורש החלטה שלך, ומהר.**

`.gitignore` של המונו-רפו הזה מתעלם מ-`/apps/**` (למעט `app.json` ו-`STATUS.md`)
ומ-`/_deploy/**`. זה **בכוונה** — לכל מערכת יש ריפו משלה. אבל התוצאה:

**התיקונים הבאים חיים בפרודקשן, קיימים על הדיסק שלך, ואינם בשום ריפו:**

| קובץ | מה תוקן |
|---|---|
| `apps/01-torah-platform/src/hooks/useTenant.tsx` | קריאת ה-seed מה-HTML |
| `apps/01-torah-platform/src/lib/tenant.ts` | ייצוא `normalizeTenant` |
| `apps/01-torah-platform/src/components/layout/PublicLayout.tsx` | הסרת שער הספינר |
| `apps/01-torah-platform/src/main.tsx` | תיעוד מדוד (התנהגות זהה) |
| `apps/32-nadlan-berega/tailwind.config.ts` | `teal` → 4.95:1 (היה 3.03:1) |
| `apps/32-nadlan-berega/components/ReportRequestForm.tsx` | h3 → h2 |
| `apps/30-zchuyotpro-crm/vercel.json` | **מתקן עמוד ריק לגמרי** — rewrite לנכסים |
| `apps/31-hebrew-bridge-crm/vercel.json` | **מתקן עמוד ריק לגמרי** — rewrite לנכסים |
| `apps/21-mthbram/src/App.tsx` | `basename="/mthbram"` — המערכת הייתה 404 |
| `apps/24-galilee-connect-hub/src/App.tsx` | `basename="/galil"` + הוסר וידאו מת |
| `apps/15-egod/src/components/home/TipsSection.tsx` | אזור מגע 24px + `aria-label` ל-8 נקודות |
| `apps/15-egod/src/components/Footer.tsx` | `py-1` ל-11 קישורי פוטר |
| `apps/03-igud-ads/app/(public)/page.tsx` | `py-1` ל-3 קישורי נווט ⏳ **טרם נפרס** |
| `apps/02-igud-transcribe/app/page.tsx` | `py-1` לקישור המייל ⏳ **טרם נפרס** |
| `apps/21-mthbram/src/pages/NotFound.tsx` | `py-1` לקישור חזרה ⏳ **טרם נפרס** |

**עודכן 03/08: כל 21 הקבצים נשמרו ב-`git add -f` ונדחפו.** סכנת האובדן המיידית הוסרה. אבל הם עדיין מוחרגים ב-`.gitignore`, ולכן שינוי עתידי בהם לא ייתפס אוטומטית, ובנייה מהריפו הנפרד של כל מערכת עדיין תתעלם מהם.

**מה צריך ממך:** להחליט אחד משניים —
1. לדחוף את השינויים לריפו של כל מערכת (`l023131500-ops/nadlan-berega` וכו'), או
2. להסיר את `/apps/**` מה-`.gitignore` ולנהל את הקוד כאן.

עד אז — **אל תבנה מחדש את torah או nadlan מריפו נפרד.**

---

## 🆕 0. מערכת המתווכים (36) — **שני מפתחות, וזה כל מה שחוסם**

> מסבב הלילה 02/08→03/08. המערכת חיה ב-`more30.com/tivuch`.
> הראיות: `QA/nadlan-pro.md` · 71/71 ב-HTTP, 43/43 בדפדפן.

**שלבים 1, 2, 4 ו-6 עובדים מקצה לקצה כבר עכשיו.** שני הפריטים למטה חוסמים את
שלב 3 ואת שליחת המסמכים לחתימה — **ורק אותם**.

### ✅ ~~2. `RESEND_API_KEY`~~ — **נסגר 03/08. שליחת מסמכים לחתימה עובדת.**

המפתח נמצא ב-`core.secrets` (scope `global`, 36 תווים). נבנתה
**Edge Function `np-send-signature`** ששולפת אותו בצד השרת ושולחת את קישור
החתימה במייל. המפתח לעולם לא מגיע לדפדפן.

ההרשאה לא ממומשת מחדש: הפונקציה קוראת את המסמך **בשם הקורא**, ו-RLS מכריעה.
נמדד בפרודקשן: בעל המשרד 200 · מתווך ממשרד אחר **403** · anon **403**.
השליחה נרשמת ביומן הראיות כאירוע `emailed`.

### 🔴 1. מפתח API של Green Invoice — **הפריט האחרון שחוסם חשבוניות**

**מה כבר יש:** `INVOICE_PROVIDER = greeninvoice` ב-`core.secrets`. זו **בחירת
הספק בלבד** — 12 תווים. התיאור של הרשומה עצמה אומר:
*"API key to be added by user when connecting a real account."*

**מה חסר:** אישורי גישה אמיתיים לחשבון Green Invoice:

| שם ב-`core.secrets` | מה זה |
|---|---|
| `GREENINVOICE_API_KEY` | ה-ID מהחשבון |
| `GREENINVOICE_API_SECRET` | ה-secret המשויך |

**מאיפה:** app.greeninvoice.co.il → הגדרות → API. שניהם נדרשים — ה-API של
Green Invoice מחליף אותם ב-JWT דרך `/account/token`.

בנוסף, למשרד עצמו: `nadlan_pro.offices.invoice_provider = 'greeninvoice'`.

**למה זה עדיין חוסם:** מיוני 2026 הסף למספר הקצאה הוא **5,000 ₪ לפני מע״מ**,
ועמלת תיווך על דירה כמעט תמיד מעליו. **החסימה מכוונת ואכופה במסד:** אי אפשר
לכתוב שורה שנראית כחשבונית מס בלי מספר הקצאה — `constraint` בטבלה, לא בדיקה
ב-UI. עד שיהיו אישורים, פאנל החשבונית מציג "נדרש חיבור ספק חשבוניות", מסביר
למה, ומציע דרישת תשלום תוך שהוא אומר שהיא **אינה** חשבונית מס.

**מה כבר מוכן ונבדק:** סף לפי תאריך (5,000 / 10,000 / 20,000 / 25,000 לפי
מועד ההפקה), רישום מספר הקצאה ו-PDF, קישור אוטומטי לעמלה בעסקה, וארבעה
מבחנים שמנסים לשבור את הקו האדום — `QA/nadlan-pro.md` §4.

### 🟡 3. שלוש החלטות קטנות (לא חוסמות)

- **תמחור — הכלי מוכן, המספרים אצלך.** ✅ נבנה `more30.com/admin/pricing`:
  לכל מערכת ולכל סוג תשלום (מנוי / סליקה / חד-פעמי) שדה סכום וכפתור שמור,
  שכותב מיד ל-`core.plans`. **ברירת המחדל נקבעה ל-0 ₪** לפי מסמך הבנייה
  (כלומר "פעיל, ללא חיוב"), ולכל מערכת נוצר מסלול **"פרו (בדיקה)" ב-1 ₪**
  כדי לאפשר בדיקת סליקה בסכום זניח.
  **מה שנשאר ממך: המספרים האמיתיים.** היכנס למסך, הקלד, שמור.
- **משתמשי QA.** נוצרו `qa.np.agent@more30.com`, `qa.np.outsider@more30.com`
  ו-`qa.kesef.test@more30.com`. הסיסמאות מופיעות בקוד הבדיקות ובתמלול —
  להתייחס כחשופות. למחוק או להחליף כשלא צריך אותם.
- **`nadlan_pro` ב-Exposed schemas.** לא נדרש: הכל עובר דרך פונקציות ב-`public`,
  בדיוק כמו kesef. אם בכל זאת תחשוף — אין צורך לשנות קוד.

---

## 🆕 0א′. שילוב kesef + kiosk — **ארבע מטלות, וזה הכל**

הרקע: `INTEGRATION.md` · הראיות: `QA/kesef.md`, `QA/kiosk.md`

**שתי המערכות חיות ומוצגות בדף הבית עם כפתור כניסה:**
`more30.com/kesef` · `more30.com/kiosk/` — נגישות **100** בשתיהן,
ניהול רשימת הכתובות של הקיוסק נבדק מקצה-לקצה ב-Playwright (15/15).

ארבעת הפריטים למטה הם **כל** מה שדורש אותך. אין חמישי.

---

### 🔴 1. רשומת DNS לקיוסק — מפעילה שליטה מרחוק בזמן אמת

**Cloudflare → DNS של `more30.com` → Add record:**

| שדה | ערך |
|---|---|
| **Type** | `CNAME` |
| **Name** | `kiosk` |
| **Target** | `dua8dycv.up.railway.app` |
| **Proxy status** | **DNS only** (ענן אפור) — חובה |
| **TTL** | Auto |

**אל תיגע ברשומת ה-root/`@`.**

*למה:* נמדד — WebSocket דרך `more30.com/kiosk` מחזיר **404 על ה-upgrade**.
rewrite של Vercel מעביר HTTP ולא upgrade.
*מה כבר מוכן:* הדומיין רשום ב-Railway, `WS_HOST` מוגדר, ו-
`more30.com/kiosk/api/config` כבר מחזיר `{"wsHost":"kiosk.more30.com"}`.
**ברגע שהרשומה תיפתר זה יעבור לזמן אמת לבד — בלי פריסה ובלי קוד.**
בינתיים הקונסולה עובדת ב-polling כל 15 שניות.

---

### ✅ 2. ~~חיבור ה-Volume בקיוסק~~ — **נסגר ואומת 04/08**

ה-Volume מחובר. אומת בשתי דרכים בלתי-תלויות:
`get-service-config` מחזיר `volumeMounts → /app/data`, **ולוג העלייה של
הפריסה האחרונה אומר בדיוק את המשפט שחיפשנו:**

```
db: /app/data/kioskfleet.db — existing file (data persisted)
```

כלומר הנתונים שרדו דיפלוי בפועל, לא רק שההגדרה נראית נכונה.
**מכאן אפשר להחליף את סיסמת האדמין (§4) בלי שהיא תתאפס.**

<details><summary>הניסוח הקודם</summary>

### 🔴 2. חיבור ה-Volume בקיוסק — בלעדיו כל דיפלוי מוחק את הנתונים

**Railway → project `kioskfleet` → service `kioskfleet` → env `production`
→ Settings → Volumes → חבר את `kioskfleet-data` ל-`/app/data` → Deploy.**

*מצב:* ה-Volume בגודל 1GB **נוצר**, אבל החיבור לא נכנס לתוקף באף מסלול API
(staged, commit, update-service — כולם נוסו; ה-CLI לא מחובר).
*נמדד:* יצרתי רשומה, עשיתי redeploy, היא נעלמה — שלוש פעמים.
*איך תדע שזה עבד:* בלוג העלייה יופיע
`db: /app/data/kioskfleet.db — existing file (data persisted)`
במקום `NEW FILE`.
*חלופה:* הרץ `railway login` בטרמינל ואחבר בעצמי.

</details>

---

### 🟡 3. `kesef` ב-Exposed schemas — הערך נשמר, ממתין לרענון

*מה נעשה:* הוספתי את `kesef` דרך ה-Management API. הערך השמור:
`public,graphql_public,nadlan,chatzor,kesef`.

> ⚠️ **כמעט-תקלה שנתפסה:** ה-GET של ה-API החזיר `public,graphql_public` בלבד,
> בעוד ה-PostgREST הרץ הגיש גם `nadlan` ו-`chatzor`. כתיבה נאיבית הייתה
> **מוחקת את שתיהן** ושוברת את מערכות 32 ו-16. תוקן לאיחוד המלא ואומת.

*מה נשאר:* ה-PostgREST הרץ טרם טען מחדש, ורק רסטארט פרויקט יחיל את זה.
**החלטת לא להפעיל מחדש** — נכון, כי אין עדיין אפליקציית kesef שצורכת את
הסכימה. **הכי פשוט:** Supabase → Settings → API → Exposed schemas → לשמור
מחדש (הערך כבר נכון). או פשוט להשאיר — זה ייכנס ברסטארט הבא.
*איך תדע:* `Accept-Profile: kesef` יחזיר 200 במקום 406.

---

### 🟡 4. סיסמת האדמין של הקיוסק — להחליף אחרי הכניסה הראשונה

הסיסמה הראשונית נוצרה על ידי ומופיעה **גם בלוג של Railway וגם בשיחה הזו**,
ולכן יש להתייחס אליה כחשופה.

**להחלפה:** `more30.com/kiosk/console` → כניסה עם `admin` → **הגדרות** →
"שינוי סיסמה". לוקח חצי דקה.

⚠️ שים לב לסדר: כל עוד ה-Volume (§2) אינו מחובר, **הסיסמה תתאפס בכל דיפלוי**
לערך שב-`SEED_ADMIN_PASSWORD`. **חבר קודם את ה-Volume, ורק אז החלף סיסמה** —
אחרת השינוי יימחק.

---

### לידיעה בלבד — לא חוסם, ולא אחת מארבע המטלות

- **קוד האפליקציה של kesef לא אותר** בשום ריפו נגיש (נבדקו 5 ענפי מונו-רפו,
  3 ענפי `zol`, 9 שמות ריפו, וכל הדיסק). אתר התדמית חי ואומר במפורש
  שהמערכת בהקמה, ולכן זה לא חוסם את השילוב — אבל בלי הקוד לא יהיו מסכים.
- **הקיוסק נועל אנדרואיד בלבד.** האתר אומר "אנדרואיד" ולא "מחשב/נייד", כי
  זה הסוכן שקיים (`kiosk/android`). נעילת Windows דורשת סוכן Windows; iOS
  דורש MDM מפוקח. **זו בנייה, לא ניסוח** — תגיד אם להתחיל בסוכן Windows.


## 🆕 0***. הכרעה ארכיטקטונית אחת ששווה חמש מערכות (מסבב 02/08 בוקר)

**השאלה:** האם לעבור לרינדור מראש (prerender) של עמודי הנחיתה?

**למה זה עלה עכשיו.** מערכת 01 עברה בסבב הזה כל סעיף ב-`DESIGN_STANDARD` —
נגישות 100, SEO 100, מצב כהה, אפס כשלי ניגודיות, CLS 0.001 — **פרט
לביצועים.** חבילת הכניסה ירדה מ-2,233KB ל-610KB (73%), והציון עלה מ-34
ל-62. **ואז נעצר.**

מדידת רשת ללא האטה מסבירה בדיוק למה:

```
 497ms   התשובה הראשונה מהשרת
 914ms   חבילת הכניסה סיימה לרדת
1048ms   הציור הראשון בכלל  ← עד כאן העמוד לבן וריק
2176ms   הכותרת מצוירת סופית
```

**התשובה הראשונה מהשרת היא `<div id="root">` ריק.** שום אופטימיזציה של גודל
לא תשנה את זה: אי אפשר לצייר טקסט שאינו נמצא ב-HTML. הסף של Lighthouse
ל-FCP הוא 1.8 שניות בפרופיל מובייל מואט, ורק ההורדה והפרסור שווים יותר מזה.

**זה לא באג של 01.** אותו חסם בדיוק מחזיק למטה:

| מערכת | נתיב | perf |
|---|---|---|
| 27 השוואת מחירים | `/mechiron` | **44** |
| 22 מימוש זכויות | `/zchuyot` | **46** (היה 36) |
| 15 איגוד | `/egod` | **50** |
| 16 חצור — המערכת | `/chatzor/` | **55** |
| 01 איגוד השיעורים | `/torah` | **62** |
| 28 קופות חולים | `/kupot` | **72** (היה 40) |

**עדכון 02/08 צהריים — מערכת 22 מחזקת את המסקנה.** שם ירדה חבילת הכניסה
מ-1,802KB ל-117KB (94%), הבייטים בקו מ-771KB ל-295KB, וזמן ריצת ה-JavaScript
מ-5.9 שניות ל-1.3. הציון עלה מ-36 ל-46 **ונעצר** — כי FCP נשאר 3.8 שניות מול
סף של 1.8, והסיבה זהה: אין מה לצייר עד שה-JS רץ.

**עדכון 02/08 אחה"צ — מערכת 28 מראה גם את הצד השני, וזה חשוב להכרעה.**
שם התברר ש**הביצועים כלל לא נחסמו על הרינדור מראש**: ה-FCP היה 2.7 שניות,
והציון 40 נבע מכך שהעמוד צבע 435 נושאים בבת אחת והיה קפוא כשש שניות אחרי
שכבר נראה מוכן. אחרי תיקון שלנו — **40 → 72**.

**המסקנה המשולבת:** רינדור מראש הוא התקרה, לא הרצפה. יש מערכות שבהן שווה
לחפש קודם את מה ששלנו (28 עלתה 32 נקודות בלעדיו), ויש מערכות שבהן כבר
נעשה הכול והן נעצרות על החסם הזה (01, 22). **ההכרעה עדיין נדרשת** — בלעדיה
אף אחת מהשש לא תעבור 90 — אבל היא לא תמיד הצעד הראשון.

**שש מערכות. סיבה אחת.** אם אלך מערכת-מערכת, אחזור על אותה עבודה שש פעמים
ואף אחת מהן לא תעבור את הסף. אם נסגור את זה בתבנית הבנייה — כולן עולות
יחד.

**מה שאני צריך ממך: הכרעה בין שלוש.**

| | מה זה אומר | עלות | מה מקבלים |
|---|---|---|---|
| **א. רינדור מראש בבנייה** (מומלץ) | `vite-plugin-prerender` או שווה ערך מייצר HTML סטטי לעמוד הנחיתה בלבד, בזמן `vite build`. שאר המסלולים ממשיכים בדיוק כפי שהם | נמוכה־בינונית. שינוי בתבנית הבנייה, לא בקוד האפליקציה | FCP/LCP נופלים לטווח הסף. אין שרת חדש, אין תלות חדשה בזמן ריצה |
| **ב. מעבר ל-SSR** | Next.js או Remix לכל מערכת | **גבוהה מאוד** — כתיבה מחדש של הניתוב והאימות ב-6 מערכות | הטוב ביותר תיאורטית, ולא שווה את זה כאן |
| **ג. לא לעשות כלום** | להצהיר שהסף ≥90 אינו חל על SPA שמוגש כך, ולתעד את זה ב-`DESIGN_STANDARD` §7 כחריג מוצהר | אפס | הסעיף נסגר בכנות במקום להישאר פתוח לנצח בשש מערכות |

**המלצתי: א.** ג' היא תשובה לגיטימית ואני אכתוב אותה כמו שצריך אם תבחר
בה — אבל א' משיגה את התוצאה האמיתית בעלות של יום עבודה, פעם אחת, לכל
הפלטפורמה.

**עד שתכריע** אני ממשיך למערכות הבאות וסוגר בהן את כל תשעת הסעיפים
האחרים; הביצועים יישארו רשומים במפורש כ"חסום על ההכרעה הזו" ולא כפער
שנשכח.

---

## ✅ 0*****. הרכיב המשותף — **אושר על ידך ובוצע 03/08**

`auth-button.js` מפרסם עכשיו `--more30-auth-inset` עם הרוחב שהוא **באמת**
תופס, נמדד מחדש כשהפונט נוחת, כשהסשן נפתר וכשחלון משתנה. כל נווט מפנה
מקום מהמשתנה במקום לנחש 112px אחד-עשר פעמים.

הפינוי מחושב ולא קבוע: הוא מקזז את השוליים שה-container כבר נותן, ולכן
בדסקטופ הסרגל נשאר איפה שהיה ורק רוחב צר משלם על הפינוי.

**נמדד: 6 נתיבים כבר נקיים בפרודקשן · 4 ממתינים לפריסה · 1 חסום על מקור**
(`/smachot` — `apps/14` הוא אתר התוכנית העסקית ולא מה שנפרס). הפירוט
המלא ב-`SYSTEMS_STATUS.md`.

<details><summary>הניסוח הקודם — הבקשה לאישור</summary>

### 11 נתיבים שבהם כפתור הכניסה מסתיר פקד

**זה לא סגנון, זה פקדים שאי אפשר ללחוץ עליהם.** התגלה בסבב 22 ונמדד על כל
24 הנתיבים (`scripts/qa/authbutton-overlap.mjs`): כדור הכניסה מוזרק
`position: fixed` בפינה השמאלית העליונה, ובעברית זה בדיוק המקום שבו נווט
מניח את הפקד האחרון שלו. בכל 11 המקרים `elementFromPoint` מאשר שהלחיצה
מגיעה **לכדור** ולא לפקד.

| נתיב | מה מכוסה |
|---|---|
| **`/torah` (01)** | **כפתור "התחבר" של המערכת — בשני הרוחבים** |
| **`/chatzor/` (16)** | **כפתור התפריט + מעבר למצב כהה (מובייל)** |
| `/smachot` (14) · `/egod` (15) · `/galil` (24) | פקד בשני הרוחבים |
| `/` (33) · `/tamlul` · `/imud` · `/smel` · `/chizukim/` · `/nadlan` | פקד במובייל |

ב-22 תיקנתי מקומית (ריפוד של 112px בנווט). **לחזור על זה ב-11 מערכות
פירושו 11 ניחושים של אותו מספר**, ולכן התיקון הנכון הוא ברכיב עצמו:
`auth-button.js` יכתוב `--more30-auth-inset` עם הרוחב שהוא באמת תופס, וכל
נווט יפנה לו מקום לפי המשתנה.

**מה שאני צריך ממך: אישור לגעת ב-`portal/public/auth-button.js`.** זהו
הקובץ היחיד שנטען בכל 24 המערכות החיות בבת אחת — טעות בו נראית בכולן
בו-זמנית, ולכן לא נגעתי בו מיוזמתי. השינוי עצמו קטן (שורה אחת שכותבת משתנה
CSS), ואחריו כל מערכת מקבלת ריפוד לפי המשתנה במקום מספר קשיח.

</details>

---

## 🆕 0****. הכרעה מוצרית קטנה מסבב 22 — שני בוטים באותו עמוד

`more30.com/zchuyot` מציג **שתי הזמנות שונות לשיחה בו-זמנית**, אחת בכל פינה
תחתונה:

| | מה זה | מצב |
|---|---|---|
| **"בודק הזכויות"** (פינה שמאלית) | שאלון מונחה בשישה שלבים — פרטים אישיים, בן/בת זוג, ילדים, תעסוקה, בריאות, דיור — ובסופו ציון זכאות ופנייה | עובד |
| **"הנציג של בקלות"** (פינה ימנית) | סוכן AI חופשי שעונה מהמאגר. נמדד: 2,300–2,700 תווי תשובה אמיתית | עובד |

שניהם תקינים ואף אחד מהם אינו שבור — ולכן **לא נגעתי**. אבל המבקר רואה שני
כפתורים צפים ואינו יודע במה לבחור, ובמובייל הם תופסים יחד את כל תחתית המסך.

**שלוש אפשרויות:** למזג לכפתור אחד שמציע שני מסלולים ("לשאול בחופשיות" /
"לעבור שאלון מסודר") · להשאיר רק את הסוכן ולהעביר את השאלון לתוך העמוד ·
להשאיר כמו שזה. **תגיד ואבצע** — זו החלטה של חצי שעה.

---

## 🆕 0**. מסבב 02/08 (נדל"ן V2 — 11/11 הושלם)

**המערכת עצמה לא חסומה.** כל 11 סעיפי `NADLAN_SPEC_V2` מיושמים ואומתו בפרודקשן
על ארבע כתובות (`QA/nadlan-v2.md`). שלושת הפריטים כאן הם **תקרות שאין לי דרך
לפרוץ בעצמי** — לא באגים.

### 🟠 1. ארבעה נתונים שדורשים ספק/מקור מורשה — הדוח אומר במפורש שאין אותם
| הנתון | למה אין | מה יפתור |
|---|---|---|
| **נסח טאבו** — בעלות, משכנתאות, עיקולים | אין ממשק ציבורי | חוזה מול ספק נסחים (יש כאלה עם API) |
| **היתרי בנייה וחריגות** | "רישוי זמין" סגור לציבור; תיק הבניין יושב בוועדה המקומית | אין פתרון ארצי. אפשר להתחיל מוועדות שכן חושפות |
| **זכויות בנייה כמותיות** (אחוזים, קומות, יח"ד) | מופיעות רק בהוראות התוכנית במבא"ת, לא בשירות המפות | אינטגרציית מבא"ת דרך `pl_number` |
| **שנת בנייה** כשאין עסקאות בבניין | אין מאגר ארצי של היתרים | GIS עירוני, עיר-עיר |

בכל אחד מהם הדוח כותב **למה** אין את הנתון ואיפה בודקים אותו ידנית. **זו החלטה
מכוונת ולא פער** — אבל אם תרצה לסגור אחד מהם, זה דורש התקשרות שלך.

### 🟡 2. מכסת Apify (לוחות המודעות) — כמה אתה מוכן להוציא
"דירות מוצעות כרגע" נמשכות בתשלום. בסבב הזה הוספתי שליטה: מי שמוריד את השכבה
בעמוד התדמית **לא משלם עליה**, והדוח החינמי לא נוגע בה כלל. עדיין —
**כמה הפקות פרימיום/VIP בחודש אתה מתקצב?** בלי תשובה אני משאיר את המכסה כפי שהיא
ומדווח כשהיא נגמרת.

### 🟡 3. חמש המלצות מה-QA — שתיים מהן שווה לאשר
מלאות ב-`QA/nadlan-v2.md` §המלצות. השתיים המשמעותיות:
1. **סעיף "הרחוב" קיים במסך בלבד** ולא במייל, ב-PDF ובמצגת (`reporthtml.ts`,
   `api/deck`). עבודה קטנה, ערך אמיתי. **אשר ואוסיף.**
2. **הכותרת מציגה את כינוי הרחוב בתוך הכתובת** —
   `"הבעשט (מוכר גם כרחוב הבעל שם טוב) 9, רחובות"`. נכון מבחינת מידע, מסורבל
   ככותרת ראשית, והשורה שמתחת כבר אומרת את זה ממילא. **אשר ואנקה את הכותרת.**

---

## ✅ 0. Google SSO — הופעל (31/07). נשאר לך דבר אחד: להיכנס פעם אחת

הכניסה האחידה **עובדת**: `eueu1234`/`eueu1234` נכנס בפועל, הסשן עובר בין
מערכות, יש יציאה, ויש כניסת ניהול. **Google הופעל בהאב** עם המזהה ששלחת;
Site URL ו-Redirect URLs אומתו כנכונים. ראיות מלאות ב-`QA/auth.md`.

**מה נותר — לחיצה אחת שלך:** להיכנס ל-`more30.com/login`, ללחוץ
"המשך עם Google" **ולהקליד את פרטי ה-Gmail שלך**. אין לי אותם, וגם לא צריך
שיהיו לי. כל השאר אומת: הספק דלוק, הכפתור מופיע, Google מקבל את המזהה ואת
הכתובת בלי `redirect_uri_mismatch`, ונתיב החזרה נשמר.
*(החשבון לא ייכפל — Supabase יקשר את Google ל-`l023131500@gmail.com` הקיים
ותשמור על הרשאת הניהול.)*

### 🔴 שתי טעויות בפרטים ששלחת — שתיהן נתפסו לפני שנגעתי במשהו
1. **`bieebmnnkffwbqlsfozh` אינו קיים** (DNS לא נפתר). הפרויקט האמיתי הוא
   **`bieebmnm`kffwbqlsfozh** — `m` ולא `n`. כלומר ה-redirect שרשום היום
   ב-Google Cloud לפרויקט הישן **מצביע לשום מקום**. אין נזק — פשוט שורה מיותרת.
2. **ה-`service_role` של `bieebmnm` שנשמר ב-4 קבצי `.env` מקומיים — פג**
   (`Invalid API key`). כל תיעוד קודם שהניח שיש לנו כתיבה לפרויקט הזה אינו
   נכון יותר. אם תרצה גישה אליו — צריך מפתח חדש מהדשבורד שלו.

### 🟠 שלושה סודות שנשלחו כטקסט גלוי — כדאי לסובב
`GOOGLE_OAUTH_CLIENT_SECRET` · `SUPABASE_ACCESS_TOKEN` (PAT, הרשאות ניהול
רחבות) · והטוקן הקלאסי של GitHub מסעיף 3.1. שלושתם שמורים ב-`core.secrets`,
אבל הם עברו בצ'אט. **ה-PAT הוא החמור מביניהם** — הוא יכול לשנות הגדרות בכל
פרויקט Supabase בחשבון.

### 🟡 שני נתיבים ממתינים לאישור שלך כדי לקבל את כפתור הכניסה
| נתיב | מה צריך |
|---|---|
| `/modaot` (03 igud-ads) | **אישור** לפרוס מחדש. השינוי הוא שורת `<script>` אחת ב-HTML ואינו נוגע במסד ולא בסליקה |
| `/bkalot` (10) | **אישור** — אם זו מערכת הזכויות ולא `bkalut-app`/`bkalot-admin`, אוסיף בדקה |

### 🟡 שאלה שנפתחה מהמדידה: שני מאגרי משתמשים
זהות הפלטפורמה יושבת בהאב. אבל 01/02/03/18 עדיין מאמתים מול `bieebmnm`.
כלומר יש **שני** מאגרי משתמשים. להעביר אותם להאב זו עבודה אמיתית (ונגיעה
במערכות עם סליקה חיה), ולכן לא התחלתי. תגיד אם זה בסדר בינתיים.

### 🟡 שני נתיבים ממתינים לאישור שלך כדי לקבל את כפתור הכניסה
| נתיב | מה צריך |
|---|---|
| `/modaot` (03 igud-ads) | **אישור** לפרוס מחדש. השינוי הוא שורת `<script>` אחת ב-HTML ואינו נוגע במסד ולא בסליקה |
| `/bkalot` (10) | **אישור** — אם זו מערכת הזכויות ולא `bkalut-app`/`bkalot-admin`, אוסיף בדקה |

### החלטה שקיבלתי כדי לא לעצור — ניתנת לביטול
ההגדרה במסמך-האב היא **כניסה אחת בכל המערכות**, ולכן זהות המשתמשים יושבת
ב**האב** (`uhnrgujbdxhhmoxcjria`) — זו למעשה תשובה ג׳ לשאלה בסעיף 1 למטה.
זה מה שגם איפשר לסשן לעבור בין מערכות בלי שום תשתית. אם תעדיף אחרת, תגיד.

---

## 🔴 0*. שני דברים פתוחים מהסבב האחרון (01/08)

### 1. מערכת 18 (העורך התורני) — הסשן לא נתפס, והעבודה עצורה באמצע
בניתי את **אזור העלאת הספר/קובץ, "המסמכים שלי" ושמירת טיוטות** — הפער שסימנת
כחובה. הקוד כתוב, נפרס, ועובר typecheck; המצב **לא-מחובר** אומת בפרודקשן
(אזור ההעלאה מוצג, ומופיעה הזמנה להתחבר).

**מה לא עובד:** אחרי כניסה דרך `more30.com/login`, הדפדפן נחת ב-`/me`, ואזור
המסמכים עדיין דיווח שאין סשן. כלומר הזהות של ההאב אינה נקראת בתוך אפליקציית
`/orech`. זה באג פתוח, לא פיצ'ר גמור, ולא סימנתי אותו כגמור בשום מקום.

**רקע להחלטה שלך:** האפליקציה יושבת על `bieebmnm` — המסד של igud-ads עם
הסליקה החיה. לכן **לא** התקנתי שם סכימה (זו ההכרעה שפתוחה אצלך מזה כמה סבבים),
אלא יצרתי את `public.orech_documents` **בהאב** עם RLS לפי המשתמש. זו הסיבה
שצריך לקרוא שני סשנים שונים באותה אפליקציה, וכנראה גם שורש הבאג.

### 2. נדל"ן — צילומי הבניין, שוב תלוי-רשת
בסבב הקודם דיווחתי שהתמונות נראו. בהרצה האחרונה **נטפרי החליפה חלק מהן**:
הצילום של שמואל הנביא 86 חזר כמציין מקום מטושטש, בעוד שדורש טוב 17 חזר תקין.
זה ארטיפקט של הרשת המקומית, לא של המערכת — אבל פירושו שהאימות הוויזואלי כאן
חלקי. מי שאינו מאחורי נטפרי יראה את התמונה האמיתית.

---

## 👁️ 0א. נדל"ן — בדיקה אחת של דקה, ושתי הכרעות (עודכן 01/08)

**כל 11 סעיפי `NADLAN_SPEC_FULL` בוצעו ואומתו.** הראיות המלאות, כולל 39
צילומי מסך: [`QA/nadlan.md`](QA/nadlan.md). מה שנשאר אצלך:

### ✅ מה שכבר נסגר — צילומי הבניין
בסבב הקודם ביקשתי שתבדוק את המראה, כי נטפרי החליפה כל תמונה במציין מקום.
**הפעם הדפדפן שהריץ את הבדיקה קיבל את התמונות האמיתיות.** בכל ארבע הכתובות
הצילום נטען 640×500 ומראה חזית בניין הממלאת את הפריים — כולל בדורש טוב 17,
הכתובת שהמפרט מציין כתקולה. אין צורך שתבדוק את זה.

**מה שעדיין שווה דקה שלך:** לוודא ש**מספר הבית** בתמונה הוא הנכון. אין בצילום
נתון קריא-מכונה של מספר הבית, ולכן ההתאמה לשלט על הקיר היא בדיקה אנושית:
`more30.com/nadlan/report?q=דורש טוב 17 ירושלים&tier=vip` (וכן שאר השלוש).
אם אחד מכוון לצד הלא נכון — תגיד לי איזה, ואכוון ידנית (`?heading=` נתמך).

### 🟠 החלטה 1: מה שאני עשיתי לדוח החינמי — ואפשר לבטל
מצאתי ש"הדוח החינמי" **עלה לנו כ-0.25$ לכל הפקה** (שבע שאילתות Google Places
וחמישה יעדי מטריצת מרחקים). §2 במפרט אומר "החינמי לא עולה לנו כלום", ולכן
חסמתי ברמה הזו את כל המקורות שנגבים לכל משיכה. **המחיר:** הדוח החינמי מציג
עכשיו מוסדות ותחנות מהמרשמים הפתוחים עם **מרחק אווירי בלבד**, בלי זמני הליכה
אמיתיים ובלי בתי כנסת/מסחר/בריאות מ-Google. **נמדד: $0.000 לדוח.**

אם ההעדפה שלך היא שהחינמי יישאר עשיר יותר וישלם — זו החלפה של שורה אחת
(`tierMayUsePaidSources` ב-`lib/report.ts`). תגיד ואחזיר.

### 🟠 החלטה 2: צילום ומצגת עברו ל-VIP
§2 מייחד ל-VIP "צילום בניין מדויק + מפה אזורית מלאה + הורדת מצגת + PDF מעוצב",
ולכן הוצאתי אותם מהרמות הנמוכות. עד עכשיו הם הוצגו בכולן. אם אתה רוצה שהצילום
יופיע גם בפרימיום — זו החלטה מסחרית שלך, לא טכנית.

### 🔑 מה שחסר כדי לסגור את שני הפריטים האחרונים
- **שנת בנייה ממקור מוסמך** (§5) — היום היא נגזרת מהעסקאות ומסומנת "הערכה",
  ובבניין בלי עסקאות היא "לא זמין". ירושלים, תל אביב וחיפה מפרסמות שכבת
  מבנים עם שנת היתר ב-GIS העירוני. **אם תאשר, אחבר אותן** — זה יהפוך את
  הסעיף ל"אמת" בערים הגדולות ויכסה בדיוק את דורש טוב 17 והדקל 22.
- **נסח טאבו** (§2 VIP, §8) — הזרימה בנויה (`lib/tabu.ts`,
  `nadlan.document_requests`), **חסר ספק**: `TABU_API_URL` + `TABU_API_KEY`.
  בלעדיהם המסלול נשאר "הזמנה רשמית מ-gov.il לפי דרישה".

---

## 🟠 0ב. שלוש הכרעות שנפתחו ממשימת ההרשאות (31/07 ערב)

### 1. אימות אימייל כובה — כדי שהרשמה תעבוד מיד
ביקשת ש"הרשמה יוצרת משתמש + חברות **והוא עובד מיד**". עם ההגדרה הקודמת
(`mailer_autoconfirm=false`) חשבון חדש לא היה מקבל סשן עד לחיצה על קישור
במייל, ולכן הדלקתי אותה. **המשמעות:** כתובת אימייל אינה מאומתת יותר בהרשמה.

*מה עשיתי כדי לצמצם:* מסלול ההרשאה שנגזר מאימייל (`more30_is_super_admin`)
דורש `email_confirmed_at`, וכתובת שכבר קיימת אינה ניתנת לרישום שוב.

*להחזיר:* Supabase → Authentication → Providers → Email → Confirm email.
**תגיד ואחזיר תוך דקה.** אין שינוי קוד — רק ההודעה בדף ההרשמה תשתנה בהתאם
(היא כבר כתובה לשני המצבים).

### 2. `/modaot` — עדיין בלי כפתור כניסה, וממתין לאישור שלך
זו המערכת היחידה מתוך 21 שאין בה את הכפתור המשותף. השינוי הוא שורת
`<script>` אחת ב-HTML ואינו נוגע במסד ולא בסליקה — אבל הפריסה מחדש היא של
מערכת עם **סליקה חיה**, ולכן לא עשיתי אותה בלי מילה ממך.
*(`/bkalot` **כן** קיבל את הכפתור: הוא מערכת הזכויות #10 ואינו אחת משתי
המערכות המוגנות של חוק 7.)*

### 3. קאט-אובר של 01/02/03/22/31 להאב — ההכרעה שסוגרת את "כניסה אחת"
חמש מערכות מחזיקות מאגר משתמשים בפרויקט Supabase משלהן, ולכן הסופר-אדמין
הגלובלי **אינו** מזוהה שם. אי אפשר לעקוף את זה בחלוקת מפתח סשן: JWT של ההאב
אינו תקף מול פרויקט אחר, ושימוש באותו מפתח היה מנתק את המשתמשים הקיימים.

| # | מערכת | הפרויקט שלה | מה צריך |
|---|---|---|---|
| 01, 02, 03 | torah · tamlul · modaot | `bieebmnm` | אישור לקאט-אובר. **שם גם הסליקה החיה** |
| 22 | zchuyot | `trerolyv` | `service_role` של הפרויקט, או אישור לקאט-אובר |
| 31 | gesher | `ygaqq` | אותו דבר |

*חלופה זולה שכבר עובדת:* ב-15 egod — שגם היא על פרויקט זר — חיברתי **חיבור
שני** להאב ששואל רק "האם המשתמש מנהל". זה נותן שער אחיד בלי קאט-אובר, אבל
הנתונים באותו מסך עדיין נשלטים ב-RLS של הפרויקט שלה. אפשר להחיל על 22 ו-31.

---

## 🔴 1. הכרעה אחת שחוסמת שלושה שלבים — איפה יושבת זהות המשתמשים

**זה הפריט היחיד שחוסם את שלב 4 (התחברות/היסטוריה/מנוי), את שלב 5 (18) ואת
כל תשתית המנויים.** הוא לא דורש מפתח — הוא דורש החלטה.

| מערכת | המסד שלה היום | הבעיה |
|---|---|---|
| 17 chizukim | `csjekrvu` | אין לי `service_role` → אי אפשר ליצור שם טבלאות |
| 02 tamlul + 18 orech | `bieebmnm` | **אותו פרויקט של igud-ads עם הסליקה החיה** |

**שלוש דרכים, בחר אחת:**

- **א. גישה ל-`csjekrvu`** → הכול נבנה במקום אחד.
  *מה צריך:* `service_role key` מ-Supabase Dashboard → Project `csjekrvu` →
  Settings → API → `service_role`.
  *לאן:* Vercel project **`chizukim2-more30`** → Environment Variables →
  `SUPABASE_SERVICE_ROLE_KEY` (Production), וגם
  `apps/17-chizukim-transcribe/.env.local` לפיתוח מקומי.

- **ב. אישור להריץ את הסכמה על `bieebmnm`** (עבור 18).
  *מה צריך:* רק **"כן"** ממך — המפתח כבר קיים מקומית.
  ⚠️ `apps/18-torah-editor-mvp/supabase/schema.sql` מתקין **טריגר על
  `auth.users`** שירוץ על **כל הרשמה בפרויקט, כולל של igud-ads**. לכן לא הרצתי.

- **ג. הזהות בהאב** `uhnrgujbdxhhmoxcjria` (סכמות `public.chizukim_*` /
  `public.editor_*`). *מה צריך:* רק "כן" — יש לי גישה מלאה. החיסרון: הזהות
  מתפצלת משני מסדים.

> **עודכן 09/08 — ההכרעה הזאת חוסמת עכשיו גם חצי מ-`core.issues #86`.**
> #86 נשא עד היום מספר אחד: "שש מערכות ציבוריות חיות בלי מסך ניהול". נמדד
> (`scripts/qa/admin-gap-what-to-manage.mjs`, 8/8) שהשש אינן משימה אחת. מסך
> ניהול הוא קורא, ולכן הוא תלוי בפרויקט שמחזיק את השורות:
>
> | מערכת | הפרויקט | מאיפה נמדד |
> |---|---|---|
> | 17 chizukim | `csjekrvukbdznetsrodj` | ליטרל במקור — `client/src/lib/supabase.ts`, `server/routes.ts` |
> | 12 smel | `csjekrvukbdznetsrodj` | ליטרל במקור — `client/src/lib/nadlanApi.ts`, `server/routes.ts` |
> | 18 orech | `bieebmnmkffwbqlsfozh` | `.env.example` + `core.projects` — **אין ליטרל במקור** |
>
> שלושתן אינן בפרויקט היחיד שהחשבון הזה מנהל, ולכן אין שם טבלה להצביע עליה,
> אין policy לכתוב ואין RPC להוסיף. **אלה אינן עבודה שממתינה לתור — הן ממתינות
> לתשובה כאן.** שלוש האחרות (34 kesef · 26 studio · 04 imud) יושבות בהאב ואינן
> חסומות; הראשונה לבנייה היא kesef, שמחזיקה 283 שורות אמיתיות בסכימת `kesef`
> (מתוכן 259 רשויות) שאיש אינו יכול לראות היום. `QA/platform/admin-gap-0809/`.

---

## 🔴 2. פרצת אבטחה פתוחה — ה-anon key מוחק את ארכיון התמלולים

**נמדד חי, לא הערכה.** בפרויקט `csjekrvu`, טבלת `recordings`
(**1,138 הקלטות ותמלולים**) פתוחה לכתיבה עם ה-**anon key** — המפתח שמוטמע
בבאנדל הציבורי וגלוי לכל מי שפותח את הדפדפן:

| פעולה עם anon | תוצאה שנמדדה |
|---|---|
| `POST /rest/v1/recordings` | **201 Created** |
| `DELETE /rest/v1/recordings?id=eq.…` | **200** — נמחק |

*(הרשומה שיצרתי בבדיקה נמחקה מיד; הספירה חזרה ל-1,138 בדיוק.)*

**מה לעשות — 5 דקות בדשבורד של `csjekrvu`:** להפעיל RLS על `recordings`,
להשאיר policy של `SELECT` ל-anon, ולהעביר `INSERT/UPDATE/DELETE` ל-`service_role`.
כדאי להריץ את אותה בדיקה על שאר הטבלאות שם (מערכות 06, 12, 27).

> **נבדק שוב 03/08 — עדיין לא ניתן לתקן מכאן, וזו אינה הנחה.**
> `list_projects` דרך ה-MCP של Supabase מחזיר **פרויקט אחד בלבד**:
> `uhnrgujbdxhhmoxcjria` (ההאב). `csjekrvu` יושב בחשבון Supabase אחר,
> ולכן גם ה-PAT שברשותי אינו רואה אותו. **צריך שתפעיל את זה בדשבורד
> שלו** — או תיתן PAT של אותו חשבון, ואז אריץ את המיגרציה בעצמי.
>
> ה-anon key של `csjekrvu` אכן מוטמע בקוד הציבורי (אומת:
> `apps/06-kupot-holim/site/supabase-config.js`, `role=anon`), כך שהפרצה
> פתוחה בדיוק כפי שתוארה.

---

## 🟠 3. מפתחות חסרים — מה, מאיפה, ולאן

| # | מערכת | המפתח | מאיפה משיגים | לאן נכנס |
|---|---|---|---|---|
| 1 | **17** chizukim | `RUNPOD_API_KEY` | runpod.io → Settings → API Keys | Vercel `chizukim2-more30` → env `RUNPOD_API_KEY` |
| 2 | **17** chizukim | `SUPABASE_SERVICE_ROLE_KEY` | Supabase `csjekrvu` → Settings → API | Vercel `chizukim2-more30` + `apps/17-.../.env.local` |
| 3 | **28** kupot | `ADMIN_TOKEN` | להמציא מחרוזת אקראית ארוכה | Vercel `kupot-more30` → env `ADMIN_TOKEN` |
| 4 | **27** mechiron | `YEMOT_TOKEN` | ימות המשיח → אזור אישי → API | Vercel `mechiron-more30` |
| 5 | **27** mechiron | `ELEVENLABS_API_KEY` | elevenlabs.io → Profile → API Key | Vercel `mechiron-more30` |
| 6 | **02** tamlul | Google OAuth `CLIENT_ID` + `CLIENT_SECRET` | console.cloud.google.com → APIs → Credentials → OAuth client | Vercel `tamlul-more30` |
| 7 | **18** orech | `TRANSKRIBUS_USER/PASS` או `KRAKEN_*` | readcoop.eu (Transkribus) / kraken — מודול ה-HTR | Vercel `orech-more30` + `apps/18-.../.env.local` |
| 8 | **32** nadlan | שדרוג תוכנית **Apify** | apify.com → Billing (המכסה מוצתה: **$5.28 מתוך $5**, מתאפסת **27/08/2026**) | אין שינוי קוד — `APIFY_TOKEN` כבר מוגדר |
| 9 | **הכול** | תיקון הרשאות ב-PAT שסיפקת | ראה סעיף 3.1 למטה — **הטוקן נשמר ועובד, ההרשאה חסרה** | — |
| 10 | **16** chatzor *(רשות)* | `GOOGLE_MAPS_API_KEY` | console.cloud.google.com → Distance Matrix API | Vercel `chatzor-more30`. **לא חוסם** — בניתי מרחק אווירי מהדפדפן בלי מפתח; המפתח ישדרג למרחק/זמן **הליכה** |

---

### 3.1 ✅ הדחיפה האוטומטית — **נפתרה** (31/07, עם הטוקן הקלאסי ששלחת)

הטוקן הקלאסי עובד: `repo` מלא, כתיבה לשני הריפואים. **10 הקומיטים שהמתינו נדחפו.**

| ריפו | מה נדחף |
|---|---|
| `l023131500-ops/-` | 9 קומיטים → `feature/unify-phase1` |
| `l023131500-ops/nadlan-berega` | 3 קומיטים → `main` |
| `l023131500-ops/chizukim-transcribe` | ענף חדש `more30/night-work` |
| `l023131500-ops/chatzor-connect` | ענף חדש `more30/night-work` |
| `l023131500-ops/get-your-rights` | ענף חדש `more30/night-work` (22 — לא היה ריפו מקומי כלל) |

**החלטה שקיבלתי בלי לשאול, ואפשר לבטל:** ל-16 ול-17 כבר היו ריפואים פרטיים
בחשבון, עם היסטוריה **אחרת** לגמרי ב-`master`. לא דרסתי כלום — דחפתי לענף נפרד
`more30/night-work` בכל אחד. אם תרצה שזה יהיה ה-`master` שלהם, תגיד ואמזג.

**מה שחסם קודם ולא היה קשור להרשאות:** ב-`gitconfig` המערכתי של GitHub Desktop
מוגדר helper בשם `manager`, שרץ **לפני** ה-`wincred` הגלובלי והחזיר את הטוקן
הישן (fine-grained, קריאה בלבד). git לוקח את התשובה הראשונה, ולכן הטוקן החדש
מעולם לא נוסה. תוקן: רשומת ה-credential הישנה נמחקה, ורשימת ה-helpers מאופסת
ב-`.gitconfig` לפני `wincred` כדי שה-helper המערכתי לא יענה ראשון.

> 🔴 **הטוקן הזה נשלח אליי כטקסט גלוי, ולכן יש להתייחס אליו כחשוף.** הוא שמור
> מוצפן ב-Windows Credential Manager (לא ב-`.git/config`, לא בקובץ טקסט), אבל
> **כדאי לסובב אותו** ברגע שהעבודה נרגעת. הרשאותיו רחבות מאוד: `repo`,
> `delete_repo`, `admin:org`, `workflow`.

### 3.2 ✅ מפתח ה-`service_role` של ההאב — **נפתר (31/07 ערב). אין צורך בך.**

התברר שהוא **כן** ניתן לשליפה עם ה-PAT שכבר סיפקת:
`GET https://api.supabase.com/v1/projects/uhnrgujbdxhhmoxcjria/api-keys?reveal=true`
מחזיר את `anon` ואת `service_role` המלאים. שלושת המסלולים שנבדקו קודם
(Vercel · Supabase MCP · `app.settings.jwt_secret`) אכן סגורים — אבל
ה-Management API לא נוסה. הסעיף הזה נשמר לתיעוד בלבד.

<details><summary>הניסוח הקודם</summary>

`core.secrets` בנוי, מאובטח ומאוכלס (71 מפתחות). מה שחסר כדי **לחבר** אליו
מערכת בפועל הוא מפתח קריאה אחד.

*מה צריך:* Supabase → פרויקט `uhnrgujbdxhhmoxcjria` → Settings → API →
`service_role`. **או** לחלופין PAT: Account → Tokens → `sbp_...` (אז אשלוף
לבד, וגם `scripts/Use-SupabasePat.ps1` יתחיל לעבוד).

*לאן:* אכניס בעצמי כ-`MORE30_SECRETS_KEY` לכל פרויקט Vercel.

*למה אני לא יכול להשיג אותו לבד — נבדק, לא הונח:*
Vercel מסמן אותו `sensitive` ולא מחזיר ערך (גם לא ב-API עם `decrypt=true`);
ה-MCP של Supabase מחזיר **רק** מפתחות פומביים; ו-`app.settings.jwt_secret`
אינו קריא במסד, כך שאי אפשר גם לייצר אישור ייעודי מצומצם במקומו.

</details>

### 3.3 🟠 31 מפתחות שקיימים **רק** ב-Vercel — ולכן ההאב עדיין לא מקורם

Vercel לא מחזיר ערכים של משתנים מסומנים `sensitive`. **הם ממשיכים לעבוד
בפרודקשן** — זה לא תקלה — אבל הם לא ב-`core.secrets`, ולכן אין להם מקור-אמת אחד.

מתוכם, אלה שהם **מפתח אמיתי** (השאר הם קונפיגורציה כמו `AI_MODEL`, `VITE_BASE`):

| מפתח | מערכת | פרויקט Vercel |
|---|---|---|
| `SUPABASE_SERVICE_KEY` | nadlan · studio · imud | `nadlan-more30` · `studio-more30` · `imud-more30` |
| `AI_API_KEY` | nadlan | `nadlan-more30` |
| `RESEND_API_KEY` | nadlan | `nadlan-more30` |
| `POI_REFRESH_TOKEN` | nadlan | `nadlan-more30` |
| `ADMIN_TOKEN` | nadlan · kupot | `nadlan-more30` · `kupot-more30` |
| `ELEVENLABS_API_KEY` | mechiron | `mechiron-more30` |
| `YEMOT_API_KEY` | mechiron | `mechiron-more30` |
| `RIGHTS_SUPABASE_KEY` | zchuyot | `zchuyot-more30` |
| `LAB_TOKEN` | chizukim | `chizukim2-more30` |
| `DATAGOV_SCHOOLS_RESOURCE` · `DATAGOV_MIKVE_RESOURCE` | nadlan | `nadlan-more30` |

**שים לב:** `ELEVENLABS_API_KEY` ו-`YEMOT_API_KEY` מופיעים בסעיף 3 למעלה
כ"חסרים" — הם בעצם **כבר מוגדרים** ב-`mechiron-more30`. אין צורך להשיג אותם
מחדש; צריך רק להעתיק את ערכם ל-`core.secrets` (או לתת לי את מפתח ההאב מ-3.2,
ואבנה מסלול שבו כל מערכת כותבת את שלה פנימה).

### 3.4 🟡 סודות Actions יושבים על ריפו **ציבורי**

9 סודות בנייה הוגדרו ב-`l023131500-ops/-` (וב-3 הריפואים הפרטיים). המונוריפו
**ציבורי**. GitHub לא מעביר סודות ל-workflow שרץ מ-PR של fork, אבל כל מי שיכול
לדחוף קובץ workflow לריפו יכול לקרוא אותם. שלוש אפשרויות: להפוך את המונוריפו
לפרטי · להשאיר כך (הסיכון תיאורטי כל עוד אתה היחיד עם גישת כתיבה) · או להסיר
משם את מפתחות ה-AI ולהשאיר רק את הפומביים. **תגיד ואבצע.**

### 3.5 🔑 הדחיפה האוטומטית — הגרסה הישנה (נשמר לתיעוד)

**מה כבר נעשה (31/07):** `credential.helper = wincred` הוגדר גלובלית והטוקן
נשמר ב-Windows Credential Manager (מוצפן, לא בקובץ טקסט ולא ב-`.git/config`,
ולכן גם לא ידלוף לריפו). **האימות עובד** — `git ls-remote` נגד המונוריפו
מצליח. מה שנכשל הוא ההרשאה, וזה דבר אחר.

**נמדד מול ה-API של GitHub:**

| בדיקה | תוצאה |
|---|---|
| `GET /user` | 200 · `l023131500-ops` · טוקן fine-grained |
| `GET /repos/l023131500-ops/-/contents/README.md` | **200** → `contents=read` **יש** |
| `POST /repos/l023131500-ops/-/git/blobs` | **403** `needs: contents=write` → **אין** |
| `GET /repos/l023131500-ops/nadlan-berega` | **404** `needs: metadata=read` → הריפו **לא ברשימת הריפואים של הטוקן** |
| ריפואים שהטוקן רואה | `-` · `more.30.com` · `zol` (שלושה בלבד) |

**שני תיקונים ב-github.com → Settings → Developer settings → Personal access
tokens → Fine-grained tokens → הטוקן הזה → Edit:**

1. **Permissions → Repository permissions → `Contents`: מ-`Read-only` ל-`Read and write`.**
   זה מה שחוסם את הדחיפה למונוריפו.
2. **Repository access → להוסיף את `nadlan-berega`** (או לבחור *All repositories*).
   כרגע הריפו הזה בכלל לא בהרשאה, ולכן גם קריאה ממנו נכשלת.

*(שינוי הרשאות לא מחליף את הטוקן — מה ששמור אצלי ימשיך לעבוד. אם בכל זאת
תיצור טוקן חדש, אמור ואעדכן. וכדאי להתייחס לטוקן הזה כחשוף, כי נשלח כטקסט.)*

**מה ממתין לדחיפה ברגע שזה יתוקן:**

| ריפו | מצב | ממתין |
|---|---|---|
| `l023131500-ops/-` (מונוריפו) | קריאה ✓ · כתיבה ✗ | **7 קומיטים** (`66d6715`…`c862c94`) |
| `l023131500-ops/nadlan-berega` | לא בהרשאה כלל | **3 קומיטים** (`3fb1935`, `f10e000`, `677f4e0`) |
| `apps/17-chizukim-transcribe` | ריפו מקומי, **אין origin** | 2 קומיטים — צריך להחליט אם ליצור ריפו ב-GitHub |
| `apps/16-chatzor-connect` | ריפו מקומי, **אין origin** | 1 קומיט — אותה שאלה |

> **החלטה קטנה שדרושה:** ל-16 ול-17 אין ריפו מרוחק. `app.json` של 17 מפנה
> ל-`l023131500-ops/chizukim-transcribe` (לא קיים), ושל 16 מפנה ל-`-` שהוא
> המונוריפו. תגיד אם ליצור להם ריפואים (ואז הטוקן צריך גם
> `Administration: write`), או להשאיר אותם מקומיים.

## 🟡 4. שלושה נתיבים בלי כפתור הכניסה המשותף (20/23 קיימים)

| נתיב | למה בחוץ | מה צריך ממך |
|---|---|---|
| `/modaot` (03 igud-ads) | סליקה חיה | **אישור** לפרוס מחדש. השינוי הוא שורת `<script>` אחת ב-HTML ולא נוגע במסד |
| `/bkalot` (10) | משפחת המערכות המוגנות | **אישור** — אם זו מערכת הזכויות ולא `bkalut-app`/`bkalot-admin`, אוסיף בדקה |
| `/admin` (nihul) | 🚨 `_deploy/nihul-more30` **מיושנת** — מגישה `/nihul` בעוד הפרודקשן מגיש `/admin`. פריסה משם **החזירה 404 על מרכז השליטה** (הוחזר מיד ב-`vercel promote`) | **המקור האמיתי** של מה שחי היום ב-`nihul-more30`, או אישור לסנכרן את התיקייה מהפרודקשן |

---

## 🟡 5. שלב 8 (עיצוב) — דורש אותך מהגדרתו

`DESIGN_STANDARD.md` (יושב ב-`C:\Users\USER\Downloads\`, **לא בריפו**) מחייב:
זהות עיצובית **ייחודית לכל מערכת** + `BRAND.md` לכל אחת + `DESIGN_DNA.md`
משותף, ו**"כל טענת סיום מחייבת build עובר + צילום מסך + הצגה למשתמש"**.

**מה צריך ממך כדי להתחיל:**
1. **סדר עדיפויות** — על אילו מערכות לעבור קודם (33 זה לא סבב אחד).
2. **בריף מותג** לכל מערכת שנבחרת, או אישור שאנסח הצעה ואתה תאשר.
3. **החלטה איפה יושב התקן** — כדאי להעביר את `DESIGN_STANDARD.md` לריפו
   (`docs/`) כדי שיהיה בגרסאות ולא בתיקיית ההורדות.

**אותו דבר לשלבים 9 ו-10:** 9 (פרסום/מיתוג) דורש בחירת סגנון + אולי מפתחות
Ideogram/Flux; 10 (סליקה) דורש פרטי מוסד נדרים פלוס — **ואל תצפה שאבצע חיוב
אמיתי בלי אישור מפורש**.

---

## 🟢 6. פריטים קטנים

- ~~**פרויקט Vercel יתום בשם `dist`**~~ — ✅ **נמחק 03/08.** לפני המחיקה
  אומת שאין לו דומיין מותאם (`more30.com` היחיד בחשבון, והוא של
  `more30-portal`) ושאף rewrite ב-`portal/vercel.dist.json` אינו מפנה אליו.
- **`DESIGN_STANDARD.md` כבר בריפו** (שורש הפרויקט, נשמר בגיט).
  `MASTER_PLAN.md` עדיין יושב ב-`Downloads\` בלבד.
- **חצור (16):** אין מקור ציבורי לזמני תפילה — הם ייכנסו רק כשגבאי יזין
  אותם באזור הניהול. גם כתובות בתי הכנסת חסרות ב-OSM (מוצג "לא זמין").
  אם יש לך עלון/קבוצה/איש קשר ביישוב — זה ימלא את שני הסעיפים.

---

## ℹ️ 7. דברים שנבדקו ואין עליהם מה לעשות (לא באג)

- **יד2 / מדלן ישירות:** `gw.yad2.co.il` מחזיר Radware Bot Manager Captcha,
  מדלן 400. **אין מסלול חלופי** מלבד ה-actors בתשלום. החלטה עסקית על עלות.
- **MAVAT (תקנון/תשריט):** `rest/api/SV4` דורש טוקן **reCAPTCHA Enterprise**
  שנוצר בדפדפן. לא עוקפים captcha. הדוח מקשר לעמוד הרשמי ואומר איפה ההורדה.
  *אם תרצה את ה-PDF בתוך הדוח* — צריך ספק מורשה או הסכמה מול מינהל התכנון.
- **חורי כיסוי במפה התכנונית** (נמדד בהדקל 22, חצור): שכבות התכנון מחזירות
  אפס על החלקה וכיסוי מלא ב-120 מ'. הדוח מבחין בין "אין תוכנית" ל"אין תוכנית
  **מקוונת**". אם זה תרחיש נפוץ אצל לקוחות — שווה לשקול חיבור ל-GIS העירוני.

---

## 🟡 0ס. מספר הטלפון של 01 — שלושה מספרים שאינם מסכימים (10/08/2026)

**מה בדיוק חסר:** מה המספר הנכון של `01-torah-platform`.

הפוטר של המערכת נושא שלושה דברים שונים, ואי אפשר לגזור מהם יעד אחד:

| מקום | מה כתוב שם |
|---|---|
| `Footer.tsx:75` | `tel:0231330600` |
| `Footer.tsx:281` | הטקסט המוצג — `023-133-0600` |
| `Footer.tsx:81` | המייל `A023131600@GMAIL.COM`, שרומז על `02-3131600` |
| `Footer.tsx:253` | `wa.me/97223130600` |

`023-133-0600` הוא עשר ספרות, ואין קידומת `023` בישראל — כלומר גם המספר
**המוצג** חשוד, לא רק הקישורים. לא נגעתי בכלום כאן: כל תיקון היה המצאה.

**מאיפה משיגים:** ממך. **לאן זה נכנס:** ארבע השורות למעלה, ואז בנייה ופריסה של
01. core.issues #138.

**שאלה שנייה, לכל המערכות:** האם `02-3131500` ו-`02-3131600` רשומים ב-WhatsApp
בכלל? שניהם קווים נייחים. יש בעץ **15 קישורי wa.me ואף אחד אינו בפורמט תקין**
(core.issues #137, `QA/platform/WHATSAPP_LINKS.md`) — 14 מהם אתקן לבד לפי המספר
שמוצג לידם, אבל אם אין חשבון WhatsApp על המספר, קישור תקין-צורנית עדיין לא ייפתח.
