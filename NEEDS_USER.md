# NEEDS_USER.md — מה שדורש אותך (מפתח / החלטה / אישור)

> נוצר לפי חוק 4 ב-MASTER_PLAN: כשמשהו דורש אותך — נרשם כאן, ואני ממשיך לשלב הבא
> במקום לעצור. עודכן: 30/07/2026.

## 🚨 דחוף — 17 chizukim: המפתח הציבורי מאפשר למחוק את כל הארכיון

**נמדד חי היום (30/07), לא הערכה.** בפרויקט `csjekrvu` הטבלה `recordings`
(1,138 הקלטות ותמלולים) פתוחה לכתיבה עם ה-**anon key**, וזה המפתח שמוטמע
בבאנדל של הלקוח וגלוי לכל מי שפותח את הדפדפן:

| פעולה עם anon | תוצאה שנמדדה |
|---|---|
| `POST /rest/v1/recordings` | **201 Created** — נוצרה שורה |
| `DELETE /rest/v1/recordings?id=eq.…` | **200** — השורה נמחקה |

כלומר כל אדם ברשת יכול להוסיף רשומות ולמחוק את הארכיון כולו.
**הרשומה שיצרתי בבדיקה נמחקה מיד; הספירה חזרה ל-1,138 בדיוק.**

**למה לא תיקנתי:** אין לי `service_role` ל-`csjekrvu` (המפתח לא קיים באף
פריסה או עותק מקומי, והפרויקט אינו בחשבון ה-MCP שלי). תיקון RLS מחייב גישה
לדשבורד של הפרויקט הזה.

**מה לעשות (5 דקות בדשבורד):** להפעיל RLS על `recordings`, להשאיר policy של
`SELECT` ל-anon בלבד, ולהעביר את ה-INSERT/UPDATE/DELETE ל-`service_role`
(השרת של האפליקציה ממילא צריך מפתח שרת). אותה בדיקה כדאי להריץ על שאר
הטבלאות ב-`csjekrvu` (מערכות 06, 12, 27).

## שלב 3 — נדל"ן: שכירות / מסחרי / קרקעות + מנוע ההיתרים

### 🔴 1. מכסת Apify מוצתה — זה מה שחוסם מודעות שכירות ומסחרי
- **המצב המדוד:** `monthlyUsageUsd = 5.28` מתוך תקרה של **$5**. המחזור מתחיל
  28/07 ומתאפס **27/08/2026**.
- **מה זה חוסם בפועל:** שני הכפתורים החדשים שנשענים על לוחות המודעות —
  שכר דירה חציוני למ"ר, אומדן שכ"ד לנכס, תשואה שנתית, ונכסים מסחריים המוצעים.
  הדוח **אומר את זה במפורש** ולא מציג רשימה ריקה כאילו אין מודעות באזור.
- **מה שנשאר עובד בלי המכסה:** מדד שכר הדירה של הלמ"ס, עסקאות מסחריות ועסקאות
  קרקע ממרשם העסקאות, זכויות מסחר מהתוכניות, הסביבה העסקית, וכל מנוע ההיתרים.
- **מה צריך ממך:** שדרוג תוכנית ב-Apify (או להמתין ל-27/08). ברגע שיש מכסה —
  אין מה לשנות בקוד; הסעיפים מתמלאים לבד.

### 🟡 2. יד2 ומדלן — אין מסלול חלופי חוקי בלי Apify
נבדק חי בסשן הזה: `gw.yad2.co.il` מחזיר **Radware Bot Manager Captcha**,
ומדלן מחזיר 400 לקריאה ישירה. כלומר המסלול היחיד למודעות הוא ה-actors
בתשלום. אין כאן באג לתקן — זו החלטה עסקית על עלות.

### 🟡 3. MAVAT — קובצי התקנון והתשריט אינם ניתנים למשיכה אוטומטית
נמדד: `mavat.iplan.gov.il/rest/api/SV4/...` דורש כותרת `authorization` שהיא
טוקן **reCAPTCHA Enterprise** שנוצר בדפדפן (בלי הכותרת 404, עם טוקן מזויף 401).
לא עוקפים captcha. הדוח מציג את רשימת המסמכים ומקשר לעמוד התוכנית הרשמי,
ואומר ללקוח במפורש שההורדה עצמה נעשית שם.
**אם תרצה את קובצי ה-PDF בתוך הדוח** — צריך ספק מורשה או הסכמה מול מינהל התכנון.

### 🟡 4. חורי כיסוי במפה התכנונית — לא באג שלנו
נמדד על **הדקל 22, חצור הגלילית**: שכבת התוכניות ושכבת ייעודי הקרקע מחזירות
**אפס** על החלקה עצמה (גם על מרכז החלקה מהקדסטר), ומחזירות כיסוי מלא כבר
ב-120 מ'. זה אופייני לשכונות שתוכננו לפני הדיגיטציה.
הדוח מבחין במפורש בין "אין תוכנית" לבין "אין תוכנית **מקוונת**", ומציג את
ייעוד השכנים כשל השכנים. **הייעוד המחייב שם מתברר רק בוועדה המקומית** —
אם זה תרחיש נפוץ אצל הלקוחות, שווה לשקול חיבור ל-GIS העירוני של אותן רשויות.

## שלב 4 — תמלול (02/17): מה חוסם ומה כן אפשר

### 🔴 1. 02 igud-transcribe — דורש אישור מפורש שלך
02 יושב על `bieebmnm` יחד עם **igud-ads**, שהיא מערכת חיה עם סליקת נדרים
ו-webhook תשלומים. הוספת התחברות ותשתית מנוי שם נוגעת באותו מסד פרודקשן.
זו החלטה שלך, לא משהו שמריצים בלילה. (הועלה כבר בסבב 26/07 ולא הוכרע.)

### 🔴 2. 17 chizukim — אין איפה ליצור משתמשים
מעבר לפרצה למעלה: בלי `service_role` ל-`csjekrvu` **אי אפשר ליצור שם טבלאות**
(משתמשים, היסטוריית עבודות, מנויים) ואי אפשר להגדיר RLS.
**שתי דרכים, ואתה מכריע:**
- **א. גישה ל-csjekrvu** (service_role או משתמש דשבורד) → הכול נבנה במקום אחד.
- **ב. הזהות בהאב** (`uhnrgujb`): התחברות ב-Supabase Auth של ההאב וטבלאות
  `public.chizukim_*` (בדיוק המתכונת שעבדה ב-26 studio), בעוד התמלולים נשארים
  ב-csjekrvu ונקראים ב-anon. עובד היום, בלי להמתין לך — אבל מפצל את הזהות
  משני מסדים, ולכן לא התחלתי בלי שתדע.

### ✅ 3. חיפוש בארכיון — **נבנה ועלה לאוויר** (30/07), לא ממתין לך
הסעיף היחיד בשלב 4 שלא דרש מפתח חדש. חי ב-**more30.com/chizukim**: חיפוש
במסד על חמישה שדות (כולל שני התמלולים), עם תקציר מודגש לכל תוצאה ומעבר
לקטע עצמו. "תשובה" → 249 · "ירושלים" → 111 · "פרשת" → 213.
אגב זה תוקן דיווח שגוי: העמוד הכריז 1,000 הקלטות על ארכיון של **1,138**,
ו-$1.7789 עלות במקום **$2.0629** — PostgREST חתך כל בקשה ב-1,000 שורות.
פירוט מלא ב-`NIGHT_PROGRESS.md`.

### 🔴 4. מה שנשאר חסום בשלב 4 — וזה תלוי בהכרעה אחת שלך
התחברות, אזור אישי, היסטוריה ותשתית מנוי הם כולם *של משתמש*, ולכן אף אחד
מהם לא ניתן לבנייה לפני שתבחר איפה יושבת הזהות (סעיפים 1–2 למעלה).
זו החלטה אחת שמשחררת את כל השאר.

## שלב 5 — עורך/כתב יד (18): חסום על אותו מסד של הסליקה

**נמדד היום (30/07):** 18 יושב על `bieebmnm` — **אותו פרויקט של igud-ads**,
זה עם הסליקה החיה. מתוך הסכמה שהאפליקציה מצפה לה, קיימת שם **רק `profiles`**;
`documents`, `citations`, `edits` ו-`htr_jobs` מחזירים 404, כלומר
`supabase/schema.sql` **מעולם לא הורץ**. ההתחברות עצמה כן אמיתית (ה-middleware
מרענן session מול Supabase Auth ולא ממציא משתמש).

**למה עצרתי ולא הרצתי את הסכמה.** `SUPABASE_SERVICE_ROLE_KEY` של הפרויקט קיים
מקומית, כך שטכנית יכולתי — אבל זה DDL על מסד שמריץ webhook תשלומים חי, והסכמה
כוללת **טריגר על `auth.users`** שירוץ על כל הרשמה בפרויקט, כולל של igud-ads.
זו בדיוק ההחלטה שפתוחה אצלך מסבב 26/07, ולא משהו שמריצים בלילה על מסד סליקה.

**מה צריך ממך — אותה הכרעה אחת של שלב 4, בניסוח של 18:**
- **א. אישור** להריץ את `supabase/schema.sql` על `bieebmnm` כמות שהוא, או
- **ב. פרויקט נפרד** ל-18 (ואז 02 ו-18 חולקים זהות משלהם, מחוץ לסליקה), או
- **ג. סכמה ייעודית בהאב** `uhnrgujb` (`public.editor_*`), כמו שהוצע ל-17.
ברגע שתכריע — כל שלב 5 ("המסמכים שלי": גמורים / באמצע / נערכו + היסטוריה)
הוא עבודת אפליקציה רגילה, בלי חסם נוסף.

## שלב 6 (חצור, 16) — **לא חסום**, וזה הבא בתור
16 יושב על ההאב `uhnrgujbdxhhmoxcjria`, סכמה `chatzor`, שאליו יש לי גישה מלאה.
13 הטבלאות קיימות וכמעט ריקות (ארגון אחד, שירות קהילתי אחד; אפס בתי כנסת,
אפס זמני תפילה). **מה שיעזור ממך אם יש:** מקור אמת לרשימת בתי הכנסת וזמני
התפילה בחצור הגלילית (עלון/קבוצה/איש קשר), ומפתח Google Maps לחישוב מרחקים.
בלי מקור אמת לא אזין זמני תפילה — זה נתון שאסור לנחש.

## נושאים פתוחים מסבבים קודמים (עדיין דורשים אותך)
- **`git push` חסום** — אין טוקן תקף ב-shell. הקומיטים יושבים מקומית
  ב-`Downloads\nadlan-berega` וב-`Downloads\more30`.
- **service_role חסר** לפרויקטי Supabase שאינם בחשבון ה-MCP: `csjekrvu`,
  `jhbeelzv`, `trerolyv`, `mwljkonw`, `aypsq`, `hkkky`, `ygaqq`.
- **02 ו-03 (igud-ads)** — מערכות חיות עם סליקה. חיבור service_role לעותק
  מקביל דורש אישור מפורש שלך.
- **מפתחות ייעודיים חסרים:** RunPod (17), ADMIN_TOKEN (28), YEMOT + ELEVENLABS (27),
  GOOGLE OAuth (02), TRANSKRIBUS/KRAKEN (18 — מודול ה-HTR).
