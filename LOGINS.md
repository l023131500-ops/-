# LOGINS.md — פרטי הכניסה לכל המערכות

> נדרש ב-`more30-priority.md` §1ב. **עודכן: 12/08/2026.**
> כל שורה כאן נבדקה מול השרת האמיתי, או מסומנת במפורש כלא-נבדקה.
> אין כאן פרט מומצא. מקור האמת לערכים: טבלת `core.secrets`.

---

## 1. סופר-אדמין

| מה | ערך | מקור |
|---|---|---|
| חשבון-על | Google של **l023131500@gmail.com** | §1ב |
| שם משתמש לפאנלים | **admin** | `core.secrets.STD_ADMIN_USER` |
| סיסמה לפאנלים | **More30Admin2026** | `core.secrets.STD_ADMIN_PASSWORD` |

**איפה זה אומת בפועל:** קיוסק (35) בלבד — `POST https://more30.com/kiosk/api/auth/login`
עונה 200 עם `role: admin` (אומת 12/08, ומאותה ריצה הסיסמה מוחלת מחדש מ-SEED
בכל עלייה). בשאר הפאנלים הסיסמה **נקבעה כתקן ולא נבדקה מסך-מסך** — אל תניח
שהיא כבר מוזנת שם.

**חריגים ידועים (לא admin/More30Admin2026):**
- **גן-קליק (40)** — אין שם משתמש; הניהול מקבל סיסמה אחת בכותרת `x-admin-key`.
  הערך ב-`core.secrets` בשם `ADMIN_PASSWORD`, scope `gannenet` (24 תווים אקראיים,
  אינו בגיט).
- **קופות (28)** — `admin_auth = 'token'`: אין מסך כניסה, הגישה דרך טוקן ב-API.
- מערכות שב-`core.projects.admin_auth` רשומות `hub` (10, 15, 16, 32, 33) —
  הניהול הוא דרך הכניסה המרכזית של more30, לא דרך שם משתמש וסיסמה משלהן.

---

## 2. משתמש לקוח לבדיקה

| מה | ערך |
|---|---|
| אימייל | **test@more30.com** |
| סיסמה | **More30Test2026** |
| שם בפרופיל | לקוח בדיקה · טלפון 0500000000 |

מאוחסן ב-`core.secrets` (`TEST_CUSTOMER_EMAIL`, `TEST_CUSTOMER_PASSWORD`).

**זו תיבת דואר אמיתית.** `test@more30.com` מנותב ל-Resend, ומיילי האימות של
Supabase באמת נוחתים בה — כך אושרו החשבונות למטה בלי גישה ל-webmail. אפשר
לקרוא אותה שוב בכל ריצה (Resend → received emails).

### היכן הוא קיים ונכנס בפועל (נבדק 12/08/2026)

לכל פרויקט: הרשמה בנתיב הציבורי עם המפתח שהאתר עצמו שולח לדפדפן, אישור המייל,
ואז **התחברות חוזרת עם אותם פרטים** — כלומר גם הבדיקה של §1א ("הרשמה →
התנתקות → התחברות").

| פרויקט Supabase | מערכות | התחברות | user_id |
|---|---|---|---|
| `uhnrgujbdxhhmoxcjria` (המרכזי) | 04 עימוד, 16 חצור, 26 סטודיו, 28 קופות, 32 נדל"ן, 33 פורטל, 34 כסף, 36 תיווך | ✅ 200 | `78600fbc-3fd3-4f02-8552-e93ab6559649` |
| `bieebmnmkffwbqlsfozh` | 01 תורה, 02 תמלול, 03 מודעות, 10 בקלות, 18 עורך | ✅ 200 | `3c03f8db-7ef8-4f2c-b8b5-5dd98ead32e3` |
| `csjekrvukbdznetsrodj` | 06 בריאות, 12 סמל, 17 חיזוקים, 27 מחירון | ✅ 200 | `edf18dd4-1d7a-4799-b44a-1267988bf16b` |
| `hkkkynyoigzlttpynoeo` | 15 איגוד | ✅ 200 | `c4857340-9e93-45b7-8e30-388c24cf05b7` |
| `mwljkonwdeuaahsigjdp` | 24 גליל | ✅ 200 | `03dd3f7d-63e0-4efc-af40-9610aa4407fe` |
| `jhbeelzvjvhnkxldqvxx` | 30 CRM | ✅ 200 | `85332816-710e-49d3-b3a1-a91aab4f7882` |
| `ygaqqnuyfnumezxxmtbh` | 31 גשר | ✅ 200 | `1458bb7c-0dea-44b4-9007-ffc3e82b430f` |
| `aypsqqvfohekxxuqsmrw` | 21 Mthbram | ✅ 200 | `6b0907f4-acae-42d6-8d79-3b07c49f9976` |
| `trerolyveytzgksawrme` | 22 זכויות | ❌ **חסום** — המשתמש קיים אך לא מאושר | — |

**22 (זכויות) הוא היחיד שנשאר סגור.** ההרשמה שם החזירה `user_already_exists`,
כלומר החשבון נוצר בעבר ומעולם לא אושר, ולכן אין לו מייל אישור חדש לפתוח.
`POST /auth/v1/resend` החזיר 200 אבל המייל טרם הגיע לתיבה. הצעד הבא: לקרוא
שוב את התיבה ולפתוח את הקישור, ואם לא הגיע — לאשר מהקונסולה של אותו פרויקט.

**08 (בקלות זכאות) לא נגענו בו בכוונה** — מוגן.

### חברות במערכות (הרשאת לקוח)

הכפתור המשותף `more30.com/auth-button.js` רושם חברות דרך `more30_join_app`.
הופעל למשתמש הבדיקה על **26 המערכות** ונוצרה שורת `member` בכל אחת:
`more30, torah, tamlul, modaot, imud, briut, bkalot, smel, smachot, egod,
chatzor, chizukim, orech, mthbram, zchuyot, galil, studio, mechiron, kupot,
crm, gesher, nadlan, kesef, kiosk, tivuch, gannenet`.
המסלול בכולן **free** — זה הנכון: החיוב במצב טסט ואין מנוי בתשלום למשתמש הזה.

---

## 3. איך מאמתים מחדש

`QA/platform/test-customer-0812/` מחזיק את הסקריפטים שהפיקו את הטבלאות למעלה:
- `qa-signup-projects.ps1` — יוצר/מוודא את הלקוח בכל פרויקט (idempotent).
- `qa-confirm.ps1` — פותח את קישורי האישור מהתיבה (הקישורים פגים; רענן מהדואר).
- `qa-join.ps1` — מריץ את `more30_join_app` על כל המערכות.
