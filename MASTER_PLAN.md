# MASTER_PLAN — תור העבודה של more30 (הטרמינל עובד לפי זה, שלב-אחר-שלב)

## חוקי המנוע (חובה — קרא לפני שמתחילים)
1. עבוד על **שלב אחד בכל פעם, לפי הסדר** למטה.
2. לכל שלב: בצע במלואו → **אמת** (Playwright + נתוני אמת + כתובות בדיקה) → `git commit` → סמן את השלב DONE ב-NIGHT_PROGRESS.md → **רק אז** עבור לשלב הבא.
3. **לעולם אל תפגע באיכות/דיוק.** עדיף לעצור שלב מאשר לזייף נתון.
4. **אם שלב דורש את המשתמש** (מפתח, החלטת עיצוב, פרטי ספק) — רשום ב-`NEEDS_USER.md` מה בדיוק חסר, **דלג לשלב הבא שאפשר לבצע לבד**, וחזור אליו כשהמשתמש יספק.
5. נתוני אמת בלבד. אל תיגע במוגן (bkalut-app, bkalot-admin, zr_*, NEDARIM3873).
6. אחרי כל עצירה (NetFree) — המשך מאותה נקודה לפי NIGHT_PROGRESS.md, בלי לשאול.
7. כתובות בדיקה קבועות: **הדקל 22, חצור הגלילית** · **הבעל שם טוב 9, רחובות**.

---

## הסדר (בנוי לפי תלויות — אל תשנה סדר)

### שלב 1 — נדל"ן מגורים: דיוק ותיקונים  (פירוט מלא: NADLAN_SPEC.md)
- עסקאות שנמכרו: ברירת מחדל 3 שנים אחרונות (חדש→ישן) + "הצג שנים קודמות"; כולן אמת מ-nadlan.gov.il; גוש/חלקה ↔ רחוב+מספר.
- יד2 residential + retry; הורדות (מצגת+PDF) עובדות; תמונת בניין+מפה+מוסדות+תחבורה+מגזרים — כולם מהמקור הנכון.
- **DoD:** שתי כתובות הבדיקה מפיקות דו"ח מלא, מדויק, אמיתי (מאומת ב-Playwright).

### שלב 2 — סיום קונסולידציה: כל המערכות ל-hub שלי
- שכפל cold-copy את הנותרים: csjekrvu (06,12,17,27), jhbeelzv (30), aypsqqv (21). גיבוי לפני כל אחד.
- cutover בזהירות: לכל סכמה — הוסף ל-Exposed schemas → health-check ל-hub API → אם נפל, rollback מיד. הפנה env, פרוס, אמת מקצה-לקצה. עדכן core.projects. אל תמחק מקור זר.
- **DoD:** כל המסדים הזרים מומרו ואומתו, hub API יציב.

### שלב 3 — נדל"ן הרחבה: שכירות / מסחרי / קרקעות + היתרים ומסמכים  (פירוט: NADLAN_EXPANSION_SPEC.md)
- 4 כפתורי כניסה: מגורים(קיים)/שכירות/מסחרי/קרקעות. מנוע היתרים+מסמכים: MAVAT (תב"ע/מתאר PDF) + iplan XPLAN + GovMap + רמ"י (הפשרה).
- **DoD:** 3 הכפתורים החדשים מפיקים דו"ח אמת עם מקור לכל נתון.

### שלב 4 — תמלול (02/17): איכות אמת + התחברות
- נתוני אמת מלאים; התחברות משתמש; אזור אישי + היסטוריה; **חיפוש בארכיון התמלולים**; תשתית מנוי (בלי לחבר סליקה עדיין).
- **DoD:** משתמש נכנס, מתמלל, מחפש בארכיון, רואה היסטוריה.

### שלב 5 — עורך/המרת כתב יד (18): התחברות + עבודות שמורות
- כניסה: **משתמש + ניהול**. "המסמכים שלי": גמורים / באמצע עבודה / נערכו + היסטוריה. כל הפונקציות מאחורי התחברות.
- **DoD:** משתמש נכנס ורואה רק את המסמכים שלו, כולל טיוטות.

### שלב 6 — חצור (16, המערכת המאוחדת "מחוברים")
- הזן תוכן ראשוני (בתי כנסת/זמנים) + צור משתמש ניהול ראשון + חבר Google Maps (מרחקים) + הצג בדף הבית.
- **DoD:** חצור חיה עם תוכן אמת ומופיעה בדף הבית.

### שלב 7 — תצוגת דף הבית
- הצג את כל המערכות האיכותיות (public_visible), הסתר לא-מוכנות. ודא שהרשימה דינמית מ-core.projects, ושכל מערכת מוצגת נטענת נכון.
- **DoD:** דף הבית מציג נכון את כל הטובות, כולל 01 ו-16.

### שלב 8 — עיצוב (DESIGN_STANDARD.md) — מעבר ראשון
- החל את התקן על המערכות (טוקנים, טיפוגרפיה, RTL/כהה/מובייל, ביצועים, SEO, נגישות).
- **דורש משתמש:** איכות ויזואלית טעונה אישור. אחרי מעבר ראשון — רשום ב-NEEDS_USER.md "מוכן לביקורת עיצוב לכל מערכת".

### שלב 9 — פרסום/מיתוג  (פירוט: more30_ad_studio_master_spec)
- בנה את התשתית (2 מצבים, תבניות, אורקסטרציית AI, מחלקת מיתוג).
- **דורש משתמש:** בחירת סגנון/אישור עיצוב + אולי מפתחות Ideogram/Flux → רשום ב-NEEDS_USER.md.

### שלב 10 — סליקה (נדרים פלוס)
- הכן תשתית סליקה + מחירון ל-02 (הכי קרוב לכסף). 
- **דורש משתמש:** פרטי מוסד נדרים → רשום ב-NEEDS_USER.md; אל תבצע חיוב אמיתי בלי אישור.

---

## דיווח
- עדכן NIGHT_PROGRESS.md אחרי כל שלב (DONE + מה אומת).
- נהל NEEDS_USER.md: רשימה מרוכזת של כל מה שדורש את המשתמש (מפתח/החלטה/אישור), עם שם השלב.
- בסוף כל סבב — דו"ח קצר: מה הושלם, מה ב-NEEDS_USER, מה הבא בתור.
