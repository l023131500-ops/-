# DESIGN_STANDARD.md — סטנדרט העיצוב והחוויה של more30

> `MORE30_MASTER_BUILD.md` §2.5 מפנה למסמך הזה כתנאי ל"מוכן", אבל המסמך עצמו
> מעולם לא נכתב — ולכן סעיף העיצוב לא היה ניתן למדידה, ואף מערכת לא יכלה לעבור
> אותו באמת. זהו המסמך. **כל סעיף כאן נמדד אוטומטית** על ידי
> `scripts/qa/platform-audit.mjs` ו-`scripts/qa/lighthouse-run.mjs`, כדי שלא
> תהיה כאן ולו דרישה אחת שאי אפשר להוכיח שהתקיימה.
>
> נכתב 02/08/2026. מתעד את שפת העיצוב **הקיימת בפועל** בפורטל (`portal/src/styles.css`)
> ומרחיב אותה לכלל 33 המערכות.

---

## 0 · העיקרון

מערכת אחת, 33 חדרים. הלקוח עובר בין `/nadlan` ל-`/chizukim` באותו דומיין ובאותה
לשונית — ולכן כל מעבר שנראה כמו אתר אחר הוא באג, לא סגנון. שלושת הדברים שחייבים
להיות זהים בכל מערכת: **הנווט העליון עם כפתור הכניסה** · **הטיפוגרפיה** ·
**התנהגות RTL ומובייל**. כל השאר יכול להשתנות לפי אופי המערכת.

---

## 1 · טיפוגרפיה — ארבעה פונטים, לכל אחד תפקיד אחד

| פונט | תפקיד | איפה |
|---|---|---|
| **Suez One** | כותרת ראשית ענקית · כותרות מקטע | `h1`, `h2` של מקטעים |
| **Secular One** | שמות מערכות · מספרים גדולים | כרטיסי מערכת, לוחות נתונים |
| **Frank Ruhl Libre** (300) | משפטי הטבה, פסקאות שנועדו להיקרא | תיאורי מערכת, קופי |
| **Heebo** | ממשק: כפתורים, שדות, טבלאות, ניווט | כל השאר |

**כללים:**
- גודל גוף מינימלי **16px**. פחות מזה נפסל — עברית מנוקדת או צפופה בגודל 14px
  אינה קריאה במובייל.
- כותרת ראשית: `clamp()` ולא גודל קבוע, כדי שלא תישבר במסך צר.
- `line-height` לפסקה עברית: **1.7–1.85**. עברית ללא אותיות עולות ויורדות זקוקה
  לאוויר שאנגלית לא זקוקה לו.
- **אף עמוד לא נטען בלי פונט חלופי מוגדר** (`system-ui, sans-serif`) — כשגוגל
  פונטס נחסם (וזה קורה כאן), העמוד חייב להישאר קריא.

---

## 2 · צבע — ה-jewel-tone של הפלטפורמה

הבסיס נלקח מ-`portal/src/styles.css` והוא **מקור האמת**:

```
--royal:   #0B0D2E   בסיס, כחול מלכותי עמוק
--royal-2: #12153F   משטח מוגבה
--royal-3: #1A1E55   משטח פעיל / ריחוף
--violet:  #241B54   מקטע משני
--gold:    #C9A227   הדגשה, קווי הפרדה, כפתור ראשי
--emerald: #14B88A   סימון "חי" / הצלחה
--ink:     #ECE9F5   טקסט ראשי
--ink-2:   #B9B6D6   טקסט משני
--muted:   #8582AC   טקסט שלישוני
```

**כללים:**
- ניגודיות: טקסט גוף ≥ **4.5:1**, טקסט גדול (≥24px) ≥ **3:1**. `--muted` על
  `--royal` עומד בזה; `--muted` על `--royal-3` **לא** — שם משתמשים ב-`--ink-2`.
- אין גרדיאנטים דקורטיביים ואין צללים כבדים. ההיררכיה נבנית מצבע, מגודל וממרווח.
- אין אייקוני AI, אין אמוג'ים בממשק. אמוג'י בכפתור הוא סימן שהקופי לא נכתב.
- **צבע לעולם אינו הסימן היחיד.** מצב "חי" מקבל גם מילה, לא רק נקודה ירוקה.

---

## 3 · מצב כהה — מה הוא אומר כאן

הפלטפורמה **כהה מלכתחילה**. לכן הדרישה אינה "להוסיף מצב כהה" אלא:

1. עמוד שמוגש בהיר (מערכות שיובאו עם ערכת נושא בהירה) **חייב** להגיב ל-
   `@media (prefers-color-scheme: dark)` — אחרת המשתמש שעובר מ-`/` הכהה
   אל `/kupot` הבהיר מקבל הבזק לבן בפנים באמצע הלילה.
2. **השאלה היחידה שנשאלת היא "האם ייתכן הבזק לבן", ולא "האם יש שתי ערכות".**
   עמוד עובר אם מתקיים אחד משניים:
   - **מגיב** — המראה משתנה בין `colorScheme: 'light'` ל-`'dark'`, או
   - **כהה מלכתחילה** — הרקע האפקטיבי כהה כבר במצב הבהיר (בהירות sRGB
     יחסית < 0.06) **וגם** מוצהר `color-scheme: dark`, כדי שגם פסי הגלילה
     ושדות הטופס יירנדרו כהים ולא ילבינו באמצע עמוד כהה.
3. **איך נמדד הרקע האפקטיבי.** לא `backgroundColor` של `body` בלבד: דף שצובע
   את רקעו בגרדיאנט (למשל `/login`) מחזיר שם `rgba(0,0,0,0)` בשני המצבים,
   וההשוואה הייתה יוצאת "זהה" ופוסלת דף עם מצב כהה תקין. הנמדד הוא הצבע האטום
   הראשון בשרשרת `body → html`, יחד עם `background-image` ועם צבע הטקסט.
4. מותר לוותר על מצב כהה רק במערכת שכל תוכנה הוא מסמך סרוק/תמונה. במקרה כזה
   נרשם ב-QA **למה**.

---

## 4 · RTL — לא רק `dir="rtl"`

- `<html lang="he" dir="rtl">` בכל עמוד. נמדד.
- מספרים, כתובות דוא"ל וקוד נשארים LTR בתוך משפט עברי (`unicode-bidi: isolate`).
  משפט שמערבב "₪ 1,234" ועברית בלי בידוד יציג את הסימן בצד הלא נכון.
- אייקון כיווני (חץ "הבא", חץ "חזור") מתהפך ב-RTL. אייקון לא-כיווני (סגירה,
  חיפוש) **לא** מתהפך.
- `margin-inline-start/end` במקום `left/right`. כל `padding-left` בקוד חדש הוא באג.
- טבלה עברית מיושרת לימין; עמודת מספרים מיושרת לימין עם `font-variant-numeric: tabular-nums`.

---

## 5 · מובייל

- נקודת המדידה הרשמית: **390×844** (iPhone 14/15). כל צילום QA כולל אותה.
- **אפס גלישה אופקית.** נמדד: `documentElement.scrollWidth > clientWidth` = כישלון.
- `<meta name="viewport" content="width=device-width, initial-scale=1">` —
  **בלי `maximum-scale` ובלי `user-scalable=no`**. חסימת זום היא כשל נגישות,
  ובארכיון טקסט עברי היא בדיוק היכולת שהמשתמש המתקשה זקוק לה.
- הנווט העליון וכפתור הכניסה נשארים נגישים במובייל — לא נגללים אל מחוץ למסך.

---

## 6 · נגישות — תקן ישראלי 5568 (WCAG 2.1 AA)

| דרישה | הסף | איך נמדד |
|---|---|---|
| יעד מגע | **≥ 24×24px** לכל אלמנט אינטראקטיבי גלוי | `getBoundingClientRect` על כל `a[href], button, input, select, textarea, [role=button], summary` |
| שם נגיש | לכל פקד יש שם | `aria-label` / `<label for>` / עטיפת `<label>` / טקסט. **`placeholder` אינו תווית** |
| `alt` לתמונה | לכל `<img>` יש `alt` (ריק לתמונה דקורטיבית) | חסר `alt` לגמרי = כישלון |
| כותרת ראשית | `h1` **אחת בדיוק** בעמוד, ובעלת טקסט | ספירת `h1` |
| מקלדת | כל פעולה זמינה במקלדת, `:focus-visible` נראה | ידני ב-QA |
| תנועה | `@media (prefers-reduced-motion: reduce)` מבטל אנימציות | קיים ב-`styles.css` |
| שפה | `lang="he"` | נמדד |

**יעד: 0 כשלים בכל שורה.** לא "מעט". הסף הזה כבר הושג ב-32 נדל"ן וב-22 זכויות,
ולכן הוא מוכח כבר-השגה ולא תיאורטי.

---

## 7 · ביצועים — Lighthouse ≥ 90

נמדד בפרופיל **מובייל** (המחמיר), על הפרודקשן, דרך `scripts/qa/lighthouse-run.mjs`.

| קטגוריה | סף |
|---|---|
| Performance | ≥ 90 |
| Accessibility | ≥ 90 |
| Best Practices | ≥ 90 |
| SEO | ≥ 90 |

**חריג מוצהר אחד:** מערכת שהפעולה המרכזית שלה היא חישוב ארוך (דוח נדל"ן,
תמלול) תימדד על **עמוד הנחיתה** שלה. עמוד תוצאה שממתין ל-API חיצוני 40 שניות
אינו מדיד ב-Lighthouse, וניסיון "לשפר" אותו יפגע במערכת. הדבר יירשם ב-QA במפורש.

**עמוד שנושא `noindex` אינו נמדד ב-SEO.** `/login`, `/me` ו-`/subscribe` חסומים
לאינדוקס בכוונה — עמוד כניסה ואזור אישי אינם אמורים להופיע בגוגל. Lighthouse
מוריד את ציון ה-SEO ל-54 בדיוק בגלל זה, ולכן בעמודים האלה נמדדים שלושת
הקטגוריות האחרות בלבד. **זה חריג מוצהר, לא ציון שנכשל.**

---

## 8 · SEO ומטא

לכל עמוד ציבורי: `<title>` ייחודי בעברית · `meta description` (120–160 תווים) ·
`canonical` שמצביע ל-**`more30.com/<נתיב>`** · `og:url` תואם.

🚨 **`canonical` או `og:url` שמצביעים ל-`*.vercel.app` או ל-`*.lovable.app` הם באג**
— הם מפנים את גוגל לכתובת שנטפרי חוסמת, ומדליפים את הארכיטקטורה הפנימית.
נמדד: כל קישור בעמוד שמכיל `vercel.app` נרשם ככישלון.

---

## 9 · הנווט וכפתור הכניסה — הרכיב המשותף היחיד

`portal/public/auth-button.js` נטען בכל מערכת ומזריק את כפתור הכניסה בנווט
**העליון**. הוא יודע מי מחובר, מציג שם משתמש ו"יציאה", ומודע להרשאות
(סופר-אדמין רואה כניסת ניהול). **מערכת בלי שורת ה-`<script>` הזו אינה עומדת
בסטנדרט** — גם אם היא יפה.

---

## 10 · קופי

עברית תקנית, בלי שגיאות כתיב, בלי אנגלית מיותרת. משפט אחד שמסביר מה המערכת
עושה — לפני כל מה שהיא יודעת לעשות. **אין נתון → "לא זמין" + משפט קצר למה**,
לעולם לא שדה ריק ולעולם לא ניחוש. מספרים מוצגים עם מפריד אלפים ועם המקור והתאריך.

---

## 11 · צ'קליסט לסגירת מערכת

```
[ ] lang=he · dir=rtl
[ ] h1 אחת · title ייחודי · meta description
[ ] canonical + og:url על more30.com — אפס אזכורי vercel.app
[ ] 0 יעדי מגע < 24px (דסקטופ ומובייל)
[ ] 0 פקדים בלי שם נגיש
[ ] 0 תמונות בלי alt
[ ] 0 גלישה אופקית ב-390px
[ ] 0 שגיאות קונסולה
[ ] מצב כהה מגיב (או נימוק כתוב ב-QA)
[ ] viewport בלי maximum-scale
[ ] כפתור הכניסה האחיד בנווט העליון
[ ] Lighthouse: perf/a11y/bp/seo ≥ 90
[ ] צילומי מסך: דסקטופ + מובייל + כהה
[ ] QA/<שם>.md נכתב, כולל סעיף המלצות
```

---

*מקור המדידה: `QA/platform/_results.json` ו-`QA/platform/_lighthouse.json`.*
