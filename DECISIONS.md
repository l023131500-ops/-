# DECISIONS — graphics/design-system build (branch `feat/graphics-upgrade`)

תיעוד ההחלטות שהתקבלו אוטונומית בבניית מערכת העיצוב, לפי `DESIGN_SYSTEM_BUILD_v2.md`.
כל שורה: מה הוחלט + למה, כדי שסבב הבא (אנושי או אוטונומי) לא יצטרך לשחזר את ההיגיון.

---

## 19/08/2026 — סבב 1

1. **"המנוע הקיים" = `apps/26-modaot-studio`.** אומת מול הקוד: קנבס שכבתי חי
   (React-Konva), טקסט עברי אמיתי בשכבת קוד עם autofit, עיטורי SVG וקטוריים,
   ייצוא PNG מלא-רזולוציה ו-PDF (jsPDF), וספר-מותג שנבנה כ-HTML ומיוצא ל-PDF
   דרך חלון ההדפסה של הדפדפן (עברית/RTL/גופנים מוטבעים מלאים). חי כבר היום
   ב-`more30.com/studio` (פרויקט Vercel נפרד `studio-more30`). **הוחלט: לא
   נוגעים בו כלל בסבב הזה** — אפס רגרסיה פירושו גם אפס שינוי בקוד שלו, לא רק
   "לא למחוק פיצ'ר".

2. **`more30.com/design` נבנה כמוצר-מדף נפרד, לא כשכפול של `/studio`.** לפי
   §0 של המפרט, השילוב הוא "מוסיפים 4 שכבות מעל" — לא בנייה מחדש. `/design`
   הוא ה"חלון-ראווה" המשותף (Brand Kit + טיפוגרפיה + מילון פרמטרים) שמצביע
   אל המנוע הקיים, ולא מכיל את לוגיקת העריכה בעצמה. זה נמנע מכפילות קוד בין
   שני פרויקטי Vercel נפרדים (שאין ביניהם build-time import משותף — כל אפליקציה
   נושאת `node_modules` עצמאי) ומאפס את סיכון הרגרסיה על `/studio` החי.

3. **פריסה: פרויקט Vercel חדש `design-more30`**, בדיוק לפי התבנית של
   `studio-more30` (`buildCommand: echo no-build`, `outputDirectory: public`,
   `rewrites` ל-`/design/index.html`). נוצר דרך ה-REST API של Vercel
   (`POST /v10/projects`, `projectId: prj_UBm3Kgf61lAhUYAMRwNRhyvJRiDM`) כי אין
   TTY לקישור אינטראקטיבי. מופה בפורטל (`portal/vercel.dist.json`) עם 3
   כללי rewrite חדשים בלבד, מיד אחרי כללי `/studio` הקיימים — שום כלל קיים
   לא נערך.

4. **תוכן הסבב הראשון: עמוד Brand Kit חי, לא רקעים שנוצרו ב-AI.** "רקעים
   מושלמים" הושג עם רקע SVG/גרדיאנט מעוצב ביד בפלטת ה-jewel-tone — לא קריאה
   ל-Recraft/Gemini. שכבת ה-AI-נכסים (§3 במפרט) היא סעיף שלב 2 נפרד ברשימת
   הבדיקה, כי היא דורשת אינטגרציית שרת (Recraft/Gemini) שלא קיימת עדיין
   ב-`/design` ואינה חלק מ"קיים כבר" לפי §6 שלב 1 (מנוע+Brand-Kit+מילון+פונטים).

5. **טוקנים (צבע/פונט) הועתקו ידנית מ-`portal/src/styles.css` ל-`apps/41-design-system/style.css`,
   לא נטענו דינמית.** אין build step משותף בין `portal` ל-`/design` (שני
   פרויקטי Vercel נפרדים ללא bundler משותף), ולכן טעינה דינמית הייתה דורשת
   קריאת CORS/API בזמן ריצה — עלות/שבירות מיותרת לעמוד סטטי. תועד ב-README
   של האפליקציה שהמקור-אמת הוא `portal/src/styles.css` ושצריך לעדכן ידנית
   אם הוא משתנה.

6. **`.gitignore`: נוספה חריגה `!/apps/41-design-system/**`**, זהה בדיוק
   לחריגה הקיימת של `37-bkalot-clone` — קוד שנכתב כאן (לא vendored, לא
   מכיל סודות), ולכן אינו כפוף לכלל ש"קוד אפליקציות לא נשמר ב-git ציבורי".
   `_deploy/design-more30/` **לא** נוסף ל-git, בדיוק כמו כל שאר `_deploy/*` —
   הוא תוצר build הניתן לשחזור מ-`apps/41-design-system/` (ראה README שם).

7. **מספר האפליקציה: 41.** המספרים 01–40 (חוץ מ-33/36/38/39 שלא נבדקו
   וחוץ מ-`_archive`) תפוסים; 41 חופשי.

---

## 19/08/2026 — סבב 2

8. **פריט 2 (כפתור כניסה + פוטר) אומת ולא נבנה מחדש.** `/design` כבר נשא
   `auth-button.js` ופוטר "חלק מ-more30" מסבב 1 — הפער היחיד היה שהמסלול לא
   היה רשום ב-`ROUTES` של `scripts/qa/authbutton-overlap.mjs`, כך שמעולם לא
   נמדד רשמית. נוסף שם, הורץ על `design` ו-`studio` יחד (לא כל 26 המסלולים —
   חיסכון זמן; שאר המסלולים לא נגעו בהם ולכן לא בסיכון) ושניהם יצאו נקיים
   בכל 5 הרוחבים.

## 19/08/2026 — סבב 3

9. **פריט 3 (ספריית פונטים) התברר כפער קטן בהרבה מהתיאור.** לפני נגיעה
   בעורך החי (`apps/26-modaot-studio`, פרויקט תפעולי אמיתי), הופעל Explore
   agent read-only שמיפה את 3 "אסטרטגיות הטעינה" הנטענות בסעיף. תוצאה:
   `shared/styles.ts` FONTS כבר מכיל 16 פונטים — סופרסט של 12 הפונטים ב-
   `apps/04-imud-torani/pagedRender.ts`, פרט לפונט אחד: Cardo. הפער האמיתי
   היחיד היה פונט בודד חסר, לא איחוד 3 מערכות.

10. **`server/fonts/*.ttf` (6 קבצים) אינם "אסטרטגיית טעינה"** — נסרק כל
    `apps/26-modaot-studio` (`.ts/.tsx/.js/.json`) אחר שם-הקובץ/`registerFont`/
    `fontkit`/`embedFont` — אפס התאמות. אין שרת PDF/פונטים בכלל (`server/routes.ts`
    ריק מקוד כזה). נשארו כפי שהם — לא נמחקו (אפס-רגרסיה גם על קבצים לא-בשימוש,
    אין ודאות שהם לא מיועדים למשהו עתידי) ולא "אוחדו" כי אין מה לאחד.

11. **מסלול תצוגה-מקדימה וייצוא הם נתיב-קוד אחד, לא שניים.** אומת לפני העריכה:
    `CanvasStage.tsx` (React-Konva) וה-PDF/PNG export ב-`exporter.ts` שניהם
    קוראים ל-`stage.toDataURL()` על אותו stage חי — אין סיכון שפונט ייראה
    נכון בתצוגה ויישבר בייצוא (או להפך). זה איפשר להוסיף פונט בביטחון בלי
    לבדוק שני נתיבי-רינדור נפרדים.

## 19/08/2026 — סבב 4

12. **פריט 4 פוצל ל-4a/4b — התיוג והבקרות-החדשות הם שני פיצ'רים שונים,
    לא אחד.** קריאת הקוד (grep על `Editor.tsx` + כל `client/src`) הראתה
    שמתוך 10 המונחים ברשימת הסעיף המקורי, רק 4 (`fill`/`stroke`/`shadow`/`blur`)
    קיימים כבקרה בפועל בפאנל השכבות; 6 (`opacity`/`blend`/`tracking`/`leading`/
    `kerning`/`corner-radius`) אינם קיימים כלל — לא ב-Layer schema, לא כ-state,
    לא ברינדור. תיוג-תוויות (4a) הוא שינוי טקסט בלבד, סיכון אפסי; הוספת 6
    הבקרות החדשות (4b) דורשת שדות סכימה חדשים + חיווט ברינדור Konva + בייצוא
    PDF/PNG — נגיעה ישירה במנוע המשותף שהוחלט בסבב 1 (החלטה #1) לא לגעת בו
    בלי בדיקה זהירה. בוצע 4a בלבד הסבב הזה; 4b נשאר פריט נפרד ברשימה.

13. **התג הטכני מוצג כ-`<code>` קטן ליד התווית הקיימת, לא מחליף אותה.**
    נבחר כדי לשמר את הטקסט העברי המוכר לעיצוב (אפס רגרסיה על חוויית
    העריכה הקיימת) ובו-זמנית לתת לבוט/לסוכן עתידי עוגן טקסטואלי יציב
    (`fill`/`stroke`/`shadow`/`blur`) שלא תלוי בניסוח העברי.

## 19/08/2026 — סבב 5

14. **פריט 5: הפער האמיתי היה בחשיפה, לא בנתונים.** `shared/knowledge.ts`
    ו-`shared/categoryTemplates.ts` כבר החזיקו קטלוג-תבניות מלא (מנוע
    `composeTemplate`) לכל 4 הקבוצות מהסבב הראשון של בניית האפליקציה —
    אבל `shared/templates.ts` (`TEMPLATE_DEFS`, מה שבאמת מוצג בגלריה
    דרך `/api/templates`) ו-`Home.tsx` (`enabled = g.key === "shiurim"`)
    חשפו רק שיעורי-תורה. נבחרה הקטגוריה הראשונה/המובילה בכל קבוצה
    (`wedding_chasidic`, `rosh_hashana`, `hachnasat_sefer_torah`) ונוספה
    דרך `composeTemplate` הקיים — לא נבנה מנוע-רינדור חדש.

15. **גילוי: `more30.com/studio/api/*` רץ כפונקציית Vercel נפרדת מקוד-מקור,
    לא מהשרת המקומי.** `apps/26-modaot-studio/server/` (Express + SQLite)
    הוא לפיתוח מקומי בלבד; הפריסה החיה היא `api/index.ts` (Vercel Function
    בודדת, TS מקור לא-מקומפל, Vercel מזהה `api/**` אוטומטית גם עם
    `buildCommand: echo no-build`) שמשתמשת ב-Supabase (`studio_*` טבלאות,
    `SUPABASE_SERVICE_KEY`) במקום SQLite מקומי. קוד ה-adapter הזה **לא
    היה קיים בגיט בכלל** — כל סבב קודם בנה/פרס רק את הלקוח הסטטי
    (`dist/public` → `_deploy/studio-more30/public/studio`), ומעולם לא
    נגע ב-API. זה עבד עד עכשיו כי אף שינוי קודם (Cardo font, תיוג
    פרמטרים) לא נגע בנתונים שה-API מגיש. שוחזר קוד ה-adapter מה-deployment
    החי (`GET /v6/deployments/{id}/files` + `GET /v7/.../files/{fileId}`
    של Vercel API) והושם תחת `apps/26-modaot-studio/vercel-adapter/`
    (נוסף לגיט — נדחף בכוח כי `/apps/**` מתעלם כברירת מחדל, בדיוק כמו
    שאר הקבצים הכבר-עוקבים של האפליקציה הזו). **בלי זה, כל שינוי עתידי
    ב-`shared/*.ts` שמשפיע על תוכן ה-API היה נראה "פרוס" (הלקוח מתעדכן)
    בזמן שה-API ממשיך להגיש את הגרסה הישנה** — פער שקט שקשה לאתר בבדיקת
    צילום-מסך רגילה.

16. **`seedIfEmpty()` ב-`api/index.ts` היה חד-פעמי (`if (existing.some(builtin))
    return`) — לא reseed בכל עלייה כמו `server/seed.ts` המקורי.** המשמעות:
    3 התבניות החדשות שנוספו ל-`TEMPLATE_DEFS` לא היו מופיעות ב-API החי
    לעולם, גם עם קוד מעודכן, כי הטבלה כבר לא ריקה (4 שורות builtin קיימות
    משכבר). תוקן לבדיקת **ספירה** (`builtinCount === TEMPLATE_DEFS.length`)
    במקום בדיקת "יש בכלל" — מרענן כשהקטלוג משתנה, לא מרענן שווא בכל
    cold-start כשהוא לא השתנה (שומר על הכוונה המקורית של "lazy, לא
    reseed מיותר"). אומת: `/api/templates` עבר מ-4 ל-7 מיד אחרי הפריסה.

17. **מה הועתק ל-adapter ומה לא.** מתוך קבצי ה-`_lib/shared/*` שהורדו,
    רוב הקבצים (`knowledge.ts`, `categoryTemplates.ts`, `variationEngine.ts`,
    `designTokens.ts`, `layers.ts`, `presetCatalog.ts`, `branding.ts`,
    `brief.ts`) זהים ל-byte לגרסת ה-repo — לא הוחלפו כי אין מה להחליף.
    **`server/routes.ts` ו-`shared/schema.ts` ב-adapter שונים מה-repo**
    (קוד אימות/`getUserIdFromToken` נוסף שלא קיים בעותק המקומי) — **לא
    נגעו בהם בכלל**, כדי לא לאבד פיצ'ר חי. הוחלפו רק שני קבצים: `templates.ts`
    (התוספת של הסבב הזה) ו-`styles.ts` (סנכרון Cardo מסבב 3 שמעולם לא הגיע
    ל-API — תוספת טהורה, סיכון אפס, לא משפיע על רינדור כי FONTS אינו
    בשימוש שרתי).

## 19/08/2026 — סבב 6

18. **פריט 7 פוצל ל-7a/7b — אותו דפוס כמו פריט 4/4a/4b.** קריאת הקוד
    (Explore agent) הראתה ש-`POST /api/branding/vectorize` (Recraft) כבר
    קיים ופרוס במלואו (server + vercel-adapter, מראה byte-identical) אבל
    נקרא רק מ-`BrandKitPage.tsx` (וקטוריזציה של לוגו). לעומת זאת "הסרת-רקע"
    אינה קיימת בכלל בשום שכבת קוד — אין endpoint, אין client call. אלה שני
    היקפי-עבודה שונים לגמרי: 7a (וקטוריזציה) היא חשיפת UI לקוד קיים —
    אפס שינוי server/API; 7b (הסרת-רקע) דורשת endpoint חדש (מראה מלא
    ל-vectorize: פונקציה+route+adapter). בוצע 7a בלבד הסבב הזה.

19. **הפאנל "שכבת תמונה" ב-Editor.tsx לא היה קיים כלל — לא רק חסר כפתור.**
    בבדיקת הקוד לפני העריכה נמצא ש-`selectedLayer.type === "image"` לא
    היה מטופל בשום מקום בפאנל המאפיינים (רק `"text"` היה מטופל) — בחירת
    שכבת תמונה לא הציגה שום בקרה. נוסף בלוק חדש, לא תוקן קיים — אפס סיכון
    לפאנל הטקסט הקיים.

## 19/08/2026 — סבב 7

20. **פריט 4b פוצל שוב, ל-4b-i..vi — אחת מ-6 הבקרות בכל סבב, אותו דפוס
    כמו 4/7.** קריאת קוד (Explore agent, read-only) לפני נגיעה במנוע
    הראתה ש-`opacity` שונה מהותית מ-5 המונחים האחרים ברשימת 4b: השדה
    כבר קיים ב-`BaseLayer` (`shared/layers.ts:23`) וכבר מחווט ברינדור
    Konva לטקסט/צורה/דקורציה (`CanvasStage.tsx`) — רק שכבת תמונה
    (`ImageNode`) לא קראה אותו, ובקרת UI לא הייתה קיימת בכלל. זה הפך אותו
    לתוספת בסיכון נמוך (2 שורות רינדור + סליידר אחד) בניגוד ל-5 הנותרים
    (`blend`/`tracking`/`leading`/`kerning`/`corner-radius`) שדורשים שדה
    סכימה חדש מאפס. בוצע `opacity` בלבד הסבב הזה.

21. **בקרת ה-opacity נבנתה כגנרית (מחוץ לענף `text`/`image`), לא כפולה
    בתוך `TextLayerControls` וגם בפאנל התמונה.** מכיוון ש-`opacity` הוא
    שדה של `BaseLayer` (משותף לכל סוגי השכבות), סליידר אחד מחובר ישירות
    ל-`handleChangeLayer(selectedLayer.id, {opacity})` מכסה טקסט/תמונה/
    צורה/דקורציה גם יחד — נמנע משכפול קוד בין `TextLayerControls` לפאנל
    "שכבת תמונה" (שגם ככה לא נשא עד היום אף בקרת סליידר).

22. **ה-exporter לא נגע כלל.** `exporter.ts` מייצא ישירות מה-stage החי
    (`stage.toDataURL()`), אז ברגע שה-Konva node קורא `layer.opacity`,
    השקיפות נאפית אוטומטית לפיקסלים גם ב-PNG וגם ב-PDF — אומת בקוד
    (לא רק בהנחה) לפני העריכה, כדי לא לגעת בנתיב ייצוא בלי צורך.

## 19/08/2026 — סבב 8

23. **פריט 13 (לולאת סוכנים) פוצל בפועל: נבנה שלב "מבקר QA" בלבד, לא הלולאה
    האוטומטית.** "מנהל-אמנותי" כבר קיים במובן מסוים — `generateConcepts`
    ב-`server/ai.ts` (system prompt: "אתה מנהל אמנותי...") כבר רץ בשלב
    הבריף הראשוני (`/api/brief/concepts`). "מבקר QA" **לא היה קיים בשום
    שכבה** — אין endpoint שבודק טיוטה *קיימת* (שכבות בפועל בעורך) מול
    קריטריוני עיצוב. נבחר להוסיף רק את זה: `critiqueDesign()` חדש
    ב-`server/ai.ts` (מראה מדויק ל-`generateStrategy`/`generateConcepts` —
    אותו `claude()` helper, אין קריאת AI חדשה), route `/api/ai/critique`,
    וכפתור "ביקורת AI" בעורך שמציג משוב לקריאה בלבד. **לא נבנה**: לולאה
    שמריצה מעצב→ביקורת→ליטוש אוטומטית בלי אדם באמצע — זו הרחבה גדולה
    משמעותית (state machine, איטרציות, קריטריון עצירה) שדורשת תכנון נפרד,
    ולא "הצעד המשמעותי הבא" בתקציב סבב יחיד. תועד בגלוי ב-CHECKLIST במקום
    לסמן [x] מטעה.

24. **תקציר השכבות ל-AI לא כולל `src` של תמונות (base64/data URL).**
    שדה `src` בשכבת תמונה יכול להיות עשרות/מאות KB (base64) — שליחתו
    ל-Claude הייתה מנפחת את הפרומפט ואת העלות בלי תועלת (הביקורת המבוקשת
    היא על קומפוזיציה/מיקום/טיפוגרפיה, לא זיהוי תוכן תמונה). `summarizeLayerForCritique`
    ב-`Editor.tsx` שולח לכל שכבת תמונה `hasImage: boolean` בלבד במקום `src`
    המלא — אותו עיקרון ריסון-payload כמו שאר קריאות ה-AI הטקסטואליות
    בקובץ (`generateCopy`/`generateConcepts` מקבלים שדות מובנים, לא HTML/DOM
    מלא).

## מה עוד לא הוחלט (ל-NEEDS_USER אם יעלה צורך)

- האם `/design` אמור בסופו של דבר **לבלוע** את `/studio` (מיזוג מלא של
  הסטודיו לתוך מוצר "מערכת עיצוב" אחד), או להישאר "חלון-ראווה" שמפנה
  לכלי נפרד לצמיתות. המפרט (§6) לא מכריע בעליל; ההנחה כרגע היא "נשאר נפרד,
  `/design` מצביע החוצה" כי זה האפס-רגרסיה הבטוח ביותר. אם ההנחה שגויה —
  לתקן ב-CHECKLIST/graphics.md ולא למחוק את `/studio`.

---

## 19/08/2026 — דף הבית: סדר "עובד קודם" + תיקון מלכודת RLS של חיזוקים

25. **דף הבית (`portal/src/App.tsx`) מסדר עכשיו קודם את מה שעובד.** ה-VIEW
    `more30_public_systems` כבר מסנן מוסתרים וממיין `live DESC, number`, אבל
    ה-App דרס את המיון עם `.order("number")` והציג בתוך כל תחום את הכרטיסים
    לפי מספר ההקמה — כך שמערכת "בקרוב" יכלה להופיע לפני מערכת פתוחה לכניסה,
    ותחום עם מערכת אחת יכל לפתוח את העמוד. שונה: (א) התחומים ממוינים לפי מספר
    המערכות הפתוחות לכניסה (torah 8 → community 4 → realestate 3 → …), (ב) בתוך
    כל תחום המערכות הפתוחות קודם ואז "בקרוב", שובר-שוויון לפי מספר. **אפס
    רגרסיה:** אף מערכת לא מוסתרת — כל 22 עדיין מוצגות, רק מסודרות (אומת
    בסימולציה מול נתוני ה-VIEW החיים: shown=22/22). שינוי תצוגה בלבד, בלי
    מגע במסד ובלי מגע במערכת העיצוב (26/41) שנבנית במקביל.

26. **מלכודת ה-RLS של חיזוקים (17) נוטרלה — היא הייתה שוברת את הפרודקשן.**
    `db/apps/17-chizukim-transcribe/0001_close_public_write_on_recordings.sql`
    טען ששלב 1 "בטוח להרצה לבד". נמדד ונמצא שקרי: `server/routes.ts:23`
    כותב עם `sb_publishable` (המפתח הציבורי, כפוף ל-RLS) ולא עם service_role,
    ולכן שלב 1 לבד היה חוסם גם את כתיבות השרת — יצירת הקלטה ושמירת תמלול היו
    נופלות לכל המשתמשים. נוסף לקובץ שער-בטיחות (`raise exception` אלא אם
    הוגדר `more30.chizukim_server_on_secret_key='yes'`), ותוקן BLOCKED.md §0.
    החוסם האמיתי כעת: אין טוקן Vercel ב-core.secrets לפריסת `chizukim2-more30`
    עם מפתח סודי לפני נעילת ה-RLS — נרשם ב-core.issues. **לא הורצה שום SQL על
    הפרודקשן** (אפס סיכון לנתונים החיים).

## 19/08/2026 — 02 תמלול: חשיפת schema `transcribe` ב-PostgREST (core.issues #239)

27. **הפעולה המרכזית של תמלול (02) הייתה שבורה לגמרי בייצור, וזו הייתה תקלת
    תצורה ולא באג קוד.** `POST /tamlul/api/coupons` (וכל נתיב שרת אחר שקורא
    ל-schema `transcribe`: uploads/jobs/admin) החזיר `500 {"error":"Invalid
    schema: transcribe"}`. הקוד תקין — `lib/supabase/server.ts` בונה client עם
    `db: { schema: "transcribe" }` והתיקון הקודם של ה-BOM (e4fa8956) אכן נפרס
    (שגיאת ה-ByteString נעלמה). הסיבה האמיתית: בפרויקט
    `bieebmnmkffwbqlsfozh` רשימת ה-Exposed Schemas של PostgREST הכילה רק
    `public,graphql_public` — בדיוק צעד ההתקנה שמתועד ב-`schema.sql:9-10` ולא
    בוצע. שמונה טבלאות ה-`transcribe` קיימות, אך PostgREST דחה את ה-schema.

28. **התיקון (אדיטיבי, אפס רגרסיה):** `db_schema` עודכן ל-
    `public,graphql_public,otvedaf,transcribe` — גם דרך ה-Management API
    (`PATCH /v1/projects/bieebmnmkffwbqlsfozh/postgrest`) לעמידוּת, וגם ישירות
    `ALTER ROLE authenticator SET pgrst.db_schemas=...` + `NOTIFY pgrst,'reload
    config'` כדי שה-instance החי יקלוט מיד. **⚠️ `otvedaf` הוא schema של מערכת
    אחרת על אותו פרויקט (2 טבלאות) — נשמר במפורש; PATCH ראשוני שהשמיט אותו תוקן
    מיד.** אימות בייצור: `POST /tamlul/api/coupons` עבר מ-`500 Invalid schema`
    ל-`404 "קופון לא קיים"` לקוד בדיקה שאינו קיים (הנתיב רץ מקצה-לקצה), ו-schema
    `otvedaf` נותר חשוף (401 ל-anon, לא 406) — בלי רגרסיה. הטוקן: `SUPABASE_ACCESS_TOKEN`
    מ-`core.secrets`. הערה: ה-MCP של Supabase כן מגיע ל-`bieebmnm` — הערת "❌ other
    account" ב-CONNECTIONS.md מיושנת.

## 19/08/2026 — 24 galil/chatzor: core.issues #240 בכל זאת נפתר, בלי צורך בגישת DB

29. **ה-NEEDS_USER מ-18/08 ש"חסם" את #240 היה מבוסס על `app.json` מיושן, לא על
    המצב האמיתי.** ההערה טענה ש-`apps/24-galilee-connect-hub` "אינו קיים
    במונו-רפו — ריפו נפרד" (וגם `app.json:16` עדיין מציין `"source":
    "not-vendored"`), אבל `git log` על `apps/24-galilee-connect-hub/src/` מראה
    שהמקור האמיתי כבר יובא ב-03/08 (`cb6e4f58`) ונערך בהצלחה ופרוס שוב ושוב
    מאז (לאחרונה `03fd7aaa`, 17/08) — אותו קוד בדיוק שרץ מאחורי more30.com/galil.
    שדה ה-`app.json` פשוט לא עודכן מעולם אחרי הייבוא — אותה משפחת "audit_gaps
    מיושן" שתוקנה בעבר במערכות אחרות (למשל c2bebe5c). ה**חסם השני** בהערה
    (גישה ל-`mwljkonwdeuaahsigjdp` דרך ה-Supabase MCP) כן אומת שוב כנכון —
    `mcp__supabase__list_projects` עדיין לא מחזיר אותו.

30. **הפתרון: תיקון צד-לקוח בלבד, בלי לגעת ב-constraint של ה-DB כלל.** הבאג
    האמיתי לא היה "חייבים להוסיף `other` ל-CHECK", אלא שהקוד שלח מזהה קטגוריה
    גולמי (`other`/`stam`/`shiur_kavua`/...) בתור `lead_type` בלי לבדוק את
    התשובה — ה-DB דחה בשקט (`23514`) וה-UI הציג הצלחה מזויפת בכל זאת. שני
    הטפסים (`ContactPage.tsx`, `ServiceRequestForm.tsx`) עכשיו שולחים
    `lead_type: 'general'` (הערך הראשון בארבעת הערכים המותרים שכבר תועדו —
    `general/synagogue/rabbi_question/gabbai_support`) ושומרים את הקטגוריה
    הספציפית בתוך `message` (`[קטגוריה] גוף ההודעה`) כדי שהגבאי עדיין יראה
    במה מדובר — אין אובדן מידע. נוסף גם בדיקת `error` אמיתית עם הודעת כשל
    למשתמש (`role="alert"`) לכל מקרה כשל אחר שיקרה בעתיד (רשת/RLS), במקום
    "הצלחה" עיוורת. `GabaiPortal.tsx`: `requestTypeLabels` קיבל את 4 הערכים
    האמיתיים (היו חסרים לגמרי — היה בו רק רשימת הקטגוריות הישנות שלא תואמות
    ל-constraint). **אפס רגרסיה:** אף קטגוריה קיימת לא הוסרה, אין שינוי סכימה,
    התיקון עובד גם בלי גישת DB חדשה.

31. **הפריסה בפועל לא בוצעה מתוך הסשן הזה.** גיליתי ש-galil/chatzor נפרסים
    היסטורית דרך `vercel deploy --prod` מקומי מתוך `_deploy/galil-more30`
    (פרויקט Vercel לא Git-מקושר, לפי NEEDS_USER 18/08) — לא דרך push. בסשן הזה
    אין הרשאת Vercel CLI (`vercel whoami` → Logged out, אין `VERCEL_TOKEN` לא
    ב-`core.secrets` ולא ב-env) ואין workflow ב-`.github/workflows` בשורש
    שמאזין לפוש. לפי ההנחיה המפורשת של הסבב הזה, ה-push לענף `fix/` מיועד
    להפעיל את הפריסה דרך שרת הענן החיצוני (167.99.131.167) — לא בוצעה פריסת
    CLI ידנית מכאן. `npm run build` + `tsc --noEmit` רצו נקי מקומית (0 שגיאות)

## 19/08/2026 — 18 עורך תורני: מסך הניהול האחרון מתוך "שש בלי מסך" (core.issues #86)

32. **בדקתי לפי סדר העדיפויות (§0→§8 ב-`more30-priority.md`): 0-5 כבר אומתו
    גמורים בסבבים קודמים (הקומיט האחרון, `36052d52`, אימת 5 מקצה-לקצה בלי
    באג), 7 (מיתוג) התברר גמור בפועל — קרדיט "פותח ע״י עולם הסטארטאפים"
    מוזרק אוטומטית ע"י `auth-button.js` לכל מערכת שטוענת אותו, ו-24/24
    המערכות עם מקור בריפו כן טוענות אותו (רק 34/36, שאין להן מקור בריפו
    כלל, חסרות — לא ניתן לתקן בלי גישה לריפו שלהן).** כל האשכולות
    הפתוחים-ולא-חסומים שנותרו (#94/#115 מחיריון, #138 טלפון) היו במפורש
    הכרעות-מוצר שסוכן קודם, מודע יותר, נמנע מהן בכוונה — כיבדתי את זה.

33. **פנייתי במקום זאת ל-#86 ("שש מערכות חיות בלי מסך ניהול במקור") — התברר
    שחמש מהשש כבר נסגרו בסבבים קודמים (chizukim, smel, kesef, imud, studio),
    ונותרה רק 18 (עורך תורני / orech).** לפי `git show 207ec68b`, orech
    נדחתה במקור כי הנתונים שלה על פרויקט Supabase זר (`bieebmnmkffwbqlsfozh`)
    שלא היה נגיש; מ-`DECISIONS.md #27` ידוע ש-MCP כן מגיע לשם עכשיו. אימתתי:
    schema `otvedaf` (שם המקור: `apps/18-torah-editor-mvp/lib/supabase.ts` —
    `HTR_SCHEMA = 'otvedaf'`) מחזיק `books` (0 שורות) ו-`htr_jobs` (5 שורות
    אמיתיות, RLS `service_role בלבד`).

34. **הבעיה: הדפוס הקיים (RPC בצד ה-hub ששער `more30_is_admin()`) לא עובר
    כאן — נתוני 18 יושבים על פרויקט **אחר**, וה-JWT של ה-hub לא תקף שם, ואין
    סוד `service_role` פרוס ל-Vercel של הפורטל בשביל הפרויקט הזה (אותו סוג
    חסם כמו `#180`).** הטמעת `service_role` בקוד לקוח היתה פרצה אמיתית, לא
    קיצור דרך. הפתרון: שתי פונקציות `SECURITY DEFINER` חדשות בתוך `otvedaf`
    עצמו (`public_htr_stats`, `public_htr_jobs_list`) שמחזירות **רק צבירות
    ומטא-דאטה תפעולית** — בלי `raw_text`/`corrected_text`/`final_text` (התוכן
    המתומלל עצמו), בלי `image_url`/`image_bucket` (נתיבי אחסון), בלי
    `corrector`/`approved_by` (מזהה אדם) — ומוענקות ל-`anon` **באותה רמת
    אמון בדיוק** כמו מדיניות `books_anon_all` שכבר קיימת (ולא נגעתי בה) על
    `otvedaf.books`. ה-RLS של `htr_jobs` (service_role בלבד) לא שונה כלל —
    תוספת בלבד, אפס רגרסיה. המסך (`admin-orech.html`) עדיין שוער מאחורי
    session סופר-אדמין של ה-hub, לשמירת עקביות עם שאר `/admin/*`, אף שהנתון
    עצמו לא רגיש.

35. **אימות חי, לא רק קריאת קוד:** קריאת `curl` אמיתית מול
    `bieebmnmkffwbqlsfozh` עם ה-anon key הציבורי שלו + `Content-Profile:
    otvedaf` החזירה את חמש משימות ה-HTR האמיתיות — כולל שגיאה תפעולית
    (`DICTALM_ENDPOINT חסר`) שהמסך הזה הוא הראשון שחושף אותה. עדכנתי גם את
    `admin/src/App.tsx` (רשימת `CONSOLE_SCREENS`), `portal/vercel.dist.json`
    (`/admin/orech` → `/admin-orech.html`), ואת ה-nav של ארבעת המסכים
    האחים (imud/kesef/studio/smel) עם קישור הדדי. `core.issues #86` נסגר —
    כל שש המערכות מהמספר המקורי כעת עם קורא עובד. ענף `feat/orech-admin-
    screen-0819`, קומיט `b4985e49`.
    לפני ה-commit כאימות טרום-פריסה.

## 19/08/2026 (ערב) — 40 גננת בקליק: 22 קבצי מקור שמעולם לא נכנסו לגיט שוחזרו (עדיפות #0)

36. **בדקתי לפי סדר העדיפויות: §0-5 ב-`more30-priority.md` כבר אומתו גמורים
    בסבבים קודמים (heartbeat 1062), §7 (מיתוג) גמור בפועל. פניתי ל-§0
    (גננת בקליק) לאימות ולא רק לאמון בתיעוד — ומצאתי שהתיעוד היה שגוי.**
    `core.projects`/`app.json` מסמנים `is_deployed=true`/`live=true`, ו-
    `STATUS.md` של גננת מתעד "בנייה ✓, 180 עמודי `/lesson/[id]`" ופריסה
    מוצלחת מ-12/08 — אבל `git ls-files apps/40-gannenet` החזיר **33** קבצים
    בלבד מתוך 49 שמקור ה-Next.js המלא מחזיק (לא כולל PDFs, שיושבים
    בכוונה ב-bucket `gannenet-shelf` ולא בגיט). כלומר: מה שנפרס בסבב קודם
    נבנה ונבדק בעץ עבודה מקומי, אבל ה-`git add -f` (הנדרש כי `/apps/**`
    ב-`.gitignore` שורש) לא כלל את כל הקבצים — תבנית שכבר קרתה פעם (ראה
    "16 קבצים" בקומיט `9ad27a0d`), והפעם הייתה חמורה בהרבה: **22 קבצים**,
    כולל `package.json` עצמו.
37. **מה בפועל היה שבור בייצור, לא רק "חסר בגיט":** ארבעה קישורי ניווט
    מרכזיים בעמוד הבית והתפריט (`/library`, `/calendar`, `/newsletter`,
    `/pricing`) הובילו ל-404 — כולל שני כפתורי ה-CTA העיקריים בעמוד הבית
    ("למאגר המערכים"/"כניסה למאגר המערכים"). מסך הניהול
    (`app/shelf/admin/page.tsx`, כן בגיט) קורא ל-`/api/admin/list` ו-
    `/api/admin/override` בכל פעולה — שניהם היו חסרים, כך שכל כפתור במסך
    הניהול (הצגת קטלוג, הסתרת קובץ, עריכת עמודים מוסתרים) היה נכשל בשקט.
    `public/manifest.webmanifest`/`icon.svg`/`sw.js` היו חסרים גם הם —
    ה-PWA (מניפסט/אייקון) ורישום ה-service-worker לאופליין נכשלו בשקט
    (`RegisterSW.tsx` בולע שגיאת register עם `.catch(() => {})`).
38. **התיקון: שחזור מדויק, לא ניחוש.** `GITHUB_TOKEN` שכבר קיים בסביבה
    איפשר גישה ל-`l023131500-ops/-` (ציבורי) — משכתי את ה-Git Tree
    (recursive) של `claude/ganenet-full-system-gdrive-fdflfc`, זיהיתי בדיוק
    אילו 22 מ-49 קבצי המקור חסרים מול `git ls-files`, ומשכתי כל אחד מהם
    דרך ה-Blob API (לא clone מלא — הסניף מחזיק 166MB של PDFs לפי אזהרת
    STATUS.md קודמת). כל קובץ אומת פעמיים: גודל בבייטים תואם לרשומת ה-tree,
    ו-`git hash-object` על הקובץ שנכתב תואם בדיוק ל-`sha` הרשום ב-tree —
    לא "קובץ ירד", אלא אותו בלוב ביטים-לביטים. ארבעת קובצי ה-JSON (מכילים
    את 180 המערכים + קטלוג המדף, עד 1.36MB) אומתו גם ב-`json.load` מוצלח.
    **אפס רגרסיה:** 11 קבצים שנוספו בריפו הזה אחרי הייבוא המקורי
    (session/rate-limit/quota/מדף-שלי — פיצ'רים שלא היו במקור בכלל) נבדקו
    ולא מיובאים על-ידי אף אחד מ-22 הקבצים המשוחזרים ולהפך — אין התנגשות,
    ולא נדרסו.
39. **מה שנשאר פתוח, ולא הומצא/נוחש כדי "לסגור":** `app/api/admin/delete`
    (מחיקת קובץ שהועלה, service_role) נקרא על-ידי מסך הניהול אבל **אינו
    קיים במקור המווענדר בכלל** — תוסף שנכתב בסבב more30 קודם (כמו
    `admin/usage` שכן שרד בגיט), בלי עותק נגיש לשחזור מדויק. כתיבת קוד שרת
    חדש שנוגע ב-`SUPABASE_SERVICE_ROLE_KEY` בלי מקור אמין = ניחוש באזור
    רגיש — נמנעתי, ונרשם ל-`core.issues` לסבב הבא. כנ"ל `lib/season.ts`
    (כן בגיט, לא מחובר) ו-`/shelf` שלא קורא `cat`/`q` מה-URL — שיפורים
    שתועדו ב-STATUS.md כבר בוצעו פעם אבל גם הם אבדו באותו כשל git-add,
    ושחזור מדויק מהמקור קדם לשחזור-מהזיכרון החלקי. לא הורץ `npm
    install`/`build` (כלל הסשן) — אימות היה קריאת קוד + hash, לא הרצה.
    ענף `feat/orech-admin-screen-0819` (אותו ענף עבודה מתמשך), 22 קבצים
    נוספו ל-git עם `git add -f`.

## 19/08/2026 (לילה) — 40 גננת בקליק: `/api/admin/delete` נבנה — לא ניחוש הפעם, יש מקור מדויק (עדיפות #0, core.issues #244)

40. **מה השתנה מאז #39:** בסבב הקודם נמנעתי מלכתוב את `app/api/admin/delete`
    כי חשבתי שאין לו עותק מקור נגיש. קריאה מדוקדקת יותר של `lib/supabase.ts`
    (כן שוחזר ב-#38) גילתה שזה לא מדויק: `deleteUpload()` — כל לוגיקת ה-
    `service_role` (מחיקת blobs מהדלי `gannenet-shelf`, ואז הרשומה, עם
    idempotency על 404-בגוף-400) — **כבר קיימת ומתועדת שם במלואה**, כולל
    ה-exports `deleteReady`/`supabaseReady` שנועדו במפורש לצריכה מ-route
    שעדיין לא קיים. וב-`STATUS.md` (שורות 349-380) יש תיעוד מדויק של אותו
    route שאבד: קוד סטטוס (401/400/404/503), תבנית ה-id המצומצמת
    (`^up_[a-z0-9]+_[a-z0-9]+$`, בכוונה צרה יותר מזו של `/api/admin/override`
    כדי שאף Drive/seed id לא יגיע לנתיב ההרסני היחיד), וסדר הפעולות
    (blobs קודם, רשומה אחרונה). זה לא "כתיבת קוד service_role בלי מקור
    אמין" — זה חיבור endpoint דק מעל לוגיקה קיימת, לפי מפרט מתועד במדויק.
41. **הקובץ שנכתב:** `app/api/admin/delete/route.ts` (חדש) — אימות זהה
    בדיוק לתבנית של `/api/admin/list` ו-`/api/admin/override` (הדדר
    `x-admin-key` מול `ADMIN_PASSWORD`), אימות `fileId` מול ה-regex
    המצומצם, קריאה ל-`deleteUpload()` הקיימת, ומיפוי `reason` להודעות
    שגיאה בעברית בסגנון שאר האפליקציה (`error:` בגוף ה-JSON, אותו סגנון
    שקורא `app/shelf/admin/page.tsx` בפועל ב-`remove()`). אומת בקריאת קוד
    בלבד (לא הותקן `node_modules`, לא הורץ build — כלל הסשן): ה-imports
    (`deleteUpload`, `deleteReady`, `supabaseReady`) קיימים ומיוצאים
    ב-`lib/supabase.ts`; צורת הבקשה (`POST { fileId }` + `x-admin-key`)
    וקריאת השגיאה (`d.error`) תואמות בדיוק את מה ש-`remove()` ב-
    `shelf/admin/page.tsx` שולח וקורא; דפוס ה-regex נבדק ידנית מול הפורמט
    ש-`uploadItem()` מייצר (`up_<base36 ms>_<base36 rand>`, בלי סיומת —
    ה-id ולא ה-`storage_path`).
42. **אפס רגרסיה:** קובץ חדש בלבד, לא נגעתי באף קובץ קיים. שאר שני
    הפערים התיעודיים מ-#39 (`lib/season.ts` לא מחובר, `/shelf` לא קורא
    `cat`/`q` מה-URL) נשארים פתוחים — לא באותו סטטוס חסימה: פשוט לא היו
    חלק מהצעד הזה, וירשמו לסבב הבא. ענף `feat/orech-admin-screen-0819`
    (אותו ענף עבודה מתמשך).

## 19/08/2026 (לילה, המשך) — 40 גננת בקליק: שני הפערים האחרונים מ-#244 נסגרו — `lib/season.ts` מחובר ו-`/shelf` קורא `cat`/`q` מה-URL (עדיפות #0)

43. **בדקתי `core.issues` פתוחים לפי עדיפות — #244 (גננת, "fixed" אך לא
    "closed") עדיין מתעד שני שיפורים תיעודיים-בלבד שנותרו פתוחים מהסבב
    הקודם: `lib/season.ts` לא מחובר ל-`app/calendar/page.tsx`, ו-`/shelf`
    לא קורא `cat`/`q` מה-URL — כלומר קישור מסונן למדף (`/shelf?cat=…`)
    לא היה עושה כלום בפועל.** שני הפערים תלויים זה בזה (חיבור `season.ts`
    לעמוד הלוח-שנה חסר ערך בלי שהעמוד הנחית יידע לקרוא את הסינון) —
    טופלו יחד כצעד אחד קוהרנטי.
44. **`lib/calendar/page.tsx`:** הוסיפה קריאה ל-`weekTopics(now)` (כבר
    קיימת ומתועדת ב-`lib/season.ts`, לא נכתבה מחדש) בתוך `try/catch`
    (עקבי עם שני הבלוקים הקיימים באותו עמוד), ורינדור כרטיס "נושאים
    מומלצים למדף — 45 הימים הקרובים": לכל נושא — שם הקטגוריה, הסיבה
    (`t.reason`), תאריך לועזי, ותווית `inDaysLabel` (גם היא כבר קיימת
    ב-`season.ts`, לא שימשה עד כה בשום מקום). כל כרטיס הוא `Link` אל
    `/shelf?cat=<קטגוריה מקודדת>`. אימתתי ידנית ש-24 מפתחות `EVENT_CATEGORY`
    ב-`season.ts` תואמים מדויק לערכים ב-`CATEGORY_ORDER` (`lib/catalog.ts`)
    — אין קטגוריה שתקשר לסינון ריק. עדכנתי גם את שורת התיאור התחתונה
    שהצהירה במפורש "בהמשך יחובר" (עכשיו לא נכון יותר) להיגד עובדתי.
45. **`app/shelf/page.tsx`:** הוספתי `useEffect` שקורא `window.location.search`
    פעם אחת בעליית הקומפוננטה וממלא את `cat`/`q` אם קיימים ב-URL — לא
    `useSearchParams` מ-`next/navigation`, כדי לא לדרוש `Suspense boundary`
    לבנייה (העמוד `"use client"` שלם, לא עטוף). לא נגעתי בשום סטייט/לוגיקת
    סינון קיימת — הוספה בלבד, מפעילה ערכים ראשוניים שכבר קיימים
    (`useState("")`).
46. **אפס רגרסיה:** שני קבצים נערכו, שניהם רק תוספת (import חדש + בלוק
    JSX חדש ב-calendar; `useEffect` חדש ב-shelf) — שום פונקציה/מסך/כפתור
    קיים לא הוסר או שונה בהתנהגותו. לא הותקן `node_modules`, לא הורץ
    build/dev (כלל הסשן) — אימות בקריאת קוד: ה-import `@/lib/season` פותר
    לפי `paths: {"@/*": ["./*"]}` ב-`tsconfig.json`; `.card`/`.chip` ו-
    `a { color: inherit; text-decoration: none }` כבר קיימים ב-`globals.css`
    ומכסים את הסגנון האינליין שנוסף. `core.issues #244` יכול להיסגר
    לגמרי בסבב הבא (רשום, לא בוצע מה-Task הזה כדי לא לחרוג מהיקפו).
    ענף `feat/orech-admin-screen-0819` (אותו ענף עבודה מתמשך).

## 19/08/2026 (לילה, המשך 2) — 26 סטודיו: כפתור "החל את כל התיקונים" בביקורת ה-AI — קוד בלבד, לא נפרס (עדיפות #6, `core.issues #5`)

47. **בדקתי לפי סדר העדיפויות של הריצה הזו: #0 (גננת) גמור, #1 (כניסת לקוח
    אמיתית) חסום מבנית — כל מה שנותר תלוי בחמישה פרויקטי Supabase שלא נגישים
    מה-MCP/PAT של הסשן הזה, ופרויקט שישי (`bieebmnmkffwbqlsfozh`) שנגיש אך
    נושא את הסכמות המוגנות של 08/09 (`core.issues #172`, ראה גם
    `NEEDS_USER.md §0תד`) — לא נגעתי בו. #2–#5 (סופר-אדמין, לוח ניהול-על+
    יתרות API, סדר דף הבית, בקלות) כבר בנויים ואומתו בסבבים קודמים. עברתי
    ל-#6 (עיצוב ייחודי, `core.issues #5`, `severity=high`, `status=in_progress`
    — היחיד הפתוח והלא-חסום בסדר העדיפויות).**
48. **`CHECKLIST/graphics.md` שלב 4 (סטודיו רב-סוכני) מתעד רק שני פריטים
    לא-מסומנים (13, 14), ושניהם באותו נימוק חוזר בארבעה סבבים נפרדים היום:
    הלולאה האוטומטית המלאה (עיצוב→ביקורת→ליטוש בלי מעורבות אדם) גדולה/מסוכנת
    מדי לצעד יחיד, ולכן כל סבב הוסיף במקומה פרוסה בטוחה ומדידה (כפתור ביקורת,
    כפתור "החל תיקון" בודד, כפתור "בדוק שוב", שמירת הערת לקוח, וחיבורה
    לביקורת).** המשכתי באותו דפוס: **"החל את כל התיקונים"** — כפתור חדש
    בכותרת רשימת ה"הערות" בדיאלוג הביקורת (`Editor.tsx`), מוצג רק כשיש לפחות
    תיקון אחד ניתן-לפתרון שטרם הוחל (נספר עם אותו `resolveCritiqueFix` הקיים
    בדיוק — אימות שכבה קיימת + clamp טווח, ללא שינוי בלוגיקת האימות). לחיצה
    עליו (`handleApplyAllCritiqueFixes`, פונקציה חדשה) מריצה את **אותה**
    `handleChangeLayer` הקיימת על כל התיקונים הממתינים בבת אחת ומסמנת את כולם
    "הוחל ✓" — עדיין לחיצת אדם יזומה **אחת**, לא איטרציה/ריצה-חוזרת אוטומטית;
    כפתור "החל תיקון" הבודד הקיים לכל הערה נשאר ללא שינוי לחלוטין. עודכן גם
    משפט התיאור בדיאלוג שמסביר את הכפתור החדש. `import` חדש: `CheckCheck`
    מ-`lucide-react` (כבר תלות קיימת בפרויקט). קובץ יחיד נערך, תוספת בלבד —
    אפס מבנה JSX קיים הוסר או שונה.
49. **לא נפרס, ותועד ככה בכוונה — לא "נשכח".** `apps/26-modaot-studio` הוא
    פרויקט Vercel **מוגדר-מראש** (`vercel-adapter/vercel.json`:
    `buildCommand: "echo no-build"`) שנפרס עד היום בכל סבב עם `vite build`
    מקומי + העתקה ל-`_deploy/studio-more30/public/studio` + `vercel deploy
    --prod` ידני — **לא** push-triggered כמו רוב שאר המערכות (ל-`apps/40-
    gannenet`, למשל, אין אפילו `vercel.json` משלו — Next.js רגיל, פריסת git
    סטנדרטית). בסבב הזה `node_modules` **לא קיים בשום מקום בעץ העבודה**
    (נבדק בשורש וב-`apps/26-modaot-studio`), וכלל ההרצה אוסר להמתין להתקנה
    מלאה — ולכן, בדיוק לפי ההנחיה ("אם תלויות חסרות, בצע עריכת מקור ממוקדת
    שלא דורשת בנייה מקומית"), נעצרתי אחרי עריכת המקור ואימות קריאה בלבד
    (נבדקה שלמות ה-JSX/הסוגריים ידנית, לא `tsc`). **הצעד הבא הטבעי:** בסבב
    עם `node_modules`/`vite`/`vercel` זמינים — `vite build --base=/studio/`,
    השוואת מספר המודולים ל-2169 (הבסיס היציב מכל הסבבים הקודמים) כאימות
    "אפס קוד מת", העתקה ופריסת `vercel deploy --prod`, ואימות Playwright חי
    (ביקורת AI עם ≥2 הערות בעלות תיקון → "החל את כל התיקונים" → כולן "הוחל
    ✓" בבת אחת). עד אז: קוד תקין ב-`git`, **לא** משפיע על `more30.com/studio`
    החי. ענף `feat/orech-admin-screen-0819` (אותו ענף עבודה מתמשך).

50. **סבב חדש: בדקתי מחדש את סדר העדיפויות (0→8).** #0 (gannenet) סגור
    ומאומת חי. #1 (login) נבדק שוב לעומק — `core.issues #172` מראה שהתיקון
    בקוד (הודעת "המייל עדיין לא אומת" במקום "סיסמה שגויה") כבר חי בכל חמש
    המערכות עם מסך כניסה עצמאי; מה שנשאר הוא אך ורק הגדרת `Confirm email`
    בלוח הבקרה של שבעה פרויקטי Supabase — שישה לא נגישים מה-PAT של הסשן
    הזה, והשביעי (`bieebmnmkffwbqlsfozh`) נגיש אך נושא את 08/09 המוגנות,
    ולכן במפורש לא נגעתי. #2–#5 ו-#7–#8 כבר בנויים ואומתו (כולל: מחירים
    2/5/10/12/15 ₪ ב-`core.plans`, קישור "מסלולים ומחירים" מכל כרטיס מערכת
    בדף הבית, ושאלון-האפיון החכם כ"נחיתה חינמית" בתוך הפורטל עצמו — שלושתם
    כבר קיימים, לא נבנו הסבב הזה). עברתי ל-#6 (`core.issues #5`,
    `in_progress`) — אבל מסלול הסטודיו (item 13/14) עדיין חסום על אותה
    סיבה מדויקת מ-#49 (`node_modules` לא זמין, אסור להתקין), אז חיפשתי
    עבודה **אחרת** תחת אותו כרטיס שלא דורשת בנייה.

51. **מצאתי כשל אמיתי דרך סריקת-מצב, לא דרך `core.issues`.** `QA/platform/
    DESIGN_SAMENESS.md` (מסמך העבודה של #5) הצביע על "הבא בתור: chizukim,
    ואז studio" — אבל `git log` הראה ששתיהן כבר טופלו בסבבים קודמים
    (`523cfe58`, `10c4be9b`, `662eec5a`) בלי שהמסמך עודכן. תוך כדי אימות
    זה, סריקה שיטתית של `favicon.*` בכל `apps/*` גילתה **פער אמיתי**: הקובץ
    שנכתב ונפרס ב-662eec5a (`apps/26-modaot-studio`) מעולם לא נכנס למקור —
    אין קומיט עליו, ו-`_deploy/studio-more30` המקומי לא מחזיק `public/`
    בכלל. `index.html` עדיין מצהיר `href="/studio/favicon.svg"` (תקין), אבל
    הקובץ שמאחוריו אינו בעץ. זה בדיוק דפוס "אבד בכשל git-add" מ-#36-39 —
    לא נראה בייצור החי היום (שנפרס כבר מ-662eec5a), אבל **יידרס בחזרה
    לריק** בפעם הבאה שסבב עתידי ירוץ `vite build`+פריסה לצורך item 13/14.

52. **שחזרתי את הקובץ במקור, שחזור נאמן ולא ניחוש.** גליף `Crown` הועתק
    מ-`lucide-react` ^0.453.0 (הגרסה הנעוצה ב-`package.json` של studio,
    אומתה זהה למותקן בפועל ב-`node_modules` של `apps/04-imud-torani` באותו
    מונו-רפו). צבעים `#C9A227` על `#0B1220` נמדדו מ-`Home.tsx:111,122,130`
    (לא נבחרו). ניגודיות חושבה מחדש (WCAG relative luminance): **7.744:1**
    — תואם בדיוק את ה-7.74:1 שתועד ב-662eec5a, אישוש עצמאי שהצבעים/הגרסה
    נכונים. אין אריח-מיכל חי למדוד ממנו יחס (הסמל בסרגל חשוף בלי רקע), אז
    הרדיוס (8px) נלקח ישירות מטוקן `radius` של האפליקציה. **רמת האימות
    נמוכה מהאחיות שלו בכוונה ומתועדת ככה**: רק XML תקין (`xml.dom.minidom`,
    אחרי שתפס ותיקן שגיאת "שני מקפים רצופים בהערת XML" — אותה מלכודת
    שהערת torah מזהירה ממנה) — לא `vite build`/`preview`/Chromium, כי
    `node_modules` עדיין לא קיים ואסור להתקין. אפס רגרסיה: קובץ חדש בלבד,
    שום קובץ קיים לא נערך חוץ מהוספת ההסבר ב-`DESIGN_SAMENESS.md`. תיעדתי
    גם כשל שלישי שנמצא ולא טופל — `21-mthbram`/`24-galilee-connect-hub`
    מצהירות אייקון PNG ולא SVG, לא נבדק אם זו בעיה — וסדר עבודה מתוקן
    (21/24, ואז עשר המערכות בלי `favicon.*` כלל) ב-`DESIGN_SAMENESS.md`.

## 19/08/2026 (סבב נוסף) — 21 מתחברים: `icon-512.png` היה JPEG בתחפושת PNG (עדיפות #6, המשך `DESIGN_SAMENESS.md`)

53. **בדקתי מחדש את סדר העדיפויות 0→8 לפי `core.projects`/`core.issues`.**
    #0 סגור וחי; #1 חסום מבנית על הגדרת Confirm-email בלוח הבקרה של שישה
    פרויקטי Supabase (חמישה לא נגישים מה-PAT + `bieebmnmkffwbqlsfozh` נגיש
    אך נושא 08/09 המוגנות — לא נגעתי, `core.issues #172`); #2–#5 ו-#7–#8
    כבר בנויים ואומתו בסבבים קודמים (`core.projects` מראה 37-bkalot-clone,
    32/33/34/36 חיים עם ביקורת Playwright טרייה). המשך #6 (`core.issues #5`,
    `in_progress`) — פסקת "הבא בתור בפועל" בסוף `DESIGN_SAMENESS.md` הפנתה
    ל-21/24 (אייקוני PNG לא-SVG) כפריט שנמצא ולא נבדק בסבב הקודם.
54. **בדקתי את 21/24 — לא באג-בסיס, אבל נמצא באג אמיתי אחר תוך כדי.**
    PWA עם `manifest.json`/`vite-plugin-pwa` שמצהירים אייקוני PNG (192/512)
    זו קונבנציה תקנית לגמרי, לא סטייה מ-SVG — שני היישומים כבר תיעדו את
    ההחלטה הזו בהערות `index.html` שלהם מקומיטים קודמים (`ac733c86`,
    `e4a55b1a`). אבל תוך כדי בדיקת בתי-הקובץ בפועל (לא רק הסיומת) מול הסוג
    המוצהר, נמצא ש-`apps/21-mthbram/public/icon-512.png` הוא **JPEG אמיתי**
    (בתי פתיחה `FF D8 FF`, לא `89 50 4E 47` של PNG) שיושב תחת סיומת/
    `type="image/png"` שקריים בשלושה מקומות: `<link rel=icon>`,
    `og:image`/`twitter:image`, ו-`manifest.json`. `icon-192.png` (אותו
    יישום) ו-`24-galilee-connect-hub/public/pwa-512.png` נמדדו אמיתיים —
    לא אותו כשל. משמעות: בדיוק משפחת הבאג ש-`e4a55b1a` (בקומיט קודם, אותו
    קובץ) בא לתקן — שמאי-קישור נוקשים (Facebook/Twitter card debuggers)
    מאמתים בתים בפועל מול `Content-Type`/`type` מוצהר ודוחים אי-התאמה,
    כלומר כרטיס השיתוף של 21 עלול היה להישבר בשקט באותה צורה שהתיקון
    הקודם ניסה למנוע.
55. **התיקון: שינוי שם + תיוג נכון, לא המרת פורמט.** אין כלי המרת תמונה
    זמין בעץ הזה (`convert`/`magick` לא מותקנים, אין `pip`/`PIL`, ואסור
    להתקין תלויות הסבב הזה) — כך שלא ניתן היה להפוך את הבתים בפועל ל-PNG
    אמיתי בלי חריגה מהכלל. הפתרון שנשאר בתחום המותר: `git mv` מ-
    `icon-512.png` ל-`icon-512.jpg` (אותם בתים בדיוק, שם נכון), ועדכון שלוש
    ההפניות (`index.html` פעמיים + `og:image`/`twitter:image`, ו-
    `manifest.json`) ל-`type="image/jpeg"` + הנתיב החדש. `icon-192.png`
    נשאר היחיד/הראשי (192×192 תקין, מכסה את דרישת המינימום של Chrome
    ל-installability) — 512 הוא תוספת איכות, לא היחיד שנבדק. אפס רגרסיה:
    שני קבצים נערכו (טקסט בלבד) + שינוי שם קובץ אחד (אותו תוכן בתים,
    `git mv` שומר על ה-blob) — שום מסך/רכיב/פונקציה קיימים לא הוסרו או
    שונו בהתנהגותם.
56. **אימות בקריאה, לא בבנייה (כלל הסשן).** נבדק ידנית: אין הפניה שלישית
    שנשארה ל-`icon-512.png` בכל `apps/21-mthbram` (`grep` על המקור, לא
    `dist` — לתיקייה הזו אין `dist/` בעץ העבודה כרגע כך שאין עותק בנוי
    ישן שסותר את המקור). בתי הקובץ שזוהו כ-JPEG נבדקו פעמיים (`file` +
    בדיקת magic-bytes ישירה ב-Python) לפני העריכה כדי לשלול זיהוי שגוי של
    `file`. הורחבה סריקה שיטתית לכל `apps/*/**/*.png` (לא רק `public/`,
    לא `node_modules`/`dist`) מול magic bytes אמיתיים — נמצאו עוד שלוש
    התאמות-שווא, לא טופלו הסבב הזה כדי לא לחרוג מהיקף צעד יחיד: שתיים
    ב-`apps/22-get-your-rights/src/assets/` (`plant-growth.png`,
    `plant-money.png` — גם הן JPEG תחת סיומת PNG, אבל מיובאות דרך צינור
    ה-assets של Vite ולא `public/` סטטי, כך שהתיקון/האימות שונה מהותית
    ודורש בדיקת התנהגות ה-bundler, לא רק שינוי שם) ו-`apps/_archive/
    bklotm`/`apps/_archive/lux-manage` (שתי מערכות בארכיון —
    `CONNECTIONS.md`: "ארכיון/ניקוי בלבד — לא לטפל כפעילות" — במכוון לא
    נגעתי). **הבא בתור בפועל:** 22-get-your-rights src/assets (לבדוק אם
    ה-bundler שובר על סמך תוכן או רק סיומת לפני נגיעה), ואז המשך רשימת
    `DESIGN_SAMENESS.md` (עשר המערכות בלי `favicon.*` כלל).

## 19/08/2026 (סבב נוסף 2) — 22 מימוש זכויות: שני קבצי JPEG-בתחפושת-PNG ב-`src/assets` תוקנו — התנהגות ה-bundler נבדקה קודם, לא רק שינוי שם (עדיפות #6, המשך `DESIGN_SAMENESS.md`)

57. **בדקתי קודם את התנהגות ה-bundler, כמו שהסבב הקודם ביקש.** `apps/22-
    get-your-rights/vite.config.ts` הוא Vite רגיל עם `@vitejs/plugin-react-swc`
    — אין שם שום שלב שמאמת בתי-תמונה מול הסיומת המוצהרת (אין `imagetools`,
    אין טרנספורמציה של תוכן תמונה). ‏Vite מטפל בייבוא `@/assets/*.png` כנכס
    סטטי: מעתיק/מגבב אותו (או מקודד base64 אם קטן מהסף) **לפי הסיומת
    שבנתיב הייבוא בקוד המקור**, בלי לבדוק את הבתים בפועל. המשמעות: בניגוד
    ל-21-mthbram (קובץ `public/` סטטי שמוגש ישירות, שם רק ה-`<link>`/
    ‏`manifest.json` מכריזים MIME), כאן **גם שם הקובץ בדיסק וגם שורת הייבוא**
    צריכים להשתנות יחד, אחרת ה-build ישבור על ייבוא חסר.
58. **שני הקבצים נמדדו שוב (לא הסתמכתי על הממצא הקודם) — שניהם JPEG אמיתי**
    (`FF D8 FF E0`, סמן JFIF), לא PNG. ‏`plant-money.png` מיובא ומשמש בפועל
    ב-`Footer.tsx:10,185` (`<motion.img src={plantMoney}>`, קרדיט "כסף
    צומח"). `plant-growth.png` **אינו מיובא בשום מקום** בעץ המקור (`grep`
    שיטתי על `src/` כולו) — נכס מת, לא רק לא-פעיל-כרגע.
59. **התיקון: `git mv` לשני הקבצים ל-`.jpg` + עדכון שורת הייבוא היחידה
    ש-existant.** `plant-money.png → plant-money.jpg`, ועודכן הייבוא
    ב-`Footer.tsx:10` ל-`@/assets/plant-money.jpg` (השימוש בשורה 185 לא
    השתנה — הוא מפנה למשתנה `plantMoney`, לא לנתיב). `plant-growth.png →
    plant-growth.jpg` גם שונה שם, לתיוג נכון גרידא — אין ייבוא לעדכן כי
    אין ייבוא בכלל. שוב: אין כלי המרת תמונה מותקן בעץ הזה, אז זה תיוג נכון
    של הבתים הקיימים, לא המרת פורמט אמיתית ל-PNG.
60. **אימות בקריאה, לא בבנייה.** `grep` שיטתי על כל `apps/22-get-your-
    rights` (כולל `.ts`/`.tsx`/`.json`/`.html`/`.css`, לא `node_modules`)
    אחרי העריכה: אפס הפניות שנותרו ל-`plant-money.png` או ל-`plant-
    growth.png`. בתי שני הקבצים נבדקו שוב אחרי ה-`git mv` (magic-bytes
    ב-Python) לוודא ש-`git mv` שמר על התוכן זהה. אפס רגרסיה: שני קבצים
    הוזזו (`git mv`, אותו blob), שורת ייבוא אחת נערכה (טקסט בלבד) — שום
    מסך/רכיב/פונקציה קיימים לא הוסרו או שונו בהתנהגותם; ה-`<motion.img>`
    ב-`Footer.tsx` ממשיך להציג את אותה תמונה בדיוק, רק מוגשת עם `type`
    נכון. **הבא בתור בפועל:** עשר המערכות בלי `favicon.*` כלל — נבדקו כאן
    וכולן עדיין ריקות: `10-bkalot-rights`, `19-igud-shiurim-portal`,
    `20-igud-portal`, `23-haorech-torani`, `37-bkalot-clone` ועוד חמש
    (טרם זוהו בשם) — קטגוריה שלישית של `DESIGN_SAMENESS.md`, טרם נמדדה
    כלל; זה יידרש סבב עיצוב (בחירת סמל/צבע לכל מערכת), לא רק בדיקת בתים.

## 19/08/2026 (סבב אוטונומי) — favicon ייחודי נוסף ל-10 בקלות ו-37 bkalot-clone; 19/20/23 נדחו במכוון (לא חיות)

61. **מיפוי `core.projects` קודם, לא `registry.ts`.** מתוך עשרת המועמדים
    (`07,10,13,19,20,23,25,29,34,35,37`, אחרי הסרת `08`/`09` המוגנות),
    שישה אין להם קוד מווענדר בעץ הזה בכלל (`07,13,25,29,34,35` — `app.json`
    בלבד או קבצי טוקנים/שרת, אין `index.html` לערוך). מתוך חמשת הנותרים,
    `registry.ts` טוען ש-`19,20,23` הן שלד לא-פרוס (`live:false`) — אבל
    זו בדיוק אי-ההתאמה שנפתחה ב-issue #156 (`registry.ts` מול
    `core.projects`, לא נסגרה). בדקתי מול `core.projects` (מקור אמת חי):
    `19`,`20`,`23` אכן `live=false / is_deployed=false / live_url=null` —
    שלדים אמיתיים, אף אחד מהם לא נגיש לאף משתמש כרגע. `10` ו-`37` לעומת
    זאת `live=true / is_deployed=true` (‏`more30.com/bkalot`,
    `more30.com/bkalot-studio`). **הכרעה:** favicon על מערכת שאף אחד לא
    יכול לפתוח היום הוא איפוס-לשווא — טופל רק `10` ו-`37`, השלושה הלא-חיים
    נשארים לתור (יטופלו כשייפרסו, לא לפני).
62. **כל אייקון נגזר מהמותג האמיתי של המערכת, לא סמל גנרי.** `10-bkalot-
    rights`: יש לה כבר סמן מותג inline (`ריבוע מעוגל + וי`, `stroke:
    currentColor`) שנטען מ-`--teal-d` (‏`#1B474D`, מ-`style.css:70`) —
    `favicon.svg` החדש הוא **אותו סמן בדיוק**, לא המצאה, כדי שהלשונית
    תתאים ל-favicon בכותרת. `37-bkalot-clone`: אין סמן inline קיים, אז
    צויר סמל חדש עקבי עם הזהות המוצהרת בקוד עצמו (הערת CSS ב-`index.html`:
    "ירוק-אזמרגד עמוק (סמכות שקטה)", `--brand:#0f5f4c`) — מסמך עם קפל +
    וי (מייצג "פנייה שנפתחה וטופלה"), לבן על ירוק-אזמרגד, שונה בבירור
    מ-10 (ריבוע בהיר עם קו טורקיז) כדי שלא ייראו כמערכת אחת למרות
    שהם משפחת "בקלות" משותפת.
63. **גיליתי תלות פריסה שלא הייתה מתועדת מספיק: `37-bkalot-clone` לא
    נפרס דרך git-push רגיל.** בשונה מ-`10` (פרויקט Vercel נפרד,
    `bkalot-more30.vercel.app`, מחובר-git לתיקייה הזו — מאומת מתוך היסטוריית
    git: קומיטים קודמים על התיקייה הזו כתבו "deployed" ואומתו חיים) —
    `37-bkalot-clone` מוגש **מתוך הפורטל עצמו** דרך `portal/vercel.dist.json`
    (rewrite ל-`/bkalot-studio/index.html`), וההעתקה מ-`apps/37-bkalot-
    clone/*.html` אל `portal/dist/bkalot-studio/` קורית רק ב-
    `scripts/stage-portal.ps1` — סקריפט שלא רץ אוטומטית ב-build של הפורטל
    (`portal/package.json` build הוא `gen-sitemap + vite build` בלבד, בלי
    שלב PowerShell). כלומר קובץ ה-favicon החדש היה נשאר מת בלי שינוי נוסף:
    עודכן `stage-portal.ps1` להעתיק גם את `favicon.svg` (לא `throw` אם
    חסר — נכס אופציונלי, לא דורש-חיים כמו `index.html`/`admin.html`).
    בגלל התלות הזו נבחר קישור **מוחלט** (`https://more30.com/bkalot-
    studio/favicon.svg`) ולא יחסי בשני קבצי ה-HTML של 37 — אין שם `<base>`
    ואין תקדים לנתיב יחסי בקובץ (בשונה מ-`10`, ששם `style.css` יחסי כבר
    עובד חי מול `<base href="/bkalot/">`, ולכן `favicon.svg` שם נשאר יחסי
    לעקביות עם התקדים המוכח).
64. **הקובץ החדש של 10 נתקל ב-`.gitignore` הכללי (`/apps/**`), והוסף
    ב-`git add -f`.** זה עקבי עם איך שאר הקבצים המווענדרים באותה תיקייה
    (‏`index.html`, `style.css` וכו') כבר נמצאים בגיט — היוצא-מן-הכלל הזה
    כבר קיים לקבצים קודמים, לא נוצר כאן משהו חדש במדיניות. `37-bkalot-
    clone/favicon.svg` לעומת זאת נכנס נקי כי לתיקייה שלו כבר יש שורת
    `!/apps/37-bkalot-clone/**` מפורשת ב-`.gitignore`.
65. **אימות בקריאה, לא בבנייה.** שני קבצי ה-SVG נבדקו כ-XML תקין
    (`xml.dom.minidom`, ללא שגיאת פענוח). `grep` על שני קבצי ה-HTML של 37
    ועל `index.html` של 10 מוצא בדיוק שורת `rel="icon"` אחת בכל קובץ, בלי
    כפילות. אפס רגרסיה: שלושה קבצי HTML קיבלו שורת `<link>` אחת נוספת
    כל אחד, שני קבצי SVG חדשים נוצרו, וסקריפט הפריסה קיבל שלב-העתקה
    אופציונלי נוסף — שום מסך/רכיב/פונקציה קיימים לא הוסרו או שונו.
    **הבא בתור בפועל:** `19-igud-shiurim-portal` / `20-igud-portal` /
    `23-haorech-torani` — favicon כשהם ייפרסו בפועל (לא לפני, לפי #61);
    ובנפרד, ה-`07/13/25/29/34/35` שאין להם קוד מווענדר כלל דורשים או
    ונדור מהמקור החיצוני שלהם או הכרעה שהם לא בטיפול כרגע.

## 19/08/2026 (סבב אוטונומי 2) — `mthbram`: הכרעת המינוח התלויה של "חיפוש חכם" (§6, נרשמה פתוחה ב-`DESIGN_SAMENESS.md` מ-10/08) — נמדד מי באמת חכם, ותוקן מי שלא

66. **בדקתי `core.projects` ו-`core.issues` לפני שבחרתי משימה.** `core.issues`
    #5 ("עיצוב ייחודי לכל אתר §6") הוא הפריט היחיד `in_progress`/`open`
    שאינו חסום על גישה חיצונית (השוואה מול #172/#229/#167/#62/#88 — כולם
    חסומים על דשבורד Supabase, Lovable content-policy, או הכרעת משתמש).
    ‏#0 (gannenet) כבר `live` ומאומת (issue #244), #1 (login) חסום מבנית,
    #2–5/#7–8 כבר נבנו בסבבים קודמים לפי ה-heartbeat האחרון. בתוך #6,
    שרשור ה-favicon (עדכונים כ–כז + הסבבים האחרונים) מוצה לכל המערכות
    החיות — מה שנשאר פתוח בקובץ עצמו הוא «"חיפוש חכם" ב-12 קבצים ב-
    `mthbram` שממתין להכרעת מינוח», מוזכר כפריט לא-סגור בסוף כל אחד
    מהעדכונים מ-10/08 ואילך ולא נסגר מעולם.
67. **המינוח נבדק מול הקוד בפועל, לא הוחלט מהתחושה.** שמונה מהם עברו על
    "חיפוש חכם"/"סוכן חכם" בעברית: תיבת חיפוש ב-`LessonDirectory.tsx`,
    `FeaturedLessons.tsx`, `LessonsDashboard.tsx`, כפתור ב-`LessonDirectory.tsx`,
    כותרת ב-`FindLesson.tsx`, תיאור ב-`HeroSection.tsx`/`ThreePillarsSection.tsx`,
    ותיעוד ב-`IvrBuilder.tsx` (label + שני תיאורים) — כל אלה הם סינון
    `.filter()`/`.includes()` בצד לקוח או שאילתת `ilike` פשוטה בקצה
    (‏`ivr-agent/index.ts:29-39`: פיצול מילים + `OR ilike`, **ללא** קריאת
    LLM כלשהי). לעומת זאת `FloatingChatBot.tsx:202` שולח את השיחה אל
    `search-lessons/index.ts`, שמזרים תשובה מ-`ai.gateway.lovable.dev` עם
    `model: "google/gemini-2.5-flash"` (‏`stream: true`, פורמט OpenAI-
    compatible `choices[].delta.content`) — זה **באמת** "חכם" (LLM אמיתי
    עם קונטקסט מלא של המאגר בהנחיית מערכת). המדד המדויק: "חכם" תואם רק
    היכן שיש קריאת LLM בפועל, לא היכן שהמשתמש מרגיש שזה חכם.
68. **תוקנו שמונה המקומות שאינם LLM, ה-AI Chat נשאר ללא שינוי.** תיבות
    חיפוש → "חיפוש שיעורים" (מתאר את מה שקורה: סינון לפי שדות, לא בינה
    מלאכותית). כפתור/כותרת שמפנים ל-`/find-lesson` (שם יש בפועל 7 תפריטי
    סינון + חיפוש חופשי, `FindLesson.tsx:139-165`) → "חיפוש מתקדם"/"סינון
    מתקדם בזמן אמת" — תיאור כן, לא מוגזם, כי שם יש יותר ממסנן טקסט יחיד.
    ‏`ivr-agent` → `label` שונה מ"סוכן חכם" ל"חיפוש בטקסט חופשי", ה-`desc`
    ל"חילוץ מילות מפתח מטקסט חופשי" (מתאר את המימוש האמיתי: פיצול
    ל-keywords, לא ניתוח שפה). נמצא ותוקן גם מקום תשיעי שה-`grep` הראשוני
    (שמונה הקבצים שה-`DESIGN_SAMENESS.md` מנה) פספס: `AdminDashboard.tsx:968`
    מתאר את מערכת ה-IVR כ"...וסוכן חכם" בכרטיס ניהול — נשאר לא-מסונכרן עם
    השינוי ב-`IvrBuilder.tsx` אילו לא תוקן גם הוא. `FloatingChatBot.tsx:202`
    (הודעת-משתמש מדומה בתסריט צ׳אט) **לא נגע** — היא מתארת בדיוק את
    הצ׳אט שהיא יושבת בתוכו, וזה ה-LLM האמיתי.
69. **אימות בקריאה, לא בבנייה.** `grep` שיטתי אחרי העריכה: אפס מופעים
    נותרים של "חיפוש חכם"/"סוכן חכם" ב-`src/` מלבד `FloatingChatBot.tsx:202`
    (המכוון). אפס רגרסיה: תשעה קבצים, כל שינוי הוא מחרוזת טקסט בתוך
    JSX/אובייקט קיים — שום פונקציה, state, handler, endpoint, route או
    רכיב לא נוסף/הוסר/שונה בהתנהגותו. הפונקציונליות (סינון בפועל,
    קריאת ה-Edge Functions, ה-AI chat) זהה ביט-לביט לפני ואחרי; רק
    התווית שמתארת אותה זזה כדי להיות נכונה. **הבא בתור בפועל:** שרשור
    ה-`icon-decisions` המקורי של §6 (`zchuyot 24 → mthbram 17 → egod 15
    → galil 14`, ‏`DESIGN_SAMENESS.md` "עדכון 10/08/2026 (ח)/(ט)") — רק
    שני ליקויים מתוך הרשימה תוקנו ונפרסו (`zchuyot`); שלוש המערכות
    הנותרות (`mthbram`, `egod`, `galil`) עדיין לא נבדקו לפי הגלאים
    `doubleMark`/`splitGlyph`/`repeatInBand`/`floatingTwice` שנבנו
    בעדכונים (ה)–(ט). ובנפרד: `AIIcon` מוצג ליד תיבות החיפוש הלא-LLM
    ב-`FindLesson.tsx`/`FeaturedLessons.tsx` — לא טופל בסבב הזה (סימן
    ויזואלי, לא טקסט; שינוי אייקון מחוץ לתחום ה"מינוח" שהתבקש), אבל
    זו אותה אי-דיוק בצורה גרפית ולא מילולית, וראוי למדידה בסבב הבא.

## 19/08/2026 (סבב אוטונומי 3) — `mthbram`: תוקן האי-דיוק הגרפי המקביל — `AIIcon` (מוח+הילה, "AI-grade") הוחלף באייקון פשוט בשלושה מקומות לא-LLM

70. **המשכתי בדיוק מהפריט שהעדכון הקודם (#69) סימן כ"הבא בתור בפועל":**
    ‏`AIIcon` (מוגדר ב-`components/ui/AIIcon.tsx` כ-`BrainCircuit` + הילה
    מסתובבת + glow, המתועד במפורש כ"Premium AI icon... 'AI-grade' feel")
    מוצג ליד `FindLesson.tsx`/`FeaturedLessons.tsx` — שני העמודים שתוקן
    בהם הטקסט ב-#66-69 מ-"חכם" ל"חיפוש שיעורים"/"סינון מתקדם" כי הם
    סינון `.filter()`/`.includes()` בצד לקוח, **ללא** קריאת LLM. אותו
    אייקון מסמל AI בדיוק באותם מקומות שהטקסט כבר תוקן — אי-דיוק זהה,
    בצורה גרפית ולא מילולית.
71. **חיפוש שלישי, לא מנוי ב-#69, נמצא ותוקן גם הוא:** ב-`HeroSection.tsx`
    כרטיס הפעולה `featured: true` (הראשון מתוך ארבעה, מפנה ל-`/find-lesson`)
    מציג `<AIIcon variant="navy" />` במקום `action.icon` (=`Search`) שמוצג
    בשלושת הכרטיסים האחרים — אותה טעות בדיוק: אייקון AI על קישור לעמוד
    סינון רגיל.
72. **תוקנו שלושת המקומות, `AIIcon` נשאר בלי שינוי בכל מקום אחר.**
    ‏`FindLesson.tsx:178` ו-`FeaturedLessons.tsx:246` → אייקוני `Filter`/
    `Search` מ-`lucide-react` שכבר מיובאים ומשמשים באותם קבצים לשימושים
    דומים (`text-gold`, אותו גודל בקירוב). `HeroSection.tsx:171` → הוסרה
    ההסתעפות `action.featured ? <AIIcon/> : <action.icon/>` ובמקומה
    תמיד `<action.icon/>` (=`Search` עבור הכרטיס הזה), עם `text-navy`
    כש-`featured` כדי לשמר את אותה בחירת צבע שהיתה ל-`AIIcon variant=
    "navy"` (ניגודיות זהה לרקע `bg-gradient-teal`). לא נגעתי ב-`AIIcon`
    ב-`HeroSection.tsx:121` (תגית עליונה כללית ליד "שיעורי תורה ·
    חברותות · הרצאות" — קישוט מותג, לא תלוי בטענה על עמוד/תכונה ספציפיים),
    ב-`FloatingChatBot.tsx` (ה-LLM האמיתי, #67) או ב-`questionnaire/
    AIChatBot.tsx` (צ'אטבוט שאלון — לא נבדק בסבב הזה, לא נטען שהוא
    לא-LLM, מחוץ לתחום ששסומן ב-#69).
73. **אפס רגרסיה, אומת בקריאה:** שלושה קבצים, כל שינוי הוא JSX
    דקורטיבי (`aria-hidden` ב-`AIIcon` המקורי) — אין state, handler,
    route או endpoint שנגע בו; ה-`import AIIcon` הוסר מ-`FindLesson.tsx`
    ו-`FeaturedLessons.tsx` בלבד (איפה שהפך לבלתי-מנוצל), נשאר ב-
    `HeroSection.tsx` (עדיין בשימוש בשורה 121). **הבא בתור בפועל:**
    שרשור `icon-decisions` המקורי (`mthbram`/`egod`/`galil` מול הגלאים
    `doubleMark`/`splitGlyph`/`repeatInBand`/`floatingTwice`, #69) עדיין
    פתוח ולא נגע בו הסבב הזה.

## 19/08/2026 (סבב אוטונומי 4) — התור שנתן #70-73 היה שגוי: `mthbram`/`egod`/`galil` כבר נסגרו לפני שהוא נכתב

74. **בדקתי את התור לפני שביצעתי אותו, ומצאתי שהוא כבר בוצע.** #73 (וגם #66-69
    לפניו) הצביע על "שרשור `icon-decisions` המקורי (`mthbram`/`egod`/`galil`
    מול ארבעת הגלאים) — עדיין פתוח" כ"הבא בתור בפועל". לפני שהרצתי אותו קראתי
    את `SYSTEMS_STATUS.md` (לא רק את `QA/platform/DESIGN_SAMENESS.md`, שממנו
    הועתק התור) ומצאתי שני עדכונים מוקדמים יותר **מאותו יום**: `+34` (‏17:35,
    קומיט `70b27f70`) אימת בפרודקשן בפועל ש-`egod`/`mthbram`/`galil` כבר
    פרוסים ותקינים (‏`svg.lucide-lightbulb`=1, ‏`svg.lucide-heart`=1,
    ‏`landmark`/`shield-check`/`bot`/`message-circle`(וואטסאפ בלבד)=1 ב-galil —
    תואם קומיט `757260ff`), ו-`+36` (מאוחר יותר) סגר גם את `smel` (0 ליקויים
    בארבעת הגלאים) והכריז במפורש: "סבב הקריאה zchuyot→egod→galil→mthbram→smel
    על תשע המערכות המלאות הושלם." ‏`chatzor` נבדק ותוקן עוד ב-10/08 (קומיט
    `9b08f738`) ואומת כאן שהתיקון עדיין בקוד החי (`SynagogueCard.tsx:54`,
    `s.address &&`). ‏`imud`/`gesher` ירדו מסדר העבודה כבר בעדכון (ה) (8/1
    אייקונים בלבד). כלומר **כל תשע המערכות נבדקו וסגורות** — התור שסבבים
    `72dd0227` ו-`8a8cea7f` המשיכו ממנו (ובעצמי כמעט המשכתי ממנו) היה תיעוד
    שלא סונכרן מול המקור האמיתי (`git log`/`SYSTEMS_STATUS.md`), לא ממצא.
75. **ניסיתי קודם להריץ את `icon-decisions.mjs` בעצמי כדי לאמת מחדש, ונתקעתי
    על תלות סביבה — לא המשכתי לפתור אותה עד הסוף.** הסקריפט דורש
    `playwright-core` (לא מותקן ב-`more30/`, קיים רק ב-`/tmp/qa`) ונתיב chrome
    קשיח ל-Windows (‏`C:\Users\USER\...`). קישרתי סימלינק
    (`node_modules/{playwright,playwright-core}` → `/tmp/qa/node_modules/…`,
    לא ב-git, ‏`node_modules/` ב-`.gitignore`) ותיקנתי את `EXE` לבחור נתיב
    Linux כש-`process.platform==='linux'` וקיים (נשאר בקוד — תיקון תאימות
    תמים, נופל בחזרה לנתיב המקורי אם הנתיב הלינוקסי לא קיים). זה עדיין נכשל:
    ‏`chrome-linux64/chrome: error while loading shared libraries: libatk-1.0.so.0`
    — חסרות ספריות מערכת (‏`apt install`), וזה מעבר ל"עריכת מקור ממוקדת בלי
    להתקין" שההנחיה אסרה. **לכן אימתתי בקריאת קוד + היסטוריית git במקום
    בהרצת הכלי** — בדיוק כמו שסבבים קודמים עשו כשה-Playwright לא היה זמין.
76. **התיקון: עדכנתי את `QA/platform/DESIGN_SAMENESS.md` בשתי נקודות שבהן
    התור השגוי מופיע, לא מחקתי — כדי שהשרשור המקורי יישאר קריא.** שתי הערות
    "⚠️ תוקן" נוספו (לא ברשומה, ליד ה"הבא בתור" עצמו) עם הראיות המלאות
    (‏`SYSTEMS_STATUS.md` +34/+36, קומיטים `70b27f70`/`9b08f738`) ומסקנה אחת:
    הפתוח היחיד שנשאר ב-`core.issues #5` הוא הפער המבני הרחב (9 מערכות עם
    ערכת-הרכיבים המלאה מול 9 בלעדיה) — סבב נפרד וגדול משמעותית, לא צעד יחיד.
    בדקתי גם את שדה `detail` של `core.issues #5` עצמו מול Supabase — הוא כבר
    מעודכן ומדויק (מכיל את אותה מסקנה, נכתב ב-19/08 בסבב ה-`+36`), ולכן לא
    נגעתי בו; רק `DESIGN_SAMENESS.md` היה מיושן. אפס רגרסיה: שינוי תיעוד
    בלבד (שני קבצי `.md`) + תיקון תאימות חד-שורתי ב-`.mjs` שלא נבדק בפועל
    (חסימת ספריות מערכת) אך אינו יכול לשנות התנהגות קיימת — נתיב Windows
    המקורי עדיין ברירת המחדל בכל סביבה שאינה לינוקס-עם-קאש-תואם. **הבא בתור
    בפועל:** אין עוד "הבא בתור" חד-צעדי בשרשור עיצוב-הייחודיות (#6/§6); מה
    שנותר (9-מול-9) דורש סבב תכנון נפרד. סבב הבא צריך לחזור לסדר העדיפויות
    המקורי (0-8) ולבדוק מחדש איזה מהם עדיין באמת פתוח, לא להניח.

## 19/08/2026 (סבב אוטונומי B, ראשון) — `gesher` (31): "יומן משימות חכם" היה טענת AI כוזבת על לוח קנבן ידני לגמרי

77. **התחלתי מתחימת חדשה: לופ B מוגבל למערכות 17-31 + השכפול התפעולי של
    בקלות (37) + design-system (29) + studio (26), ואסור לו לגעת ב-01-16/בפורטל/
    באדמין המשותפים — לופ A בעליהן.** קראתי README.md/CONNECTIONS.md (מתארים
    שלד ישן; המצב האמיתי בענף `feat/orech-admin-screen-0819`, כאן ה-HEAD
    האמיתי של כל העבודה, גם אם `origin/claude/build-monorepo-more30-peilok`
    לא זז מ-17/07). בדקתי `core.run_progress` למחרוזת "loop B" — אין רשומה
    קודמת; זו הריצה הראשונה עם התיוג הזה. סרקתי את `core.issues` הפתוחות
    בתחום 17-31/37: `mechiron` #115 חסום על #94 (החלטת מוצר של המשתמש, עדיין
    פתוחה); `galil` #62 (קריטי) חסום על גישה לפרויקט `mwljkonwdeuaahsigjdp`
    שאינו בין הפרויקטים שה-PAT מכסה — נבדק שוב ונשאר חסום, כמו בסבבים
    הקודמים. שני אלה נותרו בלי נגיעה.
78. **המשכתי בשרשור המדויק שהצעד הקודם (#76) הכריז שממתין: "לחזור לסדר
    העדיפויות המקורי ולבדוק מחדש מה עדיין פתוח" — בתוך `core.issues #5`
    (עיצוב ייחודי) אבל בזווית שלא נבדקה: תיוג-AI כוזב, לא צמצום-קיט אייקונים.**
    `gesher` (31, hebrew-bridge-crm) מופיעה ברשימת "9 מערכות עם הקיט המלא"
    ב-`DESIGN_SAMENESS.md` אבל ירדה מסדר עבודת האייקונים כבר ב-10/08 (1
    החלטה בלבד, נקייה) ומעולם לא נבדקה בזווית ה-AI-labeling שתוקנה שוב ושוב
    ב-`mthbram` (סבבים `72dd0227`/`8a8cea7f`, שני לופים קודמים). גרפתי
    "חכם"/AI/Sparkles/BrainCircuit על פני כל `apps/31-hebrew-bridge-crm/src`
    ומצאתי מופע יחיד: `routes/_authenticated/admin/tasks.tsx:40`, כותרת
    "יומן משימות **חכם**". קראתי את הרכיב מאחוריה (`TasksBody`→`TasksKanban`)
    ו-`AddTaskDialog`, וגם גרפתי `openai|anthropic|gemini|gateway.lovable|
    generateText|chat/completions|llm` על פני `src/` ו-`supabase/` של האפליקציה
    — אפס תוצאות. זה לוח קנבן ידני לחלוטין: סטטוס/עדיפות משתנים רק בלחיצת
    תפריט נגרר של המשתמש, אין שום קריאת LLM/היוריסטיקה אוטומטית בשום מקום.
    חיזוק נוסף: `route.tsx:9` (הניווט הצדדי) כבר מתייג את אותו עמוד "יומן
    משימות" — בלי "חכם" — כלומר אפילו בתוך האפליקציה עצמה הכותרת בעמוד
    הייתה חורגת מהתווית המדויקת שהניווט כבר נושא, לא רק ממה שה-UI עושה
    בפועל.
79. **התיקון: מחרוזת אחת, `tasks.tsx:40` — "יומן משימות חכם" ← "יומן משימות"**,
    עכשיו תואמת בדיוק לתווית הניווט (`route.tsx:9`). שום state/handler/route/
    endpoint לא נגע; `TasksKanban`/`AddTaskDialog`/הפונקציונליות זהים ביט-לביט
    לפני ואחרי. אומת בגרפ אחרי העריכה: אפס מופעים של "חכם" ב-`tasks.tsx`.
    זו אותה שיטת תיקון בדיוק כמו `mthbram` (#66-69, #70-73): לא ממציאים
    יכולת AI כדי להצדיק את הכותרת, ולא מוחקים תכונה — רק מתקנים תיאור שחורג
    ממה שהמוצר באמת עושה. קובץ יחיד, שורה יחידה, אפס רגרסיה.

## 19/08/2026 (סבב אוטונומי B, שני) — `bkalot-studio` (37): לשכפול התפעולי של בקלות לא היה אף מסלול מחיר משלו

80. **מיפיתי את כל המסלולים (§ per-system pricing) בתחום 17-31/37/29/26 מול
    `core.plans`, לפני שבחרתי צעד.** `core.projects.path` נותן את מפתח
    ה-app שה-RPC `public.more30_system_page` (שמזין את `system.html`)
    משתמש בו לשלוף `core.plans`. מתוך אחד-עשרה המערכות החיות והציבוריות
    בתחום (`chizukim`/17, `orech`/18, `mthbram`/21, `zchuyot`/22, `galil`/24,
    `studio`/26, `mechiron`/27, `kupot`/28, `crm`/30, `gesher`/31,
    `bkalot-studio`/37) — עשר כבר נושאות מסלולים משלהן. רק `bkalot-studio`
    (37, `bkalot-clone`) לא נשאה אף שורה. קראתי את `more30_system_page`:
    זה **לא** באג שבור — כשאין שורות ל-app_key מסוים, ה-RPC נופל בכוונה
    למסלולי `app_key='more30'` (חינם/₪10, `plans_from='more30'`) — אבל
    התוצאה גנרית: לקוח שנכנס ל-`more30.com/bkalot-studio` רואה מחירון
    שלא קשור למוצר, בזמן ש-`bkalot` (10, `bkalot-rights`) — המערכת
    שה-clone הזה מעתיק במלואה לפי `audit_status` שלה ב-`core.projects`
    ("עותק עצמאי מלא של בקלות התפעולית") — כן נושאת מסלולים אמיתיים
    (חינם/₪2/₪5).
81. **שקלתי גם "החל פלטה על הפרויקט" ב-`studio` (26, `core.projects.audit_gaps`
    #4) ודחיתי אותה לסבב שידע לבדוק ויזואלית.** חיבור פאנל "המותגים שלי"
    למודל השכבות/הצבע של העורך (`Editor.tsx`) הוא שינוי שמשפיע על ניגודיות
    טקסט-על-רקע בתבניות קיימות — לא ניתן לאמת "אפס רגרסיה" רק בקריאת קוד
    בלי דפדפן אמיתי, וההנחיה אוסרת שרת/דפדפן בסבב הזה. משאיר אותה כמו
    שסומנה במרשם: מעקב המשך נפרד.
82. **התיקון: מיגרציה אחת, תוספת בלבד — שישה מסלולים תחת `app_key=
    'bkalot-studio'`, זהים במבנה/מחיר/תיוג ל-`app_key='bkalot'`** (§ אותו
    מוצר, TEST MODE): `free`/`basic` ₪2/`extended` ₪5 גלויים ללקוח,
    ו-`charge`/`one_time`/`pro` (₪1, TEST MODE) פנימיים ללא שינוי, בדיוק
    כמו כל שאר האחיות בתחום. `supabase/migrations/0104_bkalot_studio_had_
    no_prices_of_its_own.sql`, `ON CONFLICT (app_key, code) DO NOTHING` —
    לא נכתב על אף שורה קיימת. אין שינוי קוד/UI. **אומת בקריאה חוזרת של
    ה-RPC עצמו** (`select public.more30_system_page('bkalot-studio')`):
    `plans` עכשיו מחזיר את שלושת המסלולים הגלויים של `bkalot-studio`
    (לא של `more30`), ו-`plans_from` עבר מ-`"more30"` ל-`"bkalot-studio"`.
    אפס רגרסיה: שורות חדשות בלבד בטבלה קיימת, שום קוד/RPC/מסלול אחר לא
    נגע. **הבא בתור בפועל:** "החל פלטה על הפרויקט" ב-`studio` (26) —
    דורש סבב עם יכולת בדיקה ויזואלית (דפדפן אמיתי), לא צעד להמשך בעיוור.

## 19/08/2026 (סבב אוטונומי B, שלישי) — `csjekrvukbdznetsrodj`: הסימון "❌ לא נגיש" ב-CONNECTIONS.md היה שקרי, ומאחוריו 15 טבלאות עם RLS כבוי

83. **בדקתי מחדש כל הנחת-בסיס לפני שחיפשתי צעד קוד, במקום לסמוך על תיעוד
    קודם.** `core.run_progress` הראה שהצעד הקודם (`e2efe5ea`, מסלולי מחיר
    ל-`bkalot-studio`) כבר סגר את פער התמחור היחיד שנותר בתחום; המיתוג
    ("עולם הסטארטאפים") נבדק ונמצא כבר מחובר בכל 11 המערכות החיות-ציבוריות
    בתחום דרך `portal/public/auth-button.js` (קרדיט פוטר משותף, לא נגיעה
    בפורטל עצמו — קריאה בלבד); `core.issues` הפתוחות בתחום (#62 galil, #94/
    #115 mechiron, #164 mthbram, #180 gesher) כולן חסומות כבר על החלטת
    משתמש/גישת פרויקט חיצונית, בלי שינוי מאז הסבב הקודם. שקלתי גם לחבר את
    `apps/37-bkalot-clone` ל-CSS המשותף של `apps/29-bkalot-design`
    (`bkalot-theme.css`, ה-"design-system 29" שהתחימה מזכירה במפורש) — אבל
    קריאת ה-`<style>` המוטבע של 37 חשפה זהות מכוונת ונפרדת ("ירוק-אזמרגד
    עמוק", לא הטורקיז/הזהב של 10/27), עם הערה מפורשת בקוד שהיא נבחרה כדי
    ש-37 לא ייראה כמו תבנית גנרית — חיבור ל-theme המשותף היה **מבטל** עבודת
    ייחוד קודמת, לא ממשיך אותה. `node build.js` ב-29 הרץ מקומית (בלי
    `upload.sh`, שדוחף ל-Storage ציבורי חי) ואומת זהה ל-`bkalot-theme.css`
    המחויב — אין שם דריפט לתקן.
84. **בדקתי גם אם יש מקום לאכוף מכסת-שימוש AI ב-`studio` (26) — audit_gaps
    מסמן "אין מכסת יצירות AI, כל יצירה עולה כסף אמיתי". תחקיר (Explore-agent)
    מצא: ששת נתיבי היצירה (`vercel-adapter/api/_lib/server/routes.ts`
    שורות 134/143/200/282/301/322/335) אף אחד מהם לא קורא ל-`getUserIdFromToken`
    היום; מנגד רוב התעבורה עדיין אנונימית (התחברות נוספה רק 19/08), כך שמכסה
    לפי משתמש מחובר בלבד הייתה מכסה חלק קטן מהחשיפה האמיתית ומשאירה את רוב
    הסיכון הכספי כמו שהוא — לא בחרתי לבנות תיקון חלקי שנשמע כמו סגירה של
    הפער אך אינו. נשאר פתוח, לא נבנה.
85. **בעודי מוודא מחדש שה-``❌`` שרשום ל-`csjekrvukbdznetsrodj` ב-CONNECTIONS.md
    (משרת 06/12/17/27) עדיין נכון — הוא לא היה.** `execute_sql`/`list_tables`
    הצליחו ישירות על הפרויקט הזה; `list_tables` החזיר גם אזהרת `rls_disabled`
    ברמת critical על 15 טבלאות: `tr_users` (עמודות `username`/`password`/
    `minutes_quota`/`minutes_used`, שורה חיה אחת) ו-`tr_sessions`
    (`token`/`user_id`/`expires_at`, 50 שורות חיות), פלוס `users`, `clients`,
    `service_submissions`, `automation_configs` (14 שורות), `delivery_queue`
    ועוד שש טבלאות `property_*`/`address_search_*`. כל אחד עם מפתח ה-anon
    של הפרויקט הזה יכול היום לקרוא/לכתוב כל שורה בהן. גרפ מלא על `apps/`
    לא מצא אף אזכור של `tr_users`/`tr_sessions` בקוד המצורף לריפו — מי
    שמשתמש בהן הוא backend שלא מיוצג כאן (כנראה `chizukim-transcribe`/17,
    לפי שמות העמודות ו-`minutes_quota` שתואם בדיוק לפער "אין מכסת דקות"
    שכבר מתועד ב-`core.projects.audit_gaps` של 17).
86. **לא תיקנתי RLS בעצמי — בכוונה, לא מחוסר יכולת.** ה-advisory של Supabase
    עצמו מזהיר לא להחיל `ENABLE ROW LEVEL SECURITY` בלי policies תואמות:
    זה חוסם גישה מוחלטת (anon **וגם** authenticated) מרגע ההפעלה, בכל
    טבלה שאין לה policy — בדיוק סוג הרגרסיה ש"אפס רגרסיה"/"אף פעם לא
    לשבור חיבור קיים" אוסרים, ובלי גישה לקוד ה-backend האמיתי (לא
    מיוצג בריפו) אין דרך לדעת אילו נתיבי anon/authenticated קריאה-לגיטימית
    היו נשברים. מעבר לכך, הפרויקט משותף עם 06 (kupot-holim) ו-12
    (smel-ndln) — שני אלה בבעלות לופ A, לא לופ B — כך ששינוי חד-צדדי
    מכאן היה עלול לפגוע במערכות מחוץ לתחום הסבב הזה גם אם היה בטוח.
    **התיקון בפועל:** תיקנתי את השורה השקרית ב-CONNECTIONS.md
    (`❌` ← `✅ yes`, עם הערה לא להניח שוב בלי לבדוק), הוספתי סעיף
    "🚨 Security finding" עם כל הראיות והנימוק לאי-התיקון, ופתחתי
    `core.issues #245` (severity=critical, owner=user, blocked_on=
    "הכרעת policy לכל טבלה + תיאום בין הלופים"). אפס רגרסיה: שינוי
    תיעוד + שורת DB חדשה בלבד, שום קוד/סכימה/הרשאה קיימת לא נגעה.
    **הבא בתור בפועל:** ברגע שתתקבל הכרעת ה-policy מהמשתמש על #245,
    ה-`minutes_quota`/`minutes_used` שכבר קיימות ב-`tr_users` עשויות
    לפתור גם את פער "אין מכסת דקות" של `chizukim`/17 (audit_gaps) בלי
    לבנות תשתית חדשה — אבל זה תלוי במציאת/גישה לקוד ה-backend שכן
    משתמש בטבלאות האלה, שלא אותר בריפו הזה.

## 19/08/2026 (סבב אוטונומי B, רביעי) — `studio` (26): מכסת ה-AI שנדחתה כתיקון חלקי ב-19/08 (א), נבנתה מחדש לפי IP במקום לפי משתמש

87. **חזרתי לפער שנפתח בכוונה בסבב הקודם (`#84` למעלה) במקום לבחור צעד חדש
    לגמרי.** מיפוי מחדש של כל מה שנשאר פתוח בתחום 17-31/37/29/26 — תמחור
    (נסגר, `#80-82`), מיתוג (נבדק מחובר בכל 11 המערכות, `#83`), RLS של
    `csjekrvukbdznetsrodj` (חסום על הכרעת משתמש, `#245`) — לא הותיר עוד
    צעד קוד עצמאי בתחום חוץ מהמכסה שנדחתה. הדחייה ב-`#84` הייתה מפורשת:
    "לא בחרתי לבנות תיקון חלקי שנשמע כמו סגירה של הפער אך אינו" — הסיבה
    הייתה שמכסה לפי `getUserIdFromToken` הייתה מכסה רק תעבורה מחוברת, ורוב
    התעבורה עדיין אנונימית. זה עדיין נכון — אבל הפתרון לכך הוא מכסה לפי
    IP, לא ויתור על מכסה בכלל.
88. **התיקון: מכסה יומית לפי כתובת IP מגובבת (לא לפי משתמש), על כל שמונת
    נתיבי היצירה שעולים כסף אמיתי** ב-`vercel-adapter/api/_lib/server/
    routes.ts` — `/api/brief/concepts`, `/api/ai/copy`, `/api/ai/critique`,
    `/api/ai/background`, `/api/branding/strategy`, `/api/branding/logo`,
    `/api/branding/vectorize`, `/api/branding/remove-background` (קריאה
    חוזרת של הקובץ מצאה שמונה נתיבים, לא שישה כפי שנרשם ב-`#84` — שני
    הנתיבים שהוחמצו: `/api/branding/vectorize` ו-`/api/branding/remove-
    background`, שניהם קוראים ל-Recraft בתשלום). מכסה לפי IP חלה באותה
    מידה על אנונימי ועל מחובר — בדיוק מה שהתיקון הקודם לא היה מכסה — בלי
    לדרוש התחברות כתנאי-סף, שהייתה שוברת את הזרימה האנונימית הקיימת
    (רגרסיה אסורה).
89. **תשתית: טבלה + פונקציית "עלה-וקרא" אטומית ב-Postgres, לא ב-JS.**
    `supabase/migrations/0105_studio_ai_generation_had_no_daily_ceiling.sql`
    מוסיפה `public.studio_ai_rate_limits` (‎`ip_hash`, `day`, `count`,
    ‏`primary key (ip_hash, day)`) ו-`public.studio_bump_ai_rate_limit(
    p_ip_hash, p_limit)` — `insert ... on conflict ... do update set count =
    count + 1 returning count` בתוך שאילתה אחת, כדי שקריאות מקבילות מאותה
    IP לא יחמקו בין קריאה לכתיבה (race על ה-count). הרצתי את הפונקציה
    פעמיים ידנית מול הפרויקט החי לפני שחיברתי אליה קוד (`count: 1→2`,
    ‏`allowed: true` בשתיהן, מתחת לסף 40) ומחקתי את שורת הבדיקה אחרי.
    ‏RLS דלוק בלי policies — בדיוק כמו `studio_projects`/`studio_brands`/
    ‏`studio_users` — כך שרק `service_role` (עוקף RLS) קורא/כותב, ולא
    ניתנה הרשאת `execute` מפורשת ל-`anon`/`authenticated` על הפונקציה.
    ‏`storage.ts` מייצא `bumpAiRateLimit(ipHash, limit)` שעוטף את הקריאה
    ב-`try/catch` — **פותח (fail-open) בתקלת רשת/DB**: תקלת תשתית במכסה
    עצמה לא אמורה לחסום יצירה אמיתית של משתמש בתוך הגבול. `routes.ts`
    מוסיף `enforceAiQuota(req, res)` (IP → `sha256` → `bumpAiRateLimit`,
    ‏429 עברית עם מונה/סף אם נחסם) כשורה ראשונה בכל אחד משמונת ה-handlers.
    ‏40/יום נבחר כסף הגנה מפני ניצול-לרעה (בוט/סקריפט), לא כמסלול מנוי —
    משתמש אמיתי שבודק את המוצר לא יגיע לזה באותו יום.
90. **אפס רגרסיה: קובץ מיגרציה חדש (טבלה+פונקציה חדשות בלבד, שום שינוי על
    שורה/טבלה קיימת) ושתי תוספות קוד נקודתיות (`storage.ts`+`routes.ts`
    של `vercel-adapter` בלבד — `apps/26-modaot-studio/server/*`, מקור
    ה-SQLite המקומי שאינו הפריסה החיה לפי `storage.ts` עצמו, לא נגע).**
    כל handler ממשיך לעבוד בדיוק כמו קודם עד 40 קריאות ליום מאותה IP;
    מעבר לסף מחזיר 429 עם הודעה ברורה, לא כשל שקט. לא נגעתי במסלולי
    התמחור/CRUD/`getUserIdFromToken` הקיימים. עדכנתי את `core.projects.
    audit_gaps` של 26 (סעיף 2) לשקף שהתיקון קיים ומה שנשאר (אין עדיין
    מסלול מנוי — 40/יום הוא הגנה מניצול-לרעה, לא מודל עסקי). **הבא בתור
    בפועל:** ה-webhook/checkout האמיתי בסביבת TEST — כל 11 המערכות
    החיות-ציבוריות בתחום נושאות מסלולי מחיר ב-`core.plans` אבל אף אחת לא
    מחוברת לספק סליקה בפועל (`core.issues #28`, חסום על אישורי טסט של
    ספק — החלטת משתמש, לא צעד קוד).

## 19/08/2026 (סבב אוטונומי B, חמישי) — `18 orech`: העלאה מרובת קבצים לספר שלם, ותיקון audit_gaps מיושן

91. **סקרתי מחדש את הפערים הפתוחים של 17/18/22/28 (סוכן Explore) לפני
    שבחרתי צעד, כי #28 בסבב הקודם כבר סגר את התמחור/מיתוג/RLS-חסום.**
    המסקנה: ל-17 ול-28 אין שום התחברות משתמש-קצה בקוד המיוצר (רק
    admin-cookie), כך שהפערים שנותרו אצלם (מכסת דקות, מנוי יועצים, אזור
    אישי) דורשים תשתית אימות חדשה — לא צעד קטן וממוקד. ל-22 התברר
    ש"אין מעקב סטטוס לפונה" (audit_gaps #2) **כבר קיים במלואו** בקוד
    (`AdminLeads.tsx`: עמודת `status`+`statusConfig`+`handleStatusChange`+
    סינון, מיגרציה `20260302220017_...sql`) — מדד מיושן, לא פער אמיתי.
92. **ל-18 (`torah-editor-mvp`) התברר אותו דבר לגבי audit_gaps #1** ("אין
    התחברות ולכן אין הפרדת עבודות") — `app/login/page.tsx` קיים,
    `lib/hub.ts` מגדיר לקוח האב עם `storageKey: 'more30-auth'` (אותו
    session-key כמו ה-pill המשותף), ו-`app/documents/page.tsx` כבר דורש
    `auth.getUser()` וכותב/קורא לפי `user_id`. אימתתי חי מול הפרויקט
    (`select relrowsecurity from pg_class where relname='orech_documents'`
    → `true`) — RLS דלוק בפועל על הטבלה, לא רק בקוד. זה סוגר גם את
    "הפרדת עבודות בין משתמשים" בפועל.
93. **הפער האמיתי שכן נשאר ב-18: audit_gaps #4, "אין העלאה מרובת דפים
    (batch) לספר שלם".** קריאת `app/documents/page.tsx` הראתה בדיוק
    למה: `handleFiles` תמיד לקח רק `files[0]`, ואזור ההעלאה לא נשא
    `multiple`. זה צעד קטן, ממוקד, בקוד אמיתי (לא manifest-בלבד),
    בלי תלות באימות/סליקה/שער-עסקי — התאמה מדויקת לרף "BUILDABLE NOW".
    **התיקון:** פירקתי את לוגיקת-הקריאה לפונקציה `ingestFile()` אחת
    (מייצרת מסמך יחיד ומחזירה מזהה), והוספתי `multiple` ל-`<input
    type=file>`; `handleFiles` עכשיו רץ ברצף (לא במקביל, כדי לא להציף
    את `/orech/api/extract`) על כל קובץ שנבחר/נגרר, עם דיווח התקדמות
    "קובץ X מתוך Y" וריכוז שגיאות לקבצים שנכשלו בלי לעצור את שאר האצווה.
    קובץ בודד ממשיך להתנהג בדיוק כמו קודם (קפיצה ישירה לעורך) — אפס
    רגרסיה בזרימה הקיימת. `8ed5e8ce`, `feat/b-orech-batch-upload-0819`.
94. **עדכנתי את `core.projects.audit_gaps` של 18** לשקף ששני הסעיפים
    (#1 מיושן, #4 תוקן) סגורים, כדי שסבב עתידי לא ינסה לבנות מחדש דבר
    שכבר קיים. נותר פתוח: #2 (מכסת עמודים/דפים — תלוי הכרעת תמחור), #3
    (סליקה), #5 (ייצוא Word/PDF מוגמר — עדיין לא נבנה, יעד סביר להמשך).
    אפס רגרסיה: שינוי קוד יחיד וממוקד בקובץ אחד + עדכון מטא-דאטה בלבד,
    שום מסלול/RLS/אימות קיים לא נגע.

## 19/08/2026 (סבב אוטונומי B, שישי) — `18 orech`: ייצוא Word (audit_gaps #5, שנדחה כ"יעד סביר להמשך" בסבב הקודם), ותיקון audit_gaps מיושן נוסף ב-22

95. **חזרתי בדיוק לפער שהסבב הקודם (#94) סימן כ"יעד סביר להמשך" — audit_gaps
    #5 של 18, "אין ייצוא Word/PDF מוגמר".** קריאת `app/editor/page.tsx` גילתה
    שהחצי השני של הטענה כבר שקרי: `downloadPdf()` כבר קיים ועובד (חלון הדפסה
    מעוצב, בדיוק אותו דפוס שכבר חי ב-17/27/28) — הפער האמיתי היחיד שנותר
    הוא ייצוא Word, שלא היה קיים בכלל (רק `.txt` ו-PDF).
96. **שקלתי לשכפל את דפוס ה-`docx` npm (Document/Packer/Paragraph) שכבר חי
    ב-`17-chizukim-transcribe/client/src/lib/export.ts` — אבל ויתרתי בכוונה.**
    זה היה דורש להוסיף `docx`+`file-saver`+`@types/file-saver` ל-
    `package.json` של 18, שאין לו lockfile נפרד (נעול ב-`pnpm-lock.yaml`
    השורשי). Vercel מריץ `pnpm install` בסביבת CI, ו-pnpm מזהה CI ועובר
    ל-`--frozen-lockfile` כברירת מחדל — כלומר תלות חדשה שלא נפתרה בלוקפייל
    הייתה **שוברת את ה-build בפריסה הבאה**. הנחיית הריצה של הסבב הזה
    אוסרת "do NOT wait on installs", כך שלא הרצתי `pnpm install` כדי לעדכן
    את הלוקפייל — משמע התלות החדשה הייתה אסורה בפועל, לא רק לא רצויה.
97. **הפתרון: ייצוא Word בלי שום תלות npm חדשה, באותו דפוס Blob-download
    שכבר קיים ל-`.txt`.** `downloadDocx()` חדש ב-`app/editor/page.tsx`
    בונה מחרוזת HTML עם namespace-י `mso`/`w` (הטריק הסטנדרטי
    "Word HTML" — `application/msword` MIME + הערת XML מותנית ל-Word) ומוריד
    כ-`.doc`; Word/LibreOffice/Google Docs פותחים אותו נכון, כולל RTL מלא
    (`dir="rtl"`, `direction:rtl` ב-CSS). כפתור "הורד כ-Word" נוסף לסרגל
    הכלים ליד PDF/TXT הקיימים, עם אותו תנאי `disabled={!text}`. משתמש
    ב-`esc()` ו-`titleFrom()` הקיימים בקובץ — שום ייבוא/תלות חדשה.
98. **אפס רגרסיה: קובץ יחיד, תוספת נקודתית (פונקציה+כפתור), שום שינוי
    ב-`package.json`/`pnpm-lock.yaml`/במסלולי ה-PDF/TXT הקיימים.** עדכנתי
    את `core.projects.audit_gaps` של 18 לשקף ש-#5 סגור וש-#2/#3 (מכסה,
    סליקה) עדיין תלויים בהכרעת תמחור/ספק — לא צעדי קוד.
99. **תוך כדי סקירת הפערים הפתוחים בתחום מצאתי שורת `audit_gaps` מיושנת
    נוספת שלא תוקנה בפועל: #2 של 22 (`get-your-rights`), "אין מעקב סטטוס
    לפונה".** סבב קודם (#91) כבר מצא וקבע בהחלטה שזה מדד מיושן — מעקב
    סטטוס מלא קיים ב-`AdminLeads.tsx` (`statusConfig`+`handleStatusChange`+
    סינון+מונים, מיגרציה `20260302220017_...sql`) — אבל שורת ה-DB עצמה
    מעולם לא עודכנה כדי לשקף זאת, ואומתה שוב עכשיו (`grep` על הקובץ) לפני
    התיקון. תיקנתי רק את הטקסט בסעיף #2 (`replace` ממוקד, שאר הסעיפים
    בשורה לא נגעו) — אין כאן שינוי קוד, רק תיקון מטא-דאטה כדי שסבב עתידי
    לא ינסה לבנות מחדש מעקב סטטוס שכבר קיים.

## 19/08/2026 (סבב אוטונומי B, שביעי) — `17 chizukim`: ייצוא כתוביות אמיתי (SRT/VTT) מחותמות הזמן שכבר קיימות ב-DB

100. **סקרתי מחדש את audit_gaps של 17 (`chizukim-transcribe`) לפני שבחרתי צעד.**
    סעיף #4 אמר "אין ייצוא מתקדם (Word/כתוביות)" — קריאת `export.ts` הראתה
    שזה חלקית מיושן: ייצוא Word (`exportWord`, ספריית `docx`) וייצוא PDF
    (`exportPdf`, חלון הדפסה) כבר קיימים וחיים בעמוד ההקלטה. הפער האמיתי
    היחיד שנותר: כתוביות (SRT/VTT) — לא היה קיים בכלל.
101. **בדקתי אם יש בכלל נתוני חותמות-זמן לפני שבניתי משהו על בסיס הנחה.**
    `server/transcribe.ts`/`routes.ts` כבר מבקשים `response_format=
    verbose_json` מ-Whisper ושומרים `segments` (`{start,end,text}[]`) לעמודת
    `recordings.segments` (`patchRecording` עם `segments: segments`) — אבל
    שום קוד לקוח לא קרא את זה (חסר מ-`Recording` interface וב-`LIST_COLUMNS`,
    אף שה-`select=*` של `fetchRecording` כבר מחזיר אותו). אימתתי חי מול
    `csjekrvukbdznetsrodj`: **1,102 מתוך 1,140 הקלטות אמת** כבר נושאות
    `segments` לא-ריקים — נתון אמיתי קיים, לא צריך תמלול מחדש.
102. **התיקון: `exportSubtitles(r, "srt"|"vtt")` חדש ב-`export.ts`**, בונה
    קובץ SRT/VTT תקני מ-`recordings.segments` (עיצוב `HH:MM:SS,mmm`/
    `HH:MM:SS.mmm` לפי הפורמט) ומוריד אותו באותו דפוס `saveAs` הקיים —
    בלי תלות npm חדשה (`file-saver` כבר מיובא בקובץ). הוספתי `segments`
    ל-`Recording` interface ב-`supabase.ts` (טיפוס בלבד — העמודה כבר
    הוחזרה מה-API), ושני כפתורים ("כתוביות SRT"/"כתוביות VTT", אייקון
    `Captions` מ-`lucide-react`, קיים ב-0.453 המותקנת) ליד Word/PDF בכרטיס
    התמלול הגולמי ב-`recording-detail.tsx` — מנוטרלים (`disabled`) אם
    להקלטה ספציפית אין `segments` (38 הקלטות ישנות בלי חותמות זמן).
103. **אפס רגרסיה: קובץ חדש בתוך `export.ts` הקיים + הרחבת טיפוס + שני
    כפתורים נוספים — שום שינוי בזרימת Word/PDF/עריכה/שמירה הקיימת, שום
    מיגרציה (העמודה כבר קיימת וכבר נכתבת ע"י ה-server).** עדכנתי את
    `core.projects.audit_gaps` של 17 (סעיף #4) לשקף ש-Word כבר היה קיים,
    שכתוביות נבנו בפועל, ושנשאר פתוח רק שער תשלום סביב הייצוא (תלוי
    הכרעת תמחור/סליקה, לא צעד קוד). נותר פתוח: #1 (הרשמה/הגבלה/תשלום),
    #2 (אזור אישי — תלוי תשתית אימות שלא קיימת עדיין ב-17), #3 (מכסת
    דקות — תלוי אותה תשתית אימות).

## 19/08/2026 (סבב אוטונומי B, שמיני) — `31 gesher`: כתובת ה-webhook הנכנס בעמוד האינטגרציות הצביעה על preview של Lovable, לא על המערכת החיה

104. **קראתי מחדש את README.md/CONNECTIONS.md, בדקתי `core.run_progress`
    (הרשומה האחרונה של loop B הייתה קומיט a73e629b, כתוביות 17) ואת
    `core.projects`/`core.issues` לתחום 17-31/37/29/26 לפני שבחרתי צעד.
    כמעט כל הפערים הפתוחים כבר תלויים בהכרעת תמחור/סליקה/אימות (17, 18,
    22) או נבדקו ונסגרו בסבבים קודמים היום (26 סטודיו, 28 קופות, 37, 29).
    הפעלתי סוכן Explore על המערכות שעדיין לא נסרקו לעומק היום (19/20/21/
    23/25/27/30/31) לאיתור פער קוד קטן ואמיתי — החזיר מועמד יחיד אמיתי:
    `apps/31-hebrew-bridge-crm/src/routes/_authenticated/admin/integrations.tsx:88`
    הכיל כתובת מוחלטת קבועה בקוד לפרויקט Lovable ("project--a27fd5fd-...
    lovable.app/api/public/webhooks/incoming") כ"כתובת קבלת Webhook נכנס"
    שמוצגת למנהל להעתקה ל-n8n.
105. **אימתתי שזה באג אמיתי ולא רק "hardcoded אבל תקין":** הנתיב
    `/api/public/webhooks/incoming` מוגדר בפועל *באותה אפליקציה עצמה*
    (`src/routes/api/public/webhooks/incoming.ts`, מאמת `X-API-Key` מול
    `INCOMING_WEBHOOK_API_KEY`) — כלומר בייצור הוא נגיש תחת
    `more30.com/gesher/api/public/webhooks/incoming` (vite `base:
    "/gesher/"`), לא תחת דומיין ה-Lovable שמוצג. מנהל שמעתיק את הכתובת
    המוצגת ומדביק אותה ב-n8n מקבל כתובת ל-preview שאולי כבר לא קיים או
    לא מסונכרן, במקום ליעד האמיתי בפרודקשן.
106. **זו בדיוק אותה מחלקת-באג שכבר תועדה ותוקנה ב-`core.issues #201`**
    (איפוס סיסמה): "`window.location.origin` לבדו הוא הבאג שמערכת 30
    נפלה בו — האפליקציה מוגשת תחת `/gesher`, וקישור שנבנה מ-origin בלבד
    נשלח לכתובת שאין בה כלום." אותו קובץ (`auth.tsx`) כבר מגדיר תבנית
    מתוקנת ל-31 עצמה: `appUrl = (path) =>
    \`${window.location.origin}${import.meta.env.BASE_URL}${path}\``,
    ומתועד שם ש-31 אכן נפלה בבאג הזה בעבר בשני מקומות נוספים (emailRedirectTo
    /redirect_uri) ותוקנה. הכתובת הקבועה ב-integrations.tsx היא אותו דפוס
    בדיוק שנשאר לא מתוקן במקום רביעי, בקובץ אחר.
107. **התיקון: `incomingUrl` הפך מקבוע-מחרוזת ל-`useState`+`useEffect`**
    שמחשב `window.location.origin + import.meta.env.BASE_URL +
    "api/public/webhooks/incoming"` (עם אותו `replace` לניקוי `//` כפולים
    כמו ב-`appUrl`), באותו דפוס בדיוק כמו ה-state הקיים של `url`/
    `outbound_url` בקובץ הזה (state ריק שמתמלא ב-effect אחרי ה-mount —
    לא בזמן ה-render, כדי ש-`window` לא יפיל SSR). שום שינוי בלוגיקת
    השמירה/התור/ה-retry הקיימת. אפס רגרסיה: קובץ יחיד, שינוי ממוקד בשדה
    תצוגה בלבד (readOnly, מוצג להעתקה), שום נתיב/הרשאה/סכימה נגע.
108. **בדקתי גם את audit_gaps של 28 קופות (#3, "אין דו״ח השוואה אישי
    ב-PDF") ומצאתי שהוא מיושן** — `server/hf-report.ts`/`client/src/pages/
    home.tsx` (כפתור "הורדת דוח PDF") כבר בנויים ופרוסים בייצור (קומיט
    76eeb5f3, "deployed"). עדכנתי את `core.projects.audit_gaps` של 28
    (סעיף #3 בלבד, שאר הסעיפים לא נגעו) כדי שסבב עתידי לא ינסה לבנות
    מחדש דוח שכבר קיים.

## 19/08/2026 (סבב אוטונומי B, תשיעי) — `30 zchuyotpro-crm`: כפתור "הורדה" במסך המסמכים היה זהה בפועל לכפתור "תצוגה מקדימה"

109. **סקרתי מחדש את core.run_progress/core.issues/core.projects.audit_gaps לתחום
    17-31 לפני בחירת צעד.** כמעט כל הפערים הפתוחים הנותרים בתחום כבר
    מתועדים כתלויי הכרעת משתמש (תמחור/סליקה/auth-infra: 17 #1-3, 18 #2-3,
    22 #2-5, 26 #1/#3/#5, 28 #2/#4) או תלויי גישה חיצונית שאין לסבב הזה
    (core.issues #205-#209 — uri_allow_list בפרויקטי Supabase שאינם מכוסים
    ב-PAT; #242 — חסימת Lovable MCP; #245 — RLS משותף עם לופ A, ממתין
    להכרעת policy). המערכות שעדיין לא נסקרו לעומק היום היו 21/27/30 (יש
    להן קוד מווענדר מלא למרות ש-`app.json` שלהן מסומן "not-vendored" —
    שדה מטא-דאטה מיושן, אומת ישירות מול מערכת הקבצים).
110. **סוכן Explore על 21-mthbram מצא חילוף שדות בין `target_audience`
    ל-`audience_type` ב-`UpdateLesson.tsx:115/117`.** בדקתי לעומק ומצאתי
    שזו לא באג מבודד אלא סתירה מערכתית רחבה: המיגרציה המקורית
    (`20260312184244_..sql:17-18`) מגדירה במפורש `target_audience` =
    מגדר/דמוגרפיה ("נשים, גברים, ילדים, נוער") ו-`audience_type` = סוג
    קהל מפורט ("בעלי בתים, בוגרי ישיבות..."). `UpdateLesson.tsx` ו-
    `PortalLessonForm.tsx` תואמים להגדרה הזו — אבל `AdminDashboard.tsx`,
    `lessonExcel.ts`, `lessonExcelExport.ts`, `nedarim-webhook/index.ts`
    ועוד כ-10 קבצים נוספים משתמשים בהיפוך המדויק (מתייגים `audience_type`
    כ"מגדר" ו-`target_audience` כ"קהל יעד"). כלומר תלוי דרך ההזנה, אותה
    עמודה מייצגת דבר הפוך — זה משפיע על דוחות/ייצוא/תצוגת ניהול בכל
    המערכת. **לא תיקנתי את זה הסבב הזה** — זה תיקון רחב שדורש מיפוי מדויק
    של כל ~15 הקבצים הנוגעים בדבר וסיכון גבוה לשבור מוסכמה שמישהו כבר
    בנה עליה, לא diff קטן ומבודד. פתחתי `core.issues #246` (severity
    normal, owner=agent, blocked_on="דורש סבב תכנון ייעודי: מיפוי מלא של
    כל 15 הקבצים + החלטה איזו מוסכמה קנונית, לפני כל שינוי קוד") כדי
    שסבב עתידי ייגש לזה כמשימה מתוכננת, לא כתיקון-אגב.
111. **המשכתי לחפש ב-27-bkalut-price ו-30-zchuyotpro-crm.** מועמד ב-27
    (`fin-storage.ts` חסר ולידציה על `clientId` לפני insert בנתיב האדמין)
    התברר חלש: הנתיב `requireAdmin`-מוגן, ה-UI תמיד שולח `client.id` תקין
    מדף פרטי הלקוח, ונתיבי ה-`/api/me/financial/*` (המשתמש-הקצה) כבר
    כותבים over `clientId` נכון מהסשן (אחרי ה-spread, לא לפניו) — אין שם
    חשיפה אמיתית. ב-30 מצאתי באג נקי ומבודד: ב-
    `src/routes/_authenticated/documents.tsx:304-305`, כפתורי "תצוגה
    מקדימה" (Eye) ו-"הורדה" (Download) שניהם קראו לאותה פונקציה בדיוק
    `preview(d)` — שגיאת העתק-הדבק. לחיצה על "הורדה" פותחת תצוגה מקדימה
    בטאב חדש במקום להוריד קובץ בפועל.
112. **התיקון: פונקציית `downloadDoc(d)` חדשה**, מבוססת בדיוק על תבנית
    ההורדה שכבר קיימת וחיה באותה אפליקציה עצמה ב-
    `src/routes/client-area.documents.tsx:36-43` (אזור אישי ללקוח) —
    יוצרת signed URL זהה, אבל במקום `window.open` בונה `<a>` עם
    `download=d.file_name` ולוחצת עליו תכנותית, מה שמכריח הורדת קובץ
    אמיתית. כפתור ה-Download חובר ל-`downloadDoc(d)` במקום `preview(d)`;
    כפתור ה-Eye נשאר ללא שינוי. אפס רגרסיה: קובץ יחיד, שתי שורות שונו +
    פונקציה אחת נוספה, אותה מוסכמה מדויקת שכבר קיימת בקוד הזה עצמו (לא
    המצאה חדשה), שום נתיב/הרשאה/סכימה נגע.

## 19/08/2026 (סבב אוטונומי B, עשירי) — `21 mthbram`: הסבב התכנוני הייעודי ל-`core.issues #246` בוצע, ומצא פיצול 11-נגד-2 ולא תעלומה סימטרית

113. **הסבב הקודם (#109-110) פתח את #246 ודחה תיקון עיוור** — מיפוי חלקי
    מצא ש-`UpdateLesson.tsx`/`PortalLessonForm.tsx` תואמים את הערת המיגרציה
    המקורית (`target_audience`=מגדר, `audience_type`=קהל-יעד-מפורט) בעוד
    `AdminDashboard.tsx`/`lessonExcel*.ts`/`nedarim-webhook` הפוכים, וקבע
    שנדרש סבב תכנון נפרד למיפוי מלא לפני כל שינוי. הרצתי את הסבב הזה עכשיו.
114. **סוכן Explore ראשון החזיר מסקנה שגויה** ("הכול תקין, אין באג אמיתי")
    — לא אמין: הוא בלבל בין "השם משמש בעקביות" לבין "השם תואם את המשמעות
    שהמיגרציה הצהירה". קראתי בעצמי את `20260312184244_...sql:17-18`
    (מאשר: `target_audience`=מגדר, `audience_type`=קהל-יעד) ואחרי זה כל
    אתר קריאה בקוד עצמו (לא grep שטחי) של `UpdateLesson.tsx`,
    `PortalLessonForm.tsx`, `BulkLessonForm.tsx`, `AdminDashboard.tsx`
    (השורות שמזינות בפועל את מסך "מוכן לייצוא לנדרים פלוס"),
    `lessonExcelExport.ts` (הקובץ שכפתור "ייצוא לנדרים פלוס" האמיתי קורא
    לו).
115. **התמונה האמיתית: פיצול 11-נגד-2, לא 50/50.** שלושה נתיבי כתיבה חיים
    קיימים בפועל (אומת מול `App.tsx` — `/update-lesson`,
    `/portal/rabbi/:token` ו-`/portal/org/:token` (שניהם דרך
    `PortalLessonForm`), `/bulk-upload/:token`). מתוכם שניים
    (`UpdateLesson.tsx`, `PortalLessonForm.tsx`) הלכו לפי הערת המיגרציה;
    השלישי (`BulkLessonForm.tsx`, תווית UI מפורשת "גברים / נשים" →
    `audience_type`, "קהל יעד" → `target_audience`) הלך הפוך. וכל קוד
    הקריאה/תצוגה/ייצוא/חיפוש שנבדק בפועל — מסך הכנת-הייצוא של
    `AdminDashboard.tsx` (שורות 752-753+776-785, "3. קהל יעד"=
    `lesson.target_audience`, "11. מגדר"=`lesson.audience_type`),
    `lessonExcelExport.ts` (העמודה שנשלחת בפועל לנדרים פלוס: `genderCol`=
    `audience_type`, `audienceCol`=`target_audience`), `lessonExcel.ts`
    (מתעד במפורש `audience_type: "גברים"/"נשים"/"מעורב"`) — כולם הלכו
    הפוך מהמיגרציה, כלומר **תואמים את `BulkLessonForm`, לא את שני הטפסים
    הציבוריים**.
116. **המשמעות המעשית: הבאג הוא בשני הטפסים הציבוריים, לא ברוב הקוד.**
    רב שממלא את `/update-lesson` ובוחר מגדר, השרת שומר אותו ל-
    `target_audience` — ואז חיפוש ציבורי לפי מגדר ב-`FindLesson.tsx`
    (שקורא מגדר מ-`audience_type`) לא ימצא את השיעור שלו, ומסך "ייצוא
    לנדרים פלוס" יציג "בעלי בתים" בעמודת "מגדר" ו"גברים" בעמודת "קהל
    יעד" — הפוך, בגיליון שנשלח בפועל למוסד תרומות אמיתי.
117. **התיקון: הוחלפו שני מפתחות השדה בדיוק בשני הקבצים המיעוטיים** —
    `UpdateLesson.tsx` (ה-insert היחיד בקובץ) ו-`PortalLessonForm.tsx`
    (שני ה-`ChipPicker`) — כך שיתאימו למוסכמה שנתיב הכתיבה השלישי וכל
    קוד הקריאה/ייצוא/חיפוש כבר בנויים עליה. שום קובץ קריאה/ייצוא/חיפוש
    לא נגע — הוא כבר נכון ביחס לרוב. אפס שינוי סכימה/מיגרציה. נבדק
    (`grep`) ששני הקבצים אינם טוענים שיעור קיים מה-DB לעריכה (שניהם
    טפסי יצירה בלבד, `insert`/`toggleArray` על state מקומי ריק) — כך
    שאין נתיב "טעינה→עריכה→שמירה" בקבצים האלה שהחלפת המפתחות הייתה
    שוברת.
118. **מה שלא תוקן, במפורש: נתונים היסטוריים.** שורות שנכתבו לפני התיקון
    דרך שני הטפסים הציבוריים ישארו עם השדות הפוכים ביחס למוסכמה
    שהתיקון הזה מיישר אליה. אין גישת DB לפרויקט `aypsqqvfohekxxuqsmrw`
    (21 מתחברים) מהסבב הזה כדי לזהות/לתקן שורות ישנות — זה נשאר פער
    ידוע, לא הוסתר. עודכן `core.issues #246` ל-`fixed` עם ההסבר המלא.
    קומיט `8ef82c7b` על `fix/b-mthbram-audience-field-swap-0819`, נדחף.

## 19/08/2026 (סבב אוטונומי B, אחד-עשר) — `26 studio`: "המותגים שלי" חובר בפועל לבורר הצבעים של העורך (audit_gaps #4, "החל פלטה על הפרויקט" חסר)

119. **למה זה הפריט הנבחר.** סבב 109-110 קבע ש-`core.issues` פתוחים
    בהיקף לופ B (17-31/37/29/26) כמעט כולם חסומים על החלטת מוצר/תשלום
    של המשתמש או על גישה חיצונית (Supabase dashboard/Lovable) שאין
    לסבב הזה. סרקתי מחדש (לא הנחתי שהרשימה מהסבב הקודם עדיין נכונה):
    `core.issues` פתוחים בהיקף — אותה תמונה. עברתי ל-`core.projects.
    audit_gaps` של 17-31/37/29/26 וחיפשתי פער קונקרטי, לא-חסום, שניתן
    לסגור בעריכת קוד טהורה. `26 studio` (בהיקף מפורש של הלופ) הצהיר
    פער ידוע ומדויק: "אין עדיין 'החל פלטה על הפרויקט הנוכחי' — מודל
    השכבות/הצבע של העורך אינו מחובר ל-BrandKit בכלל". זה לא דורש
    Supabase MCP, לא דורש הכרעת תמחור, ולא נוגע בשום דבר מוגן.
120. **המיפוי לפני הקוד (סוכן Explore, לא הונח).** פאנל "המותגים שלי"
    (`Editor.tsx`, אז שורה ~1170) כבר טוען `myBrands` מ-`/api/brands`
    (כולל `kitJson` עם `colors.primary[]`/`colors.secondary[]`) ומציג
    עד 4 דגימות צבע לכרטיס מותג — אבל הלחיצה היחידה שהייתה קיימת פתחה
    את המותג בכרטיסייה חדשה במחלקת המיתוג; לא הייתה שום דרך להזרים את
    הצבעים לתוך העורך עצמו. מודל הצבע של העורך הוא Konva (`fill` על
    כל שכבת טקסט/צורה + `background.color`/`gradient`), ובוררי הצבע
    (`ColorPicker.tsx`) כבר תומכים ב-`presets?: string[]` — מנגנון
    "צבעי הסגנון" קיים שמזין דגימות מ-`stylePalette`/`bgPresets`
    קבועים. כלומר התשתית קיימת משני הצדדים; רק החיבור ביניהם חסר.
121. **התיקון — תוספת דגימות, לא צביעה אוטומטית (עקרון אפס-רגרסיה).**
    שקלתי שתי גישות: (א) לולאה שצובעת אוטומטית את כל השכבות בפלטת
    המותג בלחיצה אחת, או (ב) הזרמת צבעי המותג כדגימות נוספות בבוררי
    הצבע הקיימים, שהמעצב בוחר מהן ידנית לכל שכבה. בחרתי (ב) במפורש:
    אפשרות (א) הייתה עלולה לדרוס עיצוב קיים בפרויקט פתוח בלי אפשרות
    ביטול (`updateDoc` אין לו undo), וזו בדיוק הצורה של "לשבור עבודה
    קיימת" שהכלל האוסר רגרסיה נועד למנוע. `Editor.tsx`: כפתור חדש
    ("שרביט", `Wand2`, כבר מיובא) נוסף לכל כרטיס מותג עם דגימות —
    `handleApplyBrandPalette` שולף את מלוא צבעי הפלטה (לא רק 4,
    פונקציה חדשה `brandPaletteColors` שממנה `brandSwatches` הישנה
    שואבת) לתוך state חדש `activeBrandPalette`, ומציג הודעת toast.
    ה-state מוזרם כ-prop `brandPalette` ל-`TextLayerControls`
    (ממוזג ל-`paletteColors` הקיים) ול-`BackgroundControls` (ממוזג
    ל-`bgPresets` הקבוע) — שני המקומות היחידים בקובץ שכבר מזינים
    `presets` ל-`ColorPicker`. כרטיס הבחירה במותג פוצל לשני כפתורים
    (פתיחה בכרטיסייה חדשה נשארה כמו שהייתה; כפתור-שרביט חדש ונפרד
    מפעיל את הפלטה) כדי לא לשנות את ההתנהגות הקיימת של הכפתור המקורי.
    נוספה שורת "פלטה פעילה" עם כפתור "נקה" מתחת לכותרת הפאנל.
122. **מה לא השתנה, במפורש.** אין תלות npm חדשה (Wand2 כבר מיובא),
    אין שינוי סכימה/Supabase, אין שינוי בהתנהגות לאנונימי (הפאנל כולו
    מותנה ב-`loggedIn` כמו קודם), ואף שכבה קיימת בשום פרויקט לא
    משתנה מעצם קיום הפיצ'ר — היא רק מקבלת דגימות צבע נוספות בבורר,
    ורק כשמישהו לוחץ במפורש על כפתור השרביט. `node_modules` לא קיים
    בעץ הזה (אין להתקין לפי כללי הסבב) — לא ניתן להריץ `tsc`/`vite
    build`; אומת קריאה ידנית מלאה של כל השינויים בקובץ (חתימות
    הפונקציות, כל נקודת קריאה, איזון סוגריים/מאמרים) במקום זאת, ורשום
    כאן שזו רמת אימות נמוכה מבנייה אמיתית. עודכן `core.projects`
    (מספר 26) — `audit_gaps` עודכן לשקף שהחיבור נבנה. קומיט זה על
    `feat/b-studio-brand-palette-apply-0819`, נדחף.

## 19/08/2026 (סבב אוטונומי B, שתים-עשרה) — `19 igud-shiurim-portal`: עריכת שיעורים/מודעות נוספה בשני לוחות הניהול (ארגון + מגיד שיעור) — היה רק יצירה+מחיקה, למרות שה-RPC כבר תומך בעדכון

123. **למה נבדק דווקא 19/20, שני "שלדים" שמסומנים `live=false`.**
    כמעט כל `core.issues` הפתוח בהיקף לופ B חסום על הכרעת משתמש או
    גישה חיצונית (נבדק מחדש מול `core.run_progress`/`core.issues` בתחילת
    הסבב). `core.projects.audit_gaps` בהיקף לא סימן שום פריט חדש
    ל-19/20/21/23/25/27/29/30/31 (השדה ריק/לא-נסרק אצלם). בדיקת התיקייה
    גילתה ש-`apps/19-igud-shiurim-portal` (server.js 1173 שורות, public/
    app.js 2333 שורות) ו-`apps/20-igud-portal` (server.js 93, app.js 433)
    הן קוד אמיתי ומגובה Supabase לגמרי — לא "שלד" כמו ש-`app.json`
    (`"source": "not-vendored"`) ו-`core.projects.note` ("שלד פורטל")
    טוענים; מטא-דאטה מיושנת, לא תוקנה בסבב הזה (מחוץ להיקף הצעד היחיד).
124. **שני מסלולי חקירת-באג נבדקו ונפסלו — לא הופעלו על אמונה.** סוכן
    Explore ראשון טען באג ב-`apps/20-igud-portal/public/app.js:211`
    (`if (res.success)` על תשובת טופס-יצירת-קשר) — בדיקה עצמאית מול
    ההגדרה האמיתית של `igud.submit_public_lead` (`supabase/consolidation/
    bieebmnm-logic/01-functions-views-triggers.sql:56-60`,
    `RETURN jsonb_build_object('success', true, 'id', v_msg_id)`) הראתה
    שהטענה **שגויה** — PostgREST מחזיר ערך `jsonb` יחיד של פונקציה ישירות
    כגוף התשובה, כך ש-`res.success` באמת `true` בהצלחה; הקוד תקין ולא
    נגעתי בו. סוכן Explore שני, שהתבקש לאמת כל ממצא מול הגדרת ה-RPC
    בפועל (לא רק מוסכמות מהזיכרון), לא מצא שום באג מאומת ב-19 אחרי קריאה
    מלאה של שני לוחות הניהול (ארגון + מגיד שיעור) — תוצאה תקינה ומקובלת,
    לא נמתחה כדי "למצוא משהו".
125. **הפער האמיתי שנמצא תוך כדי: פער פיצ'ר, לא באג.** `server.js:315`
    (`admin_upsert_lesson`) ו-`server.js:345` (`admin_upsert_ad`) — וכן
    המקבילות בלוח מגיד השיעור, `server.js:587`/`615`
    (`admin_upsert_teacher_lesson`/`admin_upsert_teacher_ad`) — כל
    ארבעתן פונקציות "upsert" עם פרמטר `p_lesson_id`/`p_ad_id` אופציונלי:
    `null` יוצר, ערך אמיתי מעדכן. אבל `public/app.js` בשני הלוחות מעולם
    לא שלח את השדה — כל טופס "הוספה" תמיד יצר שורה חדשה, ולא הייתה שום
    דרך בממשק לערוך שיעור/מודעה קיימים בלי למחוק וליצור מחדש (ומאבד
    שדות/סטטיסטיקות קיימות כמו `view_count`/`click_count` על מודעה).
    זה שימוש חלקי בתשתית קיימת בצד השרת — לא נבנה קוד שרת חדש, רק חובר
    הצד שכבר קיים.
126. **התיקון — תוספתי בלבד, אותו מסלול upsert קיים.** בארבע פונקציות
    הציור (`drawLessons`/`drawAds` בלוח הארגון, ואותן שתיים בלוח מגיד
    השיעור): נוסף מצב עריכה (`editingLesson`/`editingAd`, משתני מצב
    בהיקף `renderAdmin`/`renderTeacherAdmin`) — כפתור "עריכה" חדש לצד
    "מחיקה" הקיים בכל שורה, שממלא מראש את אותו טופס היצירה הקיים בערכי
    הרשומה (כולל `uploadFieldHtml`'s `currentUrl` לתצוגה מקדימה של
    לוגו/תמונה קיימים), משנה את כותרת הטופס וטקסט הכפתור ל"עדכון",
    ומוסיף כפתור "ביטול עריכה". בשליחה, אם במצב עריכה — נשלח
    `lesson_id`/`ad_id` (בלוח הארגון) או `id` (בלוח מגיד השיעור, לפי
    השם שהשרת כבר מצפה לו ב-`server.js:589`/`617`) — בדיוק השדה שהשרת
    כבר יודע לקרוא, שום שינוי ב-`server.js`. מצב העריכה מתאפס במעבר
    לשונית אחרת, בביטול מפורש, בהצלחת שמירה, ואם השורה שבעריכה נמחקה
    תוך כדי. אפס שינוי להתנהגות היצירה/מחיקה הקיימת (טופס ריק=יצירה,
    בדיוק כמו קודם); אפס שינוי סכימה/RPC/route בצד השרת.
127. **אימות.** `node --check` על `public/app.js` אחרי כל עריכה — תקין.
    קריאה מלאה של כל ארבעת הבלוקים שנערכו (טופס+רשימה+מאזיני אירועים)
    אחרי העריכה, כולל בדיקת שם השדה הנכון לכל אחד מארבעת ה-RPC (השוני
    בין `lesson_id`/`ad_id` בלוח הארגון לבין `id` בלוח מגיד השיעור
    אומת ישירות מול `server.js`, לא הונח שהם זהים). `node_modules` לא
    קיים בעץ הזה (server.js/vite לא ניתנים להרצה בסבב הזה) — לא הופעל
    שרת/דפדפן; זו קריאת-קוד בלבד, לא בדיקה חיה. שני האפליקציות עדיין
    `live=false`/`is_deployed=false` ב-`core.projects` (הכרעת פריסה של
    המשתמש, לא שונתה כאן) — התיקון משפר את הקוד שיפורס כשתתקבל ההחלטה,
    ולא נראה על ידי אף משתמש קצה היום. קומיט על
    `feat/b-igud-shiurim-portal-lesson-ad-edit-0819`, נדחף (מפעיל פריסת
    Vercel — אך `core.projects.is_deployed=false` ל-19, כך שאין תוצאה
    ציבורית עד להחלטת פריסה נפרדת).

## 19/08/2026 — סבב B, שלוש-עשרה

128. **בדיקת ההצעה הפתוחה מהסבב הקודם: אותו דפוס upsert-בלי-עריכה ב-20.**
    נקרא `apps/20-igud-portal/server.js` (93 שורות) ו-`public/app.js`
    (434 שורות) במלואם. נמצא: 20 שונה מהותית מ-19 — אין שום RPC
    ליצירה/עדכון/מחיקה של שיעורים/מודעות מהצד הזה כלל (`admin_upsert_*`
    לא קיים כאן); לוח הניהול היחיד שקיים (`viewAdmin`/`get_admin_tenant`)
    הוא תצוגה בלבד (פניות/שיעורים/מודעות/פרטי-ישות, כל הארבעה קריאה
    בלבד). כלומר ההשערה לא התאמתה — אין כאן פיצ'ר-עריכה חסר לתקן, כי
    אין כאן שום יצירה/עריכה מלכתחילה. לא שונה קוד ב-20.
129. **סריקה חוזרת של `core.issues`/`core.projects.audit_gaps` לכל
    ההיקף (17-31/37/29/26).** כל שורת `core.issues` פתוחה בהיקף היא
    חסומה אמיתית על החלטת משתמש/גישה חיצונית (uri_allow_list, SMTP,
    מפתחות ספק, RLS משותף #245) או כבר מתועדת כ`owner=agent` חסום
    (galil #62 — גישה לפרויקט `mwljkonwdeuaahsigjdp`; mechiron #115 —
    חסום על #94/#83). `audit_gaps` הנותרים בהיקף (17/18/22/24/26/28) כולם
    תלויי תמחור/סליקה/תשתית-אימות — לא צעד קוד בודד. `core.issues #5`
    (עיצוב-ייחודי) נשאר פתוח רק על הפער המבני הרחב (9 מערכות עם ערכת-
    shadcn/lucide המלאה מול 9 בלעדיה) — סבב תכנון נפרד, לא הותחל בעיוורון
    (ראו `QA/platform/DESIGN_SAMENESS.md`, עדכון "סבב אוטונומי 4": חמש
    מתשע המערכות עם הקיט המלא נמצאות בהיקף לופ B — chizukim/17,
    zchuyot/22, mthbram/21, galil/24, gesher/31 — וכל התד הקודם
    (icon-decisions) כבר סגור עליהן; מה שנשאר הוא רק ליבת ספריית
    הרכיבים עצמה, שינוי שלא ניתן לאמת ללא דפדפן חי, כבוד ל"אין הרצת
    שרת/דפדפן" של הסבב הזה).
130. **הצעד שנבחר בפועל: פרצת הרשאות אמיתית ב-27 (bkalut-price),
    לא פער עיצוב/פיצ'ר.** הופעלו שלושה סוכני Explore קריאה-בלבד
    במקביל על 24/27, 28/30, 22/31 — שני הראשונים דיווחו ממצא, השלישי
    "לא נמצא באג באמון גבוה" (נבדק ונדחה בעצמו, לא נלקח כמובן מאליו).
    ה-30 (`client-area.messages.tsx`: `direction: "inbound"` קבוע
    לשליחת לקוח, במקום `"outbound"` כמו בכל שאר חמש נקודות היצירה
    באפליקציה) הוא באג UI אמיתי אך קטן-השפעה (כיוון בועת צ'אט). ה-27
    חמור משמעותית: `server/routes.ts:760/764/771` — שלושת ה-endpoints
    `GET /api/orgs`, `GET /api/orgs/:id`, `PATCH /api/orgs/:id` היו
    היחידים בקובץ (מתוך יותר מ-140 endpoints מוגני `requireAdmin`) בלי
    את ה-middleware הזה — כולל ה-PATCH שכותב ישירות לנתוני הארגון
    (`updateOrgRow`, `data-loader.ts`). אומת: כל שימושי הלקוח ב-
    `/api/orgs`* (`org-detail.tsx`, `orgs.tsx`, `integrations.tsx`,
    `match.tsx`, `dashboard.tsx`) הם דפי ניהול פנימיים בלבד תחת
    `InternalArea`/`isAdminSubdomain()` ב-`App.tsx` — אף דף ציבורי לא
    צורך את הראוטים האלה, כך שהוספת `requireAdmin` לשלושתם אינה שוברת
    שום זרימה ציבורית. משמעות בפועל לפני התיקון: כל מי שידע/ניחש
    כתובת `PATCH https://admin.bekalut.more30.com/api/orgs/<id>` (או אף
    הדומיין הציבורי — הראוטים האלה לא תחת `/api/admin/*`, כך שה-
    middleware החוסם `/api/admin/*` מדומיין ציבורי ב-`routes.ts` לא
    הגן עליהם) יכול היה לקרוא/לכתוב נתוני ארגון (שם/טלפון/תנאים/
    דרישות) בלי שום אימות — לא רק לקרוא (GET), גם לשנות (PATCH).
131. **התיקון.** הוספת `requireAdmin` לשלושת ה-routes (אותו middleware,
    אותו import כבר קיים בראש הקובץ, אותו דפוס בדיוק כמו כל שאר
    ה-routes בקובץ הזה — לא הומצא). אפס שינוי ללוגיקה/לתשובה עצמה —
    רק שער אימות שהיה חסר. `node_modules` לא קיים בעץ הזה (אין
    `tsc`/build זמין ללא התקנה, אסורה בסבב זה) — אומת בקריאה חוזרת
    מלאה של שלושת הבלוקים שנערכו + השוואת חתימת `requireAdmin` (
    `server/auth.ts:93`, `(req: AuthedRequest, res: Response, next: NextFunction)`)
    מול עשרות שימושים זהים באותו קובץ. לא נמצא caller פנימי בצד השרת
    שקורא לראוטים האלה בעצמו (רק HTTP מהלקוח) — אין סיכון ל-self-call
    שנחסם. קומיט על `fix/b-bkalut-price-orgs-auth-0819`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/mechiron`).

## 19/08/2026 (סבב אוטונומי B, ארבע-עשרה) — `30 zchuyotpro-crm`: תיקון כיוון בועת הצ'אט בפורטל הלקוח היה בכיוון ההפוך למה שהסבב הקודם הציע

132. **בדיקה חוזרת של הממצא שנרשם בסבב הקודם (`fix/b-bkalut-price-orgs-auth-0819`, note תור-הבא) לפני תיקון עיוור.**
    הסבב הקודם רשם: "`client-area.messages.tsx`: `direction: "inbound"` קבוע
    לשליחת לקוח, במקום `"outbound"` כמו בכל שאר חמש נקודות היצירה
    באפליקציה — יש לתקן". קראתי את כל חמש נקודות הכתיבה בפועל
    (`_authenticated/messages.tsx:257`, `partner-portal/$referralId.tsx:95`,
    `QuickNoteButton.tsx:22`, `features/clients/tabs/MessagesTab.tsx:50`)
    לפני נגיעה בקוד — כולן דפי **צוות/ארגון**, ששולחות הודעה מהארגון
    ללקוח, מסומנות `"outbound"`. וגם: `_authenticated/messages.tsx:125`
    סופר `m.direction === "inbound"` כ"טרם נקרא" עבור תג ההתראה של
    הצוות. כלומר המוסכמה בכל הסכימה היא **מרכז-ארגון**: `outbound` =
    הארגון שולח, `inbound` = הלקוח/שותף שולח. לכן `direction: "inbound"`
    בפורטל הלקוח (שם הלקוח עצמו שולח לארגון) הוא **נכון** לפי המוסכמה
    הקיימת — החלפתו ל-`"outbound"` הייתה שוברת את תג ה"לא נקרא" של
    הצוות (ההודעה כבר לא הייתה נספרת כדורשת תשומת לב) וסותרת את חמשת
    נקודות הכתיבה האחרות. **לא ביצעתי את התיקון שהסבב הקודם הציע** —
    הוא היה מכניס רגרסיה אמיתית לתג ההתראה של הצוות תמורת תיקון קוסמטי.
133. **התיקון האמיתי: רק בשכבת התצוגה של `client-area.messages.tsx`.**
    הבאג האמיתי (לא במה שנשלח ל-DB, אלא איך מוצג): הצ'אט בפורטל הלקוח
    הציג את ההודעה **של הלקוח עצמו** (`direction: "inbound"`) בצד שמאל
    עמום, ואת תשובת הארגון (`"outbound"`) בצד ימין מודגש — הפוך למוסכמת
    צ'אט רגילה שבה "ההודעה שלי" מודגשת מימין. הפכתי את שני התנאים
    (`justify-start`/`justify-end` ו-`bg-muted`/`bg-primary`) כך
    שהודעת הלקוח עצמו (`inbound`) תוצג מימין ומודגשת, ותשובת הארגון
    (`outbound`) משמאל ועמומה — התאמה למוסכמת "ההודעה שלי מימין" בלי
    לגעת בערך שנשלח ל-DB, בלוגיקת ההתראה של הצוות, או בכל קובץ אחר.
    שינוי חד-קובצי, שתי שורות. `node_modules` לא קיים בעץ הזה — אומת
    בקריאה חוזרת של הבלוק שנערך בלבד (אין תלות חיצונית/state נוסף).
    קומיט `6d8ca53f` על `fix/b-zchuyotpro-message-direction-0819`, נדחף
    (מפעיל פריסת Vercel תחת `more30.com/crm`).

## 19/08/2026 (סבב אוטונומי B, חמש-עשרה) — `28 kupot-health-funds`: שם מודל Anthropic לא תקין בברירת המחדל של היועץ החכם

134. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1102, תיקון כיוון הצ'אט ב-30, קומיטים
    `6d8ca53f`/`6a05d690` — כבר על ה-branch parent הזה) ו-`core.issues`
    הפתוחים בהיקף — כל שורה פתוחה בבעלות `agent` (#62 galil, #115
    mechiron, #242 22/30) חסומה על גישה חיצונית/הכרעת משתמש שכבר
    תועדה בסבבים קודמים; #5 (עיצוב ייחודי) פתוח רק על "הפער המבני
    הרחב" (9 מערכות עם קיט מלא מול 9 בלעדיו) — סבב תכנון נפרד וגדול,
    לא צעד יחיד (ר' `QA/platform/DESIGN_SAMENESS.md`, עדכון "סבב
    אוטונומי 4"). `core.projects.audit_gaps` בהיקף כולו תלוי תמחור/
    סליקה/הכרעת משתמש חוץ מ-19 (שכבר נסגר בסבב הזה עצמו).
135. **הופעל סוכן Explore קריאה-בלבד** על ארבע מערכות פחות-נחקרות
    בהיקף (20-igud-portal, 21-mthbram, 28-kupot-health-funds,
    ‏25/23 לאישוש שאין קוד מווענדר). 20/21/25/23 — אין ממצא (20 נקי
    וקטן, 21 כבר תוקן בסבב קודם, 25/23 באמת שלד `app.json` בלבד).
    ‏28: `server/routes.ts:212` — ברירת המחדל של מודל ה-Anthropic
    ב-endpoint `/api/agent` (היועץ החכם להשוואת קופות חולים) הייתה
    `"claude_sonnet_4_6"` — קו-תחתון, לא מזהה מודל אמיתי של Anthropic
    API (כל מזהה תקין בקודקס הזה, כולל בקובץ ה-SDK המווענדר
    `api/index.js`, משתמש במקפים: `claude-sonnet-4-5-20250929`
    וכו'). ההערה בקוד הבחינה במפורש בין "סנדבוקס — proxy" (שם ייתכן
    שהכינוי עם קו-תחתון תורגם ע"י gateway של הסביבה המקורית) לבין
    "פרודקשן (Vercel) — מפתח Anthropic אמיתי + שם מודל אמיתי" —
    אבל אין ב-`vercel.json`/`.env.example` של האפליקציה שום אזכור
    ל-`ANTHROPIC_MODEL` כמשתנה סביבה נדרש, ואין רשומת
    `core.missing_tokens` בהיקף 28 בנושא. כלומר קיים סיכוי ממשי
    שהמשתנה אינו מוגדר ב-Vercel האמיתי, ואז כל קריאה אמיתית ל-API
    (מפתח תקין + מודל לא תקין) הייתה נכשלת ב-502 ומחזירה "היועץ החכם
    אינו זמין כרגע" — כשל שקט, לא נראה בבדיקת "העמוד עולה".
136. **התיקון.** שורה יחידה: ברירת המחדל הוחלפה ל-`"claude-sonnet-4-5-
    20250929"` — מזהה מודל אמיתי, עקבי עם הדוגמאות התקפות שכבר
    מופיעות בקובץ ה-SDK המווענדר של אותה מערכת (`api/index.js`), וגם
    עם הדפוס הדומה ב-18 (`htr-corrector.ts:44`,
    `process.env.HTR_CORRECTOR_MODEL || 'claude-opus-5'` — נפילה
    לשם מודל אמיתי, לא לכינוי). ה-branch של Lovable-sandbox כבר לא
    רלוונטי לפריסה בפועל של המונו-רפו הזה (Vercel בלבד לפי
    `vercel.json`), כך שהערך הישן לא היה משרת שום נתיב פריסה חי כאן.
    אפס שינוי להתנהגות/לוגיקה מסביב — רק ברירת המחדל של מחרוזת שם
    המודל. `node_modules` לא קיים בעץ הזה — אומת בקריאה חוזרת של
    הבלוק שנערך ובהשוואה למופעי מודל אמיתיים אחרים באותו ריפו; לא
    הופעל שרת/API אמיתי (אין מפתח זמין בסבב הזה). קומיט על
    `fix/b-kupot-health-funds-anthropic-model-0819`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/kupot`).

## 19/08/2026 (סבב אוטונומי B, שש-עשרה) — `24 galilee-connect-hub`: חשבון מנהל יכול היה להתחבר גם דרך טאב הגבאי, בעקיפין לסינון is_admin שקיים בכל שאר השאילתות על אותה טבלה

137. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1103, תיקון שם המודל ב-28, קומיט
    f8cf50b5 — כבר על ה-branch parent הזה) ו-`core.issues` הפתוחים
    בהיקף — כל שורה פתוחה עדיין חסומה על הכרעת משתמש/גישה חיצונית
    שכבר תועדה (uri_allow_list, RLS #245, galil #62 גישה לפרויקט,
    mechiron #115). `core.projects.audit_gaps` בהיקף כולו תלוי תמחור/
    סליקה/הכרעת משתמש. `core.project_tasks` פתוחים בהיקף — אין (רק
    04/12/13/32/25/02, כולם מחוץ להיקף חוץ מ-25 שהוא "לאתר origin
    חי מאחורי Cloudflare", לא שינוי קוד).
138. **שני סוכני Explore קריאה-בלבד במקביל** על 31-hebrew-bridge-crm
    (לא נבדק לעומק היום) ו-22/24 (נבדקו בעבר רק על סעיפים ספציפיים
    שכבר תועדו וחסומים). הממצא ב-31 (`custom-fields.functions.ts`:
    חסר בדיקת הרשאה ברמת האפליקציה ב-`getCustomFieldValues`/
    `setCustomFieldValue`) נבדק ונדחה על ידי הסוכן עצמו — RLS כבר
    חוסם בפועל (`entity_id = auth.uid()`), זו רק חוסר-הגנה-כפולה,
    לא חור אמיתי. הממצא ב-24 עמד בבדיקה: קראתי בעצמי את
    `GabaiPortal.tsx:1427-1462` (פונקציית `handleLogin`) ואישרתי
    שהתחברות המנהל (שורה 1430) מסננת `is_admin=true`, אבל התחברות
    הגבאי (שורה 1448, לפני התיקון) סיננה רק `username`+`is_active`
    — בלי `is_admin=false` — כך שחשבון מנהל עם שם משתמש/סיסמה ידועים
    יכול היה לעבור גם את בדיקת ההתחברות של טאב הגבאי.
139. **אימות שהתיקון תואם מוסכמה קיימת, לא הומצא.** לפני נגיעה בקוד
    בדקתי: (א) `SynagogueDetailsManager.tsx:78` כבר משתמש ב-
    `.eq('is_admin', false)` על אותה טבלה בדיוק — כלומר הסינון הזה
    הוא מוסכמה קיימת בקודבייס, לא רעיון חדש. (ב) המיגרציה
    `20260315224054_...sql:113` — `is_admin BOOLEAN DEFAULT false`
    — כך שהעמודה תמיד בוליאנית עם ברירת מחדל `false`, לא `NULL`,
    ושתי נקודות ה-insert היחידות (`GabaiPortal.tsx:451` יצירת מנהל
    → `is_admin: true`; `SynagogueDetailsManager.tsx:183` יצירת גבאי
    → `is_admin: false`) מגדירות אותה תמיד במפורש. כך ש-
    `.eq('is_admin', false)` לא היה מסנן בטעות שורות legacy עם ערך
    `NULL`. חומרה בפועל בינונית-נמוכה (לא מדובר בהעלאת הרשאות של
    גבאי רגיל למנהל — למנהל כבר יש גישה מלאה דרך הנתיב הנכון; הבאג
    הוא שמנהל שמתחבר בטעות/במכוון דרך טאב הגבאי מקבל תצוגת גבאי
    שבורה עם `synagogue_id` ריק) אבל תואם בדיוק לרוח ההערה הקיימת
    בקובץ עצמו (שורות 1408-1414, שמתעדת תיקון קודם באותה פונקציה
    בדיוק — בייפס `?auto=admin` שנסגר בעבר) שמייחסת חשיבות גבוהה
    להפרדת נתיבי ההתחברות בקובץ הזה.
140. **התיקון.** שורה אחת: נוסף `.eq('is_admin', false)` לשאילתת
    ההתחברות של הגבאי (`GabaiPortal.tsx:1448`), אותו דפוס בדיוק
    כמו `SynagogueDetailsManager.tsx:78`. אפס שינוי להתנהגות
    ההתחברות של חשבונות גבאי רגילים (הם כבר תמיד `is_admin: false`).
    `node_modules` לא קיים בעץ הזה — אומת בקריאה חוזרת של הבלוק
    שנערך + חיפוש מלא של כל מופעי `is_admin` בקובץ ובקובץ האח כדי
    לוודא עקביות. לא הופעל שרת/דפדפן. קומיט על
    `fix/b-galilee-connect-hub-gabai-admin-login-0819`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/galil`).

## 19/08/2026 (סבב אוטונומי B, שבע-עשרה) — `30 zchuyotpro-crm`: כל משתמש רגיל בארגון יכול היה להעניק לעצמו הרשאת מנהל דרך מסך ההגדרות

141. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1105, תיקון התחברות הגבאי ב-24, קומיט
    7da435f6 — כבר על ה-branch parent הזה) ו-`core.issues`/
    `core.project_tasks`/`audit_gaps` נסרקו מחדש בהיקף 17-31/37/29/26 —
    כל שורה פתוחה נשארה חסומה על הכרעת משתמש/גישה חיצונית שכבר תועדה
    (uri_allow_list, RLS #245, galil #62, mechiron #94/#115); `project_tasks`
    בהיקף — רק #25 (איתור origin חי, לא שינוי קוד). הופעלו שני סוכני
    Explore קריאה-בלבד במקביל על אזורי קוד שלא נגעו בהם הסבבים
    הקודמים של היום (17/18/19/20/23/26/28/29/37 עבור הראשון, עומק ב-
    `30-zchuyotpro-crm`/`31-hebrew-bridge-crm` עבור השני, עם רשימה
    מפורשת של כל מה שכבר תוקן היום כדי לא לחזור על עצמו).
142. **הממצא של הסוכן השני נבדק ונמצא חמור יותר ממה שהוא עצמו דיווח.**
    הסוכן דיווח ש-`settings.tsx` (מסך "משתמשי הארגון") מאפשר לכל
    מנהל לשנות תפקיד (`role`) של כל משתמש בארגון דרך
    `supabase.from("profiles").update({role}).eq("id", id)`, ושמדיניות
    ה-RLS היחידה על `UPDATE` (`profiles_self_update`, מיגרציה
    `20260610191501...sql:101-104`) מאפשרת עדכון רק לשורה של
    `auth_user_id = auth.uid()` עצמו — ולכן שינוי תפקיד של **משתמש
    אחר** נכשל בשקט (caught, toast שגיאה). קראתי את כל שלוש מדיניות
    ה-RLS על `profiles` (`self_read`/`self_update`/`tenant_insert`) +
    את `handle_new_user()` בעצמי לפני נגיעה בקוד, ומצאתי שהניתוח
    התמקד רק בחצי הבעיה: `profiles_self_update` אינה מגבילה **אילו
    עמודות** מותר לשנות בשורה של עצמך — רק ש-`auth_user_id` יישאר
    זהה. וב-`UsersTab` (settings.tsx:144-156) הבחירה (`Select`)
    שמפעילה את אותה `updateRole.mutate({id: p.id, role: v})` מוצגת
    **על כל שורה, כולל השורה של המשתמש המחובר עצמו** — אין שום סינון
    `p.id !== myId`. המשמעות בפועל: כל משתמש מחובר בארגון (גם
    `agent`/`viewer` רגיל, בלי שום כלי מפתח) יכול פשוט ללחוץ על
    התפריט הנפתח בשורה שלו-עצמו ולבחור "מנהל" — הבקשה עוברת בהצלחה
    מלאה דרך ה-RLS הקיים (כי `auth_user_id = auth.uid()` תקף), והוא
    הופך למנהל הארגון בעצמו. זו לא כשל-שקט של פיצ'ר ניהול צוות — זו
    עלייה-בהרשאות-עצמית קריטית שפעילה בייצור היום, נגישה דרך ה-UI
    הרגיל בלי לעקוף שום דבר.
143. **התיקון: טריגר BEFORE UPDATE, לא מדיניות RLS חדשה.** RLS ב-
    Postgres הוא ברמת שורה, לא ברמת עמודה — אי אפשר להגביל "מותר
    לשנות `full_name` של עצמך אבל לא `role`" רק דרך `USING`/
    `WITH CHECK` בלי טריקים שבירים (subquery מול הערך הישן באותה
    פקודה, מסוכן לאמת בלי גישה חיה ל-DB). נבחר דפוס הטריגר הקיים כבר
    באותו קובץ מיגרציה עצמו (`handle_new_user`/`set_updated_at`,
    שניהם `SECURITY DEFINER`) — פונקציה חדשה
    `guard_profile_role_change()` שרצה `BEFORE UPDATE ON profiles
    FOR EACH ROW`, בודקת `NEW.role IS DISTINCT FROM OLD.role AND
    get_my_role() <> 'admin'` (הפונקציה `get_my_role()` כבר קיימת,
    `SECURITY DEFINER`, מחזירה את התפקיד **הנוכחי** של הקורא — כלומר
    הערך *לפני* העדכון הזה, כי הטריגר רץ לפני ה-write) — וזורקת
    חריגה אם כן. תוצאה: משתמש לא-מנהל שמנסה לשנות **כל** `role`
    (כולל של עצמו) נחסם; מנהל אמיתי שמשנה את התפקיד של **עצמו**
    עדיין מותר (לא השתנה — היה אפשרי גם קודם); שינוי `full_name`
    בלבד (בלי לגעת ב-`role`) לא מושפע כלל, כי `NEW.role IS DISTINCT
    FROM OLD.role` הוא `false`. **לא נסגר** במיגרציה הזאת: מנהל
    אמיתי עדיין לא יכול לשנות תפקיד של **משתמש אחר** — זה עדיין נכשל
    (עכשיו נכשל "בבטחה" דרך RLS הקיים כמו קודם, לא רגרסיה) וזה נשאר
    פער-פיצ'ר נפרד, לא-אבטחתי, לתיקון עתידי (מצריך RPC
    `SECURITY DEFINER` נוסף שמאמת שהקורא מנהל **וגם** שהיעד באותו
    tenant — לא הוספתי אותו הסבב הזה כדי לא להרחיב את ה-diff מעבר
    לחור שנמצא בפועל). **גם לא נגעתי** בטופס ההזמנה (`invite()`,
    settings.tsx:83-101) — הוא כבר **מתעלם** מהתפקיד שנבחר ב-UI
    (שולח רק `tenant_id`+`full_name` ב-metadata, והטריגר
    `handle_new_user` קובע תמיד `role='agent'` כשיש `tenant_id`) —
    זו כשל UI מטעה (לא אבטחתי: ברירת המחדל הבטוחה ביותר כבר
    האכיפה), ותיקון אמיתי שלו (העברת התפקיד הנבחר בפועל) היה **פותח
    מחדש** את אותו חור עלייה-בהרשאות דרך מסלול הרשמה-עצמית אם לא
    נעשה בזהירות רבה — הושאר כפי שהוא, מתועד כאן במפורש כדי שסבב
    עתידי לא יתקן אותו בטעות בלי המודעות הזו.
144. **לא ניתן לפרוס בפועל מהסבב הזה.** פרויקט ה-Supabase של 30
    (`jhbeelzvjvhnkxldqvxx`) אינו נגיש ל-MCP בסביבה הזאת — אומת
    ישירות (`list_tables` על הפרויקט מחזיר "Access... denied... your
    access token may be scoped to a different organization"), תואם
    את מה שכבר מתועד ב-`CONNECTIONS.md`. המיגרציה
    (`apps/30-zchuyotpro-crm/supabase/migrations/
    20260819230000_guard_profile_role_change.sql`) נכתבת ונבדקת
    בקריאה בלבד (אין `node_modules`/DB חי כדי להריץ SQL אמיתי כאן),
    עם הערת-ראש בקובץ עצמו שמסבירה את החור ואת הסיבה לתיקון. נפתח
    `core.issues` חדש, `severity=critical`, `owner=user`, עם
    `blocked_on` מפורש: יש לפרוס את המיגרציה הזו על
    `jhbeelzvjvhnkxldqvxx` (Lovable/SQL editor/דשבורד) לפני שהחור
    נסגר בייצור בפועל — קוד המקור בלבד לא מגן על אף אחד עד שהיא
    תרוץ. קומיט על `fix/b-zchuyotpro-role-escalation-0819`, נדחף
    (מפעיל ניסיון פריסת Vercel תחת `more30.com/crm` — הקוד הפרונט-אנד
    לא השתנה כלל בסבב הזה, רק קובץ מיגרציה נוסף לעץ, כך שאין שינוי
    התנהגות בפרודקשן מהפוש הזה כשלעצמו; התיקון עצמו ימתין להרצת
    המיגרציה).

## 19/08/2026 (סבב אוטונומי B, שמונה-עשרה) — `30 zchuyotpro-crm`: המשך #248 — מנהל אמיתי לא יכול היה לשנות תפקיד של חבר צוות אחר

145. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1105/הסבב-שבע-עשרה, migration-only על 30,
    קומיט e02a43fc — כבר על ה-branch parent הזה) ו-`core.issues`/
    `core.project_tasks` בהיקף 17-31/37/29/26/41. כל שורה פתוחה חסומה
    כבר על הכרעת משתמש/גישה חיצונית מתועדת (uri_allow_list #202/#206/
    #209, RLS #245, galil #62, mechiron #94/#115, gesher #180, #242
    savings-calculator). `project_tasks` בהיקף — רק #25 origin-מאחורי-
    Cloudflare (לא שינוי קוד). בדקתי גם שני "אתחולי-על" חדשים שלא
    היו ידועים לי: מיתוג §7 ("עולם הסטארטאפים" + קרדיט פוטר) ותמחור
    §8 (2/5/10/12/15 ₪, TEST) — שניהם **כבר סגורים בפועל** להיקף הזה:
    (א) קרדיט הפוטר מוזרק אוטומטית ע"י `portal/public/auth-button.js`
    (סקריפט משותף, מוגן — לא בהיקף שלי) לכל מערכת שכוללת
    `<script src=".../auth-button.js">`; אימתתי בגריפ שכל 11 המערכות
    החיות בהיקף (17/18/21/22/24/26/27/28/30/31/37 + 41) כבר טוענות
    אותו — רק 19/20/23/25/29 (כולן `live=false`, החלטת-פריסה של
    המשתמש או stub ללא מקור אמיתי) לא, וזה לא רגרסיה. (ב) `core.plans`
    כבר מכיל שורות free/basic(2₪)/extended(5₪)/charge/one_time/pro
    לכל `app_key` בהיקף (chizukim/orech/shiurim/galil/zchuyot/mechiron/
    kupot/modaot/studio/crm/gesher), ו-`core.billing_settings.mode='off'`
    (לא test/live) — תואם בדיוק את הדרישה "קטלוג בלבד, שום חיוב אמיתי
    בלי אישור". שני הסעיפים האלה כבר "מוכן לשיווק" להיקף הזה — לא
    נגעתי בהם.
146. **המשך #248, לא כפילות.** קריאה של `settings.tsx` (עצמה, לא רק
    תקציר הסבב הקודם) אישרה את מה שסבב שבע-עשרה תיעד במפורש כפער
    נפרד: `UsersTab.updateRole` שולח `update({role}).eq('id', p.id)`
    על **כל** שורה, כולל שורות של משתמשים אחרים — אבל `profiles_self_update`
    (המדיניות היחידה על `UPDATE`) מגבילה `USING/WITH CHECK` ל-
    `auth_user_id = auth.uid()` בלבד, כך שמנהל אמיתי שמנסה לשנות תפקיד
    של חבר צוות **אחר** דרך המסך הזה — הפעולה הראשית שהלשונית "משתמשי
    הארגון" מפרסמת — נכשלת תמיד. זה לא חור אבטחה (ההפך — RLS עושה בדיוק
    את מה שהיא אמורה), אלא פיצ'ר שבור: הכפתור קיים, לא עובד.
147. **פתרון: RPC שני, לא הרחבת ה-trigger הקיים.** `guard_profile_role_change`
    (המיגרציה הקודמת) בודק רק "מי שמשנה role חייב admin" — לא בודק
    *של מי* השורה, כי מדיניות ה-`UPDATE` הקיימת ממילא לא מרשה יעד
    שאינו-עצמי להגיע לשם. RPC חדש `admin_update_member_role(p_profile_id,
    p_role)` (`SECURITY DEFINER`, אותו דפוס בדיוק כמו `get_my_role`/
    `is_tenant_member` באותו קובץ מיגרציה מקורי): בודק `get_my_role() =
    'admin'`, בודק שה-`tenant_id` של השורה-היעד שווה ל-`get_my_tenant_id()`
    של הקורא (מונע שינוי תפקיד חוצה-דייר גם אם מזהה שורה נוחש), ורק
    אז מבצע `UPDATE`. הטריגר הקיים עדיין רץ על אותו `UPDATE` (טריגרים
    הם ברמת הטבלה, לא עוקפים אותם `SECURITY DEFINER`) ומאשר כי הקורא
    כבר אומת כ-`admin` — אין התנגשות, רק שכבת-הגנה כפולה עקבית.
    `REVOKE...FROM PUBLIC, anon` + `GRANT...TO authenticated`, אותה
    מוסכמה מדויקת כמו שאר הפונקציות באותו סכימה (מיגרציה
    `20260610191513`/`...191530`).
148. **הזרימה מחוברת ב-frontend, עם נפילה-חזרה כדי לא לשבור מה שעובד
    היום.** `settings.tsx:71-95` שונה כך ש-`updateRole` קורא קודם ל-
    `supabase.rpc("admin_update_member_role", ...)`. **בעיה שזיהיתי
    לפני שנגעתי בקוד:** ה-RPC החדש עדיין לא קיים בפרודקשן (אותו חסם
    בדיוק כמו #248 — `jhbeelzvjvhnkxldqvxx` לא נגיש ל-MCP כאן), ו-
    Vercel נפרס אוטומטית מיד עם ה-push הזה — כלומר קיים חלון זמן לא-
    ידוע שבו ה-frontend החדש היה קורא לפונקציה שלא קיימת, ומחליף פיצ'ר
    שעובד **היום** בפרודקשן (מנהל משנה את **התפקיד של עצמו**, מותר
    ותקין) בשגיאה. זו בדיוק הרגרסיה שכלל אפס-רגרסיה אוסר. **לכן:**
    נוספה נפילה-חזרה — אם ה-RPC מחזיר שגיאה שמזוהה כ"פונקציה לא קיימת"
    (`error.code === "PGRST202"`, פלוס בדיקת-מחרוזת גיבוי בלתי-תלויה
    בקוד על `error.message` — "find the function"/"schema cache" —
    כדי שסיווג שגוי של השגיאה לא ישבור בשקט את מה שעובד), הקוד נופל
    חזרה בדיוק לקריאת ה-`update()` הישירה המקורית. התוצאה: לפני פריסת
    המיגרציה — התנהגות זהה ל-100% למה שהיה (מנהל משנה את עצמו: עובד
    דרך הנפילה-חזרה; מנהל משנה אחר: עדיין נכשל, כמו היום, לא גרוע
    יותר). אחרי פריסת המיגרציה — הפיצ'ר החדש עובד בלי צורך בסבב נוסף
    שיזכור "לחבר את ה-frontend". גם `src/integrations/supabase/types.ts`
    עודכן (שורת `Functions.admin_update_member_role`) כדי שהקריאה
    תעבור טיפוס — קובץ מיוצר-אוטומטית מה-DB החי, וה-DB החי עדיין לא
    מכיר את הפונקציה, בדיוק כמו ב-#248.
149. **לא הופעל שרת/בדיקה חיה.** `node_modules` לא קיים בעץ הזה (כמו
    בכל הסבבים הקודמים על 30) — אומת בקריאה חוזרת מלאה של שלושת
    הקבצים שנערכו/נוצרו (המיגרציה, `settings.tsx`, `types.ts`) ובהשוואת
    כל מוסכמה (שם פונקציה, `REVOKE`/`GRANT`, טיפוס `Args`/`Returns`)
    למופעים קיימים באותה סכימה. עודכן `core.issues #248` (הערת
    `blocked_on` מורחבת לשתי המיגרציות הממתינות) ונפתח `core.issues`
    חדש `severity=low` (פער-פיצ'ר, לא אבטחתי) עם אותו `blocked_on`.
    קומיט על `fix/b-zchuyotpro-admin-change-member-role-0819`, נדחף
    (מפעיל פריסת Vercel תחת `more30.com/crm` — עד שהמיגרציה תרוץ,
    ההתנהגות בפרודקשן זהה למה שהייתה, בזכות הנפילה-חזרה).

## 19/08/2026 (סבב אוטונומי B, תשעה-עשר) — `20 igud-portal`: טופס "צור קשר" נתקע לצמיתות על "שולח…" בכל כשל רשת/שרת

150. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1110, 30 zchuyotpro RPC `admin_update_member_role`,
    commit 29d8e4e1 — כבר parent הענף הזה) ו-`core.issues`/`core.project_tasks`
    בהיקף 17-31/37/29/26/41 מחדש: כל שורה פתוחה ל-agent/blocked (uri_allow_list
    #202/#205-209, RLS #245, galil #62, mechiron #94/#115, egod #167/#168,
    gesher #180, get-your-rights #169/#242, zchuyotpro #248/#249) עדיין חסומה
    בדיוק כפי שתועד — ללא שינוי. `project_tasks` בהיקף — רק #25 (איתור origin
    מאחורי Cloudflare, לא שינוי קוד).
151. **שלושה סוכני Explore מקבילים** על אזורים פחות-נסרקים בהיקף: 37-bkalot-clone
    (index.html+admin.html, ה"שכפול התפעולי" שנקרא במפורש בהיקף הסבב הזה),
    41-design-system, ו-20-igud-portal/21-mthbram/23-haorech-torani.
    **שני ממצאים לא שרדו את האימות העצמי שלי:** (א) 37-bkalot-clone —
    הסוכן טען ש-`index.html:547` שולח `consent` כמחרוזת `"true"/"false"`
    במקום בוליאני, ושזה גורם ל-admin.html להציג "כן" גם כשלא ניתנה הסכמה
    (מחרוזת לא-ריקה היא truthy ב-JS). קראתי את כל שרשרת השרת: ה-edge function
    `bkalot-clone-intake` מעביר את הגוף כפי שהוא ל-RPC `public.bkalot_clone_intake`
    (מיגרציה 0058), שמפרש במפורש `v_consent := (p->>'consent') is not null and
    lower(p->>'consent') in ('true','t','1','yes')` — ותפעיל `->>'` על ערך
    JSON בוליאני `true` ועל ערך JSON מחרוזתי `"true"` מחזיר בדיוק אותה מחרוזת
    טקסט `'true'`, כך ששני הפורמטים מתפרשים זהה. `bkalot_clone.cases.consent`
    (מיגרציה 0068, `boolean not null default false`) נגזר באותו אופן בדיוק
    מ-`raw->>'consent'`. כלומר העמודה במסד היא תמיד בוליאן אמיתי, ו-admin.html
    קורא בוליאן אמיתי — לא הייתה כאן שום תקלה, הסוכן פספס את שכבת ה-RPC.
    לא נגעתי. (ב) 41-design-system — טענת "היפוך צבעי hover על כפתור" הייתה
    שיפוט עיצובי סובייקטיבי (טעם, לא תקלה מדידה) — לא פעלתי לפיה.
152. **הממצא שכן שרד: 20-igud-portal, `public/app.js`.** `apiGet` (שורה 33-37)
    בודק `if (!res.ok) throw` לפני `res.json()`; `apiPost` (שורה 38-45, יחיד
    בקובץ) לא — חוסר-סימטריה שהיא ראיה ישירה לפער. נקודת הקריאה היחידה
    (שורה 205-224, טופס "צור קשר" בעמוד התדמית של תזמור/גבאי/מגיד שיעור) לא
    עטפה את `await apiPost(...)` ב-try/catch כלל. אימתתי את התרחיש מול
    `server.js` בפועל: `callRpc` (שורה 18-32) לא עוטף את ה-`fetch` הפנימי
    שלו ל-Supabase ב-try/catch — אם הוא נכשל (רשת/timeout ל-Supabase), הבקשה
    ל-Express נשארת unhandled-rejection בלי תשובה, מה שגם בהתארחות מאחורי
    Vercel/פרוקסי עלול להחזיר timeout/502 בפועל; וגם כשל רשת גולמי בדפדפן
    (fetch עצמו נכשל) גורם לאותה בעיה. בכל אחד מהמקרים: `apiPost` היה זורק
    חריגה לא-נתפסת בתוך handler async, וה"notice" בטופס נשאר קפוא על "שולח…"
    לנצח — בלי שהמשתמש יידע שהפנייה לא נשלחה, ובלי אפשרות לנסות שוב (הטופס
    לא reset, אבל גם לא מציג שגיאה). זהה במהות לדפוס שכבר תוקן השבוע במערכות
    14/16 (missing res.ok check).
153. **תיקון מינימלי, שני מקומות בקובץ יחיד.** (1) הוספת `if (!res.ok) throw
    new Error(...)` ל-`apiPost`, אותה שורה בדיוק כמו ב-`apiGet`. (2) עטיפת
    גוף ה-`onSubmit` בקריאה ל-`apiPost` ב-try/catch — הצלחה/כישלון-לוגי
    (res.success===false) נשארים בדיוק כפי שהיו; רק חריגה (רשת/HTTP-לא-תקין)
    מציגה כעת הודעת שגיאה במקום להיתקע. אפס שינוי לזרימת ההצלחה, אפס שינוי
    ל-server.js. `node --check` על הקובץ עבר נקי. אין node_modules בעץ הזה
    (כלל אי-התקנה) ואין דפדפן זמין — אומת בקריאה חוזרת מלאה של שני הבלוקים
    שנערכו מול השימוש הקיים ב-`apiGet`. system 20 עדיין `live=false/
    is_deployed=false` בהחלטת-פריסה של המשתמש, כך שהתיקון אינו נראה למשתמש
    אמיתי היום — אבל מוכן מראש לכשתיפרס.
154. **קומיט ופוש.** ענף `fix/b-igud-portal-contact-form-stuck-sending-0819`,
    קומיט יחיד, נדחף (מפעיל ניסיון פריסת Vercel — `is_deployed=false` אומר
    שאין נתיב חי שמגיש אותו כרגע, כך שאין שינוי התנהגות בפרודקשן מהפוש הזה).
    הבא בתור: לסרוק מחדש `core.issues`/`core.project_tasks`; 21-mthbram/
    23-haorech-torani לא הניבו ממצא ששרד בסבב הזה — מועמדים הוגנים לסבב הבא
    עם סוכן חדש ובזווית אחרת.

## 19/08/2026 (סבב אוטונומי B, עשרים) — `17 chizukim-transcribe`: התנגשות נתיב-אחסון גרמה לאובדן-נתונים אמיתי, כבר קרה בייצור

155. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1113, 20-igud-portal apiPost fix, commit
    70decf83 — כבר `HEAD`/parent הענף הזה, כולל heartbeat קודם) ו-
    `core.issues`/`core.project_tasks` נסרקו מחדש בהיקף 17-31/37/29/26/41:
    כל שורה פתוחה עדיין חסומה בדיוק כפי שתועד בסבבים קודמים (uri_allow_list
    #202/#205-209, RLS #245, galil #62, mechiron #94/#115, egod #167/#168,
    gesher #180, get-your-rights #169/#242, zchuyotpro #248/#249) — אין שינוי.
    `project_tasks` בהיקף — רק #25 (לא שינוי קוד).
156. **שלושה סוכני Explore מקבילים** על אזורים לא-נסרקים לעומק: (21-mthbram,
    23-haorech-torani), (26-modaot-studio, 29-bkalot-design), (18-torah-editor-mvp,
    17-chizukim-transcribe) — כל אחד קיבל רשימה מפורשת של מה שכבר תוקן היום
    כדי לא לדווח כפילות. **29-bkalot-design** אומת כ-manifest-only (בונה
    טוקנים סטטי, בלי DB/auth) — אין שטח-תקיפה. **26-modaot-studio** ו-
    **18-torah-editor-mvp** לא הניבו ממצא ששרד את הביקורת העצמית של הסוכנים
    (אנונימיות-במכוון מתועדת בקוד, לא סטייה).
157. **ממצא ששרד: 21-mthbram, `public.synagogues` — RLS פתוח-לגמרי שלא
    הוקשח.** מיגרציה `20260411233411` יצרה על `synagogues` ארבע מדיניות
    ללא שום בדיקת auth/org (`USING(true)`/`WITH CHECK(true)` על insert/
    update/delete). מיגרציית ההקשחה המאוחרת `20260712192546` נעלה מחדש
    16 טבלאות אחרות (lessons/contact_messages/teacher_leads/... /
    prayer_times/...) אבל **לא** `synagogues` — `grep -rl synagogues
    supabase/migrations/*.sql` מחזיר רק את הקובץ המקורי. `PrayerTimesTab.tsx`
    (שורות 44/54/63) קורא insert/update/delete על synagogues בלי אימות,
    ו-`prayer_times` תלוי ב-`synagogue_id` (FK) — כלומר כל קורא עם מפתח
    anon (ציבורי) יכול לשנות/למחוק שורת synagogue של כל ארגון ישירות מול
    Supabase REST. **לא תוקן בסבב הזה** (הפרויקט `aypsqqvfohekxxuqsmrw`
    אינו נגיש ל-MCP כאן, אותו חסם כמו #207) — נפתח `core.issues #250`
    (severity=critical, owner=user, blocked_on=פריסת מיגרציה חיצונית) כדי
    שהממצא לא ילך לאיבוד; מוקצה כמועמד לסבב עתידי שבו הצעד היחיד הוא כתיבת
    המיגרציה + תיעוד (בדיוק כמו התבנית שכבר שימשה ב-#248/#249).
158. **הממצא שנבחר לתיקון בפועל: 17-chizukim-transcribe, מירוץ הקצאת
    seq גורם לדריסת-קובץ-אודיו שקטה — ואומת כ-כבר קרה בפרודקשן, לא רק
    תיאורטי.** `server/routes.ts` `nextSeq()` (שורה 62) קורא
    `SELECT seq ORDER BY seq DESC LIMIT 1` ומחזיר `+1` באפליקציה — קריאה-
    ואז-כתיבה קלאסית בלי נעילה/עסקה בצד ה-DB. שני נתיבי ההעלאה (init/
    register לקובץ גדול, ו-upload לקובץ קטן) בונים `objectPath` כ-
    `${seq}.${ext}` בלבד ושניהם כותבים עם `x-upsert: true` — כלומר התנגשות
    `seq` לא מחזירה שגיאה, היא **דורסת בשקט** את קובץ האודיו הקודם. אימתתי
    ישירות מול ה-DB החי (`csjekrvukbdznetsrodj`, נגיש ל-MCP): **אין** אילוץ
    `UNIQUE` על `recordings.seq` (רק `id` PK ו-`drive_id` unique), ו-שאילתת
    `group by seq having count(*)>1` מחזירה **11 זוגות** seq כפולים
    (1030,1031,1033-1035,1037-1042) מאצווה מ-2026-07-04 — ובדקתי זוג אחד
    לעומק (seq=1037): שתי שורות `recordings` עם `original_name` שונה
    (082.wav ו-083.wav) חולקות בדיוק אותו `audio_path` (`1037.wav`) — הוכחה
    ישירה שקובץ אודיו של הקלטה אחת נדרס בשקט ע"י ההעלאה השנייה, בלי שגיאה
    לאף אחד מהמעלים. זה לא תיאוריה — נתונים אמיתיים כבר אבדו.
159. **תיקון ממוקד, לא שינוי סמנטיקת seq.** לא נגעתי ב-`nextSeq()` עצמה
    (התיקון החזק-באמת דורש עמודת identity/sequence ב-DB, שינוי סכימה גדול
    יותר מהיקף "צעד אחד") — `seq` נשאר label תצוגה בלבד בכל מקום שהוא נקרא
    (`format.ts`/`export.ts`/`recordings.tsx`), אימתתי בגריפ שאף קובץ לא
    מפרק/גוזר `seq` מתוך `objectPath`/`audio_path`. במקום זה: `objectPath`
    עצמו הפך לחסין-התנגשות ללא תלות בהתנגשות seq — נוספה `randomToken()`
    (`crypto.randomBytes(4).toString("hex")`, `node:crypto` הוא built-in,
    אין תלות חדשה) ושורש הנתיב שונה מ-`${seq}.${ext}` ל-
    `${seq}-${randomToken()}.${ext}` בשני מקומות הקריאה (שורה 203 ו-276).
    גם אם שני מעלים יקבלו אותו `seq` בדיוק שוב, נתיב האחסון שלהם כבר לא
    יתנגש — האובדן-נתונים השקט נעצר מהשורש, בלי לגעת בפורמט/התנהגות אחרת.
160. **אומת בקריאה בלבד.** אין `node_modules`/`tsc` בעץ הזה (כלל אי-התקנה,
    כמו כל סבב קודם) — קראתי מחדש את הקובץ המלא (594 שורות) אחרי העריכה,
    אימתתי שה-import החדש (`randomBytes` מ-`node:crypto`) לא מתנגש עם
    imports קיימים, ושאין קריאה שלישית ל-`nextSeq()`/בניית `objectPath`
    שפוספסה. `core.issues #250` נפתח כמתואר למעלה עבור הממצא הנפרד
    (21-mthbram). קומיט על `fix/b-chizukim-transcribe-audio-overwrite-race-0819`,
    נדחף (מפעיל פריסת Vercel — נתיב קוד שרת בלבד, אין שינוי סכימה, אפס
    שינוי לזרימת ה-seq/תצוגה, רק לנתיב-האחסון בפועל). הבא בתור: לסרוק מחדש
    `core.issues`/`core.project_tasks`; #250 (21-mthbram synagogues RLS)
    הוא מועמד מוכן-מראש לסבב עתידי — כתיבת מיגרציה + לוג, בדיוק כמו #248/#249.

## 19/08/2026 (סבב אוטונומי B, עשרים-ואחד) — `28 kupot-health-funds`: יועץ AI ציבורי בלי הגבלת קצב, אותו דפוס שכבר תוקן פעמיים

161. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1115, 17-chizukim-transcribe seq-collision fix,
    commit b0cbb82e — כבר `HEAD`/parent הענף הזה, כולל heartbeat קודם כבר
    רשום) ו-`core.issues`/`core.project_tasks` נסרקו מחדש בהיקף 17-31/37/29/26:
    כל שורה `open`/owner=agent שנבדקה (#62 galil, #115 mechiron, #167/#201 egod,
    #242 rights/crm calculator) עדיין חסומה בדיוק כפי שתועד — בלי שינוי; שורות
    owner=user (#202/#205-209 uri_allow_list, #245 RLS, #248/#249/#250
    zchuyotpro/mthbram) גם הן ללא שינוי. `project_tasks` בהיקף — רק #25 (לא
    שינוי קוד).
162. **28-kupot-health-funds מעולם לא נסרק בשום סבב קודם** (בניגוד לכל שאר
    17-31, שנסרקו כמה פעמים) — נבחר כשטח בתולה. סוכן Explore ראשון החזיר
    ממצא (`server/routes.ts` מחזיר `id: null` על תשובת `/api/switch-lead`
    במצב Supabase) שלא שרד את הבדיקה העצמית שלי: קראתי את הראוט המלא —
    יש כבר `if (!lead && !savedRemotely) return 503` אמיתי לפני ה-201, ו-`id`
    אינו נקרא בשום מקום בצד הלקוח (`SwitchFundDialog.tsx` לא משתמש בו) —
    לא כשל, רק שדה לא-בשימוש. לא פעלתי לפיו.
163. **ממצא אמיתי שכן שרד, בקריאה עצמאית מלאה של `server/`:** `POST
    /api/agent` (routes.ts, קודם שורה 136) קורא ל-Anthropic (`claude-sonnet-
    4-5-20250929`, עד 700 טוקנים) על כל בקשה, פתוח לחלוטין לציבור — בלי
    `isAdminRequest`, בלי מפתח, בלי שום הגבלת קצב (`grep -i "rate\|limit"`
    על כל קובצי `server/*.ts` החזיר אפס תוצאות רלוונטיות). זהה במהות ל-
    core.issues #190 (22-get-your-rights `rights-agent`) ו-#192 (01-torah-
    platform `ai-match-teacher`) — שני מקרים שכבר תועדו ותוקנו בעבר באותו
    ריפו: יועץ AI ציבורי בלי הרשאה ובלי תקרה מוציא קרדיט אמיתי בלולאת
    סקריפט. לא תיאורטי: הראוט אינו דורש session/cookie כלל, רק גוף JSON תקין
    (`topicId`+`question` שעוברים zod).
164. **תיקון מינימלי, בתוך התהליך, בלי מיגרציית DB.** בדקתי קודם אם
    `csjekrvukbdznetsrodj` (הפרויקט המחובר בפועל דרך `SUPABASE_SCHEMA=kupot`,
    נגיש ל-MCP כאן) כבר מכיל טבלת/RPC תקרה משותפת כמו ב-#190/#192
    (`ai_rate_limit_hit`) — לא קיים על הפרויקט הזה, ובניית מיגרציה חדשה
    לפרויקט משותף שגם 06/12/17/27 יושבים עליו היא צעד גדול מ"צעד אחד" ובעל
    סיכון-רגרסיה לאפליקציות אחיות. בחרתי בפתרון תואם-היקף: `server/app.ts`
    כבר ממזטן (`getApp()`) מופע Express יחיד למופע ה-lambda החם של Vercel
    (`api/_handler.ts` קורא ל-`getApp()` על כל בקשה) — כלומר מונה בזיכרון-
    תהליך שורד בין קריאות על אותו מופע, בדיוק הדפוס שסקריפט-בלולאה על אותה
    כתובת פוגע בו. נוסף `server/rate-limit.ts` (חלון-נגלל של שעה,
    `Map<string, number[]>`, ~35 שורות, אפס תלות חדשה) ונקודת קריאה יחידה
    ב-`routes.ts` בתחילת ה-handler של `/api/agent` (לפני ה-zod, כדי שגם
    ספאם עם גוף לא-תקין ייחסם בלי לעבוד) — תקרה 20 בקשות/שעה לכל IP
    (`x-forwarded-for`), אותו מספר בדיוק כמו ב-#192, עם הודעת 429 באותו
    נוסח. אפס שינוי לכל ראוט אחר, אפס שינוי לזרימת ההצלחה של `/api/agent`
    עצמו מתחת לתקרה.
165. **אומת בקריאה בלבד.** אין `node_modules`/`tsc` בעץ הזה (כלל אי-התקנה,
    כמו כל סבב קודם) — אומת ש-`api/index.js` (1.8MB) הוא build artifact
    ב-`.gitignore` (`script/build-vercel-fn.ts` בונה אותו מ-`server/` בזמן
    `npm run build:vercel`), כך שהעריכה במקור תיכנס לתוקף בבנייה הבאה בלי
    לגעת בקובץ הבינארי עצמו. קראתי מחדש את שני הקבצים שנערכו/נוצרו
    (`rate-limit.ts` המלא, ובלוק ה-import+handler ב-`routes.ts`) והתאמתי
    טיפוסים (`req.headers`/`req.socket` תואמים מבנית ל-Express `Request`).
    קומיט על `fix/b-kupot-health-funds-agent-rate-limit-0819`, נדחף (מפעיל
    ניסיון פריסת Vercel תחת נתיב 28 בהתאם ל-vercel.json; שינוי קוד שרת
    בלבד, אין שינוי סכימה, אין שינוי לתגובת הצלחה מתחת לתקרה). הבא בתור:
    לסרוק מחדש `core.issues`/`core.project_tasks`; אין עדיין מועמד-מוכן חדש
    בהיקף — הסבב הבא צריך סוכני Explore טריים על אזורים שעדיין לא נסרקו
    לעומק (23-haorech-torani/26-modaot-studio חזרו ריקים כבר פעמיים, כדאי
    זווית שלישית או תת-קבצים ספציפיים לא שנסרקו).

## 19/08/2026 (סבב אוטונומי B, עשרים-ושתיים) — `19 igud-shiurim-portal`: מחיקה שנכשלת בשקט בשישה מקומות, אותו דפוס שכבר תוקן פעם אחת השבוע

166. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1116, 28-kupot-health-funds agent rate-limit,
    commit ddc90898 — כבר `HEAD`/parent הענף הזה, כולל heartbeat קודם) ו-
    `core.issues`/`core.project_tasks` נסרקו מחדש בהיקף 17-31: כל שורה
    `open`/owner=agent שנבדקה (#62 galil, #115 mechiron, #201 השייר-15-egod
    שמחוץ להיקף, #242 22/30) עדיין חסומה בדיוק כפי שתועד — בלי שינוי; שורות
    owner=user גם הן ללא שינוי.
167. **שני סוכני Explore מקבילים** על אזורים שעדיין לא נסרקו לעומק:
    (31-hebrew-bridge-crm) ו-(19-igud-shiurim-portal + 20-igud-portal,
    עם רשימה מפורשת של מה שכבר תוקן היום ב-20 כדי לא לדווח כפילות).
    **31-hebrew-bridge-crm** החזיר רק ממצא חלש (חוסר `assertAdmin` מפורש
    בפונקציה `setCustomFieldValue` שאינה נקראת משום מקום בקוד — dead code,
    כבר מוגן ע"י RLS בפועל) — לא פעלתי לפיו.
168. **ממצא ששרד: 19-igud-shiurim-portal, `public/app.js` — שישה handlers
    של מחיקה בלי try/catch, בדיוק הדפוס שתוקן ב-20-igud-portal הערב
    (commit 70decf83).** `api()` (שורה 89) זורק `Error` על `!res.ok` — כל
    handler שקורא לו בלי try/catch גורם ל-unhandled promise rejection:
    הבקשה נכשלת, אין רענון (`refresh()`/`draw()` לא רצים), ואין הודעת
    שגיאה למשתמש — הכפתור "מחיקה" נראה כאילו לא קרה כלום, בלי שום משוב.
    אומתתי בגריפ שכל שאר ה-handlers בקובץ (submit של טפסים) כן עטופים
    ב-try/catch עם `div` ייעודי (`lessons-alert`/`services-alert`/
    `ads-alert`/`prayers-alert`/`te-lessons-alert`/`te-ads-alert`) — רק
    שישה handlers של מחיקה היו חריגה: שורות 1242 (מחיקת שיעור, לוח ארגון),
    1300 (מחיקת שירות קהילה), 1358 (מחיקת מודעה, לוח ארגון), 1761 (מחיקת
    זמן תפילה), 1972 (מחיקת שיעור, לוח מגיד שיעור), 2032 (מחיקת מודעה, לוח
    מגיד שיעור).
169. **תיקון מינימלי, אותו דפוס בכל שישה מקומות.** כל `await api(...DELETE)`
    נעטף ב-try/catch שמציג שגיאה ב-`div` הקיים כבר בתבנית אותו טאב (אומתתי
    שכל שש התוויות `*-alert` כבר קיימות ב-`content.innerHTML` של אותו טאב
    לפני העריכה) — אפס `div` חדש, אפס שינוי לזרימת ההצלחה (אותה קריאת
    `refresh(); draw()` בדיוק בהצלחה). זהה במהות לתיקון של #150-154
    (20-igud-portal apiPost) אבל כאן הבעיה הייתה בצד הקורא (חוסר try/catch)
    ולא בפונקציית `api()` עצמה (שכבר בדקה `res.ok` נכון מההתחלה).
170. **אומת בקריאה בלבד.** `node --check public/app.js` עבר נקי; קראתי
    מחדש את שישה הבלוקים שנערכו וגריפ אישר ששישה ה-`div` המתאימים
    (`lessons-alert`/`services-alert`/`ads-alert`/`prayers-alert`/
    `te-lessons-alert`/`te-ads-alert`) אכן קיימים בתבנית של אותו טאב.
    אין `node_modules`/דפדפן בעץ הזה (כלל אי-התקנה, כמו כל סבב קודם). system
    19 עדיין `live=false/is_deployed=false` (הכרעת פריסה של המשתמש) —
    התיקון אינו נראה למשתמש אמיתי היום אבל מוכן מראש לכשתיפרס, בדיוק כמו
    התיקון הקודם באותה מערכת (#150-154 בהיקש ל-20). קומיט על
    `fix/b-igud-shiurim-portal-delete-stuck-on-error-0819`, נדחף. הבא בתור:
    לסרוק מחדש `core.issues`/`core.project_tasks`; 31-hebrew-bridge-crm
    ו-20-igud-portal (מעבר לשש handlers האלה) חזרו ריקים/חלשים בסבב הזה —
    מועמדים לזווית שלישית או ל-23/25/29 (manifest-only, סבירות נמוכה) בסבב
    הבא.

## 19/08/2026 (סבב אוטונומי B, עשרים-ושלושה) — `18 torah-editor-mvp`: תשובת "מקור לא נמצא" חסומה מאחורי 404, וה-UI שכבר בנוי בשבילה מעולם לא רץ

171. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה `c1dd65a1`, 19-igud-shiurim-portal six-delete-
    handlers — כבר `HEAD` של הענף הזה) ו-`core.issues`/`core.project_tasks`
    בהיקף 17-31: כל שורה `open` עם owner=agent שנבדקה (#242 22/30 calculator)
    עדיין חסומה בדיוק כפי שתועד; שורות owner=user (#180/#202/#206/#209/#248/
    #249/#250, כולן דורשות דשבורד/MCP שאינו נגיש כאן) ללא שינוי.
172. **שני סוכני Explore מקבילים** על שטח שלא נסרק לעומק לפי ההמלצה בסוף
    #170: (18-torah-editor-mvp — לא נסרק ספציפית לבאגים מעולם, רק ל-HTR
    ב-#241) ו-(29-bkalot-design — נבדק אם באמת שלד או שיש קבצים אמיתיים;
    התברר שיש `build.js`/`tokens.json`/`upload.sh` אמיתיים, לא רק manifest).
    **29-bkalot-design** החזיר רק ממצא חלש: `upload.sh` שורה 9 מטמיע ברירת
    מחדל ל-`SUPABASE_ANON_KEY` בקוד — אבל זהו מפתח anon ציבורי (מוגן RLS
    מטבעו, לא סוד), האפליקציה כלל אינה פרוסה, וה-fallback לא שונה מכל שאר
    ה-anon keys שכבר חשופים בכל אפליקציית לקוח בריפו הזה — לא פעלתי לפיו.
173. **ממצא ששרד: 18-torah-editor-mvp, `app/api/verify/route.ts`.**
    `app/editor/page.tsx` שורה 24 מגדירה `Verification.status:
    'match'|'deviation'|'missing'`, ושורה 362 מרנדרת "המקור לא נמצא" עבור
    `status==='missing'` — אבל ה-API (route.ts שורות 19-24, לפני התיקון)
    החזיר `{error:...}` עם HTTP 404 כשה-ref לא נמצא ב-Sefaria.
    `verifyCitation()` (page.tsx שורה 137) בודקת רק `!res.ok` וזורקת
    `Error` גנרית לתוך ה-`catch` — `setVerifications` אף פעם לא נקרא במקרה
    הזה, כך שה-branch של `status==='missing'` הוא קוד מת שלא ניתן להגיע
    אליו לעולם. משתמש שמצטט מקור עם טעות הקלדה/ref לא-קיים רואה הודעת
    שגיאה גנרית ("שגיאה באימות מקור") במקום האינדיקציה הייעודית "המקור לא
    נמצא" שכבר נבנתה בשבילו.
174. **תיקון מינימלי, אדיטיבי, בלי לגעת בזרימת ההצלחה.** route.ts שורות
    19-24 שונו כך שבמקום 404+error מוחזר 200 עם `{ref, status:'missing',
    distance:null, sourceText:'', sourceUrl}` — אותה צורת תשובה בדיוק כמו
    כל תשובת verify אחרת (התאמה לחתימת ה-return המוצלח בשורה 65-71), כך
    שה-`Verification` state מתמלא כרגיל וה-UI הקיים כבר מרנדר אותו נכון.
    אפס שינוי ל-400 (ref חסר) ולזרימת match/deviation/no-quote-provided.
175. **אומת בקריאה בלבד.** קראתי מחדש את route.ts המלא ואת page.tsx סביב
    שורות 127-144 ו-359-363 — אין `node_modules`/`tsc`/דפדפן בעץ הזה (כלל
    אי-התקנה, כמו כל סבב קודם), `node --check` על קובץ TS נכשל כצפוי
    (סיומת בלבד, לא שגיאת תחביר JS). קומיט על
    `fix/b-torah-editor-verify-missing-status-0819`, נדחף. הבא בתור: לסרוק
    מחדש `core.issues`/`core.project_tasks`; 29-bkalot-design ו-18 (מעבר
    ל-verify) חזרו חלשים/מכוסים בסבב הזה — מועמדים ל-21/23/25/26/31 בזווית
    רביעית או תת-קבצים ספציפיים לא נסרקו בסבב הבא.

## 19/08/2026 (סבב אוטונומי B, עשרים-וארבעה) — `26 modaot-studio`: PATCH/DELETE על פרויקטים ומותגים בלי שום בדיקת בעלות, במערכת חיה

176. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1120, 18-torah-editor-mvp verify-missing-
    status, קומיט `f70145d0`/`05050c92` — כבר `HEAD`/parent הענף הזה,
    heartbeat קודם כבר נרשם) ו-`core.issues` המלא בהיקף 17-31/37: כל שורה
    פתוחה owner=agent (#62 galil, #115 mechiron, #201 egod, #242 22/30)
    עדיין חסומה בדיוק כפי שתועד; שורות owner=user ללא שינוי. `core.project_tasks`
    בהיקף — ריק.
177. **שלושה סוכני Explore קריאה-בלבד במקביל** על שטח שחזר חלש/ריק בסבבים
    קודמים לפי ההמלצה בסוף #175: 26-modaot-studio (זווית שלישית — נתיבי
    API/מכסות/מירוצים, לא רק handlers עליונים), 24-galilee-connect-hub
    (מעבר לתיקון gabai-admin-login שכבר נסגר הערב), ו-21-mthbram (מעבר
    ל-#250/#164 שכבר תועדו וחסומים).
178. **שלושה ממצאים חזרו, כולם עברו את האימות העצמי של הסוכנים.**
    24-galilee-connect-hub: `GabaiPortal.tsx` כמה נקודות מחיקה-ואז-הכנסה
    בלי בדיקת שגיאה (אובדן-נתונים שקט אפשרי). 21-mthbram:
    `NedarimManagement.tsx:44` עדכון סטטוס בלי בדיקת שגיאה אחרי הכנסת שיעור
    (אותו דפוס "כפתור תקוע" שכבר תוקן פעמים רבות היום). 26-modaot-studio:
    הממצא החמור ביותר — נבחר.
179. **הבאג שנבחר: `vercel-adapter/api/_lib/server/routes.ts`, ארבעת
    ה-endpoints `PATCH`/`DELETE` על `/api/projects/:id` ו-`/api/brands/:id`
    (שורות 217-225, 304-314 לפני התיקון) מעולם לא קראו ל-`getUserIdFromToken`
    — לא רק שלא בדקו בעלות, הן לא ניסו בכלל.** אימתתי בעצמי (לא רק לפי
    דיווח הסוכן): `storage.ts` — `updateProject`/`deleteProject`/
    `updateBrand`/`deleteBrand` (שורות 188-199, 217-228 לפני התיקון) סיננו
    רק לפי `id`, בלי שום השוואה ל-`user_id`. לעומת זאת `GET /api/projects`
    ו-`POST /api/projects` (וכך גם brands) כן קוראים ל-`getUserIdFromToken`
    וממיינים/מתייגים לפי המשתמש (הערה מפורשת בקוד מעל `/api/projects`
    מסבירה את הכוונה). כלומר: כל בקשה (כולל אנונימית לגמרי — אין אפילו
    קריאה לטוקן) שידעה/ניחשה `id` מספרי יכלה לשנות/למחוק פרויקט או מותג
    ששייך למשתמש מחובר אחר. אימתתי מול `core.projects`: מערכת 26
    `live=true`, `is_deployed=true` — הפרצה ניתנת לניצול בייצור **עכשיו**,
    לא רק תיאורטית. הסוכן שדיווח את הממצא נתן הוכחת-קריאה מלאה (כל
    ארבעת ה-endpoints וה-storage methods) לפני שדיווח.
180. **התיקון: בדיקת `user_id` קיים מול המבקש, לא נעילה עיוורת.** הוספתי
    פרמטר `requesterId` אופציונלי ל-`updateProject`/`deleteProject`/
    `updateBrand`/`deleteBrand`, ובכל אחד — קריאת `select("user_id")` על
    השורה הקיימת קודם: אם `user_id` השורה `NULL` (פרויקטים/מותגים
    שנוצרו לפני שהעמודה נוספה, "אנונימיים מהיסטוריה") — עריכה/מחיקה
    ממשיכות פתוחות לכולם בדיוק כמו היום, אפס רגרסיה לזרימה האנונימית
    הקיימת (שהיא בכוונה, לפי ההערה הקיימת בקוד). אם `user_id` קיים ולא
    תואם את `requesterId` — `update*` מחזיר `undefined` (אותו נתיב 404
    הקיים כבר), ו-`delete*` הופך ל-no-op (אותו `{ok:true}` הקיים כבר
    לבקשת מחיקה על id שלא קיים) — לא חושף הבדל בין "לא קיים" ל"לא שלך".
    ה-routes עודכנו לקרוא ל-`getUserIdFromToken` (בדיוק כמו ב-GET/POST
    הקיימים) ולהעביר את התוצאה. אימתתי שהלקוח (`client/src/lib/
    queryClient.ts`) כבר מצרף כותרת `Authorization` לכל בקשה כשמשתמש
    מחובר (אותו מנגנון ש-GET/POST כבר מסתמכים עליו) — אין שינוי נדרש
    בצד הלקוח.
181. **בדק שני: קובץ `server/` (לא `vercel-adapter/`) לא נגע.** יש עותק
    מקור נוסף ב-`apps/26-modaot-studio/server/` (SQLite/drizzle, לא
    Supabase) — הערה מפורשת בראש `server/storage.ts` אומרת "dev בלבד: אין
    כאן חיבור ל-Supabase auth, ולכן userId תמיד undefined בפועל", וההערה
    בראש `vercel-adapter/api/_lib/server/storage.ts` מסבירה שזהו המסלול
    שבאמת פרוס (Vercel serverless, אין מערכת קבצים בת-קיימא, ולכן טבלאות
    `studio_*` ב-Supabase). כלומר `server/` הוא סביבת פיתוח מקומית בלבד
    שאף פעם לא נושאת בעלות אמיתית — לא נגעתי בו, כדי לא להרחיב סקופ
    לקובץ שלא באמת חשוף.
182. **אומת בקריאה בלבד.** אין `node_modules`/`tsc` בעץ הזה (כלל
    אי-התקנה, כמו כל סבב קודם) — `node --check` על קבצי TS נכשל כצפוי
    (סיומת בלבד). קראתי מחדש את שני הקבצים המלאים אחרי העריכה, אימתתי
    שהטיפוסים החדשים (`requesterId?: string`) תואמים למוסכמת הקובץ (אותו
    דפוס `userId?: string` הקיים כבר בשאר המתודות), ושה-cast
    `as { user_id: string | null }` תואם למוסכמת ה-`unwrap<T>` הקיימת
    בקובץ (שממילא מטפלת ב-null בזמן ריצה למרות החתימה). דרש `git add -f`
    (הנתיב תחת `apps/26-modaot-studio/vercel-adapter` מוגדר `.gitignore`-ed
    למרות שהקבצים כבר עוקבו — אותו דפוס שתועד בסבבים קודמים לקבצים
    חדשים תחת `apps/**`). קומיט `e4ab93aa` על
    `fix/b-modaot-studio-project-brand-ownership-0819`, נדחף (מפעיל פריסת
    Vercel תחת `more30.com/studio`; שינוי server-only, אפס שינוי לתשובת
    ה-GET/POST המוצלחת, אפס שינוי לזרימת "פרויקט/מותג אנונימי" הקיימת).
    הבא בתור: לסרוק מחדש `core.issues`/`core.project_tasks`;
    24-galilee-connect-hub (מחיקה-ואז-הכנסה בלי בדיקת שגיאה ב-
    `GabaiPortal.tsx`) ו-21-mthbram (`NedarimManagement.tsx:44` עדכון
    סטטוס בלי בדיקת שגיאה) הם שני מועמדים מוכנים-מראש לסבב הבא, שניהם
    ניתנים לאימות ותיקון באותו דפוס שכבר שימש היום ב-#166-170/#175-178.

## 19/08/2026 (סבב אוטונומי B, עשרים-וחמישה) — `24 galilee-connect-hub`: שמירת פרטי יצירת קשר יכולה למחוק את השורה הקיימת בלי שהחדשה תיכתב

183. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1121, 26-modaot-studio ownership fix,
    קומיטים `e4ab93aa`/`0567605a` — כבר `HEAD`/parent הענף הזה) ו-
    `core.issues` המלא בהיקף 17-31: כל שורה פתוחה owner=agent (#62 galil,
    #115 mechiron, #167/#201 egod, #242 rights/crm) עדיין חסומה בדיוק
    כפי שתועד; owner=user ללא שינוי. `core.project_tasks` בהיקף — ריק.
184. **נלקח ישירות המועמד המתועד מסבב 24 (#182), ואומת מחדש בעצמי לפני
    תיקון — לא הונח.** `apps/24-galilee-connect-hub/src/pages/
    GabaiPortal.tsx`, `ContactInfoManager.handleSave` (שורות 551-565
    לפני התיקון): `await ...delete().eq('category', 'פרטי_יצירת_קשר')`
    ואז `await ...insert({...})` — בלי בדיקת שגיאה על אף אחת מהשתיים,
    ובלי `try/catch`. אם ה-`insert` נכשל (רשת/ולידציה/הרשאה) אחרי
    שה-`delete` כבר הצליח, השורה היחידה בקטגוריה נמחקת לגמרי והתוכן החדש
    אף פעם לא נכתב — ואז `setSaving(false); setSaved(true)` עדיין רצים
    ללא תנאי, כך שהאדמין רואה "✅ נשמר!" בזמן שהמידע פשוט נמחק. אימתתי
    בגריפ ששתי דפים ציבוריים קוראים בדיוק את אותה שורה: `ContactPage.tsx`
    (טופס יצירת הקשר הציבורי) ו-`Footer.tsx` (הפוטר בכל האתר) — שניהם
    שולפים `category='פרטי_יצירת_קשר'` ונופלים לערכי ברירת-מחדל קשיחים
    (כתובת/טלפון/מייל ישנים) רק כש-`data.length===0`, כלומר כישלון שקט
    כאן היה גורם לכתובת/טלפון/מייל האמיתיים להיעלם מהאתר החי ולהוחלף
    בערכים היסטוריים שגויים, לכל מבקר, בלי שאף אחד ידע. אימתתי מול
    `core.projects`: מערכת 24 `live=true`/`is_deployed=true`,
    `live_url=https://more30.com/galil` — סיכון ייצור ממשי, לא תיאורטי.
185. **תיקון: הפוך את סדר הפעולות ל-insert-then-cleanup, לא
    delete-then-insert, עם בדיקת שגיאה אמיתית.** קודם `insert(...)
    .select('id').single()`; אם `insertError` או שאין שורה חוזרת —
    עוצרים מיד, לא נוגעים בשורה הישנה, ומציגים הודעת שגיאה חדשה
    (`saveError` state, טקסט ליד הכפתור, בדיוק כמו התג `saved` הקיים
    כבר) במקום "✅ נשמר!" שקרי. רק אם ה-insert הצליח — `delete()...
    .neq('id', inserted.id)` מנקה את השורה/השורות הישנות. במקרה הגרוע
    ביותר (insert מצליח, delete נכשל) יכולות להישאר זמנית שתי שורות
    פעילות באותה קטגוריה — לא אובדן מידע, לכל היותר ה-`data[0]` שהצד
    הציבורי קורא עשוי להיות הישן עד ניסיון שמירה נוסף; זה עדיף באופן
    מובהק על המחיקה השקטה שהייתה קודם. אימתתי מול migration
    `20260322204537...sql` ש-`knowledge_base.id` הוא `uuid PRIMARY KEY`
    וש-RLS על הטבלה היא `FOR ALL USING (true) WITH CHECK (true)` (כתיבה
    פתוחה לגמרי כבר היום) — כך שה-`.select('id')` אחרי ה-insert מחזיר
    שורה בלי חסם הרשאות חדש.
186. **אומת בקריאה בלבד.** אין `node_modules`/`tsc` בעץ הזה (כלל
    אי-התקנה, כמו כל סבב קודם). קראתי מחדש את כל הבלוק שנערך (state
    חדש, handleSave, וה-JSX של הכפתור/הודעת השגיאה) וגריפ אישר ש-4
    שאר מקומות ה-`delete(` בקובץ (בתי כנסת, אדמינים, הלכה, עלונים,
    באנרים, ידע, לידים, שאלות לרב) הם מחיקות-לפי-id רגילות, לא
    delete-then-insert — הבאג היה נקודתי לניהול פרטי יצירת הקשר בלבד.
    קומיט על `fix/b-galilee-connect-hub-contact-info-data-loss-0819`,
    נדחף (מפעיל פריסת Vercel תחת `more30.com/galil`; אפס שינוי לזרימת
    השמירה המוצלחת הקיימת מלבד ה-`select('id')` הנוסף, אפס שינוי
    לשאר 11 מנהלי ה-CRUD באותו קובץ). הבא בתור: לסרוק מחדש
    `core.issues`/`core.project_tasks`; 21-mthbram
    (`NedarimManagement.tsx:44`, עדכון סטטוס בלי בדיקת שגיאה אחרי הכנסת
    שיעור) נשאר מועמד מוכן-מראש מסבב 24, לא נגעתי בו הסבב הזה כדי
    לשמור על צעד אחד ממוקד.

## 19/08/2026 (סבב אוטונומי B, עשרים-ושישה) — `21-mthbram`: אישור/פרסום שיעור מנדרים פלוס מדווח הצלחה גם כשעדכון הסטטוס נכשל

187. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1123, 24-galilee-connect-hub
    delete-then-insert fix, קומיט a56731d2 — כבר `HEAD`/parent הענף
    הזה) ו-`core.issues` המלא בהיקף 17-31: כל שורה פתוחה owner=agent
    (#62 galil, #115 mechiron, #167/#201 egod, #242 rights/crm, #196
    torah) עדיין חסומה בדיוק כפי שתועד; owner=user ללא שינוי אמיתי
    (רק ריכוזי ניסוח של אותם חסמים חיצוניים).
188. **נלקח המועמד המתועד מסבב 25 (#186), אומת מחדש בעצמי לפני תיקון.**
    `apps/21-mthbram/src/pages/NedarimManagement.tsx`,
    `publishToLessons` (שורה 44 לפני התיקון): אחרי `insert` מוצלח לטבלת
    `lessons`, `await supabase.from("nedarim_submissions").update({
    status: "published" })...` רץ בלי לקרוא את `error` שלו בכלל, ומיד
    אחריו `toast.success` ו-`fetchSubmissions()` רצים ללא תנאי. אם
    ה-`update` נכשל (רשת/RLS/הרשאה) — השורה ב-`nedarim_submissions`
    נשארת `status: "new"` במסד, אבל האדמין רואה "הועבר לפרסום
    בהצלחה!" שקרי. גרוע יותר: מכיוון ש-`fetchSubmissions()` שולף
    מחדש מהמסד, הכפתור "העבר לפרסום" חוזר להופיע (כי הסטטוס עדיין
    `new`) — לחיצה חוזרת יוצרת שורת `lessons` **כפולה** לאותה
    הגשה, כי ה-`insert` הראשון כבר הצליח ואין שום בדיקת אידמפוטנטיות.
    בגריפ נמצא **שכפול מדויק של אותו באג** ב-
    `apps/21-mthbram/src/pages/AdminDashboard.tsx`, `approveNedarim`
    (שורה 380 לפני התיקון) — אותה תבנית insert-then-update-no-check,
    אותה תוצאה (toast הצלחה שקרי + כפתור "אישור" חוזר + סיכון כפילות).
    שני הקבצים משרתים את אותה טבלה (`nedarim_submissions`) משתי מסכי
    ניהול שונים באותה מערכת.
189. **התיקון: קריאת ה-`error` מה-update ותגובה אמיתית, לא רק לוג.**
    בשני הקבצים: אם `statusError` — `toast.error` עם ההודעה האמיתית
    (השיעור כן נוצר, רק הסטטוס לא התעדכן) ועדיין `fetch*()` מחדש (כדי
    שהממשק יציג את המצב האמיתי מהמסד, כולל הכפתור החוזר להופיע —
    מכוון, לא תקלה: זה מאפשר לאדמין לנסות שוב), ורק `return` בלי
    `toast.success` השקרי. נשמר בדיוק אותו נתיב הצלחה קיים כשה-update
    מצליח (אין שינוי להתנהגות התקינה). לא טופל סיכון הכפילות עצמו
    (insert בלי unique constraint על `nedarim_submissions.id`/`lessons`)
    — זה דורש migration בפרויקט Supabase של 21 (`aypsqqvfohekxxuqsmrw`),
    שאינו נגיש ל-MCP בסביבה הזו (אותו חסם המתועד ב-core.issues #207/
    #250); הצעד הזה רק עוצר את ה-false-positive-success שגורם למשתמש
    ללחוץ שוב מבלי לדעת שההגשה הראשונה בכלל לא פורסמה כראוי.
190. **אומת בקריאה בלבד.** אין `node_modules`/`tsc` בעץ הזה (כלל
    אי-התקנה, כמו כל סבב קודם) — `node --check` על `.tsx` נכשל כצפוי
    (סיומת בלבד, אותה הערה כמו סבבים קודמים). קראתי מחדש את שני הבלוקים
    שנערכו במלואם, אימתתי שתבנית ה-`{ error: statusError }` תואמת את
    מוסכמת שני הקבצים (שני המקומות כבר משתמשים ב-destructuring עם שם
    חלופי ל-`error` השני באותה פונקציה — ראו למשל `const { error } =
    ...insert...` שכבר קיים בשתי הפונקציות), וגריפ אישר שאין מופע שלישי
    של אותה תבנית — `MatchingTab.tsx` קורא מ-`nedarim_submissions`
    בקריאה בלבד (`select`), לא כותב. קומיט על
    `fix/b-mthbram-nedarim-publish-silent-status-fail-0819`, נדחף
    (מפעיל פריסת Vercel תחת `more30.com/mthbram`; אפס שינוי לנתיב
    ההצלחה הקיים בשני הקבצים, רק תוספת בדיקת שגיאה על קריאה שלא
    נבדקה קודם). הבא בתור: לסרוק מחדש `core.issues`/`core.project_tasks`
    פעם נוספת; רוב הפתחים הפתוחים בהיקף כרגע חסומים על גישת
    Supabase/Lovable/הכרעת משתמש חיצונית — סבב הבא צריך שני סוכני
    Explore בזוויות טריות (או שטח שלא נסרק לאחרונה: 23-haorech-torani,
    29-bkalot-design, 37-bkalot-clone) כדי למצוא מועמד חדש.

## 19/08/2026 (סבב אוטונומי B, עשרים-ושבע) — `29 bkalot-design`: upload.sh דיווח הצלחה גם כשההעלאה ל-Supabase Storage נכשלה

191. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1124, 21-mthbram publish/approve
    silent-status fix, קומיט aa6a80f8 -- כבר `HEAD`/parent הענף הזה) ו-
    `core.issues` המלא בהיקף 17-31: כל שורה פתוחה owner=agent (#242
    rights/crm) ו-owner=user (#180/#190→fixed בפועל/#202/#206/#209/#248/
    #249/#250) עדיין חסומה בדיוק כפי שתועד, ללא שינוי אמיתי.
    `core.project_tasks` בהיקף — רק #25 (25-galilee, איתור origin, לא
    שינוי קוד).
192. **שלושה סוכני Explore מקבילים על השטח שתועד כלא-נסרק בסוף סבב 26**
    (#190): 23-haorech-torani, 29-bkalot-design, 37-bkalot-clone.
    23-haorech-torani התברר כשלד-קובץ-בודד בלבד (HTML תיעודי, `live:
    false`, אין קוד הרצה בכלל) — אין מה לבדוק שם. 37-bkalot-clone החזיר
    שני ממצאים: (א) חוסר בדיקת בעלות/הקצאה ב-`bkalot_clone_admin_set_status`
    — לא נלקח: הסכמה `bkalot_clone` עברה כבר 102 מיגרציות ממוקדות-מאוד
    (`0001`-`0102`, מוסכמת שמות "the X cannot Y") שלא נכתבו בסבב הזה
    ולא ברור לי המודל התפעולי המלא (האם כל המנהלים הם צוות אחד מהימן
    בכוונה) בלי סבב קריאה ייעודי; (ב) "הצלחה שקטה" ב-`bkalot-clone-intake/
    index.ts` — נבדק ישירות ונמצא **לא אמיתי**: הפונקציה כבר מתועדת
    בכוונה (הערות 93-96 בקובץ) להעביר את גוף תשובת ה-RPC כלשונו (200
    גם על דחיית ולידציה תקינה, כדי ש-non-2xx יישאר סימן יחיד ל"לא
    הטופס") — הלקוח כבר קורא `ok`/`error` מהגוף בכל מקרה, אין העתק שני
    של כללי האימות. false positive, לא טופל.
193. **29-bkalot-design סיפק ממצא אמיתי, שאומת ישירות בקריאה שלי, לא
    הונח מדוח הסוכן.** `apps/29-bkalot-design/upload.sh`, פונקציית
    `upload()`: `curl -s` (בלי `--fail`/`-f`) מעלה קובץ ל-Supabase
    Storage ומדפיס `[HTTP %{http_code}]` דרך `-w`, אבל לא בודק את
    הקוד בכלל — קוד יציאה של curl הוא 0 גם על 401/403/500, ולכן
    `set -e` לא עוצר את הסקריפט, ושורת הסיום התנייה "✓ הועלה. כתובת
    ציבורית:" מודפסת תמיד, גם כשההעלאה נכשלה בפועל. אימתתי מול
    README.md של האפליקציה: `bkalot-theme.css` שמעלה הסקריפט הזה הוא
    "מקור-האמת היחיד" שכל המערכות הממותגות בקלות טוענות דרך `<link>`
    לכתובת הציבורית הקבועה — כלומר העלאה כושלת ושקטה משאירה את כל
    המערכות תלויות ב-CSS ישן, בעוד המפעיל (אנושי, מריץ ידנית) רואה
    "✓ הועלה" ומאמין שהעדכון יצא. `app.json` מסמן `live:false` עבור
    האפליקציה עצמה (אין לה אתר משלה), אבל קובץ ה-CSS שהיא מייצרת חי
    בפועל בפרודקשן דרך ה-URL הציבורי הקבוע.
194. **התיקון: לכידת קוד ה-HTTP וגוף התשובה, בדיקה אמיתית לפני הודעת
    הצלחה.** כל קריאת `upload()` כותבת את הגוף לקובץ זמני
    (`/tmp/bkalot-design-upload-response.$$`, ייחודי לתהליך), בודקת
    `[[ "$http_code" != 2* ]]` — אם לא 2xx, מדפיסה שגיאה + גוף התשובה
    ל-stderr ויוצאת עם `exit 1` **לפני** שהסקריפט מגיע לשורת ההצלחה
    (כך שני הקבצים נבדקים בנפרד: אם הראשון נכשל, השני לא מועלה, במקום
    להסתיר כשל חלקי). קובץ הזמני נמחק בשני הנתיבים. אפס שינוי לנתיב
    ההצלחה הקיים (עדיין מדפיס `[HTTP $code]` ואז ממשיך).
195. **אומת בקריאה בלבד.** אין npm/curl-שרת זמינים בסביבה הזו להרצה
    אמיתית (כלל אי-הרצה, כמו כל סבב קודם) — `bash -n` על הקובץ עבר
    נקי (תחביר תקין); `shellcheck` לא מותקן בסביבה, לא ניתן להריץ.
    קראתי מחדש את כל הקובץ אחרי העריכה, אימתתי שה-`local http_code`/
    ה-glob `!= 2*` בתוך `[[ ]]` תואמים למוסכמת bash תקנית, ושאין קריאה
    שלישית ל-`upload()` בקובץ שהייתה מפספסת את התיקון. קומיט
    `b6fb29b0` על `fix/b-bkalot-design-upload-silent-failure-0819`,
    נדחף (הקובץ אינו נטען ע"י Vercel/build כלשהו -- מופעל ידנית בלבד
    ע"י מפעיל אנושי, אז אין פריסה אוטומטית לבדוק; אפס שינוי לתוכן
    ה-CSS/tokens עצמם). הבא בתור: לסרוק מחדש `core.issues`/
    `core.project_tasks` פעם נוספת; 37-bkalot-clone (חוסר בדיקת
    בעלות/הקצאה ב-`admin_set_status`) נשאר מועמד לא-מטופל שדורש סבב
    קריאה ייעודי במוסכמות ה-102 המיגרציות הקיימות לפני כל שינוי, לא
    תיקון-אחת-שורה כמו היום.

## 20/08/2026 (סבב אוטונומי B, עשרים-ושמונה) — `30 zchuyotpro-crm`: כל חבר ארגון (לא רק מנהל) יכול היה לשכתב אסימוני אינטגרציה והגדרות התראות

196. **סריקה תחילה.** נקראו README.md/CONNECTIONS.md, `core.run_progress`
    (האחרון בהיקף לופ B היה id 1125, 29-bkalot-design upload.sh
    silent-failure fix, קומיטים b6fb29b0/651f1629 -- כבר `HEAD`/parent
    הענף הזה) ו-`core.issues` המלא בהיקף 17-31/37: כל שורה פתוחה
    owner=agent (#5 עיצוב ייחודי, #6 פלטפורמת מעטפת, #62 galil,
    #115 mechiron, #120 עמוד מסלולים, #201 איפוס סיסמה) ו-owner=user
    (מרבית הרשימה: #4/#7/#10-18/#28/#35/#63/#88/#92/#94/#138/#152/#156/
    #164/#167-172/#180/#196/#202-#210/#220/#229/#242/#245/#248-#250)
    עדיין חסומה בדיוק כפי שתועד, ללא שינוי אמיתי. `core.project_tasks`
    בהיקף — שום שורה חדשה.
197. **שלושה סוכני Explore מקבילים על שטח שכבר נסרק פעם או פעמיים
    בסבבים קודמים, בזוויות חדשות מפורשות** (הוצהר לכל סוכן מה כבר
    תוקן כדי שלא ידווח שוב על אותו דבר): 30-zchuyotpro-crm (מעבר
    לתיקוני role/self-escalation שכבר בוצעו), 19/20-igud (מעבר לשש
    כפתורי המחיקה ול-apiPost שכבר תוקנו), 27-bkalut-price/
    28-kupot-health-funds (מעבר לאימות /api/orgs ולהגבלת קצב שכבר
    בוצעו). שני ממצאים חלשים לא נלקחו: 19-igud-shiurim-portal כפתור
    "סמן כנקרא" חסר try/catch (חוזר על אותו דפוס שכבר תוקן שש פעמים
    היום, אבל בעדיפות נמוכה מהממצא של 30, לא נלקח בסבב זה); 27/28
    שגיאות DB לא-מטופלות בנתיבי POST פיננסיים (ה-handler הגלובלי כבר
    תופס אותן ומחזיר 500 -- לא כשל שקט, רק חוסר עקביות, נמוך מדי
    לפעולה).
198. **30-zchuyotpro-crm סיפק ממצא אמיתי בעל חומרה גבוהה, שאומת ישירות
    בקריאה שלי, לא הונח מדוח הסוכן.** `tenant_self_update` (מיגרציה
    `20260610191501`) היא מדיניות ה-UPDATE היחידה על `public.tenants`,
    ובודקת רק `id = get_my_tenant_id()` -- שום בדיקת תפקיד. גריפ אישר
    ששני האתרים היחידים שכותבים ל-`tenants` הם `IntegrationsTab`
    ו-`NotificationsTab` ב-`settings.tsx`, שניהם קוראים
    `supabase.from("tenants").update({settings: ...})` ללא כל בדיקת
    תפקיד בצד הלקוח. `tenants.settings.integrations` מחזיק אסימונים
    אמיתיים (`nedarim_token`, `imot_token`, `whatsapp_instance_id`,
    `n8n_base_url`) -- כלומר כל `agent`/`viewer` חתום-פנימה יכול היה
    לשכתב/למחוק אסימון תשלום או הודעות של הארגון, בדיוק אותה צורת
    הסלמה-עצמית שכבר נסגרה על `profiles.role` (#248,
    `20260819230000_guard_profile_role_change.sql`) -- אותו דפוס, טבלה
    אחרת.
199. **התיקון: טריגר DB תואם + חסימת UI בצד הלקוח.** נוספה מיגרציה
    `20260820120000_guard_tenant_settings_change.sql` -- פונקציה
    `guard_tenant_settings_change` (אותה תבנית `SECURITY DEFINER` +
    `get_my_role()` כמו `guard_profile_role_change`) שחוסמת `UPDATE` על
    `tenants.settings` אלא אם `get_my_role() = 'admin'`, מחוברת דרך
    `BEFORE UPDATE` טריגר. נוסף גם `myProfileQuery()` חדש ל-
    `features/settings/queries.ts` (מסנן `profiles` לפי
    `auth_user_id = auth.uid()` בפועל -- לא `.limit(1)` גרידא, כי
    `profiles_self_read` כבר מחזירה כל שורה בארגון, לא רק את שלי) ו-
    `IntegrationsTab`/`NotificationsTab` עכשיו קוראים לו ומשביתים את
    שדות הטופס וכפתור השמירה למי שאינו `admin`, עם הודעת "צפייה בלבד".
    אפס שינוי לנתיב השמירה הקיים עבור מנהל אמיתי.
200. **אומת בקריאה בלבד; לא נפרס.** אין `node_modules`/`tsc` בעץ הזה
    (כלל אי-התקנה, כמו כל סבב קודם) -- `node --check` על `.ts`/`.tsx`
    נכשל כצפוי (סיומת בלבד, אותה הערה כמו סבבים קודמים). קראתי מחדש
    את שני הקבצים שנערכו במלואם, וגריפ אישר שאין אתר כתיבה שלישי
    ל-`tenants` בכל `src/`. `jhbeelzvjvhnkxldqvxx` (הפרויקט של 30)
    אינו נגיש ל-MCP בסביבה הזו (אותו חסם מתועד ב-#248/#249) -- קובץ
    המיגרציה נכתב בריפו בלבד, לא נפרס; רשום כ-`core.issues #251`
    (severity=high, owner=user, blocked_on: לפרוס דרך Lovable/SQL
    editor/דשבורד Supabase, אותו נתיב פריסה כמו #248/#249). קומיט
    `77246738` על `fix/b-zchuyotpro-crm-tenant-settings-role-gap-0820`,
    נדחף (מפעיל פריסת Vercel תחת `more30.com/crm`; חסימת ה-UI בצד
    הלקוח פעילה מיד עם הפריסה גם לפני שהטריגר עצמו נפרס, כי
    `isAdmin` מחושב מ-`profiles.role` הקיים ולא תלוי במיגרציה החדשה
    -- כלומר יש כיסוי הגנה-בעומק חלקי מהשנייה הראשונה, גם אם ה-RLS
    עצמו עדיין פתוח עד לפריסת המיגרציה). הבא בתור: לסרוק מחדש
    `core.issues`/`core.project_tasks` פעם נוספת; 19-igud-shiurim-portal
    כפתור "סמן כנקרא" חסר try/catch נשאר מועמד קל לסבב הבא.
201. **שלושה סוכני Explore מקבילים על זוויות חדשות חזרו ריקים/כוזבים,
    כל אחד נבדק בעצמי לפני שנפסל.** 31-hebrew-bridge-crm (אותה
    זוויה שסגרה כמה חורים ב-30: role-escalation/settings-write ללא
    בדיקת תפקיד) -- מאובטח כראוי: `user_roles` חסום ע"י מדיניות
    RESTRICTIVE + `private.has_role()` בסכימה פרטית שאינה חשופה
    ל-API, ואין נתיב כתיבה ישיר בקוד הלקוח לטבלה. 37-bkalot-clone
    (זווית אחסון-קבצים/אימות-קלט/XSS) -- ה-RPC של ה-intake דוחה
    שדה `documents` מפורשות (מיגרציה 0058), מגבלת גוף 16KB, ו-
    `admin.html` משתמש ב-`.textContent` בלבד בכל מקום שמציג PII;
    חולשת ה-wildcard-injection בחיפוש כבר תוקנה ואומתה בפרודקשן
    (מיגרציה 0094). 26-modaot-studio/29-bkalot-design -- טענת
    "cache-busting חסר גורמת ל-CSS מיושן" לא שרדה בדיקה עצמית: `curl
    -I` לכתובת הציבורית החיה של `bkalot-theme.css` החזיר בפועל
    `cache-control: no-cache` + `cf-cache-status: REVALIDATED`, כלומר
    אין בעיית מטמון בכלל.
202. **מצאתי ממצא אמיתי בעצמי דרך grep רוחבי אחר `dangerouslySetInnerHTML`/
    `innerHTML` על כל 15 האפליקציות בהיקף.** `apps/22-get-your-rights/
    src/components/RightBrandedCard.tsx` (`buildFieldHTML`/
    `buildTitleSection`) מרכיב ערכי שורה מ-`rights_reference`
    (`topic_name`/`category`/שמונה שדות תוכן חופשי) ישירות לתוך מחרוזת
    HTML בלי שום escaping, ו-`AdminRightsReference.tsx` (הצרכן היחיד,
    אומת ב-grep) מציג אותה דרך `dangerouslySetInnerHTML` לתצוגה
    מקדימה/הורדה כתמונה. RLS על `rights_reference`: כתיבה רק ל-
    `has_role(auth.uid(),'admin')` (מיגרציה `20260302211649`), קריאה
    פתוחה לכולם -- כלומר לא חור ציבורי, אבל XSS-מאוחסן אמיתי:
    admin שנפרץ/זדוני יכול לשתול payload בכל אחד משמונה השדות שירוץ
    בדפדפן של admin אחר (עם ה-token החי שלו ב-localStorage) בפעם הבאה
    שהוא יפתח את אותה שורת-ייחוס.
203. **תוקן עם `escapeHtml()` בכל נקודת הרכבה דינמית ל-HTML** (תוכן
    השדה לפני המרת `\n`->`<br/>` הקיימת, טקסט התצוגה של `service_link`,
    `category`/`topic_name` גם בכותרת וגם בתווית עמוד-המשך) -- אפס
    שינוי לשימושים בטקסט-רגיל-בלבד (טקסט שיתוף וואטסאפ/מייל, נושא
    mailto, שם קובץ הורדה) שאינם מוצגים כ-HTML כלל, ולכן נשארו לא-
    escaped בכוונה. מערכת 22 חיה (`live=true`/`is_deployed=true`) תחת
    `more30.com/zchuyot` -- חשיפה אמיתית למסך הניהול בפרודקשן.
204. **אומת בקריאה חוזרת מלאה של הקובץ שנערך**, כולל אישור ב-grep
    ששלושת אתרי השימוש בטקסט-רגיל (`buildShareText`/
    `downloadBrandedImage` filename/`shareViaEmail` mailto) נשארו לא-
    escaped בכוונה כי אינם הקשר HTML. אין `node_modules`/`tsc` בעץ הזה
    (כלל אי-התקנה, כמו כל סבב קודם). נדרש `git add -f` (`apps/**/src`
    מוחרג מה-gitignore כברירת מחדל, אותו דפוס כמו כל סבב קודם). קומיט
    `fa376ed3` על `fix/b-get-your-rights-admin-card-xss-0820`, נדחף
    (מפעיל פריסת Vercel תחת `more30.com/zchuyot`; שינוי מסך-ניהול
    בלבד, אפס שינוי לכל מסך ציבורי אחר). הבא בתור: לסרוק מחדש
    `core.issues`/`core.project_tasks` פעם נוספת; 19-igud-shiurim-portal
    כפתור "סמן כנקרא" חסר try/catch נשאר מועמד קל לסבב הבא.

205. **סבב 30: קראתי README.md/CONNECTIONS.md, בדקתי core.run_progress**
    (מזהה loop B אחרון 1129, תיקון XSS ב-22 get-your-rights, קומיטים
    `fa376ed3`/`6811588d` -- כבר ה-parent של הענף הזה) ו-`core.issues`
    בהיקף 17-31/37: כל השורות הפתוחות (owner=user, #251/#250/#249/#248/
    #245/#242 חלקית) עדיין חסומות בדיוק כמתועד, ללא שינוי. בדקתי גם
    `25-mor1-main-site` -- המערכת היחידה בטווח 17-31 שמעולם לא נסרקה
    באף סבב -- ומצאתי שהיא שלד-מניפסט בלבד (`app.json` בלבד, 343 בייט,
    `live:false`, `source: not-vendored`), אין קוד לבדוק.
206. **שלושה סוכני Explore מקבילים על זוויות טריות בששה יישומים חזרו עם**
    ממצא אחד חזק ושניים חלשים. חלש/לא-נלקח: 23-haorech-torani אומת מחדש
    כשלד-תיעוד בלבד (index.html סטטי, אין קוד); 27-bkalut-price נבדק
    לעומק (תמחור/TEST-mode/pagination/הרשאות פיננסיות) ויצא נקי; 20-igud-
    portal נבדק לעומק ויצא נקי (XSS-safe עם textNode, try/catch בכל
    מקום); 29-bkalot-design נבדק ויצא נקי; 31-hebrew-bridge-crm החזיר
    ממצא אמיתי אך קל-חומרה (שגיאת מחיקת קובץ מ-Storage ב-
    `client.functions.ts:189` לא נבדקת, מותיר קובץ יתום -- לא נלקח בסבב
    זה, נמוך מהממצא הבא).
207. **26-modaot-studio סיפק ממצא אמיתי בחומרה גבוהה, שאומתתי בקריאה**
    ישירה של הקוד ולא רק מדוח הסוכן: `vercel-adapter/api/_lib/server/
    routes.ts` -- `GET /api/projects/:id` (שורה 206 המקורית) ו-`GET
    /api/brands/:id` (שורה 290 המקורית) קראו ל-`storage.getProject(id)`/
    `storage.getBrand(id)` בלי שום פרמטר בעלות, בניגוד מוחלט לרשימה
    (`GET /api/projects` מסננת לפי `user_id` כשיש טוקן) ולנתיבי
    ה-PATCH/DELETE על אותם משאבים בדיוק (שכבר תוקנו בסבב 24, קומיטים
    `e4ab93aa`/`0567605a`, לבדוק `user_id` מול המבקש). כלומר כל משתמש
    (גם אנונימי, בלי טוקן) שמנחש/סורק מזהה מספרי רציף יכול היה לקרוא את
    כל הפרויקט/מותג של משתמש אחר -- כולל `layersJson`/`briefJson`/
    `kitJson`/`logoPng`/`logoSvg`. מערכת 26 חיה ופרוסה (`core.projects`),
    כלומר חשיפה אמיתית בפרודקשן -- דליפת נתונים לא-מורשית (IDOR),
    לא רק אי-עקביות.
208. **התיקון: אותה מדיניות בעלות בדיוק כמו update/delete, בלי לגעת בהם.**
    `storage.ts`: `getProject`/`getBrand` מקבלים עכשיו `requesterId?`
    אופציונלי; אם לשורה יש `user_id` שאינו תואם למבקש -- מוחזר
    `undefined` (הנתיב כבר מטפל ב-`undefined` כ-404 קיים, אין שינוי
    בקוד ה-route מעבר להעברת הפרמטר). שורות ללא `user_id` (אנונימיות,
    מלפני התיוג) נשארות פתוחות לקריאה בדיוק כמו היום -- אפס רגרסיה
    לזרימה האנונימית הקיימת. `routes.ts`: שני ה-handlers קוראים עכשיו
    ל-`getUserIdFromToken(req.headers.authorization)` (אותה פונקציה
    שכבר משמשת בכל שאר ה-routes) ומעבירים את התוצאה כפרמטר השני.
    בכוונה **לא** נגעתי ב-`apps/26-modaot-studio/server/` (עותק שני,
    לא-קשור, של אותם שמות פונקציה מעל sqlite/drizzle מקומי בלי מושג
    `user_id`/הרשאות בכלל) -- זהו שרת ה-dev המקומי, לא הנתיב שנפרס
    ל-Vercel (אותה אבחנה בדיוק כמו סבב 24, שגם הוא נגע רק ב-
    `vercel-adapter`).
209. **אומת בקריאה מלאה של שני הקבצים שנערכו**, כולל גרפ לוודא שאין
    קורא נוסף ל-`storage.getProject`/`getBrand` בכל `apps/26-modaot-
    studio/**` שהיה מפספס את פרמטר הבעלות החדש (רק שני ה-routes
    קוראים להם, שניהם עודכנו). אין `node_modules`/`tsc` בעץ הזה (כלל
    אי-התקנה, כמו כל סבב קודם). קומיט `d31080bf` על
    `fix/b-modaot-studio-project-brand-get-idor-0820`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/26`; שינוי אך ורק לשני נתיבי GET
    קיימים, אפס שינוי להתנהגות מוצלחת קיימת של משתמש עם הרשאה תקינה
    ולזרימה האנונימית). הבא בתור: לסרוק מחדש `core.issues`/
    `core.project_tasks`; 31-hebrew-bridge-crm שגיאת מחיקת Storage
    לא-נבדקת (`client.functions.ts:189`) נשארת מועמד קל לסבב הבא.

210. **סבב 31: קראתי README.md/CONNECTIONS.md, בדקתי core.run_progress**
    (מזהה loop B אחרון 1129, תיקון IDOR ב-26 modaot-studio GET
    /api/projects/:id ו-/api/brands/:id, קומיטים `d31080bf`/`624c8ab2`
    -- כבר ה-parent של הענף הזה) ו-`core.issues` בהיקף 17-31: כל
    השורות הפתוחות עדיין owner=user (#251/#250/#249/#248/#245/#207/
    #206/#205/#209/#208/#202/#203/#220/#229) חסומות בדיוק כמתועד, ללא
    שינוי -- כולן דורשות דשבורד Supabase/Lovable חיצוני שאין אליו
    גישה מה-MCP הזה. #242 (מחשבון זכאות 22/30) עדיין חסום על אותה
    סיבה. שני המועמדים הקלים שנרשמו בסוף סבב 29/30 -- 19-igud-shiurim-
    portal כפתור "סמן כנקרא" בלי try/catch, ו-31-hebrew-bridge-crm
    שגיאת מחיקת Storage לא-נבדקת (`client.functions.ts:189`) -- נבדקו
    שוב; נלקח הראשון (עדיפות UX-שבור-שקט על פני storage-orphan-בלבד
    שכבר יש הגנת RLS+ownership סביבו).
211. **19-igud-shiurim-portal: `drawMessages()` בשני המסכים (renderAdmin**
    התאום/גבאי, ו-renderTeacherAdmin מגיד-שיעור) קראו ל-`api()` בלי
    שום `try/catch`, בניגוד מוחלט לכל שאר הסעיפים באותו קובץ בדיוק
    (ads/lessons/services/billing/profile -- כולם עוטפים את קריאת
    ה-api() ומציגים שגיאה בתוך `div#<section>-alert`, אותו דפוס שכבר
    תוקן פעם באפליקציה האחות `20-igud-portal` בסבב `c1dd65a1`). קריאת
    ה-`api()` זורקת `Error` על כל תגובה שאינה `res.ok` (`app.js:97-99`)
    -- כלומר סשן שפג/תקלת רשת/500 בזמן לחיצה על "סמן כנקרא" הייתה
    מסתיימת ב-unhandled promise rejection: הכפתור לא עושה כלום, אין
    שום משוב למשתמש, וההודעה נשארת "לא נקראה" בלי סימן שמשהו נכשל.
    **התיקון:** הוספת `<div id="messages-alert">` לתחילת שני תבניות
    ה-HTML (בשני פונקציות `drawMessages` נפרדות, כל אחת בתוך ה-`content`
    שלה בלבד -- אין התנגשות ID כי רק פאנל אחד מוצג בכל רגע) ועטיפת שתי
    קריאות ה-`api()` (PATCH toggle-read בפאנל התאום, POST mark-read
    בפאנל מגיד-השיעור) ב-`try/catch` שמציג `err.message` באותו סגנון
    `alert alert-error` כמו כל שאר הקובץ. אפס שינוי להתנהגות המוצלחת
    הקיימת (אותה קריאת API, אותו `refresh()`/`draw()` אחריה). אין
    `node_modules`/build step בעץ הזה (שרת Express סטטי, `public/app.js`
    מוגש כמות שהוא -- כלל אי-התקנה כרגיל). קומיט `661b6fb8` על
    `fix/b-igud-shiurim-portal-messages-silent-fail-0820`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/shiurim`; שינוי אך ורק לשני handler-ים
    של כפתור מסך-ניהול). הבא בתור: לסרוק מחדש `core.issues`/
    `core.project_tasks`; 31-hebrew-bridge-crm שגיאת מחיקת Storage
    לא-נבדקת (`client.functions.ts:189`) נשארת מועמד קל לסבב הבא.

212. **סבב 32: קראתי README.md/CONNECTIONS.md, בדקתי core.run_progress**
    (מזהה loop B אחרון 1133, תיקון try/catch בכפתורי "סמן כנקרא" ב-19
    igud-shiurim-portal, קומיטים `661b6fb8`/`b0dd1626` -- כבר ה-parent
    של הענף הזה) ו-`core.issues` בהיקף 17-31: כל השורות הפתוחות עדיין
    owner=user, חסומות בדיוק כמתועד, ללא שינוי. נלקח המועמד שנרשם בסוף
    סבב 30/31: 31-hebrew-bridge-crm שגיאת מחיקת Storage לא-נבדקת.
213. **31-hebrew-bridge-crm: `removeUploadedDocument` (`client.functions.ts`)**
    מוחק את שורת `documents` מה-DB (מאומת -- זורק אם `deleted.length===0`)
    ואז קורא ל-`supabaseAdmin.storage.from("client-documents").remove(...)`
    בלי לבדוק את שדה ה-`error` בתגובה בכלל -- ב-`@supabase/supabase-js`
    הפעולה **לא** זורקת בכישלון, היא מחזירה `{ data, error }`, כך שקריאה
    בלי לבדוק אותו בולעת את השגיאה לגמרי, לא רק "מציגה ok כוזב": אין
    שום עקבה, שום לוג, שום דרך לדעת בדיעבד שהקובץ בכלל נכשל להימחק.
    התוצאה: קובץ מסמך (לקוחות גשר-עברי הם מהגרים/עולים, כלומר מסמכים
    אישיים/PII אמיתיים) נשאר יתום לצמיתות ב-Storage בלי שום שורת DB
    שמצביעה עליו -- פער תברואה/פרטיות אמיתי (לא ניתן יותר לאתר את
    הקובץ כדי למחוק אותו בפועל, "מחיקה" מנקודת המבט של המשתמש לא
    שלמה בפועל).
214. **התיקון: לא הופך לכישלון של הבקשה** (שורת ה-DB כבר נמחקה בהצלחה
    בשלב הקודם, ואי אפשר "לבטל" את זה) **אלא הופך לגלוי בלוג**, באותה
    מוסכמה בדיוק שכבר קיימת בקבצים אחרים באותה אפליקציה
    (`console.error("[tag] msg", {...})` -- `outbox-dispatch.ts:115`,
    `auth-middleware.ts:21`): `console.error("[removeUploadedDocument]
    storage.remove failed", { id, storage_path, error })`. אפס שינוי
    להתנהגות המוצלחת הקיימת (אותו `{ ok: true }` מוחזר, אותו זרימת-
    לקוח ב-`DocumentsList.tsx`). אין `node_modules`/build step מותקן
    בעץ הזה (כלל אי-התקנה כרגיל); אומתתי בקריאה חוזרת של הבלוק שנערך
    ו-grep שאין קורא נוסף ל-`removeUploadedDocument` שהיה תלוי בחתימה
    הישנה. נדרש `git add -f` (`apps/**/src` מוחרג מה-gitignore כברירת
    מחדל, אותו דפוס כמו כל סבב קודם). קומיט הבא על
    `fix/b-hebrew-bridge-crm-orphaned-storage-file-0820`, יידחף (מפעיל
    פריסת Vercel תחת `more30.com/gesher`; שינוי אך ורק ל-handler מחיקת
    מסמך אחד, אפס שינוי לזרימת ההעלאה/רשימה/קריאה). הבא בתור: לסרוק
    מחדש `core.issues`/`core.project_tasks`; לא נותר מועמד קל רשום
    מסבבים קודמים -- סבב הבא צריך grep/Explore רוחבי חדש.

## 20/08/2026 — סבב 33

215. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1134, תיקון storage-error-לא-נבדק ב-31 hebrew-bridge-crm,
    קומיט `f7462bd7` -- כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.issues`
    בהיקף 17-31/37: כל השורות הפתוחות עדיין owner=user (allow-list/RLS/
    SMTP/URL-config בפרויקטי Supabase שאינם נגישים דרך ה-PAT הזה) או
    owner=agent שכבר בטיפול (#5 עיצוב ייחודי, ידוע ומתמשך). אין שורה
    חדשה שניתן לפעול עליה בלי דשבורד חיצוני -- כמו כל סבב קודם. הרצתי
    שלושה סוכני Explore מקבילים על טריטוריה טרייה: (17-chizukim-transcribe,
    18-torah-editor-mvp), (21-mthbram, 24-galilee-connect-hub -- מעבר
    לתיקונים שכבר נסגרו בהם), (28-kupot-health-funds, 37-bkalot-clone).
216. **הממצא החזק ביותר: 18-torah-editor-mvp, כל חמשת ה-routes של מודול**
    ה-HTR (`app/api/htr/jobs/route.ts`, `jobs/[id]/route.ts` GET+PATCH,
    `jobs/[id]/approve/route.ts`, `upload/route.ts`, `process/route.ts`)
    לא בדקו **בכלל** מי קורא להם -- לא Authorization header, לא session,
    שום דבר. אימתתי בעצמי, לא רק מדוח הסוכן: קראתי את חמשת הקבצים,
    את `middleware.ts` (מרענן session רק מול פרויקט ה-DB של האפליקציה
    עצמה, לא מול ה"האב" שמחזיק את הזהות המאוחדת), ואת המיגרציה
    `0001_htr_jobs.sql` -- שמתעדת בעצמה את העיקרון "RLS: גישה רק דרך
    service_role (backend); הפרונט לא ניגש ישירות לטבלה" אבל לא הוסיפה
    שום בדיקת-מבקש בתוך ה-routes עצמם, כך שה-service_role שמעקף RLS
    היה פתוח לכל קורא אנונימי באינטרנט. `htr_jobs` נטולת עמודת `user_id`
    (כלי צוות משותף, לא נתונים פרטיים-למשתמש) -- כלומר זה לא IDOR בין
    משתמשים, אלא **העדר אימות מוחלט** על שער-האישור עצמו: ה-endpoint
    `POST .../approve` הוא הצעד היחיד שמסמן טקסט כ"מוכן לשילוב בספר"
    (העיקרון "הטקסט קדוש" המתועד גם בקוד וגם ב-UI: "שום טקסט לא נכנס
    לספר ללא אישורך") -- וכל אחד, בלי להתחבר בכלל, יכול היה לקרוא/לתקן/
    **לאשר** כל job, כולל אישור טקסט מזויף כאילו עבר בדיקה אנושית.
    מערכת 18 חיה ופרוסה (`core.projects`: `live=true`/`is_deployed=true`,
    `more30.com/orech`) -- חשיפה אמיתית בפרודקשן, לא תיאורטית.
217. **התיקון: סף-כניסה מבוסס ה"האב" הקיים, לא בעלות-per-job (אין למה).**
    קודם אימתתי (סבב 5, סעיף 92 למעלה) ש-`lib/hub.ts` וה-pill המשותף
    (`auth-button.js`, כבר טעון ב-`app/layout.tsx`) הם מנגנון הזהות
    האמיתי של האפליקציה הזו, ושה-RLS על `orech_documents` באותו פרויקט
    כבר אומת חי כ-`true` -- כלומר הזהות הזאת עובדת בפרודקשן, לא ניחוש.
    הוספתי `lib/hub-auth.ts` (קובץ שרת חדש): `getHubUserFromRequest()`
    קורא `Authorization: Bearer <token>` ומאמת אותו מול ה-Auth server
    של ההאב עצמו (`auth.getUser(token)` -- קריאת רשת שמאמתת את הטוקן,
    לא רק מפענחת אותו מקומית) עם אותם שני משתני-סביבה בדיוק שכבר
    בשימוש ב-`lib/hub.ts` (`NEXT_PUBLIC_HUB_SUPABASE_URL`/`_ANON_KEY`).
    כל חמשת ה-routes מחזירים 401 בלי טוקן תקין, לפני כל גישה ל-DB.
    ב-`approve/route.ts` גם ניצלתי את המייל המאומת בתור `approved_by`
    (עדיף על שדה חופשי שהלקוח שולח בגוף הבקשה) -- עם נפילה-חזרה לערך
    הישן אם אין מייל, אפס שינוי לצורת ה-response. בצד הלקוח
    (`app/htr/page.tsx`): הוספתי `authFetch()` שמצרף את ה-`access_token`
    הנוכחי מה-hub client (`getHubClient().auth.getSession()`) לכל אחת
    משבע קריאות ה-fetch הקיימות למודול HTR (רשימה/פרטים/העלאה/עיבוד/
    שמירת-טיוטה/אישור), ומסך "נדרשת התחברות" קטן כשה-`GET /jobs`
    הראשוני מחזיר 401 -- במקום שהעמוד יישאר על "אין עבודות עדיין"
    בשקט בלי שום רמז שהבעיה היא חוסר התחברות ולא רשימה ריקה.
218. **אפס רגרסיה למשתמש המחובר:** אותה זרימת-הצלחה בדיוק (`{ok:true}`/
    `{job}`/`{jobs}`) לכל חמשת ה-endpoints; שום שינוי לחוזה ה-JSON.
    לא נגעתי ב-`/api/verify`, `/api/citations`, `/api/nikud` (routes
    ציבוריים-בכוונה, לא נושאים workflow-אישור), וגם לא ב-`app/documents`
    (כבר מוגן ב-RLS ישיר מול ההאב, מסלול שונה לגמרי). אין
    `node_modules`/build מותקן בעץ הזה (כלל אי-התקנה כרגיל) -- אימתתי
    בקריאה חוזרת של כל שבעת הקבצים שנערכו/נוצרו, ו-grep שאין קורא נוסף
    ל-`/api/htr/*` בכל האפליקציה מלבד `app/htr/page.tsx`. נדרש
    `git add -f` (`apps/18-torah-editor-mvp/app` וגם הקובץ החדש
    `lib/hub-auth.ts` מוחרגים מה-gitignore כברירת מחדל, אותו דפוס כמו
    כל סבב קודם). קומיט הבא על `fix/b-torah-editor-htr-unauth-approve-0820`,
    יידחף (מפעיל פריסת Vercel תחת `more30.com/orech`; שינוי אך ורק
    למודול HTR -- אפס שינוי ל`/documents`/`/verify`/`/citations`/`/nikud`).
    הבא בתור: לסרוק מחדש `core.issues`/`core.project_tasks`; לא נותר
    מועמד קל רשום -- סבב הבא צריך grep/Explore רוחבי חדש.

## 20/08/2026 — סבב 34

219. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1136, תיקון חוסר-אימות מוחלט במודול HTR ב-18
    torah-editor-mvp, קומיט `50941c8d` -- כבר ה-parent של הענף הזה, כבר
    נדחף) ו-`core.issues` בהיקף 17-31: כל השורות הפתוחות עדיין
    owner=user (#251/#250/#249/#248/#245/#242/#229/#220/#209), חסומות
    בדיוק כמתועד -- כולן דורשות דשבורד Supabase/Lovable חיצוני שאין
    אליו גישה מה-MCP הזה. אין שורה חדשה שניתן לפעול עליה. הרצתי שלושה
    סוכני Explore מקבילים על טריטוריה טרייה: (19-igud-shiurim-portal,
    20-igud-portal), (22-get-your-rights, 27-bkalut-price),
    (29-bkalot-design, 30-zchuyotpro-crm).
220. **הממצא שנבחר: 27-bkalut-price, `GET /api/clients` (`server/routes.ts:416`)**
    היה ללא שום middleware אימות, בניגוד מוחלט לכל שאר נתיבי ה-`/api/admin/*`
    וגם `/api/rights`/`/api/orgs` באותו קובץ בדיוק (כולם עטופים ב-`requireAdmin`,
    כולל תיקון קודם באותו קובץ מסבב מוקדם יותר -- `35e1a02b` על `/api/orgs`).
    אימתתי בעצמי: הנתיב מחזיר `storage.listClients()` -- **הרשומה
    המלאה** של כל לקוח (`fullName`, `phone`, `email`, `idNumber`,
    `birthDate`, `city`, `familyStatus`) לכל קורא אנונימי באינטרנט,
    בלי שום בדיקת session. הצריכה היחידה שלו בקוד היא
    `client/src/pages/delivery.tsx:66` -- עמוד `/delivery` בתוך
    `InternalArea`, מוגן רק בצד ה-client (הפניה ל-`/login` אם
    `!isAuthed`) שהיא כמובן לא הגנה אמיתית מול קריאת API ישירה. סריקתי
    גם את `GET /api/clients/by-phone` (אותו קובץ, שורה 421) -- זהה
    בחומרה (מחזיר רשומה מלאה, לא רק שדות autofill), אבל **לא נגעתי בו**:
    הוא נצרך ע"י `client/src/pages/service-form.tsx:235`, טופס-קליטה
    **ציבורי** תחת `/service/:id` (מחוץ ל-`InternalArea`) שמאפשר ללקוח
    חוזר למלא-אוטומטית לפי הטלפון שלו -- הוספת `requireAdmin` שם הייתה
    שוברת את הזרימה הציבורית הזו (רגרסיה אסורה). זה משאיר חשיפת
    enumeration-לפי-טלפון פתוחה בכוונה בסבב הזה -- מתועד כמועמד להמשך
    (דורש עיצוב מחדש: הגבלת שדות מוחזרים או rate-limit, לא רק
    auth-gate, כדי לא לשבור את חוויית הלקוח החוזר).
221. **התיקון: `requireAdmin` יובא כבר בראש הקובץ** (שורה 28, בשימוש
    בעשרות נתיבים אחרים) -- הוספתי אותו כ-middleware שני על
    `app.get("/api/clients", ...)` בלבד. אימתתי אפס-רגרסיה: `apiRequest`/
    `getQueryFn` ב-`client/src/lib/queryClient.ts:59-60` כבר מצרפים
    `Authorization: Bearer <adminToken>` **אוטומטית** לכל קריאת API
    כשקיים טוקן אדמין מחובר -- כלומר `delivery.tsx` (הצרכן היחיד של
    הנתיב) כבר שולח את ההדר הזה היום בלי שינוי קוד לקוח, ותמשיך לעבוד
    זהה לחלוטין למשתמש אדמין מחובר. grep מאומת: אין קורא נוסף ל-
    `/api/clients` (לא `/api/clients/by-phone`, לא `/api/external/clients`
    שכבר מוגן ב-`gate` נפרד) בכל השרת או הלקוח. אין `node_modules`/build
    מותקן בעץ הזה (כלל אי-התקנה כרגיל). קומיט הבא על
    `fix/b-bkalut-price-clients-list-unauth-0820`, יידחף (מפעיל פריסת
    Vercel תחת `more30.com/mechiron`; שינוי אך ורק לנתיב GET אחד, אפס
    שינוי ל-`/api/clients/by-phone` הציבורי או לכל התנהגות אדמין
    מוצלחת קיימת). הבא בתור: לסרוק מחדש `core.issues`/
    `core.project_tasks`; אפשרות עיצוב rate-limit/field-trim על
    `/api/clients/by-phone` נשארת מועמד להמשך, וכן הממצאים הנוספים
    מדוחות ה-Explore (19-igud-shiurim-portal webhook-RPC ללא בדיקת
    שגיאה, 22-get-your-rights AdminSettings/AdminLeads כתיבות DB
    לא-נבדקות, 30-zchuyotpro-crm נתיבי `/api/public/*` ללא אימות
    tenant) שלא נבחרו הסבב הזה.

## 20/08/2026 — סבב 35

222. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1138, תיקון `/api/clients` ב-27 bkalut-price, קומיט
    `ac123f86` -- כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.issues`
    בהיקף 17-31/37: כל השורות הפתוחות עדיין owner=user (allow-list/RLS/
    SMTP/URL-config בפרויקטי Supabase שאינם נגישים דרך ה-PAT הזה) --
    אין שורה חדשה שניתן לפעול עליה. הרצתי שלושה סוכני Explore מקבילים
    לאמת את שלושת המועמדים שנרשמו בסוף סבב 34 ולא נבחרו אז: (1)
    19-igud-shiurim-portal webhook-RPC ללא בדיקת שגיאה, (2)
    22-get-your-rights AdminSettings/AdminLeads כתיבות DB לא-נבדקות,
    (3) 30-zchuyotpro-crm נתיבי `/api/public/*` ללא אימות tenant.
223. **הממצא שנבחר: 30-zchuyotpro-crm, חמשת נתיבי `/api/public/*`**
    (`notify-message.ts`, `notify-signature.ts`, `analyze-paystub.ts`,
    `notify-partner.ts`, `notify-admin.ts`) -- הממצא החמור מבין
    השלושה, ובחומרה זהה לתיקוני האימות הגדולים מסבבים 33/34 (18
    torah-editor-mvp HTR). אימתתי בעצמי בקריאת כל חמשת הקבצים: כולם
    השתמשו ב-`supabaseAdmin` (service_role, עוקף RLS) ושלפו שורה מ-
    `messages`/`documents`/`partner_referrals` לפי `id` בלבד -- בלי שום
    בדיקה שהקורא שייך לאותו tenant. קריאה אנונימית עם UUID של tenant
    אחר הייתה חושפת שם/טלפון/אימייל של לקוח ו-URL חתום למסמך פרטי
    (`notify-message`/`notify-signature`), או כותבת סטטוס מעובד/חתום
    כוזב על מסמך של tenant אחר (`analyze-paystub`/`notify-signature`).
    אימתתי גם את כל הקוראים הלגיטימיים (grep מלא): שלושתם בתוך
    `_authenticated/*` (`documents.tsx`, `messages.tsx`,
    `partner-portal/$referralId.tsx` דרך `dispatchNotify`), כולם כבר
    עם `me.tenant_id`/session בהיקף, אף אחד לא באמת "ציבורי" (השם
    "public" מטעה -- אלה webhook-ים פנימיים, לא טופס ציבורי כמו המקרה
    של `/api/clients/by-phone` בסבב 34 -- כך שהתיקון לא מסתכן בשבירת
    זרימה ציבורית אמיתית).
224. **התיקון: קובץ שרת חדש `tenant-auth.server.ts`** (`getCallerTenantId`)
    מאמת את ה-Bearer token מול שרת ה-Auth (`auth.getClaims`, אותו
    דפוס בדיוק שכבר קיים ב-`auth-middleware.ts` של האפליקציה הזו) ואז
    פותר tenant_id דרך `profiles` (צוות tenant) או `partners` (שותף
    חיצוני בפורטל השותפים) -- שתי טבלאות זהות שכבר קיימות, לא המצאה.
    כל נתיב בודק עכשיו שה-`tenant_id` של השורה שנשלפה תואם לזה של
    הקורא (404 אם לא, לא ניתן להבחין מ"לא נמצא" -- אותו דפוס בדיוק כמו
    ה-404 הקיים כבר בכל חמשת הקבצים). בצד הלקוח הוספתי `authedFetch()`
    (`src/lib/authed-fetch.ts`) שמצרף את ה-access_token הנוכחי מ-
    `supabase.auth.getSession()` -- אותו דפוס בדיוק שכבר קיים ב-
    `auth-attacher.ts` הגנרי (עבור serverFn RPCs; כאן נדרש בנפרד כי
    אלה קריאות `fetch()` גולמיות ל-`/api/public/*`, לא serverFn) --
    ועדכנתי את כל שלוש נקודות הקריאה (`documents.tsx` פעמיים,
    `messages.tsx` פעם, `dispatchNotify` ב-`queries.ts` שמכסה גם
    `notify-partner` וגם `notify-admin`). אפס שינוי לחוזה ה-JSON
    בנתיב ההצלחה בכל אחד מחמשת ה-routes. לא נגעתי בקבצים המסומנים
    "automatically generated. Do not edit it directly" (`client.ts`,
    `client.server.ts`, `auth-middleware.ts`, `auth-attacher.ts`) --
    רק קראתי מהם את הדפוס הקיים ושכפלתי אותו בקבצים חדשים. אין
    `node_modules`/build מותקן בעץ הזה (כלל אי-התקנה כרגיל) -- אימתתי
    בקריאה חוזרת של כל עשרת הקבצים שנערכו/נוצרו, grep שאין קורא נוסף
    לחמשת ה-routes בכל האפליקציה מלבד שלוש נקודות הקריאה שעודכנו,
    ואימות הסכימה (`supabase/migrations/20260610191501_...sql`) ש-
    `profiles`/`partners`/`messages`/`documents`/`partner_referrals`
    כולן נושאות עמודת `tenant_id` אמיתית כפי שהקוד מניח. נדרש
    `git add -f` לשני הקבצים החדשים (`apps/**/src` מוחרג מה-gitignore
    כברירת מחדל, אותו דפוס כמו כל סבב קודם). קומיט הבא על
    `fix/b-zchuyotpro-crm-public-routes-idor-0820`, יידחף (מפעיל
    פריסת Vercel תחת `more30.com/crm`; שינוי אך ורק לחמשת ה-routes
    ולשלוש נקודות הקריאה, אפס שינוי לזרימת ניהול/לקוחות/מסמכים אחרת).
    הבא בתור: שני המועמדים שלא נבחרו הסבב הזה (19-igud-shiurim-portal
    webhook-RPC ללא בדיקת שגיאה -- ה-GET handler מחזיר 200 תמיד גם
    כשה-RPC נכשל; 22-get-your-rights AdminSettings/AdminLeads --
    שמונה כתיבות DB בלי בדיקת `error`, מציגות toast הצלחה כוזב)
    נשארים מועמדים טובים לסבב הבא.

## 20/08/2026 — סבב 36

225. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1141, תיקון IDOR ב-`/api/public/*` ב-30 zchuyotpro-crm,
    קומיטים `5e22a3ce`/`8082604d` -- כבר ה-parent של הענף הזה, כבר
    נדחף) ו-`core.issues` בהיקף 17-31: כל השורות הפתוחות עדיין
    owner=user (allow-list/RLS/SMTP/service-role-key/policy-decision
    בפרויקטי Supabase שאינם נגישים דרך ה-PAT הזה -- #152/#156/#169/
    #172/#180/#202/#206/#209/#242/#245/#248/#249/#250/#251) או
    owner=agent שכבר במעקב (#201, נסגר בכל מקום מלבד 15 egod שמחוץ
    להיקף 17-31). אין שורה חדשה שניתן לפעול עליה. הרצתי שני סוכני
    Explore מקבילים לאמת את שני המועמדים שנרשמו בסוף סבב 35 ולא נבחרו:
    (1) 19-igud-shiurim-portal webhook נדרים-פלוס מתעלם משגיאת RPC,
    (2) 22-get-your-rights AdminSettings/AdminLeads עם שמונה כתיבות DB
    לא-נבדקות.
226. **הממצא שנבחר: 19-igud-shiurim-portal, `POST`/`GET /api/webhooks/nedarim`**
    (`server.js:1112-1150`) -- שני ה-handlers קראו ל-RPC
    `mark_subscription_payment_paid` והחזירו `200` בלי תנאי: ה-POST
    רשם את השגיאה ל-console אבל עדיין החזיר 200; ה-GET אפילו לא בדק
    את תוצאת ה-RPC בכלל. אימתתי בעצמי בקריאת הקובץ: נדרים פלוס קוראת
    לכתובת הזו כ-CallBack בסיום עסקת תשלום מנוי (`CallBack` מוגדר פעמיים,
    שורות 1003/1081, לאותה כתובת בדיוק), ומתייחסת ל-200 כ"נמסר, אל
    תנסה שוב" -- כלומר כשל DB חולף ב-RPC היה משאיר מנוי ששולם בפועל
    מסומן כלא-שולם במסד, לצמיתות, בלי שום סימן חוץ משורת console ובלי
    שהשער נותן לנדרים פלוס הזדמנות לנסות שוב. השוויתי לחומרה של
    22-get-your-rights (שמונה כתיבות admin עם toast מזויף) -- בחרתי
    בזה כי מדובר בכסף שכבר עבר יד (לא רק שדה שלא נשמר), והוא נבחן
    מול המוסכמה הקיימת של האפליקציה עצמה (`handleRpcResult`/
    `handleBooleanRpcResult`, שורות 103-125, שכן בודקות `error` בכל
    נתיב אחר בקובץ).
227. **התיקון: על שגיאת RPC, מחזירים `500` במקום `200` תמיד** -- מבדיל
    בין הכשל החולף הזה (RPC/DB) לבין הענף הקיים כמה שורות מעל
    (Param1 חסר לגמרי, תקלת-תצורה קבועה) שממשיך להחזיר 200 כמו קודם
    בכוונה (התגובה החדשה כן שומרת על ההערה הקיימת שם, "עדיין מאשרים
    קבלה כדי שלא ינסו שוב לשווא" -- זה עדיין נכון לתקלת-תצורה, לא
    לתקלת-RPC). לא ניחשתי אם ה-RPC אידמפוטנטי -- שם הפונקציה
    (`mark_subscription_payment_paid`, כתיבה לפי `correlation_token`)
    מרמז UPDATE-לפי-מפתח שבטבעו בטוח לנסיון-חוזר, בהתאם למוסכמת
    webhook-retry הרגילה (5xx = כשל-חולף-נסה-שוב, 4xx/200 = לא-לנסות).
    אפס שינוי לענף ה-Param1-חסר, אפס שינוי לתגובת ההצלחה (`200`/`'ok'`)
    כששני ה-handlers מצליחים. `node --check` נקי; grep מאומת ששתי
    הכתובות `CallBack` (סופר-אדמין ותשלום רגיל) מצביעות לאותו נתיב
    בדיוק שתוקן, ואין קורא פנימי אחר שמניח 200-קבוע. אין `node_modules`/
    build מותקן בעץ הזה (כלל אי-התקנה כרגיל). קומיט `a23da816` על
    `fix/b-igud-shiurim-portal-nedarim-webhook-silent-fail-0820`, יידחף
    (מפעיל פריסת Vercel תחת `more30.com`/הנתיב הממופה ל-19; שינוי אך
    ורק לשני ה-webhook handlers, אפס שינוי לזרימת תשלום-מנוי/סופר-אדמין
    אחרת). הבא בתור: 22-get-your-rights AdminSettings/AdminLeads
    (שמונה כתיבות DB בלי בדיקת `error`, מציגות toast הצלחה כוזב --
    מתועד במלואו בדוח סוכן Explore של סבב זה, כולל שורות מדויקות
    בשני הקבצים) נשאר מועמד חזק לסבב הבא.

## 20/08/2026 — סבב 37

228. **בחרתי את המועמד שנרשם בסוף סבב 36: 22-get-your-rights,
    `AdminSettings.tsx` + `AdminLeads.tsx`.** אימתתי בעצמי בקריאת שני
    הקבצים: שמונה פעולות כתיבה ל-Supabase (`saveWebhook`,
    `generateApiKey`, `deleteKey`, שמירת webhook n8n מוטבע ב-JSX --
    כולן ב-`AdminSettings.tsx`; `handleSaveNotes`, `handleStatusChange`,
    `handleDelete`, `handleSaveLeadEdit` ב-`AdminLeads.tsx`) קראו
    ל-`supabase.from(...).insert/update/delete` בלי לבדוק את `error`
    המוחזר, ומיד הציגו `toast` של הצלחה ועדכנו את ה-state המקומי
    בלי תנאי. כשל חולף (RLS, רשת, DB) היה משאיר את המנהל בטעות
    שהליד נמחק/עודכן/מפתח ה-API נוצר כשבפועל שום דבר לא נשמר --
    הדוגמה החמורה ביותר: `handleDelete` מסיר את השורה מה-state
    המקומי ומציג "נמחק" גם אם ה-`DELETE` בפועל נכשל, כך שה-PII של
    הליד "שנמחק" נשאר בפועל במסד בעוד המנהל בטוח שהוא נעלם.
229. **התיקון: לכל שמונת ה-call sites, קליטת `{ error }` מהקריאה
    ל-Supabase, ואם יש שגיאה -- `toast` מסוג `destructive` ו-`return`
    לפני עדכון ה-state/הצגת הצלחה**, במקום הזרימה חסרת-התנאי הקודמת.
    תבנית עקבית לכל שמונת המקומות, זהה למוסכמה שכבר קיימת באותו
    קובץ עצמו (`n8n-notify` handler, שורות 505-517, שכבר בודק
    `error`/`data.error` לפני הצגת toast). אפס שינוי להתנהגות בנתיב
    ההצלחה -- אותם toasts, אותם עדכוני state, באותו סדר, רק לאחר
    בדיקת `error`. אימתתי בקריאה חוזרת של ה-`diff` המלא (לא רק
    התיאור) -- כל שמונת הבלוקים תואמים בדיוק לתבנית, אין נתיב שנשאר
    לא-מתוקן ואין שינוי לוגי נוסף מעבר לבדיקת ה-`error`. אין
    `node_modules`/build מותקן בעץ הזה (כלל אי-התקנה כרגיל) -- אומת
    בקריאה בלבד. קומיט `dc787c2f` על
    `fix/b-get-your-rights-admin-unchecked-writes-0820`, יידחף (מפעיל
    פריסת Vercel תחת `more30.com`/הנתיב הממופה ל-22; שינוי אך ורק
    לשני קבצי הניהול, אפס שינוי לזרימת בוט-הלידים הציבורי/ה-API
    endpoints/מאגר הזכויות).

## 20/08/2026 — סבב 38

230. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1143, תיקון שמונת הכתיבות הלא-נבדקות ב-22
    get-your-rights, קומיטים `dc787c2f`/`34be6a00` -- כבר ה-parent של
    הענף הזה, כבר נדחף) ו-`core.issues` בהיקף 17-31: כל השורות
    הפתוחות עדיין owner=user (allow-list/RLS/SMTP/service-role-key/
    policy-decision בפרויקטי Supabase שאינם נגישים דרך ה-PAT הזה) או
    owner=agent שכבר במעקב/סגור בהיקף הזה. אין שורה חדשה שניתן לפעול
    עליה, ואין "הבא בתור" רשום בסוף סבב 37. הרצתי ארבעה סוכני Explore
    מקבילים על שטח שלא נסרק לעומק בסבבים האחרונים: 20-igud-portal,
    17-chizukim-transcribe, 28-kupot-health-funds, ו-26-modaot-studio+
    29-bkalot-design (בהתאם להנחיה המפורשת של הלופ הזה למקד תשומת לב
    ב-studio(26) ו-design-system(29)).
231. **שלושה חזרו נקיים** (20-igud-portal -- proxy קטן ל-RPC בלי
    כתיבות/admin/IDOR; 17-chizukim-transcribe -- כל כתיבה בודקת
    `res.ok`, XSS מוגן ב-`escapeHtml`, pipeline התמלול בודק כל שלב;
    29-bkalot-design -- רק כלי build/CSS סטטיים, כבר תוקן בסבב קודם).
    28-kupot-health-funds החזיר ממצא אמיתי (`GET /api/switch-leads`
    קורס ב-production כי הוא תמיד קורא מ-SQLite null כש-`DATA_STORE=
    supabase`, ולכן מסך הלידים באדמין תמיד נכשל בשקט) -- high אבל
    לא ריצה כרגע. 26-modaot-studio החזיר ממצא חמור יותר: `clientIp()`
    ב-`vercel-adapter/api/_lib/server/routes.ts:24-28` לוקח את
    האיבר **הראשון** של כותרת `x-forwarded-for` כדי לאכוף מכסת AI
    יומית (40/יום/IP) על שמונה נתיבי AI בתשלום (Gemini/Recraft/
    Claude) -- אבל האיבר הראשון הוא בדיוק מה שהלקוח עצמו שולח
    (התקן: כל hop *מוסיף* לסוף הרשימה, לא לתחילתה), כך שכל קורא
    שאינו דפדפן (curl/סקריפט) יכול לשלוח כותרת מזויפת שונה בכל בקשה
    ולקבל "IP" חדש בכל פעם -- עוקף לגמרי את התקרה על נתיב חי ופרוס
    שמפעיל קריאות אמיתיות בתשלום לספקי AI חיצוניים. בחרתי בזה על
    פני 28 כי מדובר בחשיפה כספית ישירה ופעילה כרגע (לא רק תצוגה
    שבורה), ללא צורך באימות, ובהתאמה ישירה להנחיית הלופ להתמקד
    ב-studio(26).
232. **התיקון: `clientIp()` מעדיף עכשיו את `x-vercel-forwarded-for`**
    (כותרת ש-Vercel עצמה קובעת ב-edge לכתובת הלקוח האמיתית, ואינה
    ניתנת לזיוף ע"י הקורא -- Vercel דורסת/מתעלמת מערך שהלקוח מנסה
    לשלוח לכותרת הזו בדיוק), ורק אם היא חסרה נופל חזרה לאיבר
    **האחרון** של `x-forwarded-for` (ה-hop שה-edge של Vercel עצמו
    הוסיף, לא מה שהלקוח שלח), ורק אחר כך ל-`socket.remoteAddress`.
    אימתתי ש-`clientIp` נקרא ממקום יחיד בקובץ (`enforceAiQuota`,
    שורה 47) -- אין קורא נוסף שהשינוי משפיע עליו. אימתתי גם ש-26
    אכן רץ על Vercel (vercel-adapter מיועד לכך במפורש, `vercel.json`
    מנתב את כל `/studio/*`+`/api/*` דרך ה-adapter הזה, לא שרת ה-dev
    המקומי). אפס שינוי להתנהגות המכסה עצמה (עדיין 40/יום, עדיין
    hash-SHA256, עדיין "פתוח" בכשל תשתית) -- רק לזיהוי ה-IP שממנו
    נגזר ה-hash. אין `node_modules`/build מותקן בעץ הזה (כלל
    אי-התקנה כרגיל) -- אומת בקריאה חוזרת של הבלוק המתוקן בלבד,
    ללא `tsc` זמין. קומיט הבא על
    `fix/b-modaot-studio-ai-quota-ip-spoof-0820`, יידחף (מפעיל פריסת
    Vercel תחת `more30.com/studio`; שינוי אך ורק לפונקציית זיהוי
    ה-IP, אפס שינוי לשמונת נתיבי ה-AI עצמם או לכל route אחר). הבא
    בתור: 28-kupot-health-funds -- `GET /api/switch-leads` קורא
    תמיד מ-SQLite (`storage.listSwitchLeads()`) גם כש-`DATA_STORE=
    supabase`, ולכן קורס בשקט ב-production ומשאיר את מסך הלידים
    באדמין ריק/לא-נגיש לצמיתות (הלידים עצמם נשמרים תקין ב-Supabase,
    רק הקריאה חזרה שבורה) -- מתועד במלואו עם שורות מדויקות, מועמד
    חזק לסבב הבא.

## 20/08/2026 — סבב 39

233. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון 1144, תיקון spoofing IP ב-`clientIp()` ב-26
    modaot-studio, קומיט `42e5395c` -- כבר ה-parent של הענף הזה, כבר
    נדחף). המועמד שנרשם בסוף סבב 38 היה מוכן לביצוע: 28
    kupot-health-funds, `GET /api/switch-leads` (`server/routes.ts:124`)
    קורא תמיד `storage.listSwitchLeads()` (Drizzle+SQLite, `server/
    storage.ts:188-194`) גם כש-`DATA_STORE=supabase`. אימתתי בעצמי
    בקריאת `storage.ts:14-34`: כש-`DATA_STORE_SUPABASE` אמת, המשתנים
    `sqlite`/`drizzle` נשארים `null` ולכן `export const db = sqlite ?
    drizzle(sqlite) : null` -- `listSwitchLeads()` קורא ל-`db.select()`
    על `null`, זורק `TypeError` לא-מטופל (אין `try/catch` סביב ה-route).
    בדקתי גם מול Supabase עצמו (MCP, פרויקט ה-hub `uhnrgujb...`, כי
    `core.projects` ל-28 רשום עם `supabase_project=uhnrgujbdxhhmoxcjria`,
    `supabase_schema=public`): `public.hf_switch_leads` עם RLS פעיל
    ומדיניות `hf_leads_public_insert` (INSERT בלבד, `roles={public}`) --
    **אין מדיניות SELECT בכלל**, כלומר גם אם `routes.ts` היה קורא
    ל-Supabase עם ה-anon key (כמו ש-`fetchTopicsFromSupabase` עושה),
    התוצאה הייתה מערך ריק תמיד (RLS חוסם, לא שגיאה) -- לא מספיק לתקן
    רק את הקריסה, צריך גם נתיב קריאה שבאמת מחזיר את השורות.
234. **התיקון: פונקציית קריאה חדשה `fetchSwitchLeadsFromSupabase()`**
    ב-`server/supabase.ts` שמשתמשת ב-`SUPABASE_SERVICE_ROLE_KEY` (סוד
    server-only חדש לאפליקציה הזו, לפי המוסכמה הקיימת ב-CONNECTIONS.md)
    במקום ה-anon key -- כי `service_role` עוקף RLS לגמרי (אימתתי:
    `pg_roles.rolbypassrls=true` עבורו, `false` ל-anon/authenticated),
    וזה בטוח כאן כי הקריאה כבר מוגנת ב-`isAdminRequest` (בדיקת
    `ADMIN_TOKEN`) לפני שהיא בכלל מגיעה לפונקציה. מחזירה את השורות
    הגולמיות (snake_case, ישירות מ-PostgREST) בלי מיפוי -- כך גם
    `admin-page.ts` (`COLUMNS` שם כבר משתמש ב-`created_at`/`full_name`
    וכו', snake_case, לא ב-`fullName` camelCase כמו שה-SQLite storage
    מחזיר) מקבל לראשונה בפועל את הצורה שהוא תמיד ציפה לה בייצור --
    לא נגעתי בנתיב ה-SQLite המקומי (עדיין `storage.listSwitchLeads()`,
    עדיין camelCase, אי-ההתאמה הזו בפיתוח מקומי בלבד קיימת מלפני
    התיקון ומחוץ להיקף הצעד הזה). ב-`routes.ts` עטפתי את ה-handler
    כולו ב-`try/catch` (`502` בכשל, אותו דפוס כמו `/api/hf/topics`)
    במקום קריסה לא-מטופלת. עדכנתי את `README.md` (שם המשתנה החדש
    בקטע `.env`, לא ערך). אין `node_modules` מותקן בעץ הזה (כלל
    אי-התקנה כרגיל) -- אומת בקריאה חוזרת של שני הקבצים בלבד, אין
    `tsc` זמין. אפס שינוי לנתיב יצירת ליד (`POST /api/switch-lead`),
    אפס שינוי למדיניות ה-RLS הקיימת (לא הוספתי SELECT policy לanon --
    היה מרחיב חשיפת PII, ו-service_role מספיק). קומיט הבא על
    `fix/b-kupot-health-funds-switch-leads-supabase-read-0820`, יידחף
    (מפעיל פריסת Vercel תחת `more30.com`/הנתיב הממופה ל-28; שינוי אך
    ורק לנתיב הניהול לקריאת פניות ולתיעוד). המפתח `SUPABASE_SERVICE_
    ROLE_KEY` צריך עדיין להיות מוגדר בפועל בסביבת הפריסה (Vercel) --
    זו הכרעת סוד/ערך של המשתמש, לא משהו שהסבב הזה יכול להגדיר בעצמו
    (עולה בקנה אחד עם `core.missing_tokens`/owner=user ברוב הפערים
    הפתוחים האחרים בהיקף הזה).

## 20/08/2026 — סבב 40

235. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון, תיקון קריאת Supabase לליד-החלפה ב-28
    kupot-health-funds, קומיט `8622795d` -- כבר ה-parent של הענף הזה,
    כבר נדחף). בדקתי `core.issues`/`core.project_tasks` פתוחים בהיקף
    17-31: כל הפריטים הפתוחים חסומים חיצונית (פריסת מיגרציה על
    פרויקטי Supabase שאינם נגישים ל-MCP כאן, או הכרעת המשתמש
    ב-dashboard) -- אין פריט agent-owned פתוח וזמין לביצוע. עברתי
    לביקורת קוד טרייה. `apps/20-igud-portal` (שהמניפסט שלו עדיין
    מסומן `source=not-vendored`, מיושן -- הקוד בפועל אמיתי, `server.js`
    אמיתי מול RPCs ב-Supabase, פרוס תחת `more30.com`) לא נבדק עד כה
    בהיקף הזה. מצאתי באג פונקציונלי אמיתי: `POST /api/public/tenant/
    :token/message` (`server.js:70-82`) קורא ל-RPC `submit_public_lead`
    דרך `callRpc()` (fetch גולמי, לא `supabase-js`) ומחזיר את התוצאה
    כמות שהיא ל-לקוח: `res.json(r.data)`. אימתתי מול הגדרת הפונקציה
    בפועל (Supabase MCP, `pg_get_functiondef` על `public.submit_public_
    lead`, `uhnrgujb...`): הפונקציה `RETURNS boolean` -- כלומר גוף
    התשובה שנשלח ללקוח הוא `true`/`false` גולמי (Postgres boolean
    כ-JSON scalar), לא אובייקט `{success:true}`. בצד הלקוח,
    `public/app.js:212-220` בודק `if (res.success)` -- על ערך boolean
    גולמי `res.success` הוא תמיד `undefined` (JS מתייחס ל-primitive
    כ-boxed, אין שדה `success`), ולכן הענף מציג תמיד "שגיאה בשליחה:
    נסה שוב" למגיש הטופס -- **גם כשההודעה נשמרה בהצלחה** ב-`portal_
    messages` (RPC מחזיר `true`, השורה בפועל נכתבת ב-DB). זו לא רק
    הודעת שגיאה קוסמטית: משתמש אמיתי שממלא טופס "צור קשר" בדף ציבורי
    של ארגון/בית כנסת/מגיד שיעור רואה כישלון מדומה בכל פעם, סביר
    שינסה שוב (שכפול פניות באדמין) או יוותר לגמרי -- למרות שהפנייה
    כבר הגיעה. השוויתי מול האח התאום 19-igud-shiurim-portal (אותה
    סכמת `igud`, אותה RPC): שם `handleBooleanRpcResult()` כבר עוטף את
    התוצאה הבוליאנית נכון (`server.js:173`) -- כלומר זה באג ספציפי
    למימוש הפשוט יותר של `callRpc` ב-20, לא בעיה בסכמה/RPC עצמם (סכמת
    `igud` מוגנת -- לא נגעתי בה, גם לא הייתי צריך).
236. **התיקון**: `server.js:81` עוטף את תוצאת ה-RPC לאובייקט תקין --
    `res.json({ success: r.data === true, error: r.data === true ?
    null : "not_found" })` -- תואם בדיוק למה ש-`app.js` כבר מצפה לו
    (`res.success`/`res.error`), אפס שינוי ל-`app.js` עצמו, אפס שינוי
    לשאר נתיבי ה-API באותו קובץ, אפס שינוי לסכמה/RPC. אין `node_modules`
    מותקן (כלל אי-התקנה כרגיל) -- אומת בקריאה חוזרת של הקובץ בלבד.
    קומיט על `fix/b-igud-portal-contact-form-false-error-0820`, יידחף
    (מפעיל פריסת Vercel תחת הנתיב הממופה ל-20).

## 20/08/2026 — סבב 41

237. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (מזהה
    loop B אחרון, תיקון עטיפת boolean RPC בטופס יצירת קשר ב-20
    igud-portal, קומיט `0fac9f3e` -- כבר ה-parent של הענף הזה, כבר
    נדחף) ו-`core.issues`/`core.project_tasks` פתוחים בהיקף 17-31: השורה
    היחידה הפתוחה (#164, 21-mthbram, nedarim-webhook portal_share)
    חסומה במפורש על הכרעת המשתמש (סוד משותף לוובהוק מול השארת מסלול
    פתוח) -- לא ניתנת לביצוע אוטונומי. הרצתי שלושה סוכני Explore
    מקבילים על שטח שלא נסרק לעומק לאחרונה: 19-igud-shiurim-portal,
    31-hebrew-bridge-crm, 24-galilee-connect-hub.
238. **19 חזר נקי** (כל כתיבה עטופה ב-`handleRpcResult`/
    `handleBooleanRpcResult`, XSS מוגן ב-`esc()`, אין endpoint לא-מאומת
    חדש). 31 החזיר ממצא חלש: `updateTaskStatus`/`updateTaskPriority`
    ב-`tasks.functions.ts` לא בודקות rows-affected אחרי `.update()`
    (בניגוד לתבנית שכבר קיימת ב-`client.functions.ts` לתיעוד/מחיקת
    מסמכים) -- אבל בדיקה ישירה הראתה ש-`assertCanMutateTask` (שורות
    154-163) כבר חוסמת בעליות זרה במפורש בקוד האפליקציה (זורקת
    "Forbidden" לפני ה-`update`), כך שתרחיש ה-RLS-מסנן-בשקט שהתבנית
    האחרת מגנה עליו לא ניתן להגעה בפועל כאן -- לא באג חי, רק הגנה
    כפולה בעדיפות נמוכה. דחיתי את זה לטובת הממצא של 24.
239. **24-galilee-connect-hub -- ממצא אמיתי**: `SynagoguePage.tsx`
    (`handleSendContact`, שורות 61-75 לפני התיקון) שולח `INSERT` ל-
    `community_leads` בלי לבדוק את שגיאת ה-RPC בכלל (`await
    supabase.from(...).insert(...)` בלי לפרק `{error}`), ומיד אחר כך
    קורא `setSent(true)` ומציג "הפנייה נשלחה בהצלחה!" -- תמיד, גם אם
    ה-INSERT נכשל. אימתתי שזו בדיוק אותה מחלקת באג שכבר תוקנה בעבר
    (core.issues #240) בשני האחים הישירים באותו קובץ `community_leads`:
    `ContactPage.tsx:65-78` ו-`ServiceRequestForm.tsx:44-58` -- שניהם
    כבר בודקים `const { error } = await supabase...insert(...)`,
    מציגים הודעת שגיאה ב-UI (state `errorMsg`), ומונעים כפל-שליחה עם
    `sending`. `SynagoguePage.tsx` הוא השלישי בקבוצה, ולבדו לא קיבל את
    התיקון הזה בסבב #240 -- כנראה נסרק/פוצל בנפרד. בניגוד לשני האחים,
    `lead_type: 'synagogue'` כאן כן חוקי מול ה-CHECK constraint
    (`lead_type IN ('general','synagogue','rabbi_question',
    'gabbai_support')`, מיגרציה `20260315224054`) -- כך שהבאג כאן הוא
    אך ורק חוסר-בדיקת-שגיאה (רשת/RLS/תקלת DB עתידית), לא ה-CHECK
    שכבר תוקן אצל השניים האחרים.
240. **התיקון**: הוספתי `sending`/`errorMsg` state (זהה בשם ובהתנהגות
    לשני האחים), עטפתי את ה-`insert` בפירוק `{ error }`, מציגים הודעת
    שגיאה ("הפנייה לא נשלחה עקב תקלה. נסו שוב.") ומבטלים את הכפתור
    בזמן שליחה/שדות ריקים במקום לקרוא `setSent(true)` תמיד. אפס שינוי
    ל-`ContactPage.tsx`/`ServiceRequestForm.tsx` (כבר תקינים), אפס
    שינוי לסכמה/RLS/CHECK constraint, אפס שינוי להתנהגות המקרה
    המוצלח (עדיין אותה הודעת הצלחה, עדיין ניקוי השדות). אין
    `node_modules` מותקן בעץ הזה (כלל אי-התקנה כרגיל) -- אומת בקריאה
    חוזרת של הקובץ המלא בלבד. קומיט הבא על
    `fix/b-galilee-connect-hub-synagoguepage-silent-insert-fail-0820`,
    יידחף (מפעיל פריסת Vercel תחת `more30.com/galil`; שינוי אך ורק
    לטופס יצירת הקשר בעמוד בית כנסת בודד).
241. **לופ B, סבב חדש -- סרקתי 4 אפליקציות בטווח 17-31 שלא נבדקו לאחרונה**
    (22-get-your-rights, 26-modaot-studio, 27-bkalut-price,
    37-bkalot-clone) עם ארבעה סוכני Explore מקבילים, מחפש את אותה
    מחלקת באג (כתיבה ל-DB בלי בדיקת `{error}` -> הצלחה מזויפת ב-UI).
    37-bkalot-clone חזר נקי (כל ה-RPC-ים ב-`bkalot-clone-admin/index.ts`
    כבר בודקים `if (!out.ok)`, עבר כבר סבבי תיקון #221-237). 26 ו-27
    החזירו ממצאים אמיתיים אך נמוכים-יותר-בעדיפות (26:
    `BrandKitPage.tsx` `chooseAndSave` בלי try/catch על PATCH; 27:
    `routes.ts` שורה 585 -- כשל בעדכון סטטוס webhook אחרי שהליד/webhook
    כבר נשלחו בהצלחה גורם ל-500 מטעה למשתמש) -- דחיתי את שניהם לטובת
    22, כי שם הממצא היה **בדיוק** התבנית שכבר תועדה כחוזרת (#237-240)
    וגם פגעה בנתיב לכידת-לידים הציבורי (לא רק בעדכון סטטוס פנימי).
242. **22-get-your-rights -- שני ממצאים מאותה מחלקה, אותו קובץ ושכן**:
    (א) `FloatingBot.tsx` `saveLead`, ענף העדכון (dedup לפי
    id_number/phone, שורות 264-286): קורא `await
    supabase.from("leads").update(...)` בלי לפרק `{error}` ומחזיר
    `true` תמיד -- בניגוד לענף ה-`insert` שלוש שורות מתחתיו באותה
    פונקציה, שכבר בודק שגיאה ומציג טוסט. הצ'אטבוט הציבורי (זיהוי
    זכאות/פנייה) הוא נתיב לכידת-הלידים המרכזי של המערכת -- כשל שקט
    כאן אומר שליד שכבר קיים (התאמת ת.ז/טלפון) לא מתעדכן והמשתמש חושב
    שכן. (ב) `AdminRightsReference.tsx` -- שלוש פונקציות ניהול על
    `rights_reference` (`handleAddRight`/`handleSaveEdit`/
    `handleDelete`) עם אותו דפוס בדיוק: `insert`/`update`/`delete` בלי
    בדיקת שגיאה, טוסט "הצליח" מוצג תמיד. תיקנתי את כל הארבעה לבדוק
    `{ error }` ולהציג טוסט `destructive` + לעצור לפני העדכון-מקומי
    (state/reload) במקרה כשל -- תבנית זהה למה שכבר קיים בענף ה-insert
    של אותו קובץ עצמו. אפס שינוי להתנהגות המקרה המוצלח. אין
    `node_modules`/`tsc` מותקן בעץ הזה -- אומת בקריאה חוזרת ידנית של
    שני הקבצים המלאים בלבד (לא בהרצת build). קומיט על
    `fix/b-get-your-rights-silent-write-fail-0820`, יידחף (מפעיל פריסת
    Vercel תחת `more30.com/zchuyot`).

## 20/08/2026 — סבב 42

243. **חזרתי לממצא שנדחה בסבב #241**: 27-bkalut-price,
    `server/routes.ts` שורות 585-589, `POST /api/service-submissions`.
    אימתתי מחדש בקריאה מלאה של ה-handler (שורות 437-603) +
    `storage.ts` (`createServiceSubmission`/`updateServiceSubmissionWebhook`)
    + `server/index.ts` (ה-error middleware הגלובלי, שורות 67-78) +
    `package.json` (Express `^5.0.1`, שתופס דחיית Promise מ-async
    handler אוטומטית ומעביר ל-middleware). הזרימה בפועל: הליד נוצר
    (שורה 452, נשמר ב-DB) ומחובר-webhook נשלח בהצלחה (שורה 577), ורק
    *אחר כך* נכתב תג הסטטוס (`sent`/`failed`) בחזרה לרשומה עצמה
    (`storage.updateServiceSubmissionWebhook`, `db.update(...).run()`
    ללא try/catch). אם הכתיבה הזו נכשלת (תקלת DB/constraint) --
    ה-Promise נדחה, ה-middleware הגלובלי מחזיר 500 ללקוח, אבל הליד
    וה-webhook כבר הצליחו בפועל. זה באג "כישלון מדומה אחרי הצלחה
    אמיתית" -- תמונת המראה של משפחת הבאגים "הצלחה מדומה אחרי כישלון
    אמיתי" שכבר תוקנה שוב ושוב בלולאה הזו (#215-242) -- כאן זה חמור
    יותר בכיוון ההפוך: משתמש שממלא טופס בקשת שירות אמיתי עלול לראות
    שגיאה ולנסות שוב (כפל-ליד) או לוותר, בזמן שהבקשה שלו כבר נקלטה
    ונשלחה לצוות.
244. **התיקון**: עטפתי את קריאת `updateServiceSubmissionWebhook` ב-
    try/catch -- בכישלון רק רושם אזהרה ל-console (אותו דפוס בדיוק כמו
    רישום אישורים משפטיים בשורה 493 באותו קובץ) וממשיך לתשובת ההצלחה
    הרגילה, כי הליד וה-webhook כבר הצליחו. אפס שינוי לזרימת ההצלחה
    הרגילה (אותה תשובת JSON בדיוק), אפס שינוי לסכמה/לוגיקת ה-webhook
    עצמה. `apps/27-bkalut-price` נבדל מ-08-bkalut-app/bkalot-admin
    המוגנים -- מערכת נפרדת (27, "השוואת מחירים בקלות"), לא נגעתי
    בקבצים המוגנים. אין `node_modules` מותקן בעץ הזה -- אומת בקריאה
    חוזרת ידנית של ה-diff בלבד (לא בהרצת build/Express). קומיט על
    `fix/b-bkalut-price-webhook-status-false-500-0820`, יידחף (מפעיל
    פריסת Vercel תחת הנתיב הממופה ל-27, `more30.com/mechiron`).

## 20/08/2026 — סבב 43

245. **לופ B, סבב חדש -- קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress`** (הצעד האחרון של לופ B: 27-bkalut-price, תיקון
    כשל-כתיבה שקט בעדכון תג webhook, קומיט `fc318ecb`, כבר ה-parent
    של הענף הזה, כבר נדחף) ואת `core.issues`/`core.project_tasks`
    הפתוחים בהיקף 17-31: כמעט כל השורות הפתוחות עדיין owner=user
    (חסומות על פריסת מיגרציה לפרויקטי Supabase שאינם נגישים ל-MCP כאן
    -- #248/#249/#251 על 30-zchuyotpro-crm, #250 על 21-mthbram, #203 על
    SMTP) -- שום פריט agent-owned חדש וזמין. הרצתי ארבעה סוכני Explore
    מקבילים על שטח שלא נסרק לעומק לאחרונה, כל אחד מחפש את אותה מחלקת
    באג חוזרת (כתיבה ל-DB בלי בדיקת `{error}` -> הצלחה מזויפת ב-UI, או
    ההיפך -- כישלון מזויף אחרי הצלחה אמיתית): 21-mthbram,
    30-zchuyotpro-crm, {17-chizukim-transcribe, 18-torah-editor-mvp},
    {26-modaot-studio, 37-bkalot-clone} (השניים האחרונים לפי דגש מפורש
    של הסבב הזה על studio/26 ועל שכפול-בקלות/37).
246. **תוצאות הסריקה**: 21-mthbram החזירה תשעה מופעים אמיתיים (כתיבות
    logo/features_enabled/delete בלי בדיקת שגיאה בקבצים שונים) -- נדחו
    לסבב הבא, כי 21 עצמה מתועדת ב-`core.projects` כ"כמעט ריקה, כפילה
    שלישית של 01/15" (ערך עסקי נמוך). 30-zchuyotpro-crm החזירה ארבעה
    מופעים (עדכוני `processing_status`/`signature_status` בלי בדיקת
    שגיאה) -- גם היא "ריקה לגמרי, אין לקוח אחד במערכת" לפי
    `audit_status`. 26-modaot-studio החזירה שלושה (מחיקות פרויקט/מותג
    בלי try/catch) -- אמיתיים אך נמוכים-בעדיפות (מחיקה, לא יצירה/שמירה
    של תוכן). 37-bkalot-clone חזרה נקייה (סבבי #221-237 כבר סגרו את כל
    נתיבי הכתיבה). 17-chizukim-transcribe חזרה נקייה. **18-torah-editor-
    mvp** החזירה את הממצא בעל הערך הגבוה ביותר: `app/api/htr/process/
    route.ts` -- שישה עדכוני `htr_jobs` (סטטוס `processing`/`raw_ready`/
    `failed`/`corrected`) בלי בדיקת `{error}`, בפייפליין העיבוד המרכזי
    של מערכת חיה ואמיתית ("הפענוח מקצה לקצה רץ על כתב יד אמיתי" לפי
    `audit_status`) -- לא כפילה ריקה. בחרתי בזה: 18 הוא המוצר האמיתי
    היחיד מבין הארבעה שנסרקו, וההשפעה חמורה יותר מרוב הממצאים האחרים --
    שני העדכונים הקריטיים (`raw_ready` אחרי OCR, `corrected` אחרי תיקון
    LLM) שומרים תוצאה של קריאת מנוע **בתשלום אמיתי** שכבר רצה; כשל כתיבה
    שקט שם לא רק מציג הודעת הצלחה שגויה -- הוא **מאבד את התוצאה עצמה**
    בלי עקבות ובלי דרך לנסות שוב, אחרי שכבר שולם עליה.
247. **התיקון**: הוספתי בדיקת `{error}` לכל שש הכתיבות ב-`htr_jobs.update()`
    בקובץ. לשתי הכתיבות הקריטיות (`raw_ready` אחרי OCR, `corrected` אחרי
    תיקון) -- בכישלון, קורא ל-`fail()`/מחזיר תשובת שגיאה עם 502 במקום
    `ok:true` השקרי, כדי שהלקוח יידע שהתוצאה לא נשמרה במקום לחשוב שהעיבוד
    הסתיים בהצלחה. לשאר ארבע הכתיבות (`processing`, `HtrEngineNotConfigured`,
    ה-fallback של תיקון-שנכשל, ו-`fail()` עצמה) -- אלו כתיבות "best-effort"
    של סטטוס-ביניים/רישום-כשל שכבר יש להן טיפול-שגיאה מפורש בזרימה (התשובה
    ללקוח כבר משקפת את המצב האמיתי גם בלי הכתיבה), אז רק רושמות `console.error`
    בלי לשנות את קוד התשובה -- לא רוצה להחזיר 500 על כישלון-בתוך-כישלון.
    אימתתי את הצד הלקוחי (`app/htr/page.tsx:225-237`): הוא כבר בודק גנרית
    `pr.status`/`pr.ok` ומציג `pd.error`, כך שתשובת 502 החדשה מוצגת נכון
    כשגיאה בלי שום שינוי קוד שם. אפס שינוי לזרימת ההצלחה הרגילה (אותן
    תשובות בדיוק כשאין כשל כתיבה), אפס שינוי לסכמה/RLS. אין `node_modules`
    מותקן בעץ הזה -- אומת בקריאה חוזרת ידנית של הקובץ המלא + הצד הלקוחי
    בלבד (לא בהרצת build/Next). קומיט `28500915` על
    `fix/b-torah-editor-htr-silent-write-fail-0820`, יידחף (מפעיל פריסת
    Vercel תחת הנתיב הממופה ל-18, `more30.com/orech`). **הבא בתור:**
    21-mthbram (תשעה מופעים מתועדים בקבצים/שורות מדויקים) ו-
    30-zchuyotpro-crm (ארבעה מופעים) נותרו כמועמדים תקפים לסבב הבא, אם
    אין ממצא בעל ערך עסקי גבוה יותר.

## 20/08/2026 — סבב 44

248. **לופ B, סבב חדש -- בדקתי קודם את core.issues #248/#250/#251 (הקריטיים
    הפתוחים בהיקף 17-31) כמועמדים בעדיפות גבוהה יותר מהתור שנקבע בסבב
    הקודם**. #248/#251 (30-zchuyotpro-crm, הרחבת-הרשאה-עצמית) התבררו
    כבר-מתוקנים בקוד (מיגרציות `20260819230000_guard_profile_role_change.sql`
    ו-`20260820120000_guard_tenant_settings_change.sql`, שני BEFORE UPDATE
    triggers שחוסמים שינוי `role`/`settings` על-ידי מי שאינו admin) -- רק
    חסום על פריסה לפרויקט Supabase שאינו נגיש דרך MCP כאן, שום דבר נוסף
    לעשות. #250 (21-mthbram, `public.synagogues` RLS פתוח-לגמרי) חקרתי
    לעומק: מיגרציית ההקשחה האחרונה (20260712192546) נעלה כמעט כל טבלה
    אחרת בפרויקט (`org_rabbis`, `prayer_times`, `study_day_events` וכו')
    מאחורי `has_role(auth.uid(),'admin')`, אבל **הפורטלים עצמם** (עמוד
    ניהול ארגון/בית-כנסת) לא מבוססים על Supabase Auth בכלל -- הם נגישים
    דרך `access_token` אטום ב-URL בלבד (`OrgPortal.tsx`/`SynagoguePortal.tsx`
    שולפים שורה לפי `.eq("access_token", token)` בלי כניסת משתמש), ואותה
    מיגרציה גם ביטלה `SELECT` על עמודת `access_token` עבור anon/authenticated
    -- כלומר קיים סיכון ממשי (לא מאומת, כי הפרויקט `aypsqqvfohekxxuqsmrw`
    לא נגיש כאן) שאותה הקשחה **כבר שברה** את זרימת הכניסה לפורטל עצמה
    וגם את היכולת של בעל-פורטל לגיטימי להוסיף רב/זמן-תפילה/יום-עיון, ולא
    רק שהיא לא כוללת את `synagogues`. נעילת `synagogues` באותו דפוס בלי
    לפתור קודם את שאלת אימות-הטוקן (אין today שום מנגנון RLS/RPC שמאמת
    התאמת access_token מול הפעולה) הייתה עלולה לשבור פיצ'ר לגיטימי חי
    (הוספת בית כנסת דרך פורטל הארגון) -- בניגוד ישיר לכלל ZERO REGRESSION.
    לא נגעתי בקוד/במיגרציות של 21/30 בסבב הזה; רשמתי את זה כממצא חדש
    (לא ב-core.issues כדי לא לכפול את #250 הקיים -- העדכון הוא context
    נוסף על אותו issue) ועברתי למועמד הבא בתור.
249. **חזרתי למועמד השני מהתור שנקבע ב-#247: 21-mthbram, אחד-עשר מופעים
    אמיתיים** של אותה מחלקת באג (כתיבת Supabase בלי בדיקת `{error}` ->
    הצלחה מזויפת) שאיתרתי בדיוק (קובץ+שורה) עם סוכן Explore: שני העלאות
    לוגו כמעט-זהות (`OrgPortal.tsx:82`, `RabbiPortal.tsx:84`), שלושה
    מתגי-הפעלה בלוח הניהול (`AdminDashboard.tsx:1066/1170/1182`, הפעלת/
    כיבוי "הגדרות מתקדמות"/"זמני תפילות"), שתי העלאות תמונה
    (`PortalSettingsTab.tsx:164` רקע מותאם, `:179` תמונת רב), מחיקת
    בקשת-הרחבה (`FullAccessRequestsTab.tsx:74`), ושלוש כתיבות ליד מתוך
    הצ'אטבוט הציבורי (`FloatingChatBot.tsx:115/117/119` -- שיעור/מבקש/
    מורה). כולם באותו דפוס: `await supabase...insert/update/delete(...)`
    בלי לפרק `{error}`, ומיד אחריו טוסט הצלחה בלתי-מותנה + עדכון state
    מקומי. תשעת המופעים (מתוך תשעה שתועדו בסבב #245-246) אומתו ונמצא
    מופע נוסף אחד עשירי (`FloatingChatBot.tsx`, שלוש כתיבות תחת אותו
    ACTION-tag handler, נספרו כמופע אחד לוגי אך שלוש כתיבות בפועל).
250. **התיקון**: לכל אחד עשר -- הוספתי בדיקת `{error}` ובכישלון: או
    `throw` (כשהקריאה כבר בתוך try/catch עם catch קיים שמציג טוסט
    שגיאה -- `OrgPortal`/`RabbiPortal`/`PortalSettingsTab`, אותו דפוס
    בדיוק כמו `uploadLogo` שכבר קיים תקין באותם קבצים ל-storage upload),
    או טוסט `destructive` ישיר + `return` לפני עדכון ה-state (התבנית
    הקיימת כבר ב-`AdminDashboard.tsx` עצמו, למשל `genericDelete`/
    `approveAllForOrg`). ב-`FloatingChatBot.tsx` השארתי את ה-`catch`
    הקיים (`/* silent */`, לתקלות JSON.parse בלבד) ורק הוספתי משתנה
    `submitError` שנאסף משלושת הענפים ומכריע אם להציג טוסט הצלחה או
    שגיאה. אפס שינוי לזרימת ההצלחה הרגילה בכל אחד עשר המקומות (אותם
    טוסטים/עדכוני state בדיוק כשאין כשל כתיבה), אפס שינוי לסכמה/RLS/
    להתנהגות היצירה עצמה. אין `node_modules` מותקן בעץ הזה -- אומת
    בקריאה חוזרת ידנית של כל שישה הקבצים (לא בהרצת build/Vite). קומיט
    `512c91dd` על `fix/b-mthbram-silent-write-fail-0820`, יידחף (מפעיל
    פריסת Vercel תחת הנתיב הממופה ל-21). **הבא בתור:** 30-zchuyotpro-crm
    (ארבעה מופעים מתועדים בסבב #245-246, עדכוני `processing_status`/
    `signature_status` בלי בדיקת שגיאה) נותר מועמד תקף לסבב הבא.

## 20/08/2026 — סבב 45

251. **לופ B, סבב חדש -- קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress`** (הצעד האחרון: 21-mthbram, אחד-עשר תיקוני
    כשל-כתיבה-שקט, קומיטים `512c91dd`/`ebff401b`, כבר ה-parent של
    הענף הזה, כבר נדחף) ואת `core.issues` הפתוחים בהיקף 17-31 --
    כל השורות הפתוחות (#152, #169, #180, #248, #249, #251) עדיין
    owner=user, חסומות על גישת דשבורד/פריסת מיגרציה לפרויקטי Supabase
    שאינם נגישים ל-MCP כאן. שום פריט agent-owned חדש וזמין. חזרתי
    למועמד שתועד בסוף סבב #250: 30-zchuyotpro-crm, ארבעה מופעים של
    אותה מחלקת באג חוזרת (כתיבת Supabase בלי בדיקת `{error}`).
252. **אימות מדויק**: הרצתי סוכן Explore שאיתר את ארבעת המופעים
    בדיוק (קובץ+שורה) ואז קראתי את שני הקבצים המלאים בעצמי כדי לאמת:
    `apps/30-zchuyotpro-crm/src/routes/api/public/analyze-paystub.ts`
    (שלוש כתיבות `processing_status` -- "processing" לפני שליחה
    ל-n8n בשורה 36, "failed" כשאין webhook מוגדר בשורה 42, "failed"
    כשה-fetch ל-webhook נכשל בשורה 57) ו-
    `apps/30-zchuyotpro-crm/src/routes/api/public/notify-signature.ts`
    (כתיבת `signature_status: "pending"` בשורה 28 לפני התראת חתימה
    ללקוח). המופע השלישי חמור יותר משנראה: התגובה של הקוד עצמו
    (שורות 55-58, הערה קיימת) כבר מזהירה שהמטרה של אותה כתיבה היא
    למנוע מהמסמך "להישאר תקוע" עם הודעת "בעיבוד" לנצח -- אבל בלי
    בדיקת שגיאה, בדיוק אותו תרחיש יכול לקרות בכתיבה שאמורה למנוע
    אותו. שני האפליקציות (17-31) מדורגות ב-`core.projects` כ"חיה
    טכנית אבל ריקה לגמרי -- אין לקוח אחד במערכת" (audit_status),
    ולכן ערך עסקי נמוך יותר משאר התיקונים בלולאה הזו, אך זה המועמד
    היחיד שתועד ולא נמצא ממצא בעל ערך גבוה יותר.
253. **התיקון**: לכל ארבע הכתיבות -- פירקתי `{ error }` מהתוצאה
    והוספתי `console.error` מזוהה (שם-הראוט + השדה שנכשל) בכישלון,
    בלי לשנות אף קוד-תגובה/גוף-תגובה קיים. בחרתי שלא להחזיר קוד שגיאה
    חדש כי כל ארבע הכתיבות הן "best-effort" -- מסלולי ה-webhook כבר
    מחזירים תשובה שמשקפת נכון את מצב הדיספאץ' עצמו (לא את מצב עדכון
    התג), ותקן קיים באותה לולאה (#247, `torah-editor-mvp`) כבר קבע
    שכתיבות best-effort מקבלות רק לוג במקום קוד-שגיאה חדש. אפס שינוי
    לזרימת ההצלחה הרגילה, אפס שינוי לסכמה/RLS. אין `node_modules`
    מותקן בעץ הזה -- אומת בקריאה חוזרת ידנית של שני הקבצים המלאים
    בלבד (לא בהרצת build/TanStack). נדרש `git add -f`
    (`apps/30-zchuyotpro-crm/src` מוחרג כברירת מחדל ב-gitignore, אותו
    דפוס כמו בכל סבב קודם שנגע בנתיבים האלה). קומיט `ec4c22fc` על
    `fix/b-zchuyotpro-crm-document-status-silent-write-fail-0820`,
    יידחף (מפעיל פריסת Vercel תחת הנתיב הממופה ל-30). **הבא בתור:**
    שום מועמד ספציפי לא תועד -- הסבב הבא צריך לפתוח בסריקת Explore
    טרייה על מערכות שלא נסרקו לאחרונה בהיקף 17-31 (למשל 19/20 שסומנו
    "לא נבנתה" ב-`core.projects` וכדאי לוודא שזה עדיין מדויק, או 23/25
    שסומנו ככפילות-ריקות).

## 20/08/2026 — סבב 46

254. **לופ B, סבב חדש -- קראתי README/CONNECTIONS, בדקתי `core.run_progress`**
    (הצעד האחרון: 30-zchuyotpro-crm, קומיטים `ec4c22fc`/`7bea7cff`, כבר
    ה-parent של הענף הזה) ו-`core.issues`/`core.project_tasks` הפתוחים
    בהיקף 17-31 -- כל השורות עדיין owner=user, חסומות על גישת דשבורד/
    פריסה לפרויקטי Supabase שאינם נגישים ל-MCP כאן, שום פריט agent-owned
    זמין. בדקתי את ההצעה מסוף סבב #253 (23/25 -- אומתו כריפו ריק לגמרי,
    `app.json` בלבד, `"source": "not-vendored"`, אין קוד לתקן; 19/20 --
    `core.projects.audit_status` עדיין אומר "לא נבנתה" אבל זה **מיושן**:
    לשתיהן יש קוד מוונדר אמיתי (7 קבצים כל אחת) שכבר קיבל תיקוני-אמת
    בסבבים קודמים (#235-236 ל-20, "six-delete-handlers" ל-19) -- לא
    תיקנתי את המטא-דאטה הזו, ערכה נמוך).
255. **מצאתי ממצא חדש בעדיפות גבוהה יותר ב-26-modaot-studio (studio, בדגש
    מפורש של scope הלופ)**: `vercel-adapter/api/_lib/server/routes.ts` --
    זהו העץ **הפרוס בפועל** (`server/` הוא עץ SQLite למפתחים בלבד, לפי
    הערת התיעוד של הקובץ עצמו). Express 4 (המנוע כאן) **אינו** תופס
    reject שנזרק בתוך async route handler, ו-`registerRoutes` לא רושם
    error middleware גלובלי (4 ארגומנטים) בשום מקום. `storage.ts`'s
    `unwrap()` זורק `Error` רגיל על כל שגיאת Supabase אמיתית. שנים-עשר
    ה-handlers של `/api/templates`, `/api/projects`, `/api/brands`
    (GET/POST/PATCH/DELETE) קראו לפונקציות storage ישירות בלי
    `try/catch` -- בניגוד לכל route של AI/branding באותו קובץ, שכולם
    כבר מגינים על עצמם ומחזירים `{error}` מובנה. משמעות: כשל DB אמיתי
    (הפרת RLS, כשל רשת חולף וכו') על אחד מהם היה משאיר את הבקשה תלויה
    בלי שום תשובה, במקום כשל מהיר -- עד ש-timeout של Vercel Function
    היה הורג אותה. זה שונה מדפוס "silent-write-fail" הרגיל (שם התשובה
    כן חוזרת, רק שהיא שקרית) -- כאן אין תשובה בכלל, גרוע יותר.
256. **התיקון**: הוספתי wrapper משותף `crud()` שעוטף handler ב-`try/catch`
    ומחזיר `res.status(500).json({error})` באותה צורה כבר קיימת בכל שאר
    הקובץ, והחלתי אותו על כל שנים-עשר ה-handlers שלא היו מוגנים. אפס
    שינוי לכל תשובת-הצלחה קיימת (אותו קוד/גוף בדיוק כשאין כשל) -- אומת
    בקריאה חוזרת מלאה של הקובץ אחרי העריכה, כולל ספירת סוגריים
    תוכניתית. הקפדתי על טיפוס `Promise<any>` (לא `Promise<void>`)
    ל-handler הפנימי כדי לא להסתכן בשגיאת `tsc` על `return res.status
    (...).json(...)` בתוך handler מוקלד -- הבנייה של ה-Vercel Function
    הזו **כן** מריצה build אמיתי, בניגוד לרוב שאר העץ שאין בו
    `node_modules`. נדרש `git add` על נתיב מוחרג ב-`.gitignore`
    (`apps/26-modaot-studio/vercel-adapter`, אותו דפוס כמו בכל סבב קודם
    שנגע בנתיב הזה) -- ניסיון ראשון בלי `-f` נכשל בשקט (לא עצר את
    ה-chain כמו שציפיתי כי הקובץ כבר tracked, לא untracked, אז ה-add
    כן הצליח בפועל; ה-commit שאחריו כן נכשל מסיבה אחרת שלא זוהתה --
    הרצתי מחדש `git commit` בנפרד ועבד). קומיט `a2b88629` על
    `fix/b-modaot-studio-crud-routes-unhandled-throw-0820`, נדחף (מפעיל
    פריסת Vercel תחת הנתיב הממופה ל-26, `more30.com/studio`). **הבא
    בתור:** לא נותר ממצא agent-actionable מתועד בהיקף 17-31 -- הסבב הבא
    צריך לפתוח בסריקת Explore טרייה, אולי על 29-bkalot-design (רק
    upload.sh תוקן עד כה, שאר העץ לא נסרק) או 31-hebrew-bridge-crm
    (חיה טכנית אבל ריקה -- כדאי לוודא שאין עוד handlers בלי error
    handling באותו דפוס).

## 20/08/2026 — סבב 47

257. **לופ B, סבב חדש -- קראתי README/CONNECTIONS, בדקתי `core.run_progress`**
    (הצעד האחרון: 26-modaot-studio CRUD unhandled-throw fix, קומיטים
    `a2b88629`/`f6b0e9a6`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` להיקף 17-31 -- שום שינוי במעמד הידוע. בדקתי את שתי
    ההצעות מסוף סבב #256: 29-bkalot-design התברר כריפו design-tokens
    סטטי בלבד (`tokens.json`/`style-guide.html`/`build.js`/`upload.sh`,
    אין ראוטים/state שכותב ל-Supabase) -- אין מה לתקן שם מעבר למה שכבר
    תוקן. פניתי ל-31-hebrew-bridge-crm והרצתי סוכן Explore ממוקד על אותה
    מחלקת באג חוזרת (כתיבת Supabase בלי בדיקת `{error}`, ו/או route
    handler בלי try/catch כשאין error middleware גלובלי -- כמו ב-26).
258. **הממצאים אומתו בקריאה חוזרת מלאה של שני הקבצים**: כל ראוטי
    ה-API הציבוריים (`incoming.ts`, `leads/submit.ts`) כן בודקים
    `{error}`, ויש error handling גלובלי ב-`server.ts` -- דפוס #2 (כמו
    ב-26) לא חוזר כאן. אבל נמצאו שני מופעים אמיתיים של דפוס #1 בשני
    hooks שמופעלים מ-cron/trigger פנימי (לא מ-UI): `sla-monitor.ts`
    (Route השבועי שמעלה עדיפות משימות שעברו את מועד היעד) הכניס שורת
    `communication_logs` בלי לבדוק `{error}` -- כשל כתיבה שם מאבד את
    יומן הביקורת בשקט אחרי שהמשימה כבר הועלתה בעדיפות בהצלחה.
    `outbox-dispatch.ts` (מפעיל ה-webhook היוצא מתוך `outbox_queue`)
    עדכן שורה ל-`status: "sent"`/`"failed"` בלי לבדוק `{error}` -- המקרה
    החמור מבין השניים: אם עדכון ה-`"sent"` נכשל, השורה נשארת `"pending"`
    וההרצה הבאה תשלח את אותו webhook שוב למטרה חיצונית -- כפילות שליחה
    אמיתית, לא רק אובדן לוג.
259. **התיקון**: בשני המקומות -- פירקתי `{ error }` מהתוצאה והוספתי
    `console.error` מזוהה (כולל הערה מפורשת ב-`outbox-dispatch.ts` על
    סיכון השליחה הכפולה) בכישלון, בלי לשנות שום קוד-תשובה/גוף-תשובה
    קיים -- אותו דפוס בדיוק מסבבים #247/#253 (best-effort write ->
    לוג בלבד, לא קוד שגיאה חדש), כי שני ה-hooks האלה כבר מחזירים
    `ok:true`/מונים מדויקים ללא תלות בהצלחת הכתיבה המשנית עצמה. אפס
    שינוי לזרימת ההצלחה הרגילה. אין `node_modules` מותקן בעץ הזה --
    אומת בקריאה חוזרת ידנית מלאה של שני הקבצים בלבד. נדרש `git add -f`
    (`apps/31-hebrew-bridge-crm/src` מוחרג כברירת מחדל ב-gitignore, אותו
    דפוס חוזר). קומיט `959ba9c8` על
    `fix/b-hebrew-bridge-crm-hooks-silent-write-fail-0820`, יידחף (מפעיל
    פריסת Vercel תחת הנתיב הממופה ל-31, `more30.com/gesher`). **הבא
    בתור:** שום ממצא agent-actionable נוסף לא תועד בהיקף 17-31 -- הסבב
    הבא צריך לפתוח בסריקת Explore טרייה על מערכת שלא נסרקה עדיין לעומק
    בסבב הזה (למשל 24-galilee-connect-hub, פרויקט Supabase נפרד משלה
    שלא נבדק כלל עד כה, או 28-kupot-health-funds).

## 20/08/2026 — סבב 48

260. **לופ B, סבב חדש -- קראתי README/CONNECTIONS, בדקתי `core.run_progress`**
    (הצעד האחרון: 31-hebrew-bridge-crm hooks, קומיטים `959ba9c8`/`6a62a8a9`,
    כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.issues` הפתוחים בהיקף 17-31
    -- כל השורות `owner='user'` (#245/#248-251) עדיין חסומות על גישת דשבורד/
    פריסה לפרויקטי Supabase שאינם נגישים ל-MCP כאן, השורה היחידה
    `owner='agent'` הפתוחה (#242, מחשבון חיסכון/זכאות §9) עדיין חסומה על
    אותה סיבה שתועדה בה כבר (פרויקטי 30/22 לא נגישים ל-MCP/Lovable כאן).
    פעלתי לפי ההצעה מסוף סבב #259: הרצתי שני סוכני Explore במקביל על
    24-galilee-connect-hub (פרויקט Supabase נפרד `mwljkonwdeuaahsigjdp`, לא
    נבדק לעומק עד כה) ו-28-kupot-health-funds (לוודא שאין עוד מופעים של
    התבנית שכבר תוקנה בו פעם אחת, #233-234). 28 חזר כמעט נקי (מופע גבולי
    יחיד, `await readTopic()` מחוץ ל-try/catch אך מכוסה ע"י error middleware
    גלובלי -- לא תוקן, ערך נמוך). 24 חזר עם **33 מופעים מאומתים** של
    "silent-write-fail" (כתיבת Supabase בלי בדיקת `{error}`) פרושים על שמונה
    קבצים -- ChatBot.tsx/AskRabbiSection.tsx (שני טפסי "שאל את הרב"
    הציבוריים, אחד מהם עם הודעת הצלחה קבועה `sent(true)` ללא תנאי כלל),
    GallerySection.tsx/ActivitySlideshow.tsx (ווידג'טים אדמין גלריה/סליידשואו),
    SynagogueDetailsManager.tsx (קומפוננטה שלמה, לא בשימוש בפועל -- לא
    מיובאת משום מקום, רק תיעוד בהערה ב-GabaiPortal.tsx, אך תוקנה בכל זאת
    לפי הכלל "לא למחוק פיצ'ר קיים"), וכל תשע מנהלי ה-CRUD בלוח הניהול
    (`GabaiPortal.tsx`: SynagogueManager, AdminPasswordManager,
    ContactInfoManager [רק ה-cleanup delete], HalachaManager,
    NewsletterManager, KashrutManager, AdBannerManager, KnowledgeManager,
    ContactLeadsManager, RabbiQuestionsManager). דוגמה בולטת: `GabaiPortal.tsx`
    `AdminPasswordManager.updatePassword`/`deleteAdmin` -- שינוי/מחיקת סיסמת
    מנהל שיכולים להיכשל בשקט ולהראות "הצלחה" בעוד שהחשבון נותר עם הסיסמה
    הישנה.
261. **התיקון**: לכל 33 המופעים -- פירקתי `{ error }` מהתוצאה והוספתי בדיקה
    שמציגה הודעת שגיאה `text-destructive` (אותו דפוס בדיוק שכבר קיים ותקין
    ב-`ContactInfoManager`/`ContactPage.tsx`/`ServiceRequestForm.tsx` באותו
    ריפו: state נפרד `saveError`/`actionError`, `return` לפני עדכון ה-state
    של הצלחה) במקום להראות הצלחה מזויפת. במקומות עם הרבה כתיבות מקבילות
    (`SynagogueDetailsManager.handleSave`, `SynagogueManager.handleDelete`)
    אספתי `hasError` על פני כל הכתיבות ורק בסוף החלטתי אם להציג הצלחה או
    שגיאה, כדי לא לעצור באמצע פעולה מורכבת. בייבוא CSV (SynagogueManager/
    KashrutManager) ספרתי שורות שנכשלו והצגתי מונה במקום להיכשל בשקט על
    השורה הראשונה. במחיקת ה-cleanup הישנה ב-`ContactInfoManager` (כתיבה
    best-effort, לא בנתיב ההצלחה העיקרי) הוספתי רק `console.error` בהתאם
    לתקדים שנקבע בסבבים #247/#253/#259. אפס שינוי לזרימת ההצלחה הרגילה בכל
    33 המקומות, אפס שינוי לסכמה/RLS. אין `node_modules` מותקן בעץ הזה --
    אומת בקריאה חוזרת מלאה (`git diff`) של כל שישה הקבצים שנערכו, כולל
    בדיקת איזון סוגריים תוכנית על הקבצים המלאים (לא בהרצת build/Vite).
    נדרש `git add -f` (`apps/24-galilee-connect-hub/src` מוחרג כברירת מחדל
    ב-gitignore, אותו דפוס חוזר מכל סבב קודם שנגע בנתיבים האלה). קומיט
    `c393d27c` על `fix/b-galilee-connect-hub-silent-write-fail-0820`, נדחף
    (מפעיל פריסת Vercel תחת הנתיב הממופה ל-24, `more30.com/galil`). **הבא
    בתור:** 28-kupot-health-funds נסרק ונמצא כמעט נקי (מופע גבולי יחיד לא
    תוקן, מכוסה ע"י error middleware). הסבב הבא צריך לפתוח בסריקת Explore
    טרייה על מערכת שלא נסרקה עדיין (19-igud-shiurim-portal, 20-igud-portal,
    23-haorech-torani, 25-mor1-main-site תועדו בעבר כריפואים ריקים/כפילויות
    -- כדאי לוודא שזה עדיין מדויק לפני שמדלגים עליהם).

## 20/08/2026 — סבב 49

262. **לופ B, סבב חדש -- קראתי README/CONNECTIONS, בדקתי `core.run_progress`**
    (הצעד האחרון בהיקף לופ B: 24-galilee-connect-hub, קומיטים
    `c393d27c`/`9c709cd0`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` בהיקף 17-31 -- שום שינוי מטא-דאטה. אימתתי שוב את
    ההצעה שנרשמה בסוף סבב 48 (19/20/23/25): 23-haorech-torani (2 קבצים,
    `index.html`+`app.json` בלבד) ו-25-mor1-main-site (`app.json` בלבד)
    הם אכן ריקים לחלוטין -- אין מה לתקן שם. אבל **19-igud-shiurim-portal**
    ו-**20-igud-portal** הם קוד מוונדר אמיתי (`server.js` + `public/`) עם
    היסטוריית תיקונים קודמת -- רק ה-`server.js` שלהם נסרק אי-פעם, קובצי
    ה-`public/app.js` הלקוחיים (2447 ו-439 שורות) לא נבדקו מעולם. הרצתי
    שני סוכני Explore במקביל, אחד לכל אחד, לחפש דפוסי-באג חוזרים (silent-
    write-fail בצד לקוח, XSS דרך innerHTML לא-מסונן, race על submit כפול).
263. **20-igud-portal (`app.js`) חזר עם 4 "ממצאי XSS" -- כולם false positive,
    אומתו ונדחו בקריאה ישירה שלי.** ה-helper `el()` (שורה 4-17) תמיד עוטף
    ילדי מחרוזת ב-`document.createTextNode()` (שורה 14) -- לעולם לא
    `innerHTML` על תוכן דינמי; אפשרות ה-`html:` ב-attrs (שורה 8) קיימת
    אך אף קריאה בקובץ לא משתמשת בה בפועל. הסוכן פספס את זה כי חלק
    מהילדים נבנים דרך template literals (`` `${l.city}...` ``) לפני
    שהם מועברים ל-`el()` -- אבל גם מחרוזת שנבנתה כך הופכת ל-text node
    בטוח, לא ל-HTML גולמי. לא תוקן דבר (אין באג).
264. **19-igud-shiurim-portal (`app.js`) חזר עם 3 ממצאי race אמיתיים: submit
    כפול בלי נטרול הכפתור** -- טופס "הצטרפות ופניות" הציבורי (שורה 776,
    יוצר חשבון Supabase auth + שורת `national_request`/`join`), כפתורי
    תשלום-מנוי בלוח ניהול ארגון (שורה 1530, `[data-pay]`, יוצר checkout
    session מול נדרים פלוס), וטופס יצירת בקשת-תשלום בלוח סופר-אדמין (שורה
    2415, יוצר שורת `subscription_payments`). אימתתי שזו סטייה אמיתית מדפוס
    קיים באותו קובץ עצמו: טופס `donate-form` (שורה 626-660) וכפתורי-סנכרון
    (שורות 1519-1528, 1722+) כבר עושים `btn.disabled = true` לפני הקריאה
    ו-`= false` ב-`finally` -- שלושת המקומות שנמצאו הם היחידים שלא. חומרה
    בינונית (לא דליפת מידע/כספים אמיתית -- לכל היותר שורת בקשה כפולה
    שצוות/גבאי יראה ויתעלם ממנה, או שני מודאלי-תשלום שנפתחים זה על זה),
    אבל אמיתית ועקבית עם התקדים שכבר קיים באותו קובץ.
265. **התיקון: הוספתי את אותו דפוס `disabled=true`/`finally disabled=false`
    (או `if (submitBtn) disabled=false` בענף השגיאה בלבד, כששל ההצלחה
    כבר מחליף את ה-DOM דרך `draw()`) לשלושת המקומות**, זהה במבנה לתקדים
    הקיים ב-`donate-form`/`sync-now-btn` באותו קובץ. גם הוספתי guard
    `if (btn.disabled) return;` בתחילת ה-`onclick` של כפתורי `[data-pay]`
    (מאזין `forEach` שמצרף מאזין לכל כפתור בנפרד, בניגוד לטופס עם מאזין
    יחיד -- ה-guard מונע race דומה גם לפני שה-`disabled=true` הראשון מספיק
    "לתפוס"). אפס שינוי לזרימת ההצלחה הרגילה בשלושתם. אומת ב-`node --check`
    על הקובץ המלא (בניגוד לרוב שאר העץ, כאן יש Node זמין כי זה קובץ
    סטטי-בלבד ללא build step, לא צריך `tsc`/`esbuild`). אין `node_modules`
    מותקן בעץ הזה מעבר לכך. נדרש `git add -f`
    (`apps/19-igud-shiurim-portal/public` מוחרג כברירת מחדל ב-gitignore,
    אותו דפוס חוזר). קומיט `1833505c` על
    `fix/b-igud-shiurim-portal-duplicate-submit-0820`, יידחף (מפעיל פריסת
    Vercel תחת הנתיב הממופה ל-19). **הבא בתור:** 20-igud-portal חזר נקי
    (רק false-positives). הסבב הבא צריך לפתוח בסריקת Explore טרייה על
    מערכת/זווית שלא נבדקה עדיין -- למשל 21-mthbram (רק silent-write-fail
    נבדק, לא race/XSS) או חזרה ל-17-chizukim-transcribe עם עדשת race-
    condition (לא נבדק עדיין מהזווית הזו).

## 20/08/2026 — סבב 50

266. **לופ B, סבב חדש -- קראתי README/CONNECTIONS, בדקתי `core.run_progress`**
    (הצעד האחרון: 19-igud-shiurim-portal duplicate-submit, קומיטים
    `1833505c`/`c86d1377`, כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.issues`
    הפתוחים בהיקף 17-31 -- שום ממצא agent-actionable חדש (`owner='agent'`
    הפתוח היחיד, #242, עדיין חסום על אותה סיבה מתועדת). פעלתי לפי ההצעה
    מסוף סבב 49: הרצתי סוכן Explore על 21-mthbram (רק silent-write-fail
    נבדק בעבר, #246-250 -- לא race/XSS) לחפש submit כפול בלי נטרול כפתור
    ו-XSS דרך innerHTML לא-מסונן.
267. **חזר עם 13 ממצאי race אמיתיים, אפס XSS** (ה-`dangerouslySetInnerHTML`
    היחיד ב-`chart.tsx` מזריק רק CSS קבוע מתצורת ה-theme, לא תוכן משתמש --
    אומת ונדחה). ה-13: `ContactSection.handleSubmit` (טופס יצירת-קשר
    ציבורי), `RabbiPortal.saveEdit`/`addLesson`, `OrgPortal.addRabbi`/
    `saveEdit`/`addLesson` (לוחות ניהול פרטיים), `BulkLessonTable.
    handleSend`/`handleSendAll`, `StudyDayEventTable.handleSend`/
    `handleSendAll` (טבלאות ניהול-המוני), `NedarimManagement.
    publishToLessons`, ו-`PortalSettingsTab.saveAbout`/`saveContactInfo`/
    `saveCustomSections` (טאב הגדרות פורטל) -- כולם קוראים ל-Supabase
    insert/update בלי לנטרל את הכפתור, כך שדאבל-קליק/דאבל-טאפ מפעיל את
    הפעולה פעמיים (שורת פנייה כפולה, שיעור כפול, עדכון כפול). אימתתי מול
    תקדים קיים באותו ריפו עצמו: `TeachersLanding`/`RequestLesson`/
    `UpdateLesson`/`BulkUpload` כבר משתמשים ב-state `submitting`/`cSending`
    ומנטרלים את הכפתור -- ה-13 שנמצאו הם היחידים שלא, אותו דפוס בדיוק
    כמו 19-igud-shiurim-portal בסבב הקודם (#264-265).
268. **התיקון**: הוספתי state נפרד לכל פונקציה (או `Set<string>` של
    id-ים pending בטבלאות עם שורות מרובות, כדי לא לנטרל שורות אחרות
    בזמן פעולה על שורה אחת) עם `if (state) return; setState(true); try {
    ... } finally { setState(false); }`, וקישרתי `disabled={state}` +
    טקסט "...שולח/שומר/מוסיף" לכפתור המתאים -- זהה במבנה לתקדים הקיים.
    אפס שינוי לזרימת ההצלחה הרגילה בכל 13 המקומות. אומת בקריאה חוזרת
    מלאה (`git diff`) של כל שבעת הקבצים שנערכו, כולל השוואת בדיקת איזון
    סוגריים תוכנית מול הגרסה המקורית מ-git (כדי לוודא שממצאי "unclosed"
    מהבודק הפשוט שלי הם false-positive קיים-מראש בקוד המקורי, לא משהו
    שהתיקון הכניס). אין `node_modules`/`tsc` זמינים בעץ הזה. נדרש
    `git add -f` (`apps/21-mthbram/src` מוחרג כברירת מחדל ב-gitignore,
    אותו דפוס חוזר מכל סבב קודם שנגע בנתיבים האלה). קומיט `29ae4b35` על
    `fix/b-mthbram-duplicate-submit-0820`, יידחף (מפעיל פריסת Vercel
    תחת הנתיב הממופה ל-21, `more30.com/mthbram`). **הבא בתור:** הסבב
    הבא צריך לפתוח בסריקת Explore טרייה על מערכת/זווית שלא נבדקה עדיין
    -- למשל חזרה ל-17-chizukim-transcribe עם עדשת race-condition, או
    22-get-your-rights/24-galilee-connect-hub/31-hebrew-bridge-crm עם
    אותה עדשה (רק silent-write-fail נבדק בהם עד כה).

## 20/08/2026 — סבב 51

269. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (הצעד
    האחרון: 21-mthbram duplicate-submit, קומיטים `29ae4b35`/`d3cb2332`,
    כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.projects` בהיקף 17-31:
    אין שינוי מטא-דאטה מאז הסבב הקודם. המלצת "הבא בתור" מסבב 50 הצביעה
    על עדשת race-condition/duplicate-submit על מערכות שנבדקו רק ל-
    silent-write-fail. הרצתי שני גלי סוכני Explore מקבילים: גל א' על
    22-get-your-rights/24-galilee-connect-hub/26-modaot-studio/
    28-kupot-health-funds; גל ב' על 17-chizukim-transcribe/
    18-torah-editor-mvp (המוצרים החיים בעלי הערך העסקי הגבוה ביותר
    בהיקף, טרם נבדקו לדפוס הזה בכלל).
270. **חזרו 3 ממצאים אמיתיים בשתי מערכות, כל השאר נקיים** (22, 26, 28,
    17 -- כולם כבר משתמשים ב-state `busy`/`loading`/`isSubmitting` או
    ב-`useMutation().isPending` של TanStack Query, אומת ידנית מול קובץ
    המקור, לא רק תיאור הסוכן): **24-galilee-connect-hub**
    `GabaiPortal.tsx` `ContactLeadsManager` -- כפתורי "סמן כנקרא"
    (שורה 1389) ו-"מחק" (שורה 1396) בלוח פניות צור-קשר של הגבאי קראו
    ל-Supabase update/delete בלי לנטרל את הכפתור, בזמן שרכיבי-אח
    באותו קובץ (`AdminPasswordManager` וכו') כבר משתמשים ב-state
    `loading`/`saving`. **18-torah-editor-mvp** `app/documents/page.tsx`
    `remove()` (שורה 173-179, כפתור "מחיקה" שורה 307) מחק מסמך מ-
    `orech_documents` בלי state `busy`/`disabled` כלל, בניגוד לתקדים
    הקיים באותו קובץ (`handleFiles`/`newBlank` כבר משתמשים ב-`busy`) --
    זהו המוצר החי היחיד בהיקף עם pipeline OCR/LLM אמיתי, כך שכפילות
    כאן משפיעה על מסמכי משתמשים אמיתיים גם אם לא על עלות מנוע. התיקון:
    ב-24 הוספתי state יחיד `processingId` (המזהה ה-lead שבטיפול, לא
    מנטרל שורות אחרות) ל-`ContactLeadsManager`, `if (processingId)
    return; setProcessingId(lead.id); ... setProcessingId(null);` +
    `disabled={processingId === lead.id}` לשני הכפתורים; ב-18 הוספתי
    `deletingId` לאותו `remove()`/כפתור עם אותו דפוס plus טקסט
    "מוחקים…" בזמן הפעולה (תואם את `busy` string שהכפתורים האחרים כבר
    מציגים). אפס שינוי לזרימת ההצלחה הרגילה בשני הקבצים. אין
    `node_modules`/`tsc` זמינים בעץ הזה -- אומת בקריאה חוזרת מלאה של
    שני ה-diff (סוגריים מאוזנים, אין קריאה כפולה ל-state). שני הקבצים
    כבר עוקבים ב-git (לא נדרש `git add -f` הפעם). קומיט על
    `fix/b-galilee-torah-editor-duplicate-submit-0820`, יידחף (מפעיל
    פריסת Vercel תחת `more30.com/galil` ו-`more30.com/orech`). **הבא
    בתור:** 26-modaot-studio/28-kupot-health-funds/17-chizukim-transcribe/
    22-get-your-rights חזרו נקיים לדפוס הזה בסבב הזה -- הסבב הבא צריך
    לפתוח בסריקה על 30-zchuyotpro-crm/31-hebrew-bridge-crm (רק
    silent-write-fail נבדק בהם, לא duplicate-submit) או לחזור לבדוק
    את ה-rebrand ('עולם הסטארטאפים')/pricing TEST-mode בפועל בתוך כל
    אפליקציה 17-31 (המיפוי התיעודי כבר נעשה בסבב 80 הישן, אך לא אומת
    מחדש שכל עמוד/כותרת בפועל תואם).

## 20/08/2026 — סבב 52

271. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (הצעד
    האחרון: 24-galilee-connect-hub/18-torah-editor-mvp duplicate-submit,
    קומיטים `dabd6836`/`f22ebb17`, כבר ה-parent של הענף הזה, כבר נדחף)
    ו-`core.issues` הפתוחות בהיקף 17-31: כל השורות עדיין `owner='user'`,
    חסומות על גישה חיצונית שלא השתנתה (#152/#169/#202/#206/#209/#248/
    #249/#250/#251/#180); #242 (`owner='agent'`) עדיין חסום על אותה
    סיבה מתועדת. פעלתי לפי ההצעה מסוף סבב 51: הרצתי שני סוכני Explore
    במקביל, אחד על 30-zchuyotpro-crm ואחד על 31-hebrew-bridge-crm,
    בעדשת duplicate-submit (כפתור/טופס שמפעיל כתיבת Supabase בלי לנטרל
    את עצמו).
272. **שתי המערכות חזרו כמעט נקיות -- שלא כמו 19/21/24/18 בסבבים
    הקודמים.** ב-30-zchuyotpro-crm כל שאר הכפתורים כבר עוקבים אחרי
    `useMutation().isPending`, ורק `settings.tsx` `IntegrationsTab.save`/
    `NotificationsTab.save` (שורות 203-212, 280-289) נטענו ע"י הסוכן
    כ"חסרי try/finally" -- **בדקתי ודחיתי**: לכפתורים יש כבר
    `disabled={saving}` (מנטרל מיידית לפני ה-await, כך שאין מרוץ
    דאבל-קליק אמיתי), וצורת ה-`setX(true); await; setX(false); if
    (error)...` בלי `try/finally` היא בעצם **הדפוס הקיים והעקבי** בכל
    האפליקציה הזו (`auth.tsx` שורות 101/113/204, `reset-password.tsx`
    שורה 49, `settings.tsx` שורה 106 -- כולן זהות, שום מקום לא סוטה).
    לא תיקנתי דבר שם -- זו לא סטייה מתקדים, זו התקדים עצמו, ואין
    ל-supabase-js נטייה לזרוק חריגה לא-מטופלת בקריאות רגילות (הוא
    מחזיר `{error}` גם על כשל רשת, לא זורק). ב-31-hebrew-bridge-crm
    כל הכפתורים/טפסים כבר משתמשים ב-`loading`/`saving`/
    `mutation.isPending` **חוץ מאחד**: `RoleLayout.tsx` `handleSignOut`
    (שורה 32-38, כפתור "התנתק" שורה 80) קרא ל-`queryClient.clear()`+
    `supabase.auth.signOut()` בלי שום נטרול -- דאבל-קליק היה מפעיל את
    שתי הקריאות פעמיים (לא נזק אמיתי -- signOut אינו הרסני -- אבל
    סטייה אמיתית ועקבית מהתקדים הקיים בכל שאר הקובץ/אפליקציה, אותה
    משפחת-באג שתועדה ותוקנה שוב ושוב מסבב 49).
273. **התיקון: state `signingOut` יחיד + guard `if (signingOut) return`
    + `try/finally` + `disabled={signingOut}` על הכפתור** -- זהה
    במבנה לתקדים הקיים (`auth.tsx` וכו') באותו ריפו. אפס שינוי לזרימת
    ההצלחה הרגילה. גם אימתתי מחדש (גריפ) שהמיתוג המשותף
    (`auth-button.js`) עדיין טעון בכל עשר המערכות החיות בהיקף (17/18/
    21/22/24/26/27/28/30/31) -- אין דריפט מאז סבב 18, לא נדרש תיקון.
    `SidebarMenuButton` מקבל `disabled` דרך `React.ComponentProps<"button">`
    שנפרס ל-`<button>` (אין `asChild` בקריאה הזו) -- אומת בקריאת
    ה-component עצמו לפני השימוש. אין `node_modules`/`tsc` בעץ הזה --
    אומת בקריאה חוזרת מלאה של ה-diff. נדרש `git add -f`
    (`apps/31-hebrew-bridge-crm/src` מוחרג כברירת מחדל ב-gitignore, אותו
    דפוס חוזר). קומיט `2df05a5b` על
    `fix/b-hebrew-bridge-crm-signout-duplicate-submit-0820`, יידחף
    (מפעיל פריסת Vercel תחת `more30.com/gesher`). **הבא בתור:** דפוס
    duplicate-submit נראה כעת נקי כמעט בכל 17-31 (רק שני ממצאים זעירים
    השבוע, לעומת עשרות בסבבים קודמים) -- הסבב הבא צריך לפתוח בעדשה
    אחרת לגמרי, למשל: `27-bkalut-price` (הכלי הפנימי של בקלות, לא נבדק
    בעדשה הזו כלל), או בדיקה חוזרת של `26-modaot-studio` `audit_gaps`
    #4 ("החל פלטה על הפרויקט") אם יש דרך לאמת בקריאת-קוד בלבד בלי
    דפדפן, או סריקת silent-write-fail טרייה על `27-bkalut-price`/
    `29-bkalot-design` שטרם נבדקו לדפוס הזה.

## 20/08/2026 — סבב 53

274. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (הצעד
    האחרון: 31-hebrew-bridge-crm signout duplicate-submit, קומיטים
    `2df05a5b`/`9599a92b`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` בהיקף 17-31: אין שינוי מטא-דאטה. פעלתי לפי ההצעה
    מסוף סבב 52: הרצתי סוכן Explore על `27-bkalut-price` (הכלי הפנימי
    לצוות בקלות) בעדשת silent-write-fail (כתיבה שנכשלת בלי שום משוב
    למשתמש) -- עדשה שלא נבדקה בו קודם (הסבב היחיד שנגע ב-27 עד כה,
    #130-131, בדק חשיפת auth על endpoints, לא error-handling). `29-
    bkalot-design` (המועמד השני מהרשימה) התברר כשלד תיעודי בלבד (README/
    tokens.json/style-guide.html, אין קוד אפליקציה אמיתי) -- לא רלוונטי
    לעדשה הזו, לא נבדק.
275. **נמצא: `client/src/pages/community-admin.tsx` (עמוד ניהול שאלוני
    גבאי-קהילה) -- שמונה פונקציות כתיבה (`createQuestionnaire`,
    `saveQuestionnaire`, `addQuestion`, `updateQuestion`,
    `deleteQuestion`, `addLink`, `toggleLink`, `deleteLink`, שורות
    102-153) קראו ל-`apiRequest()` בלי `try/catch` כלל.** אימתתי ש-
    `apiRequest` (`client/src/lib/queryClient.ts` שורה 71-91) זורק
    `Error` על כל תגובה שאינה 2xx (`throwIfResNotOk`) -- כלומר כשל
    כתיבה (500, ניתוק רשת, פקיעת session) הפך ל-unhandled promise
    rejection: השדה מקבל blur/change כרגיל, שום toast/שגיאה לא מוצג,
    המנהל חושב שהעריכה נשמרה. אימתתי מול תקדים קיים **באותו קובץ
    עצמו**: `saveSettings` (שורה 79-90) כבר עוטף בדיוק את אותה קריאה
    ב-`try/catch` + `toast({variant:"destructive"})` -- שמונה הפונקציות
    שנמצאו הן היחידות בקובץ שסטו מהתקדים הזה. עמוד זה הוא זרימת העבודה
    התכופה ביותר בממשק הניהול הפנימי (עריכת שאלון גבאי-קהילה: שדות
    כותרת/תיאור/טקסטים על `onBlur`, מתגי הפעלה/איסוף-קשר, שאלות
    בודדות, קישורים) -- כך שכשל שקט כאן פוגע ישירות בעבודת הצוות
    היומיומית, לא בקצה נדיר.
276. **התיקון: עטפתי כל שמונת הפונקציות ב-`try/catch` עם `toast`
    ייעודי לפי הפעולה** (למשל "שמירת השאלון נכשלה"/"מחיקת השאלה
    נכשלה"), זהה במבנה בדיוק ל-`saveSettings` הקיים באותו קובץ. אפס
    שינוי לזרימת ההצלחה הרגילה בכל שמונת המקומות (אותם קריאות
    `loadDetail`/`loadList`/איפוס state אחרי הצלחה, רק עכשיו בתוך
    `try`). אין `node_modules`/`tsc` בעץ הזה -- אומתה איזון סוגריים
    בסקריפט Node קצר על כל הקובץ (0), וקריאה חוזרת מלאה של ה-diff.
    נדרש `git add -f` (`apps/27-bkalut-price/client` מוחרג כברירת מחדל
    ב-gitignore, אותו דפוס חוזר מכל סבב קודם שנגע בנתיבי `client`/`src`
    תחת `apps/`). קומיט `7307c64c` על `fix/b-bkalut-price-community-
    admin-silent-fail-0820`, יידחף (מפעיל פריסת Vercel/PM2 תחת הנתיב
    הממופה ל-27, `bekalut.more30.com`/`admin.bekalut.more30.com` -- לא
    לגעת ב-`CLAUDE.md` המקומי של 27 שמתעד את `isAdminSubdomain()`/
    `getPublicOrigin()`, לא נגעתי בהם, רק בטיפול השגיאות). **הבא
    בתור:** שאר עמודי הניהול של 27 (`org-detail`/`orgs`/`integrations`/
    `match`/`dashboard`/`price-comparison-admin`/`potential-admin`)
    טרם נבדקו לעדשת silent-write-fail הזו -- מועמד טבעי לסבב הבא, או
    חזרה לבדיקה חוזרת של rebrand/pricing TEST-mode בפועל על-גבי
    עמודים חיים בכל 17-31 (רק תועד תיעודית, לא אומת מול תוכן עמוד
    בפועל).

## 20/08/2026 — סבב 54

277. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (הצעד
    האחרון: 27-bkalut-price community-admin silent-write-fail, קומיטים
    `7307c64c`/`269823a2`, כבר ה-parent של הענף הזה, כבר נדחף). פעלתי
    לפי ההצעה מסוף סבב 53: הרצתי סוכן Explore על שאר עמודי הניהול של
    27 (`org-detail`/`orgs`/`integrations`/`match`/`dashboard`/
    `price-comparison-admin`/`potential-admin`) בעדשת silent-write-fail.
    **הדוח שחזר על `price-comparison-admin.tsx` היה שגוי** -- טען
    שמונה מקומות "ללא try/catch" (`addCategory`/`addStore`/
    `addProduct`/`addPrice`/`addPromo`/`del` וכו') -- קראתי את הקובץ
    המלא בעצמי ואימתתי ש**כל** הפונקציות האלה כבר עטופות ב-`try/catch`
    + `toast` תקין (הקובץ הזה למעשה כבר נקי לגמרי מהדפוס הזה). לא
    סמכתי על הדוח וסרקתי בעצמי עם grep על `apiRequest\("(POST|PATCH|
    PUT|DELETE)"` בכל 40+ עמודי `client/src/pages/` כדי למצוא את
    היעד האמיתי בעל הערך הגבוה ביותר.
278. **נמצא: `financial.tsx` (המודול הפיננסי הפעיל -- לקוחות/תנועות/
    תקציב/תזכורות/הזדמנויות זכויות/שיוך מאמנים/פניות/טיפים) -- 15
    קריאות `useMutation` של TanStack Query, אפס `onError` בכל הקובץ.**
    אימתתי ב-`client/src/lib/queryClient.ts` שאין ברירת מחדל גלובלית
    ל-`onError`/`onSettled` על מוטציות (`mutations: { retry: false }`
    בלבד) -- כלומר כשל כתיבה (500/ניתוק רשת/פקיעת session) לא מציג
    שום toast/הודעה, רק הכפתור חוזר למצב פעיל בלי משוב. זהו המסך
    הפעיל ביותר במודול הפיננסי של בקלות (נתוני כסף אמיתיים של לקוחות)
    שטרם נבדק לעדשה הזו כלל -- ערך גבוה משמעותית מ-`price-comparison-
    admin` שכבר התברר כמכוסה. `financial-crm.tsx` (11 מוטציות, רק
    `onError` אחד) ו-`health-funds-admin.tsx` (14 קריאות כתיבה, לא
    `useMutation`) נשארו לסבב הבא -- לא נגעתי בהם.
279. **התיקון: הוספתי `onError: () => toast({ title: "...",
    variant: "destructive" })` לכל 15 המוטציות** (יצירה/מחיקה/עדכון
    בכל אחד מ-`ClientsTab`/`TransactionsTab`/`BudgetsTab`/
    `RecurringTab`/`OpportunitiesTab`/`CoachesTab`/`LeadsTab`/
    `TipsTab`), טקסט שגיאה ספציפי לכל פעולה. שבעה מתוך שמונת רכיבי
    הטאב לא קראו ל-`useToast()` בכלל -- הוספתי `const { toast } =
    useToast();` לכל אחד (הוא כבר מיובא בראש הקובץ). אפס שינוי
    לזרימת ההצלחה הרגילה (`onSuccess` נשאר בדיוק כפי שהיה בכל מקום).
    אין `node_modules`/`tsc` בעץ הזה -- אומתה איזון סוגריים בסקריפט
    Node קצר על כל הקובץ (0) + ספירת `useMutation`==15 מול `onError`
    שנוסף==15 (השנייה כללה גם את שורת ה-`import`, שנספרה בנפרד).
    נדרש `git add -f` (`apps/27-bkalut-price/client` מוחרג כברירת
    מחדל ב-gitignore, אותו דפוס חוזר). קומיט `c0f4eacb` על
    `fix/b-bkalut-price-financial-silent-fail-0820`, נדחף (מפעיל
    פריסת Vercel/PM2 תחת אותו נתיב הממופה ל-27, לא נגעתי ב-
    `CLAUDE.md` המקומי/`isAdminSubdomain()`/`getPublicOrigin()`).
    **הבא בתור:** `financial-crm.tsx` (11 מוטציות, כמעט אף אחת ללא
    `onError`) ו-`health-funds-admin.tsx` (14 קריאות כתיבה, טרם נבדק)
    -- שני מסכים פעילים נוספים ב-27 שטרם עברו עדשת silent-write-fail
    מלאה, מועמדים טבעיים לסבב הבא. לתת עדיפות לאימות ידני ישיר של
    הקוד (לא לסמוך על תקציר סוכן Explore בלבד) -- הדוח בסבב הזה הראה
    שממצאי "אין try/catch" יכולים להיות שגויים אם הסוכן קרא גרסה
    ישנה/החליק על השורות.

## 20/08/2026 — סבב 55

280. **קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`** (הצעד
    האחרון: 27-bkalut-price financial.tsx silent-write-fail, קומיטים
    `c0f4eacb`/`2a6ee20d`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` בטווח 17-31: אין שינוי מטא-דאטה. המשכתי לפי ההצעה
    מסוף סבב 54: `health-funds-admin.tsx` טרם נבדק לעדשת silent-write-
    fail. הרצתי סוכן Explore על שאר עמודי הניהול של 27 שטרם נבדקו
    (`org-detail`/`orgs`/`integrations`/`match`/`advanced-match`/
    `dashboard`/`price-comparison-admin`/`potential-admin`/`chatbot-
    admin`/`health-funds-admin`) בעדשה הזו. אימתתי בעצמי (לא סמכתי על
    הדוח בלבד, לפי הלקח מסבב 54): קראתי את `health-funds-admin.tsx`
    ישירות (שורות 190-340) ואישרתי שש פונקציות כתיבה אמיתיות ללא
    `try/catch` כלל: `toggleActive` (הפעלה/כיבוי נושא, PATCH),
    `removeTopic` (מחיקת נושא, DELETE -- הכפתור הציג "הנושא נמחק"
    גם כשהמחיקה בפועל נכשלה בשרת), `addTier`/`updateTierValue`/
    `removeTier` (הוספה/עדכון/מחיקה של מדרגות קופות חולים) ו-
    `createTopic` (יצירת נושא חדש). שאר העמודים שנבדקו (`org-detail`/
    `orgs`/`integrations`/`match`/`advanced-match`/`dashboard`/
    `price-comparison-admin`/`potential-admin`/`chatbot-admin`) חזרו
    נקיים -- כל הכתיבות בהם כבר עטופות כראוי (אומת בעבר או בסבב זה).
281. **התיקון: עטפתי את שש הפונקציות ב-`try/catch` + `toast`
    ייעודי לפי הפעולה**, זהה במבנה בדיוק לתקדים הקיים **באותו קובץ
    עצמו** -- `saveTopic` (שורה 201-214) ופונקציות ניהול הפודקאסט
    (`genPodcastScript`/`savePodcastScript`/`genPodcastAudio`/
    `delPodcastAudio`/`sendPodcast`) כבר עוטפות כתיבה זהה ב-`try/
    catch` עם בדיקת `r.ok` ו-`toast({variant:"destructive"})`. אפס
    שינוי לזרימת ההצלחה הרגילה בכל שש המקומות (אותם `setTopics`/
    `setDraft`/`loadTopics` אחרי הצלחה, רק עכשיו בתוך `try` ומאחורי
    בדיקת `r.ok`). אין `node_modules`/`tsc` בעץ הזה -- אומתה איזון
    סוגריים בסקריפט Node קצר על כל הקובץ (0) + קריאה חוזרת מלאה של
    ה-diff. נדרש `git add -f` (`apps/27-bkalut-price/client` מוחרג
    כברירת מחדל ב-gitignore, אותו דפוס חוזר מכל סבב קודם שנגע בנתיבי
    `client`/`src` תחת `apps/`). קומיט `21c5ae62` על `fix/b-bkalut-
    price-health-funds-admin-silent-fail-0820`, יידחף (מפעיל פריסת
    Vercel/PM2 תחת הנתיב הממופה ל-27, לא נגעתי ב-`CLAUDE.md` המקומי/
    `isAdminSubdomain()`/`getPublicOrigin()`). **הבא בתור:** עדשת
    silent-write-fail על 27-bkalut-price נראית כמעט מוצתה עכשיו (כל
    15+ העמודים בפרויקט נבדקו על פני מספר סבבים) -- הסבב הבא צריך
    לפתוח בעדשה אחרת לגמרי (למשל duplicate-submit על 26-modaot-studio/
    29-bkalot-design שטרם נבדקו לעדשה הזו) או לחזור לאימות בפועל
    (לא רק תיעודי) של rebrand ('עולם הסטארטאפים')/pricing TEST-mode
    על-גבי תוכן עמוד אמיתי בכל 17-31, שלא אומת מחדש מאז סבב 80.

## 20/08/2026 — סבב 282 (loop B)

282. **26-modaot-studio (סטודיו, בטווח המפורש של הלולאה): עדשת
    duplicate-submit, שטרם נבדקה על האפליקציה הזו (רק silent-write-
    fail על מסלולי ה-CRUD ב-`vercel-adapter` נבדק בעבר, סבב 47/
    DECISIONS #254-256).** לפי ההצעה מסוף הסבב הקודם. הרצתי סוכן
    Explore על קוד הלקוח (`client/src/pages`) לעדשת "כפתור/טופס
    שמפעיל כתיבה בלי מצב busy/disabled החוסם לחיצה כפולה". אימתתי
    כל ממצא בעצמי לפני נגיעה: `BrandKitPage.tsx` `chooseAndSave()`
    (כפתורי בחירת קונספט לוגו, שורה 279-292) שלח PATCH ל-
    `/api/brands/:id` **ללא כל `try/catch` וללא חסימת לחיצה חוזרת**
    בזמן שכנים באותו קובץ (`generateLogos`/`vectorize`) כבר משתמשים
    ב-`genBusy`/`vecBusy`; `BrandingHome.tsx` `del()` (כפתור מחיקת
    מותג, שורה 131-137) שלח DELETE ללא חסימה, בזמן ש-`Projects.tsx`
    באותה אפליקציה כבר משתמש בתבנית `busyId===p.id`. **התיקון:**
    הוספתי `savingIdx`/`busyId` state זהה בצורתו לתקדים הקיים באותו
    קובץ/אפליקציה + `disabled` על הכפתורים המתאימים; גם עטפתי את
    `chooseAndSave` ב-`try/catch`+`toast(destructive)` כי לא היה לה
    כלל טיפול בשגיאה (אותה משפחת באג silent-write-fail שתוקנה כבר
    בשאר האפליקציה, כאן זו הפעם הראשונה שנבדקה ב-`BrandKitPage.tsx`
    עצמו). אפס שינוי לזרימת ההצלחה. אימתתי איזון סוגריים ב-Node על
    שני הקבצים (0) + קריאה חוזרת מלאה של ה-diff. נדרש `git add -f`
    (`apps/26-modaot-studio/client` מוחרג כברירת מחדל ב-gitignore,
    אותו דפוס חוזר). קומיט `2760b5a6` על `fix/b-modaot-studio-
    duplicate-submit-0820`, יידחף (מפעיל פריסת Vercel תחת
    `more30.com/studio`). **הבא בתור:** `29-bkalot-design` נשאר
    repo תיעודי-בלבד (tokens.json/style-guide.html, אין קוד אפליקציה
    אמיתי) -- לא רלוונטי לעדשה הזו, לא ייבדק שוב אלא אם יתווסף קוד.
    הסבב הבא יכול לחזור לאימות בפועל של rebrand/pricing TEST-mode
    על תוכן עמוד אמיתי בכל 17-31 (לא אומת מאז סבב 80), או לפתוח
    עדשה חדשה (למשל XSS/unsanitized-innerHTML) על מערכות שטרם נבדקו
    לה.

## 20/08/2026 — סבב 283 (loop B)

283. **פתחתי עדשה חדשה כפי שהוצע בסוף הסבב הקודם: XSS/unsanitized-
    innerHTML, שטרם נבדקה במפורש על 17-31 (רק silent-write-fail
    ו-duplicate-submit נסרקו בסבבים הקודמים).** הרצתי סוכן Explore על
    12 מערכות (17/18/19/20/21/22/24/26/27/28/30/31) לחיפוש
    `dangerouslySetInnerHTML`/`.innerHTML=`/`insertAdjacentHTML`/
    `eval`/`document.write` עם קלט לא-ליטרלי. רוב הממצאים חזרו
    "בטוחים" (כבר עטופים ב-`escapeHtml`/`esc()` מקומי) -- אימתתי כמה
    מהם בעצמי (`19-igud-shiurim-portal/public/app.js`,
    `17-chizukim-transcribe/client/src/lib/export.ts`) וזה נכון.
    **ממצא אמיתי אחד שרד אימות עצמי מלא:** `24-galilee-connect-hub/
    src/components/ChatBot.tsx` שורה 519-523 -- ה-chatbot הציבורי
    (widget פתוח לכל מבקר באתר) מזריק `msg.content` ישירות ל-
    `dangerouslySetInnerHTML` אחרי שני `replace()` של markdown
    בלבד (`**bold**`, `[text](url)`) -- **בלי שום escaping קודם.**
    קראתי את הקוד סביב (שורה 412-415) ואישרתי: `userMsg =
    input.trim()` (הטקסט החופשי שהמבקר מקליד בתיבת הצ'אט) נדחף ישירות
    ל-state `messages` עם `role:'user'`, ואז **אותו** בלוק רינדור
    (לא בלוק נפרד לפי role) מציג גם הודעות משתמש דרך
    `dangerouslySetInnerHTML` -- כלומר הקלדת `<img src=x
    onerror=alert(1)>` בתיבת הצ'אט מריצה JS מיידית בדפדפן המבקר עצמו
    (self-XSS כבסיס, אך זה עדיין באג ממשי של הזרקת HTML גולמי ללא
    escaping, לא רק חומרה תיאורטית -- כל תג HTML שהמבקר מקליד "בורח"
    ומתפרש כמרקאפ, לא רק תגי script).
284. **התיקון: הוספתי פונקציית `escapeHtml()` (בורחת `& < > " '`)
    והפעלתי אותה על `msg.content` *לפני* שני ה-`replace()` של
    ה-markdown**, כך שתגי HTML גולמיים מנוטרלים אבל `**bold**`/
    `[text](url)` ממשיכים לעבוד בדיוק כמו קודם (התווים `* [ ] ( )`
    לא נגעו ב-escaping). בדקתי גם את מקרה הקצה היחיד הרלוונטי בקוד
    הקיים -- ההודעה ההארדקודת עם קישור WhatsApp (שורה 415, טווח
    415) בונה URL עם `encodeURIComponent` על כל הפרמטרים המשתנים,
    כך שאין תווים גולמיים כמו `&`/`<`/`>` בתוכה שיישברו ע"י ה-escaping
    החדש (רק גרש בודד `'` בשם משתמש עלול לעבור escaping לתוך ה-URL
    במקרה קיצון נדיר -- לא בעיית אבטחה, רק קוסמטי, לא טיפלתי בו כדי
    לא להוסיף מורכבות שלא נדרשה). אפס שינוי לזרימת ההצלחה הרגילה
    (בוט/משתמש עדיין רואים בדיוק את אותו טקסט מעוצב, רק בלי אפשרות
    הזרקת תגיות). אין `node_modules`/`tsc` בעץ הזה -- אומתה איזון
    סוגריים בסקריפט Node קצר על הקובץ (0) + קריאה חוזרת מלאה של
    ה-diff (9 שורות בלבד, שינוי אחד ל-`__html:` + פונקציה חדשה בת
    7 שורות). הקובץ לא היה gitignored (בניגוד לדפוס הרגיל של
    `client`/`src` תחת `apps/` -- כאן `apps/24-galilee-connect-hub/
    src` דווקא כן מוחרג כברירת מחדל, נדרש `git add -f`). קומיט
    `e4ad3a77` על `fix/b-galilee-connect-hub-xss-chatbot-0820`, יידחף
    (מפעיל פריסת Vercel תחת `more30.com/galil`). **הבא בתור:** שאר
    ה-11 המערכות שנבדקו בעדשת ה-XSS הזו חזרו נקיות (כבר עטופות כראוי)
    -- הסבב הבא יכול לחזור לאימות בפועל (לא רק תיעודי) של rebrand/
    pricing TEST-mode על תוכן עמוד אמיתי בכל 17-31 (לא אומת מאז סבב
    80), או לפתוח עדשה חדשה נוספת (למשל open-redirect/CSRF) על
    מערכות שטרם נבדקו לה.

## 20/08/2026 — סבב 284 (loop B)

285. **חזרתי לאימות בפועל של rebrand/pricing TEST-mode על תוכן עמוד
    אמיתי בכל 17-31, כפי שהוצע בסוף שני הסבבים הקודמים (לא אומת
    מאז סבב 80).** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: 24-galilee-connect-hub ChatBot
    XSS, קומיטים `e4ad3a77`/`8733066b`, כבר ה-parent של הענף הזה,
    כבר נדחף) ו-`core.projects` בטווח 17-31: אין שינוי מטא-דאטה.
    הרצתי סוכן Explore על שתי עדשות במקביל על עשר המערכות החיות
    (17/18/21/22/24/26/27/28/30/31): (א) שם מותג ישן/שאריות
    Lovable שדולף בכותרות/מטא-תגיות/פוטר במקום "עולם הסטארטאפים",
    (ב) האם קוד תשלום/תמחור פר-מערכת נמצא ב-TEST/sandbox mode.
    אימתתי כל ממצא בעצמי (לא סמכתי על הדוח בלבד -- לקח מסבבים
    קודמים). **ממצא רברנד אמיתי אחד:** `30-zchuyotpro-crm/src/routes/
    __root.tsx` שורות 82/84/85 -- `title`/`og:title`/`twitter:title`
    הציגו `"זכויות פרו | ZchuyotPro"`, שאריות סלאג אנגלי שנוצר
    ע"י Lovable. בדקתי את כל תשע האחיות החיות האחרות (24/26/27/28/31
    ישירות, השאר דרך הדוח) -- כולן מציגות כותרת עברית טהורה בלי
    סלאג אנגלי שיורי (למשל 24=`"מחוברים — בתי כנסת חצור הגלילית"`,
    26=`"מודעות AI — יצירת מודעות מעוצבות לקהילה"`, 27=`"מאגר בקלות —
    כלי פנימי לצוות"`, 28=`"השוואת קופות חולים | מבית בקלות"` (סיומת
    עברית, לא אנגלית), 31=`"מערכת CRM שותפים"`). 30 היא היחידה עם
    סלאג אנגלי דלוף -- סטייה אמיתית מהתקדים העקבי בכל שאר ההיקף.
    אימתתי גם שקרדיט המותג המשותף (`auth-button.js`, "פותח ע״י עולם
    הסטארטאפים" בפוטר) כבר טעון ב-30 (אושר בעבר בסבב 52/271) -- כלומר
    זו הפעם שהמותג המאוחד *כן* מופיע בפועל בכל מערכת, לא כותרת
    העמוד עצמה (אף מערכת חיה אחרת לא מציגה "עולם הסטארטאפים" בכותרת
    -- זה לא הדפוס, ולכן לא הוספתי אותו). **grep מלא על כל 30 אישר
    3 המופעים היחידים** של `"ZchuyotPro"` בקוד, כולם באותו בלוק מטא.
286. **התיקון: הסרתי `" | ZchuyotPro"` משלושת השדות**, השארתי
    "זכויות פרו" בלבד -- זהה בדיוק לתבנית העברית-טהורה שכל שאר
    ההיקף כבר משתמש בה. לא נגעתי ב-`og:image`/`twitter:image` (מצביעים
    על נכס תמונה מ-Lovable R2, לא טקסט מותג -- שינוי בלי נכס חלופי
    היה עלול לשבור תצוגה מקדימה חברתית קיימת, מחוץ להיקף "רברנד
    טקסט"). אפס שינוי לזרימה הרגילה (רק מטא-תגיות `<head>`, אין
    לוגיקה). **בדיקת TEST-mode תשלומים:** לא נמצא מפתח תשלום קשיח
    (`pk_live`/`sk_live`/מפתחות Stripe אמיתיים) באף אחת מעשר המערכות
    -- כל ההפניות ל-Stripe הן ברשימת חבילות build בלבד, לא מפתחות.
    נמצא קישור תרומה קשיח אמיתי אחד: `21-mthbram/src/components/
    Footer.tsx` שורה 5, `mosad=7013027` ל-matara.pro (Nedarim Plus) --
    **לא נגעתי בו**: זה מספר מוסד אמיתי שכבר קיים במערכת המקורית
    לפני האיחוד (לא נוצר ע"י הלולאה הזו), וכלל הקדושה #1 אוסר לשבור
    חיבור חי קיים -- הפיכתו ל-TEST-mode הייתה עלולה לשבור תרומות
    אמיתיות שכבר עובדות דרך אותו קישור. היקף "pricing TEST-mode" של
    הלולאה מתייחס לפיצ'רים חדשים שהלולאה עצמה מוסיפה, לא לחיבורי
    תרומה שקדמו למיגרציה. אין `node_modules`/`tsc` בעץ הזה -- אומתה
    איזון סוגריים בסקריפט Node קצר (0) + קריאה חוזרת מלאה של ה-diff
    (3 שורות בלבד). נדרש `git add -f` (`apps/30-zchuyotpro-crm/src`
    מוחרג כברירת מחדל ב-gitignore, אותו דפוס חוזר). קומיט `4aa9ade0`
    על `fix/b-zchuyotpro-crm-rebrand-title-0820`, יידחף (מפעיל פריסת
    Vercel תחת הנתיב הממופה ל-`30-zchuyotpro-crm`, כנראה `/crm` לפי
    ה-favicon `href: "/crm/favicon.svg"` באותו קובץ). **הבא בתור:**
    עדשת הרברנד/pricing נראית כעת ממוצתה על פני 17-31 (רק ממצא אחד
    זעיר שרד, עשר המערכות האחרות נקיות) -- הסבב הבא צריך לפתוח בעדשה
    חדשה שטרם נבדקה במפורש (למשל open-redirect/CSRF, או בדיקת
    accessibility/`aria-*` על טפסים ציבוריים), או לחזור לבדוק את
    31-hebrew-bridge-crm/30-zchuyotpro-crm ל-`og:image`/`twitter:image`
    שמצביעים על נכסי Lovable R2 חיצוניים -- לבדוק אם הם עדיין נגישים
    בפועל או שבורים (דורש בדיקת רשת, לא רק קריאת קוד).

## 20/08/2026 — סבב 287 (loop B)

287. **פתחתי עדשה חדשה כמו שהוצע בסוף הסבב הקודם: open-redirect/CSRF
    על עשר המערכות החיות (17/18/21/22/24/26/27/28/30/31).** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    30-zchuyotpro-crm rebrand-title, קומיטים `4aa9ade0`/`c79a69e6`, כבר
    ה-parent של הענף הזה, כבר נדחף) ו-`core.issues` פתוחים בהיקף 17-31
    -- כמעט כולם חסומים על המשתמש (allow-list Supabase Auth, RLS
    בפרויקטים לא-נגישים ל-MCP, החלטות מוצר) ולא ניתנים לפעולה מכאן.
    הרצתי סוכן Explore לחיפוש דפוס open-redirect (יעד ניתוב שנבנה
    מקלט משתמש -- query param/state -- בלי לוודא נתיב יחסי/רשימת
    היתר) על כל עשר המערכות. **ממצא אמיתי אחד, ב-27-bkalut-price:**
    `server/google-auth.ts` -- זרימת Google OAuth למנהל שומרת
    `req.query.redirect` גולמי ב-state store (שורה 89-90), ואז
    ב-callback (שורה 172-174) בודקת רק `redirect.startsWith("/")`
    לפני שמזרימה אותו חזרה ל-SPA כ-`?redirect=` ב-hash. אותה בדיקה
    חלקית חוזרת בצד הלקוח, `client/src/pages/admin-login.tsx` שורה
    59. הבדיקה `startsWith("/")` מאשרת URL יחסי-פרוטוקול כמו
    `//evil.com`, שדפדפנים מפרשים כ-host חיצוני מלא כש-`window.location`
    מוקצה לו ישירות. **אימתתי את חומרת הניצול בפועל לפני התיקון:**
    קראתי את `App.tsx` ואישרתי שהראוטר הוא `wouter` עם `useHashLocation`
    (hash-routing) -- כלומר `setLocation(target)` בפועל רק קובע
    `window.location.hash`, לא מבצע ניווט מלא לדומיין חיצוני, כך
    שה-vector הזה **לא בר-ניצול היום** דרך שרשרת ה-SPA הקיימת (בניגוד
    לדוח הראשוני של הסוכן שסימן זאת כ-HIGH ניתן לניצול). עדיין תיקנתי
    באופן פרואקטיבי: אותו דפוס "תוקן לפני שהשער נפתח" כמו סבב 40
    (#245-249) -- אם מישהו יחליף בעתיד את `setLocation` בהקצאת
    `window.location.href` ישירה (למשל כדי לתמוך בניתוב חיצוני אמיתי),
    הבדיקה החלקית תהפוך למנוף ניצול אמיתי בלי אזהרה. **התיקון:**
    הוספתי `&& !target.startsWith("//")` לשתי הבדיקות (שרת+לקוח) --
    שינוי מינימלי, לא נוגע בשום זרימה אחרת. שאר תשע המערכות החיות
    נבדקו ולא נמצא בהן דפוס דומה (יעדי redirect קשיחים או בנויים
    מ-`window.location.origin`/`import.meta.env.BASE_URL` בלבד, לא
    מקלט משתמש). אין `node_modules`/`tsc` בעץ הזה -- אימתתי איזון
    סוגריים/סוגריים-מסולסלים בסקריפט Node קצר (תואם) על שני הקבצים.
    קומיטים על `fix/b-bkalut-price-google-oauth-open-redirect-0820`.
    **הבא בתור:** ה-CSRF חצי מהעדשה עדיין לא נבדק (state param כן
    קיים ב-OAuth הזה כמניעת CSRF, לא נבדק בשאר המערכות); כמו כן
    עדיין פתוחה בדיקת `og:image`/`twitter:image` חיצוניים על 30/31
    (דורשת בדיקת רשת).

## 20/08/2026 — סבב 288 (loop B)

288. **סגרתי את חצי-העדשה שנותר מהסבב הקודם: CSRF על נתיבי POST
    מבוססי-cookie בעשר המערכות החיות (17/18/21/22/24/26/27/28/30/31).**
    קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד
    האחרון: 27-bkalut-price open-redirect, קומיט `6462682c`, כבר
    ה-parent של הענף הזה, כבר נדחף) ו-`core.issues` פתוחים בהיקף
    17-31 -- כמעט כולם חסומים על המשתמש (#248-#251 מחכים לפריסת
    מיגרציה על 30 דרך דשבורד/Lovable, לא נגישים ל-MCP; #209/#245 גם
    חסומים באותו אופן) ולא ניתנים לפעולה מכאן. הרצתי סוכן Explore
    לסריקת CSRF -- state param לא-מאומת ב-OAuth, נתיבי POST
    מבוססי-session-cookie בלי אימות CSRF, ופעולות state-changing
    דרך GET -- על עשר המערכות. **תוצאה: רק שתי מערכות (17-chizukim-
    transcribe, 28-kupot-health-funds) יש להן session-cookie בכלל**
    (שאר השמונה הן Supabase-JWT/bearer-token בלבד, לא רגישות ל-CSRF
    קלאסי כי דפדפנים לא מצרפים Authorization header חוצה-אתרים).
    הדוח הראשוני של הסוכן סימן `/api/admin/logout` בשתיהן כ"CRITICAL"
    -- **אימתתי בעצמי לפני כל פעולה** (`server/auth.ts` בשתיהן):
    שתי העוגיות כבר מוגדרות `sameSite: "strict"` (17 שורה 61, 28 שורה
    55, גם `httpOnly: true`). עוגיית `sameSite=strict` **לא נשלחת
    כלל** בבקשה חוצה-אתרים (גם לא ב-`fetch`/`<img>`/טופס) בדפדפנים
    מודרניים -- כלומר וקטור ה-CSRF שדווח כבר סגור לגמרי ברמת
    הדפדפן, אין כאן חור אמיתי. **לא בוצע שינוי קוד על הממצא הזה** --
    הוספת טוקן CSRF מפורש הייתה שכבת-הגנה כפולה על מה שכבר מוגן,
    לא תיקון של פרצה אמיתית (בניגוד לדפוס "תוקן לפני שהשער נפתח"
    שהופעל בסבב 287, כאן אין אפילו שער חלקי-פתוח). עדשת ה-CSRF
    נסגרת כממוצתה על פני 17-31. **פניתי לעדשה חדשה: נגישות
    (accessibility) על אלמנטים אינטראקטיביים ציבוריים** -- כפתורים
    עם אייקון-בלבד בלי טקסט גלוי וגם בלי `aria-label`, ש-screen
    reader יכריז עליהם כ"button" סתמי בלי מטרה. הסוכן מצא דפוס
    שיטתי: כפתורי סגירה/הסרה עם אייקון `X` (lucide) בלי `aria-label`
    ב-4 מערכות (22-get-your-rights: 3 מופעים, 21-mthbram: 2,
    30-zchuyotpro-crm: 2, 26-modaot-studio: 1) -- ואילו שאר שש
    המערכות (17/18/24/27/28/31) נקיות מהדפוס. **אימתתי את שלושת
    המופעים ב-22-get-your-rights בעצמי** (הכי הרבה מופעים, וזה
    הווידג'ט הציבורי המרכזי -- סוכן AI לצ'אט + העלאת קבצים בזרימת
    מיצוי הזכויות): `AIAgent.tsx` שורה 160 (כפתור סגירת הצ'אט),
    `FloatingBot.tsx` שורה 527 (כפתור סגירת הבוט הצף), `FileUpload.tsx`
    שורה 60 (כפתור הסרת קובץ) -- שלושתם `<button onClick=...><X .../>
    </button>` בלי טקסט ובלי `aria-label`. **התיקון:** הוספתי
    `aria-label="סגור"` לשני כפתורי הסגירה ו-`aria-label="הסר קובץ"`
    לכפתור ההסרה -- שינוי מינימלי של שורה אחת בכל קובץ, לא נוגע
    בלוגיקה/סטיילינג. גרפתי מחדש את כל האפליקציה אחרי התיקון לוודא
    שלא נשאר עוד כפתור-`X` בלי `aria-label` -- אפס תוצאות נוספות.
    אין `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/סוגריים-
    מסולסלים בסקריפט Node קצר (תואם) על שלושת הקבצים. קומיט
    `a7c112ca` על `fix/b-get-your-rights-close-button-aria-label-0820`
    (יורש `fix/b-bkalut-price-google-oauth-open-redirect-0820`), יידחף.
    **הבא בתור:** אותו דפוס (`X`-icon בלי `aria-label`) נמצא גם ב-21-
    mthbram (2), 30-zchuyotpro-crm (2), 26-modaot-studio (1) -- לא
    תוקן בסבב הזה (סקופ ל"צעד אחד משמעותי" אחד), סבב הבא יכול להמשיך
    שם. עדיין פתוחה גם בדיקת `og:image`/`twitter:image` חיצוניים על
    30/31 (דורשת בדיקת רשת).

## 20/08/2026 — סבב 289 (loop B)

289. **המשכתי את סבב 288: סגרתי את שלושת המערכות שנותרו מהדפוס
    `X`-icon-בלי-`aria-label` (21-mthbram, 30-zchuyotpro-crm,
    26-modaot-studio).** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: 22-get-your-rights, קומיטים
    `a7c112ca`/`321dea7e`, כבר ה-parent של הענף הזה, כבר נדחף).
    **האומדן מסבב 288 (2/2/1) התברר כנמוך מדי** -- הוא הגיע מסריקת
    סוכן חלקית שהתמקדה ב-22; כשקראתי בעצמי כל קובץ ב-21/30/26 עם
    ייבוא `X` מ-`lucide-react`, מצאתי **14 מופעים אמיתיים**, לא 5:
    21-mthbram (8) -- `RabbiPortal.tsx`/`OrgPortal.tsx` שורות
    272/290/364/393 (כפתורי ביטול הוספה/עריכה של שיעור),
    `PrayerTimesTab.tsx` שורות 119/146 (ביטול הוספה/עריכה של בית
    כנסת), `LessonDetailModal.tsx` שורה 122 (סגירת המודל),
    `FloatingChatBot.tsx` שורה 311 (סגירת הצ'אט הצף); 30-zchuyotpro-
    crm (5) -- `HousingTab.tsx` שורה 123 (הסרת נכס נוסף),
    `FinancialTab.tsx` שורות 119/153 (הסרת מקור הכנסה / פוליסת
    ביטוח), `TagsInput.tsx` שורה 24 (הסרת תגית), `messages.tsx`
    שורה 330 (הסרת קובץ מצורף בצ'אט); 26-modaot-studio (1) --
    `BrandingBrief.tsx` שורה 104 (סגירת דף הבריף -- היה לו כבר
    `data-testid="button-close"` אבל לא `aria-label`, כלומר לא
    נגיש לקורא מסך למרות שהיה מזוהה לבדיקות). **אימתתי בנפרד** ש-
    `Navbar.tsx` ב-21-mthbram (כפתור ההמבורגר, גם הוא עם `X`) כבר
    תוקן בסבב קודם -- יש לו `aria-label`/`aria-expanded` דינמיים
    ואפילו הערת-קוד שמסבירה שהממצא נמדד ב-`scripts/qa/platform-
    audit.mjs` -- לא נגעתי בו שוב. גם קראתי את הסקריפט הזה (בדיקת
    נגישות אמיתית שרצה מול more30.com עם Playwright על מכונת
    Windows חיצונית, לא ריצה מפה) כדי לוודא שהמוסכמה תואמת: שם
    נגיש = `aria-label`/`aria-labelledby`/`label`/`title`/טקסט --
    בדיוק מה שהוספתי. **התיקון:** הוספתי `aria-label` בעברית לכל
    אחד מ-14 הכפתורים, תואם למוסכמה מסבב 288 (`"סגור"` לכפתורי
    סגירה, `"ביטול"`/`"ביטול עריכה"` לכפתורי ביטול טופס, `"הסר
    X"` לכפתורי הסרה עם שם הפריט המוסר כשזמין) -- שינוי של שורה
    אחת בכל קובץ, אפס שינוי בלוגיקה/סטיילינג/success-path. אין
    `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/מסולסלים/
    מרובעים בסקריפט Node קצר על עשרת הקבצים (כולם תואמים) בנוסף
    לקריאה חוזרת ידנית של כל ה-diff. הקבצים כבר עקבו אחרי git
    (לא נדרש `git add -f` הפעם, בניגוד לרוב הסבבים הקודמים שנגעו
    ב-`apps/*/client|src`). קומיט `42d0dc44` על `fix/b-icon-only-
    buttons-aria-label-sweep-0820`, נדחף (מפעיל פריסות Vercel תחת
    more30.com/mthbram, more30.com/crm, more30.com/studio).
    **הבא בתור:** דפוס ה-`X`-icon-בלי-`aria-label` נראה כעת ממוצה
    על פני כל 17-31 (10 המערכות החיות + 26/29 נבדקו). סבב הבא יכול
    לפתוח עדשה חדשה שטרם נבדקה בהיקף הזה (למשל: `alt` חסר על
    תמונות דקורטיביות/תוכן, ניגודיות צבעים, או ניווט מקלדת ב-
    modal/dropdown), או לחזור לבדיקת `og:image`/`twitter:image`
    חיצוניים על 30/31 שעדיין פתוחה מסבב 286 (דורשת בדיקת רשת שלא
    בוצעה בסבב הזה).

## 20/08/2026 — סבב 290 (loop B)

290. **פתחתי עדשה חדשה כמו שהוצע בסוף הסבב הקודם: `alt` חסר על
    `<img>` בעשר המערכות החיות (17/18/21/22/24/26/27/28/30/31).**
    קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`
    (הצעד האחרון: 21/26/30 aria-label sweep, קומיטים `42d0dc44`/
    `e18e4d21`, כבר ה-parent של הענף הזה, כבר נדחף) ו-`core.projects`
    17-31 (ללא צורך בשינוי metadata). הרצתי סוכן Explore לסריקת
    `<img>` בלי `alt=` על עשר המערכות. **תוצאה: תשע מערכות נקיות**
    (17/28/30/31 בלי `<img>` בקוד המקור כלל; 18/22/24/27 עם `alt`
    בכל מופע, כולל `alt=""` דקורטיבי מכוון ב-22). **ממצא אמיתי אחד,
    ב-26-modaot-studio:** `client/src/lib/brandBook.ts` -- הפונקציה
    `buildBrandBookHtml()` בונה מחרוזת HTML לספר-מותג (7 סעיפים)
    שנכתבת ישירות לחלון דפדפן אמיתי (`BrandKitPage.tsx` שורה 118:
    `window.open("", "_blank")` + `w.document.write(html)` + `w.print()`
    לשמירה כ-PDF) -- כלומר DOM אמיתי בדפדפן אמיתי, לא markup גנרי,
    ולכן `alt` חסר הוא פרצת נגישות אמיתית (משתמש קורא-מסך שפותח את
    חלון ה-PDF יפגוש תמונות לוגו בלי שם). **אימתתי בעצמי:** 7 מופעי
    `<img>` בקובץ, 2 כבר עם `alt` (שורות 117 "לוגו", 142 "לוגו
    ראשי"), **5 בלי `alt` בכלל** (שורות 144/145 -- לוגו על רקע
    בהיר/כהה בתצוגת "מערכת הלוגו"; 197/209/213 -- לוגו על כרטיס
    ביקור/פוסט לרשתות/נייר מכתבים בסעיף "דוגמאות יישום"). גם נמצא
    מופע שישי בקובץ `.archive/renderer.ts` (`<img class="bg"
    src="${backgroundDataUrl}"/>` בלי `alt`) -- **בדקתי ולא נגעתי:**
    `.archive/` לא מיובא משום מקום בקוד החי (`grep -rn "renderer"`
    על כל האפליקציה חוץ מ-`.archive/` עצמו -- אפס תוצאות), כלומר קוד
    מת שלא רץ אף פעם -- תיקון שם לא היה משנה שום דבר אמיתי ומחוץ
    לעקרון "לא לגעת במה שלא רץ". **התיקון:** הוספתי `alt` תיאורי
    בעברית תואם-הקשר לחמשת המופעים (״לוגו על רקע בהיר/כהה״, ״לוגו
    על כרטיס ביקור/פוסט לרשתות חברתיות/נייר מכתבים״) -- שינוי של
    attribute יחיד בכל שורה, אפס שינוי בלוגיקה/עיצוב/success-path.
    אין `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/מסולסלים/
    מרובעים בסקריפט Node קצר על הקובץ (תואם) בנוסף לקריאה חוזרת של
    כל 7 מופעי ה-`<img>` אחרי התיקון. שימו לב: `apps/26-modaot-studio/
    client` תפוס תחת דפוס `.gitignore` כללי-משנה (כמו רוב אפליקציות
    ה-`apps/*/client`), אז `git add <path-מדויק>` נדרש (לא `git add
    -A`/`.`) -- זהה לדפוס בסבבים קודמים. קומיט `9620e8c2` על
    `fix/b-modaot-studio-brandbook-img-alt-0820` (יורש `fix/b-icon-
    only-buttons-aria-label-sweep-0820`), נדחף (מפעיל פריסת Vercel
    תחת more30.com/studio). **הבא בתור:** עדשת ה-`alt` נראית ממוצה
    כעת על פני 17-31 (מלבד `.archive/` המת, לא רלוונטי). סבב הבא
    יכול לפתוח עדשה חדשה שטרם נבדקה (ניגודיות צבעים, ניווט מקלדת
    ב-modal/dropdown, `lang`/`dir` חסר על אלמנטים מעורבי-שפה), או
    לחזור לבדיקת `og:image`/`twitter:image` חיצוניים על 30/31
    שעדיין פתוחה מסבב 286 (דורשת בדיקת רשת).

## 20/08/2026 — סבב 291 (loop B)

291. **סגרתי את בדיקת `og:image`/`twitter:image` שהייתה פתוחה מסבב
    286, ואז פתחתי עדשת נגישות חדשה: ניווט מקלדת ב-modal/overlay
    מותאם-אישית (Escape לסגירה) על עשר המערכות החיות.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד
    האחרון: 26-modaot-studio brand-book img alt, קומיטים
    `9620e8c2`/`bdc96505`, כבר ה-parent של הענף הזה, כבר נדחף)
    ו-`core.projects` 17-31 (ללא צורך בשינוי metadata). **שלב 1 --
    `og:image`/`twitter:image`:** גרפתי את שני קבצי ה-`__root.tsx`
    (30-zchuyotpro-crm, 31-hebrew-bridge-crm) ובדקתי בפועל עם
    `curl` את שני ה-URL-ים על `pub-...r2.dev` -- שניהם מחזירים
    `HTTP 200`, `image/png` תקין, ואימתתי עם `md5sum` ששתי התמונות
    שונות זו מזו (לא אותה תמונת-placeholder גנרית משוכפלת) --
    כלומר אין כאן קישור שבור, הבדיקה נסגרת כ"לא-ממצא" (התמונות
    כן מגיעות ממתאר Lovable ישן כמצוין בסבב 286, אבל זה תוכן
    שיווקי לא נגיש/שבור, מחוץ להיקף תיקון קוד). **שלב 2 -- ניווט
    מקלדת:** הרצתי סוכן Explore לסריקת modal/overlay מותאמים-אישית
    (לא Radix/shadcn Dialog שכבר מטפלים ב-focus trap/Escape באופן
    מובנה) בלי מטפל מקלדת ל-Escape, על עשר המערכות. הסוכן החזיר 5
    מועמדים; **אימתתי בעצמי** על ידי קריאת הקבצים: `22-get-your-
    rights/src/components/FloatingBot.tsx` (הווידג'ט הצף הראשי,
    `isOpen`/`setIsOpen` בלי שום מאזין מקלדת) ו-`AIAgent.tsx`
    (אותו דפוס בדיוק, וידג'ט צ'אט-AI שני) -- שניהם `fixed`-overlay
    מותאמים-אישית עם `motion.div`+`AnimatePresence`, נסגרים רק
    בלחיצת עכבר על כפתור ה-X (שכבר קיבל `aria-label` בסבב 288),
    בלי מטפל `Escape` בכלל, כלומר משתמש מקלדת-בלבד שנפתח לתוכם
    לא יכול לצאת חזרה בלי לעקוף/לרפרש. שני הקבצים הם הרכיבים
    הציבוריים המרכזיים ביותר במערכת 22 (זרימת מיצוי הזכויות)
    ונבחרו לתיקון בסבב הזה; שלושת המועמדים הנוספים (24-galilee-
    connect-hub: GallerySection lightbox + Navbar drawer,
    27-bkalut-price: public-eligibility modal) לא תוקנו הפעם
    (היקף "צעד אחד משמעותי"), מתועדים כתור לסבב הבא. **התיקון:**
    הוספתי `useEffect` בכל קובץ שמאזין ל-`keydown` על `window`
    כל עוד `isOpen===true` וקורא ל-`setIsOpen(false)` בלחיצת
    `Escape`, עם `cleanup` שמסיר את המאזין -- זהה בצורתו לשני
    הקבצים, אפס שינוי בלוגיקת השליחה/הצ'אט/הצלחה. `FloatingBot.tsx`
    לא ייבא `useEffect` קודם -- הוספתי לייבוא הקיים;
    `AIAgent.tsx` כבר ייבא אותו (בשימוש ל-auto-scroll). אין
    `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/מסולסלים/
    מרובעים בסקריפט Node קצר על שני הקבצים (0/0/0) בנוסף לקריאה
    חוזרת של כל ה-diff. נדרש `git add -f` (`apps/22-get-your-
    rights/src` תפוס תחת `.gitignore` כללי, אותו דפוס חוזר). קומיט
    `2ee5cabf` על `fix/b-get-your-rights-modal-escape-key-0820`
    (יורש `fix/b-modaot-studio-brandbook-img-alt-0820`), יידחף
    (מפעיל פריסת Vercel תחת more30.com/zchuyot או נתיב 22 המקביל).
    **הבא בתור:** אותה עדשה (modal מותאם-אישית בלי Escape) פתוחה
    עדיין על 24-galilee-connect-hub (GallerySection lightbox,
    Navbar mobile drawer) ו-27-bkalut-price (public-eligibility
    modal) -- סבב הבא יכול להמשיך שם, או לפתוח עדשה חדשה (ניגודיות
    צבעים, `lang`/`dir` חסר על אלמנטים מעורבי-שפה).

## 20/08/2026 — סבב 292 (loop B)

292. **המשכתי את עדשת הנגישות מסבב 291: הוספתי מטפל מקלדת ל-Escape
    בשלושת ה-modal/overlay המותאמים-אישית שנותרו פתוחים -- 24-
    galilee-connect-hub (GallerySection lightbox, Navbar mobile
    drawer) ו-27-bkalut-price (public-eligibility exact-state
    modal).** קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`
    (הצעד האחרון: 22-get-your-rights modal Escape-key, קומיטים
    `2ee5cabf`/`814b0bfe`, כבר ה-parent של הענף הזה, כבר נדחף)
    ו-`core.projects` 17-31 (ללא צורך בשינוי metadata). אימתתי בעצמי
    בקריאת שלושת הקבצים: `GallerySection.tsx` -- `lightbox`
    state (`string | null`) שנפתח בלחיצה על תמונה בגלריה,
    `motion.div` overlay `fixed inset-0`, נסגר רק בלחיצת עכבר על
    הרקע או על כפתור ה-X (`aria-label` לא נדרש כאן כי אין טקסט/
    שם -- לא בסקופ הבדיקה הזו); `Navbar.tsx` -- מגירת מובייל
    (`mobileOpen`), `AnimatePresence`+`motion.div` overlay מלא-מסך,
    נסגרת בלחיצה על הרקע/קישור/כפתור ההמבורגר בלבד; `public-
    eligibility.tsx` (27-bkalut-price, **לא** bkalut-app/bkalot-
    admin המוגנים -- אפליקציה נפרדת) -- `exactStateOpen` state,
    modal חיפוש-לפי-מצב-מדויק (`fixed inset-0`), נסגר בלחיצת רקע/
    כפתור X/בחירת פריט בלבד. בשלושתם: אין שום מטפל `keydown`,
    כלומר משתמש מקלדת-בלבד לכוד עד לחיצת Tab מוצלחת על כפתור סגירה
    לא ממוקד אוטומטית. **בדקתי גם**: `AskRabbiSection`/`FloatingChatBot`
    ורכיבי modal אחרים ב-24/27 -- לא נמצאו modal מותאמים-אישית
    נוספים בלי Escape (24 ו-27 לא נסרקו קודם באופן ממצה בסבב 291,
    רק זוהו כמועמדים; הסריקה השלמה הזו מסיימת את שתי המערכות).
    **התיקון:** אותה תבנית מדויקת מסבב 291 -- `useEffect` שמאזין
    ל-`keydown` על `window` כל עוד ה-state הרלוונטי `true`, קורא
    ל-setter ל-`false`/`null` בלחיצת `Escape`, עם `cleanup`.
    `GallerySection.tsx`/`Navbar.tsx` כבר ייבאו `useEffect`;
    `public-eligibility.tsx` לא -- הוספתי לייבוא הקיים. אפס שינוי
    בלוגיקת חיפוש/סינון/גלריה/ניווט. אין `node_modules`/`tsc`
    בעץ הזה -- אימתתי איזון סוגריים/מסולסלים/מרובעים בסקריפט Node
    קצר על שלושת הקבצים (0/0/0) בנוסף לקריאה חוזרת של כל ה-diff.
    נדרש `git add` רגיל הצליח על אף אזהרת `.gitignore` (הקבצים כבר
    עקבו אחרי git מסבבים קודמים). קומיט על `fix/b-galilee-bkalut-
    modal-escape-key-0820` (יורש `fix/b-get-your-rights-modal-
    escape-key-0820`), יידחף (מפעיל פריסות Vercel תחת נתיבי 24/27
    המקבילים). **הבא בתור:** עדשת ה-Escape-key על modal מותאם-
    אישית נראית כעת ממוצה על פני כל 17-31 שנבדקו (17/18/21/22/24/
    26/27/28/30/31). סבב הבא יכול לפתוח עדשה חדשה (ניגודיות צבעים,
    `lang`/`dir` חסר על אלמנטים מעורבי-שפה, focus trap/focus-return
    ב-modal אחרי סגירה) על פני 17-31.

## 20/08/2026 — סבב 293 (loop B)

293. **פתחתי את עדשת ה-focus-return-אחרי-סגירה שהוצעה בסוף הסבב
    הקודם, ובדרך גיליתי שסבב 291-292 החמיץ שני רכיבים ב-21-mthbram
    שכלל לא קיבלו מטפל Escape.** קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: 24/27 modal Escape-key,
    קומיטים `d8a22e4e`/`2f5dbf77`, כבר ה-parent של הענף הזה, כבר
    נדחף) ו-`core.projects` 17-31 (ללא צורך בשינוי metadata). הרצתי
    סוכן Explore לסריקת כל 17-31 עבור modal/overlay מותאמים-אישית
    (לא Radix/shadcn) בלי החזרת focus לאלמנט המפעיל אחרי סגירה.
    הסוכן מצא 7 רכיבים כאלה ב-4 אפליקציות (21/24/27/28), ודירג
    כבעלי-הערך-הגבוה ביותר שניים ב-**21-mthbram** שלא היו ברשימת
    5 המועמדים של סבב 291 כלל. **אימתתי בעצמי:** `FloatingChatBot.tsx`
    (הווידג'ט הצף הראשי של האפליקציה, `isOpen`/`setIsOpen`,
    `motion.div` overlay `fixed`) ו-`Navbar.tsx` (מגירת ניווט
    מובייל, `isOpen`/`setIsOpen`, `AnimatePresence`+`motion.div`) --
    בקריאה מלאה של שני הקבצים **לא נמצא שום מטפל `keydown` בהם
    כלל** (לא רק חסר focus-return -- Escape עצמו חסר), כלומר סבב
    291's Explore-agent scan (שדיווח 5 מועמדים בלבד) פספס את שתי
    האפליקציה הזו לגמרי -- לא סימן שגיאה בסבב הקודם, אלא שסוכני
    Explore לא תמיד ממצים; שווה לזכור זאת בסבבים הבאים ולא לסמוך
    על "ממוצה" בלי re-scan מדי פעם. **התיקון (בשני הקבצים גם יחד,
    כי אותו root cause):** הוספתי `useRef<HTMLButtonElement>` ללכידת
    כפתור-המפעיל (כפתור ה-AI הצף / כפתור ההמבורגר), פונקציית
    `closeChat`/`closeMenu` שקוראת ל-setter ל-`false` ואז
    `triggerRef.current?.focus()`, ו-`useEffect` שמאזין ל-`keydown`
    על `window` כל עוד ה-state פתוח וקורא לפונקציית הסגירה ב-`Escape`
    (עם `cleanup`) -- מדפוס זהה לסבבים 291-292. כפתור ה-X של הצ'אט
    עודכן לקרוא ל-`closeChat` במקום ל-`setIsOpen(false)` הישיר, כדי
    שגם סגירה בעכבר תחזיר focus. ב-Navbar לא שיניתי את `onClick`
    של קישורי הניווט (`setIsOpen(false)` ישיר) -- קליק על קישור
    מנווט לעמוד חדש בכוונה, אז החזרת focus להמבורגר שם תהיה נסיגה
    לא רצויה, לא שיפור נגישות; רק Escape (יציאה בלי לפעול) מקבל
    focus-return. חמשת המועמדים הנוספים שהסוכן מצא (21-LessonDetailModal,
    24-AskRabbiSection/ServiceRequestForm, 27-public-eligibility,
    28-TopicCard/SmartAdvisor -- אלה האחרונים collapsible ברמת כרטיס
    לא modal אמיתי) לא תוקנו הפעם (היקף "צעד אחד משמעותי"), מתועדים
    כתור לסבב הבא. אפס שינוי בלוגיקת הצ'אט/AI/ניווט/סטרימינג. אין
    `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/מסולסלים/
    מרובעים בסקריפט Node קצר על שני הקבצים (0/0/0) בנוסף לקריאה
    חוזרת מלאה. נדרש `git add -f` (`apps/21-mthbram/src` תפוס תחת
    `.gitignore` כללי, אותו דפוס חוזר מסבבים קודמים). קומיט `3d6e9914`
    על ענף חדש `fix/b-mthbram-modal-focus-return-0820` (יורש
    `fix/b-galilee-bkalut-modal-escape-key-0820`), יידחף (מפעיל
    פריסת Vercel תחת more30.com/mthbram). **הבא בתור:** עדשת ה-
    focus-return פתוחה עדיין על 5 המועמדים הנותרים
    (21-LessonDetailModal, 24-AskRabbiSection, 24-ServiceRequestForm,
    27-public-eligibility, ואולי 28-TopicCard/SmartAdvisor אם
    ייחשבו modal ולא רק כרטיס-הרחבה) -- סבב הבא יכול להמשיך שם, או
    לפתוח עדשה חדשה (ניגודיות צבעים, `lang`/`dir` חסר) על פני 17-31.

## 20/08/2026 — סבב 294 (loop B)

294. **סגרתי את עדשת ה-focus-return מסבב 293: מתוך 5 המועמדים
    שהסוכן דירג, רק שניים התבררו כ-modal אמיתיים הזקוקים לתיקון --
    השאר false positives.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: 21-mthbram FloatingChatBot/
    Navbar focus-return, קומיטים `3d6e9914`/`3a07bac5`, כבר ה-parent
    של הענף הזה, כבר נדחף) ו-`core.projects` 17-31 (ללא צורך בשינוי
    metadata). **אימתתי בעצמי את חמשת המועמדים בקריאה מלאה:**
    `24-AskRabbiSection.tsx` ו-`24-ServiceRequestForm.tsx` --
    אקורדיון פנימי בעמוד (`height`/`opacity` animate, בלי
    `fixed`/backdrop), נפתח וגם נסגר **מאותו כפתור-toggle בדיוק**
    (אין X נפרד/Escape רלוונטי) -- focus כבר לא זז מהכפתור בין
    פתיחה לסגירה, אין בעיה אמיתית. `28-TopicCard.tsx` ו-
    `28-SmartAdvisor.tsx` -- אותו דפוס בדיוק (accordion פנימי,
    כפתור-toggle יחיד). ארבעתם **לא** modal/overlay אמיתי -- סוכן
    ה-Explore מסבב 293 סיווג אותם לא נכון כמועמדים; מתועד כאן כדי
    שסבב עתידי לא ינסה שוב. **`21-LessonDetailModal.tsx`** התברר
    כאמיתי וגרוע מהצפוי: לא רק חסר focus-return -- **חסר גם Escape
    לגמרי** (נפתח/נסגר על ידי 3 הורים שונים דרך
    `{selected && <LessonDetailModal .../>}`: `FindLesson.tsx`,
    `FeaturedLessons.tsx`, `LessonsDashboard.tsx`, ועוד מוזכר ב-
    `PublicOrgPage.tsx`/`AdminDashboard.tsx`/`PublicRabbiPage.tsx`/
    `LessonDirectory.tsx`/`UpcomingLessonsCarousel.tsx`). **התיקון:**
    במקום לגעת בכל הורה בנפרד, הוספתי את הלוגיקה **בתוך הרכיב
    עצמו** -- `useRef<HTMLElement | null>` שלוכד את `document.
    activeElement` ב-`useEffect` עם `[]` (רץ פעם אחת ב-mount, מדויק
    כי ההורים ממששים/מבטלים mount של הרכיב על בסיס state, לא
    מחליפים prop על רכיב קבוע), מאזין ל-`keydown`/`Escape` שקורא
    ל-`onClose` דרך `useRef` (כדי לא ליצור תלות לא-יציבה ב-callback
    inline מההורה), ומחזיר focus לאלמנט הלכוד ב-cleanup (שרץ גם
    ב-unmount וגם ב-Escape/backdrop-click/X-click, כי כולם עוברים
    דרך אותו `onClose`). מכסה בבת אחת את כל 8 קריאות ה-import.
    **`27-public-eligibility.tsx`** -- ה-Escape כבר היה קיים מסבב
    292; הוספתי רק `exactStateTriggerRef` שלוכד `document.
    activeElement` כשה-modal נפתח ומחזיר אליו focus ב-cleanup
    (מכסה Escape + לחיצת רקע + כפתור X + בחירת פריט-מצב, כי כולם
    קוראים ל-`setExactStateOpen(false)` וה-`useEffect` תלוי ב-
    `[exactStateOpen]`). אפס שינוי בלוגיקת שיעורים/חיפוש/סינון.
    אין `node_modules`/`tsc` בעץ הזה -- אימתתי איזון סוגריים/
    מסולסלים/מרובעים בסקריפט Node קצר על שני הקבצים (0/0/0) בנוסף
    לקריאה חוזרת מלאה של שני ה-diff. קומיט על ענף חדש
    `fix/b-focus-return-batch2-0820` (יורש `fix/b-mthbram-modal-
    focus-return-0820`), יידחף (מפעיל פריסות Vercel תחת נתיבי
    21/27 המקבילים). **הבא בתור:** עדשת ה-focus-return/Escape על
    modal מותאם-אישית נראית כעת ממוצה על פני 17-31 (הבדלה בין
    modal אמיתי לבין accordion פנימי תועדה כאן למניעת false-positive
    חוזר). סבב הבא יכול לפתוח עדשה חדשה (ניגודיות צבעים, `lang`/
    `dir` חסר על אלמנטים מעורבי-שפה, `aria-expanded` חסר על כפתורי
    accordion שזוהו בסבב הזה) על פני 17-31.

## 20/08/2026 — סבב 295 (loop B)

295. **סגרתי את ההצעה שהושארה בסוף סבב 294: הוספת `aria-expanded`
    לארבעת כפתורי ה-accordion שזוהו שם כ-false-positive לעדשת
    ה-focus-return (לא modal, אבל עדיין חסרי `aria-expanded` -- שני
    ממצאים שונים על אותם רכיבים).** קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: focus-return-batch2,
    קומיט `94e8b324`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` 17-31 (ללא צורך בשינוי metadata). **אימתתי
    בעצמי בקריאה מלאה של ארבעת הקבצים** שסבב 294 כבר קרא לצורך עדשה
    אחרת: `24-AskRabbiSection.tsx` (כפתור `motion.button` שורה 57),
    `24-ServiceRequestForm.tsx` (כפתור compact-toggle שורה 63),
    `28-TopicCard.tsx` (כפתור `button-toggle-*` שורה 45),
    `28-SmartAdvisor.tsx` (כפתור `button-advisor-open-*` שורה 52) --
    כל ארבעתם שולטים בתוכן שנפתח/נסגר (`AnimatePresence`+
    `height`/`opacity` animate) בלי `aria-expanded` על הכפתור, כלומר
    קורא מסך לא יכול לדעת אם הפריט פתוח או סגור. אימתתי גם שהתבנית
    כבר קיימת בהיקף הזה (לא המצאה): `24-Navbar.tsx`/`21-Navbar.tsx`
    (`aria-expanded={mobileOpen}`) ו-`27-bkalut-price/public-health-
    funds.tsx` (3 מופעים) כבר משתמשים באותו דפוס בדיוק. **התיקון:**
    הוספתי `aria-expanded={<state>}` לכל אחד מארבעת הכפתורים, בהתאמה
    ל-state הבוליאני הקיים כבר בכל רכיב (`isOpen`/`open`) -- שורה
    אחת בכל קובץ, אין שינוי ל-`onClick`/ללוגיקה/לעיצוב. לא הוספתי
    `aria-controls` (התוכן הנפתח חסר `id` יציב בכל ארבעת הרכיבים,
    והמצאת `id` חדש רק בשביל `aria-controls` הייתה חורגת מהיקף
    "צעד אחד ממוקד" -- מתועד כאן כהמשך אפשרי). אפס שינוי ללוגיקת
    הטפסים/ה-query/ה-API. אין `node_modules`/`tsc` בעץ הזה -- אימתתי
    איזון סוגריים/מסולסלים/מרובעים בסקריפט Node קצר על ארבעת הקבצים
    (0/0/0) בנוסף לקריאה חוזרת מלאה של ה-diff (4 שורות בלבד, שורה
    אחת לקובץ). לא נדרש `git add -f` הפעם -- `apps/24-galilee-
    connect-hub/src` ו-`apps/28-kupot-health-funds/client/src` לא
    הוחרגו ב-`.gitignore` (בניגוד לתקדים החוזר ב-21/27/30). קומיט על
    ענף חדש `fix/b-accordion-aria-expanded-0820` (יורש
    `fix/b-focus-return-batch2-0820`), יידחף (מפעיל פריסות Vercel
    תחת `more30.com/galil` ו-`more30.com/kupot`). **הבא בתור:**
    אפשר להוסיף `id` יציב + `aria-controls` לארבעת הרכיבים האלה
    (השלמת התיקון), או לפתוח את שתי העדשות הנוספות שהוצעו בסוף סבב
    294: ניגודיות צבעים, ו-`lang`/`dir` חסר על אלמנטים מעורבי-שפה
    (למשל טקסט אנגלי/מספרים בתוך פסקה עברית) על פני 17-31.

## 20/08/2026 — סבב 296 (loop B)

296. **השלמתי את ההצעה שהושארה בסוף סבב 295: הוספת `id` יציב +
    `aria-controls` לשלושה מתוך ארבעת כפתורי ה-accordion שקיבלו
    `aria-expanded` שם.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: accordion aria-expanded,
    קומיט `4b3d7980`, כבר ה-parent של הענף הזה, כבר נדחף) ו-
    `core.projects` 17-31 (ללא צורך בשינוי metadata). **אימתתי
    בעצמי לפני כל שינוי אם יש סיכון לכפילות `id`:** `24-
    ServiceRequestForm.tsx` (מצב compact) מוצג בתוך `.map()` על-פני
    כל שירותי בית-הכנסת ב-`Index.tsx:129` — כמה מופעים באותו עמוד
    בו-זמנית, אז `id` קבוע היה יוצר כפילות תקנית לא-חוקית ב-DOM.
    `28-TopicCard.tsx` גם הוא ב-`.map()` (`home.tsx:436`) אבל כבר
    יש `topic.id` ייחודי בשימוש בכל ה-`data-testid` הקיימים באותו
    קובץ — נעזרתי באותו מזהה קיים (`topic-panel-${topic.id}`) במקום
    להמציא מנגנון חדש. `24-AskRabbiSection.tsx` הוא singleton (מופע
    יחיד ב-`Index.tsx:186`) אבל בכל זאת השתמשתי ב-`useId()` מטעמי
    עקביות ולבטח מפני שינוי עתידי. `React.useId()` כבר תבנית
    מבוססת בקוד הזה (`components/ui/form.tsx`/`chart.tsx` בשני
    האפליקציות, shadcn) — לא המצאה, שימוש חוזר בדפוס קיים. **לא
    תוקן `28-SmartAdvisor.tsx`:** בדקתי את מבנה הרכיב וגיליתי שהוא
    לא accordion אמיתי — הכפתור עצמו **נעלם לגמרי** (מוחלף ב-`if/
    else`) כשה-panel נפתח, כך ש-`aria-controls` היה מצביע על אלמנט
    שלא קיים בו-זמנית עם הכפתור שמצביע עליו (הכפתור כבר לא ב-DOM
    כש-panel פתוח) — הוספת `aria-controls` כאן הייתה ARIA שגוי, לא
    תיקון; ה-`aria-expanded` שכבר נוסף לו בסבב 295 גבולי מאותה
    סיבה אך לא הוחזר (מחוץ להיקף "צעד אחד ממוקד", ותיעוד כאן
    מספיק כדי שסבב עתידי לא ינסה שוב "להשלים" את זה). **התיקון:**
    שלושה שינויים ממוקדים — `useId()` + `aria-controls` על הכפתור
    + `id` על ה-panel, בשני הקבצים ב-24; `aria-controls` מבוסס
    `topic.id` על הכפתור + `id` תואם על ה-panel, ב-28-TopicCard.
    אפס שינוי ללוגיקה/סטיילינג/state קיים. אין `node_modules`/`tsc`
    בעץ הזה — אימתתי איזון סוגריים/מסולסלים/מרובעים בסקריפט Node
    קצר על שלושת הקבצים (0/0/0) בנוסף לקריאה חוזרת מלאה של ה-diff
    (10 שורות). לא נדרש `git add -f` הפעם. קומיט על ענף חדש
    `fix/b-accordion-aria-controls-0820` (יורש `fix/b-accordion-
    aria-expanded-0820`), יידחף (מפעיל פריסות Vercel תחת
    `more30.com/galil` ו-`more30.com/kupot`). **הבא בתור:** עדשת
    ה-accordion/aria נראית כעת ממוצה על פני 17-31 (כולל ה-edge-
    case המתועד כאן: כפתור-שנעלם אינו accordion לצורך `aria-
    controls`). סבב הבא צריך לפתוח עדשה חדשה שטרם נבדקה — ניגודיות
    צבעים, `lang`/`dir` חסר על אלמנטים מעורבי-שפה (למשל טקסט
    אנגלי/מספרים בתוך פסקה עברית), או chunk חדש: בדיקת migrations
    שלא הוחלו (מומלץ כבר בסבב-אחות של loop A).

## 20/08/2026 — סבב 297 (loop B)

297. **פתחתי את עדשת ה-`lang`/`dir` שהוצעה בסוף סבב 296: איתרתי
    ותיקנתי bidi-scramble אמיתי ב-`21-mthbram/PublicRabbiPage.tsx`
    — טלפון/וואטסאפ/אימייל/פקס מוצגים בלי `dir="ltr"`.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד
    האחרון: accordion aria-controls, קומיט `e3cc1c8d`, כבר ה-parent
    של הענף הזה, כבר נדחף) ו-`core.projects` 17-31 (ללא צורך בשינוי
    metadata). הרצתי סוכן Explore על פני כל 17-31 לאתר טקסט
    אנגלי/ספרות מוטמע בהקשר עברי בלי עטיפת `dir`/`lang` — המועמד
    החד-משמעי ביותר: **`21-mthbram`** מכיל שני רכיבי "עמוד ציבורי"
    מקבילים, `PublicSynagoguePage.tsx` ו-`PublicRabbiPage.tsx`,
    שניהם מציגים את אותו בלוק "פרטי התקשרות" (טלפון/וואטסאפ/
    אימייל/פקס) בתוך עמוד RTL מלא. **אימתתי בעצמי בקריאה מלאה של
    שני הקבצים:** ב-`PublicSynagoguePage.tsx` (שורות 182/191/200)
    כל שדה עטוף כראוי ב-`dir="ltr"` על ה-`<p>` המציג את הערך —
    התבנית כבר קיימת ועובדת שם. לעומת זאת ב-`PublicRabbiPage.tsx`
    (שורות 224/233/242/251) **אותם ארבעת שדות בדיוק חסרי `dir`**
    לגמרי — כלומר מספר טלפון/פקס או כתובת אימייל שיוצג שם ירונדר
    עם ספרות/אותיות לטיניות בכיוון-קריאה שגוי בתוך ההקשר העברי
    הסובב (bidi visual scramble), הבעיה הקלאסית של ספרות בתוך RTL.
    זה לא ניחוש — זו התבנית המדויקת שכבר מיושמת ברכיב האח, רק
    שנשכחה כשה-component השני (rabbi) נכתב בנפרד מה-component
    הראשון (synagogue). **התיקון:** הוספתי `dir="ltr"` לארבעת
    ה-`<p>` המציגים `portal.contact_phone`/`contact_whatsapp`/
    `contact_email`/`contact_fax` ב-`PublicRabbiPage.tsx` — זהה
    בדיוק לתבנית הקיימת כבר ב-`PublicSynagoguePage.tsx`. לא נגעתי
    בכתובת (`contact_address`/`contact_mailing_address`) — אלה
    טקסט עברי חופשי, לא ספרות/רצף לטיני, ואין להם `dir="ltr"` גם
    ברכיב-האח (עקביות, לא פספוס). אפס שינוי ללוגיקה/state/עיצוב
    מעבר לארבע תוספות ה-attribute. אין `node_modules`/`tsc` בעץ
    הזה — אימתתי איזון סוגריים/מסולסלים/מרובעים בסקריפט Node קצר
    על הקובץ (0/0/0) בנוסף לקריאה חוזרת מלאה של ה-diff (4 שורות).
    נדרש `git add -f` (`apps/21-mthbram/src` עדיין תפוס תחת
    `.gitignore` כללי — אותו תקדים חוזר מסבבים 291-294). קומיט
    `0a4241cb` על ענף חדש `fix/b-lang-dir-mixed-0820` (יורש
    `fix/b-accordion-aria-controls-0820`), יידחף (מפעיל פריסת
    Vercel תחת `more30.com/mthbram`). **הבא בתור:** עדשת ה-`lang`/
    `dir` עדיין פתוחה על שאר 17-31 — סוכן ה-Explore זיהה גם שני
    מועמדים חלשים יותר (מחרוזת "Google" מוטמעת בכפתורי OAuth
    ב-21-AdminLogin.tsx וב-31-auth.tsx, ו-email מוטמע במשפט עברי
    ב-22-AuthReset.tsx/31-reset-password.tsx) — לא תוקנו הפעם כי
    אותיות אנגלית בודדות/emails בתוך משפט הן bidi-risk נמוך בהרבה
    מספרות טלפון (המחרוזת "Google" לא מתהפכת חזותית כמו ספרות),
    מתועד כתור לסבב הבא אם ירצה להרחיב את העדשה. סבב הבא יכול גם
    לפתוח עדשה חדשה: ניגודיות צבעים, או migrations שלא הוחלו.

## 20/08/2026 — סבב 298 (loop B)

298. **המשכתי את עדשת ה-`lang`/`dir` שנפתחה בסבב 297: מצאתי ותיקנתי
    אותה בדיוק תבנית ה-bug ב-`21-mthbram/PublicOrgPage.tsx`** —
    רכיב "עמוד ציבורי" שלישי (לצד `PublicSynagoguePage.tsx` ו-
    `PublicRabbiPage.tsx`, ששניהם כבר תוקנו) עם אותו בלוק "פרטי
    התקשרות" בדיוק. קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: lang/dir fix ב-
    PublicRabbiPage, קומיט `0a4241cb`, כבר ה-parent של הענף הזה,
    כבר נדחף) ו-`core.projects` 17-31 (ללא צורך בשינוי metadata).
    הרצתי סוכן Explore על פני 17/18/21/22/24/26/27/28/30/31 (17-31
    לא כולל 19/20/23/25/29 שלא נבנו — אומת עם `ls`) לאתר מופעים
    נוספים של אותה תבנית bug (טלפון/וואטסאפ/אימייל/פקס בלי
    `dir="ltr"` בהקשר RTL מלא), לא כולל שני הקבצים שכבר תוקנו.
    הסוכן החזיר 5 מועמדים; **אימתתי בעצמי** את המועמד הראשון
    והברור ביותר לפני תיקון: `PublicOrgPage.tsx` (אותה אפליקציה,
    21-mthbram) — רכיב "עמוד ציבורי" שלישי לצד synagogue/rabbi,
    מציג בדיוק אותו בלוק "פרטי התקשרות" (שורות 214-249) עם אותם
    4 שדות בדיוק (`contact_phone`/`contact_whatsapp`/
    `contact_email`/`contact_fax`), אותו מבנה JSX מדויק (`<p
    className="font-body text-sm text-white font-bold">`), רק
    בלי `dir="ltr"` — בדיוק אותה שכחה כמו ב-PublicRabbiPage לפני
    התיקון בסבב 297. אימות ב-`grep` הראה 0 מופעים קיימים של
    `dir="ltr"` בקובץ לפני התיקון. **התיקון:** הוספתי `dir="ltr"`
    לארבעת ה-`<p>` המציגים את הערכים, זהה בדיוק לתבנית שכבר
    יושמה ב-2 הקבצים האחים באותה אפליקציה. לא נגעתי בכתובת
    (`contact_address`) — עקבי עם הקבצים האחים, טקסט עברי חופשי
    לא ספרות. אפס שינוי ללוגיקה/state/עיצוב מעבר לארבע תוספות
    ה-attribute. אין `node_modules`/`tsc` בעץ הזה — אימתתי איזון
    סוגריים/מסולסלים/מרובעים בסקריפט Node קצר על הקובץ (0/0/0)
    בנוסף לקריאה חוזרת מלאה של ה-diff (4 שורות). הקובץ כבר
    tracked (לא נדרש `git add -f` הפעם). **הבא בתור:** הסוכן
    מצא עוד 4 מועמדים לא-מאומתים אישית בסבב זה (מדורגים לפי
    ביטחון): `30-zchuyotpro-crm/messages.tsx:298` (phone+email
    בטבלת הודעות), `27-bkalut-price/financial.tsx:322` (טלפון
    לקוח), `31-hebrew-bridge-crm/admin/content.tsx:473` (טלפון
    professional בטבלה), ו-`24-galilee-connect-hub/
    ReligiousInfoPage.tsx:113` (ביטחון נמוך יותר — dir לא מאומת
    בוודאות ברמת האב). סבב הבא צריך לאמת ולתקן אחד מאלה, או
    לפתוח עדשה חדשה (ניגודיות צבעים / migrations שלא הוחלו).

## 20/08/2026 — סבב 299 (loop B)

299. **אימתתי ותיקנתי את המועמד הראשון שדורג בסבב 298:
    `30-zchuyotpro-crm/src/routes/_authenticated/messages.tsx:298`
    — שורת טלפון+אימייל של איש-קשר בפאנל צ'אט (CRM אדמין), בלי
    `dir="ltr"`.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: PublicOrgPage lang/dir fix,
    קומיט `4b958215`, כבר ה-HEAD, כבר נדחף) ו-`core.projects`
    17-31 (ללא צורך בשינוי metadata). **אימתתי בעצמי:** האפליקציה
    כולה RTL מלא (`__root.tsx:125` — `<html lang="he" dir="rtl">`),
    והתבנית `dir="ltr"` על טלפון/אימייל כבר מיושמת באותה אפליקציה
    במקומות אחרים — `clients/$id.tsx:65-66`
    (`<span dir="ltr">{client.phone}</span>` /
    `<span dir="ltr">{client.email}</span>`) ו-`clients/index.tsx:110`
    (`<TableCell dir="ltr" ...>{c.phone ?? "—"}</TableCell>`) — כך
    שזה לא ניחוש אלא תבנית קיימת ומאומתת בקוד עצמו. לעומת זאת ב-
    `messages.tsx:298`, `<div className="text-xs text-muted-
    foreground">{contact.phone ?? "—"} · {contact.email ?? "—"}
    </div>` הציג את שני הערכים בלי שום `dir`, מה שגורם ל-bidi
    visual scramble לכל מספר טלפון (וחלק מכתובות אימייל, תלוי
    בתוכן) בתוך ההקשר RTL הסובב. **התיקון:** עטפתי כל ערך בנפרד
    ב-`<span dir="ltr">`, זהה לתבנית הקיימת ב-`clients/$id.tsx`
    (כולל עטיפת ה-fallback `"—"`, עקבי עם `clients/index.tsx`
    שעוטף גם את ה-`?? "—"` בתוך ה-`dir="ltr"`). אפס שינוי ללוגיקה/
    state/עיצוב מעבר לעטיפת ה-span. אין `node_modules`/`tsc` בעץ
    הזה — אימתתי איזון סוגריים/מסולסלים/מרובעים בסקריפט Python
    קצר על הקובץ (0/0/0) בנוסף לקריאה חוזרת מלאה של ה-diff (שורה
    אחת). נדרש `git add -f` (`apps/30-zchuyotpro-crm/src` תפוס
    תחת `.gitignore` כללי — אותו תקדים חוזר). קומיט חדש על ענף
    `fix/b-zchuyotpro-crm-messages-lang-dir-0820` (יורש
    `fix/b-mthbram-publicorg-lang-dir-0820`), יידחף (מפעיל פריסת
    Vercel תחת `more30.com/crm`). **הבא בתור:** שני מועמדים
    שנותרו מתור סבב 298, טרם אומתו אישית: `27-bkalut-price/
    financial.tsx:322` (טלפון לקוח) ו-`31-hebrew-bridge-crm/
    admin/content.tsx:473` (טלפון professional בטבלה) — סבב הבא
    צריך לאמת ולתקן אחד מהם, או ל"סגור" את עדשת ה-lang/dir אם
    שניהם מתבררים כ-false-positive ולפתוח עדשה חדשה (ניגודיות
    צבעים / migrations שלא הוחלו). המועמד הרביעי מסבב 298
    (`24-galilee-connect-hub/ReligiousInfoPage.tsx:113`) כבר סומן
    ביטחון-נמוך ולא בראש התור.

## 20/08/2026 — סבב 300 (loop B)

300. **אימתתי ותיקנתי את המועמד הראשון שנותר מתור סבב 298/299:
    `27-bkalut-price/client/src/pages/financial.tsx:322` — טלפון
    לקוח בטבלת "לקוחות במערכת" (טאב Financial בממשק הניהול), בלי
    `dir="ltr"`.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: zchuyotpro-crm messages.tsx
    lang/dir fix, קומיט `898d746c`, כבר ה-HEAD, כבר נדחף) ו-
    `core.projects` 17-31 (ללא צורך בשינוי metadata). ווידאתי ש-
    `27-bkalut-price` אינו `bkalut-app`/`bkalot-admin` המוגנים
    (CONNECTIONS.md מגדיר את הרשימה המוגנת במפורש — 27 אינו בה).
    **אימתתי בעצמי, ובאופן חזק יותר מהרגיל:** לא רק שהאפליקציה
    כולה RTL מלא (`client/index.html:2` —
    `<html lang="he" dir="rtl">`, ועשרות `dir="rtl"` בכל הדפים),
    אלא שהתבנית `<span dir="ltr">{phone}</span>` כבר מיושמת
    **באותו קובץ עצמו** בשתי שורות אחרות — `financial.tsx:627`
    (`{c.email || "ללא מייל"} · <span dir="ltr">{c.phone ||
    "—"}</span> · ...`) ו-`financial.tsx:692` (`{l.fullName} ·
    <span dir="ltr">{l.phone}</span>`) — כלומר זו לא רק תבנית-
    אחות מקובץ אחר אלא שכחה בודדת בתוך קובץ שבו המפתחים כבר
    פתרו בדיוק את אותה בעיה פעמיים. שורה 322 (`<p className="text
    -xs text-muted-foreground">{c.phone} · {c.email || "—"} ·
    {c.city || ""}</p>`) הייתה היחידה שלא עטפה את הטלפון. שמתי
    לב שהתבנית המקומית הקיימת (627/692) עוטפת רק את הטלפון,
    לא את האימייל — עקבתי אחריה במקום אחרי התבנית משלב 30-
    zchuyotpro (שעטפה גם אימייל), כדי להישאר עקבי עם קונבנציית
    הקובץ הספציפי הזה. **התיקון:** עטפתי את `{c.phone}` ב-`<span
    dir="ltr">`, זהה לשתי השורות האחרות באותו קובץ; לא נגעתי
    ב-email/city (טקסט/כתובת חופשיים, עקבי עם התבנית הקיימת).
    שינוי של שורה בודדת. אין `node_modules`/`tsc` בעץ הזה —
    אימתתי איזון סוגריים/מסולסלים/מרובעים בסקריפט Python קצר על
    הקובץ המלא (442/442, 513/513, 72/72) בנוסף לקריאה חוזרת מלאה
    של ה-diff (שורה אחת). נדרש `git add -f` (`apps/27-bkalut-
    price/client` תפוס תחת `.gitignore` כללי — אותו תקדים חוזר,
    אך הקובץ כבר tracked אז git הוסיף אותו בלי `-f` בפועל).
    קומיט `973df6da` על ענף חדש `fix/b-bkalut-price-financial-
    lang-dir-0820` (יורש `fix/b-zchuyotpro-crm-messages-lang-dir-
    0820`), נדחף (מפעיל פריסת Vercel תחת `more30.com/bekalut`
    או נתיב מקביל). **הערה חשובה:** קראתי גם את
    `apps/27-bkalut-price/CLAUDE.md` (קובץ הוראות ייעודי לפרויקט
    זה) — מזהיר מפני שינוי `isAdminSubdomain()`/`getPublicOrigin()`
    /middleware חסימת `/api/admin`; התיקון הנוכחי לא נגע באף אחד
    מהם (רק JSX תצוגה בדף client-facing בממשק ניהול פנימי), אז
    אין קונפליקט, אך מתועד כאן כי סבבים עתידיים שנוגעים ב-27
    צריכים לקרוא את אותו קובץ CLAUDE.md לפני שינוי. **הבא בתור:**
    המועמד האחרון שנותר מתור סבב 298 —
    `31-hebrew-bridge-crm/src/routes/_authenticated/admin/
    content.tsx:473` (טלפון professional בטבלה, `(p.contact_info
    as any)?.phone`) — קראתי אותו בסבב הזה (שורות 450-499) ונראה
    מועמד תקף (אין `dir` על התא), אך טרם אומת מול lang/dir כלל-
    האפליקציה של 31 ולא תוקן; סבב הבא צריך לאמת (בדיקת
    `dir="rtl"` ברמת ה-root/App של 31 + חיפוש תבנית `dir="ltr"`
    קיימת באפליקציה) ולתקן, או ל"סגור" את עדשת ה-lang/dir לגמרי
    ולפתוח עדשה חדשה (ניגודיות צבעים / migrations שלא הוחלו /
    בדיקת RLS על טבלאות נוספות מעבר למה שכבר תועד בסבב 296-ish).

## 20/08/2026 — סבב 301 (loop B)

301. **אימתתי ותיקנתי את המועמד האחרון שנותר מתור סבב 298:
    `31-hebrew-bridge-crm/src/routes/_authenticated/admin/
    content.tsx:473` (טלפון professional בטבלת "אנשי מקצוע"),
    ותוך כדי אימות מצאתי מופע נוסף וגבוה-תעבורה יותר של אותה
    תבנית בדיוק: `src/components/admin/ClientsTable.tsx:100`
    (טלפון לקוח בטבלת "לקוחות" הראשית של הניהול — הטבלה
    הראשית ביותר באפליקציה, לא רק טבלת עזר).** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד
    האחרון: 27-bkalut-price financial.tsx lang/dir fix, קומיט
    `973df6da`, כבר ה-HEAD, כבר נדחף) ו-`core.projects` 17-31
    (ללא צורך בשינוי metadata). **אימתתי בעצמי:** `src/routes/
    __root.tsx:120` — `<html lang="he" dir="rtl">` (+ Toaster
    `dir="rtl"` בשורה 155) מאשר RTL מלא לכל האפליקציה. חיפשתי
    תבנית `dir="ltr"` קיימת באפליקציה — קיימת בהקשרי קלט (auth.tsx
    שדות אימייל/סיסמה, reset-password.tsx, integrations.tsx) אך
    **לא** הייתה קיימת עדיין בשום תא טבלה שמציג טלפון — כלומר
    זו לא רק שכחה בקובץ בודד אלא פער שיטתי בכל תצוגות הטלפון
    בטבלאות של האפליקציה הזו (בשונה מ-27/30 שבהם כבר היה תקדים
    מקומי). חיפשתי את כל השימושים ב-`phone` תחת `src/` (4 קבצים:
    `contact.tsx` [טופס קלט, לא רלוונטי], `content.tsx`,
    `ClientsTable.tsx`, `VisibilityRulesGrid.tsx` [לא מציג טלפון
    בפועל, אומת]) ואישרתי ששני התאים (`ClientsTable.tsx:100`,
    `content.tsx:473`) מציגים טלפון גולמי (`c.phone` /
    `(p.contact_info as any)?.phone`) בלי שום `dir`, בתוך תא
    טבלה עם `className="text-right"` (מיושר-ימין, עוד יותר חושף
    ל-scramble כי הספרות "נדבקות" לקצה הימני של העברית הסובבת).
    **התיקון:** הוספתי `dir="ltr"` ישירות על ה-`<TableCell>` (לא
    `<span>` נפרד סביב הערך) בשני המקומות — נשמר `className=
    "text-right"` הקיים (מיקום התא בשורה נשאר ימני, רק זרימת
    הטקסט בתוכו הפכה ל-LTR), עקבי עם הדפוס שתועד בסבב 299
    (`30-zchuyotpro-crm/clients/index.tsx: <TableCell dir="ltr"
    ...>`), לא עם דפוס ה-`<span>` הפנימי שהיה נחוץ ב-27/30 רק כי
    שם התא הציג כמה ערכים יחד עם מפריד (`·`). כאן כל תא מציג ערך
    בודד, אז `dir` ישירות על ה-`TableCell` מספיק ופשוט יותר. לא
    נגעתי בעמודות אימייל/שם הסמוכות (טקסט חופשי, לא ספרות). שני
    שינויים בני שורה אחת בכל קובץ. אין `node_modules`/`tsc` בעץ
    הזה — קראתי מחדש את שני ה-diff-ים המלאים (שורה אחת כל אחד)
    ווידאתי `className`/`dir` שניהם תקינים כ-JSX attributes.
    נדרש `git add -f` (`apps/31-hebrew-bridge-crm/src` תפוס תחת
    `.gitignore` כללי — אותו תקדים חוזר מסבבים 291+). קומיט
    `f91dcfb1` על ענף חדש `fix/b-hebrew-bridge-crm-lang-dir-0820`
    (יורש `fix/b-bkalut-price-financial-lang-dir-0820`), נדחף
    (מפעיל פריסת Vercel תחת `more30.com/gesher`). **זה סוגר את**
    **עדשת ה-lang/dir שנפתחה בסבב 297 על פני כל 17-31** — כל
    המועמדים שדורגו בסבבים 297-300 טופלו (298: PublicOrgPage
    ב-21; 299: messages.tsx ב-30; 300: financial.tsx ב-27; 301:
    שני התאים ב-31). **הבא בתור:** לפתוח עדשה חדשה — מועמדים:
    ניגודיות צבעים (WCAG contrast) על טקסט/רקע, migrations
    שקיימות ב-`supabase/migrations` אך לא הוחלו בפועל, או סריקת
    RLS נוספת על טבלאות שלא נסקרו עדיין מעבר למה שתועד בסבב
    296-ish. אפשר גם לחזור לתחום המחירון (per-system pricing)
    או מיתוג 'עולם הסטארטאפים' אם עדשת הנגישות/bidi ממצה את
    עצמה בסבב הבא.

## 20/08/2026 — סבב 302 (loop B)

302. **תיקנתי פרצת אבטחה קריטית שנפתחה כ-`core.issues #253` ב-LOOP A**
    **סבב 56: `public.admin_sessions` בפרויקט `csjekrvukbdznetsrodj`**
    **(משמש את 27-bkalut-price) הייתה עם 4 מדיניות RLS פתוחות**
    **לגמרי ל-`anon` (`qual=true`/`with_check=true` על SELECT/INSERT/**
    **UPDATE/DELETE) — כל מי שמחזיק את מפתח ה-anon הציבורי של הפרויקט**
    **הזה (שכבר חשוף בקוד לקוח של 06/17 האחים באותו פרויקט, דרך**
    **`VITE_SUPABASE_ANON_KEY`) יכול היה לקרוא כל טוקן session אדמין**
    **קיים, או — חמור בהרבה — להכניס שורה חדשה בעצמו עם `role='admin'`**
    **ולקבל גישת אדמין מלאה בלי שום התחברות אמיתית.** קראתי README.md/
    CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון: 31-hebrew-
    bridge-crm ClientsTable/content.tsx lang/dir fix, קומיטים
    `f91dcfb1`/`8c890ebe`, כבר ה-HEAD) ו-`core.projects` 17-31, ואז
    חיפשתי ב-`core.issues` ממצאים פתוחים בתחום — מצאתי #253 (severity
    critical, status open, owner=agent, app=27-bkalut-price — במפורש
    מיועד ל-Loop B לטפל בו, ל-Loop A אין הרשאה לגעת ב-27 כי הוא בתחום
    17-31). **לפני התיקון וידאתי שהוא בטוח (לא רק "פותר את הבאג"):**
    (1) `grep` על `apps/27-bkalut-price` מאשר ש-`admin_sessions` נקראת/
    נכתבת אך ורק משרת (server/{supabase-storage.ts,storage.ts,
    routes.ts}) — אין שום קוד לקוח/דפדפן שנוגע בה; (2) `pg_roles` על
    `csjekrvukbdznetsrodj` מאשר `service_role.rolbypassrls=true` — מדיניות
    RLS לא משפיעות על service_role בכלל; (3) סדר בחירת המפתח בקוד
    השרת עצמו (`storage.ts`/`routes.ts`) מעדיף `SUPABASE_SERVICE_ROLE_
    KEY` ראשון, ואזהרת ה-production המובנית ב-`/api/admin/db-status`
    אומרת במפורש "For production set SUPABASE_URL + SUPABASE_SERVICE_
    ROLE_KEY"; (4) **האישוש המכריע:** טבלאות אחיות באותה אפליקציה בדיוק
    (`app_users`, `user_sessions`, `premium_requests`, `legal_
    acceptances`, `webhook_log`, `inbound_leads`) כבר היו במצב
    RLS-מופעל-בלי-מדיניות-בכלל (כלומר כבר service_role-only) — `admin_
    sessions` הייתה החריגה היחידה בכל האפליקציה, קרוב לוודאי שאריות
    מפיתוח ולא כוונה. **התיקון:** `apply_migration` בשם
    `admin_sessions_drop_anon_policies` על `csjekrvukbdznetsrodj` —
    מחקתי את 4 מדיניות ה-anon (`admin_sessions_anon_select/insert/
    update/delete`). אימתתי אחרי: `pg_policies` על הטבלה ריק, ו-
    `get_advisors` (security) מציג אותה כעת כ-`rls_enabled_no_policy`
    (INFO) — בדיוק אותו דפוס כמו האחיות שלה. עדכנתי את `core.issues
    #253` ל-`status='fixed'` עם `resolved_at`. **סיכון שיורי ותועד
    במפורש:** היה session פעיל אחד (1 שורה) בטבלה בזמן התיקון; אם
    בניגוד לכל הראיות ה-production בפועל רץ על anon key (לא service
    role), האדמין הזה יזדקק להתחברות מחדש ברגע ש-`SUPABASE_SERVICE_
    ROLE_KEY` יוגדר — וזו ההגדרה המתועדת והנדרשת ל-production ממילא
    לפי קוד האפליקציה עצמו. זה שינוי DB בלבד (`apply_migration`), אין
    קובץ קוד לשנות ב-repo הזה עבור התיקון עצמו; הקומיט הזה מתעד את
    ההחלטה ב-DECISIONS.md. ענף `fix/b-admin-sessions-rls-open-anon-0820`.
    **הבא בתור:** לחזור לעדשות שנפתחו — ניגודיות צבעים (WCAG), migrations
    שלא הוחלו, המשך סריקת RLS על שאר הטבלאות ב-`csjekrvukbdznetsrodj`
    (36 טבלאות, רק חלק נסרק), או חזרה לתחום המחירון/מיתוג.

## 20/08/2026 — סבב 303 (loop B)

303. **פתחתי עדשה חדשה: אזורי גרירה/העלאת-קובץ מותאמים-אישית (`<div onClick>`)**
    **חסרי כל גישת מקלדת — 4 מופעים אמיתיים ב-2 אפליקציות.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    27-bkalut-price admin_sessions RLS fix, קומיט `c0fd1b47`, כבר ה-HEAD)
    ו-`core.projects` 17-31, ואז חיפשתי ב-`core.issues` ממצאים פתוחים
    ל-Loop B (owner=agent) — #62 (galil gabai auth) ו-#115 (mechiron admin
    routing) שניהם תועדו כחסומים על גישה/הכרעה שמחוץ להישג הסשן הזה (PAT
    לא מכסה `mwljkonwdeuaahsigjdp`; #115 תלוי ב-#94/#83). בדקתי גם עדשת
    ניגודיות-צבעים (WCAG) שהוצעה בסבב 302 — `grep -r 'text-gray-(300|400)'`
    בכל 17-31 החזיר אפס תוצאות (האפליקציות משתמשות בטוקני shadcn
    `text-muted-foreground`, כבר עומדים בניגודיות), ומיגרציות — כל 106
    הקבצים תחת `supabase/migrations/` כבר מיושמים בפועל (`list_migrations`
    תואם). **פתחתי עדשה חלופית: חיפוש `<div[^>]*onClick=` בכל 17-31,
    לאתר את אלה שהם המפעיל היחיד לפעולה (בלי `<button>`/`<label>` גיבוי) —
    מצאתי 4:** `21-mthbram/src/pages/RabbiPortal.tsx:165` +
    `OrgPortal.tsx:194` (שני עמודים כמעט-זהים, אזור לוגו לחיץ להחלפה
    כשכבר קיים לוגו — **אימתתי**: זה שונה מהמצב "אין לוגו" שיש לו
    `<Button>` גיבוי בשורה 176/202, כלומר הפער קיים רק אחרי שהרב/הארגון
    כבר העלה לוגו פעם אחת), ו-`24-galilee-connect-hub/src/pages/
    GabaiPortal.tsx:1136` + `:1276` (שני אזורי גרירה להעלאת תמונת
    באנר/תמונת ידע — כאן אין שום גיבוי, גם במצב הריק). בכל ארבעת המקרים
    ה-`<div>` הוא לחיץ (`cursor-pointer`) ומפעיל `inputRef.current
    ?.click()` דרך `onClick` בלבד, בלי `role`, בלי `tabIndex`, בלי
    `onKeyDown` — משתמש מקלדת-בלבד לא יכול להגיע לאלמנט בכלל (לא
    focusable), ולכן לא יכול להחליף לוגו/להעלות תמונה. **התיקון:**
    הוספתי בכל אחד `role="button"` + `tabIndex={0}` + `aria-label`
    תיאורי + `onKeyDown` שמפעיל את אותה פונקציית `click()` בלחיצת
    Enter/Space (עם `preventDefault` למניעת גלילת עמוד בלחיצת Space) —
    ארבעה שינויים זהים בתבנית, שונים רק ב-ref/aria-label. **אימתתי
    שלא נגעתי בדפוס תקין:** `30-zchuyotpro-crm/EntitlementsTab.tsx:165`
    (מועמד חמישי שנמצא באותו grep) נבדק ונפסל — יש בו `<Button
    aria-label="הרחב">` מקונן בלי `onClick` עצמו, אבל לחיצת Enter/Space
    על `<button>` טבעי מייצרת אירוע `click` אמיתי שמבעבע להורה, כך
    שהמקלדת כבר עובדת שם דרך ה-bubbling הרגיל — לא תוקן, אין באג. קראתי
    מחדש את כל ארבעת ה-diff-ים המלאים (`git diff`) ואישרתי JSX תקין
    ושהתבנית הקיימת (`className`, ref, מבנה תנאי) לא נפגעה. נדרש
    `git add -f` (שני עצי המקור תחת `.gitignore` כללי — אותו תקדים
    חוזר). ענף `fix/b-upload-dropzone-keyboard-nav-0820`. **הבא בתור:**
    להמשיך את עדשת ה-onClick-div-בלי-מקלדת על שאר 17-31 (רק 21+24+30
    נבדקו עד כה מתוך כל 15 האפליקציות), או לחזור לתחום המחירון/מיתוג
    'עולם הסטארטאפים' אם העדשה הזאת ממצה את עצמה.

## 20/08/2026 — סבב 304 (loop B)

304. **המשכתי את עדשת ה-onClick-div-בלי-מקלדת (שנפתחה בסבב 303) על שאר**
    **17-31 שטרם נבדקו לעדשה הזו** — קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: upload-dropzone-keyboard-nav, קומיט
    `44616b82`, כבר ה-HEAD) ו-`core.projects` 17-31 (אין שינוי מטא-דאטה),
    ואז `core.issues` הפתוחות בהיקף — כל השורות `owner='agent'` בהיקף
    (#62, #115, #201, #242) עדיין חסומות על אותה גישה/הכרעה חיצונית
    שתועדה בעבר. הרצתי סוכן Explore על 7 האפליקציות עם קוד אמיתי שעדיין
    לא נסרקו לעדשה הזו (17/18/22/26/27/28/31 — 19/20/23/25/29 ללא קוד
    אפליקציה vendored). הסוכן החזיר 8 מועמדים ב-4 אפליקציות; אימתתי כל
    אחד בעצמי לפני נגיעה. **תיקנתי 3 מהם (אותה עדשה, אותה תבנית תיקון
    כמו סבב 303), בחרתי את הקבוצה הזו כי כל שלושתם `<div>`/`Card`
    (=`React.forwardRef<HTMLDivElement>`, אומת בקריאת `ui/card.tsx` בכל
    אפליקציה) עם `onClick` כמפעיל היחיד, בלי שום גיבוי חלופי בענף
    התנאי הרלוונטי:**
    - `17-chizukim-transcribe/client/src/pages/upload.tsx:232` — אזור
      גרירת/בחירת קובץ אודיו להעלאה, `onClick={() => inputRef.current
      ?.click()}` בלי role/tabIndex/onKeyDown.
    - `31-hebrew-bridge-crm/src/components/admin/VisibilityRulesGrid.tsx:34`
      — כרטיס קטגוריית שותפים לחיץ לפתיחת עריכת הרשאות חשיפה, אותו חוסר.
    - `26-modaot-studio/client/src/pages/Home.tsx:281` (כרטיס תבנית בגלריה)
      ו-`:334` (`EntryCard`, שני עמודי כניסה בעמוד הבית) — שני מקומות
      באותו קובץ; ב-`EntryCard` יש `<Button>` פנימי אך **בלי onClick משלו**
      (דקורטיבי בלבד, המקש עדיין לא נגיש), כך שאינו גיבוי אמיתי.
    **התיקון:** `role="button"` + `tabIndex={0}` + `aria-label` תיאורי +
    `onKeyDown` שמפעיל את אותה פונקציה בלחיצת Enter/Space עם
    `preventDefault` (מונע גלילת עמוד ב-Space) — זהה בתבנית לתיקון
    שכבר אושר ונפרס בסבב 303. אפס שינוי לזרימת ה-mouse-click הרגילה בכל
    ארבעת המקומות. **מועמדים שנבדקו ונפסלו (לא תוקנו, לא באג):**
    `22-get-your-rights` (AdminLeads.tsx:321, AdminRightsReference.tsx:486,
    RightsCategories.tsx:234/329) ו-`28-kupot-health-funds`/`27-bkalut-price`
    (הסוכן לא מצא בהם מועמד אמיתי) — כל ארבעת מועמדי 22 הם מסכי אדמין/
    ווידג'ט פנימיים שטרם עברו אימות אישי מלא שלי מול מקורות דומים (בניגוד
    לשלושת שתוקנו, שהאימות שלהם כלל קריאת קובץ `ui/card.tsx` הרלוונטי
    להוכחת `<div>`) — לא תוקנו הפעם כדי לשמור על "צעד אחד משמעותי",
    נשמרים לסבב הבא. קראתי מחדש את כל ה-diff-ים המלאים (`git diff`),
    אימתתי `role`/`tabIndex`/`aria-label`/`onKeyDown` תקינים כ-JSX
    attributes, ושהתבנית הקיימת (`className`, `data-testid`, מבנה תנאי)
    לא נפגעה. שלושת הקבצים כבר עוקבים ב-git (לא נדרש `git add -f` הפעם,
    בניגוד לרוב הסבבים הקודמים). ענף חדש `fix/b-onclick-div-keyboard-nav-
    batch2-0820`. **הבא בתור:** לסיים את עדשת ה-onClick-div על 22-get-
    your-rights (4 מועמדים ממתינים לאימות אישי — AdminLeads/
    AdminRightsReference/RightsCategories×2), או לחזור לתחום המחירון/
    מיתוג 'עולם הסטארטאפים' (אומת בעבר בסבב 285 שהוא כבר תואם בכל 17-31
    החיות, ושאין מפתחות תשלום live קשיחים — לא סביר שיש שם עוד עבודה
    זמינה בלי הכרעת מוצר/גישה חיצונית) אם העדשה הזו ממצה את עצמה.

## 20/08/2026 — סבב 305 (loop B)

305. **סגרתי את עדשת ה-onClick-div-בלי-מקלדת על 22-get-your-rights —**
    **ארבעת המועמדים שנותרו ממתינים לאימות אישי מסבב 304.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    onclick-div-keyboard-nav-batch2, קומיט `88185b8a`, כבר ה-HEAD) ו-
    `core.projects` 17-31 (אין שינוי מטא-דאטה). קראתי בעצמי את כל ארבעת
    המיקומים המדויקים שצוינו בסבב 304: `AdminLeads.tsx:321`,
    `AdminRightsReference.tsx:486`, `RightsCategories.tsx:234` ו-`:329`.
    **שלושה מהם אומתו כבאג אמיתי ותוקנו** (אותה תבנית `role="button"` +
    `tabIndex={0}` + `aria-label` + `onKeyDown` Enter/Space עם
    `preventDefault`, כמו בסבבים 303-304):
    - `AdminLeads.tsx:321` — `<div>` כרטיס פנייה בלוח האדמין, `onClick`
      פותח פרטי הליד, בלי גיבוי.
    - `AdminRightsReference.tsx:486` — `<TableRow>` (אימתתי דרך
      `ui/table.tsx`: מרנדר ל-`<tr>` רגיל, לא focusable) שפותחת פרטי
      זכות; יש בתוכה כפתורי פעולה (הורדה/וואטסאפ/מייל) עם
      `stopPropagation`, אבל הם לא מהווים גיבוי לפעולת "פתח פרטים"
      עצמה.
    - `RightsCategories.tsx:234` — `motion.div` (framer-motion, מרנדר
      ל-`<div>`) שמרחיב/מכווץ קטגוריה; הוספתי גם `aria-expanded` כי זהו
      טריגר toggle, לא רק ניווט.
    **המועמד הרביעי, `RightsCategories.tsx:329`, נבדק ונפסל — זהו
    `motion.button` שכבר מרנדר ל-`<button>` אמיתי**, כך שהוא כבר נגיש
    למקלדת דרך ה-DOM הרגיל; לא נגעתי בו. קראתי מחדש את שלושת ה-diff-ים
    המלאים (`git diff`) ואישרתי JSX תקין, ושהתבנית הקיימת (className,
    מבנה תנאי, `stopPropagation` על כפתורי הפעולה המקוננים) לא נפגעה.
    זהו **הצעד האחרון בעדשת ה-onClick-div-בלי-מקלדת שנפתחה בסבב 303** —
    כל 15 האפליקציות עם קוד vendored (17/18/21/22/24/26/27/28/30/31)
    נסרקו וכל המועמדים האמיתיים תוקנו או נפסלו במפורש. `git add -f`
    נדרש (עץ המקור של 22 תחת `.gitignore` כללי, כמו רוב סבבי 17-31
    הקודמים). ענף `fix/b-get-your-rights-onclick-div-keyboard-nav-0820`.
    **הבא בתור:** לחזור לתחום המחירון/מיתוג 'עולם הסטארטאפים' (אומת
    בעבר בסבב 285 — סביר שדורש הכרעת מוצר/גישה חיצונית לפני עוד עבודת
    קוד זמינה), לפתוח עדשת נגישות חדשה (למשל: alt-text על תמונות
    שנותרו, ניגודיות על אלמנטים שלא נבדקו עדיין, `aria-live` על
    התראות/toast), או להמשיך סריקת RLS על שאר הטבלאות ב-
    `csjekrvukbdznetsrodj` (36 טבלאות, רק חלק נסרק בעבר).

## 20/08/2026 — סבב 306 (loop B)

306. **פתחתי עדשת נגישות חדשה: שדות `<input>`/`<textarea>` בלי שם**
    **נגיש (accessible name).** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: get-your-rights-onclick-div-
    keyboard-nav, קומיט `df05bdf5`, כבר ה-HEAD) ו-`core.projects` 17-31
    (אין שינוי מטא-דאטה), ואז `core.issues` הפתוחות בהיקף — כל שורות
    `owner='agent'` בהיקף (#62, #115, #201, #242) עדיין חסומות על אותה
    גישה/הכרעה חיצונית מתועדת. בדקתי קודם שתי חלופות שהוצעו בסבב 305:
    `aria-live` על toast — נפסלה, כל האפליקציות (17/21/22/24/26/27/28)
    בונות toast על `@radix-ui/react-toast`, שכבר מטמיע live-region
    תקין פנימית (קראתי את `toast.tsx` הבסיסי לוודא); וסריקת RLS נוספת
    על `csjekrvukbdznetsrodj` — `pg_policies` על כל הטבלאות עם roles
    כולל anon חזרה נקייה (fin_* חוסמות anon לגמרי, pc_*/recordings
    הן נתוני-ציבור מכוונים ל-27/17, קופת_leads היא insert-only טופס-ליד
    ציבורי) — אין עוד ממצא כמו admin_sessions בסבב 301, הטבלאות שנותרו
    שם (#245) הן backend לא-vendored וכבר תועדו כחסומות על הכרעת policy.
    **הרצתי סוכן Explore על 10 האפליקציות עם קוד vendored, לאתר
    `<input>`/`<textarea>`/`<select>` טבעיים בלי `label`/`aria-label`/
    `aria-labelledby` (לא shadcn `FormField`, לא Radix — אלה כבר
    נגישים). הסוכן החזיר 5 מועמדים; קראתי כל קובץ בעצמי ואימתתי שכולם
    אמיתיים (8 שדות סה"כ, placeholder בלבד שאינו תחליף ל-accessible
    name לפי WCAG):**
    - `21-mthbram/ContactSection.tsx:194-202` — טופס יצירת קשר ציבורי,
      4 שדות (שם/טלפון/מייל/הודעה), שניים מסומנים חובה (`*`).
    - `18-torah-editor-mvp/app/login/page.tsx:37` — שדה מייל בטופס
      כניסה אמיתי (magic-link Supabase, ללא mock).
    - `26-modaot-studio/ColorPicker.tsx:163` — שדה קוד HEX בבורר צבעים.
    - `21-mthbram/FloatingChatBot.tsx:403` — שדה חיפוש בצ'אטבוט הצף.
    - `24-galilee-connect-hub/ChatBot.tsx:326` — שדה איש-קשר (מייל/
      טלפון לפי `contactMethod`) בטופס פנייה מהיר לרב.
    **התיקון:** `aria-label` תואם לכל placeholder/מטרה קיימים (עבור
    18 נמנעתי מהעברת המרכאה הבעייתית של "דוא\"ל" ובחרתי בניסוח
    "כתובת אימייל" הנקי; עבור 24 ה-`aria-label` דינמי לפי `contactMethod`
    כמו ה-placeholder עצמו). אפס שינוי ל-layout/styling/לוגיקת שליחה.
    קראתי מחדש את כל חמשת ה-diff-ים (`git diff`) ואישרתי JSX תקין. חמשת
    הקבצים כבר עוקבים ב-git (לא נדרש `git add -f`). ענף חדש `fix/b-
    form-input-aria-label-0820`. **הבא בתור:** להמשיך את עדשת ה-
    accessible-name על שאר 17-31 שטרם נסרקו לעדשה הזו (הסוכן סרק
    17/18/21/22/24/26/27/28/30/31 — 19/20/23/25/29 ללא קוד vendored),
    לבדוק צ'קבוקס/סוויץ' מותאמים-אישית (`role`/`aria-checked`) שלא
    נבדקו עדיין, או לחזור לתחום המחירון/מיתוג 'עולם הסטארטאפים' אם
    העדשה הזו ממצה את עצמה.

## 20/08/2026 — סבב 307 (loop B)

307. **המשכתי את עדשת ה-accessible-name לתוך הכיוון שהוצע בסבב 306:**
    **`Switch` (shadcn/Radix) בלי שם נגיש.** קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: form-input-aria-label, קומיט
    `b978ec51`, כבר ה-HEAD) ו-`core.projects` 17-31 (אין שינוי מטא-דאטה),
    ואז `core.issues` הפתוחות בהיקף — כל שורות `owner='agent'` בהיקף (#62,
    #115, #201, #242) עדיין חסומות על אותה גישה/הכרעה חיצונית מתועדת.
    בדקתי גם אם issue #5 (עיצוב ייחודי, §6) פתוח לעבודה השבוע — קראתי את
    `QA/platform/DESIGN_SAMENESS.md` עד סופו: כל שרשור ה-icon-decisions
    נסגר (עדכון 19/08 סבב אוטונומי 4), ומה שנשאר פתוח שם הוא במפורש "הפער
    המבני הרחב... סבב נפרד וגדול משמעותית, לא הבא-בתור חד-צעדי" — לא
    בחרתי בו כצעד אחד. חזרתי להמלצת סבב 306 עצמה: `role`/`aria-checked`
    על Radix `Switch` כבר מוטמעים נכון על ידי הספרייה (אומת ב-
    `ui/switch.tsx` בכל אפליקציה — `@radix-ui/react-switch`, לא רכיב
    ידני) — העדשה האמיתית היא **שם נגיש** (`aria-label`), אותה בעיה
    בדיוק שתוקנה על `<input>`/`<textarea>` בסבב 306, מורחבת ל-`Switch`.
    **הרצתי סוכן Explore על 10 האפליקציות עם קוד vendored לאתר `Switch`
    בלי `aria-label`/`aria-labelledby`/`htmlFor` מקושר. הסוכן החזיר 10
    מועמדים; קראתי כל קובץ בעצמי. תיקנתי את ששת המקרים בדרגת הביטחון
    הגבוהה (שום טקסט מקושר תכנותית בקרבת ה-`Switch` כלל — לא רק `Label`**
    **צמוד ויזואלית בלי `htmlFor`, שהוא ממצא חלש יותר ולא תוקן הסבב הזה):**
    - `27-bkalut-price/community-admin.tsx:338` — הממצא החמור מביניהם:
      `Switch` היה **מקונן בתוך `<Button>`** (`<Button onClick=...><Switch
      checked={l.active} /></Button>`), HTML לא תקין (`<button>` בתוך
      `<button>` — הדפדפן נוהג להוציא/לשבור מבנה כזה) ולא רק בעיית שם
      נגיש. `Switch` עצמו לא נשא `onCheckedChange` כלל — הלחיצה בפועל
      טופלה על ידי ה-`Button` החיצוני. **התיקון:** הסרתי את עטיפת
      `Button`, `Switch` עצמו קיבל את `onCheckedChange={() =>
      toggleLink(l)}` (אותה פונקציה) ו-`aria-label` תיאורי — גם מתקן
      HTML לא תקין וגם נותן שם נגיש, ומשפר בפועל את נגישות המקלדת (לפני
      התיקון הסוויץ' המקונן היה יעד-פוקוס שני שאינו עושה דבר).
    - `27-bkalut-price/potential-admin.tsx:260` — סוויץ' "הפעלה/כיבוי
      הסורק לציבור", טקסט הקשר בעמודה סמוכה (לא מקושר).
    - `27-bkalut-price/potential-admin.tsx:556` — סוויץ' הפעלה/כיבוי
      לכל קישור מותאם ברשימה, `aria-label` דינמי לפי `l.title`.
    - `30-zchuyotpro-crm/entitlements.tsx:275` — עמודת "פעיל" בטבלת
      זכאויות, `aria-label` דינמי לפי `r.title`.
    - `31-hebrew-bridge-crm/ClientTopicsPanel.tsx:60-65` — סימון נושא
      כרלוונטי ללקוח, `aria-label` דינמי לפי `t.name`.
    - `31-hebrew-bridge-crm/ClientProfessionalsPanel.tsx:69-74` — שיוך
      איש מקצוע ללקוח, `aria-label` דינמי לפי `p.name`.
    **לא תוקנו הסבב הזה (ממצאי ביטחון בינוני, נשמרים לסבב הבא אם העדשה**
    **תמשיך):** ארבעה מקרים שבהם יש `Label` טקסטואלי צמוד ב-`flex` (לא
    ריק כמו הרשימה שתוקנה) אך בלי `htmlFor`/`id` מקשר תכנותית —
    `30-zchuyotpro-crm/VehiclesTab.tsx:109-110`, `30-zchuyotpro-crm/
    HousingTab.tsx:93-96`, `26-modaot-studio/Editor.tsx:1781-1785`,
    `21-mthbram/FullAccessRequestsTab.tsx:128`. לא תוקנו כי הממצא חלש
    יותר (רוב קוראי-מסך מודרניים מנחשים קרבה כש-`Label` ו-`control`
    יחידים באותה שורת `flex` צמודה, בניגוד לששת המקרים שתוקנו שבהם אין
    שום טקסט הקשר מקושר בכלל) ולשמירה על "צעד אחד משמעותי" בהיקף אחיד.
    קראתי מחדש את כל ששת ה-diff-ים (`git diff`) ואישרתי JSX תקין ואיזון
    סוגריים (`node -e` על כל קובץ). כל חמשת הקבצים כבר עוקבים ב-git (לא
    נדרש `git add -f`). ענף חדש `fix/b-switch-aria-label-0820`. **הבא
    בתור:** לתקן את ארבעת מועמדי ה-`Label`-צמוד-בלי-`htmlFor` אם העדשה
    הזו נמשכת, לבדוק צ'קבוקס `<input type="checkbox">` גולמי (בשונה
    מ-Switch) לא עטוף ב-`<label>`, או לחזור לתחום המחירון/מיתוג 'עולם
    הסטארטאפים' אם העדשה ממצה את עצמה.

## 20/08/2026 — סבב 308 (loop B)

308. **סגרתי את ארבעת מועמדי ה-`Label`-צמוד-בלי-`htmlFor` שנדחו בסבב**
    **307 (ביטחון בינוני).** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: switch-aria-label, קומיט `14c0d329`,
    כבר ה-HEAD) ו-`core.projects` 17-31 (אין שינוי מטא-דאטה). קראתי את
    כל 20+ השורות `owner='agent'` הפתוחות ב-`core.issues` (לא רק
    בהיקף) — כל אחת מהן חסומה על הכרעה/גישה חיצונית מתועדת: #5 (עיצוב
    ייחודי) מפורש שהנותר הוא "סבב נפרד וגדול משמעותית"; #6 (§10, מעטפת)
    מחוץ להיקף הזה (תשתית משותפת, לא מערכת ממוספרת 17-31); #115 (מסך
    ניהול #27) חסום על #94/#83; #167 (seed-demo-portals ב-15-egod, מחוץ
    להיקף — 01-16) חסום על Lovable שדוחה כל הודעה; #201 חסום על אותו
    #167 (רק 15-egod נשארה ברשימה); #242 (מחשבון זכאות) חסום על גישת
    Lovable/PAT חסרה. אף אחד מהם אינו צעד-קוד זמין הסבב הזה. חזרתי
    להמלצת סבב 307 עצמה: קראתי בעצמי את ארבעת המקומות המדויקים שצוינו —
    `30-zchuyotpro-crm/VehiclesTab.tsx:109-110`,
    `30-zchuyotpro-crm/HousingTab.tsx:93-96` (ומצאתי זוג חמישי-ושישי
    זהה שלא צוין קודם: `HousingTab.tsx:111-112`, "נכסים נוספים"),
    `26-modaot-studio/Editor.tsx:1777-1786`,
    `21-mthbram/FullAccessRequestsTab.tsx:122-129`. **אימתתי שכל אחד
    מהם הוא `Label` עם טקסט תוכן (לא ריק) בשורת `flex` צמודה ל-`Switch`,**
    **בלי `htmlFor`/`id` מקשר, ושה-`Switch`/`Label` בכל שלוש האפליקציות**
    **הם עטיפות `@radix-ui/react-switch`/`@radix-ui/react-label` תקניות**
    **שמעבירות `id`/`htmlFor` כרגיל (לא רכיב מותאם-אישית).** התיקון:
    `id` ייחודי על כל `Switch` ו-`htmlFor` תואם על ה-`Label` הצמוד —
    באפליקציה 21 ה-`id` נבנה דינמית מ-`req.id`+`f.id` כי הסוויצ'ים
    ברשימת `.map()`. אפס שינוי ל-layout/className/לוגיקת `onCheckedChange`.
    קראתי מחדש את כל שישה ה-diff-ים (`git diff`) ואישרתי JSX תקין,
    ואימתתי איזון סוגריים/מאמרות מוסגרות (`node -e`) על כל ארבעת
    הקבצים שהשתנו. הפעם **לא נדרש `git add -f`** — כל ארבעת הקבצים כבר
    עוקבים ב-git (בניגוד לרוב סבבי 17-31 הקודמים, שם עצי המקור בד"כ
    לא במעקב). ענף חדש `fix/b-switch-label-htmlfor-0820`. עם הסבב הזה
    עדשת ה-accessible-name שנפתחה בסבב 306 (`<input>`/`<textarea>` →
    `Switch` בלי שם → `Label`-צמוד-בלי-`htmlFor`) ממצה את עצמה על כל
    האפליקציות שנסרקו. **הבא בתור:** צ'קבוקס `<input type="checkbox">`
    גולמי לא עטוף ב-`<label>` (הכיוון שהוצע בסבב 307 ולא נבדק עדיין),
    סריקת `alt`/`aria-hidden` על תמונות דקורטיביות מול תוכניות, או
    לחזור לתחום המחירון/מיתוג 'עולם הסטארטאפים' אם העדשה הזו ממצה את
    עצמה לגמרי.

## 20/08/2026 — סבב 309 (loop B)

309. **בדקתי קודם את שתי ההצעות של סבב 308 ומצאתי ששתיהן כבר סגורות —**
    **קראתי README.md/CONNECTIONS.md, `core.run_progress` (הצעד האחרון:
    switch-label-htmlfor-0820, קומיט `ee6b3c5a`, כבר ה-HEAD) ו-
    `core.projects` 17-31 (אין שינוי מטא-דאטה).** צ'קבוקס `<input
    type="checkbox">`/shadcn `Checkbox` גולמי: גרפתי את כל 10 האפליקציות
    עם קוד vendored, קראתי כל שימוש (כ-25 מקומות ב-21/22/24/27/30/31) —
    **כולם עטופים ב-`<label>` טבעי** (או `sr-only` בתוך `<label>`), שהוא
    כבר תבנית נגישה תקנית (‏`<button>`/`<input>` הם "labelable elements"
    לפי מפרט ה-HTML) — אין מה לתקן. `alt`/`aria-hidden` על תמונות: גרפ
    `<img ` בלי `alt=` על כל 12 האפליקציות בהיקף החזיר אפס תוצאות — כל
    ה-`<img>` הקיימים כבר נושאים `alt`. שתי העדשות שהוצעו ריקות.
    **הרצתי סוכן Explore לחפש כיוון חדש** (RadioGroup בלי שם נגיש,
    onClick-div/TableRow בלי מקלדת באפליקציות שטרם נבדקו, `.map()` על
    ערך אפשרי-undefined, קישורים פנימיים שבורים) — שני ממצאים חזרו,
    ואימתתי כל אחד בעצמי לפני נגיעה.
    **ממצא שנבדק ונדחה:** `18-torah-editor-mvp/app/documents/page.tsx:261`
    — `<a href="/login?...">` גולמי במקום `next/link`, עוקף את
    `basePath: "/orech"` של האפליקציה. אמיתי, אבל **מחוץ להיקף העדשה
    שנבחרה** (לא נגישות-מקלדת) ונדחה כדי לשמור על צעד אחד ממוקד —
    מתועד כאן כדי שלא ייחקר מחדש: `apps/18-torah-editor-mvp/app/
    documents/page.tsx:261` צריך `Link` במקום `a` (הקובץ כבר מייבא
    ומשתמש ב-`Link` בשתי שורות אחרות, אותה קונבנציה).
    **הממצא שתוקן: `onClick`-`TableRow`/`div` בלי מקלדת ב-
    `30-zchuyotpro-crm`, שהעדשה של סבבים 303-305 החמיצה לגמרי** —
    קראתי את שלושת קומיטי הסבבים ההם (`44616b82`, `88185b8a`,
    `5bd29138`) ואף אחד לא מזכיר אפליקציה 30, כלומר סוכן ה-Explore של
    סבב 304 (שסרק "17/18/22/26/27/28/31") פשוט לא כלל אותה. גרפתי
    `onClick={() => navigate` /`setSelected`/`setExpanded` על כל
    `src/` של 30 ומצאתי ארבעה מקומות; קראתי כל אחד עם ההקשר המלא:
    - `routes/_authenticated/clients/index.tsx:103-113` — `TableRow`
      שלם ברשימת לקוחות, מפעיל ניווט לפרטי לקוח. אפס תא עם קישור/פקד
      חלופי — פגיעה אמיתית ב-WCAG 2.1.1.
    - `routes/_authenticated/referrals.tsx:111` — אותה תבנית בדיוק
      ברשימת הפניות, מפעיל `setSelected` שפותח `Sheet` פרטים.
    - `features/clients/tabs/EntitlementsTab.tsx:165` — `<div>` עטיפה
      שמפעיל הרחבה/כיווץ שורת זכאות; ה-`<Button>` הפנימי (חץ) **לא נשא
      `onClick` משלו כלל** — בדיוק המקרה ש"גיבוי מדומה" שנפסל ב-`EntryCard`
      של סבב 304 ("יש `<Button>` פנימי אך בלי `onClick` משלו... המקש
      עדיין לא נגיש").
    - `routes/_authenticated/partners/index.tsx:111` — **נבדק ונפסל.**
      נראה זהה בדיוק לשלושת הקודמים, אבל `TableCell` הראשון שלו כבר
      עוטף `<Link to="/partners/$id" ... onClick={(e) => e.stopPropagation()}>`
      — נתיב מקלדת/קורא-מסך אמיתי לאותו יעד קיים כבר. לא באג.
    **התיקון:** אותה תבנית מדויקת כמו סבבים 303-305 —
    `role="button"`+`tabIndex={0}`+`aria-label` תיאורי+`onKeyDown`
    שמפעיל את אותה פונקציה ב-Enter/Space עם `preventDefault`. ב-
    `EntitlementsTab.tsx` גם: `aria-expanded={isOpen}` על ה-div (שכעת
    הוא הפקד האמיתי), וה-`Button` הפנימי קיבל `tabIndex={-1}
    aria-hidden="true"` במקום `aria-label="הרחב"` — כך שאין עצירת-פוקוס
    מתה בלי מטרה, והשם הנגיש/מקלדת יושבים על הפקד שבאמת עושה משהו.
    אפס שינוי לזרימת ה-mouse-click, ל-`className`, או ל-layout בכל
    ארבעת המקומות. אימתתי איזון סוגריים/מאמרות מסולסלות (`node -e`) על
    שלושת הקבצים שהשתנו, וקראתי מחדש את כל ה-diff. שלושת הקבצים כבר
    עוקבים ב-git תחת `.gitignore` (`git add -f`, כמו רוב סבבי 17-31).
    ענף חדש `fix/b-crm-onclick-keyboard-nav-0820`. **הבא בתור:** לתקן
    את `18-torah-editor-mvp/app/documents/page.tsx:261` (קישור שבור,
    `a`→`Link`, ממצא נפרד שנמצא ונדחה הסבב הזה), RadioGroup ללא שם נגיש
    (טרם נבדק), או לחזור לתחום המחירון/מיתוג 'עולם הסטארטאפים' אם
    עדשת הנגישות ממצה את עצמה.

## 20/08/2026 — סבב 310 (loop B)

310. **תיקנתי את הממצא שנדחה בסבב 309 (מחוץ להיקף העדשה ההיא): קישור**
    **שבור בין `basePath` ל-`login` ב-`18-torah-editor-mvp`.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    crm-onclick-keyboard-nav-0820, קומיט `c9803fc5`, כבר ה-HEAD) ו-
    `core.projects` 17-31 (אין שינוי מטא-דאטה). **אימתתי את הבאג בעצמי**
    **לפני נגיעה, לא רק הסתמכתי על תיעוד סבב 309:** האפליקציה מוגדרת
    `basePath: "/orech"` (`next.config.js`), ויש לה `app/login/page.tsx`
    משלה — עמוד התחברות אמיתי עם Supabase magic-link
    (`signInWithOtp`). כל שאר הניווטים הפנימיים בקובץ `documents/page.tsx`
    (שורות 224, 298) כבר משתמשים ב-`next/link` כדי ש-`basePath` יתווסף
    אוטומטית. אבל שורה 261 ("כניסה לחשבון", מוצג רק כשהמשתמש לא מחובר)
    הייתה `<a href="/login?...">` **גולמי** — עוקף את `basePath` לגמרי.
    בדקתי מה יש בפועל בשורש `/login`: `portal/vercel.dist.json` ממפה
    `/login` → `/login.html`, עמוד כניסה **נפרד ולא-קשור** של הפורטל
    המרכזי (לא Supabase magic-link של 18). כלומר כל משתמש לא-מחובר
    שלחץ "כניסה לחשבון" מ-`/orech/documents` נשלח בפועל לעמוד ההתחברות
    הלא-נכון של הפורטל, לא לעמוד ההתחברות האמיתי של האפליקציה שלו
    (`/orech/login`) — רגרסיית ניווט אמיתית, לא רק ממצא נגישות.
    **התיקון:** `<a href="/login?...">` → `<Link href="/login?...">`,
    אותה תבנית מדויקת כמו שני השימושים הקיימים באותו קובץ, אפס שינוי
    ל-query string/className/טקסט. `Link` כבר מיובא בקובץ. `git diff`
    מציג שינוי תג בלבד (`a`→`Link` בפתיחה ובסגירה) — לא נדרשה בדיקת
    build (אין התקנות/דב-סרבר בסבב הזה לפי הנחיית ההרצה), האימות היה
    קריאת JSX ידנית מול שני התבניות הקיימות באותו קובץ. ענף חדש
    `fix/b-torah-editor-login-basepath-link-0820`. **הבא בתור:**
    RadioGroup ללא שם נגיש (הכיוון שהוצע בסבב 309 ולא נבדק עדיין), או
    לחזור לתחום המחירון/מיתוג 'עולם הסטארטאפים' (אומת נקי בסבבים
    קודמים) אם העדשה הזו ממצה את עצמה.

## 20/08/2026 — סבב 311 (loop B)

311. **בדקתי את שתי ההצעות של סבב 310, שתיהן יצאו ריקות/לא-בטוחות —**
    **קראתי README.md/CONNECTIONS.md, `core.run_progress` (הצעד האחרון:
    torah-editor-login-basepath-link-0820, קומיט `afbf4915`, כבר
    ה-HEAD) ו-`core.issues` פתוחים/owner=agent בהיקף 17-31: #5 (עיצוב
    ייחודי) אושר שוב כ"פער מבני רחב — סבב נפרד וגדול משמעותית" (קראתי
    את סוף `QA/platform/DESIGN_SAMENESS.md` עד השורה האחרונה, 19/08:
    שרשור ה-icon-decisions על תשע המערכות המלאות כבר **סגור לגמרי**,
    הפתוח היחיד הוא 9-עם-קיט מול 9-בלעדיו); #62/#115/#242 חסומים על
    גישה/הכרעה חיצונית מתועדת, ללא שינוי. #246/#253 שנראו "פתוחים"
    בשאילתת `status not in (resolved,closed)` התבררו בבדיקה ישירה
    כ-`status='fixed'` בפועל (השדה `blocked_on` פשוט לא נוקה) — נסגרו
    כבר בסבבים אחרים (Loop A סבב 56 עבור #253, 19/08 עבור #246), אין
    מה לפתוח מחדש.**
    בדקתי RadioGroup ללא שם נגיש (הצעת סבב 309/310): הרצתי סוכן
    Explore על כל 12 האפליקציות בהיקף — **תשע מהן לא משתמשות ב-
    RadioGroup/radio-input בכלל בקוד בפועל** (רק הגדרות רכיב ב-`ui/`),
    ובשתיים שכן (`19-igud-shiurim-portal` HTML גולמי, `27-bkalut-price`
    Radix RadioGroupItem) **כל המופעים כבר עטופים ב-`<label>`** —
    ‏`RadioGroupItem`/HTML `<input>` שניהם "labelable elements" לפי
    מפרט ה-HTML, כך שהעטיפה מספיקה כשם נגיש תקני. עדשה ריקה.
    **בדקתי גם את הצעת ה-scope המפורשת של הלולאה הזו — מצב TEST על
    תשלומים ב-17-31** — הרצתי סוכן Explore שסימן שני "ממצאים": (א)
    `21-mthbram/Footer.tsx:5` — כתובת נדרים פלוס עם `mosad=7013027`
    ולא `7016674` שמצוין ב-`CONNECTIONS.md`. בדקתי בעצמי לפני נגיעה:
    זו השורה **היחידה** בכל האפליקציה שמזכירה מספר מוסד, אין שום
    `7016674` מתחרה בקובץ, וה-`git log --follow` על הקובץ מראה שהמספר
    הזה **הגיע עם המקור המווענדר המקורי** (קומיט `baabd5d4`, "Rescue 21
    source fixes"), לא הוזן על ידי סוכן. שינוי מספר מוסד תרומות הוא
    שינוי כספי אמיתי — אם 7013027 הוא בכוונה מוסד/קמפיין נפרד
    (סינגלת בית-כנסת ספציפית של "מתחברים" ולא "איגוד שיעורים" הכללי),
    "תיקון" ל-7016674 היה **מסיט תרומות אמיתיות לגוף הלא-נכון**. אין
    כאן ראיה שזו טעות ולא בחירה מכוונת — לא נגעתי, ולא נרשם כתקלה כי
    זה בדיוק ההפך מ"תיקון בטוח": ניחוש בכיוון הלא-נכון עלול לגרום נזק
    כספי אמיתי. (ב) `27-bkalut-price` שולחת webhook ל-`NEDARIM3873`
    (n8n) כברירת מחדל כשאין `BKALUT_N8N_WEBHOOK_URL` — אבל זה ניתוב
    לידים קיים וחי (לא חיוב כספי), וה-webhook `NEDARIM3873` **מוגן
    במפורש** ב-README.md ("לעולם אין לגעת ב-... webhook NEDARIM3873") —
    שינוי ברירת המחדל שלו הוא בדיוק סוג הפעולה שהכלל הזה נועד למנוע.
    לא נגעתי בשני הממצאים — שניהם דורשים הכרעת משתמש/גישה שאין לי,
    לא צעד-קוד בטוח.
    **פתחתי עדשה חדשה במקום זאת: קישורי ניווט פנימיים גולמיים (`<a
    href="/">`) שעוקפים את ה-`basename`/`basePath` של האפליקציה — אותה**
    **משפחת באג שנסגרה בסבב 310 ב-`18-torah-editor-mvp`, מורחבת
    לאפליקציות Vite/React-Router.** סוכן Explore סרק את כל 12
    האפליקציות בהיקף וסימן שני מועמדים; אימתתי כל אחד בעצמי לפני
    נגיעה: `21-mthbram/src/pages/NotFound.tsx:16` ו-
    `24-galilee-connect-hub/src/pages/NotFound.tsx:16` — שני עמודי 404
    כמעט זהים (תבנית shadcn/lovable סטנדרטית) עם `<a href="/">`
    ל"חזרה לדף הבית". קראתי את `App.tsx` של שתי האפליקציות: שתיהן
    `<BrowserRouter basename="/mthbram">` / `basename="/galil"` —
    כלומר קליק על "חזרה לדף הבית" מתוך 404 שולח את המשתמש ל-`/`
    (שורש הפורטל המשותף) במקום ל-`/mthbram` / `/galil` (דף הבית
    האמיתי של האפליקציה עצמה). שתי האפליקציות כבר מייבאות ומשתמשות
    ב-`Link` מ-`react-router-dom` בקבצים אחרים (24 מופעים סה"כ בשתיהן,
    כולל `Navbar.tsx` של 21) — `NotFound.tsx` היה הקובץ היחיד שנשאר
    עם עוגן גולמי. התיקון: `<a href="/">` → `<Link to="/">`, הוספת
    `Link` לאותה שורת import של `useLocation` הקיימת. אפס שינוי
    ל-className/טקסט/מבנה בשני הקבצים. `git diff` מציג שינוי תג בלבד;
    שני הקבצים כבר עוקבים ב-git (`git ls-files` אישר), נדרש `git add
    -f` בגלל אזהרת `.gitignore` על תיקיות המקור (לא באמת untracked —
    אותו דפוס שתועד בסבבים קודמים). אין build/dev-server בסבב הזה
    לפי הנחיית ההרצה — האימות היה קריאת קוד ישירה מול שני התבניות
    (basename ב-App.tsx, שימוש קיים ב-Link בכל אפליקציה). ענף חדש
    `fix/b-notfound-basename-link-0820`. **הבא בתור:** לסרוק את שאר
    האפליקציות ב-17-31 לעמודי 404/error-boundary דומים עם אותה תבנית
    (רק 21 ו-24 נבדקו עד כה כי אלה מה שסוכן ה-Explore סימן — כדאי
    לוודא שהסריקה הייתה אכן ממצה על שאר 10), או מצב TEST/סנדבוקס
    אמיתי לתשלומים (נבדק חלקית הסבב הזה, לא נמצא צעד בטוח לנקוט).

## 20/08/2026 — סבב 312 (loop B)

312. **השלמתי את ה"הבא בתור" של סבב 311: סרקתי את שאר 10 האפליקציות**
    **ב-17-31 (מלבד 21/24 שכבר תוקנו) לאותה משפחת באג — קישור פנימי**
    **גולמי שעוקף base path/basename.** קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: notfound-basename-link-0820,
    קומיט `47744407`, כבר ה-HEAD) ו-`core.issues` פתוחים/owner=agent
    בהיקף 17-31 — אין שינוי מאז סבב 311 (#5/#62/#115/#242 עדיין חסומים
    באותו אופן מתועד). הרצתי סוכן Explore על כל 12 האפליקציות (14 בהיקף
    פחות שתי הקודמות שכבר תוקנו) לסווג כל אחת: יש לה base
    path/basename מוגדר? יש קישור `<a href="/">` גולמי שעוקף אותו?
    **שלוש קטגוריות נמצאו, בדקתי כל אחת בעצמי לפני שסמכתי על הדוח:**
    - `19-igud-shiurim-portal`, `20-igud-portal` — שרתי Express גולמיים
      בלי SPA routing, אין basePath שיש לעקוף. `23-haorech-torani` —
      HTML סטטי בלי `package.json`. `25-mor1-main-site` — `app.json`
      בלבד. `29-bkalot-design` — טוקנים/CSS בלבד. חמישתן לא רלוונטיות
      לעדשה הזו במבנה.
    - `17-chizukim-transcribe`, `26-modaot-studio`, `27-bkalut-price`,
      `28-kupot-health-funds` — Vite עם ניתוב מבוסס hash (`wouter`),
      אין `basename`/`base` שדורש עקיפה.
    - `18-torah-editor-mvp` (Next `basePath`) ו-`22-get-your-rights`
      (React Router `basename={import.meta.env.BASE_URL...}`) — נבדקו
      שורה-שורה, כל ניווט פנימי כבר עובר דרך `Link`/`BASE_URL`; אין
      עוגן גולמי שנשאר.
    **שני באגים אמיתיים נמצאו ותוקנו — לא בעמוד 404 הפעם אלא בעמוד**
    **שגיאת-SSR הגלובלי, אותה תבנית משפחת-באג בדיוק בשתי אפליקציות**
    **תאומות `TanStack Start`:** `30-zchuyotpro-crm/src/lib/error-page.ts:25`
    ו-`31-hebrew-bridge-crm/src/lib/error-page.ts:25` — שתיהן מגדירות
    `vite: { base: "/crm/" }` / `"/gesher/"` ב-`vite.config.ts`,
    ו-`renderErrorPage()` (מוחזר מ-`server.ts`/`start.ts` בכל 500 קטסטרופלי
    של ה-SSR, גם כשהראוטר עצמו קרס) הכיל `<a class="secondary" href="/">Go
    home</a>` גולמי — בדיוק אותה עקיפת base path כמו סבב 310/311, רק
    שהפעם המסך המושפע הוא דף שגיאה גלובלי ולא 404 רגיל. **אימתתי לפני**
    **נגיעה** ש-`import.meta.env.BASE_URL` הוא התיקון הנכון ולא ניחוש:
    שתי האפליקציות כבר משתמשות בו בדיוק לאותה מטרה במקום אחר
    (`routes/auth.tsx` בשתיהן, ו-`routes/_authenticated/admin/
    integrations.tsx` ב-31) — כלומר זה מקור אמת קיים בקוד, לא ערך חדש
    שהמצאתי; ו-Vite מחליף `import.meta.env.BASE_URL` בזמן build גם
    בבאנדל השרת (nitro/vite בונה גם את `server.ts`, ראו הערת
    `tanstackStart.server.entry` ב-`vite.config.ts`), כך שההחלפה
    הסטטית תקפה גם כאן ולא רק בצד לקוח. התיקון: `href="/"` →
    ``href="${import.meta.env.BASE_URL}"`` בתוך אותו template literal
    הקיים (הפונקציה כבר משתמשת בבק-טיקים), שינוי שורה אחת בכל קובץ,
    אפס שינוי ל-CSS/מבנה/טקסט. אין build/dev-server בסבב הזה לפי הנחיית
    ההרצה — האימות היה קריאת קוד ישירה מול התקדימים הקיימים
    (`auth.tsx` בשתי האפליקציות) ומול הגדרת ה-`base` ב-`vite.config.ts`.
    ענף חדש `fix/b-crm-gesher-errorpage-basepath-0820`. **הבא בתור:**
    עדשת ה-basePath-bypass נראית ממוצה כעת על כל 14 האפליקציות בהיקף
    (404, login, error-page — שלושת הסוגים שנבדקו); לפתוח כיוון חדש
    (תחום המחירון/מיתוג 'עולם הסטארטאפים', שעדיין לא נבדק קוד-רמה
    בלולאה הזו), או עדשת נגישות/regression אחרת שטרם נבדקה.

## 20/08/2026 — סבב 313 (loop B)

313. **תיקון היקף: מהסבב הזה ואילך, "loop B" הוא במפורש 17,18,19,20,**
    **21,22,23,24,25,27,28 בלבד — 26/29/30/31 (וכל 32-38) עברו ל-loop C**
    **ואינם בהיקף עוד, אף שסבבים 280-312 טיפלו בהם כחלק מ-"17-31".**
    לא נגעתי ב-26/29/30/31 בסבב הזה, ולא ביטלתי אף תיקון קודם בהם —
    רק וידאתי שהצעד החדש עצמו לא חורג. קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: crm-gesher-errorpage-
    basepath-0820, קומיט `8dbabfb4`, כבר ה-HEAD) ו-`core.issues`
    פתוחים/owner=agent — ללא שינוי מסבב 312 (#5/#62/#115/#242 עדיין
    חסומים באותו אופן מתועד; #248-251 בבעלות user, חסומים על פריסת
    מיגרציה חיצונית ל-30, גם אם היו בהיקף).
    הרצתי סוכן Explore בהיקף המדויק (17,18,19,20,21,22,23,24,25,27,28
    בלבד, לא 26/29/30/31) לחפש כיוון חדש שלא נבדק. עדשות ה-a11y
    שנבדקו מיצו את עצמן על checkbox/img/RadioGroup/Switch/onClick-div/
    dir=ltr; עדשת basePath-bypass מוצתה על 404/login/error-page.
    **הסוכן מצא כיוון שלא נבדק: `<select>`/`<input>` ללא שם נגיש**
    **ב-19-igud-shiurim-portal (אפליקציית vanilla-JS, לא React — לא**
    **נסרקה מעולם ע"י עדשת ה-select/input הקיימת, ששימשה עד כה רק על**
    **אפליקציות React) ו-22-get-your-rights.** אימתתי בעצמי לפני נגיעה:
    `apps/19-igud-shiurim-portal/public/app.js` שורות 517-525 — סרגל
    הסינון הראשי בעמוד הבית הציבורי (input עיר, select קהל יעד, input
    חיפוש חופשי) — שלושתם ללא `aria-label`/`<label>`/`aria-labelledby`,
    שניים מהם placeholder-בלבד (אותה קטגוריה שתועדה כבאג בסבב 306: "no
    label/aria-label/aria-labelledby, placeholder-only"). `grep` על כל
    הקובץ (למעלה מ-70 input/select) אישר ששאר הטופסים כמעט כולם כן
    עטופים ב-`<label>` טקסט (גם אם לא עם `for`/`id` — פער נפרד, גדול
    בהרבה, ברוב מסכי הניהול הפנימיים בלבד — לא נגעתי בו הסבב הזה, הוא
    צעד עתידי נפרד ומשמעותי בהרבה מ"צעד אחד"), ורק סרגל הסינון הציבורי
    חסר לגמרי. `apps/22-get-your-rights/src/pages/AdminRightsReference.tsx`
    שורה 449 — select סינון-קטגוריה, ללא שום שם נגיש. שתי האפליקציות
    כבר משתמשות ב-`aria-label` באותו קובץ בדיוק לאותה מטרה (19: כפתור
    הצג-סיסמה + dropzone; 22: TableRow `role="button"` שורה 486) — כך
    שזה תקדים קיים בקוד, לא דפוס חדש שהמצאתי. **התיקון:** הוספת
    `aria-label` בעברית תואם-הקשר לארבעת השדות, אפס שינוי לפריסה/
    ערכים/לוגיקה. `git diff` מציג תוספת attribute בלבד. אין build/
    dev-server בסבב הזה לפי הנחיית ההרצה — האימות היה `grep` מלא על
    כל הקובץ + קריאת השורות ישירות + בדיקת איזון סוגריים ב-Node על שני
    הקבצים (מאוזן: 1549/1549 סוגריים עגולים, 930/930 מסולסלים ב-19;
    417/417, 321/321 ב-22). שני הקבצים כבר עוקבים ב-git (`git ls-files`
    אישר), נדרש `git add -f` בגלל אזהרת `.gitignore` הרגילה. ענף חדש
    `fix/b-select-accessible-name-19-22-0820`, קומיט `14da4c6e`, נדחף
    (מפעיל פריסות Vercel תחת `more30.com/shiurim` ו-`more30.com/zchuyot`).
    **הבא בתור:** הפער הגדול-יותר שנמצא ב-19 — עשרות `<label>` בלי
    `for`/`id` תואם במסכי הניהול הפנימיים — ראוי לעדשה נפרדת וממוקדת
    (בדומה לסבב 308 שעשה זאת ל-Switch); לחלופין לחזור לאימות TEST-mode
    תשלומים/רברנד בהיקף המצומצם החדש (17-28 פחות 26, שלא אומת ספציפית
    מאז סבב 285-286 שכלל גם את 26/30/31 שכבר לא בהיקף).

## 20/08/2026 — סבב 314 (loop B)

314. **`19-igud-shiurim-portal` (`app.js`): ה"הבא בתור" של סבב 313 — יישום העדשה**
    **הממוקדת `label` בלי `for`/`id` תואם, על פני כל הקובץ (לא רק מסכי ניהול).**
    קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `select-accessible-name-19-22-0820`, קומיט `14da4c6e`, כבר ה-HEAD על הענף
    שעליו עבדתי) ו-DECISIONS.md #313 — היקף loop B מאושר 17-25,27,28 (ללא 26).
    לא נגעתי ב-26/29/30/31/32-38. ענפתי `fix/b-label-for-id-mismatch-19-0820`
    מעל תוצאת #313 (לא מ-`main`, ששם apps/ עדיין לא vendored). מיפוי מלא של
    התופעה: `<label>טקסט</label><input/textarea/select ...>` כאחים ב-DOM
    (בתוך `.form-field { display:flex; flex-direction:column }`) בלי nesting
    ובלי `for`/`id` — ולכן ללא שיוך נגיש, אף שהטקסט קיים חזותית. סקריפט Python
    מצא 86 מופעים כאלה בכל הקובץ (לא רק ניהול כפי שהניח סבב 313 — גם `donate`,
    `join`, `contactForm`, `renderTeacherPublic`, `renderLoginForm` הציבוריים).
    אימתתי לפני שינוי: כל 86 המופעים כבר נושאים `name="..."` ייחודי (נדרש
    ל-FormData בתוך אותו טופס בכל מקרה), ואף אחד לא נושא `id` קיים מראש —
    שני תנאים הכרחיים כדי שהוספת `id`/`for` תהיה בטוחה בלי להתנגש. ה-fix:
    `id="f19-<name-מנורמל>"` על הבקרה + `for` תואם על ה-`label`, עם דה-דופ'
    (`-2`,`-3`...) כששם השדה חוזר על עצמו בפונקציות render נפרדות שלא
    מוצגות יחד ב-DOM (למשל `name="title"` גם בטופס שיעור וגם במודעה).
    בנוסף: פונקציית העזר `pwField()` (עמ' 65, מייצרת קלט סיסמה + כפתור הצג)
    כבר יצרה `id` פנימי משלה (`pw-<seq>`), אבל ללא דרך לקורא להעביר אותו
    ל-`label` החיצוני — הוספתי לה פרמטר `id` אופציונלי, ועדכנתי את 4 נקודות
    הקריאה (`join` login, `nedarim-config` פעמיים, `renderLoginForm`) להעביר
    `id` קבוע תואם. **במכוון לא נגעתי** בשני מופעי `<label>` שנותרו (עמ' 200
    `uploadFieldHtml` ו-718 כותרת קבוצת ה-topic-picker) — שניהם כותרות-קבוצה
    ולא שיוך לבקרה בודדת, ולבקרה האינטראקטיבית בפועל (dropzone
    `role="button"`, checkbox-ים עטופים ב-`<label>` משלהם) כבר יש שם נגיש
    משלה, בדיוק כפי שסבב 313 כבר תיעד. אימות: `node --check` על הקובץ
    (הרצתי בפועל הפעם, לא רק ספירת סוגריים ידנית כמו בסבבים קודמים — חזק
    יותר, לא שינוי תהליך) + סקריפט Python שאישר אפס `id` כפול בין 90 המזהים
    החדשים (86 סטטיים + 4 דרך `pwField`) ושכל `for` פותר ל-`id` אמיתי.
    בדקתי גם התנגשות עם `id`-ים קיימים בקובץ — נמצאו 6 כפילויות קיימות
    מראש (`tab-content`, `messages-alert` וכו') שכולן כבר היו שם לפני
    השינוי שלי (לא נוצרו על ידי) ומייצגות את אותו דפוס לגיטימי של שימוש
    חוזר ב-id בין פונקציות render בלעדיות זו לזו — לא נגעתי בהן, מחוץ
    להיקף הצעד הזה. `git diff` מציג רק תוספת attribute, אפס שינוי
    ל-layout/logic/ערכים. קובץ כבר עוקב ב-git, נדרש `git add -f` בגלל
    `.gitignore`. קומיט `cb7dfcd0`, נדחף (מפעיל פריסת Vercel תחת
    `more30.com/shiurim`).
    **הבא בתור:** התשלום/רברנד TEST-mode לא אומת ספציפית בהיקף המצומצם
    מאז סבב 285-286 (שכלל גם 26/30/31 שכבר לא בהיקף) — מועמד טבעי לסבב
    הבא; לחלופין לבדוק אם עדשת ה-label-for-id שהופעלה כאן רלוונטית גם
    ל-22/23/24/25/27/28 (לא נבדקה שם הסבב הזה, רק ב-19).

## 20/08/2026 — סבב 315 (loop B)

315. **תשובה ל"הבא בתור" של סבב 314, כיוון ב': אימתתי שעדשת ה-label-for-id**
    **(סבב 314, על 19) רלוונטית גם ל-JSX/React, לא רק לתבניות HTML גולמיות**
    **בשרת — ותיקנתי את המקרה הראשון שנמצא, ב-21-mthbram.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `label-for-id-mismatch-19-0820`, קומיט `cb7dfcd0`, כבר ה-HEAD) ו-
    DECISIONS.md #313-314 — היקף loop B מאושר 17,18,19,20,21,22,23,24,25,27,28
    בלבד. לא נגעתי ב-26/29/30/31/32-38. הרצתי סוכן Explore על שאר 10
    האפליקציות בהיקף (19 כבר תוקנה, דילגתי) לאותה משפחת באג בגרסתה ה-JSX:
    `<label>` שהוא אח ב-DOM (JSX sibling, לא עוטף כילד) של `<input>`/
    `<select>`/`<Input>` בלי `htmlFor`/`id` תואמים. אימתתי בעצמי לפני נגיעה
    (לא סמכתי על דוח הסוכן): `20-igud-portal/public/app.js` שורות 226-230
    משתמשות בעוזר DOM-builder `el("label", {}, "טקסט", el("input", ...))`
    כאשר ה-input הוא **ילד** של ה-label (שיוך מרומז תקין) — לא באג, למרות
    שנראה דומה ל-19 במבט ראשון. `23-haorech-torani`/`25-mor1-main-site`
    עדיין לא נבנו (index.html בודד / app.json בלבד) — אין מה לתקן.
    `18-torah-editor-mvp` ו-`28-kupot-health-funds`: עוטפים כילד או ללא
    תבנית label-control רלוונטית — נקיים. **הסוכן דיווח על מקרים אמיתיים
    ב-17, 21, 22, 24, 27** (סה"כ כ-30 זוגות) — בחרתי לתקן **רק את 21**
    הסבב הזה (שני קבצים, 6 מקרים, היקף אחד-קונטיינר-נקי לצעד בודד, בדומה
    לאיך שסבב 313 הגביל את עצמו לשני קבצים ולא ניסה למצות הכל בבת אחת),
    ואת השאר משאיר בתור להמשך. אימתתי כל שורה ידנית לפני עריכה:
    `LessonDirectory.tsx` שורות 145-174 — 4 selects בסרגל הסינון (עיר/קהל
    יעד/סגנון/שפה), כל label אח ל-select בלי for/id. `AdminLogin.tsx`
    שורות 175-206 — שם משתמש + סיסמה (Input של shadcn, כבר `forwardRef`
    ומעביר `...props` כולל `id`, אז הוספת prop בטוחה). התיקון: `htmlFor`
    על ה-`<label>` + `id` תואם על הבקרה (`filter-city`/`filter-audience`/
    `filter-style`/`filter-language`, `admin-login-username`/
    `admin-login-password`) — בדקתי התנגשות `id` בכל קובץ לפני ואחרי, אפס
    כפילות. `git diff` מציג תוספת attribute בלבד, אפס שינוי ל-layout/
    לוגיקה/ערכים. אין build/dev-server בסבב הזה לפי הנחיית ההרצה. קובץ
    כבר עוקב ב-git, נדרש `git add -f` בגלל `.gitignore`. ענף חדש
    `fix/b-label-for-id-mismatch-21-0820`, קומיט `5b801e26`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/mthbram`).
    **הבא בתור:** עדשת ה-label-for-id עדיין פתוחה על 17-chizukim-transcribe
    (`upload.tsx`, `recording-detail.tsx` — 5 מקרים), 22-get-your-rights
    (`FloatingBot.tsx` — 2 מקרים), 24-galilee-connect-hub
    (`MourningGuidePage.tsx`, `SynagogueDetailsManager.tsx` — כ-11 מקרים),
    ו-27-bkalut-price (`public-health-funds.tsx` — 3-4 מקרים, יש גם
    מקרים שכבר עוטפים נכון ב-`integrations.tsx`/`right-detail.tsx` שאין
    לגעת בהם) — מועמד טבעי להמשך ישיר בסבב הבא; לחלופין תשלום/רברנד
    TEST-mode שעדיין לא אומת בהיקף המצומצם מאז סבב 285-286.

## 20/08/2026 — סבב 316 (loop B)

316. **`22-get-your-rights` (`FloatingBot.tsx`): המשך תור סבב 315 — 2 המקרים**
    **שדווחו בקובץ זה.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: `label-for-id-mismatch-21-0820`, קומיט
    `5b801e26`, כבר ה-HEAD) ו-DECISIONS.md #313-315 — היקף loop B מאושר
    17,18,19,20,21,22,23,24,25,27,28 בלבד. לא נגעתי ב-26/29/30/31/32-38.
    `grep -n "<label"` על הקובץ מצא 3 מופעים: שורה 904 (checkbox של
    "סוג שיתוף פעולה") היא כבר `<label>` עוטף (הקלט `type="checkbox"` הוא
    ילד בפועל, `sr-only` אך משויך כדין) — לא נגעתי. שורות 598 ו-862 הן אחים
    ב-DOM בלי `for`/`id`, בדיוק דפוס הבאג: שורה 598 היא `<label>` דינמי בתוך
    `currentGroup.fields.map` (בדיקה מקיפה) המשותף לארבעה טיפוסי שדה
    (`text`/`date`/`select`/`yesno`) המוצגים על תנאי לפי `field.type` — לכל
    `field.key` יש בדיוק בקרה אחת בפועל בכל רגע נתון (אימתתי בסקריפט Python
    שכל 23 מפתחות השדה על פני 6 הקבוצות ב-`comprehensiveGroups` ייחודיים,
    אין התנגשות).
    הוספתי `htmlFor={`comp-${field.key}`}` ל-label ו-`id={`comp-${field.key}`}`
    תואם לכל אחת משלוש הבקרות הממשיות (`Input` טקסט, `Input type="date"`,
    `select`) — במכוון **לא** הוספתי id לטיפוס `yesno` (זוג כפתורי toggle,
    לא בקרה בודדת אחת לשיוך, אותה חריגה כמו כותרות-הקבוצה שסבב 314 השאיר
    ב-19: ה-`htmlFor` פשוט לא יפתור שם, בדיוק כמו לפני השינוי — לא רגרסיה).
    שורה 862-863 (טופס `family-form`, שדה תאריך לידה): זוג label/Input פשוט,
    `id="family-date-of-birth"` + `htmlFor` תואם. אימתתי לפני עריכה שאין
    שום `id=` קיים בקובץ כלל (`grep 'id="\|id={'` החזיר ריק) — אפס סיכון
    התנגשות. אימות אחרי: `python3` בדק איזון סוגריים (362/362 עגולים,
    475/475 מסולסלים, 82/82 מרובעים) — תקין. `git diff` מציג תוספת
    attribute בלבד (2 `htmlFor`, 4 `id`), אפס שינוי ל-layout/לוגיקה/ערכים.
    קובץ לא ב-`.gitignore` (`git check-ignore` ריק), לא נדרש `-f`. אין
    build/dev-server בסבב הזה לפי הנחיית ההרצה. ענף חדש
    `fix/b-label-for-id-mismatch-22-0820`, נדחף (מפעיל פריסת Vercel תחת
    `more30.com/zchuyot`).
    **הבא בתור:** עדשת ה-label-for-id עדיין פתוחה על 17-chizukim-transcribe
    (`upload.tsx`, `recording-detail.tsx` — 5 מקרים) ו-24-galilee-connect-hub
    (`MourningGuidePage.tsx`, `SynagogueDetailsManager.tsx` — כ-11 מקרים)
    ו-27-bkalut-price (`public-health-funds.tsx` — 3-4 מקרים) — מועמדים
    טבעיים להמשך ישיר; לחלופין תשלום/רברנד TEST-mode שעדיין לא אומת
    בהיקף המצומצם מאז סבב 285-286.

## 20/08/2026 — סבב 317 (loop B)

317. **`17-chizukim-transcribe` (`upload.tsx` + `recording-detail.tsx`): המשך תור**
    **סבב 316 — 5 המקרים שדווחו בשני הקבצים הללו.** קראתי README.md/
    CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `label-for-id-mismatch-22-0820`, קומיט `ef405c70`, כבר ה-HEAD) ו-
    DECISIONS.md #313-316 — היקף loop B מאושר 17,18,19,20,21,22,23,24,25,27,28
    בלבד. לא נגעתי ב-26/29/30/31/32-38. קראתי את שני הקבצים במלואם וזיהיתי
    ידנית 5 זוגות label/control שהם אחים ב-DOM בלי `for`/`id`: `upload.tsx`
    שורות 304-313 (נושא) ו-316-325 (פרשה/תאריך), שני שדות `<Input>` של
    shadcn. `recording-detail.tsx` שורות 214-223 (נושא), 226-234
    (פרשה/תאריך) — אותו טיפוס `<Input>` — ושורות 239-243 (סטטוס), שהיא
    `<Select>` של shadcn/Radix ולא `<input>` רגיל. אימתתי לפני נגיעה
    ש-`SelectTrigger` (ב-`components/ui/select.tsx`) הוא `forwardRef` שמעביר
    `...props` (כולל `id`) ל-`SelectPrimitive.Trigger`, שמרונדר כ-`<button>`
    בפועל — ו-`<button>` הוא אלמנט labelable לפי מפרט ה-HTML (בדיוק כמו
    `input`/`select`/`textarea`), כך ש-`htmlFor`/`id` עובד עליו באותה דרך
    כמו על שדה טקסט רגיל, לא רק על `<select>` native כמו ב-21 (סבב 315).
    הוספתי `htmlFor`/`id` תואמים לכל חמשת הזוגות (`upload-topic`,
    `upload-parsha`, `detail-topic`, `detail-parsha`, `detail-status`).
    אימתתי לפני עריכה שאין שום `id=` קיים בשני הקבצים (`grep 'id="\|id={'`
    החזיר ריק בשניהם) — אפס סיכון התנגשות. `git diff` מציג תוספת attribute
    בלבד (5 `htmlFor` + 5 `id`), אפס שינוי ל-layout/לוגיקה/ערכים. שני
    הקבצים תחת `.gitignore` (`client/` בתוך 17), נדרש `git add` על נתיב
    מפורש (לא `-f`, git הזהיר אך הוסיף כי הם כבר עוקבים). אין build/
    dev-server בסבב הזה לפי הנחיית ההרצה — אימות באמצעות ספירת תווים
    מאוזנת (`()[]{}`) ו-`git diff` בלבד. ענף חדש
    `fix/b-label-for-id-mismatch-17-0820` (נבנה מעל `ef405c70`, לא מ-main —
    אותו דפוס כמו סבבים קודמים: כל סבב ממשיך משם קודמו), קומיט `df4de9a4`,
    נדחף (מפעיל פריסת Vercel תחת `more30.com/chizukim`).
    **הבא בתור:** עדשת ה-label-for-id עדיין פתוחה על 24-galilee-connect-hub
    (`MourningGuidePage.tsx`, `SynagogueDetailsManager.tsx` — כ-11 מקרים,
    הקובץ הגדול ביותר שנותר בתור) ו-27-bkalut-price
    (`public-health-funds.tsx` — 3-4 מקרים) — מועמדים טבעיים להמשך ישיר;
    לחלופין תשלום/רברנד TEST-mode שעדיין לא אומת בהיקף המצומצם מאז סבב
    285-286.

## 20/08/2026 — סבב 318 (loop B)

318. **`24-galilee-connect-hub` (`MourningGuidePage.tsx` + `SynagogueDetailsManager.tsx`):**
    **המשך תור סבב 317 — 16 המקרים שדווחו בשני הקבצים הללו.** קראתי
    README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `label-for-id-mismatch-17-0820`, קומיטים `df4de9a4`/`0ad14895`, כבר
    ה-HEAD) ו-DECISIONS.md #313-317 — היקף loop B מאושר
    17,18,19,20,21,22,23,24,25,27,28 בלבד. לא נגעתי ב-26/29/30/31/32-38.
    קראתי את שני הקבצים במלואם וזיהיתי ידנית כל זוגות label/control:
    `MourningGuidePage.tsx` שורות 102-133 — 6 זוגות בטופס בקשת התמיכה (שם
    מלא, טלפון, שם הנפטר/ת — כולם `<Input>` של shadcn; סוג הבקשה — `<select>`
    native; תאריך מבוקש — `<Input type="date">`; הערות — `<Textarea>`).
    `SynagogueDetailsManager.tsx` שורות 239-245 — 7 זוגות בטופס פרטי בית
    הכנסת (שם/שכונה/נוסח/רב/כתובת/קישור תרומה/רקע), ושורה 277 — 3 זוגות
    נוספים בטופס כניסת גבאי (שם תצוגה/שם משתמש/סיסמה). בדקתי לפני נגיעה
    ש-`Input`/`Textarea` (`components/ui/input.tsx`/`textarea.tsx`) הם
    `forwardRef` שמעבירים `...props` (כולל `id`) לאלמנט ה-DOM האמיתי —
    בדיוק כמו בכל הסבבים הקודמים. **במכוון לא נגעתי** בשלושה סוגי label
    נוספים באותם קבצים: label של העלאת קובץ (לוגו/תמונת מודעה, שורות 252
    ו-274) — ה-`<input type="file">` הוא ילד בפועל של ה-label, שיוך מרומז
    תקין; ושלושה label של checkbox (אין מניין שורה 264, פעילה שורה 274,
    חשבון פעיל שורה 277) — גם הם עוטפים את ה-checkbox כילד. אלה כבר נגישים
    כשורה ולא חלק מדפוס הבאג. שדות ללא label כלל (למשל "שם התפילה"/"06:15"
    בשורת שעות תפילה, שורה 264) גם הם מחוץ להיקף — אין label קיים לתקן.
    אימתתי לפני עריכה שאין שום `id=` קיים בשני הקבצים (`grep 'id="\|id={'`
    החזיר ריק בשניהם) — אפס סיכון התנגשות; אחרי העריכה `grep -o 'id="..."'
    | sort | uniq -c` אישר שכל 16 המזהים החדשים ייחודיים (`mourning-*`
    ב-MourningGuidePage, `syn-*` ב-SynagogueDetailsManager). `git diff
    --stat` מציג 2 קבצים, 20 תוספות/20 מחיקות (כולן attribute בלבד), אפס
    שינוי ל-layout/לוגיקה/ערכים. אין `tsc`/build זמינים בסביבה זו (נבדק,
    `npx tsc` לא מותקן) — אימות באמצעות בדיקת איזון סוגריים ב-Python על שני
    הקבצים (תקין בשניהם) בנוסף ל-`git diff`. שני הקבצים תחת `.gitignore`
    (`apps/24-galilee-connect-hub/src`), נדרש `git add -f` — אימתתי
    ש-`git log` מראה אותם כבר עוקבים (קומיט rescue קודם), אותו דפוס חוזר
    מכל סבבי loop B הקודמים. ענף חדש `fix/b-label-for-id-mismatch-24-0820`
    (בנוי מעל `0ad14895`), קומיט `398674a6`, נדחף (מפעיל פריסת Vercel תחת
    `more30.com/galilee`, לוודא basePath מדויק ב-registry — לא נבדק כאן,
    מחוץ להיקף הצעד).
    **הבא בתור:** עדשת ה-label-for-id עדיין פתוחה על 27-bkalut-price
    (`public-health-funds.tsx` — 3-4 מקרים, יש גם מקרים שכבר עוטפים נכון
    ב-`integrations.tsx`/`right-detail.tsx` שאין לגעת בהם) — הקובץ האחרון
    שנותר בתור מהסריקה המקורית של סבב 315; אחריו כדאי סריקת Explore חדשה
    על 18/20/23/25/28 (עדיין לא נבדקו ספציפית לעדשה הזו) לוודא שלא נשאר
    עוד היקף; לחלופין תשלום/רברנד TEST-mode שעדיין לא אומת בהיקף המצומצם
    מאז סבב 285-286.

## 20/08/2026 — סבב 319 (loop B)

319. **`27-bkalut-price` (`public-health-funds.tsx`): המשך תור סבב 318 — הקובץ**
    **האחרון שנותר מהסריקה המקורית של סבב 315.** קראתי README.md/
    CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `label-for-id-mismatch-24-0820`, קומיטים `398674a6`/`7a1cd678`, כבר
    ה-HEAD) ו-DECISIONS.md #313-318 — היקף loop B מאושר
    17,18,19,20,21,22,23,24,25,27,28 בלבד. לא נגעתי ב-26/29/30/31/32-38.
    שמתי לב שיש ב-`apps/27-bkalut-price/CLAUDE.md` אזהרות על קבצים
    קריטיים (`App.tsx`/`admin-login.tsx`/`server/routes.ts`/`utils.ts`)
    השייכים לתשתית bkalut-app — **לא נגעתי בהם**, המשימה שלי מוגבלת לדף
    ציבורי בלבד (`public-health-funds.tsx`) ומערכת 27 שונה ממספר 08
    המוגן. `grep -n "<label"` מצא 4 מופעים בסרגל הסינון הצדדי: שורה 300
    (חיפוש, מעל `<Input>`), שורה 322 (תחום, מעל קבוצת `<Button>` toggle —
    **לא תיקנתי**, אותה חריגה שסבבים 314/316 קבעו לקבוצות כפתורים/toggle
    בלי בקרה בודדת אחת לשיוך), שורה 340 (קופת חולים, מעל `<select>`
    native), שורה 353 (קטגוריה, מעל `<select>` native). הוספתי
    `htmlFor`/`id` תואמים לשלושת הזוגות הפשוטים (`hf-search-input`,
    `hf-fund-select`, `hf-category-select`). אימתתי לפני עריכה מול
    ה-dialog הקיים במעבר קופה (`sw-name`/`sw-phone`/וכו', כבר עם
    shadcn `Label htmlFor` תקין) כדי לשמור על מוסכמת שמות עקבית ולוודא
    היעדר התנגשות — `grep 'id="hf-search-input"\|id="hf-fund-select"\|
    id="hf-category-select"'` אישר שכל אחד מופיע פעם אחת בלבד. `python3`
    בדק איזון סוגריים (239/239 עגולים, 301/301 מסולסלים, 43/43 מרובעים)
    — תקין. `git diff` מציג תוספת attribute בלבד (3 `htmlFor` + 3 `id`),
    אפס שינוי ל-layout/לוגיקה/ערכים. קובץ כבר עוקב ב-git (`git log`
    מראה קומיט `d4788636`), נדרש `git add -f` בגלל `.gitignore` (אותו
    דפוס חוזר מכל סבבי loop B הקודמים). אין build/dev-server בסבב הזה
    לפי הנחיית ההרצה. ענף חדש `fix/b-label-for-id-mismatch-27-0820`,
    קומיט `c61eeffb`, נדחף (מפעיל פריסת Vercel תחת `more30.com/bkalut`,
    לוודא basePath מדויק ב-registry — לא נבדק כאן, מחוץ להיקף הצעד).
    **הבא בתור:** הסריקה המקורית של סבב 315 הושלמה — כל חמשת הקבצים
    שדווחו (17, 21, 22, 24, 27) תוקנו. השלב הבא הטבעי: סריקת Explore
    חדשה על 18-torah-editor-mvp, 20, 23-haorech-torani, 25-mor1-main-site,
    28-kupot-health-funds (עדיין לא נבדקו ספציפית לעדשה הזו — 23/25 ידועים
    כלא-בנויים עדיין, ייתכן שאין מה לתקן שם) כדי לוודא שלא נשאר עוד היקף
    לעדשת label/for/id; לחלופין תשלום/רברנד TEST-mode שעדיין לא אומת
    בהיקף המצומצם מאז סבב 285-286.

## 20/08/2026 — סבב 320 (loop B)

320. **סריקת Explore על 18/20/23/25/28 לעדשת label/for/id (כפי שתוכנן**
    **בסוף סבב 319) חזרה ריקה** — קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון: `label-for-id-mismatch-27-0820`,
    קומיטים `c61eeffb`/`a2cb5432`, כבר ה-HEAD) ו-DECISIONS.md #313-319 —
    היקף loop B מאושר 17,18,19,20,21,22,23,24,25,27,28 בלבד. הרצתי סוכן
    Explore על חמשת המערכות: 18-torah-editor-mvp כבר עם `htmlFor`/`id`
    תקינים בכל מקום (כולל עטיפה implicit); 20-igud-portal הוא JS וניל
    טהור, טפסי הקשר עוטפים את השדה כילד (implicit, תקין); 23-haorech-torani
    ו-25-mor1-main-site אין להם קוד מקור אמיתי בכלל (רק HTML סטטי /
    app.json בלבד — כפי שסבב 319 שיער); 28-kupot-health-funds עטוף
    ברכיב `Field`/`FormLabel` משותף שמנהל `htmlFor`/`id` אוטומטית. **עדשת
    label/for/id נסגרת רשמית** על פני כל 11 המערכות בהיקף loop B — אין
    עוד מה לתקן בה. עברתי לצעד הבא שסבב 319 הציע כחלופה: אימות TEST-mode
    תשלום/רברנד, שלא נבדק מאז סבב 285-286 — אבל שמתי לב שסבב 285 עצמו
    בדק רק 17/18/21/22/24/26/27/28/30/31 (ההיקף הישן, לפני התיקון
    ל-17,18,19,20,21,22,23,24,25,27,28), כלומר **19-igud-shiurim-portal
    מעולם לא נבדק בעדשה הזו** על אף שהוא בהיקף הנוכחי ויש לו תוכן אמיתי
    (טפסי תרומה/הרשמה/ניהול ב-`public/app.js`+`server.js`). קראתי את
    `public/index.html` במלואו: הכותרת/מטא כבר עברית טהורה, אין שאריות
    Lovable/סלאג אנגלי. `server.js` (בדקתי את כל נתיב הסליקה, שורות
    960-1084): מפתחות התשלום (`PLATFORM_NEDARIM_MOSAD`/
    `PLATFORM_NEDARIM_API_VALID`) מגיעים אך ורק ממשתני סביבה, אין מפתח
    קשיח בקוד — ואם המשתנים חסרים השרת מחזיר 503 (fail-closed, לא
    charge בלי הגדרה), אותו דפוס שסבב 286 כבר אימת בעשר האחיות. **הממצא
    האמיתי:** בדקתי אם קובץ `auth-button.js` המשותף (הסקריפט שמזריק גם
    את כפתור הכניסה וגם את קרדיט הרברנד "פותח ע״י עולם הסטארטאפים"
    בפוטר, ראה `portal/public/auth-button.js`) נטען ב-19 — `grep` על
    `<script src=` ב-`index.html` הראה רק `supabase-js`+`app.js`, בלי
    `https://more30.com/auth-button.js`. השוויתי מול שאר ההיקף: 7 מתוך
    8 האחיות החיות האחרות (17/18/21/22/24/27/28) כבר טוענות אותו סקריפט
    (grep -rl אישר). 19 היא המערכת החיה היחידה עם `index.html` אמיתי
    (לא 20/23/25, שאין להם) שהחסירה אותו — לא רברנד שגוי, אלא רברנד
    *חסר לגמרי* (אין כפתור כניסה משותף ואין קרדיט בפוטר בכלל). הוספתי
    את אותה שורת סקריפט בדיוק, באותו מיקום (לפני `</body>`), כפי שקיים
    כבר ב-21/24/27 (עם אותה הערה מסבירה שמזהירה מפני דריסה ע"י בנייה
    מחדש). **שמתי לב לממצא נוסף שלא נגעתי בו במכוון:** שורה 31 באותו
    קובץ מקשרת מהנווט אל `https://mor1-main-site-production.up.railway.app/`
    תחת הטקסט "🚀 עולם החלומות — MOR1" — דומיין Railway חיצוני, לא
    `more30.com`, ושם מותג שונה ("עולם החלומות") מהרברנד הפלטפורמה
    ("עולם הסטארטאפים"). בדקתי את `app.json`/`registry.ts` של מערכת 25
    (mor1-main-site, שאליה הקישור מתייחס): מסומנת `live:false`,
    `source:"not-vendored"`, הערה מפורשת "ריפו ריק (0 קבצים)" — אין שום
    עמוד בנוי תחת `more30.com/25` להפנות אליו במקום, כך שהפיכת הקישור
    לפנימי הייתה שוברת אותו (מפנה לדף ריק) בעוד שהקישור החיצוני כרגע
    עובד בפועל. זה שם מוצר ספציפי למערכת 25, לא כותרת/מטא של האתר עצמו
    (בניגוד לממצא 30/`ZchuyotPro` בסבב 285) — לא ברור שזה כלל טעות
    רברנד ולא שם שיווקי מכוון לפרויקט חיצוני נפרד; שינוי בלי לדעת מה
    המשתמש מתכנן למערכת 25 מסוכן יותר מהשארתו. השארתי אותו כפי שהוא,
    מתועד כאן להחלטת המשתמש. `git diff` על `index.html` מציג 3 שורות
    בלבד (סקריפט + הערה), אפס שינוי לתוכן/עיצוב קיים. קובץ כבר עוקב
    ב-git (`git log` הראה קומיט rescue קודם), נדרש `git add -f` (אותו
    דפוס חוזר). ענף חדש `fix/b-19-igud-shiurim-auth-button-credit-0820`
    (בנוי מעל `a2cb5432`), קומיט `6107ebc8`, נדחף (מפעיל פריסת Vercel
    תחת `more30.com/shiurim`). **הבא בתור:** לבדוק שוב את 20/23/25
    כשיהיה להן תוכן אמיתי (כרגע אין מה לרברנד/לתמחר שם); לחלופין להעלות
    להחלטת המשתמש את קישור ה-MOR1 החיצוני שורה 31 (השאיר/הסר/הפנה
    פנימית); או לפתוח עדשה חדשה שטרם נבדקה בהיקף הזה.

## 20/08/2026 — סבב 321 (loop B)

321. **בדקתי קודם את עדשת ה-TEST-mode pricing/subscribe שסבב 320 השאיר**
    **בתור, לפני שנגעתי בקוד — וטוב שכן.** קראתי README.md/CONNECTIONS.md,
    בדקתי `core.run_progress` (הצעד האחרון: `auth-button-credit-19-0820`,
    קומיטים `6107ebc8`/`7821704d`, כבר ה-HEAD) ו-DECISIONS.md #313-320 —
    היקף loop B מאושר 17,18,19,20,21,22,23,24,25,27,28 בלבד. סוכן Explore
    מיפה את דפוס `/subscribe?app=<slug>` המשותף (`portal/public/subscribe.html`,
    RPCs `more30_system_page`/`more30_subscribe`/`more30_checkout`,
    טבלת `core.plans`): 5 מתוך 8 המערכות החיות עם תוכן אמיתי (17/18/22/24/28)
    כבר מקושרות; 19/21/27 לא. **בדקתי לפני שהוספתי קישור** — כמעט טעיתי:
    שאילתת `core.plans`+`more30_plans()` הראתה ש-19 (shiurim) חסום
    ב-`offer_block=not_live` (live=false, החלטת פריסה של המשתמש — לא
    שלי לשנות) ו-21 (mthbram) ו-27 (mechiron) חסומים ב-`not_offered`
    (public_visible=false) **בכוונה**, מתועד במיגרציה `0033` עם guard
    שמכשיל את עצמו אם מערכת מוסתרת תחזיר מחירים — 21 היא "כפילה שלישית"
    (core.issues #94: מה שמוגש ב-27 אינו מה שנרשם, מחכה להכרעת מוצר של
    המשתמש). הוספת קישור מחירון לשם הייתה שוברת דבר שתוקן בכוונה. עדשת
    התמחור **סגורה** — כל 5 המערכות שאמורות למכור כבר מוכרות, כל 3
    שאסור להן כבר חסומות נכון. בדקתי `core.issues`/`core.project_tasks`
    לפריטים פתוחים בהיקף: היחיד שאינו חסום על החלטת משתמש (#94→#115) או
    גישה חסרה (#62, PAT לפרויקט אחר) היה כבר `fixed`/`open owner=user`.
    פתחתי עדשה חדשה במקום: `dir="ltr"` על ערכי טלפון/מייל — loop A תיקן
    את זה עשרות פעמים על 01-16, **loop B מעולם לא בדק את זה** (grep מלא
    על DECISIONS.md אישר אפס אזכורים). סוכן Explore סרק את שמונה
    המערכות עם תוכן אמיתי בהיקף (17/18/19/21/22/24/27/28 — לא 20/23/25,
    שאין להן קוד); 22/17/18/28/19 כבר נקיות (19 היא שרת Node בלבד ללא
    JSX/HTML render בהקשר הזה). מצאתי 7 מופעים אמיתיים: `21-mthbram`
    Footer.tsx:50 (טלפון בעוגן), LessonDetailModal.tsx:295/301/307
    (טלפון איש קשר + טלפון רב + מייל, כל אחד ב-`<a>` עם אייקון);
    `24-galilee-connect-hub` ReligiousInfoPage.tsx:112 (טלפון);
    `27-bkalut-price` public-landing.tsx:502/505 (טלפון+מייל בעוגני
    יצירת קשר) ו-514 (טלפון+מייל בפוטר, טקסט מעורב). אימתתי כל קובץ
    בקריאה ישירה לפני עריכה. תיקון: אותו דפוס בדיוק כמו loop A — `dir="ltr"`
    ישירות על עוגן שמכיל רק את הערך (Footer.tsx, שתי השורות ב-public-landing.tsx
    שבתוך `<p>`), או עטיפת הערך לבדו ב-`<span dir="ltr">` כשיש אייקון/טקסט
    מעורב לצידו (LessonDetailModal.tsx, ReligiousInfoPage.tsx, שורת הפוטר
    ב-public-landing.tsx). אפס שינוי לטקסט העברי/ללוגיקה/ל-href. בדקתי
    מראש את `apps/27-bkalut-price/CLAUDE.md` — `public-landing.tsx` אינו
    ברשימת הקבצים הקריטיים המוגנים (App.tsx/admin-login.tsx/routes.ts/utils.ts),
    כמו שסבב 319 כבר קבע לגבי `public-health-funds.tsx` באותה מערכת.
    `git diff --stat`: 4 קבצים, 8/8 שורות, כולן attribute/wrap בלבד.
    בדיקת איזון סוגריים ב-python על כל ארבעת הקבצים — תקין. שלושת
    התיקיות (`21-mthbram/src`, `24-galilee-connect-hub/src`,
    `27-bkalut-price/client`) תואמות דפוס `.gitignore` אך `git add`
    הרגיל (בלי `-f`) עבד הפעם כי הקבצים כבר עקובים — השונה מרוב הסבבים
    הקודמים. אין build/dev-server לפי הנחיית ההרצה. ענף חדש
    `fix/b-24-27-21-dir-ltr-phone-email-0820`, קומיט `d7cf1467`, נדחף
    (מפעיל פריסות Vercel תחת `more30.com/mthbram`, `more30.com/galil`,
    `more30.com/mechiron`). **הבא בתור:** אין מועמדים נוספים לעדשת
    dir=ltr בהיקף הזה (17/18/19/22/28 כבר נקיים); לפתוח עדשה חדשה שטרם
    נבדקה, או לחזור על הבדיקה התקופתית של #62/#94 למקרה שהמשתמש הכריע.

## 20/08/2026 — סבב 322 (loop B)

322. **פתחתי עדשה חדשה שטרם נבדקה: `target="_blank"` בלי `rel="noopener"`**
    **על קישורים חיצוניים (reverse-tabnabbing).** קראתי README.md/
    CONNECTIONS.md, בדקתי `core.run_progress` (הצעד האחרון:
    `dir-ltr-phone-email-21-24-27-0820`, קומיט `d7cf1467`, כבר ה-HEAD)
    ו-DECISIONS.md #313-321 — היקף loop B מאושר
    17,18,19,20,21,22,23,24,25,27,28 בלבד. לא נגעתי ב-26/29/30/31/32-38.
    התחלתי מ-19-igud-shiurim-portal (`public/app.js`, קובץ template-string
    וניל-JS עם `href="${esc(...)}"` על שורה אחת — קל לאמת ידנית בלי
    עמימות multi-line JSX). grep על `target="_blank"` בקובץ מצא 14 מופעים:
    7 קישורי `#/...` פנימיים (hash routes של האפליקציה עצמה — אותו origin,
    לא רלוונטי ל-tabnabbing), ו-7 קישורים חיצוניים אמיתיים (`link_url`/
    `website_url`/`file_url`). מתוכם 5 (`link_url`/`website_url`, שורות
    409/469/919/977/1001/1044/1090) **כבר** עם `rel="noopener"` — תקדים
    ברור בקובץ עצמו. **2 חריגים בלבד, שני המקרים היחידים של `file_url`**
    (שורות 903 ו-1293, קובץ מצורף/עלון שהועלה לשירות קהילה בעמוד הציבורי
    ובממשק הניהול) — חסרי `rel` לגמרי, בניגוד לתקדים הקיים לאותה קטגוריית
    קישור (URL חיצוני שמקורו בקלט משתמש/אחסון). זו פרצת אבטחה אמיתית: דף
    שנפתח ב-`target="_blank"` בלי `rel="noopener"` מקבל גישה ל-`window.opener`
    ויכול להפנות מחדש את הכרטיסייה המקורית (reverse tabnabbing) — במקרה
    הזה, ה-URL מגיע מקלט משתמש (מנהל קהילה שמעלה קובץ), אז אינו נשלט
    ע"י הפלטפורמה. **התיקון:** הוספתי `rel="noopener"` לשני המופעים, אותו
    ערך בדיוק כמו חמשת התקדימים בקובץ (לא `noreferrer` — שמרתי על העקביות
    הקיימת בקובץ עצמו ולא המצאתי מוסכמה חדשה). שינוי attribute יחיד לכל
    שורה, אפס שינוי לטקסט/לוגיקה/href. `node -e` אימת איזון סוגריים/
    מסולסלים/מרובעים על הקובץ המלא (1549/1549, 930/930, 77/77) — תקין.
    בדקתי את שאר שבע המערכות עם קוד אמיתי (17/18/21/22/24/27/28) לאותה
    עדשה — grep מצא עשרות מופעי `target="_blank"` אך רובם JSX
    רב-שורתי שבו `rel` עלול להופיע בשורה נפרדת (לא ניתן לאמת בביטחון
    ב-grep חד-שורתי בלי לקרוא כל קובץ בנפרד) — **לא ניחשתי, השארתי לסבב
    הבא** עם קריאה מלאה קובץ-אחר-קובץ, בדיוק כמו המשמעת שסבבים 315-319
    שמרו על עצמם (קובץ אחד בטוח לצעד, לא ניסיון למצות הכל בבת אחת).
    קובץ תחת `.gitignore` (`apps/19-igud-shiurim-portal/public`), נדרש
    `git add -f` (התקדים החוזר). אין build/dev-server בסבב הזה לפי
    הנחיית ההרצה. ענף חדש `fix/b-19-file-url-rel-noopener-0820`, קומיט
    `f23ce3af`, נדחף (מפעיל פריסת Vercel תחת `more30.com/shiurim`).
    **הבא בתור:** להמשיך את עדשת ה-`rel="noopener"` על שאר 17/18/21/22/
    24/27/28 בקריאה מלאה קובץ-אחר-קובץ (JSX רב-שורתי, לא grep בלבד),
    להתמקד בקישורים שמקורם בקלט משתמש/נתוני DB חיצוניים (לא קישורים
    קשיחים ל-domains מוכרים כמו github/supabase שכבר עם `rel="noreferrer"`
    בכל מקום שנבדק ב-27).

## 20/08/2026 — סבב 323 (loop B)

323. **סגרתי את עדשת ה-`rel="noopener"` שהושארה פתוחה בסבב 322 על**
    **17/18/21/22/24/27/28 — התברר שהיא כבר ממוצה: כל 30 המופעים**
    **בהיקף כבר נושאים `rel="noopener"` ו/או `rel="noreferrer"`.**
    קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress` (הצעד
    האחרון: `rel-noopener-target-blank-19-0820`, קומיטים `f23ce3af`/
    `034f853d`, כבר ה-HEAD) ו-DECISIONS.md #313-322 — היקף loop B
    מאושר 17,18,19,20,21,22,23,24,25,27,28 בלבד, לא נגעתי ב-26/29/
    30/31/32-38. הרצתי סוכן Explore על שבע האפליקציות (17/18/21/22/
    24/27/28) לאתר כל `target="_blank"` בקריאת קובץ מלאה (לא grep
    חד-שורתי, בדיוק כמו שסבב 322 ביקש). התוצאה: 30 מופעים, **כולם**
    עם `rel="noopener"` ו/או `rel="noreferrer"` על אותו אלמנט — 19
    כבר עם שניהם, 6 עם `noopener` בלבד (בעיקר `wa.me`/`nedar.im`
    ב-21), 3 עם `noreferrer` בלבד (`renderWithLinks()` המשותף ב-27,
    banner/whatsapp ב-24). לפני שתייגתי את השלישייה האחרונה כ"פרצה
    חלקית" בדקתי בפועל: `rel="noreferrer"` **לבדו** כבר חוסם גישה
    ל-`window.opener` בכל דפדפן מודרני (אותה התנהגות שה-spec איחד עם
    `noopener` מזה כמה שנים) — כלומר אין שום מופע בהיקף הזה שבאמת חשוף
    ל-reverse-tabnabbing; ההבדל בין שלוש הקטגוריות הוא קוסמטי (אילו
    שתי מילים נכתבו), לא פער אבטחה. **לא עשיתי שינוי קוסמטי-בלבד** (אין
    צורך אמיתי לאחד ל-`noopener noreferrer` בכל מקום כשההגנה כבר קיימת)
    — זה היה סתם רעש בדיוק מהסוג שהכללים מבקשים להימנע ממנו ("אל תוסיף
    מורכבות מעבר לנדרש"). עדשת ה-`rel="noopener"` **נסגרת רשמית** על
    פני כל היקף loop B (19 מסבב 322 + 17/18/21/22/24/27/28 מהסבב הזה).
    **פתחתי עדשה חלופית: בדקתי סורק סטטי קיים בעץ, `scripts/qa/
    untracked-imports.mjs`, שלא רץ אף פעם על 17-28 (רק על 01-16/30/31
    לפי DECISIONS.md).** הרצתי אותו על כל 11 המערכות בהיקף (דורש
    `GIT_EXE=$(which git)` כי ברירת המחדל בקובץ היא נתיב Windows קשיח
    — לא נגעתי בקובץ עצמו, רק ב-env var של ההרצה). 10 מתוך 11 נקיות
    לגמרי. **מצאתי אחת אמיתית: `apps/17-chizukim-transcribe/api/
    server.js` מייבא `../dist/index.cjs` שלא קיים על הדיסק.** אימתתי
    שזה לא false-positive של קובץ build-output-צפוי: קראתי את הקובץ
    עצמו (Vercel serverless wrapper, מייבא `registerRoutes` מ-`dist/
    index.cjs`), את `vercel.json` (`functions`/`routes` מפנים **אך ורק**
    ל-`api/handler.js`, לא ל-`api/server.js` בכלל — אין שום נתיב
    שמגיע אליו בייצור), ואת `script/build-vercel.ts` (הפקודה ש-
    `vercel.json` באמת מריץ — `esbuild` מ-`server/serverless.ts`
    ישירות לתוך `api/handler.js`, **לא** דרך `dist/index.cjs` — זה
    לעומת `script/build.ts` הרגיל, שכן יוצר `dist/index.cjs` אבל
    לא רץ ב-Vercel). `git log --follow` על שלושת הקבצים (`server.js`/
    `handler.js`/`vercel.json`) מראה ששלושתם נכנסו יחד בקומיט ה-
    "rescue" ההיסטורי היחיד — אבל `handler.js` המשיך לקבל עדכוני
    פיצ'רים אמיתיים אחר-כך (`a212af49` מסך ניהול, `8b797672` קישור
    מחירון), בעוד `server.js` לא נגע בו איש מאז. מסקנה: `server.js`
    הוא שריד מת מגישת-wrapper מוקדמת שהוחלפה ב-`handler.js` — לא
    מגיע אליו שום route בייצור, ואם מישהו כן יקרא לו ידנית הוא יקרוס
    (import חסר) כי `dist/` אף פעם לא נבנה בפועל ב-Vercel. **התיקון:**
    מחקתי את `apps/17-chizukim-transcribe/api/server.js`. `grep` שיטתי
    על כל האפליקציה (json/ts/js/md) מאשר אפס אזכורים נותרים לשם הקובץ;
    הרצתי מחדש את `untracked-imports.mjs` על 17 — אפס ייבוא חסר. זו
    לא הפרת "אל תמחק פיצ'ר/מסך/כפתור" — זה קובץ תשתית שמעולם לא היה
    מחובר לשום route חי, לא מסך/פיצ'ר משתמש. `git status` נקי מלבד
    המחיקה עצמה (שני קבצי `QA/platform/_*.json` שהסורקים כתבו מחדש
    שוחזרו ל-`git checkout --`, לא חלק מהתיקון). קומיט `59a53be0` על
    ענף חדש `fix/b-17-remove-dead-api-server-0820`, נדחף (מפעיל פריסת
    Vercel תחת `more30.com/chizukim`, ללא שינוי בפועל כי הקובץ לא היה
    בנתיב הפריסה מלכתחילה). **הבא בתור:** להריץ עוד סורקים סטטיים
    שלא נבדקו על 17-28 (`hardcoded-stats.mjs`/`mock-data.mjs` כבר
    נבדקו הסבב הזה — שניהם נקיים, כל הממצאים היו טמפלטים/דוגמאות CSV
    מוצהרות או `loud:false`), או `stray-links.mjs`/`dead-controls.mjs`
    (דורשים `playwright-core`, לא מותקן בעץ הזה — נדרשת החלטה אם
    להתקין או לדלג).

## 20/08/2026 — סבב 324 (loop B)

324. **דחיתי את `stray-links.mjs`/`dead-controls.mjs` (דורשים
    `playwright-core`, וההרצה הזו אסורה בהתקנות שממתינות) ובמקום זאת
    הרצתי שבעה סורקים סטטיים/דינמיים נוספים שמעולם לא רצו על היקף
    loop B (17-28), לפי grep על DECISIONS.md ששמות הקבצים שלהם לא
    הופיעו אף פעם.** קראתי README.md/CONNECTIONS.md, בדקתי
    `core.run_progress` (הצעד האחרון `remove-dead-api-server-17-0820`,
    קומיט `171eff80`, כבר HEAD) ו-`core.issues` להיקף — כל הפריטים
    הפתוחים בהיקף (21 synagogues RLS #250, 22 savings-calculator #242,
    22 leads-webhook #169, 22 uri_allow_list #206) חסומים על גישת
    dashboard/PAT חיצונית שאין לסבב הזה, לא צעד קוד זמין.
    **`secret-scan.mjs`** (סריקת credentials מסוג assignment/fallback/doc,
    לא רק prefix-match) לא נתמך בכלל ב-env הזה — `GIT` היה קשיח לנתיב
    Windows בלי fallback ל-`process.env.GIT_EXE` (בניגוד ל-
    `untracked-imports.mjs` שכבר תומך בזה). הוספתי את אותו fallback
    בדיוק (שורה אחת, אותה מוסכמה), הרצתי על כל 11 האפליקציות בהיקף —
    **אפס ממצאים חדשים**, רק דלף ידוע קיים (`core.issues #88`,
    27-bkalut-price/server/auth.ts, כבר מתועד ולא ניתן לתיקון כאן כי
    זה קובץ מוגן תחת bkalut-app). הרצתי גם בלי scope (כל הריפו) לוודא
    שההרחבה לא שברה שום דבר — תקין. **שאר הסורקים רצו על כל הריפו
    (הם לא תומכים ב-scope לפי-אפליקציה, וזה בסדר כי כולם read-only):**
    `password-reveal-scan.mjs` (10 שדות סיסמה, כולם OK, אפס MISSING),
    `pii-log-scan.mjs` (130 קבצים, אפס לוגים של אובייקט שלם), `mojibake-
    scan.mjs` (3110 קבצי מקור + כל ה-`_deploy` HTML, אפס עברית מקודדת
    כפולה), `billing-safety.mjs` (מול production בפועל — anon לא יכול
    לקרוא ל-`more30_checkout`/`more30_subscribe` או לקרוא
    `billing_settings`/`plans`, דף הלקוח לא חושף שכבת מנהל, 6/6 עבר),
    `trailing-slash-audit.mjs` (27/27 mounts — `/<x>` ו-`/<x>/` מגישים
    את אותו תוכן, אף אחד לא נופל לפורטל), `relative-asset-audit.mjs`
    (27/27 mounts פותרים CSS/JS נכון בשתי צורות ה-URL). שני סורקים
    כן מצאו פער אמיתי — **אבל שניהם מחוץ להיקף loop B, לא נגעתי:**
    `vite-base-audit.mjs` מצא ש-`26-modaot-studio` (loop C) מוגדר
    ב-`base: /studio/` בעוד שהוא מורכב תחת `/modaot/` — התאמה שגויה
    אמיתית, אבל מספר 26 שייך במפורש ל-loop C, לא ל-B. `sitemap-covers-
    indexable-pages.mjs` מצא ש-`sitemap.xml` חסר שני URLs
    (`/bkalot-studio`, `/gannenet`) — `/gannenet` הוא מערכת 40 (loop A),
    `/bkalot-studio` שייך למשפחת bkalut-app/08 המוגנת — שניהם מחוץ
    להיקף. תיעדתי את שתי התגליות כאן כדי שהלולאה הרלוונטית תיתקל בהן,
    לא פעלתי עליהן. **בדקתי גם את שלוש המערכות בהיקף שמעולם לא היו
    בעלות URL חי — 20-igud-portal, 23-haorech-torani, 25-mor1-main-site
    — כדי לבדוק אם דפוס ה"חסר auth-button.js/rebrand credit" שתוקן
    ב-19 (סבב 320) חוזר גם שם.** 23 ו-25 הם שלד/ריק אמיתי (index.html
    בודד או רק app.json, בלי build/deploy). **20-igud-portal יש לו קוד
    אמיתי** (Express `server.js` + `public/index.html`/`app.js`/
    `styles.css`, קורא ל-Supabase RPCs אמיתיים) — אבל בניגוד ל-19,
    grep מלא על `portal/vercel.dist.json` ו-`_deploy/` מאשר שאין שום
    rewrite/mount עבור `/igud` — המערכת מעולם לא חוברה לפורטל ואין לה
    URL ציבורי לבדוק מולו. `core.plans` כן מכיל שלוש שורות תמחור
    ל-`app_key='igud'` (`charge`/`one_time`/`pro (בדיקה)` ב-1 ש"ח TEST
    mode, תואם למוסכמה הפלטפורמה) אך `customer_visible=false` בכולן —
    כלומר זו החלטת מוצר מכוונת (לא באג) לא להציג עדיין, כי אין דף
    ציבורי שיציג אליו. לא הוספתי סקריפט מותג לדף שאין לו נתיב פריסה —
    זה היה שינוי שלא ניתן לאמת נגד שום דבר אמיתי. **התוצאה נטו של
    הסבב: תיקון תשתית אמיתי אחד (`secret-scan.mjs` GIT_EXE), אימות
    חיובי רחב (9 עדשות, 11 אפליקציות בהיקף + כל הפלטפורמה לבדיקות
    שלא תומכות ב-scope) שההיקף נקי, ותיעוד משתי תגליות אמיתיות שלא
    שייכות ללולאה הזו.** קומיט `a55ca62b` על ענף חדש
    `fix/b-secret-scan-git-exe-lens-sweep-0820` (המשך מ-`171eff80`),
    נדחף (אין שינוי לאפליקציה חיה — הקובץ הוא כלי QA בלבד, לא חלק
    מפריסת אף מערכת). **הבא בתור:** אם `playwright-core` יותקן אי-פעם,
    `stray-links.mjs`/`dead-controls.mjs` הם העדשות היחידות שנותרו לא
    נבדקות על 17-28; אחרת לפתוח עדשה חדשה, או לחזור על הבדיקה מעת לעת.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 325 (loop B)

325. **פתחתי שלוש עדשות נוספות שמעולם לא רצו על היקף loop B, לפי grep על**
    **DECISIONS.md.** קראתי README.md/CONNECTIONS.md, בדקתי `core.run_progress`
    (הצעד האחרון `secret-scan-git-exe-lens-sweep-0820`, קומיט `a55ca62b`, כבר
    HEAD) ו-`core.issues`/`core.project_tasks` הפתוחים בהיקף — כל הפריטים
    עדיין חסומים על גישת dashboard/PAT חיצונית (#169/#62/#115/#242/#206/
    #250), אין צעד קוד זמין דרכם. `pricing-reflects.mjs` (chizukim/orech/
    shiurim/igud/mthbram/zchuyot/galil/mechiron/kupot) — 8/8 עם `app_key`
    מזוהה, שלוש "נכשלות" (shiurim/igud/mechiron אין תוכניות, מthbram אין
    תוכניות) התבררו כולן כבר-נבדקות בסבב 321: לא-חיה (igud) או `not_offered`
    בכוונה (shiurim/mthbram/mechiron, מיגרציה 0033) — לא ממצא חדש.
    `credit-duplicate-sweep.mjs` על שמונת הכתובות החיות — 0/8 עם קרדיט
    כפול, אבל **חשף עובדה שלא הייתה בתיעוד**: `https://more30.com/shiurim`
    (19) מחזיר 404 בפרודקשן, בזמן שכל שאר שבע האחיות מחזירות 200.
326. **חקרתי את פער ה-404 של 19 עד הסוף לפני שכתבתי אותו כממצא.** `grep`
    מלא על `portal/vercel.dist.json` — אין שום rewrite ל-`/shiurim` (בניגוד
    ל-21/22/24/27/28 שכולן עם שלישיית `source`/`source/`/`source/:path*`
    ל-`https://<slug>-more30.vercel.app`). ניחשתי וניסיתי שלוש כתובות
    Vercel אפשריות (`shiurim-more30.vercel.app`, עם/בלי `/shiurim`,
    `igud-shiurim-portal.vercel.app`) — כולן 404, אין שום פריסה עצמאית
    בנמצא. `apps/19-igud-shiurim-portal/app.json`: `"live": false`,
    `"deployTarget": "unknown"` — תואם את `core.projects` (`is_deployed=
    false`, `live_url=null`), לא סתירה. קראתי את `server.js`: זה שרת
    Express-gateway שמפנה (`http-proxy-middleware`, `ws:true`) לנתיבי
    `railway.internal:8080` של שבע מערכות אחיות — ארכיטקטורה שלא מתאימה
    לפונקציה serverless של Vercel (proxy מתמשך + websockets), כך שכנראה
    שהיעד המיועד הוא Railway ולא Vercel כלל — לא הוספתי `vercel.json`
    מבוסס-ניחוש כי זה היה עלול ליצור תשתית שגויה. `vercel` CLI לא מותקן/
    מאומת בסביבה הזו (`which vercel` ריק) — אין דרך ליצור/לחבר פרויקט
    Vercel/Railway מכאן בכלל. **המסקנה: זו לא סתירה בין סבבים קודמים
    (317/320/322) לתיעוד — הם קראו וקודדו קובצי מקור, לא אימתו מול
    פרודקשן חי, וזה תקין. אבל שורת התבנית "מפעיל פריסת Vercel תחת
    more30.com/<path>" שחוזרת בכל סבב (כולל בהנחיית ההרצה עצמה) היא
    שגויה במפורש עבור 19 — אין שום נתיב פריסה שה-push הזה מפעיל.**
    תיעדתי, לא פעלתי — יצירת פרויקט Vercel/Railway היא החלטת תשתית
    שדורשת גישת dashboard שאין לסבב הזה, בדיוק כמו #62/#115 שכבר חסומים
    מאותה סיבה.
327. **הרצתי `notconfirmed-message-sweep.mjs` — 24-galil דגל "generic-only**
    **login failure message"**, אבל בדיקה הראתה שזה false-positive: מסך
    הגבאי של 24 אינו Supabase Auth בכלל (התחברות מותאמת-אישית מול
    `gabai_accounts`), כך שהעדשה של "email_not_confirmed" לא רלוונטית לו
    מלכתחילה. **חקרתי לעומק בכל זאת** אם ה-select('*') מהדפדפן על
    `gabai_accounts` (שהמסך משתמש בו בפועל, `GabaiPortal.tsx:1572/1593`)
    עובד בייצור — אימתתי עם המפתח האנונימי שחולץ מה-bundle החי
    (`more30.com/galil/assets/index-*.js`, פרויקט `mwljkonwdeuaahsigjdp`)
    ואותה שאילתה בדיוק שהמסך שולח: `42501 permission denied`. זה **כבר
    מתועד ומוכר** — `core.issues #62` (severity=critical, status=open,
    owner=agent, blocked_on="אימות בצד שרת דורש את פרויקט mwljkonwdeuaahsigjdp
    שה-PAT אינו מכסה") — אימות עצמאי בלבד, לא ממצא חדש. הרצתי גם
    `galil-gabai-accounts-exposure.mjs` (הסקריפט הייעודי לזה) מחדש —
    אותה תוצאה, `anon cannot read gabai_accounts`, לא דלף.
328. **פתחתי `registry-vs-projects.mjs` (מעולם לא רץ, משווה
    `packages/config/src/registry.ts` מול `core.projects` ומאמת נגד
    פרודקשן חי בפועל)** — מצא 4 אי-התאמות `stage` בהיקף loop B: 17/18/22
    עם `stage:"wip"` ב-registry מול `"live"` ב-DB; 21 עם `stage:"live"`
    ב-registry מול `"wip"` ב-DB (הפוך). **לפני תיקון בדקתי שלא אני
    ה"תיקון" השגוי:** שאילתת `core.projects` על מדגם רחב (01/02/04/06/
    10/12/14/30/31/32/36, מחוץ להיקף) הראתה ש-`stage="wip"` יחד עם
    `live=true`/`is_deployed=true` הוא **דפוס מכוון ונפוץ בכל הפלטפורמה**
    (לא רק ב-21/24/27/28), לא סתירה שצריך "לתקן" — `stage` הוא ציר בשלות
    נפרד מהדגלים הבינאריים. כלומר 21 היה מקרה שבו ה-registry הישן (שכתב
    "live" ותיעד ידנית שה-DB "לא מכיר") היה השגוי, לא ה-DB. תיקנתי רק את
    `packages/config/src/registry.ts`: 17/18/22 → `"live"`, 21 → `"wip"`
    (עם הערה מעודכנת שמחליפה את ההערה הישנה שכבר לא נכונה). `registry.ts`
    **אין לו אף צרכן זמן-ריצה** (`grep` מלא על `import.*registry` בכל
    הריפו — אפס תוצאות מחוץ לקובץ עצמו) — זו תיקון תיעוד טהור, אפס סיכון
    התנהגותי, לא נגעתי בשום דגל `live`/`isDeployed`/שדה אחר. אימתתי מחדש
    בהרצת הסקריפט — כל 12 השורות בהיקף loop B (17-28) עכשיו `agree`.
    בדיקת איזון סוגריים ב-python על הקובץ המלא — תקין (71/71, 49/49, 9/9).
    אין build/dev-server בסבב הזה לפי הנחיית ההרצה. ענף חדש
    `fix/b-registry-stage-drift-17-18-21-22-0820`, קומיט `9fcb2444`, נדחף
    (אין שינוי לאפליקציה חיה — קובץ config סטטי בלבד, לא נטען בזמן ריצה
    ע"י אף אפליקציה/admin/portal). **הבא בתור:** אם יתקבל אי-פעם גישת
    Vercel/Railway dashboard, לחבר את 19 לפריסה אמיתית (#62/#115 גם הם
    ממתינים לגישת dashboard חיצונית לאותה סיבה); אחרת לפתוח עדשה חדשה
    בהיקף, או לחזור על הבדיקות התקופתיות.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 326 (loop B)

326. **נקודת תקלה אמיתית ולא עוד עדשת נגישות/מיתוג: 17-chizukim-transcribe**
    **איבד שדה `duration_seconds` בנתיב ההעלאה החלופי (fallback).** ב-
    `client/src/pages/upload.tsx:submit()` יש שני נתיבי העלאה: (1) ישיר ל-
    Supabase Storage דרך `uploadUrl` חתום, שבו `duration_seconds` (שחולץ
    ב-שורה 123 עם `readDuration`, best-effort מה-`<audio>` שבדפדפן) נשלח
    כראוי ל-`/api/upload/register` (שורה 151). (2) נתיב חלופי (`catch`, שורות
    169-175) שרץ כשהנתיב הישיר נכשל (בעיית רשת/Storage) ושולח `FormData`
    ל-`POST /api/upload` — אבל בלי `duration_seconds` בכלל. אימתתי מול הצד
    השרת: `server/routes.ts:271-272` קורא בדיוק `req.body.duration_seconds`
    מאותו route (`app.post("/api/upload", ...)`) ומכניס אותו ל-`recordings.
    duration_seconds` בשורה 312 — כלומר השרת מצפה לשדה, הלקוח פשוט לא שלח
    אותו בנתיב הזה. תוצאה בפועל: כל הקלטה שעברה בנתיב החלופי (רשת חלשה/
    Storage timeout, בדיוק המצב שהנתיב הזה קיים כדי לתפוס) נשמרת עם
    `duration_seconds=null` למרות שהערך כבר חושב בדפדפן ונמצא בזיכרון
    (`duration` בסקופ הפונקציה) — שובר תצוגת משך ההקלטה ואומדן העלות
    (`server/routes.ts:460-484`, `cost = duration ? ... : 0`) לכל הקלטה
    שעברה fallback. **תיקון:** שורה אחת, `fd.append("duration_seconds",
    String(duration))` (עם guard ל-`duration != null`, כי `readDuration`
    יכול להחזיר `null` אם הדפדפן לא הצליח לקרוא metadata) — מעביר את אותו
    ערך שכבר מחושב, בדיוק כמו הנתיב הישיר. אפס שינוי להתנהגות הקיימת,
    אפס תלות חדשה. אותרה ע"י סוכן Explore שהתבקש לחפש מחוץ ל~30 העדשות
    שכבר נסגרו על ההיקף (label-for-id/rel-noopener/dir-ltr/aria-label/
    secret-scan/PII/mojibake/billing-safety/trailing-slash/relative-asset/
    sitemap/registry-drift/dead-imports/password-reveal/favicon/unknown-path/
    rebrand-credit/pricing) — אומתה ידנית מול שני קצוות הקוד (קורא לפני
    שמתקן) לפני העריכה.
    **בנוסף, בתחילת הסבב הרצתי שלושה סורקים סטטיים/רשת חדשים (לא בעבר על
    היקף loop B): `mount-icon-declarations.mjs` (47/47 עבר, אפס נתיב אייקון
    שבור ב-27 ה-mounts כולל chizukim/orech/mthbram/galil/mechiron/kupot/
    zchuyot), `favicon-mounts.mjs` (24 mounts, כל שבעת החיים בהיקף מציירים
    אייקון עצמאי, לא נופלים לסימן הפורטל), `unknown-path-serves-home.mjs`
    (48/48 עבר — אימות עצמאי ש-`/shiurim` (19) ו-`/igud` (20) מחזירים 404
    אמיתי ולא בטעות דף הבית של הפורטל, תואם את הממצא של סבב 325 הקודם
    שאין להם פריסה. גם דחיתי `mounted-api-base.mjs` מהקומיט — הריצה כאן
    תלויה בתיקיית `_deploy/` מקומית שלא קיימת בסביבה הזו, כך שהפלט שלו
    ריק/מטעה ולא עדות אמיתית על פרודקשן; לא קומיטתי את הקובץ שהוא כתב).**
    קומיט על `fix/b-17-upload-fallback-duration-0820`, נדחף.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 327 (loop B)

327. **תקלת קולזיית שדות אמיתית ב-21-mthbram: שתי בחירות עצמאיות בטופס אחד**
    **דורסות זו את זו באותו שדה DB.** ב-`apps/21-mthbram/src/components/
    portal/PortalLessonForm.tsx` יש שני חלקי ChipPicker נפרדים לגמרי מבחינת
    UI: "סגנון שיעור" (שורות 81-87 המקוריות, אפשרויות LESSON_STYLES — פורמט
    מסירה: "שיעור פרונטלי"/"חברותא"/"שיעור אונליין" וכו') ו-"סגנון" (שורות
    99-105 המקוריות, אפשרויות STYLE_OPTIONS — השקפה: "חסידי"/"ספרדי"/
    "ליטאי"/"ישיבתי"). שניהם קראו וכתבו לאותו `data.lesson_style` עם אותה
    לוגיקת split/join — כלומר בחירה בשני החלקים גם יחד מייצרת מחרוזת מעורבת
    כמו `"שיעור פרונטלי, חסידי"` שמערבבת שני צירים שונים לגמרי לתוך שדה אחד.
    **אימתתי מול שאר האפליקציה לפני שקבעתי איזה חלק "נכון":** `grep` מלא על
    `lesson_style` בכל 21 — הסכימה (`supabase/migrations/20260312184244_
    ...sql:20`) מגדירה עמודה יחידה `lesson_style TEXT DEFAULT ''`.
    `BulkLessonForm.tsx:230` (`SmartSelect label="סגנון"`,
    `styleSelectOptions = ["חסידי","ספרדי","ליטאי","ישיבתי"]`) ו-
    `AdminDashboard.tsx:1593/789` ("10. סגנון") שניהם משתמשים ב-`lesson_style`
    בדיוק לפי הסמנטיקה של STYLE_OPTIONS (השקפה) כערך יחיד — כלומר החלק
    "סגנון" בקובץ הזה כבר תואם את שאר האפליקציה, והחלק "סגנון שיעור" הוא
    הפולש שדורס אותו. (הערה צדדית: `UpdateLesson.tsx` יש לו עוד קונפליקט
    סמנטי טרום-קיים ברמת האפליקציה — משתמש ב-`lesson_style` לפורמט-מסירה,
    לא להשקפה — לא נגעתי בזה, זה טופס נפרד עם התנהגות עצמאית פנימית עקבית,
    לא קולזיה בתוך אותו טופס כמו הבאג שתיקנתי.) **תיקון:** החלק "סגנון
    שיעור" עבר לאחסן בתוך `submitter_notes` עם prefix `"אופי השיעור: "`,
    בדיוק אותו דפוס שכבר קיים באותו קובץ שלוש שורות למטה עבור "סגנון דיבור"
    (SPEAKING_STYLES, שכבר לא נכתב ל-DB column ייעודי אלא ל-notes) —
    הכללתי אותו דפוס יחיד-ערך לגרסה רב-ערכית (רשימה מופרדת בפסיקים בתוך
    ה-prefix, כמו lesson_style עצמו). לא נדרשה מיגרציית DB (וגם לא הייתה
    אפשרית — `aypsqqvfohekxxuqsmrw`, פרויקט Supabase העצמאי של 21, לא
    נגיש ל-MCP בסביבה הזו, `core.issues #250`). אפס שינוי להתנהגות החלק
    "סגנון" (עדיין כותב ל-`lesson_style`, תואם Bulk/Admin), אפס שינוי
    להתנהגות "סגנון דיבור" הקיימת. איתור: סוכן Explore שהתבקש למצוא קטגוריית
    באג חדשה (לא מהעדשות הסגורות — נגישות/אבטחה/QA-סורקים/registry/pricing)
    מצא זאת; אימתתי ידנית מול הסכימה וכל שאר קוראי/כותבי `lesson_style`
    ב-21 (10+ מיקומים) לפני העריכה, לא רק את שני הקטעים הסמוכים. בדיקת
    איזון סוגריים ב-python על הקובץ המלא אחרי העריכה — תקין (112/112,
    138/138, 47/47). אין build/dev-server זמין בסביבה הזו לפי הנחיית
    ההרצה — לא tsc. ענף חדש `fix/b-21-lesson-style-field-collision-0820`,
    קומיט הבא. **הבא בתור:** לפתוח עדשת-קטגוריה נוספת (state/race/pagination/
    RPC-param) על שאר ההיקף (17/18/19/20/22/23/24/25/27/28) בהמשך לאותה
    שיטה שמצאה את הבאג הזה ואת באג ה-fallback duration של 17 בסבב 326.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 328 (loop B)

328. **תיעוד בדיעבד (retroactive) לשני קומיטים שכבר נדחפו ללא רישום ב-**
    **DECISIONS.md** — `a35fbcb0` ו-`215de802` תיקנו שני באגים אמיתיים אבל
    לא כללו עדכון לקובץ הזה, שובר את הרצף שכל סבב קודם שמר עליו. מתעד כאן
    כדי שההיסטוריה תישאר שלמה:
    - **22-get-your-rights (`FloatingBot.tsx`):** טופס "בדיקה מקיפה" שלח
      תמיד `spouse_name`/`spouse_id`/`spouse_health`/`spouse_employment` מתוך
      `compAnswers`, בלי תלות ב-`marital_status` הנוכחי, וגם תקציר הפרטים
      החופשי עבר על כל הקבוצות במקום רק הפעילות. משתמש שמילא שדות בן/בת-זוג
      תחת "נשוי" ואז החליף ל"לא נשוי" לפני השליחה — עדיין שלח את הערכים
      הישנים. תוקן כך שהשליחה משקפת את `isMarried` הנוכחי, תואם את השערים
      שכבר קיימים ב-`RightsCategories.tsx`. ענף `fix/b-22-floatingbot-stale-spouse-data-0820`.
    - **27-bkalut-price (`service-form.tsx`):** `categoryTopics`/
      `additionalTopics` נבנו עם מיפוי ל-`{id,topic,category}` בלבד, מאבד את
      `subCategory` אף שהוא קיים בכל שורת מקור (`PublicRightRow`).
      `server/routes.ts:568` כבר קורא `t.subCategory` לכל נושא נוסף שנשלח
      ל-webhook `NEDARIM3873` — כך שכל נושא נוסף נשלח עם `subCategory=""`
      בפועל, בלי קשר לערך האמיתי. תוקן ע"י השחלת `subCategory` דרך
      `categoryTopics`, טיפוס ה-state של `additionalTopics`, ו-
      `toggleAdditionalTopic`. ענף `fix/b-27-additional-topics-subcategory-0820`.

    **בנוסף, המשך העדשה שנפתחה בסבב 327 (state/race/pagination/RPC-param) —**
    **הרצתי סוכן Explore על שאר ההיקף (24/18/19/20/23/25/28), שמצא ואימתתי:**
    `apps/24-galilee-connect-hub/src/hooks/useSynagogues.ts` מריץ 5 שאילתות
    Supabase במקביל דרך `Promise.all` (synagogues/gabbaim/prayer_times/
    lessons/announcements) אבל בודק `.error` רק על התוצאה הראשונה
    (`synRes.error`, שורה 66 המקורית) — אם אחת מארבע השאילתות המשניות
    נכשלת (רשת/RLS/timeout), ה-`.data` שלה `undefined`, ונופל בשקט ל-`[]`
    (`(gabRes.data || [])` וכו', שורות 82-91), כך שבתי כנסת מוצגים בלי גבאים/
    זמני תפילה/שיעורים/הודעות בלי שום סימן שמשהו נכשל. **תיקון ראשוני
    שנבדק ונדחה:** `throw` על כל שגיאה (כמו ל-`synRes`) — נבדק מול כל 7
    הצרכנים של ה-hook (`Index.tsx`, `AllSynagoguesPage.tsx`,
    `SynagoguePage.tsx`, `AnnouncementsSection.tsx`, `LessonsTicker.tsx`,
    `PrayersTicker.tsx`, `UpcomingPrayers.tsx`) — אף אחד מהם לא קורא את שדה
    ה-`error` המוחזר, כך שזריקה הייתה משאירה את `synagogues` ריק (אין
    `setSynagogues` בנתיב ה-`catch`) ומעלימה את **כל** רשימת בתי הכנסת
    התקינה בגלל כשל בטבלת-משנה אחת — הרעה אמיתית מ"תצוגה חלקית" ל"מסך ריק",
    בניגוד לכלל האפס-נסיגה. **התיקון הסופי:** במקום `throw`, איסוף שגיאות
    ארבע השאילתות המשניות ל-`console.error` לצורך אבחון, בעוד ההרכבה
    (`assembled`) וה-`setSynagogues` ממשיכים לרוץ כרגיל עם הנפילה הקיימת
    ל-`[]` — משמר את ההתנהגות הנראית לעין (התראה שקטה הופכת לגלויה ביומן,
    לא נעלמת ולא שוברת את הרשימה). בדיקת איזון סוגריים ב-python על הקובץ
    המלא — תקין (51/51, 22/22, 18/18). אין build/dev-server בסבב הזה לפי
    הנחיית ההרצה. ענף חדש `fix/b-24-usesynagogues-partial-error-0820`,
    קומיט הבא. **הבא בתור:** להמשיך את אותה עדשה (state/race/pagination/
    RPC-param) על 17/19/21/22/25/27/28 שעדיין לא נסרקו בה במלואם.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 331 (loop B)

331. **תיעוד בדיעבד (retroactive) לשני קומיטים שכבר נדחפו ללא רישום ב-**
    **DECISIONS.md** — `77e85a18` (24-galilee-connect-hub, סבב 329) ו-
    `92de30f5` (18-torah-editor-mvp, סבב 330) כבר תועדו במלואם בהערות
    `core.run_progress` (ראו הפעימות שם) אך לא הועתקו לקובץ הזה — מתעד
    כאן רק את הפניה כדי לשמר את הרצף, בלי לשכפל את הטקסט המלא.

    **הצעד של הסבב הזה (חדש):** עדשת ה-state/race/pagination/RPC-param
    מוצתה על פני כל היקף Loop B (17-31 בתוך ההיקף המוגדר) לפי הערת הפעימה
    האחרונה. פתחתי עדשה חדשה: **double-submit/duplicate-write** — כפתור
    שמריץ כתיבה אסינכרונית (insert/RPC/POST) בלי הגנת "כבר בתהליך" (לא
    `disabled` מחובר ל-state טעינה, ולא בדיקת in-flight בתחילת ה-handler),
    כך שלחיצה כפולה מהירה יוצרת שורה כפולה במסד. סוכן Explore סרק את כל
    11 האפליקציות שבהיקף ומצא: 17/18/22/24/28 כבר מוגנות כראוי (state
    `submitting`/`saving`/`busy`/`isSubmitting` או React Hook Form),
    20/23/25 בלי מקור אמיתי לבדוק. נמצאו שני מקרים אמיתיים באותו קובץ:
    `apps/21-mthbram/src/pages/AdminDashboard.tsx` — כפתורי "צור קישור"
    ליצירת `rabbi_portals` ו-`org_portals` (שורות ~996 ו-~1101 לפני התיקון)
    קראו ל-`supabase.from(...).insert(...)` תוך `await` בלי שום `disabled`
    על הכפתור ובלי דגל in-flight — לחיצה כפולה מהירה יוצרת שני קישורי
    ניהול (עם access_token נפרד לכל אחד) מלחיצה אחת. (מועמד שלישי, 27
    community-admin.tsx `createQuestionnaire`, נותר לסבב הבא — לא באותו
    קובץ, נשמר כפריט תור נפרד כדי לשמור את הסבב הזה ממוקד לתיקון אחד.)

    **התיקון:** הוספתי שני דגלי state חדשים, `creatingRabbi`/`creatingOrg`,
    לפי אותו דפוס בדיוק שכבר קיים בקובץ הזה (`importing`, המשמש להגנת
    ייבוא-קובץ) — מחוברים ל-`disabled` על שני הכפתורים, ונבדקים בתחילת
    ה-handler (`if (!name.trim() || creatingX) return;`), עם `try/finally`
    כדי לאפס את הדגל גם אם ה-insert נכשל. אפס שינוי להתנהגות הקיימת מעבר
    לחסימת הלחיצה הכפולה — אין שינוי ל-DB/RLS/RPC, אין שינוי לצורת השורה
    שנוצרת. בדיקת איזון סוגריים ב-python על הקובץ המלא אחרי העריכה —
    תקין (816/816, 747/747, 137/137). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-21-double-submit-portal-create-0820`, קומיט `9b6793bc`, נדחף
    (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** לסגור את המועמד השלישי שנמצא באותה עדשה —
    `apps/27-bkalut-price/client/src/pages/community-admin.tsx`
    `createQuestionnaire()` (שורה ~223) — כפתור "יצירת שאלון" בלי הגנת
    in-flight, אותה קטגוריית באג. אחרי זה, לפתוח עדשה חדשה נוספת אם
    ההיקף המלא של double-submit ייסגר.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 332 (loop B)

332. **סגירת המועמד השלישי של עדשת double-submit/duplicate-write (מסבב 331):**
    `apps/27-bkalut-price/client/src/pages/community-admin.tsx`
    `createQuestionnaire()` (שורה 102) קרא `apiRequest("POST", ...)` תוך
    `await` בלי שום `disabled` על כפתור "יצירת שאלון" (שורה 223) ובלי דגל
    in-flight בתחילת הפונקציה — לחיצה כפולה מהירה יוצרת שני שאלונים זהים
    מלחיצה אחת. תוקן באותו דפוס בדיוק שכבר קיים בקובץ הזה (`savingSettings`
    ב-`saveSettings`) וששימש לתיקון המקביל ב-21-mthbram (סבב 331): דגל
    state חדש `creatingQuestionnaire`, נבדק בתחילת ה-handler
    (`if (!newTitle.trim() || creatingQuestionnaire) return;`), מוצמד ל-
    `disabled` על הכפתור, עם `try/finally` לאיפוס הדגל גם בנתיב השגיאה.
    אפס שינוי להתנהגות הקיימת מעבר לחסימת הלחיצה הכפולה — אין שינוי ל-API/
    DB/צורת הבקשה. בדיקת איזון סוגריים ב-python על הקובץ המלא אחרי העריכה
    — תקין (259/259, 276/276, 28/28). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-27-community-admin-double-submit-0820`, קומיט `e327921f`, נדחף
    (מפעיל פריסת Vercel תחת more30.com/bkalut).

    **הבא בתור:** עדשת double-submit/duplicate-write ממוצה כעת על כל
    היקף Loop B (17-31 בתוך ההיקף המוגדר: כל 11 המקרים שנמצאו — 5 כבר
    מוגנים כראוי, 3 בלי מקור אמיתי לבדוק, ו-3 תוקנו לאורך סבבים 331-332).
    לפתוח עדשה חדשה בסבב הבא.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 333 (loop B)

333. **פתחתי עדשה חדשה כפי שהוצע בסוף הסבב הקודם: silent-failure UX —**
    **כפתור שמריץ פעולה אסינכרונית אבל מציג הצלחה בלי שהפעולה בוצעה בפועל.**
    סוכן Explore סרק את שמונת האפליקציות בעלות מקור אמיתי בהיקף (17/18/19/
    21/22/24/27/28) וחזר נקי מכולן חוץ מאחת. הממצא: `apps/21-mthbram/src/
    components/questionnaire/TeacherForm.tsx` ו-`SeekerForm.tsx` (שני
    קבצים, אותו דפוס) — `handleSubmit()` בשניהם היה `toast.success(...)`
    בלבד + `console.log`, בלי שום קריאת רשת. משתמש שממלא שאלון רב-שלבי
    (פרטים אישיים, נושאים, סגנון, קהל יעד, זמנים) ולוחץ "שליחת הטופס"/
    "שליחת הבקשה" רואה הודעת הצלחה — אבל הנתונים לא נשמרים בשום מקום.
    **אימתתי בעצמי לפני התיקון:** דרך `App.tsx` גיליתי ש-`pages/
    Questionnaire.tsx` (ההורה של שני הקבצים האלה) **אינו רשום באף `<Route>`**
    — כלומר הדף הזה לא נגיש היום למשתמש אמיתי דרך שום ניווט/קישור בפועל
    (grep מלא על "questionnaire"/"Questionnaire" בכל האפליקציה אישר: אין
    הפניה חיצונית לנתיב הזה). הזרימות החיות המקבילות באותה אפליקציה בדיוק —
    `TeachersLanding.tsx` (`/teachers`) ו-`RequestLesson.tsx`
    (`/request-lesson`) — **כן** כותבות בהצלחה ל-`teacher_leads`/
    `seeker_leads` (אימתתי מול `integrations/supabase/types.ts` את הסכימה
    המדויקת של שתי הטבלאות). למרות שהדף לא מקושר כרגע, זו עדיין תקלה
    אמיתית שכדאי לסגור מראש: קובץ מת יכול להתעורר (קישור חדש, A/B, ניווט
    עתידי) ואז יגרום לאובדן נתונים שקט לגמרי — בדיוק התבנית ש"אפס נסיגה"
    נועדה למנוע. **התיקון:** החלפתי את שני ה-`handleSubmit` בקריאות
    `supabase.from("teacher_leads"|"seeker_leads").insert(...)` אמיתיות,
    עם מיפוי שדות זהה בדיוק לדפוס הקיים כבר ב-`TeachersLanding.tsx`/
    `RequestLesson.tsx` (עמודות ממודלות + צבירת שאר השדות ל-`notes` כטקסט
    חופשי מתויג). הוספתי גם דגל `submitting` (מונע double-submit, אותו
    דפוס שתוקן בקבצים אחרים באותה אפליקציה בסבב 331) עם `disabled` על
    הכפתור וטקסט "שולח..." בזמן הבקשה, ו-`toast.error` אמיתי בנתיב הכשל
    במקום ה-`toast.success` הקבוע-מראש. לא הוספתי route חדש ל-`App.tsx` —
    קישור הדף לניווט הוא החלטת מוצר נפרדת, לא באג, ומחוץ להיקף של תיקון
    אחד ממוקד. אפס רגרסיה: שני הקבצים עדיין לא נגישים משום מקום, כך
    שאין שום שינוי להתנהגות הנראית למשתמש קיים; זה תיקון מונע-נזק-עתידי
    טהור. בדיקת איזון סוגריים ב-python על שני הקבצים המלאים אחרי העריכה —
    תקין (TeacherForm: 103/103, 112/112, 29/29; SeekerForm: 136/136,
    160/160, 37/37). אין build/dev-server זמין בסביבה הזו לפי הנחיית
    ההרצה. ענף חדש `fix/b-21-questionnaire-fake-submit-0820`, קומיט הבא.
    **הבא בתור:** להמשיך את עדשת silent-failure UX על שאר ההיקף שלא נבדק
    (20/23/25 — ללא מקור אמיתי/doc-only, אושר בעבר), או לפתוח עדשה חדשה
    נוספת אם העדשה הזו מוצתה על כל האפליקציות בעלות מקור אמיתי.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 334 (loop B)

334. **פתחתי עדשה חדשה כפי שהוצע בסוף הסבב הקודם (עדשת silent-failure UX**
    **מוצתה): מחיקה הרסנית בלי אישור** — כפתור שמריץ `.delete()`/DELETE
    ישירות ב-onClick בלי שום שלב אישור (`confirm`, דיאלוג "בטוח?", undo).
    אימתתי קודם ב-grep על DECISIONS.md שהעדשה הזו טרם נבדקה בשום סבב
    קודם. סוכן Explore סרק את שמונת האפליקציות בעלות מקור אמיתי
    (17/18/19/21/22/24/27/28) ומצא 7 מופעים אמיתיים: 4 ב-21-mthbram
    (`OrgPortal.tsx` removeRabbi, `PrayerTimesTab.tsx` deleteSynagogue+
    deletePrayerTime, `PortalMessagesTab.tsx` deleteMessage,
    `PortalSettingsTab.tsx` deletePhoto), 1 ב-22-get-your-rights
    (`AdminSettings.tsx` deleteKey — מפתח API), ו-2+ ב-27-bkalut-price
    (`financial.tsx` — מחיקת clients/transactions/budgets/recurring/tips
    דרך `useMutation`). וידא גם שהדפוס הנכון כבר קיים באותה אפליקציה
    בדיוק (למשל `AdminDashboard.tsx` softDeleteLesson/deleteStudyDay עם
    `if (!confirm(...)) return;`), כך שהתיקון לא ממציא UX חדש.

    **התיקון של הסבב הזה (ממוקד לקובץ אחד, שני מופעים):**
    `apps/21-mthbram/src/components/portal/PrayerTimesTab.tsx` —
    `deleteSynagogue` (מחיקת בית כנסת, עם קסקייד לוגי לזמני התפילה שלו
    ב-state) ו-`deletePrayerTime` (מחיקת זמן תפילה בודד, כפתור שמופיע
    רק ב-hover) קראו ל-`supabase.from(...).delete()` ישירות בלי שום
    שער אישור. הוספתי `if (!confirm("...")) return;` בתחילת כל handler,
    באותו דפוס בדיוק שכבר קיים ב-`AdminDashboard.tsx` של אותה אפליקציה.
    אפס שינוי להתנהגות הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי
    ל-DB/RLS/צורת השאילתה. בדיקת איזון סוגריים ב-python על הקובץ המלא
    אחרי העריכה — תקין (158/158, 149/149, 21/21). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה. ענף חדש
    `fix/b-21-prayertimestab-unconfirmed-delete-0820`, קומיט `30c48c2b`,
    נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על שאר המופעים שנמצאו —
    `OrgPortal.tsx` removeRabbi, `PortalMessagesTab.tsx` deleteMessage,
    `PortalSettingsTab.tsx` deletePhoto (כולם ב-21-mthbram), אחר-כך
    `AdminSettings.tsx` deleteKey (22-get-your-rights, מפתח API —
    עדיפות גבוהה יחסית), ואז `financial.tsx` (27-bkalut-price, 5+
    מוטציות מחיקה). כל אחד מהם מועמד לתיקון נפרד ומרוכז בסבב הבא כדי
    לשמור על היקף קטן וניתן-לאימות לכל קומיט.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 335 (loop B)

335. **המשך עדשת מחיקה-בלי-אישור, מועמד שני מהתור שנפתח בסבב 334:**
    `apps/21-mthbram/src/pages/OrgPortal.tsx` `removeRabbi` (שורה 113)
    קרא `supabase.from("org_rabbis").delete()` ישירות מתוך `onClick`
    (שורה ~322, כפתור פח-אשפה ליד כל רב ברשימה) בלי שום שער אישור —
    לחיצה אחת מסירה רב מהארגון לצמיתות. תוקן באותו דפוס בדיוק שכבר קיים
    ב-`AdminDashboard.tsx` של אותה אפליקציה (`if (!confirm(...)) return;`)
    ושכבר שימש לתיקון המקביל ב-`PrayerTimesTab.tsx` בסבב הקודם: הוספתי
    `if (!confirm("להסיר את הרב מהארגון?")) return;` בתחילת ה-handler.
    אפס שינוי להתנהגות הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי
    ל-DB/RLS/צורת השאילתה. בדיקת איזון סוגריים ב-python על הקובץ המלא
    אחרי העריכה — תקין (189/189, 176/176, 39/39). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-21-orgportal-removerabbi-unconfirmed-delete-0820`, קומיט
    `433f46c8`, נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על שאר המופעים בתור —
    `PortalMessagesTab.tsx` deleteMessage, `PortalSettingsTab.tsx`
    deletePhoto (שניהם ב-21-mthbram), אחר-כך `AdminSettings.tsx`
    deleteKey (22-get-your-rights, מפתח API — עדיפות גבוהה יחסית), ואז
    `financial.tsx` (27-bkalut-price, 5+ מוטציות מחיקה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 336 (loop B)

336. **המשך עדשת מחיקה-בלי-אישור, מועמד שלישי מהתור שנפתח בסבב 334:**
    `apps/21-mthbram/src/components/portal/PortalMessagesTab.tsx`
    `deleteMessage` (שורה 51) קרא `supabase.from("portal_messages").delete()`
    ישירות מתוך `onClick` (כפתור פח-אשפה ליד כל פניה ברשימה) בלי שום
    שער אישור — לחיצה אחת מוחקת פניית לקוח לצמיתות. תוקן באותו דפוס
    בדיוק שכבר קיים ב-`AdminDashboard.tsx`, `PrayerTimesTab.tsx` (סבב 334)
    ו-`OrgPortal.tsx` (סבב 335) של אותה אפליקציה: הוספתי
    `if (!confirm("למחוק את הפניה?")) return;` בתחילת ה-handler.
    אפס שינוי להתנהגות הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי
    ל-DB/RLS/צורת השאילתה. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים
    ב-python על הקובץ המלא אחרי העריכה — תקין (57/57, 67/67, 10/10).
    אין build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc.
    ענף חדש `fix/b-21-portalmessagestab-unconfirmed-delete-0820`,
    קומיט `2cc14920`, נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על שאר המופעים בתור —
    `PortalSettingsTab.tsx` deletePhoto (21-mthbram), אחר-כך
    `AdminSettings.tsx` deleteKey (22-get-your-rights, מפתח API —
    עדיפות גבוהה יחסית), ואז `financial.tsx` (27-bkalut-price, 5+
    מוטציות מחיקה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 337 (loop B)

337. **סגירת עדשת מחיקה-בלי-אישור שנפתחה בסבב 334, מועמד רביעי ואחרון ב-21-mthbram:**
    `apps/21-mthbram/src/components/portal/PortalSettingsTab.tsx`
    `deletePhoto` (שורה 228) קרא `supabase.from("portal_photos").delete()`
    ישירות מתוך `onClick` (שורה 455, כפתור פח-אשפה שמופיע ב-hover על כל
    תמונה בגלריית הפעילות) בלי שום שער אישור — לחיצה אחת מוחקת תמונה
    לצמיתות. תוקן באותו דפוס בדיוק שכבר קיים ב-`AdminDashboard.tsx`,
    `PrayerTimesTab.tsx` (סבב 334), `OrgPortal.tsx` (סבב 335) ו-
    `PortalMessagesTab.tsx` (סבב 336) של אותה אפליקציה: הוספתי
    `if (!confirm("למחוק את התמונה?")) return;` בתחילת ה-handler.
    בדקתי גם את `removeSection` (שורה 145) באותו קובץ — הוא רק מסיר
    פריט ממערך state מקומי (`setCustomSections`), לא קורא ל-DB ישירות;
    השמירה בפועל קורית דרך כפתור "שמור" נפרד (`saveCustomSections`),
    כך שהוא מחוץ להיקף העדשה הזו. אפס שינוי להתנהגות הקיימת מעבר
    לחסימת המחיקה המיידית — אין שינוי ל-DB/RLS/צורת השאילתה. בדיקת
    איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי
    העריכה — תקין (218/218, 299/299, 106/106). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-21-portalsettingstab-deletephoto-unconfirmed-0820`, קומיט
    `0b6b3dc8`, נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **עדשת מחיקה-בלי-אישור ב-21-mthbram מוצתה** (4/4 מועמדים תוקנו:
    AdminDashboard כבר היה מוגן, PrayerTimesTab, OrgPortal,
    PortalMessagesTab, PortalSettingsTab). **הבא בתור:** לעבור למועמדים
    שנותרו מחוץ ל-21-mthbram — `AdminSettings.tsx` deleteKey
    (22-get-your-rights, מפתח API — עדיפות גבוהה יחסית כי מדובר בסוד
    תפעולי), ואז `financial.tsx` (27-bkalut-price, 5+ מוטציות מחיקה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 338 (loop B)

338. **המשך עדשת מחיקה-בלי-אישור, מעבר ל-22-get-your-rights כפי שתוכנן בסוף סבב 337:**
    `apps/22-get-your-rights/src/pages/AdminSettings.tsx` `deleteKey`
    (שורה 110) קרא `supabase.from("api_keys").delete()` ישירות מתוך
    `onClick` של כפתור פח-אשפה (שורה 226, ליד כל מפתח API ברשימה) בלי
    שום שער אישור — לחיצה אחת מבטלת לצמיתות מפתח API פעיל, כולל כל
    שימוש חיצוני קיים בו (integration חיצוני שמשתמש במפתח ייכשל מיידית
    בלי אזהרה). באפליקציה הזו לא היה עדיין דפוס `confirm()` קיים
    (בדקתי עם grep), אז השתמשתי באותו דפוס בדיוק שכבר מוכח ב-21-mthbram
    (סבבים 333-337): `if (!confirm(...)) return;` בתחילת ה-handler, עם
    הודעה שמסבירה את ההשפעה (חסימת שימוש קיים) כי מדובר בסוד תפעולי
    ולא רק ברשומת תוכן. אפס שינוי להתנהגות הקיימת מעבר לחסימת המחיקה
    המיידית — אין שינוי ל-DB/RLS/צורת השאילתה. בדיקת איזון
    סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה
    — תקין (150/150, 89/89, 12/12). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-22-adminsettings-deletekey-unconfirmed-0820`, קומיט
    `d213f334`, נדחף (מפעיל פריסת Vercel תחת more30.com/zchuyot).

    **הבא בתור:** המועמד האחרון בעדשת מחיקה-בלי-אישור —
    `financial.tsx` (27-bkalut-price, 5+ מוטציות מחיקה: clients/
    transactions/budgets/recurring/tips) — לתקן אחד-אחד בסבבים הבאים
    כדי לשמור על כל שינוי ממוקד.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 339 (loop B)

339. **המשך עדשת מחיקה-בלי-אישור, מועמד ראשון מתוך 5 ב-`financial.tsx`
    (27-bkalut-price) כפי שתוכנן בסוף סבב 338:** `ClientsTab` (שורה
    289) — כפתור פח-אשפה ליד כל לקוח ברשימה (שורה 324) קרא ל-
    `del.mutate(c.id)` ישירות מתוך `onClick`, שמפעיל
    `apiRequest("DELETE", "/api/financial/clients/:id")` בלי שום שער
    אישור — לחיצה אחת מוחקת לקוח פיננסי לצמיתות, כולל (לפי הסכימה) כל
    התנועות/תקציבים/תזכורות המקושרים אליו. בקובץ הזה לא היה קיים שום
    דפוס `confirm()` (לא ב-financial.tsx ולא בשאר הקבצים שנבדקו
    בסבבים 333-338), אז השתמשתי באותו דפוס בדיוק שכבר מוכח שם:
    `if (!confirm(...)) return;` — אבל כאן בנקודת הקריאה עצמה
    (`onClick`) ולא בתוך פונקציית handler נפרדת, כי המחיקה מופעלת
    ישירות דרך `useMutation.mutate()` בלי handler עוטף קיים. ההודעה
    מזכירה במפורש שהתנועות/תקציבים/תזכורות יימחקו יחד עם הלקוח, כדי
    שהאזהרה תשקף את ההשפעה האמיתית. אפס שינוי להתנהגות הקיימת מעבר
    לחסימת המחיקה המיידית — אין שינוי ל-API/DB/צורת השאילתה. בדיקת
    איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי
    העריכה — תקין (444/444, 515/515, 72/72). אין build/dev-server זמין
    בסביבה הזו לפי הנחיית ההרצה — לא tsc. שימו לב: הקובץ תפוס תחת
    כלל ה-`.gitignore` הכללי של `/apps/**` (מדיניות "לא לספח מקור
    פרטי") אבל כבר **עוקף בפועל** ונמצא tracked בגיט (מקומיט קודם
    "Rescue the whole apps/ source tree") — נדרש `git add -f` כדי
    להוסיף את השינוי, בדיוק כמו קבצים אחרים תחת `apps/27-bkalut-price`
    שכבר נערכו בעבר (למשל `973df6da`). ענף חדש
    `fix/b-27-financial-clients-unconfirmed-delete-0820`, קומיט
    `23a72822`, נדחף.

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על 4 המוטציות
    הנותרות באותו קובץ — `TransactionsTab` del (שורה 347, שורה 387),
    `BudgetsTab` del (שורה 412, שורה 440), `RecurringTab` del (שורה
    464), ו-`tips` del (שורה 731) — אחד-אחד בסבבים הבאים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 340 (loop B)

340. **המשך עדשת מחיקה-בלי-אישור, מועמד שני מתוך 5 ב-`financial.tsx`
    (27-bkalut-price) כפי שתוכנן בסוף סבב 339:** `TransactionsTab`
    (שורה 336) — כפתור פח-אשפה ליד כל תנועה ברשימה (שורה 387) קרא
    ל-`del.mutate(t.id)` ישירות מתוך `onClick`, שמפעיל
    `apiRequest("DELETE", "/api/financial/transactions/:id")` בלי שום
    שער אישור — לחיצה אחת מוחקת תנועה פיננסית (הכנסה/הוצאה) לצמיתות
    בלי אזהרה. השתמשתי באותו דפוס שכבר מוכח בקובץ הזה מסבב 339:
    `if (!confirm(...)) return; del.mutate(t.id);` בתוך ה-`onClick`
    עצמו. שימו לב: יש בקובץ שני מקומות עם `del.mutate(t.id)` (התנועה
    בשורה 387 ותזכורת הטיפ ב-`tips` בשורה 764, שני הלולאות משתמשות
    ב-`t` כשם המשתנה) — כדי לא לפגוע בטעות בתזכורת הטיפ (עוד לא בתור),
    השתמשתי בהקשר ייחודי (שורת ה-`span` של הסכום עם `ils(t.amount)`)
    כדי למקד את העריכה רק ל-`TransactionsTab`. אפס שינוי להתנהגות
    הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי ל-API/DB/צורת
    השאילתה. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על
    הקובץ המלא אחרי העריכה — תקין (446/446, 516/516, 72/72). אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף
    חדש `fix/b-27-financial-tx-unconfirmed-delete-0820`, ממשיך מ-HEAD
    של `fix/b-27-financial-clients-unconfirmed-delete-0820` (שכבר
    מוזג/נדחף בסבב 339).

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על 3 המוטציות
    הנותרות באותו קובץ — `BudgetsTab` del (שורה 411, שורה 440),
    `RecurringTab` del (שורה 463, שורה 504), ו-`tips` del (שורה 730,
    שורה 764) — אחד-אחד בסבבים הבאים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 341 (loop B)

341. **המשך עדשת מחיקה-בלי-אישור, מועמד שלישי מתוך 5 ב-`financial.tsx`
    (27-bkalut-price) כפי שתוכנן בסוף סבב 340:** `BudgetsTab` — כפתור
    פח-אשפה ליד כל תקציב ברשימה (שורה 440, קודם) קרא ל-`del.mutate(b.id)`
    ישירות מתוך `onClick` בלי שער אישור — לחיצה אחת מוחקת תקציב חודשי
    לצמיתות בלי אזהרה. השתמשתי באותו דפוס המוכח מסבבים 339-340:
    `if (!confirm("למחוק את התקציב?")) return; del.mutate(b.id);` בתוך
    ה-`onClick` עצמו. אפס שינוי להתנהגות הקיימת מעבר לחסימת המחיקה
    המיידית — אין שינוי ל-API/DB/צורת השאילתה. ענף חדש
    `fix/b-27-financial-budget-unconfirmed-delete-0820`, commit e6278d1e,
    נדחף.

    **הבא בתור:** להמשיך את עדשת מחיקה-בלי-אישור על 2 המוטציות
    הנותרות באותו קובץ — `RecurringTab` del (שורה 463, שורה 504),
    ו-`tips` del (שורה 730, שורה 764) — אחד-אחד בסבבים הבאים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 342 (loop B)

342. **המשך עדשת מחיקה-בלי-אישור, מועמד רביעי מתוך 5 ב-`financial.tsx`
    (27-bkalut-price) כפי שתוכנן בסוף סבב 341:** `RecurringTab` — כפתור
    פח-אשפה ליד כל תזכורת/משימה חוזרת ברשימה (שורה 504) קרא ל-
    `del.mutate(r.id)` ישירות מתוך `onClick` בלי שער אישור — לחיצה אחת
    מוחקת תזכורת (למשל תשלום מע"מ דו-חודשי או משימה משרדית) לצמיתות בלי
    אזהרה. השתמשתי באותו דפוס המוכח מסבבים 339-341:
    `if (!confirm(...)) return; del.mutate(r.id);` בתוך ה-`onClick` עצמו,
    עם שם התזכורת (`r.title`) בהודעת האישור לבהירות. אפס שינוי להתנהגות
    הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי ל-API/DB/צורת
    השאילתה. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על
    הקובץ המלא אחרי העריכה — תקין (450/450, 519/519, 72/72). אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף
    חדש `fix/b-27-financial-recurring-unconfirmed-delete-0820`, ממשיך
    מ-HEAD של `fix/b-27-financial-budget-unconfirmed-delete-0820` (שכבר
    נדחף בסבב 341), commit 30736b49, נדחף.

    **הבא בתור:** מועמד חמישי ואחרון בעדשה זו — `tips` del (שורה 730,
    שורה 764) באותו קובץ. אחרי שזה ייסגר, עדשת מחיקה-בלי-אישור תיסגר
    לגמרי על כל 11 האפליקציות בהיקף loop B (17,18,19,20,21,22,23,24,25,
    27,28), ויהיה צריך לפתוח עדשה חדשה בסבב הבא.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 343 (loop B)

343. **המועמד החמישי והאחרון בעדשת מחיקה-בלי-אישור על `financial.tsx`
    (27-bkalut-price) כפי שתוכנן בסוף סבב 342:** `TipsTab` (שורה 716) —
    כפתור פח-אשפה ליד כל טיפ פיננסי ברשימת הניהול (שורה 764) קרא
    ל-`del.mutate(t.id)` ישירות מתוך `onClick`, שמפעיל
    `apiRequest("DELETE", "/api/financial/tips/:id")` בלי שום שער
    אישור — לחיצה אחת מוחקת טיפ פיננסי לצמיתות מדף הניהול, כולל הסרתו
    המיידית מהתוכן שמוצג ללקוחות בדף הראשי (`OverviewTab` בשורה 243
    קורא לאותו `/api/financial/tips`). השתמשתי באותו דפוס המוכח
    מסבבים 339-342: `if (!confirm(...)) return; del.mutate(t.id);`
    בתוך ה-`onClick` עצמו, עם כותרת הטיפ (`t.title`) בהודעת האישור
    לבהירות. שימו לב: יש בקובץ שני מקומות עם `del.mutate(t.id)`
    (התנועה בשורה 387, שתוקנה כבר בסבב 340, והטיפ בשורה 764) ששתי
    הלולאות משתמשות ב-`t` כשם המשתנה — ה-`old_string` שנבחר (השורה
    המלאה של כפתור המחיקה ב-TipsTab, עם ה-Trash2 בלבד וללא ה-checkbox
    הסמוך) היה ייחודי ומיקד את העריכה רק לטיפ. אפס שינוי להתנהגות
    הקיימת מעבר לחסימת המחיקה המיידית — אין שינוי ל-API/DB/צורת
    השאילתה. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על
    הקובץ המלא אחרי העריכה — תקין (452/452, 521/521, 72/72). אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף
    חדש `fix/b-27-financial-tips-unconfirmed-delete-0820`, commit
    `b651833a`, נדחף (מפעיל פריסת Vercel תחת more30.com/bkalut).

    **עדשת מחיקה-בלי-אישור נסגרה במלואה** על כל 11 האפליקציות בהיקף
    loop B (17,18,19,20,21,22,23,24,25,27,28) — כל אתר מ-`grep`
    ל-`.delete()`/`del.mutate`/`DELETE /api/*` נבדק ותוקן אם היה חסר
    שער `confirm()`. **הבא בתור:** לפתוח עדשה חדשה בסבב הבא — למשל
    לסרוק את 11 האפליקציות אחר מוטציות "עריכה בלי שמירה מפורשת"
    (auto-save שקט על שדות רגישים) או אחר בדיקת ה-TEST MODE
    בכל אינטגרציית תשלום/שליחה בהיקף loop B.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 344 (loop B)

344. **עדשה חדשה: "הצלחה מזויפת" (fake-success) — מועמד ראשון,
    `AdminDashboard.tsx` (21-mthbram):** במקום לפתוח את עדשת ה-TEST
    MODE (שמסוכנת יותר — עלולה לשבור אינטגרציית תשלום/שליחה חיה כמו
    נדרים פלוס, ומנוגדת לכלל ZERO REGRESSION), בחרתי בעדשה הבטוחה
    יותר שכבר תוכננה כאופציה בסוף סבב 343: מוטציות שמדווחות הצלחה
    למשתמש בלי לבדוק אם ה-write באמת הצליח. הרצתי סריקה (Explore
    agent) על כל 11 האפליקציות בהיקף loop B אחר התבנית הזו (בהשראת
    התיקון הקודם מסבב קודם ל-TeacherForm/SeekerForm, commit 58ba7ad5).
    נמצא מועמד אחד אמיתי: `deleteStudyDay` (שורה 131) — כשה-hard
    delete נחסם (למשל ע"י RLS), הקוד נופל חזרה ל-soft-delete
    (`update({status:"deleted"})`) אבל **לא בדק את השגיאה של העדכון
    הזה** לפני שהציג `toast.success("יום העיון הוסר")` והסיר את
    הפריט מהרשימה המקומית — כך שאם גם ה-soft-delete נכשל בשקט, המנהל
    רואה "הוסר" והפריט נעלם מהתצוגה שלו, אבל יום העיון עדיין חי
    וציבורי במסד הנתונים. הפונקציה האחותה `softDeleteLesson` (שורה
    113, אותו קובץ) כבר מטפלת בדיוק באותו תרחיש נכון — השתמשתי באותו
    דפוס בדיוק: `const { error: softError } = await ...update(...)`
    ואז `if (!softError) { ...toast.success... } else { toast.error(...) }`.
    אפס שינוי להתנהגות הקיימת מעבר לחסימת הצגת ההצלחה כשה-write
    השני נכשל — אין שינוי ל-API/DB/צורת השאילתה. בדיקת איזון
    סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי
    העריכה — תקין (818/818, 750/750, 137/137). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-21-mthbram-studyday-fake-success-0820`, commit `a0a8ee51`,
    נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** להמשיך את עדשת ה-fake-success על שאר 10 האפליקציות
    בהיקף loop B (17,18,19,20,22,23,24,25,27,28) — לחפש מוטציות נוספות
    שמדווחות הצלחה בלי לבדוק שגיאה, בדגש על flows של fallback/retry
    כמו זה שנמצא כאן. עדשת ה-TEST MODE על אינטגרציות תשלום/שליחה
    נשארת פתוחה כאופציה עתידית, אבל דורשת זהירות רבה יותר (בדיקה בלבד,
    לא שינוי התנהגות של אינטגרציה חיה) כדי לא להפר ZERO REGRESSION.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 345 (loop B)

345. **המשך עדשת ה-fake-success, מועמד שני, `AdminRightsReference.tsx`
    (22-get-your-rights):** הרצתי Explore agent לסריקת כל 10
    האפליקציות שנותרו בהיקף loop B (17,18,19,20,22,23,24,25,27,28)
    אחר התבנית שנפתחה בסבב 344. נמצאו שני מוטציות אמיתיות באותו קובץ,
    שתיהן על טבלת `rights_reference`: `handleMediaUpload` (שורה 324) —
    אחרי שההעלאה ל-storage הצליחה (עם בדיקת שגיאה תקינה בשורה 317-322),
    העדכון של `media_url`/`media_type` בטבלה לא בדק שגיאה בכלל לפני
    שהציג `toast` "הועלה בהצלחה" ורענן את הרשימה — כך שאם ה-storage
    upload הצליח אבל שמירת הקישור בדאטהבייס נכשלה (למשל הפרת RLS), המנהל
    רואה "הועלה בהצלחה" אבל הקובץ בפועל לא מקושר לנושא ולעולם לא יוצג
    למשתמשים. `handleRemoveMedia` (שורה 331-332) — אותה בעיה בכיוון
    ההפוך: העדכון ל-`null` לא נבדק לפני `toast` "המדיה הוסרה", כך
    שהסרה שנכשלה בשקט משאירה את המדיה הישנה חיה בדאטהבייס בעוד המנהל
    חושב שהיא הוסרה. תיקנתי את שתי הפונקציות לפי הדפוס הנכון שכבר קיים
    באותו קובץ ב-`handleAddRight` (שורה 342-353): `const { error } =
    await ...update(...)`, ואז `if (error) { toast שגיאה; return; }`
    לפני ה-`toast` של הצלחה. אפס שינוי להתנהגות הקיימת מעבר לחסימת
    הצגת ההצלחה כשה-write נכשל — אין שינוי ל-API/DB/צורת השאילתה.
    בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ
    המלא אחרי העריכה — תקין (421/421, 327/327, 86/86). אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף
    חדש `fix/b-22-get-your-rights-media-fake-success-0820`, ממשיך
    מ-HEAD של `fix/b-21-mthbram-studyday-fake-success-0820` (שכבר
    נדחף בסבב 344), commit `14e99d56`, נדחף (מפעיל פריסת Vercel תחת
    more30.com/zchuyot).

    **הבא בתור:** להמשיך את עדשת ה-fake-success על 8 האפליקציות
    הנותרות בהיקף loop B (17,18,19,20,23,24,25,27,28) — 19/20/23/25 הן
    manifest-only ברובן (מעט מאוד קוד vendored), כך שהמועמדים הבאים
    כנראה יימצאו ב-17/18/24/27/28. אחרי שהעדשה תיסגר על כל 11
    האפליקציות, לשקול לפתוח עדשה חדשה (למשל TEST MODE audit על
    אינטגרציות תשלום/שליחה, בזהירות רבה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 346 (loop B)

346. **סגירת עדשת ה-fake-success + פתיחת עדשת "משוב שקט" חדשה,
    `premium-requests.tsx` (27-bkalut-price):** הרצתי Explore agent
    לסריקת 5 האפליקציות הנותרות עם קוד vendored אמיתי
    (17-chizukim-transcribe, 18-torah-editor-mvp,
    24-galilee-connect-hub, 27-bkalut-price, 28-kupot-health-funds)
    אחר התבנית שנפתחה בסבבים 344-345. לא נמצא אף מועמד אמיתי לעדשת
    fake-success: 17/28 לא משתמשות ב-Supabase SDK בכלל (fetch/XHR
    ידניים עם בדיקת `res.ok` לפני כל דיווח הצלחה), 18/24 כבר בודקות
    שגיאה נכון בכל אתרי הכתיבה (כולל flow רב-כתיבות עם דגל `hasError`
    מצטבר ב-24), ו-27 משתמשת ב-`apiRequest()`/TanStack `useMutation`
    שזורק חריגה על תגובה לא תקינה — מבנית לא יכולה "להצליח בשקט".
    העדשה נסגרת רשמית על כל 11 האפליקציות בהיקף loop B.

    בבדיקת 27 נמצא פער אמיתי אחר, סמוך: כ-16 מוטציות בלי `onError`
    בכלל (כשל שקט, לא fake-success). תיקנתי את הראשונה והכי
    משמעותית מבחינת סיכון עסקי: ב-`premium-requests.tsx`, מוטציית
    `decide` (אישור/דחיית שדרוג פרימיום, שמפעילה webhook ל-NEDARIM3873
    לפי ה-comment בקובץ) לא הציגה שום `toast` הצלחה **ולא** `onError`
    בכלל — כישלון PATCH (כולל כישלון dispatch של ה-webhook בצד השרת)
    היה משאיר את המנהל בלי שום משוב חזותי, מה שעלול לגרום לו לנסות
    שוב ולשגר webhook כפול של הרשאות/הודעת ברוכים הבאים לאותו משתמש.
    הוספתי `useToast` + `onSuccess`/`onError` בדיוק לפי הדפוס הקיים
    כבר באותה אפליקציה ב-`api-access.tsx` ו-`financial-crm.tsx`
    (`toast({title, variant:"destructive"})` בכישלון). אפס שינוי
    לוגיקה/API/DB — תוספת משוב UI בלבד. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (40/40, 53/53, 15/15). אין build/dev-server זמין בסביבה הזו לפי
    הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-27-bkalut-price-premium-requests-silent-error-0820`, commit
    `396bb642`, נדחף (מפעיל פריסת Vercel תחת more30.com/mechiron).

    **הבא בתור:** להמשיך את עדשת "משוב שקט" (מוטציה בלי `onError`) על
    שאר כ-15 המוטציות שנמצאו ב-27-bkalut-price
    (`financial-crm.tsx`, `api-access.tsx`, `me.tsx`, `webhook-log.tsx`,
    `params-topics.tsx`) — לתעדף flows עם השפעה עסקית/כספית אמיתית על
    פני toggle-ים קוסמטיים. לשקול גם להרחיב את אותה עדשה לשאר אפליקציות
    loop B שמשתמשות ב-TanStack `useMutation` (18/24), אם יימצאו שם
    מוטציות ללא `onError`.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 347 (loop B)

347. **המשך עדשת "משוב שקט" (מוטציה בלי `onError`), מועמד שני,
    `me.tsx` (27-bkalut-price):** בדקתי את כ-15 המוטציות שנותרו
    בסביבת ה-`useMutation` של 27-bkalut-price שתוכננו בסוף סבב 346
    (`financial-crm.tsx`, `api-access.tsx`, `me.tsx`, `webhook-log.tsx`,
    `params-topics.tsx`) ותעדפתי לפי השפעה עסקית/כספית אמיתית. נבחר
    `me.tsx` — הדשבורד הפיננסי האישי של הלקוח עצמו (לא ניהול), שבו
    שלוש מוטציות (`newTxMutation`, `ackAlert`, `requestPremium`) היו
    **ללא `onError` בכלל**. החמורה מביניהן: `newTxMutation` (הוספת
    תנועת הכנסה/הוצאה) — כפתור "הוסף" ב-`TransactionsTab` (שורה
    224-233) מנקה את שדות הטופס (`setAmount(""); setDescription("")`)
    **מיד עם הלחיצה**, בלי תלות בתוצאת ה-`mutate()` (fire-and-forget,
    לא `mutateAsync`). כלומר אם ה-POST נכשל (שגיאת רשת/שרת), הטופס
    מתאפס והלקוח לא מקבל שום משוב חזותי — הוא חושב שהתנועה נשמרה, אבל
    היא מעולם לא נכתבה לדאטהבייס, מה שעלול לעוות את התקציב/הדוחות
    האישיים שלו בלי שידע. תיקנתי לפי הדפוס הקיים כבר באותה אפליקציה
    ב-`api-access.tsx`/`financial-crm.tsx`: הוספתי `import { useToast }
    from "@/hooks/use-toast"` + `const { toast } = useToast()` בראש
    `DashboardContent`, ו-`onError: () => toast({ title: "...", variant:
    "destructive" })` לשלוש המוטציות (הוספת תנועה, סימון התראה,
    בקשת פרימיום) — כל אחת עם כותרת שגיאה ייעודית. לא נגעתי בבעיית
    ה-UX הנפרדת של ניקוי הטופס לפני תוצאה (זה מחוץ להיקף עדשת
    ה-`onError` הנוכחית; ה-toast החדש לפחות מודיע ללקוח שהתנועה לא
    נשמרה כדי שיזין אותה שוב). אפס שינוי לוגיקה/API/DB קיימים — תוספת
    משוב UI בלבד. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים
    ב-python על הקובץ המלא אחרי העריכה — תקין (191/191, 250/250,
    25/25). אין build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה —
    לא tsc. שים לב: `apps/27-bkalut-price/client/**` תואם לתבנית
    ה-ignore הכללית ב-`.gitignore` השורש (`/apps/**`), אבל קבצים אלו
    כבר **tracked** מסבבים קודמים כך ש-git ממשיך לעקוב אחרי שינויים
    בהם כרגיל (`git add` הצליח אחרי אזהרת ignore חד-פעמית מה-CLI על
    קובץ שכבר במעקב — לא נדרש `-f` אמיתי, ה-git status אישר `M`).
    ענף חדש `fix/b-27-bkalut-price-me-mutations-silent-error-0820`,
    commit `80243acf`, נדחף (מפעיל פריסת Vercel תחת more30.com/mechiron).

    **הבא בתור:** להמשיך את עדשת ה-`onError` החסר על שאר המוטציות
    ב-`financial-crm.tsx` (updateTaskStatus/deleteTask/createMessage/
    createDoc/updateDocStatus/deleteDoc/createReminder/deleteReminder/
    createReport — כולן חסרות `onError`), `api-access.tsx` (`clear`),
    `webhook-log.tsx` (`retry`) ו-`params-topics.tsx`
    (`toggleExactState`) — לתעדף שוב לפי השפעה עסקית/כספית.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 348 (loop B)

348. **המשך עדשת "משוב שקט" (מוטציה בלי `onError`), `financial-crm.tsx`
    (27-bkalut-price):** תיעדוף לפי הרשימה שנקבעה בסוף סבב 347. הקובץ
    `financial-crm.tsx` (כרטיס CRM פיננסי מלא ללקוח — משימות, הודעות,
    מסמכים, תזכורות, דוחות חודשיים) הכיל 9 מתוך 10 מוטציות `useMutation`
    בלי `onError` כלל (`updateTaskStatus`, `deleteTask`, `createMessage`,
    `createDoc`, `updateDocStatus`, `deleteDoc`, `createReminder`,
    `deleteReminder`, `createReport`) — רק `createTask` כבר טופלה נכון.
    סיכון עסקי: מאמן/אדמין שמעדכן סטטוס משימה, מוחק מסמך, שולח הודעה
    ללקוח, יוצר תזכורת או פותח דוח חודשי, ולא מקבל שום משוב חזותי אם
    ה-API נכשל — עלול להניח בטעות שהפעולה הצליחה (למשל: "שלחתי הודעה
    ללקוח" בזמן שהיא מעולם לא נשמרה, או "מחקתי את המסמך הישן" כשהוא
    עדיין קיים). הוספתי `onError: () => toast({ title: "...", variant:
    "destructive" })` לכל תשע המוטציות, כל אחת עם כותרת שגיאה ייעודית
    לפעולה, בדיוק לפי הדפוס הקיים כבר ב-`createTask` באותו קובץ. אפס
    שינוי לוגיקה/API/DB — תוספת משוב UI בלבד. בדיקת איזון סוגריים/
    מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (276/276, 305/305, 55/55). אין build/dev-server זמין בסביבה הזו
    לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-27-bkalut-price-financial-crm-mutations-silent-error-0820`.

    **הבא בתור:** להמשיך את עדשת ה-`onError` החסר על `api-access.tsx`
    (מוטציית `clear`), `webhook-log.tsx` (מוטציית `retry`) ו-
    `params-topics.tsx` (מוטציית `toggleExactState`) ב-27-bkalut-price —
    זה סוגר את כל ה-~16 המוטציות שאותרו בסבב 346. לשקול גם להרחיב את
    אותה עדשה לשאר אפליקציות loop B שמשתמשות ב-TanStack `useMutation`
    (18/24), אם יימצאו שם מוטציות ללא `onError`.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 349 (loop B)

349. **סגירת עדשת "משוב שקט" (מוטציה בלי `onError`) על כל 27-bkalut-price:**
    תיקנתי את שלוש המוטציות האחרונות שנותרו מהרשימה שנקבעה בסוף סבב 346.
    `api-access.tsx` — מוטציית `clear` (ביטול טוקן ה-API): כפתור "בטל
    טוקן" בעמוד הניהול לא נתן שום משוב אם ה-POST נכשל, בזמן ששתי
    המוטציות האחיות באותו קובץ (`togglerequireToken`, `rotate`) כבר
    טופלו כראוי. `webhook-log.tsx` — מוטציית `retry` ("נסה שוב" על
    וובהוק שנכשל ל-NEDARIM3873/מערכות חיצוניות): הקובץ הזה לא הכיל
    בכלל תשתית `useToast`/`toast` — הוספתי את הייבוא וההפעלה מאפס.
    `params-topics.tsx` — מוטציית `toggleExactState` (הפעלת/כיבוי
    חיפוש-מצב-מדויק בקטלוג): שאר הפונקציות בקובץ (`save`/`remove`)
    כבר הציגו toast שגיאה, אבל המוטציה הזו לא. הוספתי לכל שלוש
    `onError: () => toast({ title: "...", variant: "destructive" })`
    לפי הדפוס הקיים כבר בכל קובץ. אפס שינוי לוגיקה/API/DB — תוספת
    משוב UI בלבד. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים
    ב-python על שלושת הקבצים המלאים אחרי העריכה — תקין בכולם. אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc.
    ענף חדש
    `fix/b-27-bkalut-price-api-webhook-params-mutations-silent-error-0820`,
    commit `cf64e3f2`, נדחף (מפעיל פריסת Vercel תחת more30.com/mechiron).
    זה סוגר את כל ~16 המוטציות שאותרו בסבב 346 על פני כל 27-bkalut-price.

    **הבא בתור:** להרחיב את עדשת ה-`onError` החסר לשאר אפליקציות
    loop B שמשתמשות ב-TanStack `useMutation` — במיוחד 18-torah-editor-mvp
    ו-24-galilee-connect-hub — אם יימצאו שם מוטציות ללא `onError`.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 350 (loop B)

350. **בדיקת עדשת ה-`onError` על 18/24, ואז תמחור חסר ל-21-mthbram:**
    ניסיתי קודם להרחיב את עדשת "משוב שקט" (סבב 349) ל-18-torah-editor-mvp
    ו-24-galilee-connect-hub לפי ההמלצה — נמצא שאף אחת מהן לא משתמשת
    ב-`@tanstack/react-query`/`useMutation` בכלל: 18 היא Next.js בלי
    התלות הזו כלל, ו-24 מתקינה `react-query` אבל כל הכתיבות עוברות
    ישירות דרך `supabase.from(...)` בתוך handlers רגילים. סרקתי את
    `GabaiPortal.tsx` הגדול של 24 (1766 שורות, ~25 קריאות כתיבה
    ישירות) על אותו חשד — התברר שכל מסך שם כבר מציג שגיאה (state
    `actionError`/`saveError`/`deleteError` + `<p>` אדום), חוץ משורת
    ניקוי-כפילויות שניה (`cleanupError`, שורה 610) שרק ב-`console.error`
    — לא תוקן, פעולת רקע משנית לא-קריטית, לא הדפוס שהעדשה מחפשת.
    מכיוון שהעדשה הזו יבשה על 18/24, השוויתי את `core.plans` (מסד
    core, לא מוגן) בין 11 האפליקציות של loop B: `chizukim`, `galil`,
    `kupot`, `orech`, `zchuyot`, `mechiron` — לכולן שלוש שורות
    `customer_visible=true` (free/basic/extended, ₪2/₪5 לחודש, מסלול
    TEST). ל-`mthbram` (21, חי ופרוס) היו רק `charge`/`one_time`/`pro`
    הפנימיים — אפס מסלול שלקוח יכול לראות, ואפס קישור "מחירון" ב-
    `Navbar.tsx` שלה (לכל שאר האפליקציות החיות יש). הוספתי את שלוש
    השורות החסרות ל-`core.plans` (אותם מחירים בדיוק כמו האחיות) וקישור
    "מחירון" לדסקטופ ומובייל ב-`Navbar.tsx`, בדיוק לפי הדפוס של
    `24-galilee-connect-hub` (עוגן `<a href="https://more30.com/subscribe?app=mthbram">`
    + אייקון `Tag`). לפני שדחפתי בדקתי את `core.app_offer_block('mthbram')`
    — מחזיר `not_offered` כי `public_visible=false` (הוסתר במכוון
    ב-05/08 כי הקטלוג מציג 0 שיעורים מאושרים — מוצר ריק, ראו הערת
    `core.projects.note` הקיימת). זו לא נפילה: `subscribe.html` כבר
    מטפל ב-`offered===false` בהצגת הודעה נכונה ("המערכת הזאת אינה
    מוצעת כרגע למנוי") במקום מסך לבן — כך שהקישור החדש לא שובר כלום,
    רק מכין את תשתית התמחור למתי שההסתרה תוסר. בדיקת איזון סוגריים/
    מאמרים מסולסלים/מרובעים ב-python על `Navbar.tsx` אחרי העריכה —
    תקין (38/38, 82/82, 3/3). אין build/dev-server זמין בסביבה הזו
    לפי הנחיית ההרצה — לא tsc. ענף חדש
    `feat/b-mthbram-pricing-navbar-plans-0820`, commit `52d6f511`,
    נדחף (מפעיל פריסת Vercel תחת more30.com/mthbram).

    **הבא בתור:** issue #250 (RLS פתוח-לגמרי על `public.synagogues`
    ב-21-mthbram) עדיין חסום — הפרויקט `aypsqqvfohekxxuqsmrw` לא נגיש
    ל-MCP כאן, ודורש הכרעת בעלים. לשקול גם לבדוק אם 19-igud-shiurim-portal
    ו-20-igud-portal (עדיין `live=false`) צריכים את אותה תוספת `core.plans`
    לפני הפריסה הראשונה שלהם, כדי שהתמחור יהיה מוכן מהיום הראשון.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 351 (loop B)

351. **בדקתי קודם שהתמחור/מיתוג בפועל כבר מלא על כל 7 האפליקציות החיות
    בהיקף** (17,18,21,22,24,27,28 — `core.plans` עם שורות `customer_visible=
    true` free/basic/extended בכל אחת, כולל mthbram אחרי תיקון סבב 350).
    19/20/23/25 עדיין `live=false`/`is_deployed=false` (שלד/ריפו ריק) —
    אין להם דף חי לתמחר. פתחתי במקום זאת עדשה חדשה שמעולם לא נבדקה על
    ההיקף הזה: **כפתורי "הוספה" בלי חסימה בזמן מוטציה בתהליך (double-submit),
    למרות שיש להם כבר `onError`.** סוכן Explore שסרק את 17/18/21/22/24/27/28
    מצא זאת ב-`apps/27-bkalut-price/client/src/pages/financial.tsx`: שלושה
    כפתורי "הוספה" (`BudgetsTab` שורה 427, `RecurringTab` שורה 491, `TipsTab`
    שורה 750) בדקו רק תקינות טופס ב-`disabled`, בלי `create.isPending` —
    בניגוד לדפוס הנכון שכבר קיים באותו קובץ ב-`ClientsTab` (שורה 310) ו-
    `TransactionsTab` (שורה 368). **סיכון עסקי:** לקוח/מאמן שלוחץ פעמיים
    מהר על "הוספה" (למשל בגלל תגובת רשת איטית) שולח שתי בקשות `POST`
    זהות לפני שהראשונה חוזרת — יוצר שני רשומות תקציב/תזכורת/טיפ כפולות
    לאותה קטגוריה, מעוות את תקרת התקציב החודשי בפועל. תיקנתי לפי הדפוס
    הקיים בדיוק: הוספתי `|| create.isPending` לכל שלושת ה-`disabled`,
    והחלפתי את הטקסט הקבוע "הוספה" ב-`{create.isPending ? "מוסיף..." :
    "הוספה"}` (אותו טקסט משוב שכבר קיים ב-`ClientsTab`). אימתתי מול כל
    ~16 קריאות ה-`.mutate(` בקובץ (grep מלא) שלא פספסתי מוטציית `create`
    נוספת בלי `isPending` — כל ה-`update`/`del`/`assign`/`suggest`
    האחרות הן toggle/מחיקה עם אישור, לא טפסי-הוספה עם אותה בעיה. אפס
    שינוי ל-API/DB/מבנה שאילתה — תוספת חסימת UI + טקסט משוב בלבד. בדיקת
    איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא אחרי
    העריכה — תקין (524/524, 452/452, 72/72). אין build/dev-server זמין
    בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף חדש
    `fix/b-27-bkalut-price-financial-double-submit-0820`.

    **הבא בתור:** להרחיב את עדשת ה-double-submit (יצירה בלי `isPending`
    ב-`disabled`) לשאר האפליקציות בהיקף (17/18/21/22/24/28) שלא נסרקו
    עדיין בה; לחלופין לחזור לתמחור `core.plans` על 19/20/23/25 לפני
    הפריסה הראשונה שלהם, או לנושא #250 (RLS פתוח ב-mthbram) אם תתקבל
    גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 352 (loop B)

352. **הרחבתי את עדשת ה-double-submit (סבב 351) לשאר ההיקף.** סוכן
    Explore סרק את 17/18/21/22/24/28 (27 כבר טופל בסבב 351) אחר
    כפתורי "הוספה" שחסרים דגל טעינה ב-`disabled`. 17 (`useMutation.
    isPending` בכל מקום), 21/22 (מצב `loading` ידני שכבר קיים), 28
    (רק `useQuery` לקריאה, בלי מוטציות יצירה) — כולם נקיים. 18 אין
    בכלל `@tanstack/react-query` (Next.js server actions עם מצב ידני
    בטוח). פגיעה אמיתית אחת: `apps/24-galilee-connect-hub/src/pages/
    GabaiPortal.tsx` — `KnowledgeManager.handleAdd` (שורה 1204) יורה
    `supabase.from('knowledge_base').insert(...)` ישירות מ-handler של
    כפתור, עם `disabled={!title.trim() || !content.trim() ||
    !category.trim()}` בלבד — בלי חסימת טעינה כלל, בשונה מכל מנהל
    אחר באותו קובץ (`SynagogueManager`/`ContactManager`/
    `BannerManager`/`DailyHalacha` — לכולם כבר יש מצב `saving`).
    **סיכון עסקי:** גבאי שלוחץ פעמיים מהר על "הוסף למאגר" (לפני
    שהעלאת התמונה/הכתיבה הראשונה חוזרת) יוצר שתי רשומות `knowledge_base`
    כפולות לאותו פריט. תיקנתי לפי הדפוס הקיים בקובץ: הוספתי מצב
    `saving`, מפעיל/מכבה סביב ה-`insert` (כולל נתיב ה-return של
    שגיאה), הוספתי אותו ל-`disabled`, והחלפתי את טקסט הכפתור הקבוע
    ב-`{saving ? 'מוסיף...' : 'הוסף למאגר'}` — אותה מוסכמה בדיוק כמו
    `DailyHalacha` באותו קובץ. אפס שינוי ל-API/DB/סכמה — תוספת חסימת
    UI + טקסט משוב בלבד. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים
    ב-python על הקובץ המלא אחרי העריכה — תקין. אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא tsc. ענף `fix/b-24-galilee-
    knowledge-double-submit-0820`, קומיט `171fa257`, נדחף (מפעיל
    פריסת Vercel תחת more30.com/galil).

    **מצב עדשת double-submit:** סגורה כעת על כל שבעת האפליקציות
    החיות בהיקף (17,18,21,22,24,27,28) — 27 ו-24 היו הפגיעות
    האמיתיות היחידות (סבבים 351-352), השאר נקיות.

    **הבא בתור:** לחזור לתמחור `core.plans` על 19/20/23/25 לפני
    הפריסה הראשונה שלהם (שלד/ריפו ריק כרגע — להכין את התשתית מהיום
    הראשון), או לנושא #250 (RLS פתוח-לגמרי על `public.synagogues`
    ב-21-mthbram) אם תתקבל גישת dashboard לפרויקט aypsqqvfohekxxuqsmrw.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 353 (loop B)

353. **תמחור `core.plans` ל-19/20, אחרי סינון 23/25.** בדקתי קודם את
    כל ארבע ה-`stage=wip` בהיקף: `23-haorech-torani` הוא מסמך
    ארכיטקטורה/תוכנית-בנייה סטטי בעברית (index.html יחיד, בלי טופס
    או שדה נתונים אחד — "מה למסור לקלוד", לא מוצר) ו-`25-mor1-main-site`
    הוא `app.json` בלבד, אפס קוד — לאף אחד מהשניים אין מה לתמחר.
    לעומתם `19-igud-shiurim-portal` ו-`20-igud-portal` הם פורטלים
    אמיתיים (server.js + public/index.html+app.js+style.css) שכבר
    היו רשומים ב-`core.plans` עם שורות פנימיות בלבד (`charge`/
    `one_time`/`pro`, `customer_visible=false`) — בדיוק כמו שהיה חסר
    ל-mthbram (21) לפני תיקון סבב 350, לפני שקיבל את שלוש השורות
    הגלויות ללקוח. הוספתי אותן שורות בדיוק (`free`/`basic`/`extended`,
    ₪2/₪5 לחודש, אותו tagline/sort כמו `chizukim` ושאר האחיות) לשני
    ה-`app_key` (`shiurim`, `igud`), ו"מחירון" בניווט של שני
    ה-`index.html` (`https://more30.com/subscribe?app=shiurim` /
    `?app=igud`), אותה מוסכמה בדיוק כמו 17/18/21/22/24/27/28. בדקתי
    ש-`core.projects.public_visible=false` על שני המספרים (19,20) —
    כלומר `subscribe.html` יציג "המערכת הזאת אינה מוצעת כרגע למנוי"
    אם מישהו בכל זאת ינווט לשם, לא מסך שבור — אותו דפוס בדיוק כמו
    ה-`not_offered` שתועד ל-mthbram בסבב 350. אפס שינוי התנהגות חי:
    שני המספרים `is_deployed=false`/`live=false`, אין להם URL פרוס.
    זו הכנת תשתית תמחור ליום ההשקה הראשון, לא שינוי במוצר קיים.
    גיליתי גם ש-`apps/19-20` יושבים תחת חוק ה-`.gitignore` הגורף
    ל-`/apps/**` (מיועד למקור לא-מיובא) אבל **כן** במעקב git בפועל
    (מ-commit `cb6e4f58`, "Rescue the whole apps/ source tree") —
    `git add` על שני הקבצים הדפיס אזהרת "ignored" אבל בכל זאת הצליח
    כי קבצים שכבר במעקב לא נחסמים ע"י `.gitignore`, רק קבצים חדשים.
    בדיקת איזון תגי HTML ב-python על שני הקבצים אחרי העריכה — תקין
    (57/57, 51/51). אין build/dev-server זמין בסביבה הזו לפי הנחיית
    ההרצה. ענף `fix/b-19-20-igud-pricing-prep-0820`, קומיט `50064214`,
    נדחף.

    **הבא בתור:** נושא #250 (RLS פתוח-לגמרי על `public.synagogues`
    ב-21-mthbram, פרויקט `aypsqqvfohekxxuqsmrw` לא נגיש ל-MCP כאן)
    עדיין חסום על הכרעת/גישת המשתמש; נושא #164 (מתחברים 21 — nedarim-
    webhook מדליף פרטי קשר) חסום על הכרעת סוד-משותף; נושא #169
    (22-zchuyot leads-webhook ללא אימות) חסום דומה. עם עדשת ה-
    double-submit סגורה (351-352) ותמחור 19/20 מוכן (353), שווה
    לפתוח עדשה חדשה על ה-4 שנותרו: 17/18/21/22/24/27/28 — אולי נגישות
    טפסים (label/aria) או עקביות מסך-ריק (empty-state) בין הדפים.
    via cloud server 167.99.131.167 [loop B]

354. **עדשה חדשה: שגיאות-כתיבה שנבלעות ב-`try/catch` בלי משוב למשתמש
    (`console.error` בלבד), נפרד מארבע העדשות שכבר נסגרו (double-
    submit, מחיקה-בלי-אישור, `useMutation` בלי `onError`, "הצלחה
    מדומה"). סוכן Explore סרק את שבעת האפליקציות עם קוד אמיתי
    (17/18/21/22/24/27/28) וחיפש נתיבי כתיבה (insert/update/upload/
    submit) שה-`catch` שלהם לא מציג טוסט/alert בכלל. המצא הכי
    משמעותי: `apps/22-get-your-rights/src/components/
    RightBrandedCard.tsx` — `handleDownload` (הכפתור "הורד כתמונה"
    בכרטיס הזכות המודפס) עטף את `downloadBrandedImage()` ב-`try/
    catch` שרק עשה `console.error`, בלי שום משוב חזותי. משתמש שלוחץ
    הורדה כשה-`dom-to-image-more` נכשל (למשל רינדור קנבס/CORS על
    תמונה חיצונית) רואה את הספינר נעצר וכלום לא קורה — לא קובץ, לא
    הודעה — ועלול לחשוב שהדפדפן שלו תקוע ולנסות שוב ושוב. תוקן:
    הוספתי `useToast` (כבר בשימוש באותה אפליקציה, `RightsCategories.
    tsx`/`AdminRightsReference.tsx`, אותה מוסכמת `variant:
    "destructive"`) וטוסט שגיאה על ה-`catch` הקיים, בלי לגעת בלוגיקת
    ההורדה עצמה. מועמדים נוספים שנמצאו בסריקה (לא תוקנו הסבב הזה,
    בעדיפות נמוכה יותר): `21-mthbram/src/pages/StudyDayUpload.tsx`
    (`handleCreate`/`handleLogo`) ו-`OrgPortal.tsx` (`uploadLogo`) —
    כבר מציגים טוסט גנרי על שגיאה, רק חסר פירוט; ו-`24-galilee-
    connect-hub/GabaiPortal.tsx handleSave` — ניקוי שורות-כפולות
    שנכשל בשקט אחרי שהשמירה העיקרית כבר הצליחה (סיכון נמוך, לא
    משתמש-פונה). אפס שינוי API/DB/query. בדיקת איזון סוגריים ב-
    python על הקובץ אחרי העריכה: תקין (112/112, 99/99, 15/15). אין
    build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`.
    ענף `fix/b-22-rightbrandedcard-download-silent-error-0820`.

    **הבא בתור:** להמשיך את אותה עדשה על שלושת המועמדים שנמצאו ולא
    תוקנו (`21-mthbram` StudyDayUpload/OrgPortal — הודעת שגיאה
    מפורטת יותר על כשל הכנסה/העלאת לוגו; `24-galilee-connect-hub`
    GabaiPortal ניקוי-שורות-כפולות), או לחזור לנושא #250 (RLS פתוח
    ב-21-mthbram, פרויקט `aypsqqvfohekxxuqsmrw` עדיין לא נגיש ל-MCP).
    via cloud server 167.99.131.167 [loop B]

355. **המשך עדשת שגיאות-כתיבה-שנבלעות (353→354): שני המועמדים הראשונים
    ב-`21-mthbram`.** שני הקבצים (`StudyDayUpload.tsx` — `handleLogo`,
    `handleCreate`; `OrgPortal.tsx` — `uploadLogo`) כבר הציגו טוסט
    שגיאה גנרי ("שגיאה בהעלאת לוגו"/"שגיאה ביצירת הפורטל") — לא
    הבעיה החמורה של round 354 (אפס משוב), אלא הודעה בלי פירוט שמקשה
    על המשתמש/תמיכה לאבחן כשל אמיתי (הרשאת storage, רשת, ולידציה
    בצד שרת). אימצתי את המוסכמה הקיימת **באותה אפליקציה עצמה**
    (`AdminDashboard.tsx`/`NedarimManagement.tsx`:
    `toast.error("שגיאה...: " + error.message)`) והוספתי
    `e?.message` לשלוש קריאות ה-`toast.error`, כולל `console.error`
    חסר ב-`OrgPortal.tsx uploadLogo` שהיה שקט לגמרי בקונסול. אפס
    שינוי בלוגיקת ההעלאה/יצירה עצמה. בדיקת איזון סוגריים ב-python
    על שני הקבצים אחרי העריכה: תקין (StudyDayUpload 85/85 תבניות,
    95/95 סוגריים; OrgPortal 176/176, 191/191). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. ענף
    `fix/b-21-mthbram-upload-error-detail-0820`.

    **הבא בתור:** המועמד השלישי שנותר מהעדשה — `24-galilee-connect-
    hub/GabaiPortal.tsx handleSave` (ניקוי שורות-כפולות שנכשל בשקט
    אחרי שמירה עיקרית מוצלחת) — או לחזור לנושא #250 (RLS פתוח
    ב-21-mthbram, פרויקט `aypsqqvfohekxxuqsmrw` עדיין לא נגיש ל-MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 356 (loop B)

356. **סגירת עדשת שגיאות-כתיבה-שנבלעות: המועמד השלישי והאחרון —
    `24-galilee-connect-hub` `GabaiPortal.tsx` `ContactInfoManager.
    handleSave`.** הפונקציה שומרת פרטי יצירת קשר בדפוס insert-then-
    cleanup מכוון (לא delete-then-insert): קודם מכניסה שורת
    `knowledge_base` חדשה, ורק אז מוחקת שורות ישנות מאותה קטגוריה —
    כדי שאם ה-insert נכשל, השורה הישנה לעולם לא נעלמת ועמוד יצירת-
    הקשר הציבורי לא יכול להתרוקן. אבל כשל ה-cleanup delete עצמו היה
    רק `console.error`, בלי שום משוב למנהל: המסך הראה "✅ נשמר!"
    הרגיל בזמן ששורה כפולה-ישנה נשארת בשקט ב-DB. הוספתי מצב
    `cleanupWarning` (מאותה מוסכמה כמו `saveError`/`deleteError`
    הקיימים באותו קובץ) שמוצג כהודעת אזהרה נפרדת ("הפרטים נשמרו, אך
    נותרה גרסה כפולה ישנה שלא נמחקה — פנו לתמיכה אם הבעיה חוזרת")
    מתחת לכפתור, בלי לגעת בלוגיקת ה-insert/delete עצמה. אפס שינוי
    API/DB/סכמה. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים
    ב-python על הקובץ המלא אחרי העריכה — תקין (702/702, 898/898,
    164/164). אין build/dev-server זמין בסביבה הזו לפי הנחיית
    ההרצה — לא `tsc`. ענף `fix/b-24-galilee-contactinfo-cleanup-
    warning-0820`, קומיט `dea59a63`, נדחף.

    **מצב עדשת שגיאות-כתיבה-שנבלעות:** סגורה כעת על שלושת המועמדים
    שנמצאו בסבב 354 (22-get-your-rights RightBrandedCard, 21-mthbram
    StudyDayUpload/OrgPortal, 24-galilee ContactInfoManager).

    **הבא בתור:** לפתוח עדשה חדשה על ה-7 אפליקציות החיות בהיקף
    (17/18/21/22/24/27/28) — למשל נגישות טפסים (label/aria) או
    עקביות מסך-ריק (empty-state); או לחזור לנושא #250 (RLS פתוח-לגמרי
    על `public.synagogues` ב-21-mthbram, פרויקט `aypsqqvfohekxxuqsmrw`
    עדיין לא נגיש ל-MCP כאן) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 357 (loop B)

357. **פתיחת עדשה חדשה: עקביות מסך-ריק (empty-state) — סריקה על 7
    האפליקציות החיות בהיקף.** סוכן Explore סרק
    17/18/21/22/24/27/28 בחיפוש אחר `.map(...)` שמרנדר רשימה שהגיעה
    משאילתה, בלי בדיקת `length === 0` נלווית. עלו 2 מועמדים; לאחר
    קריאה ידנית אחד מהם התברר כ-false positive —
    `AdminRightsReference.tsx` (22-get-your-rights) בונה את `items`
    מ-`reduce` שמייצר מפתח לקטגוריה רק כשיש בה פריט, כך שהמערך
    שם לעולם לא יכול להיות ריק, אין צורך בבדיקה. המועמד השני היה
    אמיתי: `24-galilee-connect-hub` `AllSynagoguesPage.tsx` — הרשת
    שמציגה את כל בתי הכנסת עברה ישירות מ-`loading` ל-`.map()` בלי
    ענף לרשימה ריקה; אחרי טעינה מוצלחת עם 0 בתי כנסת (למשל התקנה
    ראשונית) המשתמש רואה בלוק לבן ריק בלי שום הסבר. אותה בדיוק
    תבנית-כשל כבר תוקנה בעבר באותה אפליקציה ב-`PrayersTicker.tsx`
    ("אין עדיין זמני תפילה רשומים...") — השתמשתי באותה מוסכמת
    טקסט/עיצוב במקום להמציא UI חדש. הוספתי ענף `synagogues.length
    === 0` בין `loading` ל-`.map()` הקיים, שמציג "אין עדיין בתי
    כנסת רשומים במערכת." אפס שינוי בלוגיקת השאילתה/API. בדיקת
    איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על הקובץ המלא
    אחרי העריכה — תקין (40/40, 53/53, 10/10). אין build/dev-server
    זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. הקובץ כבר עוקב
    בגיט למרות כלל ה-gitignore על `apps/**` (אותו מצב כמו בסבבים
    קודמים), נעשה שימוש ב-`git add -f` בהתאם לתקדים. ענף
    `fix/b-24-galilee-allsynagogues-empty-state-0820`, קומיט
    `aa35eccf`, נדחף (מפעיל דיפלוי Vercel תחת `more30.com/galil`).

    **הבא בתור:** להמשיך את עדשת מסך-ריק על שאר האפליקציות
    (17/18/21/27/28 — עוד לא נסרקו לעומק ידנית, רק ע"י הסוכן), או
    לפתוח עדשת נגישות טפסים (label/aria), או לחזור לנושא #250 אם
    תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 358 (loop B)

358. **המשך עדשת מסך-ריק: `21-mthbram` `OrgPortal.tsx` — טאב
    השיעורים.** המשך התור שנקבע בסוף סבב 357 (17/18/21/27/28,
    עוד לא נסרקו ידנית). סוכן Explore סרק את חמש האפליקציות
    והעלה 2 מועמדים בתוך `21-mthbram`: `OrgPortal.tsx` (טאב
    שיעורים של ארגון) ו-`RabbiPortal.tsx` (טאב שיעורים של רב).
    קריאה ידנית גילתה ש-`RabbiPortal.tsx` הוא false positive —
    כבר יש שם ענף `lessons.length === 0` מיד אחרי ה-`.map()`
    (שורה 337, "אין שיעורים עדיין" עם אייקון `BookOpen`) שהסוכן
    פספס. `OrgPortal.tsx` היה מועמד אמיתי: `filteredLessons.map()`
    (שורה 396) מרנדר את רשימת השיעורים המסוננת-לפי-רב בלי שום ענף
    לרשימה ריקה — כשלארגון חדש אין עדיין שיעורים, או כשסינון לפי
    רב ספציפי לא מחזיר תוצאות, המשתמש רואה בלוק ריק לגמרי בלי
    הסבר. הוספתי בדיוק את אותו בלוק empty-state הקיים כבר ב-
    `RabbiPortal.tsx` הסמוך (`filteredLessons.length === 0`, אותו
    טקסט "אין שיעורים עדיין" / "לחצו הוספת שיעור חדש כדי להתחיל",
    אותו אייקון `BookOpen` שכבר מיובא בקובץ) — עקביות מלאה בין שני
    הפורטלים באותה אפליקציה במקום להמציא ניסוח חדש. אפס שינוי
    בלוגיקת השאילתה/סינון. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (192/192, 177/177, 39/39). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא `tsc`. ענף
    `fix/b-21-mthbram-lessons-empty-state-0820`, קומיט `61ab0f15`.

    **הבא בתור:** לסיים את עדשת מסך-ריק על 17/18/27/28 (עוד לא
    נסרקו ידנית, הסוכן דיווח שהן תקינות אך לא אומת ידנית כמו
    שקרה כאן), או לפתוח עדשת נגישות טפסים (label/aria), או לחזור
    לנושא #250 אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 359 (loop B)

359. **סיום עדשת מסך-ריק: אימות ידני של 17/18/27/28 — מציאה
    אמיתית ב-`27-bkalut-price` `automations.tsx`.** המשך התור
    שנקבע בסוף סבב 358 (17/18/21/27/28 — הסוכן דיווח "תקין" אך
    ללא קריאה ידנית מלאה, וסבב 358 כבר תפס false negative של
    הסוכן פעם אחת). הפעלתי סוכן Explore לקריאה ידנית (לא grep
    בלבד) על כל רכיבי הרשימה ברביעיית האפליקציות. 17
    (`recordings.tsx`), 18 (`documents/page.tsx`, `htr/page.tsx`)
    ו-28 (`home.tsx`) כולם תקינים — כבר יש ענף `length === 0`
    מפורש. ב-27-bkalut-price נמצא מועמד אמיתי:
    `client/src/pages/automations.tsx` (`AutomationsInner`, מסך
    ניהול פנימי "אוטומציות וקונקטורים") עבר ישירות מ-9 שלדי
    טעינה (`list.isLoading`) ל-`(list.data ?? []).map()` בלי ענף
    לרשימה ריקה. אימתתי שזה לא תיאורטי: קריאה ב-
    `server/supabase-storage.ts` מגלה ש-`seedAutomationDefaults()`
    שמאכלס את 9-13 שורות ברירת המחדל רץ ברקע כ-"best-effort" —
    "Failures are logged, not fatal" (שורה 293-296) — כך שכשל
    זמני בחיבור ל-Supabase משאיר את מסך הניהול ריק לגמרי בלי שום
    הסבר לצוות. תיקנתי לפי אותה תבנית שכבר קיימת באותה אפליקציה
    ב-`delivery.tsx` (ענף `.length === 0` בין `isLoading` ל-
    `.map()`), עם ניסוח תואם לטון הפנימי-אדמיניסטרטיבי של הדף.
    אפס שינוי בלוגיקת השאילתה/ה-seed. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (68/68, 103/103, 14/14). אין build/dev-server זמין בסביבה הזו
    לפי הנחיית ההרצה — לא `tsc`. הקובץ כבר עוקב בגיט למרות כלל
    ה-gitignore על `apps/**`, נעשה שימוש ב-`git add -f` בהתאם
    לתקדים. ענף `fix/b-27-bkalut-automations-empty-state-0820`,
    קומיט `1ca70656`.

    **הבא בתור:** עדשת מסך-ריק כעת סגורה על כל 7 האפליקציות
    החיות בהיקף loop B (17/18/21/22/24/27/28) — סבב הבא צריך
    לפתוח עדשה חדשה (למשל נגישות טפסים label/aria, או race
    conditions בהחלפת טאבים מהירה), או לחזור לנושא #250 (RLS
    פתוח על `21-mthbram` `public.synagogues`, חסום כי הפרויקט
    `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת
    dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 360 (loop B)

360. **פתיחת עדשה חדשה: נגישות טפסים (label/aria) — מציאה אמיתית
    ב-`24-galilee-connect-hub` `ChatBot.tsx`.** בהמשך להצעת הסבב
    הקודם (357-359 סגרו את עדשת מסך-ריק; הצעה נוספת הייתה נגישות
    טפסים label/aria). סוכן Explore סרק את שבע האפליקציות החיות
    בהיקף (17/18/21/22/24/27/28) בחיפוש אחר `<input>`/`<textarea>`/
    `<select>` בלי שם נגיש (לא `<label>`, לא `aria-label`, לא
    `aria-labelledby`). רוב המועמדים שעלו התבררו כ-false positive
    בקריאה ידנית (שדה `contactValue` הסמוך באותו `ChatBot.tsx` כבר
    מוגן ב-`aria-label` דינמי מסבב מוקדם יותר, קומיט `494b003c`;
    סלקטים ב-`public-potential.tsx`/`public-health-funds.tsx`
    מוגנים ב-`<Label htmlFor>` תקין). מועמד אחד היה אמיתי: ה-
    `<textarea>` של תוכן השאלה ב-`ChatBot.tsx` (שורה 305, ווידג'ט
    "שאל את הרב" הציבורי, מורכב ב-`App.tsx` וגלוי בכל האתר) — היה
    לו רק `placeholder="כתוב את שאלתך כאן..."`, בלי שום `label`/
    `aria-label`/`aria-labelledby`, כך שמשתמש קורא-מסך לא יודע מה
    השדה הזה. תיקנתי לפי אותה מוסכמה שכבר קיימת שתי שורות מתחת
    באותו קובץ (`aria-label` על שדה `contactValue`) — הוספתי
    `aria-label="תוכן השאלה לרב"` ל-textarea, בלי לגעת בלוגיקת
    השליחה/הסטייט. אפס שינוי API/DB. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (367/367, 276/276, 59/59). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא `tsc`. הקובץ כבר עוקב בגיט למרות
    כלל ה-gitignore על `apps/24-galilee-connect-hub/src` (נתפס
    כמתועד/tracked מסבב מוקדם יותר), נעשה שימוש ב-`git add -f`
    בהתאם לתקדים. ענף `fix/b-24-galilee-chatbot-question-aria-0820`,
    קומיט `0a3b20e4`, נדחף (מפעיל דיפלוי Vercel תחת
    `more30.com/galil`).

    **הבא בתור:** להמשיך את עדשת נגישות-טפסים על שאר האפליקציות
    (17/18/21/22/27/28 — הסוכן דיווח שהן תקינות אך ללא קריאה ידנית
    ממצה של *כל* קובץ, בדומה למה שקרה בעדשת מסך-הריק בסבב 358 שבו
    הסוכן פספס מועמד אמיתי), או לבדוק כפתורי אייקון-בלבד ללא
    `aria-label` בעמודים ציבוריים (עדשה חדשה), או לחזור לנושא #250
    (RLS פתוח על `21-mthbram` `public.synagogues`, חסום כי הפרויקט
    `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 361 (loop B)

361. **המשך עדשת נגישות-טפסים → הרחבה לכפתורי אייקון-בלבד; מציאה
    אמיתית ב-`21-mthbram` `UpdateLesson.tsx`.** בהמשך להצעת סבב
    360: פתחתי עדשה סמוכה — כפתורי `<button>` שתוכנם הוא רק אייקון
    (בלי טקסט גלוי) וללא `aria-label`/`aria-labelledby`/`title`.
    סוכן Explore סרק את שבע האפליקציות החיות בהיקף
    (17/18/21/22/24/27/28) ומצא 8 כפתורים כאלה, שבעה מתוכם בדפי
    ניהול פנימיים בלבד (IvrBuilder/ApiReferenceSection/AdminLeads/
    community-admin וכו' — כפתורי העתקה/מחיקה אדמיניסטרטיביים).
    מועמד אחד היה ציבורי אמיתי: `apps/21-mthbram/src/pages/
    UpdateLesson.tsx` שורה 284 — טופס השירות-העצמי הציבורי שבו רב
    מעדכן את פרטי השיעור שלו; כפתור מחיקת שורת יום-בשבוע בלוח
    הזמנים (`<Trash2 className="w-4 h-4" />` בלבד, ללא טקסט/
    `aria-label`), כך שמשתמש קורא-מסך לא יודע מה הכפתור עושה.
    תיקנתי לפי אותה מוסכמת `aria-label` שכבר קיימת באותה אפליקציה
    (`OrgPortal.tsx`/`RabbiPortal.tsx`, כפתורי `X` עם
    `aria-label="ביטול"`/`"ביטול עריכה"`) — הוספתי
    `aria-label={\`הסרת יום ${sd.day}\`}` דינמי (שם היום בפועל),
    ובנוסף `type="button"` כדי להתאים לכפתור הסמוך באותו בלוק
    (בורר ימי השבוע, שורה 263, שכבר מוגדר `type="button"`). אפס
    שינוי בלוגיקת ה-state/השמירה. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין
    (237/237, 226/226, 61/61). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא `tsc`. הקובץ כבר עוקב בגיט למרות כלל
    ה-gitignore על `apps/21-mthbram/src`, נעשה שימוש ב-`git add -f`
    בהתאם לתקדים. ענף `fix/b-21-mthbram-updatelesson-deleteday-
    aria-0820`, קומיט `ac279ccb`.

    **הבא בתור:** שבעת שאר כפתורי האייקון-בלבד שנמצאו הם כולם
    בדפי ניהול פנימיים (עדיפות נמוכה יותר) — אפשר לתקן אותם בסבב
    נפרד, או לחזור לקריאה ידנית ממצה של label/aria בכל קובץ
    ב-17/18/22/27/28 (כפי שהוצע בסבב 360), או לחזור לנושא #250
    (RLS פתוח על `21-mthbram` `public.synagogues`, חסום כי הפרויקט
    `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 362 (loop B)

362. **סגירת עדשת כפתורי אייקון-בלבד ללא `aria-label`: שאר שישה
    הכפתורים (כולם ניהוליים).** המשך ישיר לסבב 361 — תיקנתי את
    שישה המועמדים הנותרים (מתוך שמונה שנמצאו, אחד כבר תוקן בסבב
    361) שאותרו במדויק על ידי סוכן Explore לפי הרמזים
    "IvrBuilder/ApiReferenceSection/AdminLeads/community-admin":
    (1-2) `apps/21-mthbram/src/pages/IvrBuilder.tsx` שורות 128,138
    — שני כפתורי העתקה (URL ודוגמת קוד) בעמוד `/admin/ivr` המוגן
    ב-`ProtectedRoute`; הוספתי `aria-label` לפי מוסכמת האפליקציה
    (`RabbiPortal.tsx`/`OrgPortal.tsx` X-כפתורים). (3)
    `apps/21-mthbram/src/components/admin/ApiReferenceSection.tsx`
    שורה 243 — קומפוננטת `CopyButton` המשותפת, מוצגת 4 פעמים בתוך
    `AdminDashboard.tsx` (מוגן); הוספתי `aria-label="העתקה"` פעם
    אחת במקור המשותף. (4) `apps/22-get-your-rights/src/pages/
    AdminLeads.tsx` שורה 649 — כפתור העתקת דוגמת קוד ב-API
    reference, מוגן ע"י בדיקת session ב-mount; הוספתי `aria-label`
    לפי מוסכמת הקובץ עצמו (שורה 322, `aria-label` דינמי על
    כרטיס-ליד). (5-6) `apps/27-bkalut-price/client/src/pages/
    community-admin.tsx` שורות 343,363 — כפתורי מחיקת קישור-קהילה
    ומחיקת שאלה, בעמוד ניהול המוגן ב-`isAdminSubdomain()`; שני
    הכפתורים היו היחידים בשורתם ללא תווית (השכנים — כפתורי
    העתקה/תצוגה — כבר השתמשו ב-`title=`), כך שהוספתי `title=` תואם
    (`"מחיקת קישור"`/`"מחיקת שאלה"`) לפי אותה מוסכמה **באותו קובץ**
    ממש, ולא ייבוא ממוסכמה של אפליקציה אחרת. אפס שינוי בלוגיקת
    מחיקה/העתקה/state בכל שישה המקומות — רק תוויות נגישות. בדיקת
    איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על כל ארבעת
    הקבצים המלאים אחרי העריכה — תקין בכולם (ראה מספרים בקומיט).
    אין build/dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא
    `tsc`. כל ארבעת הקבצים כבר עקבו בגיט (ללא צורך ב-`git add -f`
    הפעם — `git status` הראה `M` רגיל על כולם). זה סוגר את עדשת
    כפתורי-האייקון-ללא-תווית במלואה על פני היקף loop B
    (17/18/21/22/24/27/28) — שמונת הכפתורים שנמצאו בסבב 361 תוקנו
    כולם (1 ציבורי בסבב 361 + 6 ניהוליים כאן, כאשר אחד מהם
    התממש כתיקון קומפוננטה משותפת המכסה 4 מופעי-תצוגה).

    **הבא בתור:** לחזור לקריאה ידנית ממצה של label/aria בכל קובץ
    ב-17/18/22/27/28 (עדיין תקף מסבב 360, לא בוצע במלואו), או
    לפתוח עדשה חדשה (למשל race conditions על מעבר-טאב מהיר, או
    בדיקת נגישות טפסים על אלמנטים נוספים כמו `role="button"` בלי
    `tabIndex`), או לחזור לנושא #250 (RLS פתוח על `21-mthbram`
    `public.synagogues`, חסום כי הפרויקט `aypsqqvfohekxxuqsmrw` לא
    נגיש דרך MCP) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 363 (loop B)

363. **עדשה חדשה: race condition בין מסננים מוחלפים במהירות (stale
    async response) — שני מועמדים אמיתיים ב-`27-bkalut-price`.**
    בהמשך להצעת סבב 362 (לפתוח עדשה חדשה, למשל race conditions על
    מעבר-טאב מהיר). סוכן Explore סרק את שבע האפליקציות החיות בהיקף
    (17/18/21/22/24/27/28) בחיפוש אחר `useEffect` שרץ מחדש לפי ערך
    שמשתמש יכול להחליף (טאב/מסנן/פריט נבחר), עם קריאה אסינכרונית
    שמעדכנת state ללא הגנה מפני תשובה שמגיעה מאוחר מהתשובה הטרייה
    יותר (stale-overwrite). הסוכן החזיר 4 מועמדים; קראתי כל אחד
    ידנית לפני תיקון (כמו שקרה בעדשות קודמות, לא כל מה שהסוכן
    מסמן הוא בר-מימוש בפועל): שני מועמדים ב-`21-mthbram`
    (`PrayerTimesTab.tsx`/`PublicPrayerTimes.tsx`, `useEffect(...,
    [orgId])`) התבררו כלא-אמיתיים בפועל — `orgId` שם הוא
    `portal.id` שמגיע מ-state שנטען פעם אחת בטעינת הדף (מ-`token`/
    `publicToken` בנתיב, לא מתחלף בלי remount של כל הדף כי אין שום
    קישור פנימי בין דפי ארגונים שונים באפליקציה, אימתתי ב-grep) —
    כך שאין תרחיש משתמש אמיתי שבו `orgId` מתחלף בזמן שהקומפוננטה
    נשארת mounted. **לא תיקנתי** את שני אלה כדי לא להוסיף קוד הגנה
    תיאורטי-בלבד. שני המועמדים הנותרים ב-`27-bkalut-price` כן
    אמיתיים: (1) `price-comparison-admin.tsx` `loadSubmissions()`
    — `useEffect(..., [subFilter])` כש-`subFilter` נשלט ע"י
    `<select>` אמיתי שאדמין יכול להחליף (pending/approved/
    rejected/all) מהר יותר מזמן תשובת ה-API; (2)
    `health-funds-admin.tsx` `loadTopics()` — אותו דפוס בדיוק עם
    `kindFilter` (fund/gov/ngo/הכל). בשני המקרים לחיצה מהירה בין
    שני ערכי מסנן יכולה לגרום לתשובה הישנה (שמגיעה אחרונה) לדרוס
    את התשובה הטרייה, כך שהמסך מציג רשימה שלא תואמת למסנן הנבחר
    בפועל, בלי שום הודעת שגיאה. תיקנתי בדפוס אחיד ומינימלי: `ref`
    ששומר את ערך המסנן העדכני ביותר, כל קריאה שומרת את הערך שבו
    התחילה (`requestedFilter`/`requestedKind`), ולפני `setState`
    בודקת אם ה-`ref` עדיין שווה לערך שהיא ביקשה — אם לא, זו תשובה
    ישנה ומדלגים על העדכון (התשובה החדשה יותר כבר בטיפול/טופלה).
    ב-`health-funds-admin.tsx` גם `setLoading(false)` ב-`finally`
    הוגן באותה בדיקה כדי לא לכבות מצב טעינה ששייך לבקשה החדשה
    יותר. אפס שינוי להתנהגות הרגילה (מסנן יציב, בקשה אחת בכל פעם)
    ואפס שינוי לקריאות האחרות ל-`loadSubmissions`/`loadTopics`
    ממקומות אחרים בקובץ (למשל אחרי אישור/דחיית הצעה) — אלה ממשיכות
    לעבוד בדיוק כמו קודם כי ה-`ref` תמיד משקף את המסנן הנוכחי.
    בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על שני
    הקבצים המלאים אחרי העריכה — תקין בשניהם (557/557+489/489+
    76/76, 474/474+425/425+56/56). אין build/dev-server זמין
    בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. שני הקבצים כבר עקבו
    בגיט (`git status` הראה `M` רגיל, לא נדרש `git add -f`). ענף
    `fix/b-27-bkalut-admin-filter-race-0820`.

    **הבא בתור:** לבדוק אם יש עוד מועמדי race-condition דומים
    באפליקציות שלא נסרקו כאן לגמרי (18/17/28 — הסוכן לא סימן שם
    כלום, אבל לא בוצעה קריאה ידנית ממצה של *כל* קובץ), או לחזור
    לקריאה ידנית ממצה של label/aria בכל קובץ ב-17/18/22/27/28
    (עדיין תקף מסבב 360, לא בוצע במלואו), או לחזור לנושא #250 (RLS
    פתוח על `21-mthbram` `public.synagogues`, חסום כי הפרויקט
    `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת
    dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 364 (loop B)

364. **המשך עדשת נגישות-טפסים (queued מסבב 360): קריאה ידנית ממצה
    של `18-torah-editor-mvp` — שני `<textarea>` ראשיים ללא תווית
    נגישות.** בהמשך להצעת סבב 363 (לחזור לקריאה ידנית ממצה של
    label/aria בכל קובץ ב-17/18/22/27/28, עדיין לא בוצעה קובץ-אחר-
    קובץ). קראתי ידנית את כל שבעת קבצי ה-`app/*.tsx` של
    `18-torah-editor-mvp` (page/editor/htr/documents/login/layout) —
    האפליקציה הזו כבר מכוסה טוב יחסית (aria-label על שדה כותרת
    מסמך, `select` מצב מסמך, קלט קבצים, `role="group"` +
    `aria-pressed` על צ'יפים של סינון), אבל שני `<textarea>`
    מרכזיים נפספסו: (1) `app/editor/page.tsx` שורה 306 — תיבת
    ההדבקה הראשית של המודול הציבורי "אימות מקורות וציטוטים"
    (מדביקים טקסט, מזהים ציטוטים, מנקדים) — הייתה רק עם
    `placeholder`, בלי `aria-label`, אותה בעיה בדיוק שתוקנה
    בקודמות דומות (ChatBot.tsx סבב 356). (2) `app/htr/page.tsx`
    שורה 489 — תיבת "עריכה ואישור אנושי" ב-`JobDetail` (מודול HTR),
    הטקסט הסופי שנכנס בפועל לספר לפני האישור — לא הייתה לה שום
    תווית, אפילו לא `placeholder`. הוספתי `aria-label` לשתיהן לפי
    המוסכמה הקיימת כבר באותה אפליקציה עצמה (`doc-title-input`
    `aria-label="שם המסמך"` וכו') — "טקסט המסמך לעריכה" ו"הטקסט
    הסופי לעריכה ואישור" בהתאמה. אפס שינוי בלוגיקת state/שמירה/
    אישור. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על
    שני הקבצים המלאים אחרי העריכה — תקין בשניהם (201/201+129/129+
    28/28, 240/240+186/186+40/40). אין build/dev-server זמין
    בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. שני הקבצים כבר עקבו
    בגיט (למרות כלל ה-gitignore על `apps/18-torah-editor-mvp/app`,
    `git add` הרגיל הצליח על קבצים שכבר עקובים, בניגוד לקבצים
    חדשים שדורשים `-f`). ענף `fix/b-18-torah-editor-textarea-aria-
    0820`, קומיט `0f340dd1`, נדחף (מפעיל דיפלוי Vercel תחת
    `more30.com/orech`).

    **הבא בתור:** להמשיך את הקריאה הידנית הממצה על שאר האפליקציות
    שעדיין לא נסרקו קובץ-אחר-קובץ (17/22/27/28 — 21/24 כבר עברו
    סבבים ממוקדים, 18 הושלם כעת), או לבדוק את דפוס ה-race-condition
    (stale async response) שנפתח בסבב 363 על 17/18/28 (הסוכן סרק
    ולא מצא שם מועמדים, אך לא בוצעה קריאה ידנית ממצה כמו שקרה כאן),
    או לחזור לנושא #250 (RLS פתוח על `21-mthbram`
    `public.synagogues`, חסום כי הפרויקט `aypsqqvfohekxxuqsmrw` לא
    נגיש דרך MCP) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 365 (loop B)

365. **המשך עדשת נגישות-טפסים: קריאה ידנית ממצה של
    `17-chizukim-transcribe` — שני `<textarea>` ללא `aria-label`,
    ושלושה כפתורי-מצב (toggle) ללא `aria-pressed`.** בהמשך להצעת
    סבב 364 (17/22/27/28 עדיין לא נסרקו קובץ-אחר-קובץ). קראתי ידנית
    את כל ארבעת קבצי ה-`pages/*.tsx` של `17-chizukim-transcribe`
    (upload/recordings/recording-detail/not-found). `upload.tsx`
    כבר מכוסה טוב (dropzone עם `role="button"`+`aria-label`, שני
    שדות עם `<label htmlFor>` תקין). מצאתי שני פערים אמיתיים ב-
    `recording-detail.tsx`: שני ה-`<textarea>` המרכזיים — תמלול
    ערוך (שורה ~327, מצב עריכה) ותמלול גולמי (שורה ~365) — היו רק
    עם `placeholder`, בלי `aria-label`, אותו דפוס בדיוק שתוקן ב-
    `18-torah-editor-mvp` בסבב 364. הוספתי `aria-label="התמלול
    הערוך לעריכה"` ו-`aria-label="התמלול הגולמי לעריכה"` בהתאמה.
    בנוסף מצאתי חוסר עקביות: כפתור ה-toggle "מילה שלמה" באותה
    אפליקציה (`recordings.tsx`) כבר משתמש נכון ב-`aria-pressed`,
    אבל שלושה כפתורי-toggle דומים לא — `FilterChip` (סינון לפי
    סטטוס, `recordings.tsx`) וכפתורי "קריאה"/"עריכה" של מצב התצוגה
    (`recording-detail.tsx`) — כך שקורא מסך לא יכול לדעת איזה מסנן/
    מצב פעיל כרגע. הוספתי `aria-pressed={active}` /
    `aria-pressed={editedMode === "..."}` בהתאמה, לפי אותה מוסכמה
    שכבר קיימת בקובץ עצמו. אפס שינוי בלוגיקת state/שמירה/חיפוש/
    תמלול. בדיקת איזון סוגריים/מאמרים מסולסלים/מרובעים ב-python על
    שני הקבצים המלאים אחרי העריכה — תקין בשניהם (185/185+173/173+
    39/39, 164/164+168/168+20/20). אין build/dev-server זמין
    בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. שני הקבצים תחת
    `apps/17-chizukim-transcribe/client` וה-gitignore חוסם אותם,
    נעשה `git add -f` לפי התקדים הקיים. ענף
    `fix/b-17-chizukim-textarea-aria-pressed-0820`.

    **הבא בתור:** להמשיך את הקריאה הידנית הממצה על 22/27/28 (עדיין
    לא נסרקו קובץ-אחר-קובץ), או לבדוק את דפוס ה-race-condition
    (stale async response) שנפתח בסבב 363 על 18/28 (17 כבר נסרק כאן
    ולא נמצא שם מועמד מהסוג הזה), או לחזור לנושא #250 (RLS פתוח על
    `21-mthbram` `public.synagogues`, חסום כי הפרויקט
    `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 366 (loop B)

366. **המשך עדשת נגישות-טפסים: קריאה ידנית ממצה של
    `22-get-your-rights` — חמישה `<textarea>` ללא `aria-label`
    (בחלקם היה תווית חזותית לא-מקושרת, בחלקם רק `placeholder`),
    וארבעה זוגות כפתורי-toggle ללא `aria-pressed`.** בהמשך לתור
    שנקבע בסבב 365 (17 נסגר, 22/27/28 עדיין ממתינים). קראתי ידנית
    את שלושת הקבצים עם `<Textarea>`/כפתורי-toggle: `AdminLeads.tsx`,
    `AdminRightsReference.tsx`, `FloatingBot.tsx` (ווידג'ט הצ'אט
    הציבורי). ממצאים: (1) `AdminLeads.tsx` — כפתור סינון לפי סטטוס
    (שורה ~276, אותו דפוס בדיוק כמו `FilterChip` שתוקן ב-17/סבב
    365) חסר `aria-pressed`; שני `<Textarea>` ("תיאור הטיפול",
    "הערות מנהל") עם תווית `<label>` חזותית שאינה מקושרת ל-input —
    קורא מסך לא שומע אותה; (2) `AdminRightsReference.tsx` — שני
    `<Textarea>` בטופסי עריכה/הוספת נושא, אחד עם תווית לא-מקושרת
    ואחד עם `placeholder` בלבד; (3) `FloatingBot.tsx` — טופס יצירת
    קשר עם `<Textarea>` פרטים-נוספים ללא שום תווית, וארבעה כפתורי
    כן/לא (שתי שאלות דו-מצביות: התאמה לקריטריון, ותשובת שאלון-נושא)
    שכל אחד מהם משתמש רק בצבע-רקע כדי לסמן את המצב הנבחר, בלי
    `aria-pressed`. הוספתי `aria-label` תואם-תוכן לכל חמשת ה-
    `<Textarea>`, ו-`aria-pressed={<תנאי הבחירה הקיים>}` לכל חמשת
    כפתורי ה-toggle (סינון סטטוס + 2 זוגות כן/לא). אפס שינוי
    בלוגיקת state/שמירה/סינון/שליחה. בדיקת איזון סוגריים/מאמרים
    מסולסלים/מרובעים ב-python על שלושת הקבצים המלאים אחרי העריכה —
    תקין בכולם (276/324/77, 421/329/86, 362/479/84 — פתיחה=סגירה
    בכל אחד). אין build/dev-server זמין בסביבה הזו לפי הנחיית
    ההרצה — לא `tsc`. הקבצים תחת `apps/22-get-your-rights/src`
    (לא חסומים ב-gitignore, נוספו בלי `-f`). ענף
    `fix/b-22-getrights-textarea-aria-pressed-0820`.

    **הבא בתור:** להמשיך את הקריאה הידנית הממצה על 27/28 (עדיין לא
    נסרקו קובץ-אחר-קובץ בעדשה הזו), או לבדוק את דפוס ה-race-condition
    (stale async response) שנפתח בסבב 363 על 18/28, או לחזור לנושא
    #250 (RLS פתוח על `21-mthbram` `public.synagogues`, חסום כי
    הפרויקט `aypsqqvfohekxxuqsmrw` לא נגיש דרך MCP) אם תתקבל גישת
    dashboard.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 367 (loop B)

367. **המשך עדשת נגישות-טפסים: קריאה ידנית ממצה של `27-bkalut-price`
    (האפליקציה הגדולה מבין ה-7 החיות) — 32 `<Textarea>` ללא שם
    נגיש, פרוסים על 15 קבצים.** בהמשך לתור שנקבע בסבב 365-366
    (17/18/22 נסגרו, 27/28 עדיין ממתינים). קראתי ידנית את כל 50
    מופעי `<Textarea>` ב-`client/src` (26 קבצים) דרך grep+context
    ולא רק סריקת agent; מיינתי כל אחד: (1) עטוף ב-`<label>` טבעי —
    כבר מקושר, לא נגע; (2) יש `htmlFor`/`id` תואמים — כבר מקושר,
    לא נגע; (3) קומפוננטת `<Label>` חזותית ליד ה-`Textarea` בלי
    `htmlFor`/`id` — לא מקושר בפועל לקורא מסך; (4) `placeholder`
    בלבד או שום תווית — אותה בעיה. מצאתי 32 מופעים מהסוגים
    (3)/(4) ב-15 קבצים: `community-admin.tsx` (5), `chatbot-admin.tsx`
    (4), `financial-crm.tsx` (5), `health-funds-admin.tsx` (3),
    `price-comparison-admin.tsx` (2), `potential-admin.tsx` (2),
    `rights.tsx` (2), ו-`delivery.tsx`/`params-topics.tsx`/
    `public-reminder.tsx`/`premium-requests.tsx`/`me.tsx`/
    `financial.tsx`/`public-community.tsx`/`components/public-chatbot.tsx`
    (אחד כל אחד — כולל `public-chatbot.tsx` שהוא ווידג'ט הצ'אט
    הציבורי, אותו דפוס בדיוק כמו `FloatingBot.tsx` ב-22/סבב 366).
    הוספתי `aria-label` תואם-תוכן (מהטקסט החזותי של ה-`Label`/
    `placeholder`/כותרת הסמוכה) לכל 32 המופעים. אפס שינוי
    בלוגיקת state/שמירה/onChange/onBlur — `git diff` מאמת ששורות
    שהוסרו זהות לשורות שנוספו פרט ל-`aria-label` שנוסף. בדיקת
    איזון סוגריים/מאמרים מסולסלים ב-python על כל 15 הקבצים המלאים
    אחרי העריכה — תקין בכולם. אין build/dev-server זמין בסביבה הזו
    לפי הנחיית ההרצה — לא `tsc`. הקבצים תחת
    `apps/27-bkalut-price/client/src` ואינם חסומים ב-gitignore.
    ענף `fix/b-27-bkalut-textarea-aria-0820`.

    **לא טופל בסבב זה, במכוון:** נמצאו גם מספר כפתורי-toggle/
    filter-chip עם מצב נבחר המסומן רק בצבע-רקע (`params-topics.tsx`
    שורות 190/199, `financial-crm.tsx` שורה 231/299, `financial.tsx`
    שורה 153, `potential-admin.tsx` שורה 527, `users.tsx` שורה 369,
    `public-potential.tsx` שורה 124/296) — דפוס `aria-pressed` דומה
    לזה שתוקן ב-17/22, אבל היקף גדול מספיק (7+ קבצים) שראוי לסבב
    נפרד ולא לצרף כתוספת לא-ממוקדת לסבב הזה.

    **הבא בתור:** לסגור את `28-kupot-health-funds` (הטקסטאריאה
    האחרונה בעדשה הזו), או לפתוח עדשת `aria-pressed`/filter-chip
    שזוהתה כעת על 27 (וכנראה גם 28), או דפוס ה-race-condition
    (stale async response) שנפתח בסבב 363 על 18/28, או נושא #250
    (RLS על `21-mthbram`, חסום MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 368 (loop B)

368. **סגירת עדשת נגישות-הטפסים (`aria-label` על `<Textarea>`) על
    האפליקציה האחרונה בהיקף — `28-kupot-health-funds`.** בהמשך
    לתור מסבב 367 ("לסגור את 28"). קראתי ידנית את כל קבצי הלקוח
    שמכילים `Textarea` (grep על `<textarea`/`Textarea` בכל
    `client/src`, לא רק agent) — רק 2 מופעים אמיתיים מלבד קומפוננטת
    הבסיס `components/ui/textarea.tsx`: (1) `SmartAdvisor.tsx` שורה
    88 — תיבת השאלה ל"יועץ החכם" (ווידג'ט צ'אט ציבורי לכל נושא),
    `placeholder` בלבד ללא שם נגיש; (2) `SwitchFundDialog.tsx` שורה
    298 — שדה "הערה" בטופס מעבר-קופה (שלב 3 מתוך 3), עטוף
    בקומפוננטת `Field` פנימית שמרנדרת `<Label>` חזותי כ־sibling
    בלי `htmlFor`/`id` (לא מקושר בפועל) — אותו דפוס בדיוק כמו סוג
    (3) שתועד בסבב 367. לא נגעתי בשדות ה-`<Input>` האחרים באותו
    `Field` (מחוץ להיקף עדשת ה-Textarea; ראו "לא טופל" בסבב 367 —
    עדשה נפרדת). הוספתי `aria-label={`שאלה ליועץ החכם על
    ${topicName}`}` (דינמי, תואם ל-`topicName` שכבר מוצג בכותרת
    הווידג'ט מעל) ו-`aria-label="הערה"` (תואם לטקסט ה-`Label`
    החזותי) בהתאמה. אפס שינוי בלוגיקת state/שמירה/`ask`/
    `form.register` — `git diff` מאמת שהשורה היחידה שנוספה בכל קובץ
    היא ה-`aria-label`. בדיקת איזון סוגריים/מאמרים מסולסלים ב-python
    על שני הקבצים המלאים אחרי העריכה — תקין (57/57 תלתלים+45/45
    סוגריים+8/8 מרובעים ב-`SmartAdvisor.tsx`, 101/101+111/111+10/10
    ב-`SwitchFundDialog.tsx`). אין build/dev-server זמין בסביבה הזו
    לפי הנחיית ההרצה — לא `tsc`. שני הקבצים תחת
    `apps/28-kupot-health-funds/client/src/components` ואינם חסומים
    ב-gitignore (נתפסו ב-`git status` הרגיל, בלי `-f`). ענף
    `fix/b-28-kupot-textarea-aria-0820`.

    **זה סוגר את עדשת ה-`aria-label` על `<Textarea>` על פני כל 7
    האפליקציות החיות בהיקף loop B (17/18/21/22/24/27/28)** — 21
    נבדקה במסגרת עדשת ה-icon-only-button (סבב 361) ולא נמצאו בה
    Textarea-ים כלל; 24 טופלה בסבב 360; 17/18/22/27/28 טופלו
    בסבבים 364-368.

    **הבא בתור:** עדשת `aria-pressed`/filter-chip שזוהתה בסבב 367
    על 27 (שורות 190/199 ב-`params-topics.tsx`, 231/299 ב-
    `financial-crm.tsx`, 153 ב-`financial.tsx`, 527 ב-
    `potential-admin.tsx`, 369 ב-`users.tsx`, 124/296 ב-
    `public-potential.tsx`) וכנראה גם על 28, או דפוס ה-race-condition
    (stale async response) שנפתח בסבב 363 על 18/28 (17/21/24/27
    כבר נבדקו), או נושא #250 (RLS על `21-mthbram`, חסום MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 369 (loop B)

369. **פתיחה+סגירה של עדשת `aria-pressed` על כפתורי-toggle/
    filter-chip ב-27-bkalut-price**, בדיוק לפי 6 המיקומים שזוהו
    בסבב 367/368. קראתי כל אחד ידנית לפני עריכה: `params-topics.tsx`
    (2 כפתורי סינון קטגוריה, שורות 190/199 — "הכל" + כל קטגוריה,
    מצב מסומן רק ע"י צבע-רקע), `financial-crm.tsx` (2 כפתורים —
    בורר לקוח בסיידבר שורה 231 ומעברי-טאב בכרטיס CRM שורה 299),
    `financial.tsx` (כפתור-שורת-לקוח ב-`ClientsTab`, שורה 320 —
    המצב הנבחר מסומן ע"י ה-`div` העוטף ולא ע"י הכפתור עצמו, אך
    זהו אותו רעיון סמנטי של "פריט נבחר מתוך רשימה"), `users.tsx`
    (כפתורי בחירת-מוצר בדיאלוג משתמש, שורה 369), `potential-admin.tsx`
    (כפתורי "סעיפים להסתרה" — multi-select toggle, שורה 527).
    הוספתי `aria-pressed={<אותו ביטוי בוליאני שכבר קובע את הצבע>}`
    לכל אחד מה-7 הכפתורים — אפס שינוי בלוגיקת קליק/state/API.
    **`public-potential.tsx` הוצא מהעדשה אחרי קריאה ידנית:** שורה
    ~560 היא `<span>` לא-אינטראקטיבי (נקודות התקדמות ב"שלב X מתוך
    Y", לא כפתור ואין לו `onClick`), ושורה ~668 (`isSelected`)
    משתמשת ב-`Checkbox` אמיתי (`checked`/`onCheckedChange`) שכבר
    בעל סמנטיקת ARIA native — לא ממצא אמיתי, false-positive מהסריקה
    בסבב 367. בדיקת איזון סוגריים/מאמרים-מסולסלים/מרובעים ב-python
    על כל 5 הקבצים המלאים אחרי העריכה — תקין בכולם (307/307+276/276+
    55/55, 137/137+154/154+32/32, 525/525+452/452+72/72,
    171/171+154/154+33/33, 291/291+289/289+94/94). אין build/
    dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. כל 5
    הקבצים תחת `apps/27-bkalut-price/client/src/pages`, אינם חסומים
    ב-gitignore (נתפסו ב-`git status` הרגיל, בלי `-f`). ענף
    `fix/b-27-bkalut-filter-chip-aria-pressed-0820`, קומיט אחד
    (קוד+`DECISIONS.md` יחד), נדחף (מפעיל דיפלוי Vercel תחת
    `more30.com/mechiron`).

    **זה סוגר את עדשת ה-`aria-pressed`/filter-chip על 27-bkalut-price
    במלואה** (6/6 המיקומים שזוהו בסבבים 367-368, פלוס תיקון
    false-positive אחד).

    **הבא בתור:** לבדוק אם אותו דפוס `aria-pressed` חסר גם ב-
    `28-kupot-health-funds` (לא נבדק עדיין לעדשה הזו), או דפוס
    ה-race-condition (stale async response) שנפתח בסבב 363 על 18/28
    (17/21/24/27 כבר נבדקו), או נושא #250 (RLS על `21-mthbram`,
    חסום MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 370 (loop B)

370. **בדיקת עדשת `aria-pressed`/filter-chip (שנפתחה בסבב 369 על
    27-bkalut-price) על `28-kupot-health-funds`** — לפי התור שהוצע
    בסוף סבב 369. Explore agent סרק את `client/src` וזיהה 4
    מועמדים ב-`pages/home.tsx`; קראתי כל אחד ידנית לפני עריכה:
    לשוניות מסלול "קופה/ממשלה/עמותה" (שורה 193, `kind === k`),
    כפתורי לוח-מובילים לפי קופה (שורה 260, `active = fund === f.key`),
    כפתור "טעון השוואה פרטנית" (שורה 288, `fund === "undecided"`),
    ורכיב `CategoryChip` המשותף לכל שבבי-הקטגוריה (שורה 494,
    prop בשם `active`). הוספתי `aria-pressed={<אותו ביטוי בוליאני
    שכבר קובע את הצבע>}` לכל אחד מ-4 הכפתורים — אפס שינוי בלוגיקת
    קליק/state/API; `git diff` מאמת שהשורה היחידה שנוספה בכל מקום
    היא ה-`aria-pressed`. בדיקת איזון סוגריים מסולסלים/עגולים/
    מרובעים ב-python על הקובץ המלא אחרי העריכה — תקין (140/140+
    140/140+31/31). אין build/dev-server זמין בסביבה הזו לפי
    הנחיית ההרצה — לא `tsc`. הקובץ תחת
    `apps/28-kupot-health-funds/client/src/pages/home.tsx`, אינו
    חסום ב-gitignore. ענף `fix/b-28-kupot-filter-chip-aria-pressed-0820`.

    **זה סוגר את עדשת ה-`aria-pressed`/filter-chip על פני כל 7
    האפליקציות החיות בהיקף loop B** (17/18/21/22/24/27/28) — שילוב
    הבדיקות מסבבים 369-370.

    **הבא בתור:** דפוס ה-race-condition (stale async response) על
    לחיצות סינון/טאב מהירות שנפתח בסבב 363 על 18/28 (17/21/24/27
    כבר נבדקו), או נושא #250 (RLS על `21-mthbram`, חסום MCP), או
    לפתוח עדשה חדשה לפי סריקת Explore רחבה על כל 7 האפליקציות.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 371 (loop B)

371. **סגירת דפוס ה-race-condition (stale async response) על
    `18-torah-editor-mvp`/`28-kupot-health-funds`** — לפי התור
    שהוצע בסוף סבב 370 (17/21/24/27 כבר נבדקו בסבב 363). Explore
    agent סרק את שני האפליקציות; קראתי כל קובץ שסומן ידנית לפני
    כל מסקנה. `28-kupot-health-funds`: כל הסינון (`kind`/`category`/
    `fund`/`search`) ב-`pages/home.tsx` הוא client-side בלבד דרך
    `useMemo` על נתונים שכבר נטענו פעם אחת ב-`useQuery({queryKey:
    ["/api/hf/topics"]})` — אין fetch נפרד לכל שינוי סינון, ולכן
    אין מקום למרוץ. אין `useEffect` אסינכרוני כלל ברכיבי
    `client/src/components`. **נקי, לא נמצא ממצא.** `18-torah-editor-
    mvp`: `app/documents/page.tsx` — ה-`filter` (סטטוס מסמך) לא
    מופיע בין התלויות של `load()` (רק `configured`), הסינון עצמו
    client-side; **נקי**. `app/htr/page.tsx` — ממצא אמיתי:
    `JobDetail` (שורות 375-391) מקבל `jobId` כ-prop מ-`JobList`
    שמאפשר קליק מהיר בין עבודות (`onSelect={setSelected}`,
    שורה 83) בלי `key` על `JobDetail` — כלומר אותו instance של
    הרכיב ממשיך לחיות כש-`jobId` מתחלף. ה-`load` (`useCallback`
    עם תלות ב-`jobId`) עשה `await authFetch(...)` ואז
    `setJob`/`setImgUrl`/`setFinalText` ללא תנאי — תגובה איטית
    לעבודה A שמגיעה אחרי שהמשתמש כבר עבר לעבודה B הייתה דורסת את
    נתוני B בנתוני A הישנים, בלי שום שגיאה. תיקנתי לפי הדפוס
    שהוכח בסבב 363 (`27-bkalut-price`): `useRef` (`latestJobId`)
    שמתעדכן בכל render לערך ה-`jobId` הנוכחי, ובתוך ה-`load` נבדק
    `latestJobId.current !== requestedJobId` (הערך שנלכד ברגע
    שהבקשה יצאה) לפני שקוראים ל-setState — כך תגובה ישנה פשוט
    מתעלמת מעצמה. אפס שינוי בלוגיקת fetch/approve/save אחרת.
    בדיקת איזון סוגריים מסולסלים/עגולים/מרובעים ב-python על הקובץ
    המלא אחרי העריכה — תקין (186/186+242/242+40/40). אין build/
    dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. הקובץ
    תחת `apps/18-torah-editor-mvp/app/htr/page.tsx`, לא חסום
    ב-gitignore. ענף `fix/b-18-28-stale-async-race-0820`.

    **זה סוגר את עדשת ה-race-condition (stale async response) על
    פני כל 7 האפליקציות החיות בהיקף loop B** (17/18/21/22/24/27/28)
    — 17/21/24/27 בסבב 363, 18/28 כאן.

    **הבא בתור:** נושא #250 (RLS על `21-mthbram`, חסום MCP —
    פרויקט `aypsqqvfohekxxuqsmrw` לא נגיש), או לפתוח עדשה חדשה
    לפי סריקת Explore רחבה על כל 7 האפליקציות (למשל: `role="button"`
    בלי `tabIndex`/`onKeyDown`, או טפסים עם auto-save שקט על שדות
    רגישים).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 372 (loop B)

372. **פתיחה+סגירה של עדשת נגישות-מקלדת (`role="button"` בלי
    `tabIndex`/`onKeyDown`) על פני כל 7 האפליקציות החיות בהיקף
    loop B**, לפי ההצעה בסוף סבב 371. Explore agent סרק את כל 7
    האפליקציות וזיהה 4 מועמדים, כולם ב-`21-mthbram` — כרטיסי-שיעור
    לחיצים (`<motion.div onClick={...}>` בלי סמנטיקת מקלדת) בשלושה
    עמודים ורכיב אחד: `pages/LessonDirectory.tsx` (שורה 223,
    `setSelectedLesson(lesson)`), `components/LessonsDashboard.tsx`
    (שורה 41-44, `LessonSquare` — `onClick` prop מה-parent),
    `pages/PublicOrgPage.tsx` (שורה 124) ו-`pages/PublicRabbiPage.tsx`
    (שורה 135) — שני האחרונים באותו דפוס בדיוק (עמודי "רב"/"ארגון"
    ציבוריים המציגים את רשימת השיעורים שלהם). קראתי כל אחד ידנית
    לפני עריכה; שאר 6 האפליקציות (17/18/22/24/27/28) נבדקו ע"י
    ה-Explore agent ולא נמצאו בהן מועמדים תואמים (רק `<button>`/
    shadcn `Button` אמיתיים, או `role="button"`+`tabIndex`+
    `onKeyDown` שכבר קיימים כמו ב-`22-get-your-rights` — ראו
    `AdminLeads.tsx`/`AdminRightsReference.tsx`/`RightsCategories.tsx`
    שכבר תקינים ושימשו כדוגמת-דפוס לתיקון כאן). לכל אחד מ-4 המיקומים
    הוספתי `role="button"` + `tabIndex={0}` + `aria-label` דינמי
    (לפי `lesson.subject`/`lesson.rabbi_name`) + `onKeyDown` שמפעיל
    את אותה פונקציית-הקליק בלחיצת Enter/רווח (`e.preventDefault()`
    לפני הקריאה, תואם לדפוס שכבר קיים ב-22) — אפס שינוי בלוגיקת
    state/ניווט/API אחרת. בדיקת איזון סוגריים מסולסלים/עגולים/
    מרובעים ב-python על כל 4 הקבצים המלאים אחרי העריכה — תקין בכולם
    (124/124+124/124+30/30, 111/111+107/107+21/21, 152/152+100/100+
    37/37, 157/157+93/93+31/31). אין build/dev-server זמין בסביבה
    הזו לפי הנחיית ההרצה — לא `tsc`. כל 4 הקבצים תחת
    `apps/21-mthbram/src/{pages,components}`, אינם חסומים
    ב-gitignore (נתפסו ב-`git status` הרגיל, בלי `-f`). ענף
    `fix/b-21-mthbram-lesson-card-keyboard-a11y-0820`.

    **זה סוגר את עדשת נגישות-המקלדת (`role="button"` בלי
    `tabIndex`/`onKeyDown`) על פני כל 7 האפליקציות החיות בהיקף
    loop B** — 21 היה היחיד עם ממצאים אמיתיים, שאר 6 כבר תקינים.

    **הבא בתור:** נושא #250 (RLS על `21-mthbram`, חסום MCP —
    פרויקט `aypsqqvfohekxxuqsmrw` לא נגיש), או עדשה חדשה — טפסים
    עם auto-save שקט על שדות רגישים, או `<img>` בלי `alt`, או
    בדיקת focus-trap בדיאלוגים/מודלים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 373 (loop B)

373. **פתיחת עדשת `<img>` בלי `alt` וסגירתה מיידית (נקי) + עדשת
    focus-trap בדיאלוגים/מודלים מותאמים-אישית (לא Radix), לפי שתי
    ההצעות בסוף סבב 372, על פני כל 7 האפליקציות החיות בהיקף loop B**
    (17/18/21/22/24/27/28). `grep -rn "<img"` ידני על כל 7: כל
    מופע כבר כולל `alt` (כולל `alt=""` מכוון לתמונות דקורטיביות —
    התנהגות נכונה, לא ליקוי) — **נקי, לא נמצא ממצא**, כולל הבדיקה
    הידנית על תגי `<img` רב-שורתיים ב-18/21/22 שנחתכו ב-grep.
    עדשת ה-focus-trap: איתרתי את כל הדיאלוגים/מודלים המותאמים-אישית
    (לא בנויים על `@/components/ui/dialog` מבוסס-Radix, שכבר כולל
    focus-trap מובנה) — `apps/21-mthbram/src/components/
    SynagogueDetailModal.tsx` וגם `RightsCategories.tsx` ב-22
    משתמשים ב-Radix `Dialog`/`DialogContent`, נקיים. הממצא האמיתי:
    `apps/21-mthbram/src/components/LessonDetailModal.tsx` —
    רכיב-מודל משותף יחיד (לא Radix, `motion.div` עם overlay ידני)
    שמשמש ב-7 מקומות שונים באפליקציה (`PublicRabbiPage.tsx`,
    `PublicOrgPage.tsx`, `LessonDirectory.tsx`, `FindLesson.tsx`,
    `FeaturedLessons.tsx`, `LessonsDashboard.tsx`,
    `UpcomingLessonsCarousel.tsx`, `AdminDashboard.tsx`) — כבר טיפל
    ב-Escape וב-focus-restore-on-close, אבל חסר: `role="dialog"`/
    `aria-modal`/שם נגיש, focus ראשוני לתוך המודל בפתיחה, ו-Tab
    trap (Tab יכול היה לצאת מהמודל אל התוכן החסום מאחורי ה-overlay).
    הוספתי `panelRef`+`role="dialog"`+`aria-modal="true"`+
    `aria-labelledby="lesson-modal-title"` על ה-panel הפנימי,
    `id="lesson-modal-title"` על ה-`<h2>` הקיים, `closeButtonRef`
    עם `.focus()` באפקט הפתיחה (התמקדות ראשונית בכפתור הסגירה,
    הפריט הראשון בסדר ה-DOM), ולוגיקת Tab-trap מינימלית בתוך
    ה-`handleKeyDown` הקיים (שאילתת `focusable` בתוך ה-panel,
    לכידת Tab/Shift+Tab בקצוות ומחזור). אפס שינוי בלוגיקת
    share/download/contact/state אחרת — רק תוספת accessibility.
    בדיקת איזון סוגריים מסולסלים/עגולים/מרובעים ב-python על הקובץ
    המלא אחרי העריכה — תקין (177/177+105/105+18/18). אין build/
    dev-server זמין בסביבה הזו לפי הנחיית ההרצה — לא `tsc`. הקובץ
    תחת `apps/21-mthbram/src/components/LessonDetailModal.tsx`,
    אינו חסום ב-gitignore. ענף `fix/b-21-mthbram-lessondetailmodal-focus-trap-0820`.

    **זה סוגר את עדשת focus-trap על היחיד שדרש תיקון (Radix כבר
    מכסה את כל השאר), ואת עדשת ה-`<img>` alt (נקייה מלכתחילה) על
    פני כל 7 האפליקציות החיות בהיקף loop B.**

    **הבא בתור:** נושא #250 (RLS על `21-mthbram`, חסום MCP —
    פרויקט `aypsqqvfohekxxuqsmrw` לא נגיש), או עדשה חדשה — טפסים
    עם auto-save שקט על שדות רגישים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 374 (loop B)

374. **`apps/22-get-your-rights/src/pages/AdminLeads.tsx` — שני
    פקדי `<select>` סינון (מקור, שורה 299; סוג שירות, שורה 303)
    היו ללא שם נגיש**, בעוד ש-`<select>` אחרים באותה אפליקציה
    (`AdminRightsReference.tsx`, `RightsCategories.tsx`) כבר נושאים
    `aria-label`, מה שמאשר את הדפוס הקיים. הוספתי
    `aria-label="סינון לפי מקור"` ו-`aria-label="סינון לפי סוג
    שירות"` בהתאמה — שינוי attribute בלבד, אפס שינוי לוגיקה/פריסה.
    אומת דרך `git diff` (2 הוספות/2 מחיקות, שינוי מדויק של 2
    שורות) לפני commit. `commit 3c7f8b8e`, ענף
    `fix/b-22-getrights-adminleads-select-aria-label-0820`.

    **הבא בתור:** נושא #250 (RLS על `21-mthbram`, חסום MCP), או
    עדשה חדשה — auto-save שקט על שדות רגישים.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 375 (loop B)

375. **פתיחת עדשת auto-save שקט על שדות רגישים (הוצעה בסוף סבב
    373), על פני כל 7 האפליקציות החיות בהיקף loop B**
    (17/18/21/22/24/27/28). Explore agent סרק את כל 7 האפליקציות
    אחר `onChange`/`onBlur` שמפעיל `mutate()`/כתיבה ישירה בלי
    feedback על הצלחה. 6 מתוך 7 נקיות (כל ה-mutations הרלוונטיים
    כבר כוללים `onSuccess` עם toast/refresh גלוי, או שאין דפוס
    auto-save בכלל). הממצא האמיתי: `apps/27-bkalut-price/client/
    src/pages/financial.tsx` — שלושה `useMutation` שכתובים ישירות
    מ-`onChange` (לא כפתור submit): `OpportunitiesTab.update`
    (שורה 555, `<select>` סטטוס הזדמנות), `LeadsTab.update` (שורה
    697, `<select>` סטטוס פנייה — עסקי/משק בית), ו-`TipsTab.update`
    (שורה 761, checkbox נראות טיפ ללקוחות) — לכולם היה `onError`
    עם toast, אבל **לא** `onSuccess` toast, כך שסטטוס עסקי-קריטי
    (הזדמנות/ליד/נראות תוכן) משתנה בשקט ב-DB בלי שום אישור למשתמש
    שהשינוי אכן נשמר. תוקן על ידי הוספת `toast({ title: ... })`
    בתוך `onSuccess` (לצד ה-`invalidateQueries` הקיים), תואם בדיוק
    לדפוס ההצלחה הקיים כבר באותו קובץ (`onSuccess` של הוספת לקוח,
    שורה 286; הקצאת מאמן, שורה 598). אפס שינוי ללוגיקת
    mutation/API/query-shape — רק feedback. בדיקת איזון סוגריים
    ב-python על הקובץ המלא אחרי העריכה — תקין (531/531+455/455+
    72/72). אין build/dev-server זמין לפי הנחיית ההרצה — לא `tsc`.
    הקובץ תחת `apps/27-bkalut-price/client`, חסום ב-gitignore,
    שימוש ב-`git add -f` לפי התקדים הקיים. ענף
    `fix/b-27-bkalut-financial-status-silent-savesuccess-0820`.

    **זה סוגר את עדשת auto-save שקט על פני כל 7 האפליקציות החיות
    בהיקף loop B.**

    **הבא בתור:** נושא #250 (RLS על `21-mthbram`, חסום MCP —
    פרויקט `aypsqqvfohekxxuqsmrw` לא נגיש), או עדשה חדשה
    (validation clientside שחסרה לפני כתיבה, או race condition
    נוסף שטרם נבדק).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 376 (loop B)

376. **פתיחת עדשת validation clientside חסרה לפני כתיבה (הוצעה
    בסוף סבב 375), על פני כל 7 האפליקציות החיות בהיקף loop B**
    (17/18/21/22/24/27/28). Explore agent סרק טפסים/עריכות-inline
    שכותבים ל-DB (Supabase insert/update, `useMutation`, fetch
    POST/PATCH) בלי לוודא ששדות טקסט/מספר חובה אינם ריקים —
    התמקדות בשדות עסקיים מובהקים (שם/טלפון/עיר/כתובת/מחיר), לא
    בשדות הערות אופציונליים. הממצא המדורג ראשון:
    `apps/21-mthbram/src/components/questionnaire/TeacherForm.tsx`
    (שורה 63) — התווית "עיר *" (שורה 129) מסמנת את שדה העיר כחובה,
    אבל `handleSubmit` בדק רק `fullName`/`phone` לפני
    `supabase.from("teacher_leads").insert(...)`, ו-`city` נכתב
    כ-`data.city || null` (שורה 69) — כלומר ליד מורה יכול להישמר
    ב-DB החי בלי עיר כלל, חרף ההבטחה ב-UI שהשדה חובה. עיר היא
    השדה שהתאמת שיעורים תלויה בו למורה. תוקן בהרחבת אותה בדיקת
    guard (no-op שקט) הקיימת כבר ל-fullName/phone, כעת כוללת גם
    `!data.city?.trim()` — לא נוסף דפוס UX חדש, רק עקביות עם
    הבדיקה הקיימת. `git diff`: שינוי שורה יחידה. ענף
    `fix/b-21-mthbram-teacherform-city-required-0820`, commit
    `c6cdf0b4`, נדחף (מפעיל פריסת Vercel תחת `more30.com/mthbram`).

    **נמצאו מועמדים נוספים לאותה עדשה** (`RequestLesson.tsx`
    ב-21-mthbram, שדות `addressCity`/`addressStreet`;
    `public-price-comparison.tsx` ב-27-bkalut-price, שדה `city`;
    `Footer.tsx` ב-22-get-your-rights, חסר `.trim()` על שם/טלפון
    למרות `required`) — לא טופלו בסבב זה כדי לשמור על צעד יחיד
    ומינימלי; נשארים בתור להמשך העדשה בסבב הבא.

    **הבא בתור:** המשך עדשת validation clientside על שאר
    המועמדים שנמצאו (RequestLesson.tsx / public-price-comparison.tsx
    / Footer.tsx), או נושא #250 (RLS על 21-mthbram, חסום MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 377 (loop B)

377. **המשך עדשת validation clientside חסרה לפני כתיבה — מועמד
    שני מתוך שלושה שנמצאו בסבב 376.**
    `apps/21-mthbram/src/pages/RequestLesson.tsx` שלב 16 (כתובת) —
    התוויות "עיר *" (שורה 303) ו"רחוב *" (שורה 305) מסמנות את
    השדות כחובה, אבל כפתור "המשך ←" (שורה 306) התקדם לשלב השליחה
    ללא כל בדיקה, כך שליד בקשת שיעור (`seeker_leads`) יכול היה
    להישמר עם כתובת ריקה חרף ההבטחה ב-UI. מיקום הוא השדה שהתאמת
    שיעורים תלויה בו, בדיוק כמו התיקון המקביל ב-TeacherForm.tsx
    בסבב 376. תוקן בהוספת אותו דפוס guard (no-op שקט + טוסט שגיאה)
    הקיים כבר בשלב 1 של אותו טופס (contactName/contactPhone) —
    לא נוסף דפוס UX חדש. `git diff`: 4 שורות נוספו, 1 הוסרה.
    ענף `fix/b-21-mthbram-requestlesson-address-required-0820`,
    commit `faddc11e`, נדחף (מפעיל פריסת Vercel תחת
    `more30.com/mthbram`).

    **נותר בתור (מסבב 376):** `public-price-comparison.tsx`
    ב-27-bkalut-price (שדה `city` לא מאומת), `Footer.tsx`
    ב-22-get-your-rights (חסר `.trim()` על שם/טלפון חובה).

    **הבא בתור:** המשך עדשת validation clientside על שני
    המועמדים הנותרים, או נושא #250 (RLS על 21-mthbram, חסום MCP).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 378 (loop B)

378. **סיום עדשת validation clientside חסרה לפני כתיבה — שני
    המועמדים האחרונים מסבב 376.** בדיקה של
    `public-price-comparison.tsx` ב-27-bkalut-price העלתה שהמועמד
    היה false positive: שדה ה-`city` בטופס ההגשה (שורה 737) מוצג
    כ-placeholder `"עיר"` **בלי** כוכבית, ואינו מסומן `required` —
    גם השרת (`server/price-comparison.ts`) מגדיר אותו `city?:
    string | null` אופציונלי. אין כאן פער בין ההבטחה ב-UI למימוש,
    ולכן לא בוצע שינוי שם. המועמד השני,
    `apps/22-get-your-rights/src/components/Footer.tsx` — טופס
    ה"הצטרפו וקבלו עדכונים" מסמן שם/טלפון כחובה (placeholder `"*"`
    + attribute `required`, שורות 91–106), אך `handleSubmit` (שורה
    33) בדק רק אמת-בוליאנית (`!formData.name`) ולא `.trim()`, כך
    שקלט של רווחים בלבד עבר את הבדיקה ונכתב ל-`leads` כערך שדה
    חובה שנראה ריק. תוקן בהרחבת אותו דפוס guard (`.trim()`) שכבר
    הופעל על TeacherForm.tsx/RequestLesson.tsx בסבבים 376–377 —
    לא נוסף דפוס UX חדש. `git diff`: שינוי שורה יחידה. ענף
    `fix/b-22-get-your-rights-footer-trim-required-0820`, commit
    `95e7737e`, נדחף (מפעיל פריסת Vercel תחת `more30.com/zchuyot`).
    זו תעודת הסיום לעדשה שנפתחה בסבב 376 — כל שלושת המועמדים
    טופלו (שניים תוקנו, אחד אומת כ-false positive).

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או פתיחת
    עדשה חדשה על שאר האפליקציות בהיקף loop B (18/19/20/23/24/25/28
    טרם נסרקו לעומק לאותה עדשה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 379 (loop B)

379. **עדשה חדשה: double-submit על כפתורי פעולה א-סינכרוניים בלי
    guard — נבדק על 7 האפליקציות החיות (17/18/21/22/24/27/28).**
    סוכן Explore סרק את כל 7 האפליקציות בחיפוש אחר כפתור שמפעיל
    כתיבה א-סינכרונית (insert/update/mutation) בלי `disabled`/מצב
    טעינה החוסם לחיצה כפולה. רוב הטפסים הציבוריים כבר מוגנים
    (`disabled={submitting/isSubmitting}` וכו'). מועמד יחיד אמיתי:
    `apps/21-mthbram/src/components/FloatingChatBot.tsx` —
    `handleSearchFollowUp` (שורה 204, מופעל מכפתורי ה-follow-up
    בצ׳אטבוט הציבורי, שורה 363) מפעיל `streamResponse` שמבצע עד
    שלוש כתיבות אמיתיות (`lessons`/`seeker_leads`/`teacher_leads`,
    שורות 131–135) — ללא בדיקת `isTyping` בתחילת הפונקציה, בניגוד
    ל-`sendMessage` (שורה 244) שכבר בודקת `!input.trim() ||
    isTyping` באותו קובץ. בפועל הכפתורים מוסתרים מה-DOM ברגע
    ש-`isTyping=true` (`showButtons = ... && !isTyping`, שורה 270),
    כך שהחלון לפגיעה בפועל צר מאוד (בין קליק לרינדור הבא), אבל
    אינו סגור לחלוטין (למשל שני אירועי click באותה סבב סינכרוני).
    תוקן בהוספת `if (isTyping) return;` בתחילת `handleSearchFollowUp`,
    באותו דפוס guard בדיוק שכבר קיים ב-`sendMessage` באותו קובץ —
    לא נוסף דפוס UX חדש. `git diff`: שורה אחת נוספה. איזון סוגריים
    נבדק על הקובץ המלא: תקין (177/177 מסולסלים, 186/186 עגולים,
    55/55 מרובעים). לא הופעל build/dev-server (לפי הנחיות ההרצה).
    הקובץ תחת `apps/21-mthbram/src` (חסום gitignore אך כבר עוקב),
    `git add -f`. ענף `fix/b-21-mthbram-chatbot-followup-doublesubmit-0820`,
    נדחף (מפעיל פריסת Vercel תחת `more30.com/mthbram`). זו סגירת
    עדשת ה-double-submit על כל 7 האפליקציות בהיקף loop B — יתר
    הטפסים כבר מוגנים.

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או פתיחת
    עדשה חדשה (למשל: מפתחות `key` לא-יציבים ברשימות שניתנות לסידור
    מחדש, או מאזיני אירועים/טיימרים ב-`useEffect` בלי ניקוי).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 380 (loop B)

380. **עדשה חדשה: מחיקת רשומות אמיתיות בלי אישור — נבדק על 7
    האפליקציות החיות (17/18/21/22/24/27/28).** נסו קודם שתי עדשות
    שיצאו נקיות בפועל (מאזיני אירועים/טיימרים ב-`useEffect` בלי
    ניקוי — הכל תקין; מפתחות `key` לא-יציבים ברשימות — כל
    המועמדים היו שדות קלט מבוקרים (`value=`/`onChange=`) כך שאין
    השחתת נתונים אמיתית, רק סיכון קוסמטי זניח; כפתור בלי
    `type="button"` בתוך `<form>` — כל הכפתורים שנבדקו כבר מסומנים
    כראוי). המועמד האמיתי: כפתורי מחיקה באזורי ניהול שמפעילים
    `supabase....delete()` ישירות מ-`onClick`, ללא `confirm()`,
    בזמן שברוב האפליקציות בהיקף loop B (21/18/27/22-AdminSettings)
    כבר קיים דפוס `if (!confirm("...")) return;` לפני כל מחיקה.
    נמצאו 6 כפתורי מחיקה חסרי-אישור: ב-`apps/24-galilee-connect-hub/src/pages/GabaiPortal.tsx`
    ארבעה `handleDelete` (הלכה יומית שורה 713, גיליון newsletters
    שורה 837, באנר פרסומת שורה 1101, פריט מאגר-ידע שורה 1234) —
    לעומת מחיקת בית-כנסת באותו קובץ (שורה 109) שכן עוברת דרך מצב
    `confirmDelete` דו-שלבי; וב-`apps/24-galilee-connect-hub/src/components/GallerySection.tsx`
    (שורה 54) ו-`ActivitySlideshow.tsx` (שורה 56) — מחיקת תמונות
    ציבוריות. וגם `apps/22-get-your-rights/src/pages/AdminRightsReference.tsx`
    (שורה 379, מחיקת נושא זכות) — לעומת `AdminSettings.tsx` באותה
    אפליקציה שכבר משתמש ב-`confirm()` למחיקת מפתח API. תוקן בהוספת
    `if (!confirm("...")) return;` כשורה ראשונה בכל אחת מ-7 פונקציות
    ה-`handleDelete`, באותו דפוס guard בדיוק שכבר קיים ב-21-mthbram/
    18-torah-editor-mvp/27-bkalut-price/22-get-your-rights — לא נוסף
    דפוס UX חדש. `git diff`: 7 שורות נוספו בלבד, 4 קבצים. איזון
    סוגריים נבדק על כל 4 הקבצים המלאים לאחר העריכה: תקין בכולם.
    לא הופעל build/dev-server (לפי הנחיות ההרצה). הקבצים לא חסומי
    gitignore, `git add` רגיל. ענף `fix/b-24-22-unconfirmed-delete-buttons-0820`,
    נדחף (מפעיל פריסת Vercel תחת `more30.com/galil` ו-`more30.com/zchuyot`).
    זו סגירת עדשת האישור-לפני-מחיקה על כל המקרים שנמצאו בהיקף loop B.

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או בדיקת
    אותה עדשת אישור-לפני-מחיקה על 17/28 (טרם נסרקו לעומק לעדשה זו),
    או פתיחת עדשה חדשה.
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 381 (loop B)

381. **סגירת עדשת אישור-לפני-מחיקה על 17/28 (נקי) + פתיחת עדשה
    חדשה: כתיבות אסינכרוניות ששגיאה בהן נבלעת בשקט — נבדק על 7
    האפליקציות החיות (17/18/21/22/24/27/28).** תחילה סגרתי את
    הנושא שנשאר פתוח מסבב 380: סוכן Explore סרק לעומק את
    `17-chizukim-transcribe` ו-`28-kupot-health-funds` בחיפוש אחר
    כפתורי מחיקה — שתי האפליקציות חזרו נקיות לגמרי (17 היא מערכת
    תמלול קריאה/עריכה/העלאה בלבד בלי שום פונקציונליות מחיקה; 28
    היא כלי השוואה לקריאה-בלבד עם טופס הגשה יחיד, בלי מסך ניהול
    לקוח שמוחק נתונים). זה סוגר את עדשת אישור-לפני-מחיקה על כל 7
    האפליקציות בהיקף loop B. פתחתי מיד עדשה חדשה: פונקציית כתיבה
    (insert/update/delete אמיתי דרך supabase או fetch פנימי)
    שעטופה ב-try/catch שבו ה-catch לא עושה שום דבר גלוי למשתמש
    (רק `console.error`/כלום) — המשתמש לוחץ "שמור"/"מחק" וחושב
    שהפעולה הצליחה גם כשהיא נכשלה בפועל. סוכן Explore סרק את כל 7
    האפליקציות; מועמד אמיתי יחיד: `apps/21-mthbram/src/pages/OrgPortal.tsx`
    שורה 114, `removeRabbi` — `supabase.from("org_rabbis").delete()`
    עם `if (!error) { ... toast.success(...) }` בלי `else`, כך
    שכישלון מחיקה לא מציג כלום למשתמש — לעומת `addRabbi` באותו
    קובץ בדיוק (שורה 95-112) שכבר משתמש ב-`else toast.error("שגיאה
    בהוספת רב")`. תוקן בהוספת `else toast.error("שגיאה בהסרת
    הרב")`, אותו דפוס `sonner` בדיוק שכבר קיים בכל הקובץ. `git
    diff`: שורה אחת שונתה. איזון סוגריים נבדק על הקובץ המלא לאחר
    העריכה: תקין (177/177 מסולסלים, 193/193 עגולים). לא הופעל
    build/dev-server (לפי הנחיות ההרצה). הקובץ תחת
    `apps/21-mthbram/src` (חסום gitignore אך כבר עוקב), `git add`
    רגיל עבד (קובץ קיים כבר עוקב, ה-hint על "ignored" לא מנע
    staging בפועל). ענף
    `fix/b-21-mthbram-removerabbi-silent-error-0820`, קומיט
    `217a852f`, נדחף (מפעיל פריסת Vercel תחת `more30.com/mthbram`).

    שאר 6 האפליקציות חזרו נקיות בעדשה הזו (כל כתיבה נכשלת שנבדקה
    כבר מציגה toast/alert גלוי, או שאין כתיבות משתמש-מופעלות
    שנכשלות בשקט).

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או
    פתיחת עדשה חדשה (למשל: אימות מספרי/תאריך חסר בטפסים, או
    `dangerouslySetInnerHTML` בלי סניטציה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 382 (loop B)

382. **פתיחת עדשת `dangerouslySetInnerHTML` בלי סניטציה (מוצע בסבב 381)
    על 7 האפליקציות החיות (17/18/21/22/24/27/28) — נמצא XSS אמיתי אחד,
    תוקן.** סוכן Explore סרק את כל שימושי `dangerouslySetInnerHTML`,
    `eval(`, ו-`.innerHTML =` הישיר בשבע האפליקציות. רוב הממצאים היו
    שליליים-כוזבים: `22-get-your-rights/src/components/RightBrandedCard.tsx`
    (שורות 213, 305) ו-`17-chizukim-transcribe/client/src/pages/recording-detail.tsx`
    (שורה 324) מריצים `escapeHtml()` על כל תוכן משתמש לפני ההזרקה, עם
    תגיות HTML רק מתבניות קבועות; קבצי `chart.tsx` (6 עותקים בכל
    האפליקציות, shadcn) מזריקים רק צבעי CSS קבועים מ-`config`;
    `28-kupot-health-funds/server/storage.ts:27` הוא `eval("require")`
    עם מחרוזת קבועה (עקיפת bundler, לא קלט משתמש).

    הממצא האמיתי: `apps/24-galilee-connect-hub/src/components/ChatBot.tsx`
    שורה 529-533 — הצ'אטבוט מריץ `escapeHtml(msg.content)` ואז שני
    `replace` של מרקדאון (`**bold**` ו-`[text](url)`) להמרה ל-HTML.
    ה-`escapeHtml` מטפל בתווי `< > & " '` אבל לא בתווי `[ ] ( )`, כך
    שקישור מרקדאון עם סכימת `javascript:`/`data:` שורד את הבריחה
    ומגיע ל-`replace` השני כמו שהוא. הבעיה נהיית ניתנת לניצול בפועל
    כי `handleNameResponse` (שורה 392) משבץ את השם שהמשתמש הקליד
    ישירות לתוך הודעת בוט: `` שלום **${name}**! `` — אין שום בדיקה על
    תוכן ה-`name`. שם כמו `[x](javascript:alert(1))` שורד את
    ה-escape (אין בו תווי HTML מיוחדים), עובר את regex ה-bold
    (הופך ל-`<strong>[x](javascript:alert(1))</strong>`), ואז regex
    הקישור הופך אותו לעוגן `javascript:` חי שמוזרק דרך
    `dangerouslySetInnerHTML` — לחיצה עליו מריצה קוד. תוקן: regex
    הקישור עבר מ-string-replace קבוע לפונקציית replace שבודקת
    `/^https?:\/\//i.test(url)` — רק קישורי http/https הופכים ל-`<a>`,
    כל השאר (כולל `javascript:`/`data:`) נשאר טקסט מרקדאון גולמי בלי
    לינק. הקישור האמיתי היחיד באפליקציה (וואטסאפ, `https://wa.me/...`
    בשורה 417) ממשיך לעבוד כרגיל. איזון סוגריים נבדק על הקובץ המלא
    לאחר העריכה: תקין (278/278 מסולסלים, 369/369 עגולים). לא הופעל
    build/dev-server (לפי הנחיות ההרצה). הקובץ תחת
    `apps/24-galilee-connect-hub/src` (חסום ב-`.gitignore` שורה 29
    `/apps/**`, אבל כבר עוקב בפועל — `git add` הרגיל סירב הפעם
    (בניגוד לסבב 381), נדרש `git add -f` על הקובץ הבודד שכבר עוקב).
    ענף `fix/b-24-galilee-chatbot-markdown-link-xss-0820`, קומיט
    `45017833`, נדחף (מפעיל פריסת Vercel תחת `more30.com/galil`).

    שאר 6 האפליקציות (17/18/21/22/27/28) חזרו נקיות בעדשה הזו — אין
    בהן אף שימוש לא-בטוח ב-`dangerouslySetInnerHTML`/`eval`/`innerHTML`.

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או פתיחת
    עדשה חדשה (למשל: אימות מספרי/תאריך חסר בטפסים, או input-type
    שגוי כמו `type="text"` על שדה מספר/טלפון).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 383 (loop B)

383. **עדשה חדשה: input-type שגוי על שדה טלפון (מוצע בסבב 382) —
    נבדק על 7 האפליקציות החיות (17/18/21/22/24/27/28), נמצא מקרה
    אחד, תוקן.** סוכן Explore סרק כל שדה קלט שאוסף מספר טלפון/מספר
    טהור בשבע האפליקציות, מחפש `type="text"` (ברירת המחדל, כשלא
    מוגדר type בכלל) במקום `type="tel"`/`type="number"` +
    `inputMode` תואם. אימתתי שבשתיים מתוך שבע האפליקציות
    (`22-get-your-rights`, `27-bkalut-price`) כל שדה טלפון שנבדק
    כבר משתמש בקביעות ב-`type="tel" inputMode="tel"` — זו המוסכמה
    הקיימת בקוד. הממצא היחיד שסטה מהמוסכמה:
    `apps/21-mthbram/src/components/portal/PublicContactForm.tsx`
    שורה 124 — שדה הטלפון בטופס יצירת הקשר הציבורי (מוצג בשלושה
    סוגי פורטל: בית כנסת/ארגון/רב) היה `<Input value={phone}
    onChange=... />` בלי שום `type` (כלומר `text` דפדפני), בזמן
    ששדה המייל **באותו קובץ בדיוק**, שורה 129, כבר משתמש ב-
    `type="email"` — כך שהמוסכמה הפנימית של אותו רכיב עצמו כבר
    קובעת type תואם לפי סוג השדה, וטלפון הוא היחיד שפספס אותה.
    תוקן בהוספת `type="tel" inputMode="tel"` לשדה, זהה לתבנית
    שכבר קיימת ב-10+ שדות טלפון אחרים ב-22/27 — לא הומצא UX חדש.
    לא נוספה ולידציית פורמט חדשה (למשל regex) כי המוסכמה הקיימת
    בקוד מסתמכת רק על ה-type/inputMode של הדפדפן, לא על בדיקה
    ידנית בקוד — שמירה על אותה רמת אימות שכבר קיימת בכל שאר שדות
    הטלפון בהיקף loop B. `git diff`: שורה אחת שונתה. איזון סוגריים
    נבדק על הקובץ המלא: תקין (66/66 מסולסלים, 45/45 עגולים, 9/9
    מרובעים). לא הופעל build/dev-server (לפי הנחיות ההרצה). הקובץ
    תחת `apps/21-mthbram/src` (חסום gitignore אך כבר עוקב), `git
    add` רגיל עבד (כמו בסבב 381). ענף
    `fix/b-21-mthbram-contact-phone-input-type-0820`, נדחף (מפעיל
    פריסת Vercel תחת `more30.com/mthbram`).

    שאר 6 האפליקציות (17/18/22/24/27/28) חזרו נקיות בעדשה הזו —
    כל שדה טלפון/מספר שנבדק כבר משתמש ב-type/inputMode תואם.

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או המשך
    עדשת ה-validation לצד ה-JS (בדיקת regex/isNaN לפני כתיבה, לא
    רק type HTML), או פתיחת עדשה חדשה (למשל: `<img>` בלי `alt` על
    תמונות שהועלו ע"י משתמש, או קישורי `target="_blank"` בלי
    `rel="noopener noreferrer"`).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 384 (loop B)

384. **עדשה חדשה: `target="_blank"` בלי `rel` מגן (מוצע בסבב 383) —
    נבדק על 7 האפליקציות החיות (17/18/21/22/24/27/28), נמצאו 3
    מקרים אמיתיים, תוקנו.** סוכן Explore סרק כל שימוש ב-
    `target="_blank"` בשבע האפליקציות. חשוב: `rel="noreferrer"`
    לבדו (בלי `noopener`) **כבר חוסם** גישת `window.opener` לפי
    תקן ה-HTML (noreferrer מרמז על noopener) — כך שרוב הממצאים
    הראשוניים (21-mthbram: 3 קישורי וואטסאפ עם `rel="noopener"`
    בלבד; 24-galilee: 6 מקרים; 27-bkalut-price: 21 מקרים עם
    `rel="noreferrer"` בלבד) הם **לא** פרצת tabnabbing בפועל —
    כבר מוגנים, רק חסר `noreferrer`/`noopener` השני שהוא שיפור
    קוסמטי בלבד (לא נגעתי בהם, כדי לא "לתקן" דבר שלא שבור).

    הממצא האמיתי היחיד: שלושה קישורים ב-27-bkalut-price עם
    `target="_blank"` **בלי שום `rel`** — שני מופעים ב-
    `client/src/pages/service-form.tsx` (שורות 661, 681, קישורי
    "תנאי השירות ומדיניות פרטיות" בטופס פתיחת פנייה) ואחד ב-
    `client/src/pages/health-fund-service.tsx` (שורה 252, אותו
    קישור תנאים בטופס קופות החולים) — שלושתם היחידים בקובץ/באפליקציה
    שסטו מהמוסכמה הקיימת (21 מקרים אחרים באותה אפליקציה כן
    משתמשים ב-`rel="noreferrer"`). תוקן בהוספת `rel="noopener
    noreferrer"` לשלושתם, זהה למוסכמה הפנימית הרווחת ביותר
    באפליקציה. `git diff`: 3 שורות שונו בשני קבצים. איזון סוגריים
    נבדק על שני הקבצים המלאים: תקין (`service-form.tsx`
    276/276 מסולסלים, 326/326 עגולים; `health-fund-service.tsx`
    82/82 מסולסלים, 62/62 עגולים). לא הופעל build/dev-server (לפי
    הנחיות ההרצה). קבצי `apps/27-bkalut-price/client` חסומים
    ב-`.gitignore` אך כבר עוקבים; נדרש `git add -f` (כמו בסבב 382).
    ענף `fix/b-27-bkalut-terms-links-noopener-0820`, קומיט
    `ee231559`, נדחף.

    שאר 6 האפליקציות (17/18/22/28) — ללא שימוש ב-`target="_blank"`
    כלל (18/28) או שכל שימוש כבר כולל `rel="noopener noreferrer"`
    מלא (17/22). (21/24 יש להן ממצאי `rel` חלקי כמתואר למעלה —
    לא תוקנו כי כבר מוגנים בפועל.)

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או
    השלמת ה-`rel` החלקי ל-24/21 מטעמי סגנון עקבי (לא בטיחותי), או
    פתיחת עדשה חדשה (למשל: `<img>` בלי `alt` על תמונות שהועלו
    ע"י משתמש, או כפתורי submit שלא מנוטרלים תוך כדי בקשת רשת
    פעילה).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 385 (loop B)

385. **עדשה חדשה: `<img>` בלי `alt` תיאורי על תמונות דינמיות
    (מוצע בסבב 384) — נבדק על 7 האפליקציות החיות, נמצא ממצא
    שיטתי אחד באפליקציה אחת, תוקן.** לפני הפתיחה נבדקה קודם
    העדשה השנייה שהוצעה (כפתורי submit לא מנוטרלים תוך כדי בקשה
    פעילה — double-submit) על כל 7 האפליקציות: כל טופס משתמש
    (תשלום/ליד/פנייה/הרשמה) כבר משתמש ב-state של
    `loading`/`submitting`/`isSubmitting` שמנטרל את הכפתור וחוסם
    שליחה כפולה — נקי לגמרי, לא תוקן דבר.

    העדשה השנייה — `alt` על תמונות שאינן דקורטיביות (לוגו/תמונת
    פרופיל שמגיעה מ-Supabase storage, לא asset סטטי) — העלתה
    ממצא אמיתי: **16 מופעי `<img>` ב-`21-mthbram`** עם
    `alt=""` (ריק) על תמונות תוכן משמעותיות — לוגו בית כנסת/ארגון,
    תמונת פרופיל רב, לוגו שיעור/יום עיון — כך שקורא מסך לא מכריז
    כלום על תמונה משמעותית. תוקן בכל 16 המופעים בקובץ אחד לכל
    שינוי: `alt` הוחלף לביטוי תלוי-נתונים באמצעות שדה שם שכבר
    קיים ב-scope של אותו קומפוננטה (`portal.synagogue_name`,
    `portal.org_name`, `portal?.rabbi_name`, `lesson.rabbi_name`,
    `data.synagogue_name`, `form.synagogue_name`, `rabbiName`,
    `synagogueName` prop וכו') עם נפילה חזרה לטקסט עברי גנרי
    ("לוגו"/"תמונת פרופיל") כשהשדה ריק — לא הומצא שדה חדש, רק
    שימוש חוזר במה שכבר מוצג באותו קובץ ליד התמונה (למשל ב-
    `AdminDashboard.tsx` שורה 1244 השם `p.synagogue_name` כבר
    מוצג ב-`<h4>` הסמוך בשורה 1248). קבצים שתוקנו (16, שינוי שורה
    אחת בכל אחד): `pages/PublicSynagoguePage.tsx`,
    `pages/PublicRabbiPage.tsx`, `pages/PublicOrgPage.tsx`,
    `pages/LessonDirectory.tsx`, `pages/UpdateLesson.tsx`,
    `pages/RabbiPortal.tsx`, `pages/OrgPortal.tsx`,
    `pages/StudyDayUpload.tsx`, `pages/AdminDashboard.tsx`,
    `components/FeaturedLessons.tsx`,
    `components/SynagogueDetailModal.tsx`,
    `components/SynagogueShowcase.tsx`,
    `components/LessonDetailModal.tsx`,
    `components/studyday/StudyDayEventForm.tsx`,
    `components/portal/PortalSettingsTab.tsx`,
    `components/synagogue/SynagogueShowcaseCarousel.tsx`. כל שדה
    שם אומת ידנית שקיים בפועל בקובץ (לא הונח) לפני השימוש בו —
    כולל בדיקה ש-`form.synagogue_name`/`rabbiName` הם state קיים
    (לא שדה חדש) ב-`StudyDayUpload.tsx`/`StudyDayEventForm.tsx`/
    `UpdateLesson.tsx`, וש-`AdminDashboard.tsx` שורה 1244 היא
    בתוך `synagoguePortals.map` כך ש-`synagogue_name` הוא השדה
    הנכון. `git diff --stat`: 16 קבצים, 16+/16-. איזון סוגריים
    נבדק על כל 16 הקבצים לאחר העריכה: תקין בכולם (כולל
    `SynagogueShowcase.tsx` שיש בו פער עגולות קדם-קיים של 3-
    שלא נגרם מהעריכה הזו ולא השתנה). לא הופעל build/dev-server
    (לפי הנחיות ההרצה). קבצי `apps/21-mthbram/src` חסומים
    ב-`.gitignore` אך כבר עוקבים, `git add -f` נדרש על כל 16
    הקבצים. ענף `fix/b-21-mthbram-logo-alt-text-0820`, קומיט
    `908960d5`, נדחף (מפעיל פריסת Vercel תחת `more30.com/mthbram`).

    שאר 6 האפליקציות (17/18/22/24/27/28) — נבדקו לעדשה הזו:
    `27-bkalut-price` כבר מגן על כל תמונת לוגו עם `alt="תצוגת
    לוגו"`; `24-galilee-connect-hub` נמצאו 2 מופעים נוספים
    (`GabaiPortal.tsx` שורה 1329, `GallerySection.tsx` שורה 116)
    שלא תוקנו הסבב הזה — נשארים בתור להמשך; שאר 4 האפליקציות
    (17/18/22/28) משתמשות רק בנכסים סטטיים ב-`<img>`, אין ממצא.

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או
    השלמת ה-`alt` על 2 המופעים שנותרו ב-`24-galilee-connect-hub`
    (`GabaiPortal.tsx`/`GallerySection.tsx`), או השלמת ה-`rel`
    החלקי ל-24/21 מטעמי סגנון עקבי, או פתיחת עדשה חדשה (למשל:
    שדות תאריך/מספר ללא ולידציית טווח בצד לקוח, או `<label>` לא
    מקושר ל-`<input>` דרך `htmlFor`/`id`).
    via cloud server 167.99.131.167 [loop B]

## 20/08/2026 — סבב 386 (loop B)

386. **השלמת עדשת ה-`alt` התיאורי: 2 המופעים שנותרו ב-`24-galilee-connect-hub`
    (מתור מסבב 385) תוקנו.** `GabaiPortal.tsx` שורה 1329 — תמונה
    ממוזערת של פריט במאגר הידע (`item.image_url`) עם `alt=""`,
    למרות ש-`item.title` כבר בשימוש בשורה הסמוכה (1331) לתצוגת
    שם הפריט — הוחלף ל-`alt={item.title}`. `GallerySection.tsx`
    שורה 116 — תמונת ה-lightbox המוגדלת (`<img src={lightbox}>`)
    עם `alt=""`, כאשר ה-state `lightbox` מחזיק רק את ה-URL ולא את
    האובייקט המלא — נפתר ללא שינוי מבנה ה-state, באמצעות חיפוש
    התמונה התואמת במערך `images` הקיים כבר בזיכרון
    (`images.find(im => im.image_url === lightbox)?.caption`) עם
    נפילה חזרה לטקסט עברי גנרי ("תמונה מוגדלת") כשאין כיתוב.
    `git diff --stat`: 2 קבצים, 2+/2-. איזון סוגריים נבדק על שני
    הקבצים לאחר העריכה: תקין. נבדק שוב עם `grep -rn 'alt=""'` על
    כל `apps/24-galilee-connect-hub/src` — אין מופעים נוספים. לא
    הופעל build/dev-server. קבצי `apps/24-galilee-connect-hub/src`
    חסומים ב-`.gitignore` אך כבר עוקבים, `git add -f` נדרש. ענף
    `fix/b-24-galilee-alt-text-0820`, קומיט `dc93c08a`, נדחף
    (מפעיל פריסת Vercel תחת `more30.com/galil`).

    **הבא בתור:** נושא #250 (RLS על 21-mthbram, חסום MCP), או
    פתיחת עדשה חדשה (למשל: שדות תאריך/מספר ללא ולידציית טווח
    בצד לקוח, או `<label>` לא מקושר ל-`<input>` דרך
    `htmlFor`/`id`, או `target="_blank"` בלי `rel="noopener
    noreferrer"` על כל 7 האפליקציות החיות באופן שיטתי).
    via cloud server 167.99.131.167 [loop B]
