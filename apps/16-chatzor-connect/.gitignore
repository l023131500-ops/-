# dependencies
node_modules/

# build output
dist/
dist-ssr/

# env — NEVER commit real secrets; only .env.example (names only) is tracked
.env
.env.local
.env.*.local

# logs & editor
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
