## כלל-על — פונקציה עכשיו, ליטוש לבקלוג, שיטה עצמאית (חל על כל המערכות)

### א. פונקציה עכשיו, ליטוש לסוף
תקן עכשיו רק מה ששובר/מסכן: כפתור/מסך/זרימה שלא עובד, נתון שהולך למקום הלא-נכון, כניסה/הרשאה שבורה, קריסה, באג נתונים, אבטחה.
דחה לסוף — רשום ל-POLISH_BACKLOG.md (+ core.issues severity='low') והמשך: ניסוח/קופי, פיסוק, נגישות (aria/פוקוס/מקלדת), ניגודיות/מצב-כהה, מיקרו-UX, שיפור הודעת שגיאה כשכבר יש אחת עובדת.
בכל מערכת: הבא ל-v1 מלא (כל הכפתורים עובדים, נתוני אמת, פרוס וחי, אפס באגים) → עבור לבאה. מערכת שכבר עובדת ונשאר בה רק ליטוש → לבקלוג, אל תישאר עליה.

### ב. שיטה עצמאית ל-Supabase (אל תזדקק למשתמש בכל פעם)
השתמש ב-SUPABASE_ACCESS_TOKEN (core.secrets, service supabase-management) עם ה-Management API כדרך הסטנדרטית להגדיר Supabase לכל מערכת בעצמך:
- Auth: Redirect URLs (הוסף https://more30.com/*), כיבוי אישור-מייל, SMTP.
- Secrets/env, מיגרציות, RLS — לפי הצורך.
מוגן: אל תיגע ב-08/09/zr_*/bkalut.
אם הטוקן לא מגיע לפרויקט (403/404) — רשום ב-NEEDS_USER בדיוק איזה פרויקט ומה חסר, פעם אחת, ואל תחסום את השאר.

### ג. בסוף
כשכל המערכות עובדות ופרוסות — עבור על POLISH_BACKLOG והשלם ליטושים.
## ⚡ מיידי — עצור §5ב, בצע את §1בב++ קודם, ואז חזור ל-§5ב.
## §1בב++ — אישור לתקן הגדרות Supabase בכל הפרויקטים (הסרת חסם הכניסה)
אתה מאושר להשתמש ב-SUPABASE_ACCESS_TOKEN (core.secrets, service supabase-management) מול כל פרויקטי המשתמש כדי לתקן את הגדרות ה-Auth שחוסמות כניסה/איפוס:
1. Redirect URLs: הוסף https://more30.com/* לכל פרויקט — סוגר #202/#205/#206/#207/#208/#209.
2. אישור מייל: כבה "Confirm email" (auto-confirm) בכל פרויקט — סוגר #172.
3. SMTP (#203): אם חסר, הסתמך על auto-confirm; אל תחסום.
4. SUPABASE_SERVICE_ROLE_KEY: הגדר בסביבת Vercel של gesher (#180) מ-core.secrets.
מוגן: אל תיגע ב-08/09 (bkalut-app/bkalot-admin/zr_*). אמת כל שינוי וסגור את ה-issue.
## §1בב+ — האצה + steer (תוספת לתוכנית, לא במקומה)

1. אל תשקיע בנגישות-מקלדת / ליטוש של קונסולת הקיוסק עכשיו. הקיוסק מסלול נפרד (§ג) — עזוב אותו בינתיים.
2. סַיֵּם מערכות חיות אחת-אחת עד "מוכן" המלא (MORE30_MASTER_BUILD §2): כניסה, פעולה מרכזית, נתוני אמת, בדיקת כל כפתור/מסך, פרוס וחי.
3. לכל מערכת: כתוב QA/<שם>.md — מה עובד, מה תוקן, 3–6 המלצות שיפור; יישם את הבטוחות; עדכן STATUS.md.
4. החלטות שאושרו: בטל אישור-מייל (auto-confirm) בכל הפרויקטים; הסר סיסמאות מהקוד → STD_ADMIN_PASSWORD; גישה חינם לכל הפיצ'רים כברירת מחדל; חיבורים native פר-לקוח (INTEGRATIONS_BUILD.md) לתור §3/§8.

המשך לפי הסדר הקיים. זו האצה, לא נוסח חדש.
### #215 — הקיוסק: אחד לגרסת המונו-רפו
אחד את הקיוסק לגרסת המונו-רפו (apps/35-kioskfleet) — היא השלמה יותר (כולל מסך 'clients' = מודל מזהה-לקוח) וגם מכניסה את הקיוסק למקום אחד עם כל המערכות.
חובה לפני פריסה: אמת את גרסת המונו-רפו מקצה-לקצה (כל מסך, כל כפתור, כניסה, נעילה). רק אחרי אימות מלא — פרוס אותה כמקור היחיד במקום הגרסה החיה (zol), ואל תשאיר שתי גרסאות מפוצלות. אל תפרוס גרסה לא-בדוקה מעל אחת עובדת.
(זה כשמגיעים לקיוסק, §ג — לא עכשיו.)
## §1בב+ — החלטות שאושרו + האצה (תוספת לתוכנית, לא במקומה)

יישם את ההחלטות:
1. כניסה חסומה (#172): בטל אישור-מייל (auto-confirm) בכל הפרויקטים — הרשמה→כניסה עובדת מיד, בלי מחסום.
2. סיסמאות (#88 וכל דליפה): הסר כל סיסמה מהקוד, קרא מ-STD_ADMIN_PASSWORD, וסובב את הערך לחדש פעם אחת.
3. חיבורים (ימות/נדרים/מייל): native פר-לקוח לפי INTEGRATIONS_BUILD.md (לא N8N). לתור §3/§8.
4. גישה: ברירת מחדל חינם לכל הפיצ'רים (כבר ב-§1בב).

האצה — סַיֵּם מערכות, אל תשאיר פתוחים:
- לכל מערכת שאתה נוגע: סיים עד הגדרת "מוכן" המלאה של MORE30_MASTER_BUILD §2 (כניסה, פעולה מרכזית, נתוני אמת, פרוס וחי).
- בדוק כל פיצ'ר וכל כפתור בפועל (Playwright/דפדפן). כתוב ל-QA/<שם>.md: מה עובד, מה תוקן, ו-3–6 המלצות שיפור (§8). יישם את הבטוחות.
- עדכן STATUS.md אחרי כל מערכת — שהמשתמש יראה תוצאה: מה גמור, מה נשאר.
- המשך לפי הסדר הקיים בתוכנית. זו רק האצה — לא שינוי סדר, לא נוסח חדש.## §1בב — אבטחה + סיסמה אחידה + גישת חינם כברירת מחדל (מיידי, גבוה)

### 1. אבטחה — תקן, פרוס, ודווח כל בעיה
- תקן כל פרצת אבטחה. במיוחד: #62 גליל (?auto=admin פותח ניהול בלי סיסמה → סגור, בנה אימות בצד שרת); #88 קופות (סיסמה גלויה בקוד → הסר, קרא מ-core.secrets).
- חוק: שום סיסמה/סוד לא בקוד — תמיד מ-core.secrets/env.
- אחרי כל תיקון: כתוב ל-core.issues מה תוקן + פרוס, כדי שהמשתמש יֵדע.

### 2. סיסמת אדמין אחת לכל המערכות
- מקור יחיד: core.secrets → STD_ADMIN_USER + STD_ADMIN_PASSWORD (scope=all). כל מערכת (כולל קיוסק+גן-קליק) קוראת מהם ומבצעת upsert בכל עלייה.
- לקוח בדיקה אחיד: TEST_CUSTOMER_EMAIL + TEST_CUSTOMER_PASSWORD.
- כתוב/עדכן LOGINS.md עם הפרטים האחידים לכל מערכת.

### 3. גישה — ברירת מחדל: הכל חינם
- כל עוד לא הוגדר מה כלול בכל מסלול → תן גישה חופשית לכל הפיצ'רים בכל המערכות. אל תנעל כלום מאחורי מנוי. מסלולי 2/5/10/12/15 ₪ = תצוגה בלבד.
- עתידי (רשום ל-core.issues, אל תבנה עכשיו): פונקציה בניהול שקובעת מה מקבל חינם/פרימיום/מקסימום.

### 4. ספקים קיימים — הפסק לחסום
- נדרים פלוס: קיים ב-core.secrets — השתמש, סגור #7.
- ימות: ייכנס תחת service='yemot' כשיסופק; עד אז אל תחסום מערכות אחרות בגללו.## §0.2 — פריסות שהמתינו למכסת Vercel (#83) — נסה שוב עכשיו (השודרג ל-Pro)
מכסת הפריסה החינמית הוסרה (החשבון עבר ל-Vercel Pro). נסה מחדש את כל הפריסות שנחסמו על #83:
- גן-קליק (40) תחת more30.com/gannenet — פרוס ואמת שהוא חי (מחזיר 200).
- כל מערכת אחרת שסומנה "תוקן במקור, לא נפרס / חסום על #83" — פרוס ואמת.
תזכורת: אם הפריסה דורשת SUPABASE_SERVICE_ROLE_KEY בסביבת Vercel (גן-קליק, עימוד #04) והוא עדיין חסר — רשום ל-NEEDS_USER.md, אל תיתקע.
אחרי כל פריסה מוצלחת: heartbeat + עדכן core.issues שהחסם נסגר.### §0.3 — קליטת חבילות תוכן לגן-קליק (אוטומטי, קבוע)
בכל מעבר על גן-קליק, בדוק אם בתיקייה `gannenet-incoming/` יש קובצי ZIP חדשים. לכל ZIP:
1. חלץ. אם יש בו קובץ הוראות (הוראות.md/README) — פעל לפיו; אחרת החלף/הוסף את קבצי content/lib/app שבו לתוך gannenet-app.
2. ⚠️ מזג, אל תמחק — שמור את כל התוכן הקיים, הוסף/עדכן רק את החדש.
3. `npm run build` + אמת שהעמודים נוצרים. `npm run dev` + בדוק /library.
4. עבר → commit+push, והעבר את ה-ZIP ל-`gannenet-incoming/done/`. נשבר → עצור, רשום ל-NEEDS_USER, אל תמחק כלום.

אימות (חובה, לפי הוראות המשתמש):
1. `npm run build` בתוך gannenet-app → ודא שנוצרים 180 עמודי `/lesson/[id]`.
2. `npm run dev` → ב-`/library` מופיעים 180 מערכים עם סינון לפי תחום, ופתיחת מערך מציגה את פורמט המסירה המלא.
אם 180 לא נוצרים או משהו נשבר → עצור, רשום ל-NEEDS_USER.md, אל תמשיך. אחרת: commit+push. אל תיגע במערכות אחרות.# more30 — הנחיית עדיפות עליונה (קרא ובצע קודם)

> המטרה: **מערכות פעילות שמכניסות כסף** + ניהול-על שרואה הכל + עיצוב ייחודי ומיתוג "עולם הסטארטאפים". נתוני אמת בלבד. **מוגן = לא לכתוב** (אסור לשנות/למחוק/לשבור): bkalut-app, bkalot-admin, zr_*, NEDARIM3873, מערכת 08 ("בקלות זכאות"), 09 ("בקלות ניהול"). חריג מאושר: מותר **לקרוא** את 08/09 קריאה-בלבד לשכפול לפי BKALOT_CLONE_BUILD.md. מצב טסט לשליחה/חיוב עד אישור. אחרי כל צעד — כתוב heartbeat ל-core.run_progress. **בלי לפגוע במה שכבר עובד.**

### ⚑ כלל אנטי-דריפט + סריקה רוחבית על כל המערכות (חדש — קרא לפני הכל, מעל כל הסעיפים)
> עיקרון-על: מביאים כל מערכת ל"עובד + פרוס", עוברים להבאה, ולא נתקעים. סורקים את כל ~38 המערכות לפי הסדר, ורק אחרי סבב מלא חוזרים לליטוש/פיצ'רים.
- פונקציה לפני ליטוש. בכל מערכת: קודם שהכל עובד (כניסה + פעולה מרכזית + נתוני אמת) ונפרס (200) — ורק אז עיצוב/נגישות/ניגודיות. אסור להיתקע על ליטוש (contrast/focus/פיקסלים/ניסוח) לפני שהפונקציונליות והפריסה הושלמו.
- תקרת צעדים למערכת: אל תשקיע יותר מ-~6 צעדים רצופים על אותה מערכת. ברגע שהפעולה המרכזית עובדת ונפרסה — סמן "עבר בסבב הזה" ועבור למערכת הבאה. ליטושים זעירים → POLISH_BACKLOG.md, לא עכשיו.
- הרף למעבר ("עבר בסבב הזה") = מוגדר: כניסה עובדת + פעולה מרכזית עובדת בפועל + נתוני אמת + נפרס ומחזיר 200. זה הרף — לא שלמות מושלמת.
- סבב מלא על כל המערכות: עבור מערכת-מערכת לפי סדר העדיפויות (§0→§8). כל מערכת → לרף → הבאה. אל תחזור ללטש מערכת שכבר עברה עד שסיימת סבב על כולן.
- חסום? דלג מיד. דורש משתמש/מפתח/כלי חיצוני → NEEDS_USER.md ועבור הלאה. מערכת שלמה חסומה → מערכת הבאה. אל תמלא זמן בליטוש.
- אל תעזוב מערכת עם ליבה שבורה (פעולה מרכזית לא עובדת) כדי לקפוץ — סיים את הליבה ואז עבור. (ליבה שבורה מסיימים; ליטוש דוחים.)
- אל תעצור. אחרי כל מערכת → heartbeat + הבאה, ברצף, בלי לחכות לאישור בין מערכות.
- בסוף כל סבב מלא: כתוב ל-NEEDS_USER.md רשימת מטלות מרוכזת — לכל מערכת: מה עובד+פרוס, מה נשאר, ומה דורש אותך (מפתח/החלטה/מכשיר). זו הרשימה שהמשתמש קורא. ואז התחל את הסבב הבא (ליטוש→פיצ'רים) לפי אותו סדר.
- אל תמציא משימות מילוי. אין עבודה פונקציונלית + הכל חסום? → עצור וכתוב סיכום ל-NEEDS_USER.md, אל תלטש.
## 0. דחוף — שילוב "גננות בקליק" (#40) תחת הדומיין (בהקדם, בלי לפגוע באיכות)
- שלב את **גננות בקליק** לתוך המונו-רפו כך שיוגש תחת **more30.com/gannenet**, כמו כל המערכות (בלי נטפרי, בלי railway/vercel חיצוני). פרטים מלאים ב-`core.projects` #40 (note).
- מקור: repo `l023131500-ops/-`, branch `claude/ganenet-full-system-gdrive-fdflfc`, תיקייה `gannenet-app` (Next.js 14). **הוספה בלבד** — אל תיגע במערכות אחרות; אמת שכל הבנייה עוברת.
- **מפתחות מ-core.secrets בלבד** (כבר קיימים: ANTHROPIC_API_KEY, SUPABASE_URL/ANON_KEY). צור bucket `gannenet-shelf`; אחסן ADMIN_PASSWORD.
- **כרגע: אין שיפורי פיצ'רים** — רק להביא לחיים תחת הדומיין. שיפורים אחרי השלמת העבודה הכללית.
- כשמוגש וחי ואומת (בדיקה+צילום מסך) → `public_visible=true` והצג כרטיס בדף הבית.

## 1. כניסת לקוח אמיתית + באגים קריטיים + סיסמאות קבועות (מיידי #1 — לפני הכל אחרי §0)

### 1א. באגים קריטיים לתיקון מיידי (המשתמש חסום)
- 🐞 **כניסה מחזירה לתדמית:** מי שמתחבר/נרשם **חוזר לדף התדמית עם שמו למעלה** במקום להיכנס לתוך המוצר לבצע פעולות. **תקן: אחרי כניסה → הפניה לתוך הממשק הפיזי של המערכת** (לא מסך "תודה"/תדמית). בדוק בכל מערכת עם משתמש בדיקה.
- 🐞 **הרשמה→התחברות "סיסמה שגויה":** נרשמים, ואז התחברות עם אותם פרטים נכשלת ("סיסמה שגויה"). כמעט תמיד **אי-התאמה בין הצפנת הסיסמה בהרשמה לבדיקה בהתחברות (hash/salt)**. תקן שההרשמה וההתחברות משתמשות באותה שיטת hash, ואמת: הרשמה → התנתקות → התחברות עובדת.
- **הצגת סיסמה:** הוסף כפתור "הצג סיסמה" (עין) בכל שדות הסיסמה. אם יש דרישות סיסמה — הצג אותן ליד השדה בזמן ההקלדה.

### 1ב. סיסמאות קבועות וידועות לכל המערכות (חובה — כדי שהמשתמש יוכל לבדוק)
- **סופר-אדמין מאוחד:** Google של l023131500@gmail.com. בנוסף, לכל פאנל עם שם-משתמש/סיסמה — **admin / More30Admin2026** (קבוע, זהה בכולן). שמור ב-core.secrets.
- **משתמש לקוח לבדיקה, עם הרשאת לקוח בכל המערכות:** אימייל **test@more30.com** · סיסמה **More30Test2026** (צור אותו בכל מערכת/מרכזי). שמור ב-core.secrets.
- **קיוסק (#35):** כבר נקבע ב-Railway — admin / More30Admin2026 (SEED_ADMIN_*). ודא שהאדמין נוצר/מתעדכן מ-SEED בכל עלייה (upsert), לא רק כשריק.
- כתוב קובץ **LOGINS.md** עם כל פרטי הכניסה (אדמין + לקוח) לכל מערכת, ועדכן אותו בכל שינוי.

### 1ג. כניסה לתוך המוצר (הבסיס)
- מי שנכנס/נרשם/מתחבר (Google חינם, או מנוי) **נכנס לתוך המוצר עצמו כלקוח מלא** — לא נשאר בתדמית. כל פעולות השימוש + אזור אישי. כפתורי כניסה בעיצוב כל אתר. אמת בכל מערכת עם משתמש בדיקה.

## 2. סופר-אדמין — "כניסה לניהול" (עדיפות #1)
- כשהאדמין (l023131500@gmail.com) מתחבר → כפתור **"כניסה לניהול"** לסופר-אדמין.
- שם רואים את **כל המערכות** + **ניהול מלא ומתקדם של כל מערכת** (בנייה, לקוחות, לידים, תוכן, הגדרות).
- **גישה כללית ללידים** של כל המערכות במקום אחד.

## 3. לוח ניהול-על — more30.com/admin (עדיפות #1)
לכל מערכת: סטטוס (חיה/שבורה/מוסתרת), קישור, Lighthouse; **באגים: תוקן / בטיפול / פתוח**; **מה דורש אותך מול מה שאני עושה לבד** (את הניתן-לבד — בצע בעצמך); הכנסות/מנויים/כניסות; קישור "נהל מערכת זו".

### 3א. יתרות ה-API (חדש — חשוב)
מסך שמראה לכל שירות שאנחנו מחוברים אליו: **האם החיבור פועל**, **כמה יתרה/קרדיט נשאר**, ו**קישור להוספת קרדיט לעמוד הנכון** של אותו ספק. כסה את כל הספקים ב-core.secrets: Apify, ElevenLabs, ימות המשיח, Resend, Google Maps, Gemini, RunPod, נדרים, Supabase, Vercel וכו'. אם ספק לא חושף יתרה ב-API — הצג סטטוס + קישור ישיר לעמוד החיוב שלו.

## 4. דף הבית — סדר ותצוגה (עדיפות #2)
- **מערכות פעילות באמת קודם**, מסודרות בראש. לא-עובדות → **מוסתרות**. חומרים בגיט שאינם אתר → **מוסתרים לגמרי**. עדכן `core.projects.public_visible`.

## 5. בקלות — הצגה בדף הבית + שכפול תפעולי מלא (עדיפות #1 של המשתמש)

### 5א. הצגה בדף הבית (כבר נעשה ברובו)
- הצג את **מימוש זכויות בקלות (#10, bkalot)** ואת **מאגר הזכויות (rights.catalog)** בדף הבית, לא נעול, כולל ניהול מערכת 10.

### 5ב. שכפול תפעולי מלא — עותק עצמאי של המשתמש (חדש, מאושר)
- **המשתמש רוצה עותק מלא ועצמאי של מערכת בקלות התפעולית הפעילה** — האתר שקולט פניות ו**שולח אוטומטית מיילים/מסמכים** — כדי להפעיל, ללמוד את השיטה ולהמשיך לפתח, בלי תלות במפתח החיצוני.
- **בנה לפי `BKALOT_CLONE_BUILD.md`** — שם המפרט המלא: שלוש שכבות (אפליקציה/ניהול/מנוע שליחה) + מאגר זכויות, בסכמה `bkalot_clone` (מנוע השליחה על `bkalot_auto`), פרויקט חדש `bkalot-clone`, נתיבים משלו.
- ✅ **מותר לקרוא קריאה-בלבד את 08/09** כדי לשכפל את השיטה נאמנה. תעד את השיטה ב-`BKALOT_METHOD.md`.
- 🚫 **אסור לשנות/למחוק/לשבור** את 08/09/bkalut-app/bkalot-admin/zr_*/NEDARIM3873. 🚫 אל תשתמש בסכמות `csj`/`csj_src`/`igud` כמקור (ארגונים אחרים). 🚫 מצב טסט לשליחה — שום הודעה אמיתית החוצה בלי אישור.

## 6. עיצוב ייחודי לכל אתר — לא "AI גנרי" (עדיפות #1 לאיכות)
- **תלונה אמיתית: כל האתרים נראים אותו סגנון של AI.** חובה: לכל מערכת **זהות עיצובית ייחודית** — פלטת צבעים, טיפוגרפיה, פריסה ואווירה משלה. ברמה גבוהה, חדשנית, לא-תבניתית. לפי DESIGN_STANDARD + BRAND.md לכל מערכת.
- **קופי**: טקסט מקצועי, אנושי, מדויק — לא "ריח של AI".
- אל תיגע בפונקציונליות שעובדת — רק שדרג עיצוב וקופי.

## 7. מיתוג "עולם הסטארטאפים" (חדש)
- החלף את שם המותג בכל המערכות ל-**"עולם הסטארטאפים"** — **בלי "מור מערכות תוכנה"** ובלי כל וריאציה של זה.
- בכל אתר, **קישור בפוטר: "פותח ע"י עולם הסטארטאפים"** שמוביל לאתר התדמית שלנו.

## 8. מוכן להכנסה — מחירים ומסלול חינמי (עדיפות #2)

### 8א. סולם המחירים שנקבע (2/5/10/12/15 ₪ — לפי הסדר, מהזול ליקר)
המשתמש קבע: **2, 5, 10, 12, 15 ₪ לפי הסדר הנכון**. המיפוי:
- **2 ₪ / חודש** — מסלול בסיס למערכת בודדת קלה (למשל פורטל שיעורים 01/19, תמלול קצר 02).
- **5 ₪ / חודש** — מסלול פלוס למערכת בודדת (פיצ'רים מתקדמים במערכת אחת).
- **10 ₪ / חודש** — **פרימיום כללי (more30/premium)** — כל המערכות הציבוריות. ✅ כבר הוזן ב-core.plans.
- **12 ₪ / חודש** — **פרימיום נדל"ן (nadlan/premium)**. ✅ כבר הוזן.
- **15 ₪ / חודש** — **VIP נדל"ן (nadlan/vip)** — ניתוח אנושי. ✅ כבר הוזן.
- מסלולי ה-2 ₪ וה-5 ₪: צור/הפעל אותם על המערכות הקלות שכבר חיות (billing_kind='subscription', customer_visible=true, period='month'), בסדר עולה נכון. אל תמציא מערכת — רק על מה שחי.
- **הכל במצב טסט בלבד** — mode ב-core.billing_settings נשאר off/test; אין חיוב אמיתי בלי אישור מפורש של המשתמש. price_ils הוא קטלוג/תצוגה בלבד עד אז.

### 8ב. המסלול החינמי — כניסה מיידית לתוך המוצר
- **מסלול חינם = שתי דרכים בלבד:** (א) **התחברות עם Google** (חינם), או (ב) **הרשמה מיידית** בטופס קצר: **שם · טלפון · מייל · בחירת סיסמה**.
- מיד לאחר ההרשמה/ההתחברות → **הלקוח נכנס לתוך המערכת עצמה וממשיך פנימה** (לא נשאר בתדמית, לא מסך "תודה"). ממשיך ישירות אל פעולות המוצר.
- זה מתחבר ל-§1 (כניסת לקוח אמיתית): אותה זרימה, אותו יעד — לתוך המוצר.
- אמת עם משתמש בדיקה: הרשמה מלאה → נחיתה בתוך המוצר → פעולה אמיתית עובדת.

### 8ג. חיבור לזרימת סליקה
- חבר את המחירים (core.plans) לזרימת הסליקה **במצב טסט**. ודא שמערכות שאמורות לעבוד עובדות מקצה לקצה (מחירון #27, קופות #28 וכו').

## 9. הרחבת פיצ'רים (בסוף — לפי more30-feature-expansion.md)
רק **אחרי** שסעיפים 1–8 מסודרים בכל המערכות: קרא את `more30-feature-expansion.md` והוסף את הפיצ'רים המומלצים, מדויקים לכל מערכת. אל תתחיל את זה לפני שהבסיס מסודר.

## 10. מעטפת — פלטפורמת מדריכי חתנים/כלות (שלב אחרון)
רק **אחרי** שסעיפים 1–9 והרחבת הפיצ'רים הושלמו: קרא את `MAATEFET_BUILD.md` ובנה את פלטפורמת "מעטפת" לפי 5 השלבים שבו. אל תתחיל אותה לפני שכל השאר מסודר. אם כבר סיימת הכל — התחל אותה משלב 0.

## כללי
כל שלב: אמת (בדיקה + צילום מסך) → git commit + push → heartbeat ל-core.run_progress → עדכן SYSTEMS_STATUS + NEEDS_USER. את הניתן-לבד עשה לבד. אל תיגע במוגן. נתוני אמת. אל תעצור.

התחל מסעיף 0 (גננות בקליק — דחוף), ואז המשך מסעיף 1.
