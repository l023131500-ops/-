# more30 — מסמך בנייה: כניסת לקוח + סופר-אדמין של בית תוכנה

> מסמך מחייב לטרמינל. מטרה: להפוך את הפלטפורמה למוכנה לשיווק ולהכנסה — כל אתר עם כניסת לקוח אמיתית, וסופר-אדמין שממנו שולטים במחירים, במשתמשים, בתשלומים ובתצוגה. איכות גבוהה, נתוני אמת, בלי לגעת במוגן (bkalut-app, bkalot-admin, zr_*, NEDARIM3873). בנה על ענף נפרד, PR בלי מיזוג בלי אישור.

## 0. עקרונות
- נתוני אמת בלבד; אין נתון → "לא זמין". סודות רק ב-core.secrets.
- כל שלב: אמת (בדיקה + צילומי מסך) → commit + push → עדכן STATUS/SYSTEMS_STATUS/NEEDS_USER.
- בזמן הזה: תשלומים במצב טסט/סימולציה בלבד; אל תבצע חיוב אמיתי על לקוח אמיתי בלי אישור סופי ממני.

## 1. כניסת לקוח בכל אתר (לא רק תדמית)
בכל מערכת בנפרד:
- **כניסה חינם עם Google** — כל משתמש נכנס ומקבל גישת **לקוח מלאה** (לא רק אתר התדמית): רואה את המערכת, מבצע פעולות שימוש, ואזור אישי.
- **גם סיסמה** — אפשרות שהאדמין קובע שם משתמש/סיסמה לכל לקוח בכל אתר (בנוסף ל-Google).
- אחרי התחברות: הפניה אל **המערכת עצמה**, לא חזרה לתדמית. שמירת מצב התחברות.
- מודל הרשאות: לקוח רגיל מוגבל לאתר שנרשם אליו; הסופר-אדמין (l023131500@gmail.com) נכנס לכל אתר ולכל ניהול.
- אימות מלא: התחבר עם משתמש בדיקה בכל אתר, ודא שמגיע למערכת ומבצע פעולה.

## 2. סופר-אדמין — שליטת מחירים (הליבה של "להתחיל להרוויח")
מסך `more30.com/admin/pricing`, סופר-אדמין בלבד:
- לכל **מערכת** ולכל **סוג תשלום**, שדה סכום + כפתור **שמור**:
  - **מנוי** (חוזר חודשי)
  - **סליקה** (תשלום כללי)
  - **שימוש חד-פעמי**
- לחיצה על "שמור" כותבת מיד ל-`core.plans` (עדכון `price_ils` + סוג). שינוי משתקף מיד באתר הלקוח.
- **ברירת מחדל: 0 ₪** לכל מסלול (המערכת פעילה, ללא חיוב) עד שתקבע מחיר.
- **מסלול בדיקה: "פרו" ב-1 ₪** — ליצירת בדיקת סליקה אמיתית קטנה; אתה תשנה בניהול.
- כלל בטיחות (נשמר): אי אפשר לפתוח חלון תשלום על סכום ריק/null — רק על מחיר שנקבע.

## 3. סופר-אדמין — דוח לכל מערכת בנפרד
מסך `more30.com/admin` עם כרטיס לכל מערכת, וכניסה לדוח מלא לכל אחת:
- **סטטוס**: חי/שבור, קישור, Lighthouse, תאריך פריסה אחרון.
- **פיצ'רים**: רשימת היכולות של המערכת ומצב כל אחת.
- **בדיקות/QA**: תוצאות probe, בדיקות עשן, חוויית משתמש, מה תוקן/פתוח.
- **משתמשים**: כמה נכנסו, פעילים (24ש'/7י'/30י') — מ-`auth.users` + `core.app_memberships`.
- **מנויים/הכנסות**: כמה מנויים פעילים, לפי מסלול, הכנסה משוערת.
- **ניהול לקוחות מלא (CRM)**: רשימת לקוחות, סטטוס, מנוי, מסמכים, פעולות (חסימה/שדרוג/איפוס סיסמה).
- כבר קיים בסיס ב-`more30.com/admin/activity` (משתמשים פעילים) — הרחב אותו למבנה הזה.

## 4. סופר-אדמין — כפתור "הצג בעולם הסטארטאפים"
- לכל מערכת: מתג **"הצג באתר עולם הסטארטאפים / הסתר"**. שמור ב-`core.projects` (שדה `show_in_showcase`).
- אתר "עולם הסטארטאפים" (showcase שיווקי) מציג רק את המערכות שסומנו — כדי שתשלוט מה מוצג למשקיעים/לקוחות.

## 5. מה צריך מערכת ניהול של אתר + בית תוכנה (מחקר ליישום)
בנה את הסופר-אדמין כך שיכסה את כל אלה (סטנדרט של פלטפורמת SaaS/בית תוכנה):
- **משתמשים והרשאות**: תפקידים (לקוח / אדמין אתר / סופר-אדמין), חסימה, איפוס סיסמה, איחוד זהות.
- **חיוב ומנויים**: מסלולים, מחירים, מצב מנוי, חידוש, ביטול, היסטוריית תשלומים, חשבוניות (דרך ספק — ראה מסמך CRM).
- **אנליטיקה**: כניסות, משתמשים פעילים, המרה חינם→משלם, נטישה (churn), הכנסה חודשית (MRR).
- **בריאות מערכת**: סטטוס פריסה, שגיאות, Lighthouse, uptime.
- **תוכן ודגלים**: הפעלה/כיבוי פיצ'רים, מצב "בקרוב", תצוגה ב-showcase.
- **אבטחה ותיעוד**: RLS על הכל, יומן פעולות אדמין (audit log), ניהול סודות מרוכז.
- **תמיכה ומסמכים**: פניות לקוח, מסמכים לכל לקוח, שיתופי פעולה (בסיס ה-CRM).

## 6. סדר ביצוע
1. כניסת לקוח (Google + סיסמה) בכל אתר — אמת בכל אחד.
2. מסך שליטת מחירים בסופר-אדמין (שמור → core.plans). ברירת מחדל 0 ₪, מסלול פרו-בדיקה 1 ₪.
3. דוח לכל מערכת בסופר-אדמין (סטטוס/פיצ'רים/בדיקות/משתמשים/מנויים/CRM).
4. מתג "הצג בעולם הסטארטאפים" + אתר showcase.
5. סליקה במצב טסט מחוברת למחירים (בלי חיוב אמיתי בלי אישור).
6. אמת הכל, פרוס (כשמכסת Vercel מאפשרת), עדכן דוחות.

## 7. אימות
לכל מערכת: כניסת לקוח עובדת ומגיעה למערכת; שינוי מחיר בסופר-אדמין משתקף באתר; דוח המערכת מציג נתוני אמת; מתג ה-showcase עובד. צילומי מסך + QA לכל שלב. משימה שדורשת אותי (מפתחות/מחירים סופיים/סליקה אמיתית) → NEEDS_USER.
