# GANNENET_BUILD — מפרט בנייה: "גננות בקליק" כמערכת ניהול גן מלאה (SaaS) — מערכת 40

> ⭐ מטרה: להפוך את `gannenet-app` הקיים (מדף תוכן + מחולל + לוח שנה בסיסי) ל**מערכת ניהול גן מקיפה**, שבה **לכל גננת מנוי/חשבון משלה** והיא מנהלת את כל הגן. בונים **מעל הקיים** — לא שוברים את המדף/המחולל/הלוח.
>
> מקור אמת: המפרט הזה + תיאור הבעלים. נתוני אמת בלבד. אל תיגע במוגן. איכות לפני מהירות.

---

## 0. מה כבר קיים (לבנות מעליו — לא לשכתב)
- **ספריית חומרים (`/library`)** — 258 קבצים ב‑bucket `gannenet-shelf/seed/…` (PDF/תמונות), לפי פרשה/נושא.
- **מחולל שיעורים ב‑AI (`/generator`, `api/ai-generate`)**, **צפייה בשיעור (`/lesson/[id]`)**, **תמחור (`/pricing`)**, **ניוזלטר (`/newsletter`)**.
- **לוח שנה עברי בסיסי (`/calendar`)** — מציג תאריך עברי, פרשת השבוע וחגים (`@hebcal/core`, חישוב מקומי). **עדיין לא מחובר** לחומר המומלץ ולא תפעולי.
- אין אימות/מנויים/נתוני גננת. הכל סטטי/כללי.

---

## 1. החזון החדש (מה להוסיף) — לפי הבעלים
**כל גננת = לקוח מנוי** עם מרחב נתונים פרטי משלה (RLS מלא). היא מקבלת "תיק גן" מלא לכל השנה. הרכיבים:

### א. חשבון ומנוי לגננת
- הרשמה/כניסה דרך הכניסה האחידה של המונו‑רפו; מנוי דרך `core.plans`.
- בידוד מוחלט בין גננות (RLS לפי `teacher_id`).

### ב. ניהול ילדים (Roster)
- רשימת ילדי הגן: שם, פרטים, גיל/קבוצה, הורים/קשר, סטטוס.
- הוספה/עריכה/ארכוב; ייבוא רשימה.

### ג. תיק ילד (per‑child file) — "כל התוכן על כל ילד"
- **תוכנית אישית** לילד.
- **בדיקות/הערכות נדרשות:** מה נבדק ✓, מה לא, ומה נדרש — מעקב סטטוס לכל בדיקה (התפתחותי, שפה, מוטוריקה, חברתי…).
- ציר זמן התפתחותי; מסמכים מצורפים.

### ד. יומן ילד (Journal) — פדגוגיה + ניתוח התנהגות
- רישום תצפיות שוטף לפי כללי הפדגוגיה.
- **ניתוח התנהגות (ABC — Antecedent/Behavior/Consequence)**, דפוסים, יעדים והתקדמות.
- הערות פרטיות לגננת; אפשרות לדוח מסכם.

### ה. לוח שנה עברי שנתי — **עמוד השדרה התפעולי**
- **כל תאריך בשנה**: יום, מטלה, מה לומדים — מסודר לכל השנה.
- **חגים וחופשות** (מ‑`@hebcal/core`) משובצים אוטומטית.
- **קישור לחומר של אותו שבוע**: כל שבוע → פרשה + נושא → **החומר המומלץ מהמדף הקיים** (חיבור ה‑calendar ל‑library — זה ה"בהמשך יחובר" שכבר תוכנן).
- תצוגת שנה/חודש/שבוע/יום; לחיצה על יום פותחת את המטלות והחומר שלו.

### ו. משימות ותזכורות (יזום)
- ניהול משימות לגננת: מטלות יומיות, בדיקות שצריך לבצע, אירועים.
- **תזכורות אוטומטיות** על כל דבר (בדיקה שמגיע מועדה, מטלה, תאריך) — מייל/וואטסאפ/פנימי.

### ז. תוכן יזום מהניהול (Admin → גננות)
- מנהל‑על שולח לכל הגננות **תוכן כללי ומשימות באופן יזום** (עדכוני תכנית, חומרים, הנחיות).
- כל גננת רואה את מה שהניהול דחף + התוכן האישי שלה.

### ח. סדר יום
- סדר יום יומי נגזר מלוח השנה + המטלות; תצוגה נקייה ליום העבודה.

---

## 2. ארכיטקטורה
- **סכמה `gannenet`** ב‑hub (uhnrgujbdxhhmoxcjria); RLS לפי `teacher_id`.
- כניסה אחידה + סופר‑אדמין גלובלי; מנוי דרך `core.plans`; מפתחות מ‑`core.secrets` (SUPABASE_URL/ANON_KEY/ANTHROPIC/APP_BASE_PATH=gannenet).
- מוגש תחת `more30.com/gannenet` (אין תלות בדיפלוי הרֵיילוויי החיצוני הישן).
- החומרים מה‑bucket `gannenet-shelf/seed/<id>.<ext>` (כבר מלא — 258 קבצים).

## 3. מודל נתונים (טיוטה)
- `teachers` (גננת = לקוח), `subscriptions` (מ‑core.plans).
- `children` (roster), `child_plans`, `child_assessments` (סטטוס: נדרש/בוצע/תוצאה), `child_journal` (תצפיות + ABC).
- `calendar_days` (תאריך עברי, יום, חג/חופש), `calendar_tasks`, `curriculum_weeks` (שבוע→פרשה→נושא→חומרים מהמדף).
- `reminders`, `admin_broadcasts`.

## 4. שלבי בנייה (פאזות — קודם להציג, אחר כך להעמיק)
1. **פאזה 1 — הצגה:** לסיים build+deploy של האפליקציה הקיימת תחת `/gannenet` (מדף+מחולל+לוח בסיסי). `public_visible=true` אחרי בדיקה. (זה כמעט מוכן.)
2. **פאזה 2 — חשבונות + ילדים:** מנוי/כניסה לגננת, RLS, ניהול ילדים, תיק ילד בסיסי.
3. **פאזה 3 — לוח שנה תפעולי:** לוח שנתי מלא + חגים/חופשות + מטלות + חיבור החומר השבועי מהמדף + סדר יום.
4. **פאזה 4 — יומן והערכות:** יומן ילד + ניתוח התנהגות + מעקב בדיקות + תזכורות + תוכן יזום מהניהול.

## 5. איכות ובדיקות
- RLS נבדק (גננת א' לא רואה נתוני גננת ב'). בדיקת זרימה מלאה לכל פאזה + צילום מסך.
- הלוח מציג נכון חגים/חופשות ופרשה; קליק על יום מביא את החומר הנכון.
- אל תיגע במוגן. כל שלב: בדיקה → commit+push → heartbeat → עדכן SYSTEMS_STATUS + NEEDS_USER.

## 6. מה דורש אותך (NEEDS_USER)
- רשימת סוגי הבדיקות/הערכות הסטנדרטיות שגננת מנהלת (כדי לבנות תבנית).
- העדפת ערוץ תזכורות (וואטסאפ/מייל/פנימי).
- מבנה שנה"ל/חופשות ספציפי אם שונה מברירת המחדל של הלוח העברי.

## 7. הרחבה לפי מסמך־העל (GAN_CLICK_MASTER_SPEC) — מודולים נוספים ופאזות
> מקור אמת מלא: `GAN_CLICK_MASTER_SPEC.docx`. להלן התוספות מעל §1-§6, לפי מחקר שוק עולמי + דרישות משרד החינוך.

**קבוצות מודולים נוספות לבנות:**
- **תפעול הגן:** נוכחות/צ'ק־אין (QR/קיוסק/PIN, מסך רשימה חי עם תגי אלרגיה), בטיחות + הצהרות בריאות, יומן אירועי בטיחות (body-map), סייעות/צוות.
- **הורים:** תקשורת 1:1+שידור, פיד תמונות, טפסים בחתימה דיגיטלית, RSVP, התראות; אפליקציית הורה חינמית.
- **חומרים ורכש:** BOM לכל פעילות → מחשבון כמויות+תקציב → רשימת קניות שבועית → קישורי רכש/שותפים; ערוצי הכנסה (עמלות/ערכות/דפים).
- **תוכן AI מורחב:** דפי קשר להורים (מודול 3), אשף הכנה לשבוע (4.5), מחשבון חומרים (4.6), קול־להפקה (4.7), מחולל שירים/סיפורי־שמע.
- **ניהול־על ורשתות (B2B):** לוח מפקחת, אנליטיקה, רשימת "דורש טיפול".
- **AI מתקדם (בידול):** ניסוח סיפור־למידה מתמונה, תיוג פנים אוטומטי, הצעת אבני־דרך, המלצת פעילויות לפי פערים.

**מעקב התפתחותי — מסגרות לאמץ:** דומיינים קנוניים (מוטוריקה גסה/עדינה, שפה, קוגניציה, רגשי־חברתי, עצמאות + "מידות וקודש"). מרשם בדיקות לפי גיל בסטטוס נדרש/בוצע/באיחור, מיושר ל-ASQ-3 / מפתח הל"ב / GOLD / CDC. יומן התנהגות ABC → FBA → PBS. IEP/שילוב.

**רגולציה (חובה):** תיקון 13 לחוק הגנת הפרטיות — הצפנה, RLS, הסכמת צילום, מדיניות מחיקה, DPA (בלי אימון AI על תמונות ילדים). תאימות NetFree/רימון. השלמת דיווחי משרד החינוך (נוכחות, מפתח הל"ב, אח"מ).

**פאזות (סדר בנייה):** 1) הצגת הקיים תחת /gannenet + חשבון גננת. 2) ניהול ילדים: Roster + תיק ילד + בריאות/אלרגיה + מרשם בדיקות + נוכחות. 3) לוח שנתי תפעולי מחובר לחומר + אשף הכנה + מחשבון חומרים + דפי קשר. 4) יומן תצפיות + ABC/FBA/PBS + IEP + מעקב דומיינים. 5) AI מתקדם (מתמונה/פנים/אבני־דרך/המלצות/קול). 6) רכש/שותפים + לוח מפקחת + אנליטיקה + הורים + WhatsApp/Drive/סליקה.

## כללי בנייה
נתוני אמת בלבד; אין נתון → "לא זמין". בנה מעל הקיים. אל תיגע במוגן. עמוד בתיקון 13 ובתאימות סינון. איכות לפני מהירות.
