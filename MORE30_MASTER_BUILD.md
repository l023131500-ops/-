# more30 — מסמך-אב ראשי: החזון והבנייה

> זהו מקור-האמת העליון של הפרויקט. הטרמינל פועל לפיו, **מעל** MASTER_PLAN ושאר המפרטים, שלב-אחר-שלב, באיכות הגבוהה והמדויקת ביותר. איכות ודיוק לפני מהירות — תמיד.

---

## 0 · החזון
more30 היא פלטפורמה אחת המאחדת ~33 מערכות תחת דומיין אחד (more30.com/<שם>), במטרה להיות מערכת הסטארטאפים האיכותית והמדויקת בעולם. כל מערכת: נתוני אמת בלבד, עיצוב ברמה עולמית, חווית משתמש מושלמת, כניסה אחידה, וקופי משכנע. מטרת-על: שכל מה שהלקוח צריך נמצא במקום אחד, מדויק, ושלא יחפש בשום מקום אחר.

---

## 1 · עקרונות-על (מעל הכול)
1. נתוני אמת בלבד. אין נתון ← "לא זמין". לעולם לא להמציא.
2. איכות ודיוק לפני מהירות. מותר שייקח זמן. עדיף לעצור שלב מאשר לפגוע באיכות.
3. מקור-אמת אחד לסודות: `core.secrets` ב-hub (uhnrgujbdxhhmoxcjria). מפתח אחד משמש את כל המערכות.
4. כניסה אחידה בכל מערכת (Google SSO + משתמש/סיסמה + כניסת ניהול), עיצוב זהה.
5. ביקורת חווית-משתמש (UX/QA) חזותית לכל מערכת — Playwright + צילומי מסך.
6. git commit + push אוטומטי אחרי כל שלב.
7. אל תיגע במוגן: bkalut-app, bkalot-admin, zr_*, NEDARIM3873.
8. אחרי עצירה (NetFree) — המשך מ-NIGHT_PROGRESS בלי לשאול.
9. כל דבר שדורש את המשתמש ← NEEDS_USER.md, ודלג לשלב הבא שאפשר לבצע לבד.

---

## 2 · הגדרת "מוכן" (Definition of Done) — כל מערכת חייבת לעבור את כל אלה
מערכת נחשבת "פעילה/מוכנה" רק כשכל הסעיפים מתקיימים ומאומתים:
1. **כניסה:** Google SSO (l023131500@gmail.com כמנהל) + משתמש/סיסמה (שם משתמש `eueu1234`, סיסמה `eueu1234`) + כניסת ניהול — עובד בפועל.
2. **פעולה מרכזית ב-UI:** הפעולה המרכזית של המערכת זמינה ונגישה (למשל: העלאת ספר/קובץ, שליחת בקשה, יצירת מודעה, הפקת דו"ח).
3. **נתוני אמת מלאים**, בלי המצאות; "לא זמין" היכן שאין.
4. **UX/QA עבר:** צילומי מסך של כל מסך עיקרי + קובץ `QA/<שם>.md` (מה עובד / מה חסר / מה תוקן).
5. **עיצוב** לפי DESIGN_STANDARD.md: RTL, מובייל, מצב כהה, ביצועים (Lighthouse ≥ 90), SEO, נגישות (תקן ישראלי).
6. **קופי:** טקסט שיווקי ברור ומשכנע בעמוד הנחיתה ובתוך המערכת (עברית תקנית, בלי שגיאות).
7. **הורדות/ייצוא** עובדים (PDF / מצגת / קובץ) היכן שרלוונטי.
8. **דף הבית:** מופיעה נכון — "כניסה" אם פעילה, "בקרוב" + משפט שלב אם לא.
9. **פרוס ונגיש** ב-more30.com/<שם>, מחזיר 200.
10. **גיט:** commit + push בוצע; NIGHT_PROGRESS.md ו-STATUS.md עודכנו.

---

## 3 · מבנה סטנדרטי מומלץ לכל מערכת
עמוד נחיתה (hero + הסבר ערך) · כניסה אחידה · אזור אישי + היסטוריה · פאנל ניהול · הפעולה המרכזית · עזרה/צור קשר · סליקה (נדרים פלוס) היכן שרלוונטי · אנליטיקס בסיסי.

---

## 4 · לוח מצב — STATUS.md (חובה לתחזק)
צור ותחזק קובץ `STATUS.md` עם טבלה:

| מס' | שם מערכת | נתיב | סטטוס (חי/בבנייה/בקרוב) | מה הושלם | מה נשאר | קישור QA |

עדכן אותו אחרי כל מערכת, מתוך core.projects + מצב הבנייה בפועל. **זהו הלוח שהמשתמש בודק כדי לדעת בדיוק מה גמור ומה נותר.** שמור עליו מדויק ומעודכן.

---

## 5 · תהליך העבודה (מנוע)
1. קרא: NIGHT_PROGRESS.md, STATUS.md, NEEDS_USER.md.
2. בחר את המערכת/שלב הבא לפי סדר העדיפויות (סעיף 6).
3. בנה עד להגדרת "מוכן" המלאה (סעיף 2).
4. אמת: נתונים + UX/QA + Playwright + צילומי מסך + כתובות הבדיקה (הדקל 22 חצור הגלילית; הבעל שם טוב 9 רחובות).
5. git commit + push. עדכן NIGHT_PROGRESS + STATUS.
6. חסימה ← NEEDS_USER.md, דלג לבא.
7. חזור. שלב אחד בכל פעם, בלי לפגוע באיכות.

---

## 6 · סדר עדיפויות
א. **סיים מערכות שכמעט פעילות** — כדי שהמשתמש יראה אותן גמורות: השלם לכל אחת את הגדרת "מוכן" המלאה (תמלול 02/17, עורך כתב יד 18, חצור 16, ניהול פיננסי, וכל מערכת עם כניסה חסרה).
ב. **כניסה אחידה + דף הבית** — בכל המערכות (Google SSO + משתמש/סיסמה + ניהול; הצגת כל המערכות עם כניסה/בקרוב).
ג. **נדל"ן 32** — 3 התיקונים + הרחבה (שכירות/מסחרי/קרקעות) + מנוע היתרים/מסמכים + טאבו.
ד. **עיצוב + קופי** — מעבר על כל המערכות לפי הסטנדרט.
ה. **פרסום/מיתוג (26/03)** — סטודיו המודעות.
ו. **סליקה (נדרים פלוס)** — תשתית + חיבור.
ז. **הרחבות והמלצות** — פונקציות מומלצות לכל מערכת (סעיף 8).

---

## 7 · דגשים פר-מערכת (כל מה שסיכמנו)
- **נדל"ן (32):** שמות רחוב מאומתים בלבד (בלי ג'יבריש); רקע שכונה עשיר בסגנון ויקיפדיה; בלי חשיפת מקורות ללקוח (מקורות בלוג פנימי בלבד); עסקאות 3 שנים אחרונות + "הצג שנים קודמות"; דו"ח למייל; הרחבות שכירות/מסחרי/קרקעות; מנוע תב"ע/היתרים (MAVAT/iplan/GovMap/רמ"י); טאבו (רגיל/היסטורי/מרוכז) דרך ספק. מגזרי אוכלוסייה — בלי לחשוף שזה לפי הצבעה, מסומן "הערכה".
- **תמלול (02/17):** כניסה, חיפוש בארכיון, אזור אישי + היסטוריה, תשתית מנוי (בלי סליקה עדיין).
- **עורך/המרת כתב יד (18):** כניסה + "המסמכים שלי" (גמורים/באמצע/נערכו) + **אזור העלאת ספר/קובץ** (הפער שהמשתמש זיהה — חובה) + טיוטות.
- **חצור (16):** תוכן אמת (בתי כנסת/זמנים), פאנל ניהול, Google Maps (מרחקים), הצגה בדף הבית.
- **ניהול פיננסי ומערכות נוספות:** ודא כניסה, הפעלה, והצגה בדף הבית — אף מערכת לא "נעלמת" ולא נשארת בלי כניסה.
- **פרסום/מיתוג (26/03):** 2 מצבים (תבנית + חכם מבריף), עשרות סגנונות, אורקסטרציית AI, טקסט עברית מרונדר בקוד על תמונות, מחלקת מיתוג (לוגו + שפת מותג). מחליף את הצורך בגרפיקאי/מחלקת פרסום.
- **עיצוב:** DESIGN_STANDARD.md על כל המערכות.
- **קופי:** טקסט שיווקי איכותי ומשכנע לכל מערכת.
- **סליקה:** נדרים פלוס (הפרטים כבר בפרויקט).

---

## 8 · המלצות ושיפורים פר-מערכת
לכל מערכת, בקובץ `QA/<שם>.md` הוסף סעיף **"המלצות"**: 3–6 שיפורים/פונקציות מומלצות בעלי ערך אמיתי (חווית משתמש, אמון, המרה, פונקציונליות). יישם את הבטוחים והברורים; שיפורים גדולים/עקרוניים ← רשום ב-NEEDS_USER.md לאישור המשתמש לפני יישום. לעולם אל תפגע באיכות או בדיוק לטובת "עוד פיצ'ר".

---

## 9 · דיווח ושקיפות
אחרי כל מערכת: עדכן STATUS.md + NIGHT_PROGRESS.md. בסוף כל סבב: דו"ח קצר — מה הושלם, מה ב-NEEDS_USER, מה הבא בתור. שמור על שקיפות מלאה: מה אמת, מה חסר, מה תוקן — בלי לייפות.

---

*התחל מ: קרא את הקבצים, צור/עדכן STATUS.md, והצג אותו למשתמש כדי שיראה מה גמור ומה נשאר. ואז המשך לפי סדר העדיפויות.*
